#!/usr/bin/env node
// Stage 6 / Quiz Phase 2 — explfix S116 (2009h21a): key_guard resolution.
//
// merge は FINAL (裁決後) の key_guard を sidecar に publish し、round-1 は key_guard_round1 に
// 併記される (suspect の union は不変 = anti-masking)。本 script は final key_guard に
// **裁決注記**を追記し、是正済みの腐敗 flag を解決状態にする。
//
// Run: node scripts/quiz-phase2-explfix-S116.mjs   (then: merge → verify-result)

import { readFileSync, writeFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const P2 = path.join(ROOT, "data/ip/quiz/.phase2");
const q = (n) => `2009h21a-q${String(n).padStart(3, "0")}`;

const RESOLVES = {
  // ══ key 是正 (ユーザー再確認済み) ══
  [q(91)]: "【S116 裁決】**stored key を イ → ウ に是正した (ユーザー再確認済み)**。key_guard の導出 (ウ=14) が正しい。" +
    "経緯: 本問は中問A の一部で、引当規則「通常注文は在庫数の 70％まで引当可能／優先注文は在庫数まで引当可能」は" +
    "**前文 (源 page-32) にしか無く、その前文が corpus のどこにも存在しなかった**。" +
    "S111 §5 はその状態でユーザーに判断を仰ぎ、表だけから 100−80−10=10 と計算して イ を確認してもらい、" +
    "**元々 ウ だった answer_keys を イ に変更**していた。本 session で D-141 の前文を投入したことで" +
    "70% 規則が corpus に入り、正しい導出 (100×70%=70 引当→在庫30 / 優先10 引当→在庫20 / 20×70%=**14**) が可能になった。" +
    "選択肢構成が裏づけになる: ア7 (在庫を10と誤る) / **イ10 = 100−80−10 = まさに 70% 規則を無視した誤答肢** / ウ14 (正解) / エ20 (最後の70%を忘れる)。" +
    "`quiz-keyfix-S116-2009h21a-q091.mjs` で question_bank / answer_keys / by_year の 3 層を是正し交叉核対 mismatch 0 を確認。" +
    "なお by_year は一貫して ウ のままで、S111 の変更が上流に伝播していなかった (元の抽出値が正しかったことの追加証拠)。" +
    "**教訓: 源の一部が corpus に無い状態で key を裁決してはいけない。**",

  // ══ caveat 掛け直しで捕らえた表示層欠陥 (双 pass 一致) ══
  [q(6)]: "【S116 裁決】選択肢ウ「設罰した」→ 源「設置した」(非語) を是正。併せて イ 末尾のハイフン様記号、ウ 末尾の鉤括弧 2 個、**エ (正解肢) の「運用 。する」に混入した句点**を除去 (fidfix-S116b、双 pass 一致)。key エ 不変。",
  [q(32)]: "【S116 裁決】選択肢エ末尾の長音記号 2 個 (源に無い幻字) を除去 (fidfix-S116b)。key ウ 不変。",
  [q(53)]: "【S116 裁決】選択肢エ末尾の鉤括弧 2 個 (源に無い幻字) を除去 (fidfix-S116b)。key ア 不変。",
  [q(58)]: "【S116 裁決】選択肢ア・イ末尾の鉤括弧 (源に無い幻字) を除去 (fidfix-S116b)。key エ 不変。",
  [q(63)]: "【S116 裁決】**正解肢イ**「デークタ更新」→ 源「データ更新」(タ と ク が入れ替わった非語) を是正し、末尾の幻字も除去 (fidfix-S116b、双 pass 一致)。key イ 不変。",
  [q(69)]: "【S116 裁決】選択肢の線形化を自完結形式に正規化 (fidfix-S116c)。源は 4 行 × 3 列の 1 つの表で ①②③ の見出しを 4 肢が共有しているため、**ア だけが表ヘッダ行ごと抱え、イ/ウ/エ は列見出しの無い断片**になっていた。アプリは選択肢を単独表示するので、zh/en が既に採っていた「① X　② Y　③ Z」形式に JP を揃えた。**SMTP/POP3 の並びは不変**。保真核験は源との語句一致を根拠に CLEAN と判定しており、その判定自体は正しい (保真監査と表示体裁は別ゲート)。key ウ 不変。",
  [q(70)]: "【S116 裁決】選択肢エ「400   」」→「400」。末尾の閉じ鉤括弧 (源に無い幻字) を除去 (fidfix-S116b)。key ウ 不変。",
  [q(78)]: "【S116 裁決】**正解肢ア**「高速性や**硬**故障性」→ 源「**耐**故障性」を是正。**RAID の主目的である耐障害性という語そのものが壊れていた**。併せて イ「小容量の**科**体」→ 源「**筐**体」(ルビ「きょう」付き) と末尾の幻字も是正 (fidfix-S116b、双 pass 一致)。key ア 不変。",
  [q(85)]: "【S116 裁決】選択肢エ「**披**認証者」→ 源「**被**認証者」(肢ウと同語) を是正。併せて **ページ下部のノンブル「− 30 −」が選択肢に流れ込んでいた**分と幻字を除去 (fidfix-S116b、双 pass 一致)。key ウ 不変。",
  [q(96)]: "【S116 裁決】選択肢エ末尾の長音記号 2 個 (源に無い幻字) を除去 (fidfix-S116b)。中問B 前文と共有図 (ワークシート表) は D-141 に基づき埋め込み済で、**本問は表が無いと品目数を数えられなかった**。key ア 不変。",

  // ══ 決定的 junk スキャン由来 ══
  [q(65)]: "【S116 裁決】選択肢ア「4 **パ**イト表記」→ 源「4 **バ**イト表記」、ウ「国**ど**とに」→ 源「国**ご**とに」を page-25 実読で是正し、ア 末尾の空白列とピリオドも除去 (fidfix-S116)。**同一設問の 2 肢で別々の濁点崩れ**が起きていた。**バ/パ は決定的 decoder にとって新しい字形ペア**で、S115 の ポ/ボ に続き濁点/半濁点の取り違えが 3 exam 連続で出ている。key エ 不変。",
  [q(80)]: "【S116 裁決】選択肢エ「文字や図形 静止画像動画像, 音声など」→ 源「文字や図形，静止画像，動画像，音声など」を page-29 実読で是正 (読点 2 個が脱落し 1 個は空白化)。同設問の ア/イ/ウ は読点を保持しており**設問内で表記が割れていた** (fidfix-S116)。key ア 不変。",

  // ══ 中問 (D-141) ══
  [q(89)]: "【S116 裁決】中問A の共有前文 (S社の販売管理業務 / 〔販売管理業務の概要〕(1)〜(3) / 〔販売管理システムの概要〕) が **corpus のどこにも存在しなかった**ため、D-141 に基づき q089〜q092 の 4 問すべてに埋め込んだ (jp/zh/en、Extract≠Verify で PASS)。key ウ 不変。",
  [q(90)]: "【S116 裁決】中問A 前文を D-141 に基づき埋め込み済。引当規則 (通常注文 70%／優先注文 全量) が読めるようになり、引当数の流れ図が判断可能になった。key イ 不変。",
  [q(92)]: "【S116 裁決】中問A 前文を D-141 に基づき埋め込み済。請求締め日 (毎月 20 日) と請求処理の規則が読める。key イ 不変。",
  [q(93)]: "【S116 裁決】中問見出しが「…**問に答えよ**」= 源の「問93 〜 96 に答えよ」の対象範囲を潰した形だった。旧前文と**重複していたワークシート表**ごと剥がし、原典逐字の前文 + 共有図に差し替え済 (D-141)。key イ 不変。",
  [q(94)]: "【S116 裁決】中問B 前文 + **共有図 (商品別販売分析ワークシート)** を D-141 に基づき埋め込み済。本問はバブルチャートの図を持つがワークシートの数値も参照する。key ウ 不変。",
  [q(95)]: "【S116 裁決】分野見出し〔ストラテジ〕の前置を除去し、**重複していたワークシート表**も除去して共有図側に一本化 (設問固有の〔条件〕とランク表は保持)。key ア 不変。",
  [q(97)]: "【S116 裁決】中問C 前文 + **共有図 (図1 データベースの構造 / 図2 請求書の様式)** を D-141 に基づき埋め込み済。key ウ 不変。",
  [q(98)]: "【S116 裁決】「取引先担当者表のレコードを一意に特定する主キー」を問うのに、**図1 の列構成が corpus に無く、本問には図の添付も無かった** (= 解答不能)。共有図のテキスト化を前文に追記して解消 (D-141 + sharedfig-S116)。key ア 不変。",
  [q(99)]: "【S116 裁決】「請求書 1 枚の発行で 4 表を何回参照するか」を問うのに、**図1・図2 の両方が corpus に無く、本問には図の添付も無かった** (= 解答不能)。共有図のテキスト化で解消。key イ 不変。",
  [q(100)]: "【S116 裁決】中問C 前文 + 共有図を D-141 に基づき埋め込み済。正規化の判断に必要な図1 の列構成と図2 の項目が読める。key ウ 不変。",
};

const p = path.join(P2, "generate_result_2009h21a.json");
const gr = JSON.parse(readFileSync(p, "utf-8"));
let n = 0;
for (const r of gr.results ?? []) {
  const note = RESOLVES[r.id];
  if (!note) continue;
  if (!r.key_guard) throw new Error(`${r.id}: no key_guard`);
  if (String(r.key_guard.note_jp ?? "").includes("【S116 裁決】")) continue;
  r.key_guard.note_jp = `${r.key_guard.note_jp ?? ""}\n${note}`.trim();
  r.key_guard.stem_corruption_suspected = false;
  // q091 は key を導出値に合わせたので suspect も解消する
  if (r.id === q(91)) { r.key_guard.matches_key = true; r.key_guard.suspect = false; r.suspect = false; }
  n++;
}
writeFileSync(p, JSON.stringify(gr, null, 2) + "\n");
console.log(`✓ quiz-phase2-explfix-S116: ${n} 問 (RESOLVES 定義 ${Object.keys(RESOLVES).length} 件、未適用 ${Object.keys(RESOLVES).length - n})`);
