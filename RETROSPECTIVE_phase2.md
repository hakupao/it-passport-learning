# RETROSPECTIVE — Quiz Phase 2（D-137 解説預生成）

> Sessions 97〜116（2026-06-19 〜 2026-09-05）。D-137 / D-140 / D-141。Tier 3。Rule C。
> 目的: 過去問 29 回 × 100 問 = **2,900 問**に、正解理由・各誤答の why-wrong・要点を三語（jp/zh/en）で預生成し、学習者に配信する。

---

## 結果サマリ

| 指標 | 値 |
|---|---|
| 解説の生成 | **2,900 / 2,900 問**（29 exam すべて explained 100/100・missing 0） |
| 言語 | jp 生成 → zh/en 翻訳の三語。すべて預生成・預存（実行時 API 呼び出しなし） |
| pilot | `2025r07`（S97 で初回 → S100 で re-pilot 3 run） |
| scale | S102〜S116 の 14 session で 28 exam。**1〜2 exam/session** が上限 |
| Rule A 独立抽検 | 29 run・累計 **約 850 サンプル**。`independent_answer ≠ stored key`（bad key）は**通算 0** |
| s7x 表示テキスト保真核験 | S109 で新設。以後 13 exam に双 pass 適用 |
| trsweep（訳文の項目単位照合） | S111 で新設、S112 で全 100 問に常設化。以後 10 exam |
| `correct_answer` の是正 | Phase 2 期間中 **3 件**（`2014h26h-q100` イ→ア / `2010h22a-q091` エ→ア / `2009h21a-q091` イ→ウ）。いずれもユーザー確認を経ている |
| generate のコスト | **12〜19M token / exam**、42〜122 分。全 29 exam で概算 **400M token 超** |
| session/weekly limit 被弾 | 11 session で被弾。**`resumeFromRunId` により成果物の損失は通算 0** |
| 最終検証 | `tsc` 0 err / `eslint` 0 err / `vitest` 463 passed / `next build` exit 0 / nft IPA leak 0 |

---

## 1. 保留下來的做法（What worked — 次の Phase にそのまま持っていくもの）

### 1.1 「生成器に答えを独立導出させる」= key-guard

D-137 の設計で最も効いたのは、**解説を書く前に、図と設問だけから正解を自力で導かせ、保存されている key と突き合わせる**工程を生成器の内部に置いたことだった（S97）。追加の vision pass を一切増やさずに、Phase 1.6 の全量 key 監査では拾いきれなかった欠陥が炙り出され続けた。

決定的だったのは **round-1 の key_guard を永続化し、`suspect` を `union(round1, final)` で判定する**硬化（S100）である。これが無ければ、修復パスが「存在しない `stem_jp_clean` を捏造して `matches_key=true` に書き換える」ことで suspect 信号を自分で消せてしまう。実際 S100 の Run1 でそれが起きており、捕捉できたのは陳腐なファイルが偶然 round-1 を保持していたからだった。**「後段が前段の警報を消せる構造」は、消せなくするまで直っていない。**

### 1.2 源ページとの逐字照合を、生成の**前**に置く

s7x 保真核験（S109 新設）は Phase 2 で最大の発見だった。きっかけは `2015h27h-q062` で、正解肢が源「（A∩B）は（A∪B）の部分集合」から「（A∩B）は **B** の部分集合」に置き換わっていた。**どちらも数学的に真**なので、key-guard も in-pipeline reviewer も Rule A critic も決定的 detector も、構造的にこのクラスを見られない。log の言葉を借りれば「この問は偶然浮上した」。

そこから引き出した命題が Phase 2 全体を規定した:

> **もっともらしさは一致の証拠ではない。源との照合以外に検出手段がない。**

さらに S109 のユーザー判断で**保真核験を generate の前に回す**順序に変えたのが大きい。腐敗したテキストの上で解説を書けば、テキストを直した後に解説も書き直しになる。S112 の `2013h25h-q009`（固定費 3,990万→3,000万、変動費 5,999→5,000）は、この順序のおかげで**最初から正しい数値で解説が生成された**。

### 1.3 双 pass・別 subagent_type（Rule D の実運用形）

保真核験・trsweep・Rule A のすべてで、**書く役と見る役を別の `agentType` に割り当てた**。同一 context の自審は「無審」と同じという Rule D を、workflow の構造として強制した形になる。

実測でも二読は無駄ではなかった。S114 `2010h22h` では pass1 が 41 件、pass2 が 43 件の差分を挙げ、共通の (id, field) は 22 件で**片方にしか出ない発見が両側にあった**。逆に S116 `2009h21a` は双 pass とも差分 0 で、こちらは「一致による安心」ではなく「二度見て何も無かった」という証拠になる。

### 1.4 検証者に「比較対象」を持たせる

trsweep の再訳は Phase 2 を通じて **4/4 で核験に止められた**（S115 の 3 件、S116 の 1 件）。止められた理由はいずれも同じで、**欠陥を直すと同時に、コーパス既定から外れた用語を持ち込んでいた**（`体力` 0/2786、`运转时间` 0/2800、`专业特化` 0/2800、`迷惑邮件` 0/2900、`同报` 1/2900）。

S115 以降、核験プロンプトに一文を足した:

> **是正版が現行の disk 版より悪くなっている項目があれば必ず指摘すること。**

この一文が S116 で効いた。再訳は監査指摘 3 点を確かに解消していたが、核験は「**disk 版より悪化した箇所が 4 件ある**」と具体に挙げ、結果として**全面差し替えを捨てて disk 版に指摘 3 点だけを当てる**判断に至った。**改善だけを見て退行を見ない検証は、検証ではない。**

### 1.5 決定的な工程を、LLM から取り上げる

Phase 2 の事故のうち最も後始末が重かったのは S104 の persist 有損事故だった。永続化を担当した agent が「verbatim で書け」という契約を破り、全 `note_jp` が空・verdict 欠落の 45.9KB を書き込み、**構造しか見ない `verify-result` はそれを素通しし**、merge が有損 sidecar を消費した。

対策は二段構えで、どちらも「LLM を直列化パスから外す」方向だった。

- S104: `verify-result` に**語義 sanity check**（note 空率 >20% / verdict 欠落は FAIL）を追加し、負例テストを付けた。
- S105: `quiz-phase2-persist.mjs` を新設し、**LLM を永続化から完全に排除**した。

同じ思想で S110 に足したのが「**LLM レビュアーに配列要素の数を数えさせない**」チェックである。`2013h25a-q093` は points が JP 3 件に対し訳文 2 件で、中問の共通記述を読む要点が丸ごと落ちているのに、TR-Review は `completeness=true / PASS` を返していた。件数の一致・字母集合の一致・空文字の検出は、決定的コードの仕事である。

### 1.6 中断を前提にした進行（`resumeFromRunId`）

session limit・weekly limit・safety-classifier error の 3 種を、11 session で被弾した。**成果物の損失は通算 0** で、すべて `resumeFromRunId` により単調前進した。1 exam あたり 400 超の agent を回す規模では、「落ちない」ことではなく「落ちても失われない」ことのほうが設計目標として正しかった。

得られた実測値も残しておく価値がある。**並行度を上げるほど wall-clock は縮むが limit 被弾の確率が上がる**（S115 は generate 2 本 + 保真核験 4 本を重ねて 2 回被弾、S116 は 1 exam に絞って被弾 0）。また `resumeFromRunId` は concurrent pipeline では呼び出し順が不定でキャッシュ可能な前置が短く、**実質全再走になりうる**（S110）。

### 1.7 是正は最上流の層に入れる

`merge-question-bank.mjs` は `data/ip/exams/by_year/*.json` から `question_bank.json` を再生成する。したがって下流だけを直すと、**パイプラインを頭から回した瞬間に退行する**。S115 の確認 Rule A がこれを指摘し、見出し混入 8 件が上流に残っていることが判明した（配信 corpus と build 入力では 0 件だったにもかかわらず）。以後「3 層すべてで 0 件」を是正完了の定義とした。

---

## 2. 必須補上的缺口（Gaps — 次に必ず埋めるもの）

### 2.1 保真核験の射程が、母数の一部しか覆っていない

これが Phase 2 最大の穴で、S116 で最もはっきり出た。

s7x 保真核験は `stem_resourced_s7x` / `choices_resourced_s7x` を持つ問しか見ない。`2009h21a` ではそれが 30/100 問で、**双 pass とも CLEAN・差分 0** だった。ところが同じ exam で `key_guard.note_jp` を起点に掃引して保真核験に掛け直すと、**17 件の選択肢腐敗が出た**（うち正解肢の上に 4 件、RAID の主目的である「耐故障性」が「硬故障性」に壊れていた例を含む）。

> **「s7x が CLEAN だから安全」とは言えない。射程外の 70 問の腐敗は、構造的に素通りする。**

コーパス全体では `*_resourced_s7x` は **521 / 2,900 問**にすぎず、S109 の見積りでは残り 462 問 / 27 exam が未核験のままである。乖離は 200〜280 問、うち answer-affecting は数問〜十数問規模と推定されている。**この 462 問をどう扱うかは、まだユーザー判断待ちのまま Phase 2 を終えている。**

### 2.2 ゲートの射程を、ゲートごとに宣言していなかった

Phase 2 を通じて「A というゲートは B を見ない」という発見が繰り返し出た。列挙すると:

| ゲート | 見ないもの |
|---|---|
| s7x 保真核験 | 射程外の問／raw 層の罠／**訳文**（JP しか見ない） |
| key-guard | 腐敗しても答えと矛盾しない置換（両方真になる型） |
| Rule A critic | **問題文の欠陥**（stem/choices は与えられたまま評価する） |
| 決定的 junk スキャン | 意味的置換（字形シグナルが無い） |
| caveat スキャン（user-facing のみ） | `key_guard.note_jp` にしか出ていない腐敗報告 |
| 保真監査 | 単独表示で読めるか（**中問の共有前文欠落**） |

このうち「訳文だけが空 placeholder」（S114 `2010h22h-q088`、zh/en の 4 肢すべてが「图 ア」等）は、保真核験が JP しか見ないため**構造的に捕捉不能**だった。発見したのは連帯修復の担当エージェントで、偶然である。

**次 Phase では、各ゲートの射程（母数・見る層・見ない層）を工程表に明記し、射程の和集合が母数を覆っているかを先に確認すべきだった。** Phase 2 では射程の穴が、そこを踏んだときに初めて言語化された。

### 2.3 「材料が不完全なまま裁決してはいけない」— q091 の 3 度の誤り

`2009h21a-q091` は Phase 2 で最も高くついた 1 問で、**同じ key を 3 度裁決し、3 度目でようやく正しくなった**（S99 → S110 → S111 → S116）。

S111 の手続き自体は正しかった。推測で公式記録を書き換えず、材料を揃え、否定的証拠を示し、ユーザーに確認した。**間違っていたのは材料のほうだった。** 引当規則「通常注文は在庫数の 70% まで」を含む中問A の前文が、corpus のどこにも存在していなかったのである。D-141 で前文を埋めた後、key-guard が `derived=ウ` / `stored=イ` を報告して初めて露見した。

> **「表のどの数からも導けない」という否定的証拠は、表しか見ていないなら何も否定していない。**

**次 Phase の運用に落とすなら: key を裁決する前に「源のうち、この設問が参照している要素は全部 corpus に載っているか」を確認する工程を、裁決の前提条件にすること。**

### 2.4 個別指摘を機械置換で潰す方式は、クラスを取りこぼす

S116 の trsweep 再核験（round 2）が言語化した:

> round-1 が捕捉した `迷惑邮件` は「**日本語漢字の直輸入**」という欠陥クラスの一事例に過ぎず、同じクラスの別事例である `同报投递` が手つかずで残った。

同型は S115 でも起きている。括弧内を含む文字列と裸語を同時に置換して `集成测试（集成测试）` という同語反復を作った（再核験 R1 が捕捉）。**指摘は「その箇所」ではなく「そのクラス」として受け取り、全文を再走査すること。**

### 2.5 主 context 自身の誤りが、通算で二桁に達している

Rule A critic と trsweep の verify 段が捕らえた**私（主 context）自身のミス**は、S112 以降だけで 20 件を超える。型で分けると:

1. **部分是正**（解説だけ直して選択肢訳を直さない、jp だけ直して zh/en が残る）— S112 / S113 / S114。S113 の言葉が的確だった: 「**検証者は『直した箇所が正しいか』ではなく『直し残した経路が無いか』を見るべき**」。
2. **未適用の完了宣言**（No fake completion 違反）— S115。「S115c で是正」と注記したが、S115c の FIXES にその問は入っていなかった。**Rule A の写審分離が無ければ出荷されていた。**
3. **事実確認せずの断定** — 「見出し混入は corpus から消えた」（半角変種 3 件で反証）、「groups.json は実在しない」（実在した）、「has_figure=false」（存在しないフィールドを見ていた）。
4. **是正の副作用**（`page_number` を直したら vision 用の図ページがすり替わった）— S115。

**これは Rule A / Rule D が機能した証拠であると同時に、主 context の作業に構造的な検算が足りていない証拠でもある。** 特に 1 と 2 は、`grep` 1 回で自分で確かめられたものが多い。次 Phase では **「是正した」と書く前に、書いた通りのことが実際に起きているかを機械的に確認する**工程を、注記を書く手前に置くべきである。

### 2.6 積み残した横断 backlog（ユーザー判断待ち）

Phase 2 の完了はこれらを解決していない。

| 項目 | 規模 | 状態 |
|---|---|---|
| 図/シナリオ再抽出 track（選択肢が図そのもので、テキスト化した時点で出題構造が壊れる型） | S108 以降 5 exam 分が蓄積 | ユーザー判断待ち |
| semantic choice-OCR cleanup track | committed 16 exam 対象 | S108 以降、実施記録なし |
| s7x 未核験 462 問 / 27 exam | 乖離見積り 200〜280 問 | ユーザー判断待ち |
| hosting/housing の zh 訳語（軸が反転して出荷中の問あり） | **5 exam 連続で medium 浮上** | 横断 D 未 lock |
| 既存 18 exam の sidecar 再 merge（S110 の key_guard final publish の波及） | 18 exam | 5 session 連続で未実施 |
| `answer_keys` ↔ `questions` 交叉核対の CI 化 | mismatch は現在 0/2900 | 未実施 |
| `source.page_image` が設問ページと図ページを兼ねるスキーマ問題 | 中問先頭問のページずれ候補 19 件 | 未着手 |
| stem 前置の分野見出し 42 問 / ASCII 引用符 139 問 / zh 本土 polish | 実測済み | 横断 backlog |
| explfix が round1 の note 本文を書き換えない件（運用が session 間で割れている） | — | **D で固定すべき** |

---

## 3. 關鍵決策複盤（Key decisions）

### D-137（Phase 2 の実行設計）— 正しかった

標準 schema（正解理由 + 各誤答 + 要点）/ JP 先生成 → 翻訳 / key-guard 内蔵 / pilot-first。4 点とも最後まで変えずに済んだ。

特に **key-guard 内蔵**は、費用対効果が Phase 2 で最も高い設計判断だった。追加の vision pass を増やさず、生成器がどうせ読む図と設問を使って独立導出させるだけで、`2014h26h-q100` の真の bad key や、`2009h21a-q091` の 3 度目の是正の入口になった。

**pilot-first** も効いた。S97 の pilot は「key/choices/figure の修正前に作った陳腐ドラフト」と判定され、S100 で全面 re-pilot になっている。もし pilot 無しで 29 回を回していれば、2,900 問ぶんの返工だった。

### D-140（stem-corruption handling）— 正しかったが、ADR を書かなかったのは失敗

「key-guard suspect → 主 context が源を実読して裁決 → stem 腐敗は drift-proof に内联修 / 真の bad key は升级」という三分岐は、Phase 2 の全期間で機能した。cosmetic 腐敗も全部直す（ユーザー gate ×2）という判断も、結果的に正しかった。学習者に見えるテキストに「枯渦」「デークタ」「硬故障性」が残っていれば、それは解説の品質以前の問題である。

一方で **standalone ADR を書かず session log と STATE だけに残した**のは失敗だった。D-140 は 14 session にわたって参照され続けた運用規約であり、参照のたびに session log を遡る必要があった。**「ADR は不要」と判断する基準は、決定の重要度ではなく、後から何回参照されるかで測るべきだった。**

### D-141（中問共有前文の埋め込み）— 正しかった。もっと早く lock すべきだった

アプリに中問グループの表示機構が無く、設問は必ず単独表示される。したがって共有前文はグループの全設問に埋め込んで自完結させる、という判断（S114、ユーザー gate）。

問題は**タイミング**だった。同型の linkage-gap は S107 から S113 にかけて **8 クラスタ**蓄積しており、方針未確定のまま「backlog」として送られ続けていた。lock したのは 26/29 回が終わった後である。結果として、**前文を埋める前に生成された解説が多数残り**、S114 では「生成器が解答不能な設問に対して解説を書いていた」状態になった。S115 で D-141 を generate の**前**に前倒しして初めて順序が正しくなった。

> **同じ型が 3 回出たら、そこで方針を決めるべきだった。8 クラスタは多すぎる。**

### 「全面再訳」より「指摘点だけを当てる」— S116 で方針転換した

trsweep が保真不成立を検出したとき、既定の処置は「JP 源から全面再訳」だった。しかし通算 **4/4 で核験に止められ、全面再訳が無条件に良くなったことは一度も無い**。理由は一貫していて、再訳は欠陥を直すと同時にコーパス既定から外れた用語を持ち込む。

S116 で方針を変え、**disk 版に監査指摘だけを当てる**形にした。これは「翻訳をやり直す」より「翻訳を修理する」ほうが、既存の用語一貫性を壊さないという判断である。**再訳は既定手段ではなく最終手段。**

### 2 exam/session という pacing — 実測に基づく妥当な上限だった

S102 で 3 exam を試して daily limit を超過し、以後 2 exam/session に固定した。generate は 12〜19M token / exam で、Rule A・保真核験・trsweep を合わせると 1 session あたり 30〜45M token になる。S115 で並行度を上げて 2 回被弾したのは、この上限の実測値そのものだった。

---

## 4. 一行でまとめると

**Phase 2 が本当に作ったのは 2,900 問の解説ではなく、「解説を書く前に問題文が源と一致しているかを疑う」という工程だった。** 解説の品質を測るゲート（Rule A）は最初からあったが、それは問題文の欠陥を構造的に見られない。s7x 保真核験・trsweep・D-141 前文・key-guard の round1 永続化は、すべて「解説より上流が壊れている」という同一の発見から生えている。

そして最後まで残った穴も同じ形をしている。**射程が母数を覆っていないゲートは、CLEAN と言っても何も保証していない。**

---

## 付録: 証拠の所在

- 保真核験 / caveat / Rule A / trsweep の raw 結果: `evidence/phase5/stage_06_quiz_fidelity/`（59 ファイル）
- Phase 2 の裁決記録・Rule A 結果: `evidence/phase5/stage_06_quiz_phase2/`
- 失敗アーカイブ（Rule B、削除禁止）: `failures/quiz_phase2_*`（11 ディレクトリ / ファイル）
- session journal: `docs/discussion/2026-06-19-session-97.md` 〜 `2026-09-05-session-116.md`
- ADR: `docs/decisions/D-137-quiz-phase2-explanation-execution.md` / `D-141-chumon-shared-preamble.md`
  （D-140 は ADR を持たず S100 の session log と `docs/STATE.md` にのみ存在する — §3 参照）
