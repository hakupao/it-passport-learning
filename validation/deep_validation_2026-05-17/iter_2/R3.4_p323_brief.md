# R3.4 — page_323 Empty Structured + Missing Hex→Decimal Worked Example Brief

## Defect

R2.B validator flagged page_323 as WARN with HIGH severity finding: "entire hex→decimal worked-example diagram lost". The page's structured array is **EMPTY (0 entities)** in the original release — the page was effectively unprocessed.

## Your task

1. **Read the page image**: `data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/raw/pages/page_323.jpg`
2. **Read classified status**: `.../classified/page_323.json` — note the existing label so we understand why Stage 4 produced 0 entities (likely classified as "blank" or similar).
3. **Inspect what's actually on the page**:
   - Identify any lesson/section headings
   - The hex→decimal worked-example diagram (likely showing step-by-step conversion like "1A3F (hex) → 1×16³ + 10×16² + 3×16 + 15 = 6719 (dec)")
   - Any body explanation, term definitions, table content
4. **Produce a full structured array from scratch**:
   - Capture all visible content as appropriate entities (section / term / table / figure as fits)
   - jp surface for each
   - Especially capture the hex→decimal worked example as a `table` or `figure` entity with step-by-step rows
5. **Produce trilingual translations** for all extracted entities.
6. **Write 3 files**:
   - `validation/deep_validation_2026-05-17/iter_2/fixed_structured/page_323.json` (full array, may have multiple entities)
   - `validation/deep_validation_2026-05-17/iter_2/fixed_translated/page_323.json` (same with jp/zh/en)
   - `validation/deep_validation_2026-05-17/iter_2/fixed_output/pages/page_323.json` — Stage 7 wrapper shape; check existing original `.../output/pages/page_323.json` (RUN) for the wrapper schema (page_meta + entities), match that shape.
7. Verify `json.loads` parses all 3.

## Privacy: no textbook title/author.

## Final return

6-line summary: entities added, key zh/en terms, conversion example transcribed, file sizes, uncertainties.
JSON only on disk.
