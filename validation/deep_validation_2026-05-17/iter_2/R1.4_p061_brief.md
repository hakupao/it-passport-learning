# R1.4 — page_061 Waterfall Chart Re-OCR & Re-Translation Brief

## Defect

Phase 1's Mistral OCR garbled the 5-profit waterfall chart on page 61 («5つの利益の関係»). The cascading relationships (売上高 → 売上総利益 → 営業利益 → 経常利益 → 税引前当期純利益 → 当期純利益) and the deductions (売上原価, 販管費, 営業外損益, 特別損益, 法人税等) were rendered as a corrupt flat markdown table.

V1 OCR validation verdict: FAIL (score 0.62, high-severity table_format).

## Your task

1. **Read the page image**: `data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/raw/pages/page_061.jpg`
2. **Read the existing structured JSON**: `data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/structured/page_061.json` — note the entity that represents this chart (likely a `table` entity).
3. **Read the existing translated JSON**: `data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/translated/page_061.json` — note current jp/zh/en.
4. **Produce corrected entity**:
   - Inspect the chart carefully in the image.
   - The chart shows: starting amount (売上高 100) cascading down through profit levels with deductions branching off. Capture the relationship as a structured table (e.g., columns: 段階, 利益, 控除項目, 金額).
   - Provide jp surface for each cell.
   - Provide zh translation for each cell — use natural Chinese financial terminology.
   - Provide en translation for each cell — use standard accounting terminology (Gross Profit, Operating Income, Ordinary Income, Pre-tax Net Income, Net Income; deductions COGS, SG&A, Non-Op Loss, Special Loss, Income Tax).
5. **Produce two output files**:
   - `validation/deep_validation_2026-05-17/iter_2/fixed_structured/page_061.json` — the full corrected structured array (use existing array as base, replace the buggy entity)
   - `validation/deep_validation_2026-05-17/iter_2/fixed_translated/page_061.json` — same shape but with all three jp/zh/en filled
   - `validation/deep_validation_2026-05-17/iter_2/fixed_output/pages/page_061.json` — same as fixed_translated/page_061.json (Stage 7 export shape; the existing output may already be there, overwrite it)
6. Validation post-step: confirm via `json.loads` that all 3 files parse cleanly and have matching entity count + IDs.

## Output JSON entity schema (preserve original structure)

The buggy entity is probably the only `type: table` entity on the page. Keep all other entities (terms, figures, sections, etc.) unchanged. Only replace the buggy waterfall table entity.

For the table entity, use this shape:
```json
{
  "id": "itpassport_r6::table::p061::N",
  "anchor": {"page": 61, "block_id": "page_061_block_N", "section_path": []},
  "type": "table",
  "caption": {"jp": "...", "zh": "...", "en": "..."},
  "rows": [
    [{"jp":"段階","zh":"阶段","en":"Stage"}, {"jp":"利益","zh":"利润","en":"Profit"}, {"jp":"控除項目","zh":"扣除项","en":"Deduction"}, {"jp":"金額","zh":"金额","en":"Amount"}],
    [{"jp":"売上高","zh":"销售额","en":"Net Sales"}, ...]
  ]
}
```

(Match whatever shape the original entity has if different — preserve schema.)

## Privacy rule (mirrored)

Do NOT mention textbook title or author. Generic terms only.

## Final return

After writing all 3 files, return a 6-line summary: entity replaced (id), rows produced, key zh/en terms chosen, files written + sizes, any uncertainties (e.g., illegible portions of the chart).

JSON only on disk. Do not print verdicts inline.
