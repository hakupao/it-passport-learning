# Session 79 — G4 Phase A 実行 (pilot ToC 規劃 → 審査ゲート)

> 日付: 2026-06-02 / フェーズ: Phase 5 Stage 4 Phase A (AI 教科書生成、実施阶段)
> ユーザー指示: 「開始 G4 Phase A」
> 前提: Session 78 で D-128〜132 lock (生成設計確定)。Phase A = pilot 3節点の per-topic 規劃 pass → 統合 ToC → ユーザー審査ゲート。

## 本セッションのタスク (D-128-A 二段式の Phase A)

1. 機械装配: 3 pilot 節点の規劃入力を確定的に生成 (D-132 非 LLM)。
2. 規劃 pass: Workflow + Opus subagents で unit 分割/排列 (D-130) + Rule D 核験。
3. 統合 ToC → ユーザー審査ゲート (D-128-A)。

## 実行ログ

### 1. 機械装配 (`scripts/stage4-assemble-planning-input.mjs`)

- 入力: `knowledge_tree.json` (63 topic) + `question_bank.json` (2,900題 enriched, `syllabus_refs`)。
- 3 pilot 節点を特定 (ID 完全一致): `strategy-02-04` 知的財産権(18語) / `management-11-29` サービスマネジメントシステム(25語) / `technology-16-43` システムの構成(25語)。
- 各 term に題頻を付与: `freq_in_topic` (primary が当該 topic の題で当該 term を含む数) + `freq_global` (全題库で当該 term を含む数)。
- 節点級 badge (D-131-C): 全63節点 primary 数の三分位 → **低頻 < 27.7 ≤ 標準 < 39.7 ≤ 頻出**。strategy(69)/management(77)=頻出、technology(38)=標準。
- 出力: `data/ip/textbook/.planning/input_{topic}.json` + `planning_stats.json`。
- 直配0題 term (即時チェックは節点級 fallback D-131-A): strategy 3 / management 4 / technology 6。

### 2. 規劃 pass + Rule D (`scripts/stage4-phaseA-planning.workflow.mjs`, Workflow `wf_9dacedbb-06f`)

- pipeline: **plan (general-purpose/opus, writer)** → **review-and-repair (code-reviewer/opus, Rule D, 別 subagent_type)**。非 PASS は指摘注入し再規劃 (最大3ラウンド)。
- 全工程 Claude Code (ultracode, model=opus, 外部 API 不使用 D-132)。10 subagent / ~556秒 / 510k tokens。
- 結果: **3/3 PASS**。

| topic | units (sizes) | Rule D | rounds |
|-------|---------------|--------|--------|
| strategy-02-04 | 3 (8/5/5) | PASS | 1 |
| management-11-29 | 5 (5/5/5/5/5) | PASS | 2 (CONCERNS→PASS) |
| technology-16-43 | 4 (6/6/6/7) | PASS | 2 (CONCERNS→PASS) |

- repair が機能: management は u02 が4語(D-115 下限割れ)→需要管理を u01 から移動し5語に是正。technology は仮想化群10語膨張→u03(基本6語)/u04(応用7語)に再分割。

### 3. 独立確定性検算 (Rule A 構造核験)

LLM 核験とは独立に機械照合 (全3 plan): 入力term数=plan term数 (18/25/25 完全一致)、欠落0・捏造0・重複0、unit サイズ全て5〜8、unit_order 網羅、term 文字列完全一致。

### 4. 統合 ToC (`scripts/stage4-merge-toc.mjs`)

- plan + 確定的頻度 + Rule D verdict を結合 → `data/ip/textbook/unit_index.pilot.json` (骨格) + `evidence/phase5/stage_04_toc_pilot/toc_pilot.md` (人間可読、審査用)。
- 計 **3 topic / 12 unit / 68 用語**。
- badge は節点級 (D-131-C)。unit 級は raw `unit_total_freq_in_topic` を併記 (データ裏付け、tier は捏造せず Phase B/全量で確定)。

### 残存 issue (全 low / 非ブロック)

Rule D 指摘は全て頻度根拠の**表記口径** (order_reason で freq_global と freq_in_topic の取り違え等)。骨格 (境界/順序/prerequisites) には影響なし。Phase B で order_reason の metric を freq_in_topic に統一すれば解消。証拠: `evidence/phase5/stage_04_toc_pilot/audit.md`。

## 決定 (本セッション)

- 設計決策の新規 lock なし (D-128〜132 を実行したのみ)。
- 微小実装決定 (session log のみ):
  - **unit_id = `{topic_id}-u{nn}`** (例 `strategy-02-04-u01`)。D-118 の例示 `tech-0701-u01` は実 topic id 体系に合わせ `{topic_id}-uNN` に具体化。
  - pilot ToC は `unit_index.pilot.json` (全量 ToC `unit_index.json` とは別ファイル、完成度を誤認させない)。
  - Rule A 重量級意味抽検は Phase B (正文の改写を伴う) に適用。Phase A は 確定性検算 + Rule D + ユーザーゲートの三層で担保。

## Phase B 前置 (未解決、非ブロック)

- **mmdc/mermaid-cli 未インストール** (PATH/node_modules/package.json いずれにも無し)。図レンダは Phase B。着手時にインストール方式をユーザー確認。

## ToC ゲート結果

**ユーザー承認 (2026-06-02)**: pilot ToC (12 unit) 骨格を承認 → **Phase B へ進む**。修正要望なし。D-128-A ゲート通過。

## What's Next

**Phase B 起動**: D-019 で 3 点をユーザー確認 → 回答:
- **Phase B pilot scope = 全 12 unit** (3 pilot 節点の全 unit を試作)。
- **言語順 = 日語先行 → 承認後翻訳** (二段ゲート: 日語形式を最廉価で固めてから zh/en、D-129-C 踏襲)。
- **図工具 = 今 mmdc 導入 → end-to-end** (@mermaid-js/mermaid-cli、Mermaid→SVG レンダまで pilot で検証、D-131-E)。

locked-by-default (確認不要、既定実行): 四段構造 D-115 / 記憶フック D-116 / 三語平铺・quiz は ID 参照 D-118 / 即時・チャレンジ選題=確定的スクリプト D-131-A/B / badge=節点級 D-131-C / 原裁剪図 source_figures + figure_index D-131-E / Rule D 写審分離 + Rule A 意味抽检。

### Phase B 実行序 (本セッション = 日語ゲートまで)
1. mmdc 導入 + レンダ検証。
2. 確定的装配: 選題 (即時チェック per term + チャレンジ per unit, D-131-A/B) + 原裁剪図索引 (source_figures + figure_index, D-131-E)。
3. Workflow (opus): 12 unit の**日語**正文生成 (概要/用語講解×N/まとめ + Mermaid source)。Rule D 写審分離。
4. 確定的装配: LLM 日語 + quiz fixtures + source_figures → `units/{id}.json` (日語フィールドのみ)。
5. Mermaid→SVG レンダ。
6. Rule A 意味抽检 + Rule D。
7. **日語ゲート**: ユーザー審査 → 承認後に zh/en 翻訳 pass + 吞吐/耗時外挿。

### Phase B 実行結果 (日語ゲート前まで完了)

- **B0 mmdc**: @mermaid-js/mermaid-cli v11.15 + puppeteer Chromium を workspace 根に導入 (allowBuilds: puppeteer)。テストレンダ成功。
- **B1 選題 (`stage4-phaseB-fixtures.mjs`)**: 12 unit に inline(即時) + challenge + source_figures。**Rule A 指摘で稀少度優先分配に修正** (term 碰撞 starvation 解消)。最終 inline=88(fb13) / challenge=60 / source_fig=6。
- **B2 図索引**: figure_index.pilot.json (原裁剪図/group共有図 溯源)。
- **B3 日語生成 (`stage4-phaseB-content-jp.workflow.mjs`, wf_ac7a2e63)**: writer=general-purpose(opus) → reviewer=code-reviewer(opus, Rule D) + repair。**12/12 PASS** (tech-u02 のみ2R)。26 agent / 1.64M tok / ~9.7分。
- **B4 装配 (`stage4-phaseB-assemble-units.mjs`)**: `units/*.json` (日語 + quiz ID参照 + figure + source_figures、lang_status zh/en=pending)。
- **B5 レンダ (`stage4-render-figures.mjs`)**: **19/19 SVG 成功** (`\n`→`<br/>` 日本語多行正常)。失敗0。
- **Rule A 抽检 (`stage4-phaseB-ruleA-audit.workflow.mjs`, wf_ef989791, critic/opus N=15)**: **事実正確 15/15 (100%)**。severity none9/low5/medium1/high0。medium1=サブスク選題碰撞→**修正済**。low5=表現润色 (翻訳前に採否)。
- workflow `args` が JSON 文字列で届く事象を発見、各 workflow で頑健パース追加 (Phase A は default fallback で偶然成立していた)。
- 微小実装決定 (session log): est_minutes=確定的 `round(term*2.5+3)` / 即時選題=稀少度優先分配(2題目は後続pass、fallback は unit term 接触優先) / unit json schema `stage4-unit-v1-jp` (lang_status で三語進捗管理)。

### 日語ゲート (D-128-A 第2ゲート) — **ユーザー承認** (low5 翻訳前適用)
証拠: `evidence/phase5/stage_04_content_jp/{audit.md, sample_jp.md}`。

### 翻訳 pass (zh/en) + 三語完成
- low5 润色適用 (`stage4-phaseB-apply-lowfixes.mjs`, 9置換) → 再装配/再レンダ 19/19。
- **翻訳** (`stage4-phaseB-translate.workflow.mjs`, wf_7bf47316): 日語固定源→zh/en (D-129-C)。translator=general-purpose(opus) → Rule D=code-reviewer(opus)+repair。11 PASS+1 CONCERNS(u04 zh 稼动率 自然さ→修正)。28 agent/1.45M tok/~8.7分。
- **三語マージ** (`stage4-phaseB-merge-translations.mjs`): 12/12 unit `_jp/_zh/_en` 平铺、schema=trilingual、lang_status 三語 generated。
- **翻訳 Rule A** (`stage4-phaseB-ruleA-translation.workflow.mjs`, wf_0de28bdc, critic/opus N=8): 忠実 **8/8 (100%)**、none3/low5。low5=中国本土自然さ→明確4件適用(zh, jp不変)。
- **全量への学び**: translator prompt に「日式借词回避・中国本土標準 IT 用語」指針+例を追加 (系統傾向、full-corpus 反映)。
- 微小実装決定追加: schema `stage4-unit-v1-trilingual` / 翻訳マージは term_jp 整合キー / 三語ゲートを Phase B pilot 完了の最終ゲートに。

### 三語ゲート (Phase B pilot 最終) — **ユーザー承認** (2026-06-02)
- 承認 → 全量 (63 topic) へ、**次 session** で着手。今コミットは選択せず (未コミット状態で session 終了)。
- 最終構造検証: 12 unit / 68語×3言語 = 204 字段组、欠落・空 0、図 19/19 rendered。lang_status 全 generated。
- 全量前 prep 済: translator prompt に「日式借词回避・中国本土標準 IT 用語」指針+例を追加 (`stage4-phaseB-translate.workflow.mjs`)。

## Phase B pilot 完了サマリ (Session 79)

| 項目 | 結果 |
|------|------|
| ToC ゲート | 承認 (12 unit/68語, Rule D 3/3) |
| 日語ゲート | 承認 (Rule D 12/12, Rule A 15/15 正確, low5 润色) |
| 三語ゲート | 承認 (翻訳 Rule D 11+1, Rule A 8/8 忠実, 本土化4) |
| 産出 | `units/*.json`(12,三語) + `figures/*.svg`(19) + figure_index + unit_index.pilot + 全中間 |
| pipeline | 規劃→ToC門→選題→日語→渲染→門→翻訳→三語、端到端打通・3門検証 |

## What's Next (次 session = 全量) — **起動語「开始 G4 全量」**

Session 79 末にユーザーと合意した順序 (pilot 承認済、コスト低い順):
1. **脚本泛化** (LLM不要・機械): `assemble-planning-input.mjs` PILOTS→全63 topic / `merge-toc.mjs`→正式 `unit_index.json` / `fixtures・assemble-units・render・merge-translations` の入力を `.pilot.json`→正式 index。
2. **全量 Phase A 規劃のみ** (`phaseA-planning.workflow`, 63 topic を 2〜3 批) → 全書 `unit_index.json` → **ToC ゲート** (安く構造を先に確定)。
3. **schema 落地体検**: 既存 12 pilot unit を web app 最小阅读視図に接続し JSON/三語/図レンダを UI で確認 (schema 修正は 12 で発見≫250)。大量内容生成の前に。
4. **全量 Phase B**: 日語 分批→ゲート→翻訳 分批→マージ。第一批は保守的に (1 category+1ゲート)。translator 本土指針=適用済・未検証 (第一批 Rule A 確認)。91語級 topic 切分も未圧測。
5. 各段 Rule A/D。完了後 Stage 5 (コード整理) → Stage 6 (Web 統合)。
- pilot 成果 (scripts 11 + evidence + docs) は commit `f3a5cf4` で push 済。data/ は gitignore (最終出力は Release)。

## Pre-close self-check (D-027 第5条)

- D 新規 lock なし (D-128〜132 を実行)。OQ 変更なし。
- 微小実装決定は本ログに記録 (unit_id 形式 / est_minutes / 稀少度優先選題 / schema trilingual / 翻訳マージ整合キー / 三語ゲート)。
- STATE: 最后更新 / 当前阶段 / Stage4行 / 次セッション 全 sync (本 session 内、複数回)。
- 証拠: `evidence/phase5/stage_04_toc_pilot/{toc_pilot.md,audit.md}` + `stage_04_content_jp/{sample_jp.md,sample_trilingual.md,audit.md}`。failures/ は図レンダ全成功のため空 (Rule B 発動なし)。
- invariants 不変 (question_bank/knowledge_tree/answer_keys 未改、textbook は新規生成物)。
- 全 workflow Claude Code 経路・model=opus (D-129/D-132 遵守)。Rule D 全段 writer≠reviewer (general-purpose≠code-reviewer)、Rule A 独立 (critic)。
- 未コミット (ユーザーが commit を選択せず)。push は未実施 (要承認)。

## Pre-close self-check (D-027 第5条)

- D 新規なし。OQ 変更なし。
- STATE: 最后更新 / 当前阶段 / 次セッション を Phase A 完了・ToC ゲート待ちに sync (本 turn)。
- 産出物: unit_index.pilot.json + .planning/* + evidence/stage_04_toc_pilot/{toc_pilot.md,audit.md} + scripts 3本。データ invariants 不変 (textbook は新規生成物、question_bank/knowledge_tree 未改)。
- commit/push は別途 (push は要ユーザー承認)。
