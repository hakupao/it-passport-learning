# Session 99 — 2026-06-21

> Phase 5 / Stage 6 (Web App データ統合) / サブ段階「Quiz 接過去問」/ **Phase 1.6 全量 (D-139-B)**。
> 前段: S98 で Phase 1.5 (stem 源再構成) 全 29 回完了 + D-139 lock + D-139-A 確定 bad key 3 件是正 + D-139-B pilot。
> 本 session ルート = ユーザー路由词「**Phase 1.6 全量**」(S98 で STATE に登録済の手順)。

---

## ゴール
D-139-B: 図題 **467 全数**の key+choices を盲推審計 (stored key 非開示) → bad key を写審分離で裁決 → confirmed のみ是正 → choices 腐敗は backlog → 検証 + commit → Phase 2 へ。

## 実行ログ

### Phase A — 盲推 (deriver=general-purpose opus, figure_png + figure_page_png 両読)
- `prep` 全 29 回 (blind 入力、correct_answer 除外) → `batch FULL` (467 図題、pilot 済 34 を skip-existing で除外 → 433 items)。
- 自包含 run script 生成 (`quiz-keyaudit.run.generated.mjs`、433 items 内嵌。Workflow 沙箱は FS 不可のため `*.run.generated.mjs` 慣例)。
- **run1** `wf_1a4c4ae8-d6f`: 433 中 **383 done** (high 380/med 2/low 1)。**session limit (17:00 JST) 直撃で末尾 50 fail** (2022r04/2023r05/2025r07/2026r08)。result_<id>.json 落盘で部分確定。
- quota 回復 (19:45 JST、17:00 reset 経過) → `batch REM` (skip-existing で残 45) → run script 再生成 → **run2** `wf_0afb93da-238`: **45/45 done** (全 high、1 choices-issue 追加)。
- **合計 467/467 盲推完了**。`compare`: bad-key 候補 **5** / choices_faithful=false **28** / underivable **3**。

### Phase B — 独立検証 (verifier=oh-my-claudecode:critic ×3/候補, blind=key も deriver 答も非開示)
- `quiz-keyaudit-verify.workflow.mjs` 新規 (critic は Write 不可 → StructuredOutput のみで返却)。5 候補 ×3 票 = 15 critic。`wf_491d0f03-4dd` 15/15。
- **写審分離 Rule D**: deriver(general-purpose) ≠ verifier(critic) ≠ 裁決(主 context)。

### 裁決 (主 context, sips crop + 4〜6x 実読。S98 教訓=低解像度初読は誤判)
sharp で per-panel/per-cell 拡大実読 (ImageMagick 不在 → Next 同梱 sharp 利用)。各候補 **5 独立パス** (3 critic + deriver + 主 context):

| id | type | key | derived | critic×3 | 主 context | 裁決 |
|---|---|---|---|---|---|---|
| **2009h21a-q012** | 順序図 | ア | ウ | ウ/ウ/ウ 全unique | ウ | 🔴 **BAD KEY → ウ** |
| **2010h22a-q091** | 網羅性 (出力結果表) | ア | エ | エ/エ/エ 全unique | エ | 🔴 **BAD KEY → エ** |
| 2009h21h-q096 | 積み上げグラフ | ア | イ | イ/ア/イ **全non-unique** | ア figure-true | ✅ key OK (曖昧問) |
| 2012h24h-q091 | テストデータ b/c | ウ | エ | ウ/エ/ウ | ウ | ✅ key OK |
| 2014h26h-q090 | 製造原価グラフ | ウ | ア | イ/ウ/ウ | ウ | ✅ key OK |

**確定 bad key 2 件 (確定的)**:
- `2009h21a-q012`: 図 ①→②→③→④→実行計画策定。④直前は『戦略立案』(ウ) 以外不可 (戦略→実行計画)。CSF抽出(ア)=③。pilot 発見を全量で確証。
- `2010h22a-q091`: 表2 = 地区4×3辺計3×重量3 = **36 列全組合せ穷举** = 網羅(エ)。ア(エラー推測=選択的抽出) は全数表と矛盾。**全量 net-new** (S97 抽様外)。

**deriver 偽陽性 3 件 (key 正、是正回避) = 写審分離の核心価値**:
- `2014h26h-q090` (=D-139 事前「疑い1」): 主 context 高倍率 + critic pixel 計測で図 ア は破線切片 +23px 高 (固定費増=不正解)。**ウ** のみ同截距(2,000)+急傾き直線 (部品J=変動費=傾き増)。deriver の低解像度誤読 (S98 同型)。
- `2012h24h-q091`: **No.4=コースR はテキスト代請求なし**ゆえ「欠席→テキスト代不請求」検証不能。No.2=欠席+コースQ(テキスト代有) のみ可 → c=2 = key ウ 正。
- `2009h21h-q096`: 図実値 10〜14[20代15/30代25/40代75/50代44] (主 context 6x で 30代=25 確認) で **ア・イ 双方 figure-true** (ア:200%<315%、イ:40代 最大率)。critic 3 全 unique=false。図が key ア を矛盾しない=bad key 非該当。曖昧問 (stem_jp_clean 表は別途腐敗) → backlog。

### ユーザーゲート (AskUserQuestion)
- Q1 (2 bad key 是正?) → **「両方修正 (推奨)」**。
- Q2 (choices 28 + underivable 3 backlog?) → **「別開 choices-fidelity track (推奨)」** (本 Phase では key のみ、choices 不動)。

### 是正 (drift-proof, D-139-A 同方式)
- `quiz-keyaudit-fix-S99.mjs` (現値 assert: 両者 ア を確認後変更) → raw bank `question_bank.json` (gitignored) 編集 → `build-quiz-corpus.mjs` 再生成。
- **questions.json diff = correct_answer 2 件のみ (2+/2−)**: `2009h21a-q012` ア→ウ / `2010h22a-q091` ア→エ。stem/choices/quiz_index/translations 不変 (git diff + tracked-file scan で確証)。

### 検証 (全 GREEN)
- tsc 0err / eslint 0err (既存 warning 1=tTerm RetroGlossary) / **vitest 463 passed** (2 skipped、どのテストも当該 key を非ハードコード) / **build exit 0** / **nft IPA leak 0** (quiz route nft.json の files[] に data/ip/exams hits 0、quiz 派生データ [questions/quiz_index/translations/explanations] のみトレース)。

## 成果物
- 新 scripts: `quiz-keyaudit-verify.workflow.mjs` / `quiz-keyaudit-verify.run.generated.mjs` / `quiz-keyaudit.run.generated.mjs` (FULL→REM 再生成) / `quiz-keyaudit-fix-S99.mjs`。
- evidence: `evidence/phase5/stage_06_quiz_keyaudit/` に `rule_a_audit_S99_fullsweep.md` + `full_sweep_compare.txt` + `phaseB_verify_raw.txt` + README 全量節追記。
- questions.json: correct_answer 2 件更新。raw bank (gitignored) 同期。

## backlog (要ユーザー判断、key と別軸)
- **choices_faithful=false 28 件** (値/区切り腐敗・選択肢 swap[2024r06-q057 字母取り違え危険]・別問混入)。→ choices-fidelity track (別 track、ユーザー承認済の defer)。
- **underivable 3 件** (`2010h22a-q094`/`2012h24h-q087`/`2015h27a-q100`、incomplete-source)。→ Phase 2 で追加対応 or 源再OCR。
- 全リスト = `rule_a_audit_S99_fullsweep.md` / `full_sweep_compare.txt`。

## What's Next
1. **Phase 2 (D-137、解析預生成) を再 pilot → scale**: figure-fidelity hardening + **全問 key-guard** (盲推 derived ≠ key を解説生成前にゲート) 込み。S97 で着手済の Phase 2 を D-139 完了状態で再開。
2. **choices-fidelity track** (28 件): 別 track として figure 逐条核験 + drift-proof 是正 (ユーザー起動待ち)。
3. underivable 3 件は Phase 2 で自然遭遇時に判断。

---

## 追記 — choices-fidelity track (同 session、ユーザー「phase2 我下个session再做、phase2 之前的都做完、收尾」)

D-139 は「key **+ choices** 完整性」を Phase 1.6 と定義。key 完了後、ユーザー指示で choices-fidelity track も同 session 完了。

### 対象
全量 sweep の choices_faithful=false **28 件** + underivable **3 件**。

### パイプライン (写審分離 3 パス)
1. **prep** `quiz-choicesfix-prep.mjs` (28+3 blind 入力、deriver_issues は参考のみ)。
2. **Phase A proposer** `quiz-choicesfix.run.generated.mjs` (general-purpose、原典フルページから ア〜エ 独立逐字転写) `wf_e0c2c9eb-f17`: 31/31 → **needs_fix 28 / 偽陽性 0 / answer-affecting 18**。**choices flag は key flag (偽陽性 3/5) と違い全真**。
3. **Phase B critic verify** `quiz-choicesverify.run.generated.mjs` (critic、図から独立再読+key 整合) `wf_4afa2078-f04`: 28/28 = **APPROVE 27 / WITH_EDIT 1 (q087) / DISPUTE 0 / key 破れ 0**。
4. **主 context spot-check** (q057 swap / q099 別問混入 / q096 LIKE): zh/en も corrected_jp 忠実 (Rule A)。

### jp 是正 (drift-proof)
`quiz-choicesfix-apply.mjs` (現値 assert、74 letter) → raw bank → `build-quiz-corpus.mjs`。**questions.json diff = choices 74 letter のみ (74+/74−)、correct_answer 0 変更 (git diff 確証)**。

### zh/en 再同期
key 観察: **数値選択肢の zh/en は Phase 1 vision 翻訳で既に figure 正** (q036 jp OCR "1000"/"2.000" 腐敗だが zh/en は "1,000"/"2,000")。テキスト/表選択肢の zh/en は腐敗 jp を訳していた (q057 zh c:快/d:慢)。`quiz-choicestr.run.generated.mjs` (変更字母のみ corrected_jp から再訳、現訳が正なら踏襲) `wf_3574fed8-4aa` 28/28 → **43 letter 是正・残踏襲** → `quiz-choicestr-merge.mjs` で 13 sidecar 更新 (84+/84−)。

### underivable 3 件
- `2010h22a-q094`: **incomplete-source** (「形式1」書式定義が page-40 に無し) → Phase 2 / 源再OCR backlog。
- `2012h24h-q087`: 解答可 (表4/表5 から a∈{4,5}、deriver 見落とし)。問題なし。
- `2015h27a-q100`: **incomplete-source** (HDD 容量/台数欠落で time 計算不能) → Phase 2 / 源再OCR backlog。

### 検証 (全 GREEN)
tsc 0 / eslint 0err (既存 warning 1) / **vitest 463** / build exit 0 / **nft IPA leak 0** / 構造 (28 問 jp/zh/en 全非空)。

### 教訓
- **choices flag は key flag より信頼度高い** (deriver 偽陽性 0 vs key 3/5): 図描画選択肢の逐字不一致は明確、判断問の答案推論より誤りにくい。
- **zh/en は jp と独立に腐敗しうる (両方向)**: jp OCR 腐敗を zh/en vision 訳が修正済の場合 (数値) と、zh/en が腐敗 jp を訳した場合 (テキスト) の両方が存在 → choices 修正時は三語を個別検証。
- choices 修正は key invariant (figure faithful 化は字母位置を変えない)。

## Pre-close self-check (D-027 §5)
- **D 新規**: なし。D-139-B の執行 (既 lock)。**锁定决策 139 のまま**。
- **OQ**: 新規なし。
- **STATE sync**: 最后更新 / 当前阶段 / 次セッション を S99 (Phase 1.6 全量完了 + 2 bad key 是正 + Phase 2 next) に更新。
- **Rule A/D**: 全量 467 盲推 (deriver) + 5 候補 ×3 独立 critic 検証 + 主 context 高倍率裁決 = 写審分離 3 層 (deriver≠critic≠主 context)。偽陽性 3 件を是正前に捕捉。証拠 `rule_a_audit_S99_fullsweep.md`。
- **Rule B**: 失敗 attempt なし (盲推/提案は read-only、是正は assert 付き 1 発成功)。bad key/choices は ADR + 是正で追跡。
- **invariants**: questions.json は key 段で correct_answer 2 件 + choices 段で choices 74 letter。**correct_answer は choices 段で 0 変更 (key invariant、git diff 確証)**。quiz_index/reader コード 不変。translations は choices 段で 13 sidecar の該当字母のみ。raw bank (gitignored) 同期編集。
- **コミット**: ① key 段 `8d79632` (済) ② choices 段 = questions.json + translations 13 + scripts (choicesfix/verify/tr 一式) + evidence + STATE + session log。push せず (push はユーザー確認ゲート)。
