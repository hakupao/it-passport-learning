# Session 75 — Stage 2.7 成果コミット → Stage 2.7b hi-dpi 二次修復 → Stage 3 ゲート

> 日付: 2026-05-31 / フェーズ: Phase 5 Stage 2.7 収尾 + 2.7b（実施阶段）
> ユーザー指示: 「3 → 2 → 1 の順で実行」（① Stage 2.7 成果を commit / ② hi-dpi 二次修復 / ③ ゲート受容 + Stage 3 開始）
> 決定: **D-125**（hi-dpi/多ページ二次パス方式）

## 本セッションのタスク（3→2→1）

1. **Step 3 (先に実行)**: Session 74 の未コミット成果（Stage 2.7）を commit。
2. **Step 2**: 残存 71 フラグ（62 unresolved + 9 anomaly）を hi-dpi/多ページ double-blind で二次修復。
3. **Step 1 (最後)**: D-122 ゲート条件5 を受容 → Stage 3（知識マッピング）開始。

## 進捗ログ

### Step 3 — Stage 2.7 成果コミット ✅
- commit `30209a3`: STATE + RETROSPECTIVE_stage2.7 + D-123/D-124 + session-74 + evidence 4 + failures 2 + scripts 13。data/ は gitignore のため methodology/evidence のみ（設計通り）。**push せず**（要ユーザー承認）。

### Step 2 — Stage 2.7b hi-dpi/多ページ二次修復 ✅（D-125）

**診断**: 71 題を当前 vs backup で全量分析 → 回帰 2（q099-2014/q086-2015a の choices が空に上書きされていた）、跨ページ多数、figure-choices 多数。

**根因実証**: 2015h27a-q086 の IF 式選択肢は **p38（次ページ）** にあり、stored page_number=37 の page-unit scan が空 choices を生んでいた。**300dpi 整页は無効**（downsample で 173dpi と同等）、**分帯クロップが本質**を再確認。

**パイプライン**（全スクリプト `scripts/stage027b-*.mjs`）:
1. prep: N + N+1 を 300dpi → 3 帯クロップ（127 ページ × 4 = 508 枚）+ blind manifest。
2. double-blind workflow（71×2=142, A=explore / B=code-reviewer, Opus, Rule D, 8 並列）→ 142 reads 成功（6M tok, 14分）。
3. reconcile（bank 規約に前缀/図ブロック剥離 → A↔B 一致かつ stored 否定で confirmed）: **confirmed 20 / figure_inherent 15 / cleared 28 / still_unresolved 8**。
4. apply（`.pre-s027b` backup、不変量厳守）。
5. **Rule A 監査**（N=31, general-purpose lane, +答案字母映射核験）: 23 match / 4 minor / 4 mismatch / answer_consistent=false 1。**適用 20 修復は answer 20/20 整合**。
6. post-audit: 監査が捕捉した真欠陥を双盲裏付けで修正（q025 critical=cross-question choice swap / q090 句読点 garble / q089 顔度→頻度 / q087 一→ーー）、inline-table garble 2（q099/q091-2012h24h）を再フラグ。

**最終**: 残存フラグ **71→10（2.45%→0.34%）**、figure_inherent 15、s027b 修復 stem 14 + choices 14。空 stem/choices/answer = 0。answer_keys/correct_answer/figure/group 不変。証拠 `evidence/phase5/stage_027b_repair.md`。

**教訓**（D-125）: ① NFKC+strip 類似度は句読点/記号に盲目 → 独立 Rule A 逐字監査が機械の盲点を埋める唯一の保険。② figure_inherent は stored が当該問題に関連する検証が必要（swap 偽装）。③ 跨ページは N-1 も要る場合あり（q088-2012）。

## 決定（本セッション locked）

- **D-125 (Locked)**: Stage 2.7b hi-dpi/多ページ二次パス方式。300dpi 分帯クロップ + N/N+1 レンダ + double-blind(explore/code-reviewer) + bank 規約正規化 + figure_inherent 明示分類 + Rule A 逐字監査(答案映射核験)。ADR: `docs/decisions/D-125-stage-2.7b-hidpi-multipage-secondpass.md`。

## ゲート状態（D-122 + D-123、最終）

1. heuristic 速報 ✅
2. 改良 scan 全1208ページ ✅
3. scan 先行ゲート（候補603/20.8%）✅
4. 全候補検証 → 確定521適用（Stage 2.7）✅ + **二次パス 71→10 残存（Stage 2.7b）✅**
5. 再CI ✅ → **二次パスで残存 ≈5%→0.34% flagged（全て answer 保存）**。

## What's Next

1. **Step 1**: ユーザーがゲートを受容 → **Stage 3（知識マッピング）開始**。Stage 2.7b で主毒除去・残 10 は answer 不変のため Stage 3 をブロックしない。
2. 残 10 題（figure-stem 描述差 + inline-table garble 2 + N-1 跨ページ）は任意の per-question 手当て対象（Stage 3 と並行可）。
3. 本セッションの Stage 2.7b 成果を commit（scripts/stage027b-* + evidence/stage_027b_repair + D-125 + session-75 + STATE）。
