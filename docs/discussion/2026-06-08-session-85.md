# Session 85 — Stage 6(Web App 統合)起動: v1 教科書リーダー設計 + D-133 lock

> 日付: 2026-06-08 / フェーズ: Phase 5 Stage 6(設計→実施 移行中)
> 起動: ユーザー「现在可以做 stage 6 吗」。
> 前提: Session 84 で Stage 4 完了(244/244 三語、commit `33412c9`)、ワークツリー clean。

## 0. Stage 6 着手可否(冒頭判定)

前置条件すべて充足 → **着手可**:

| 前置 | 状態 |
|------|------|
| Stage 4 データ | ✅ `units/*.json` 244/244 三語 + `unit_index.json`(887KB) + `figures/*.svg` 409 |
| Stage 5 整理 | ✅ Session 63 提前完了 |
| Schema 落地 | ✅ Session 81 harness(pilot 12)で loader pipeline 実証(`validate.test.ts` 9/9) |
| Git 境界 | ✅ clean、S84 commit 済 |

## 1. 設計 3 問(D-019 slow pace)→ ユーザー回答

| Q | 回答 |
|---|------|
| Q1 範囲 | **教科書リーダー(244 unit 上線)に集中**、Quiz/Glossary/Tutor は後続サブ段階 |
| Q2 生産データ配信 | **gitignore 解除**(理由: 教科書抽出路線は放棄済 D-108、内容は全部自作) |
| Q3 リーダー粒度 | **精簡優先**、まず全量データを通す、体験は後続反復 |

## 2. Q2 の前提検証 → 食い違いを surface(著作権境界)

`data/` 実測: 自作は `textbook/`(25M)のみ。`exams/`(1.2G)・`sources/`(168M)は **IPA 著作物**。remote は **public**。
→ 「全 un-gitignore」は IPA 過去問 1.2G を public repo へ commit する著作権 + 容量リスク。ユーザーに AskUserQuestion で範囲確認 → **「只 textbook/(推奨)」選択**。

## 3. D-133 lock(本 session)

**D-133 — 自作教科書データ(`data/ip/textbook/`)のみ in-repo 化、IPA 由来(exams/sources/syllabus)は gitignored 維持。** ADR: `docs/decisions/D-133-textbook-data-in-repo.md`。
- 却下案: 全 un-gitignore(著作権+肥大) / 現状 `TEXTBOOK_DATA_ROOT` 直読(生産で不成立) / quiz-id manifest 先行(YAGNI)。
- unit の IPA 由来参照(inline_quiz/challenge=question_bank ID、source_figures=exam PNG)は v1 で**繰延**、loader は欠落を null 降級。

## 4. v1 リーダー残サブ決定(ユーザー GO: 「按你的推荐来」)

| 項目 | 決定 |
|------|------|
| 三語表示 | per-locale 単語(next-intl active locale 追従、三語同屏は繰延) |
| ナビ | category→topic→unit 分組目録(D-114 path 順)。双軌導航は繰延 |
| テーマ | 単一クリーン読書面(shell chrome=nav は既存継承、3 テーマ化は繰延) |
| 入口 | NavTabs に「教科書」タブ追加(3 shell 先頭) |
| データ配信 | in-repo `data/ip/textbook/` を server fs 読込 + `outputFileTracingIncludes` で serverless 同梱 |

## 5. 実装(実施阶段)

- **データ層** `lib/textbook/reader.ts`(自己完結・loader.ts 非依存): `loadReaderIndex`/`loadUnit`(React `cache()`)/`buildNav`/`neighbors`/`pick`*/`toDataLang`(**locale `ja`→corpus suffix `jp` ブリッジ**)。`textbookRoot()` は `data/ip/textbook` 直接解決。
- **TDD** `reader.test.ts` **13/13**(buildNav 順序/unit_order 保持/ghost 除外/toDataLang/pick*)。
- **UI**(精簡・新規): `TextbookToc.tsx`(目録)+ `UnitReader.tsx`(概要→用語[定義/解説/たとえ/記憶フック+SVG inline]→まとめ→繰延注記→prev/next)+ `reader.module.css`(self-contained 読書面)。
- **ルート rewrite**: `[locale]/textbook/page.tsx`(目録)+ `[unitId]/page.tsx`(リーダー)。S81 検証 harness の 3 UI(`SchemaReport`/`UnitDetail`/`Trilingual`+css)は**削除**(`loader.ts`/`validate.ts`/`validate.test.ts` は schema 守として温存)。
- **i18n**: 3 catalog に `Nav.textbook` + `Textbook` namespace 追加(構造一致・ICU 引数整合)。
- **Nav**: 3 shell に `textbook` タブ + RetroMenuBar の key cast 更新。
- **.gitignore**(D-133 cascade) + **next.config**(`outputFileTracingRoot`/`Includes`/`Excludes`)。

## 6. Rule D 独立レビュー(写審分離)

- writer=main(general) ≠ reviewer=`oh-my-claudecode:code-reviewer`(opus, 別 session)。**VERDICT: APPROVE**(0 BLOCKER/0 HIGH/1 MEDIUM/4 LOW)。reviewer は独立再検証(tsc/eslint/vitest + nft + 全 409 SVG の XSS スキャン 0)。
- **MEDIUM → OQ-03 登録**(下記)。LOW のうち glob 絞り込み・`cache()` 化を適用、SVG content invariant / loader dead-export は backlog。
- **⚠ レビュー後の独自全軸 nft 再検査で 1.2GB の IPA exams leak を捕捉**(reviewer は textbook 軸のみ集計で見逃し)。真因=`reader.ts` の `data/ip` base → nft 過剰トレース。修正=`textbookRoot` 直接解決 + `outputFileTracingExcludes`。最終 nft: **EXAMS=0/SOURCES=0/SYLLABUS=0**。教訓: 単一 reviewer PASS ≠ 全軸検証(規則 A の精神)。

## 7. 検証(全 GREEN)

- `tsc` clean / `eslint .` 0 error(1 既存 warning 無関係)/ `vitest run` **434 passed**(新 reader 16 含む)/ `next build` exit0。
- runtime smoke(`next start`): `/{ja,zh,en}/textbook` + unit ページ 全 **200**、カテゴリ見出し localized(ja/en)、**per-locale 切替動作**、SVG inline 2、pager 動作。
- screenshots: `toc_ja.png`/`unit_ja.png`/`unit_en.png`。実データスモーク 244 unit 三語非空。
- 証拠: `evidence/phase5/stage_06_reader_v1/`(README + rule_d_review + screenshots)。

## 8. OQ-03 → b-cheap で解消(ユーザー「开干」)

- **OQ-03**(MEDIUM, reviewer 提起): ToC タイトルが全 locale JP。ユーザー判断 = **b-cheap 実施**(タダの部分を今やる)。
- **実測でデータ実態判明**: unit title_zh/en は全 244 に既存(0 欠落)、major/medium 訳語は knowledge_tree に topic_id で 63/63 結合可。**小分類 name のみ訳語ゼロ**(corpus も IPA シラバスも JP のみ)。→ 二択 (a)/(b) は粗く、実は ToC 大半は訳語既存・足りぬは小分類 63 のみと判明。
- **実装**(LLM 不使用・決定的): `scripts/enrich-toc-i18n.mjs` で unit_index.json を enrich(unit→title_zh/en, topic→major/medium_zh/en、追加のみ)。invariant クリーン検証 = 追加キーのみ・順序/既存値保存・fallback 0。`reader.ts` `buildNav(index, locale)` 化 + types 拡張 + test 13→**16**。`TextbookToc` per-locale。
- runtime: zh/en ToC で unit タイトル + グループ見出し母語化、**小分類は JP 固定**(OQ-03 設計確定)、ja 回帰なし。全テスト **434 passed**、nft IPA leak 0 維持、build exit0。screenshot `toc_zh.png`。
- **OQ-03 = CLOSED**(b-cheap で解消、小分類 JP 固定はユーザー合意の意図的設計)。残backlog(任意): 63 小分類名の翻訳(要 LLM+Rule A)。

## 既知の非ブロック事項

- `/`(→/quiz)が 500: `_fixtures/v1.0.3/index.v2.json` 欠落(S63 削除)。**Stage 6 と無関係の既存事項**、後続「Quiz 接過去問」サブ段階の対象。教科書ルートは独立で全 200。
- nft local trace に pilot 2 件残存(gitignored→Vercel 不在、無害)。
- backlog: SVG content invariant のコード強制(生成 pipeline assert or runtime strip)、loader.ts の dead export(`loadAllUnits`/`loadUnitIndex`)。

## What's Next

- **OQ-03 = CLOSED**(b-cheap 実施済、小分類 JP 固定はユーザー合意)。
- 後続サブ段階(ユーザー選択): ① Quiz 接過去問(`_fixtures` 欠落の現行 quiz 是正含む、IPA question_bank の配信を別途決定)② Glossary 接官方用語 ③ Tutor system prompt 更新 ④ 双軌導航(D-114)/3 テーマ化 ⑤ デプロイ smoke(Vercel で tracing 実証)。任意 backlog: 63 小分類名の翻訳(要 LLM+Rule A)。

## Pre-close self-check(D-027 第5条)

- **D 新規**: D-133 lock(ADR + 本ログ + STATE 同回合)。新 D なし(実装は D-133 範囲)。
- **OQ**: **OQ-03 提起 → 同 session で b-cheap 実施 → CLOSED**(ユーザー「开干」合意、小分類 JP 固定が設計)。OQ-01/02 carryover。
- **STATE sync**(本 turn): 锁定决策 133 / Stage 6 行 v1+b-cheap 完了 / OQ 行 OQ-03 closed / 次セッション / 最后更新。
- **Rule A**: 実データスモーク(244 unit 三語非空)+ reviewer の corpus 構造スキャン(NaN0/空 intro0/欠落 SVG0)。ToC enrich は LLM 不使用の決定的データ結合のため Rule A 対象外、代わりに add-only invariant 機械検証(fallback 0)。
- **Rule D**: writer=general ≠ reviewer=code-reviewer(別 subagent_type)。VERDICT APPROVE + 写審分離が見逃した leak を独自検証で補修。
- **Rule B**: failed attempt なし(全 attempt が前進)。
- **invariants**: knowledge_tree/question_bank/answer_keys/unit_index.json(ToC)未改。教科書 unit 内容未改(表示のみ)。
- **未コミット**: code(reader/components/pages/nav/i18n/next.config/.gitignore/`scripts/enrich-toc-i18n.mjs`)+ data(`data/ip/textbook/` 655 件、enrich 済 unit_index.json 含む)+ docs(D-133/session-85/STATE)+ evidence(README/rule_d_review/screenshots ×4)。**commit はユーザー承認待ち**。
