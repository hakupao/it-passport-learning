# Session 117 — 2026-09-06 — 横断 backlog 第一步 (④⑥⑧③)

启动词「backlog 第一步」。ユーザー指示: 「同意，按 ④⑥⑧③ 顺序开跑 backlog 第一步」。
S116 §18 / `RETROSPECTIVE_phase2.md` §2.6 の backlog のうち、方法論を新設せずに片付く 4 項目を先に処理し、
① (s7x 未核験 462 問) の pilot 方案は本 session 末に PLAN 化してユーザー gate に回す。

| # | 項目 | 本 session の到達点 |
|---|---|---|
| ④ | 既存 18 exam の sidecar 再 merge (S110 §3(a) key_guard final publish の波及) | §1 |
| ⑥ | `answer_keys` ↔ `questions` 交叉核対の CI 化 | §2 |
| ⑧ | explfix の note 更新方針を D で固定 | §3 (D-143) |
| ③ | hosting/housing zh 訳語の横断 D + 一括正規化 | §4 (D-142) |

## §0 前提確認

- `git status` clean・`main` = `origin/main` (5c1280e)。STATE.md / S116 §18 / retro §2.6 を読了。
- ① (s7x 462 問) は方法論の選択 (A: s7x 射程拡大 / B: note 起点全量掃引 / C: pilot で A・B 併走) を含むため
  本 session では**実行せず**、§5 に pilot 方案を書いてユーザー gate に回す。

## §1 ④ 既存 18 exam の sidecar 再 merge — 完了

**何を波及させたか**: S110 §3(a) で merge は「sidecar に **final** (裁決後) の key_guard を publish し、round1 が
異なる場合のみ `key_guard.round1` に併記」する形に変わったが、S110 より前に merge 済の 18 exam
(2014h26a, 2015h27a〜2026r08) は round1 の物語をそのまま出荷したままだった (S111 から 6 session 連続で未実施)。

**手順**: `.phase2/` に 29 exam 分の `expl_jp_/expl_tr_` (各 2900) と `generate_result_*.json` (round1 100/100)
が全て残存していることを確認 → 18 exam に `quiz-phase2-merge.mjs` を再実行 → HEAD との diff を**フィールド単位で分類**。

| 結果 | 値 |
|---|---|
| 変化した sidecar | **11 / 18** (2014h26a, 2015h27a, 2015h27h, 2016h28a, 2017h29a, 2017h29h, 2019h31h, 2019r01a, 2021r03, 2023r05, 2025r07) |
| **user-facing テキスト (correct / distractors / points) の変化** | **0** |
| `key_guard.note_jp` (final に置換) | 18 問 |
| `key_guard.figure_derivable` true→false | 3 問 (2015h27a-q094 / 2021r03-q038 / 2023r05-q056。いずれも既に suspect=true、**suspect 不変**) |
| `key_guard.round1` block 新設 | 18 問 = note が置換された 18 問と同一 (90 フィールド。2014h26a 6 問 / 2015h27a・2021r03 各 2 問 / 他 1 問ずつ) |
| top-level count / suspect_count / stem_corruption_count | 不変 |

final の note は round1 より正確になっている例が多い (2014h26a-q085: 中問A の設問範囲「問83〜86」→「問85〜88」に是正、
q043/q053: stem_jp_clean=null を明示、2025r07-q066: ログイン記録表の実読値を明記)。

**副産物**: 7 exam は byte 同一 = round1 と final が全問一致していた回 (2016h28h, 2018h30a, 2018h30h, 2020r02o, 2022r04, 2024r06, 2026r08)。

## §2 ⑥ `answer_keys` ↔ `questions` 交叉核対の CI 化 — 完了、**初回実行で 3 件捕捉**

`scripts/quiz-keys-crosscheck.mjs` を新設 (決定的、LLM なし):

| Layer | 対象 | 実行環境 |
|---|---|---|
| A1〜A4 | questions.json 2900・id 一意・key ∈ 4 肢 / explanations の distractors 字母 == 非正解 3 肢 / translations の 4 肢 / quiz_index の stats・exam 数 | committed data のみ → **vitest から常時** (`quizCorpusInvariants.test.ts`) |
| A5 | D-142 禁止語 (主机托管 / 服务器托管) の zh 残存 | 同上 |
| B1〜B3 | questions ↔ question_bank ↔ answer_keys ↔ by_year の correct_answer 一致 | raw (gitignored) がある環境のみ。無ければ「skipped (raw absent)」を明示して exit 0、`--require-raw` で exit 1 |
| B4 | sidecar の key_guard が generate_result から決定的に導かれる状態か: round1 は「差分がある時に限り publish・全フィールド一致」、final の derived 3 フィールド一致、`stem_corruption_suspected` / `suspect` は union 則 (D-143) | `.phase2` がある環境のみ |

> B4 の初版は「sidecar に round1 が**あれば**比較」だけで、reviewer が変異試験で **round1 削除・final 漂移 (= ④ が是正した状態そのもの) を素通り**することを示した。
> round1Differs を generate_result から再計算する形に書き直し、④ 適用前の HEAD 版 2015h27a を食わせると B4 が 3 件 (round1 欠落 2 + figure_derivable 漂移 1) を検出することを確認。
> 同時に A2 に `suspect_count` / `stem_corruption_count` の実数一致、A5 に教科書 unit (`*_zh` 文字列/配列) と「托管（hosting）」を追加 (両 reviewer 指摘)。

root `package.json` に `check:quiz` (full) / `check:quiz:ci` (committed-only) を追加。
負例プローブ: translations に禁止語を一時注入 → A5 が exit 1 で捕捉 → 原状復帰 (byte 同一) を確認。

**初回実行 (full) が B3 で 3 件を捕捉した**:

| 問 | by_year | 確定 key | 由来 |
|---|---|---|---|
| 2011h23a-q100 | イ | **ウ** | D-139-A (S98、ユーザー承認) |
| 2014h26h-q100 | イ | **ア** | S110 §3 (stem 2,900→2,000 是正に伴う key 裁決) |
| 2015h27h-q100 | エ | **ア** | D-139-A (S98、ユーザー承認) |

question_bank / answer_keys / questions.json は 3 件とも確定値で一致しており、**by_year (最上流) だけが是正前の
抽出値のまま**だった (S98/S110 の是正は by_year を層に含めておらず、含めたのは S115 が最初)。
`scripts/quiz-keyfix-S117-byyear-sync.mjs` で FROM 値を assert しつつ同期 (**新しい key 裁決ではない**、承認済みの値への追従)。
同期後 full 実行 = **A1〜A5, B1〜B4 すべて成立**。

> S116 §18 が「現在 mismatch 0/2900」と書いたのは question_bank ↔ answer_keys の 2 層だけを見た数字で、
> by_year を入れると 3 件あった。**ゲートを常設した初日に穴が見つかる**という、⑥ を先にやる判断の裏づけになった。

## §3 ⑧ explfix の note 更新方針 — **D-143 lock**

`docs/decisions/D-143-explfix-keyguard-note-policy.md`。要旨:

1. explfix が書くのは generate_result の **final** だけ。`key_guard_round1` には触れない。sidecar 直編集もしない。
2. final の `note_jp` は**追記ではなく全文書き換え** (現状を現在形、round1 の捕捉は過去形で含める)。
3. final の boolean/derived は是正後の真値に更新。
4. `suspect = union(round1, final)` は不変 (S110 anti-masking)。web UI は `suspect` を描画していない (実測: `quizModel.ts` の型のみ)。
5. round1 不変性は crosscheck **B4** で決定的に監視。
6. 既存の S114〜S116 式「【S1NN 裁決】」追記 note は遡及しない (次に当該 exam を触る時に書き直す)。
   S107 式で round1 を final と同一に上書きした 2017h29a-q009/q016/q040 は原記録復元不能 (Rule B 記録)。

却下: S107 式の正式化 (anti-masking 破壊) / S114 式の正式化 (note 内矛盾が構造的) / merge による自動整形 / 今すぐ全量遡及。

## §4 ③ hosting/housing zh 訳語 — **D-142 lock + 横断正規化 完了**

`docs/decisions/D-142-zh-hosting-housing-terms.md`。

**実測した現状 (S117)**: 対比設問 12 問 + 教科書 1 unit に **3 系統が併存**、多数派 (2009h21a / 2009h21h / 2010h22h /
2011h23a / 2016h28h / 2023r05 / 教科書) は「ホスティング → 主机托管」で**大陸慣用 (主机托管 = colocation = ハウジング) と反転**。
「主机托管服务」が 2012h24a-q100 では housing、2023r05-q047 では hosting を指す**同語異義**も実在。
外部ソース (華為雲 知識ベース / 百度百科 / 知乎) で 托管=自購硬件を機房に置く、租用=事業者の硬件を借りる、を裏取り。

**固定**: ホスティング → **主机租用**（hosting） / ハウジング → **机房托管**（housing/colocation） / 禁止語 主机托管・服务器托管 /
escrow の 第三方托管 は対象外。

**実装** `scripts/quiz-zhterm-hosting-D142.mjs` (決定的、gloss 付き → bare の順、2012h24a-q100 のみ housing 意):

| 層 | 変更フィールド |
|---|---|
| translations (6 exam) + explanations (8 exam) + 教科書 strategy-06-20-u03 | **37** |
| merge 入力層 `.phase1/tr_` + `.phase2/expl_tr_` (S115 教訓: sidecar だけ直すと再 merge で復活) | **28** |
| merge 入力層 `.phase1/.phase2/input_<exam>.json` + `input_batch_S9x.json` (prep 出力 = generate の文脈。**reviewer 指摘で run3 として追加**: 再 prep せず generate を回すと旧 zh 選択肢がモデルに渡る) | **31** |
| 計 / ID 数 | **96 / 11** (2011h23a-q005 が 27 で最多) |

- 全件 before/after: `evidence/quiz_zhterm_hosting_D142_S117.md` (出荷層 37 は git HEAD 差分から再生成、入力層 run3 31 は原記録。
  **Rule B**: run1 の入力層 28 件の原記録は run2/run3 の上書きで失われた → script を追記モードに変更。詳細は evidence 冒頭)
- 冪等性: 再実行 0 件。禁止語残存チェックは出荷層 + 教科書 + 入力層を対象にし、dry-run でも走る (reviewer 指摘 #3/#4 対応)。bare 服务器托管 の規則も追加 (#1)。**対象 8 exam の explanations を再 merge しても byte 同一** (入力層と sidecar の一致を実証)。
- jp / en / `key_guard.note_jp` 不変。
- 途中の失敗 (Rule B 相当、データ損失なし): 初版は入力層の対象 id を「sidecar の変更有無」から導いていたため、
  sidecar 是正後の再実行で入力層が 0 件になり evidence も上書きされた → sidecar を HEAD に戻し → ④ の 18 exam 再 merge →
  入力層は全ファイル走査で対象を決める形に直して 1 回で適用。
- 範囲外として backlog 継続: 労務派遣 (用工单位/劳务派遣单位)・中問の zh 呼称揺れ (S114 提起)。

## §5 ① s7x 未核験 — 母数の再実測と pilot 方案 (ユーザー gate 待ち、本 session では実行しない)

### §5a 母数は 462/27 ではなく **241 問 / 16 exam**

「462 問 / 27 exam」は S109 時点の見積りで、その後 S110〜S116 が 13 exam に双 pass を掛けたぶんが差し引かれていない。
raw bank の `stem_resourced_s7x | choices_resourced_s7x` を exam ごとに数え直した (S117 実測):

| 区分 | s7x 問数 | exam |
|---|---|---|
| 双 pass 済 (S109〜S116) | **280** | 2009h21a〜2015h27h の 13 exam |
| **未核験** | **241** | 2015h27a 14 / 2016h28a 13 / 2016h28h 21 / 2017h29a 8 / 2017h29h 10 / 2018h30a 13 / 2018h30h 21 / 2019h31h 12 / 2019r01a 25 / 2020r02o 22 / 2021r03 12 / 2022r04 25 / 2023r05 9 / 2024r06 8 / 2025r07 17 / 2026r08 11 |
| 計 | 521 | 29 exam |

つまり **A 案 (s7x 射程のみ) の残作業は 241 問**。Phase 2 の実績 (1 exam あたり双 pass 1 workflow ×2) で 16 exam = 32 workflow。

### §5b 方法論の選択肢

| 案 | 射程 | 期待 | コスト (概算) |
|---|---|---|---|
| **A** s7x 射程を 16 exam に拡大 | 241 問 (s7x のみ) | S109〜S116 の乖離率 (2010h22a 17/34・2010h22h 43/32・2009h21a 0/30) から 50〜120 問の乖離 | 32 workflow |
| **B** note 起点掃引 → 保真核験に掛け直し (S116 §10 方式) を 16 exam 全問 | 1,600 問 (s7x 外の腐敗も見る) | 2009h21a では s7x CLEAN の下で 17 件 (正解肢上 4) が出た。同率なら 16 exam で 200〜300 件 | A の 3〜5 倍 (note 掃引 + 掛け直しの 2 段) |
| **C** pilot で A・B を同じ exam に併走 → 実測で決める | pilot 2 exam | 「B が A より何件多く捕るか / うち正解肢上が何件か」を数字で得る | pilot = A×2 + B×2 |

### §5c 推奨 = **C**、pilot exam の提案

- **2019r01a** (s7x 25 = 未核験群で最多、古め) と **2024r06** (s7x 8 = 最少、新しい) の 2 exam。
  s7x 母数の大小が腐敗の多さと比例しないこと (S116 §3 の反例) を pilot 自身で確かめられる組。
- 各 exam に A (双 pass、別 subagent_type) と B (note 起点掃引 → 保真核験) を独立に走らせ、
  **B∖A の件数と、そのうち正解肢上の件数**を主指標にする。
- 判定規則 (事前に固定): B∖A の正解肢上が 2 exam 合計 **0〜1 件なら残 14 exam は A のみ**、**2 件以上なら B を全 exam に**。
- 是正は Phase 2 常設 8 手順に従う (fidfix は raw + tr + expl_tr の 3 層、caveat 除去は merge 入力層、再 merge、crosscheck full)。
- ゲート射程は工程表に明記する (retro §2.2 の教訓: A は「s7x 問の JP 表示テキスト」だけ、B は「note が指した問の JP 表示テキスト」だけを見る。**どちらも zh/en 訳文は見ない** → 訳文は既存 trsweep が射程)。

**ユーザーに決めてもらう点**: (1) C で行くか、(2) pilot exam の 2 つ、(3) 判定規則の閾値 (上記 2 件)。

## §6 Rule D 独立審査 (writer = 主 context / reviewer = 別 subagent_type ×2、read-only)

| reviewer | 対象 | 判定 | 主な指摘 → 対応 |
|---|---|---|---|
| `oh-my-claudecode:code-reviewer` | ③ D-142 の 65 フィールド全件 × JP 原文 | **PASS** (誤訳・自己矛盾 0、重点 4 件 [2012h24a-q100 housing / 2023r05-q047・2019h31h-q052 hosting / 2011h23a-q005 の a-b-c 対応 20 件] 全て正) | (1) `.phase1/.phase2/input_<exam>.json` に旧語残存 → prep 未実行で generate を回すと旧語が文脈に入る → **run3 で 31 フィールド是正** (2) dry-run が残存チェックを飛ばす・残存チェックが教科書/入力層を見ない → 修正 (3) bare 服务器托管 の規則欠落 → 追加 (4) 文体 nit 3 件 (「主机租用（租用服务器）」の同語反復) → 不採用 (S89 方針: low の任意提案は採らない) (5) `ruleA_samples_*` / `sidecar_batch_S9x` に旧語が残るのは凍結監査記録 → **意図的に不変** |
| `pr-review-toolkit:code-reviewer` | ④ 再 merge / ⑥ crosscheck / ⑧③ ADR | **A PASS / B PASS-with-notes / C PASS-with-notes**。A: 独立 deep-diff で主張を全項目再現、merge ロジックを複製して **29/29 exam byte 同一**、suspect の union 再計算 2900/2900 一致。3 件の figure_derivable flip は退行ではなく「round1 無しで suspect=true」という**導出不能状態の是正** | B: **B4 が round1 削除と final 漂移を素通り** (変異試験 11/13 検出、2 件不検出) → B4 を generate_result から round1Differs 再計算 + final 一致 + union 則の双方向検査に書き直し、HEAD 版 2015h27a で 3 件検出を確認。A2 に count 一致、A5 に教科書 + 「托管（hosting）」追加。test の `stderr==""` 脆弱 → exit code 判定に変更。C: D-143 の「18 exam 分」は実測 **9 exam・158 問** → 訂正。「決定的に守る」の過大表現 → B4 双方向化で実装を文言に合わせた。note_jp も B4 で比較すべき → 採用 |

両 reviewer とも: 業務判定を覆す欠陥なし。`data/` 配下に zh でも key_guard でもない内容変更は 0。

## §7 検証

| 項目 | 結果 |
|---|---|
| `tsc --noEmit` | 0 err |
| `eslint` | 0 err (warning 1 = 既存 `RetroGlossary.tsx` の未使用変数、本 session 無関係) |
| `vitest run` | **464 passed** (463 + 新規 `quizCorpusInvariants.test.ts`) / 2 skipped (既存) |
| `next build` | exit 0、27 static pages |
| `node scripts/quiz-keys-crosscheck.mjs` (full) | A1〜A5, B1〜B4 全成立 |
| questions.json | 2900 問不変・`correct_answer` 変更 0 (by_year 3 件は承認済み値への追従) |
| jp / en フィールド | 不変 (両 reviewer 実測) |

Commit gate: 本 session はユーザーが同席していない自律実行のため、**commit のみ・push はしない** (CLAUDE.md「push は明示承認」)。

## §8 What's Next

1. **① s7x 未核験 = 241 問 / 16 exam** (§5 で母数を再実測)。**ユーザー判断 3 点**: C 案 (pilot 併走) で行くか / pilot exam (提案 2019r01a + 2024r06) / 判定閾値 (B∖A の正解肢上 2 件以上で B 全展開)。
2. ② 図/シナリオ再抽出 track (5 exam 分蓄積)。
3. ⑤ semantic choice-OCR cleanup track (16 exam)。
4. ⑦ `source.page_image` スキーマ / 中問先頭問のページずれ候補 19 件。
5. ⑨ cosmetic 横断 (分野見出し 42 / ASCII 引用符 139 / zh polish / 図クロップ端切れ)。
6. D-142 の範囲外に残した zh 訳語: 労務派遣 (用工单位/劳务派遣单位)・中問の呼称揺れ (S114 提起)。
7. D-143 §6 の遡及: `【SNN 裁決】` 追記 note 9 exam・158 問は当該 exam を次に触る時に全文書き換え。
8. crosscheck の運用: `pnpm test` (vitest) が committed 層を常時検査。raw 層 (B) は `node scripts/quiz-keys-crosscheck.mjs` を **commit 前に手で 1 回** (常設 8 手順の末尾に追加)。
9. push はユーザー gate。

## Pre-close self-check (D-027 第 5 条)

- D-142 / D-143 = ADR on disk (`docs/decisions/`)、本 log に要旨、STATE.md に反映。
- OQ: 新規なし・close なし。
- STATE.md: 最后更新 / 当前阶段 (backlog ③④⑥⑧ 消込、① 母数更新) / 次セッション を書き換え済。
