# Session 80 — G4 全量起動：脚本泛化 + 全量 Phase A 規劃 → ToC ゲート

> 日付: 2026-06-02 / フェーズ: Phase 5 Stage 4 全量 (実施阶段)
> ユーザー起動語: 「开始 G4 全量」(Session 79 末で合意した順序の発火語)
> 前提: Session 79 で pilot (3節点/12 unit/68語×3言語) が三道ゲート (ToC/日語/三語) 全通過、commit `f3a5cf4`。

## 本セッションのスコープ (ユーザー確認済 2026-06-02)

合意した全量順序 (Session 79 末) のうち **step 1 + step 2** を実行し、**ToC ゲートで停止**。

- **Phase A 批次戦略** = 「先验证批 → 再全量」(ユーザー選択)。91/88語級の大 topic 切分が「未圧測」(Session 79 flag) のため、極端サイズを含む小さな検証批を先に流し Rule D + 構造復検を人間が確認してから残りを開放。
- **本場終点** = ToC ゲート (構造層を安く先に固める)。step3 (schema 体検) / step4 (全量 Phase B) は次 session 以降。

### 全量規模 (knowledge_tree 実測)

- 63 topic / 1,417 term / 推定 ~218 unit (D-128 の ~180〜240 と整合)。
- category: strategy 24 topic/575語、technology 31/720、management 8/122。
- 大規模 (≥51語, 5件, **未圧測**): strategy-01-02(91)、technology-23-63(88)、technology-23-61(77)、strategy-01-01(70)、strategy-05-14(57)。
- 小規模 (≤5語, 11件): 3〜4語 topic は「5〜8/unit」を満たせず単一小 unit になる (境界ケース)。

## 実行ログ

### 0. 設計確認 (script 泛化前)

- **review 永続化の缺口を発見**: pilot では `review_{id}.json` を書く script が存在せず、Session 79 は workflow 返回値から手動落盘していた。`code-reviewer` agent はツール集に **Write を含まない** (read-only, Rule D 設計) ため reviewer 自身は書けない。→ review データは **workflow 返回値経由でしか永続化できない**。泛化で `phaseA-persist` を新設して解決。
- **3 pilot plan は再規劃せず複用**: pilot の plan を再生成すると unit 境界が変わり、既生成の 12 三語 unit (Phase B pilot 成果) と乖離するリスク。よって全量 Phase A は **60 非pilot topic のみ規劃**し、merge-toc で 3 pilot (既存 plan_/review_) + 60 新規を統合。

### 1. 脚本泛化 (step 1, 機械, 非 LLM)

| script | 変更 | テスト |
|--------|------|--------|
| `stage4-assemble-planning-input.mjs` | `PILOTS` 定数撤去 → CLI args 駆動 (空=全63 topic、id 検証付)。`allTopicIds` をシラバス自然順で構築。stats.generated_for=TARGETS。badge 閾値は元から全63分位 (不変)。 | ✅ 全63実行 0.14s → input_*.json 63件 + planning_stats.json |
| `stage4-phaseA-planning.workflow.mjs` | ① 返回値を lean 化: `{summary, reviews[]}` (reviews=review_{id}.json形、heavy history 除外、plan は planner が既にディスク書込のため返さない)。② planner/reviewer prompt に**境界ケース指針**追加 (topic<5語=単一unit正当 / 大規模>40語=同規則で多数unit, 端数unit可)。③ args 頑健パース (`typeof args==='string'?JSON.parse:args`、Session 79 で Phase B に入れた対策を Phase A にも。未硬化だと文字列 args で静默 pilot fallback の危険)。 | 構文OK (top-level return の node --check 誤報は想定内)。実行=検証批で確認 |
| `stage4-phaseA-persist.mjs` (**新規**) | workflow 返回 reviews[] を per-topic `review_{id}.json` (pilot 同形) に分割落盘。**理由**: reviewer=`code-reviewer` は Write 非搭載 (read-only, Rule D 設計) で自己永続化不可。配列/`{reviews:[]}` 両受理、final_review.verdict 欠落で throw。 | 構文OK。実行=検証批 return 後 |
| `stage4-merge-toc.mjs` | ① PILOTS→plan_ 存在する全 topic (シラバス順)。② 出力を正式 `unit_index.json` (schema `stage4-phaseA-toc-v1`, scope=full) + `evidence/.../stage_04_toc/{toc.md, structural_audit.json}`。③ **確定性結構復検を内化** (Session 79 §3 の手検をゲート化): HARD=term 過不足/捏造/重複/unit_order 網羅漏れ→`process.exit(2)`、SOFT=サイズ5〜8逸脱→note。④ category 別 stats + md 見出し。⑤ plan 欠落 topic を gap 報告。 | ✅ pilot 3 で smoke: HARD失敗0/サイズ逸脱0/RuleD PASS=3/exit0 |

- **微小実装決定** (session log): Phase A workflow 返回=lean reviews (plan はディスク経由+merge-toc 構造復検で担保) / 境界 prompt 指針 / persist 経由 review 永続化 / merge-toc HARD gate (term 整合性) は process.exit(2)。
- **3 pilot 複用方針**: 全量 Phase A は 60 非pilot のみ規劃。pilot plan_/review_ は既存を温存し merge-toc で統合 (再規劃すると 12 三語 unit が乖離)。

### 2. 検証批 (step 2a) — 5 topic, サイズ極端カバー

Workflow `wf_95479e6c-8ad` (背景), args=`[strategy-01-02(91), technology-23-63(88), technology-23-62(43), technology-17-48(3), strategy-07-24(4)]`。
目的: 91/88語級の大節点切分 (未圧測) + 3/4語の亜下限境界 + 新 review 永続化経路 を Rule D + 構造復検で人間確認 → OK なら残り 55 topic 開放。

#### 検証批 結果 (18 agent / 1.17M tok / ~27分)

| topic | 語数 | units | Rule D | rounds | 検証点 |
|-------|------|-------|--------|--------|--------|
| strategy-01-02 | 91 | 15 | **CONCERNS** | 3 | 最大節点。term 91/91完璧、唯一 u08=9語(上限+1) を3R で解消し切れず CONCERNS。構造完整性は perfect |
| technology-23-63 | 88 | 13 | PASS | 1 | **88語→13unit を1Rで PASS**。大節点切分の圧測クリア |
| technology-23-62 | 43 | 7 | PASS | 3 | 中大型。CONCERNS→CONCERNS→PASS の repair 機能 |
| technology-17-48 | 3 | 1 | PASS | 1 | 亜下限。reviewer が「3語が D-115 5語未満例外条項に該当」と明示 → **境界 prompt 正確生効** |
| strategy-07-24 | 4 | 1 | PASS | 1 | 亜下限。単一 unit PASS |

- 全 5 topic で **all_terms_covered=true**(過不足0/重複0/捏造0)。
- persist (新 script) → review_*.json 5件落盘成功。merge-toc 8 topic(pilot3+検証5): **HARD失敗=0** / サイズ逸脱11(全て Rule D が rationale 付で判定済 SOFT) / RuleD CONCERNS1+PASS7 / exit0。
- **端到端打通**: assemble→workflow→persist→merge-toc(構造 HARD gate)→unit_index.json/toc.md/structural_audit。

#### 検証批ゲート判定 = **PASS** (approach 健全、全サイズ谱系で成立)

- CONCERNS は許容: term 完整性 = HARD bar はクリア、CONCERNS は valid terminal state (ToC ゲートでユーザーに可視、Phase B 消費可)。大節点が3R で PASS 未達でも構造は健全。MAX_ROUNDS=3 は据え置き。

### 3. 全量 Phase A (残り 55 topic)

Workflow `wf_fdd23701-9ef` (背景), args=55 topic (含 大節点 strategy-01-01[70]/technology-23-61[77]/strategy-05-14[57])。

#### 結果 (186 agent / 9.29M tok / ~50分)

- **55 topic: PASS=47 / CONCERNS=8 / FAIL=0**。全 review に all_terms_covered=true、verdict 欠落0。
- 大節点全 PASS: strategy-01-01(70)=2R PASS、**technology-23-61(77)=1R PASS**、strategy-05-14(57)=2R PASS。大節点切分は全く問題なし。
- persist (batch2) → review_*.json 55件。**全63 topic で plan_/review_ 各63件 揃った**。

### 4. 全量 ToC 統合 (merge-toc 全63)

```
topics=63/63  units=244  terms=1417
  strategy:   24 topic /  98 unit / 575 term
  management:  8 topic /  23 unit / 122 term
  technology: 31 topic / 123 unit / 720 term
Rule D: PASS=54 / CONCERNS=9 / FAIL=0
構造復検: HARD失敗=0 / サイズ逸脱=40  (exit 0)
```

- **Term 整合性 (HARD gate)**: 入力1417 = plan配置1417、**過不足0・捏造0・重複0・unit_order漏れ0**。knowledge_tree 全 1417 用語を過不足なく1回ずつ配置。
- **Rule D 全63**: PASS=54 / CONCERNS=9 / FAIL=0 (pilot3 PASS + 検証5[4P+1C] + 全量55[47P+8C])。
- CONCERNS 9件: いずれも term 健全、9件中5件は low のみ(実質 PASS 相当)、4件は亜下限 unit の medium (概念境界 rationale 付)。
- サイズ逸脱40: >8語=3件(不可分概念束)、<5語=37件(小規模topic/大規模端数/独立意味カテゴリ)。極小2語=technology-13-34-u07(待ち行列+最適化問題)/technology-23-61-u13(フットプリンティング+ポートスキャン) — 共に語義自洽。
- 平均 5.81 term/unit、推定 ~61h (15分/unit)。D-128 推定 ~180〜240 unit に対し 244 (近似)。

産出:
- `data/ip/textbook/unit_index.json` (正式 ToC、schema `stage4-phaseA-toc-v1`、866KB)
- `evidence/phase5/stage_04_toc/toc.md` (人読、1737行)
- `evidence/phase5/stage_04_toc/structural_audit.json` (確定性復検)
- `evidence/phase5/stage_04_toc/gate_report.md` (ゲート要約)

### 5. ToC ゲート (D-128-A) — **ユーザー審査待ち**

Phase A 三層担保 (確定性検算 HARD=0 + Rule D 54P/9C/0F + ユーザーゲート) のうち前2層完了。

#### ゲート結果 — **ユーザー承認** (2026-06-02、選択肢A「承認」)

- 修正要望なし。63 topic/244 unit/1417語 の ToC 骨格を承認 → D-128-A 通過。
- 9 CONCERNS (亜下限 unit の概念境界拆分 + low 排列) は許容、強制 5〜8 化はしない (教学連貫優先)。
- 次 session: step3 (pilot 12 unit を web app 最小阅读視図に接続し schema 体検) → step4 (全量 Phase B)。

## Pre-close self-check (D-027 第5条)

- **D 新規 lock なし** (D-128〜132 を実行のみ)。OQ 変更なし (OQ-01/02 carryover 据え置き)。
- **STATE sync** (本 turn): 最后更新 / 当前阶段 / Stage4 表行 / 次セッション を Phase A 完了・ToC ゲート承認に更新。
- **微小実装決定** (本ログ §1): Phase A workflow lean reviews 返却 / 境界 prompt 指針 (≤5語=単一unit, >40語=同規則+端数可) / persist 経由 review 永続化 (reviewer=code-reviewer は Write 非搭載) / merge-toc 確定性結構復検を HARD gate (process.exit 2) / args 頑健パース / Phase B 脚本泛化を step4 後ろ倒し / 3 pilot plan 複用。
- **証拠**: `evidence/phase5/stage_04_toc/{toc.md, structural_audit.json, gate_report.md}`。`failures/` は HARD 失敗0・FAIL0 のため発動なし (Rule B)。
- **Rule A 不要 (Phase A)**: Session 79 決定通り重量級意味抽検は Phase B (正文改写) に適用。Phase A は 確定性検算 + Rule D + ユーザーゲートの三層で担保 (全完了)。
- **Rule D 全段**: writer=general-purpose ≠ reviewer=code-reviewer (異 subagent_type)。
- **invariants 不変**: knowledge_tree / question_bank / answer_keys 未改。textbook は新規生成物。
- **全 workflow**: Claude Code 経路・model=opus (D-129/D-132 遵守、外部 API 不使用)。検証5=`wf_95479e6c-8ad`、残55=`wf_fdd23701-9ef`。
- **未コミット**: ユーザーが commit 時機を選択 (push は要承認)。
