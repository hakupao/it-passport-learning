# Session 76 — Stage 3 知識マッピング実装（G3 起動）

> 日付: 2026-05-31 / フェーズ: Phase 5 Stage 3（実施阶段）
> ユーザー指示: 「開始 G3」
> 設計: **D-126**（Session 75 locked、二層粒度 / primary+secondary[] / 双盲+coverage）
> 前提: question_bank.json 2,900 題、全 `syllabus_refs: []`。knowledge_tree.json 63 小分類 / 1,413 用語。

## 本セッションのタスク（G3 パイプライン）

1. **G3-1 Prep**（確定性）— syllabus index + batch manifest。
2. **G3-2 Pilot**（N≈50 層化）— double-pass + reconcile + mini Rule A → パラメータ確定（D-126 が G3 起動時に確定と明記した model / subagent_type / batch size）。
3. **G3-3 Full** — 2,900 題 × 2 独立パス（Rule D）。
4. **G3-4 Reconcile + coverage** — primary 一致判定 + 63 小分類 gap 報告。
5. **G3-5 Rule A 監査** — N=20 層化、第 3 subagent_type。
6. **G3-6 Apply** — syllabus_refs 充填 + invariants 検証。
7. **G3-7 Evidence + STATE sync + commit**。

## 進捗ログ

### G3-1 — Prep ✅
- `scripts/stage03-build-index.mjs` → `data/ip/syllabus/_mapping_index.json`（63 小分類、各々 category/major/medium 文脈 + objective + 用語リスト。64.2 KB ≈ マッパーが Read する単一参照ファイル）。sanity OK（id 一意・既知カテゴリ）。
- `scripts/stage03-build-batches.mjs` → `.tmp/s03/batches/batch_NNN.json`（116 batch × 25 題）+ `_batch_manifest.json` + `_pilot_ids.json`（50 題、29 套全網羅、図題 25）。
- 実数: 2,900 題 / 63 topics / 1,413 terms。batch=25 → full は 116 batch × 2 pass = 232 mapper agent（lifetime cap 1000 内）。

### データ構造メモ（実測）
- topic id 実体は `strategy-01-01` 形式（D-126 例の `tech-07-01` は示意。実 prefix = strategy/management/technology）。
- terms は素の文字列配列。
- question: `{id, question_number, stem_jp, choices_jp, correct_answer, has_figure, figure_description, syllabus_refs:[], source}`。group_id は一部のみ。

## 決定（本セッション locked）

D-126 が「実装着手時（G3）に確定」と明記した付帯パラメータをユーザー問答で確定（新 D 番号は振らず、D-126 実装パラメータとして記録）:

- **Mapper モデル = Opus**（ユーザー選択）。理由: 日本語 IT 術語 + 跨主題判定の正確性最優先、Ultracode 下で成本非約束、本プロジェクトは Rule A 一貫で正確性重視。却下: Sonnet（D-126 は分類可能と注記するも精度劣後）/ 混合 A=Opus・B=Sonnet（reconcile 不一致率↑・escalate 増）。
- **双盲 subagent_type（Rule D）**: pass A=`general-purpose` / pass B=`explore`（D-126 §手法の例示通り、相異なる type）。Rule A 監査=`code-reviewer`（第 3 type、stage01/02.5 の監査実績流用）。三者相異 ✅。
- **batch size = 25 題/agent**。full = 116 batch × 2 pass = **232 agent**（lifetime cap 1000 内）。索引 64KB は agent が Read（prompt 直貼り回避）。
- **Pilot-first ゲート**（ユーザー選択）: N=50（29 套全網羅・図題 25）で double-pass→reconcile→mini Rule A を先行、agree 率 + 精度を確認してから full 2,900 へ。pilot 失敗は `failures/` 帰档（Rule B）。

### G3-2 — Pilot 実行
- prep: `_pilot_manifest.json`（2 batch × 25）+ `pilot/batch_00{0,1}.json`。
- map workflow run: `wf_98ef5ed7-499`（4 Opus agents, A=general-purpose / B=explore）。
- harvest: 100 mappings（A=50/B=50）、invalid_primary=0 / invalid_secondary=0 / primary_in_secondary=0 / unknown_term=1（"売上高"、reconcile の索引照合で自動除去）。
- **reconcile: primary 一致率 94%（47/50）、escalate=3**（全て strategy 内相邻トピックの真性歧義: 01-01⇄03-09 / 03-10⇄03-11 / 06-21⇄07-22）。
- **mini Rule A（code-reviewer, 第3 type, N=10）: correct=9 / acceptable=1 / wrong=0 → 妥当率 100%**。terms ok=9 / partial=1。証拠 `evidence/phase5/stage_03_pilot_audit.md`。
- 副産物知見: knowledge_tree の terms に一部の規範文書名（例「システム管理基準」）が未収録 → terms が partial 化する場合あり（primary は正しい）。マッピング誤りではなく syllabus 完整性の限界。G3 ではブロックせず記録のみ。
- **ゲート判定: PASS**（一致率 94% + 妥当率 100% + 非法 id 0）。prompt/schema/index/reconcile/harvest 全検証済 → full 2,900 へ。
- 最適化: map workflow を pass×file flatten（2 pass interleave）で wall-clock ≈半減。prompt/schema は pilot 検証通り不変。

### G3-3 — Full double-pass mapping
- 116 batch × 2 pass = 232 agent、throttle 10、Opus。run: `wf_f350fa3a-abb` ✅（232/232, 5,799 mappings, 16.6M tok, 35分）。
- harvest: A=2,899 / B=2,900（pass A が q064 1 題 drop）、invalid_primary=1（`strategy-12-32`→実は management-12-32、カテゴリ接頭辞誤り）、unknown_terms=39（0.67%、reconcile の索引照合で自動除去）。
- **reconcile: 一致率 95.9%（2,782/2,900）、escalate=117 + only_one_pass=1（2012h24a-q064）**。escalate 内訳: 同 medium 26 / 同 category 別 medium 53 / 跨 category 38。

### G3-4 — Reconcile + tie-break（3rd pass, D-126）+ coverage
- 118 contested（117 escalate + 1 onlyOne）→ tie-break workflow（`analyst`= 第3 subagent_type、A/B/auditor と相異、Rule D）。run `wf_16e1c2b3-d70`: 106/118（chunk 7 が StructuredOutput 未呼出で失敗 — 限額起因）→ 残 12 を redo run `wf_2a9018db-12a` で 12/12 完了。**合計 118/118**。
- tie-break が valid id を強制選択 → invalid `strategy-12-32` も解決。
- **reconcile3: agreed 2,782 + reconciled 118 = 2,900 final refs**（invalid decisions 0）。
- **coverage: gap node 0/63（全小分類に最低 1 題）**。分布 strategy 1,028(35.4%) / management 587(20.2%) / technology 1,285(44.3%) ≈ IPA 公式 domain_composition(35/20/45) と一致。証拠 `evidence/phase5/stage_03_coverage.md`。
- apply --dry 検証: 2,900/2,900、invariant 変更 0、status agree2782/reconciled118、confidence high2280(78.6%)/medium561/low59(2.0%)。

### G3-5 — Rule A 監査 N=20 ✅
- 層化（strategy7/management4/technology9）、`code-reviewer`（A/B/analyst と相異、Rule D）。run `wf_afa18454-0c1`。
- **correct 18 / acceptable 2 / wrong 0 → 妥当率 100%**。terms ok14/partial5/bad1。証拠 `evidence/phase5/stage_03_mapping_audit.md`。
- terms partial/bad は全て **syllabus 語彙ギャップ**（スタンドアロン/ワイルドカード/HRM/保守の移行 等が knowledge_tree 未収録）に起因、**primary は全件正しい**。マッピング誤りではない。

### G3-6 — Apply ✅
- `stage03-apply.mjs`（backup `.pre-s03`）。2,900/2,900 充填、空 0、**invariant violation 0**（correct_answer/stem/choices をバックアップ比較）。by_year 29 ファイル in-place 更新（exam_id/year_label/fiscal_year/question_count 保全）。answer_keys.json 不変。
- status: agree 2,782 / reconciled 118。confidence high 2,280(78.6%)/medium 561/low 59(2.0%)。

### G3-7 — Evidence + state sync + commit ✅
- 証拠: `evidence/phase5/stage_03_mapping.md`（実施総括）+ `stage_03_coverage.md` + `stage_03_mapping_audit.md` + `stage_03_pilot_audit.md`。
- 失敗アーカイブ（Rule B）: `failures/stage_03_tiebreak_chunk_fail.md`（tie-break chunk 7 限額起因の StructuredOutput 未呼出 → 欠落 12 題のみ redo）。
- STATE.md / PLAN §5 を Stage 3 ✅ に同期。commit 実施（**push せず**、要ユーザー承認）。

## D-126 実装パラメータ確定（本セッション）
新 D 番号は振らず D-126 の付帯として記録: Mapper=Opus / 双盲 A=general-purpose・B=explore / tie-break=analyst / Rule A=code-reviewer / batch=25 / pilot-first ゲート。三 LLM 段全て異 subagent_type → Rule D 全段充足。

## What's Next

1. **Stage 3 完了**。Phase 5 は Stage 4（AI 教科書生成、G4）が次。設計は D-114〜D-118（Session 64）で既存。起動はユーザー gate 待ち。
2. enriched `question_bank.json` は Stage 4 の primary_topic→ユニット構成 / terms→用語講解 / question id→inline_quiz の基盤（D-118）。
3. 任意フォローアップ（非ブロック）:
   - syllabus 語彙ギャップ補完（terms partial 題向けに knowledge_tree へ出題語を追補）— Stage 4 と並行可。
   - low confidence 59 題 + Stage 2.7b 残 10 フラグ題の per-question 見直し。
4. 本セッション成果は commit 済・**未 push**。
