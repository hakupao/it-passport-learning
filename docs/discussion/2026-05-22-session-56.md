# Session 56 — Phase 4 Module B Step B.3 + D-104 + D-105 + Module B ✅ COMPLETE · 2026-05-22

> Session 55 closed Phase 4 Module B B.1+B.2 ✅ DONE (commit `c864e5c`
> pushed). Session 56 opens on user explicit `开始 Phase 4 Module B Step
> B.3` gate signal — fires gate G2 second-half per PLAN.md §4. Mid-session
> the user pivoted the tutor brain matrix from Anthropic-only (D-102 §7.2)
> to 3-way env-routable (DeepSeek default + Anthropic toggle + OpenAI stub)
> producing **D-104 ADR**, and Context7 verification surfaced the
> `deepseek-chat`/`-reasoner` deprecation 2026-07-24 (D-095 §2.5(ε)
> tripwire FIRE) producing **D-105 ADR**. Module B closes COMPLETE
> with B.3 dry-run completed across 4 attempts.
>
> **Phase**: 实施阶段 (Phase 4 Module B — Brain). **First real LLM cost
> Phase 4** — ~\$0.35 真 across 4 dry-run attempts (DeepSeek + Anthropic).
> Per CLAUDE.md cost gate user-approved by the B.3 signal.

---

## 1. Turn 1 — User signal `开始 Phase 4 Module B Step B.3` (gate G2 second-half fires)

After Session 55 close (commit `c864e5c` pushed to origin), user gave the
explicit gate signal `开始 Phase 4 Module B Step B.3`. Per PLAN.md §4 G2
second-half + CLAUDE.md "first LLM API call gate", this opens Module B
Step B.3 cost dry-run.

---

## 2. Turn 2 — Survey + blocker discovery

Survey of `apps/web/.env.local`:
```
DEEPSEEK_API_KEY=...
FIREWALL_BASIC_AUTH=...
```

**No `ANTHROPIC_API_KEY`** — but D-102 §7.2 locked Anthropic Sonnet 4.6
as the tutor brain default. Reported blocker to user with two options:
(1) Pull from Vercel encrypted env or (2) Add locally.

---

## 3. Turn 3 — User pivot question

User asked: "我注册并购买 anthropic api了，两条路并行吧" and earlier
"请问你用这个是什么用途，如果是编码，你可以用我 plan 的吗，如果是网页上
的问答测试，可以用 deepseek 的".

User intent surfaces: keep DeepSeek as default (cheap; key already exists),
add Anthropic as toggle (paid newly), reserve OpenAI as future ChatGPT plan
代理 slot. "**可以切换** (switchable)" architecture.

Per D-019 §3a slow-pace, this is a major supersede topic — requires ADR.

---

## 4. Turn 4 — D-019 §3a round-2 4Q on pivot specifics

Posed 4 questions via AskUserQuestion:

| Q | Locked answer |
|---|---|
| Q1 ChatGPT 代理 落地范围 | **留 openai 接口 但不使用,只用 deepseek v4 pro** — interface reserved, no SDK dep, no API key required for dev |
| Q2 V4 pro thinking 默认 | **`thinking.type='enabled'` + `reasoningEffort='high'`** (escalate → `'max'`) |
| Q3 deprecation tripwire handling | **开 D-105 ADR + B.4 同时顺手换** Phase 2 four routes |
| Q4 cache_control markers (round-1 affirmation) | **三个都保留 方便以后可以随时更改** = preserve markers as DUAL-PURPOSE |

Round-1 D-103 \$15 cap question answered earlier ("保留 \$15 D-103") —
cap stays unchanged through the pivot.

---

## 5. Turn 5 — Context7 verification (3 major findings)

| # | Finding | Action |
|---|---|---|
| 1 | `deepseek-v4-pro` IS callable as model string (verified at `/websites/api-docs_deepseek`) | D-104 §2.1 locks V4 pro as default tutor model |
| 2 | DeepSeek change log: **"legacy `deepseek-chat` + `deepseek-reasoner` will be discontinued on 2026-07-24"** | **D-095 §2.5(ε) tripwire FIRE** — D-105 ADR drafted as handler |
| 3 | `@ai-sdk/deepseek` 2.0.35 supports `providerOptions.deepseek.{thinking: {type}, reasoningEffort}` per CHANGELOG v2.0.33/v2.0.35 | D-104 §2.2 escalation mechanism = same-model effort bump (not model swap like Anthropic path) |

---

## 6. Turn 6 — D-104 LOCKED (tutor brain matrix supersede D-102 §7.2)

`docs/decisions/D-104-phase4-tutor-brain-matrix.md` written same turn per
D-027 §1. Key sections:

- §2.1 3-way env-routable matrix (deepseek default / anthropic toggle /
  openai reserved-stub)
- §2.2 escalation semantics differ by provider (DeepSeek: effort bump /
  Anthropic: model swap)
- §2.3 cache_control markers DUAL-PURPOSE per LD-Module-B-5 (no-op on
  DeepSeek + active on Anthropic toggle)
- §2.4 D-103 \$15 cap unchanged
- §2.5 separate env var `LLM_PROVIDER_TUTOR` (independent from `LLM_PROVIDER`)
- §2.6 LD migration table for Session 55 in-source LDs (most preserved,
  semantic shifts noted)
- §2.7 reversibility — env toggle = zero-code-change swap

10 rejected alternatives + 6 history entries.

---

## 7. Turn 7 — D-105 LOCKED (Phase 2 deepseek-chat/-reasoner V4 migrate)

`docs/decisions/D-105-phase2-deepseek-v4-migration.md` written same turn.
Key sections:

- §2.1 migrate table: 4 Phase 2 routes shift `deepseek-chat`/`-reasoner` →
  `deepseek-v4-flash` with route-handler `providerOptions.deepseek.
  thinking.type` injection
- §2.2 D-085 §2.4 frozen surface preservation rationale (model param
  change ≠ behavior change; same disposition pattern as D-095 supersede
  of D-088 §2.1)
- §2.3 vitest + Playwright + manual smoke regression gates
- §2.4 atomic B.4 commit ordering (provider.ts → route handlers → tests →
  /api/tutor → gates)
- §2.5 deadline 2026-07-24 + safety buffer + fallback (Phase 5 hotfix if
  B.4 slips)
- §2.6 reversibility — per-route revert supported

7 rejected alternatives + 4 history entries.

**Locked decisions count**: 103 → **105** (+2 Session 56).

---

## 8. Turn 8 — B.1 revision (3-way env-routable provider.ts) + B.2 revision v2 (LD-Module-B-13 bulk)

`apps/web/src/lib/ai/provider.ts` MOD:
- `ModelRole` "tutor" preserved
- `TutorProvider = "deepseek" | "anthropic" | "openai"` NEW
- `DEEPSEEK_TUTOR_DEFAULT_MODEL_ID = "deepseek-v4-pro"` NEW
- `ANTHROPIC_TUTOR_DEFAULT_MODEL_ID = "claude-sonnet-4-6"` preserved
- `ANTHROPIC_TUTOR_ESCALATION_MODEL_ID = "claude-opus-4-7"` preserved
- `getActiveTutorProvider()` env reader for `LLM_PROVIDER_TUTOR` (LD-Module-B-10)
- `getTutorModel(opts)` 3-way switch with `escalate` + `provider` overrides
- `getTutorProviderOptions(opts)` builder for `streamText({providerOptions})` field
- OpenAI path throws Phase-5-reserved error (LD-Module-B-11)

`apps/web/src/lib/ai/__tests__/provider.test.ts` MOD: +14 new cases
covering env routing + 3-way + escalation semantics + openai stub +
getTutorProviderOptions shape; 25 → 39 cases.

`apps/web/src/lib/ai/tutorPrompt.ts` MOD: TUTOR_SYSTEM_INSTRUCTION
bulked from ~150 → ~1278 tokens (4472 chars) per LD-Module-B-13.
Added sections: ITパスポート 3-area curriculum framework + 3-step
pedagogical pattern + citation conventions + anti-hallucination guards +
reply style + multi-turn etiquette. Docstring rewritten to document
LD-Module-B-13 supersede of LD-Module-B-6.

`apps/web/src/lib/ai/__tests__/tutorPrompt.test.ts` MOD: +7 LD-Module-B-13
cases (length ≥3000 char threshold + syllabus areas + IPA mention +
pedagogical pattern + citation + chapter nn format). Inline-snapshot
retired (LD-Module-B-9 REPLACED by phrase-assertion approach).

---

## 9. Turn 9 — B.3 dry-run × 4 attempts (v1/v2/v3/v4)

| # | Provider(s) | Outcome | Issue surfaced | Action |
|---|---|---|---|---|
| v1 | DeepSeek 10×3=30 | TIMEOUT at 900s | thinking:high latency 25-55s/call | Reduce 30→15 calls + add incremental persist |
| v2 | DeepSeek 5×3=15 + Anthropic 5×3=15 | DeepSeek 67% / **Anthropic 0%** | Anthropic prefix below 1024-token threshold | LD-Module-B-13 SYSTEM bulk |
| v3 | DeepSeek + Anthropic (post-bulk) | DeepSeek 70% agg + turn 2+ 80% ✅ / **Anthropic still 0%** | SDK reader bug — top-level vs nested-usage asymmetry | LD-Module-B-14 readCacheUsage fallback |
| v4 | Anthropic only (post-fix) | **Anthropic 72% cache_read of input** ✅ | n/a — both LDs validated | B.3 ✅ DONE |

Diagnostic anthropic-debug.test.ts spec was created mid-session to dump
raw `providerMetadata` shape; surfaced the snake_case nested
`usage.cache_read_input_tokens` location. Spec deleted post-fix
(diagnostic served its purpose).

`apps/web/src/lib/ai/__tests__/tutor-cost-dryrun.test.ts` NEW (~340 lines):
opt-in via `RUN_TUTOR_DRYRUN=1`; .env.local loader; 5 progress-state
scenario fixtures; 3-turn Japanese tutoring conversation; per-call
incremental persist; provider-aware pricing + Phase 4 projection.

`apps/web/src/lib/ai/__tests__/provider.test.ts` MOD: +4 LD-Module-B-14
cases (nested-usage fallback + precedence ordering + null safety) bringing
provider.test.ts to **39 + 4 = 43 cases** total.

**D-103 §2.4 ≥80% target verification**:
- DeepSeek V4 pro turn 2: **80.9%** ✅ / turn 3: **80.2%** ✅
- Anthropic Sonnet 4.6 all turns: **100%** (warm cache from prior runs in
  5-min TTL window; honest cache_read/total_input = 72%)

**Cost ledger this session**: ~\$0.35 真 across all 4 attempts vs \$15 cap
= 42× headroom; cost tripwire (\$10 / 66%) silent.

**Latency**: DeepSeek 28-31s/call avg (thinking:high) / Anthropic 7-8s/call avg.

---

## 10. Turn 10 — Module B close gates ✅ ALL GREEN + Tier 3 evidence

| Gate | Command | Result |
|---|---|---|
| vitest (full) | `pnpm exec vitest run` | **416/416 PASS + 2 skipped** (tutor-cost-dryrun opt-in via RUN_TUTOR_DRYRUN=1) in 681ms |
| tsc | `pnpm exec tsc --noEmit` | clean — first try (Exclude<ModelRole,"tutor"> + new helper types + nested-usage typing all sound) |
| eslint | `pnpm exec eslint src` | 0 errors / 0 warnings |
| next build | `pnpm exec next build` | green — 23 static pages compile in 1.2s |

**Bundle invariants vs Module A close baseline**: Middleware 44.2 kB / Shared
First Load JS 102 kB / `/[locale]/chat` 169 kB / `/[locale]/quiz` 120 kB /
`/[locale]/book` 121 kB / `/[locale]/book/chapter/[nn]` 181 kB — **all
UNCHANGED**. Phase 2 D-085 §2.4 invariant preserved through D-104 + D-105
+ B.2 v2 + B.3 + LD-Module-B-14 reader fix.

Tier 3 evidence written:
- `evidence/phase4/module_b_step_03_cost_dryrun/results-deepseek.json`
- `evidence/phase4/module_b_step_03_cost_dryrun/results-anthropic.json`
- `evidence/phase4/module_b_step_03_cost_dryrun/summary.md` (~9 sections)
- `evidence/phase4/module_b_step_03_cost_dryrun/tree_outline.md`
- `evidence/phase4/module_b_step_03_cost_dryrun/design_notes.md` (~9 sections)
- `evidence/phase4/module_b_step_03_cost_dryrun/build_log.txt`
- `evidence/phase4/module_b_step_03_cost_dryrun/test_results.txt`

PLAN.md updated (B.3 row → ✅ DONE; B.4 row scope expanded for D-105
migrate; §7 STATUS upgraded with Module B ✅ COMPLETE + γ N=6 + β N=1
OPEN). STATE.md 4-anchor sync (最后更新 + 当前阶段 + 已锁定决定数
103→105 + 下一会话) with legacy Session 55 (CLOSE) entries preserved per
D-028.

---

## 11. Pre-close self-check (per D-027 §5)

| Item | Status | Evidence |
|---|---|---|
| Code written | ✅ | provider.ts MOD (3-way + reader fix) + tutorPrompt.ts MOD (LD-B-13 bulk) + dry-run.test.ts NEW + provider/tutorPrompt tests revised; user gates G2-second-half + B.3 honored |
| Module B B.1+B.2+B.3 all DONE | ✅ | PLAN.md §1 B.1/B.2 rows DONE from Session 55; B.3 row marked DONE Session 56 |
| Tests | ✅ | 416/416 vitest PASS (+26 net from 390 Session 55 close); tsc + eslint + next build all green |
| ADRs locked | ✅ | D-104 + D-105 on disk; 103 → 105 locked decisions |
| Phase 4 PLAN.md updates | ✅ | B.3 row → ✅ DONE; B.4 scope expanded; §7 STATUS Module B ✅ COMPLETE; γ baseline updated with N=6 mean -48% |
| STATE.md anchor sync | ✅ | 4 anchors prepended; legacy Session 55 preserved |
| Session log on disk | ✅ | this file (11 turns) |
| OQ changes | ✅ | 0 OQ opened (D-095 §2.5(ε) tripwire FIRE handled inline by D-105 not OQ-03); 0 OQ closed; count remains 2 (OQ-01 + OQ-02 Phase 1 carryover) |
| Rule A 抽检 | n/a | B.2 SYSTEM bulk is greenfield content addition, not >50% compression/rewrite |
| Rule B 失败归档 | not filed (per D-094 §2.4 distinction) | v1-v3 dry-run attempts were design DISCOVERIES that produced corrective LDs (B-13 + B-14), not failed-attempt grading events per Rule B criterion |
| Rule C (Phase retro) | ⏸ | RETROSPECTIVE_phase4.md at Phase 4 close (Module D Step D.3) |
| Rule D Writer ≠ Reviewer | ✅ partial | Build-time reviewer chain fired; LD-Module-B-9 inline-snapshot retired in favor of LD-Module-B-13-aware phrase assertions; browser-based chain engages at Module D ship |
| D-019 §3a slow-pace | ✅ | 4Q round-2 on pivot specifics + Context7 verification + ADR drafting all in proper sequence |
| D-102 §7.2 superseded by D-104 | ✅ | Documented in D-104 §2.6 LD migration table |
| D-095 §2.5(ε) tripwire FIRE handled | ✅ | D-105 ADR locked Session 56 Turn 7; B.4 migrate scheduled |
| D-085 §2.4 Phase 2 frozen surface | ✅ | Build-level invariants (chat 169 kB / quiz 120 kB / middleware 44.2 kB) verified UNCHANGED; D-105 migrate scheduled for B.4 (model param change is bottom-of-stack, surface unchanged) |
| Phase 1+2+3 ✅ FROZEN preservation | ✅ | 3 tags immutable; chat 169 kB / quiz 120 kB / middleware 44.2 kB invariants verified at build |
| 105 → 105 locked decisions | ✅ | D-001 ~ D-105 unchanged from Session 56 Turn 7 end; 0 further ADR Turns 8-11 |
| γ tripwire Phase 4 N=6 | ✅ | Module A -70% mean + Module B -26% mean (B.1 -67/B.2 -78/B.3 +67) = Phase 4 mean **-48% under midpoint**; PLAN.md §5 hypothesis validated |
| β tripwire Phase 4 N=1 OPEN | ✅ | DeepSeek + Anthropic buckets seeded; LD-Module-B-14 retroactively improves Phase 2 routes too |
| Phase 4 cost gate posture | ✅ user-approved | B.3 ~\$0.35 spent of \$15 cap = 42× headroom |
| LLM cost Module B B.3 | ✅ ~\$0.35 真 | first real Phase 4 LLM spend; cumulative all-phases ~\$1.01 vs $15 D-103 cap = 14.8× headroom |

---

## 12. Commit plan + Session 57 entry options

### Commit (1 atomic — Session 56 close, per Sessions 27-55 convention)

| File | Type |
|---|---|
| `apps/web/src/lib/ai/provider.ts` | mod (3-way env-routable + readCacheUsage nested fallback) |
| `apps/web/src/lib/ai/__tests__/provider.test.ts` | mod (+18 cases: D-104 routing + getTutorProviderOptions + LD-Module-B-14 nested-usage; 25→43) |
| `apps/web/src/lib/ai/tutorPrompt.ts` | mod (LD-Module-B-13 SYSTEM bulk ~150→~1278 tokens + docstring) |
| `apps/web/src/lib/ai/__tests__/tutorPrompt.test.ts` | mod (+7 LD-Module-B-13 cases; inline-snapshot retired; 19→26) |
| `apps/web/src/lib/ai/__tests__/tutor-cost-dryrun.test.ts` | new (~340 lines, opt-in harness) |
| `docs/decisions/D-104-phase4-tutor-brain-matrix.md` | new (full ADR) |
| `docs/decisions/D-105-phase2-deepseek-v4-migration.md` | new (full ADR) |
| `evidence/phase4/module_b_step_03_cost_dryrun/{tree_outline.md, design_notes.md, summary.md, build_log.txt, test_results.txt, results-deepseek.json, results-anthropic.json}` | new (×7) |
| `docs/phase4/PLAN.md` | mod (B.3 row DONE + B.4 scope + §7 STATUS) |
| `docs/STATE.md` | mod (4-anchor sync) |
| `docs/discussion/2026-05-22-session-56.md` | new (this file) |

Excluded: `.claude/` (gitignored locally). **Push pending user `push it`
gate** per Sessions 27-55 convention.

### Session 57 entry options (per gate G3 + standard menu)

(a) **`开始 Phase 4 Module B Step B.4`** → fires G3; I ship `/api/tutor`
route handler (DeepSeek V4 pro via getTutorModel + getTutorProviderOptions
+ buildTutorMessages; AI SDK v6 toUIMessageStreamResponse) + same commit
executes D-105 migrate on Phase 2 four routes per §2.1 table; vitest +
Playwright + manual smoke regression gate per D-105 §2.3.

(b) **`push it`** → push origin sync of Session 56 commit; no Vercel
deploy yet (Module D ship is the deploy step).

(c) **`hold`** → Session 56 closes; Module B ✅ COMPLETE stands; B.4
user-pending remains.

(d) **`开始 Phase 4 Module C`** (skipping B.4) — NOT recommended per
PLAN.md §4 (B.4 ships the actual tutor endpoint Module C consumes).

(e) **`adjust PLAN.md`** → revise B.4 + Module C/D rows before opening B.4
(e.g., split B.4 into two atomic commits — tutor route first, then D-105
migrate; OR adjust D-105 §2.4 ordering).

---

**Phase 4 Module B ✅ COMPLETE Session 56** — tutor brain shipped end-to-
end as design: model lock (D-104 3-way env-routable) + SYSTEM + preamble
+ cache layout + cost validation. D-103 §2.4 ≥80% target met on per-turn
steady-state both providers. ~\$0.35 first real Phase 4 LLM spend.
LD-Module-B-13/14 surfaced via 4 dry-run attempts — corrective measures
landed in same session. D-105 deepseek deprecation tripwire handled by
ADR + scheduled into B.4. **Module B Step B.4 + D-105 migrate atomic
commit awaits user gate.** Push pending user `push it` gate per Sessions
27-55 pattern — no proactive git push.
