# Session 119 — 2026-09-08 — ⑤-2 U0 脚本改修 (D-146 準拠)

> 接続元: STATE.md「次セッション」+ `docs/phase5/PLAN_52_units.md` U0。前セッション S118 は 17c0213 で push 済。

## §1 起動前申告 (D-146 第 2 条) → ユーザー承認
- unit = **U0** (脚本改修)。writer 1 (`oh-my-claudecode:executor`, opus) + reviewer 1 (`feature-dev:code-reviewer`, opus)、直列。予想 ≤ 300 万 token、30〜45 分。
- 停止条件: 改修が波 1〜3 の machdiff 残差を変えたら差し戻し / limit 到達は Rule B 記録後に停止。
- ユーザー回答: 「放行，按申报执行」。
- 事前調査 (主 context): `question_bbox_pct` は現データ 0 件 (D-145 記載どおり)、`mappings/*_pages.json` も page 番号のみで y1/y2 無し → precrop は全題「問N」見出し行帯検出に依存する。sharp は root `node_modules` に有り。波 1〜3 の run 結果は `evidence/phase5/stage_06_quiz_fidelity/full52_fidelity_S118_<exam>_<gp|cr>.json` (audits[] 全文入り) に保存済 → return 縮約の回帰試験に使う。

## §2 U0 writer (u0-writer = oh-my-claudecode:executor, opus) — 完了、Rule D 待ち
- **設計変更 (阻却事項)**: Workflow 脚本には fs / Node API が無い (`workflow-authoring` skill 明記、既存 43 本も import 不使用)。→ 各核験 agent が自分の audit を part file へ Write し、新脚本 `scripts/quiz-fidelity-merge-parts.mjs` が 0 token で従来同形の全文 JSON に束ねる。return は計数 + 40 字要約 discrepancies + `out_path`/`parts_dir`/`merge_cmd` のみ (`audits` 無し)。`model: args.model ?? 'opus'`。AUDIT_SCHEMA はバイト一致不変。
- prompt 追記: crop 優先読み / `source_text` に注記禁止 / 区切り記号脱落・引用符字種置換・源に無い挿入の計上 / discrepancies≥1 なら必ず DISCREPANT。
- `--precrop`: exam 全ページ横断で head/body インデント較正 → 問N 見出し行検出 → 帯裁断。節見出し・罫線の除外、「問」字内で割れた run の併合。2015h27a **97/100** (page-37/39 は skip = 安全側劣化)、目視 5/5 完全 (q066 の N7 裁断破損は再現せず)。他 exam 検出率 154/157 ページ。
- machdiff: `VERDICT_CONFLICT` (CLEAN かつ discrepancies 非空) を追加、exit 1 条件に算入。負検証で捕捉確認。
- 回帰把関: 2015h27a_gp same=338 / AGENT_MISSED=2、2017h29h_cr same=422 / AGENT_MISSED=3、改修前後同一。merge 往復 deep equal。縮約後 return サイズ 70170→2886 字、78248→811 字。
- 未決: (1) run 後に `merge_cmd` を 1 手実行してから machdiff (手順書へ) (2) part file は schema 未検証 (merge 側で型 + 全数被覆のみ) (3) 実 Workflow 起動での crop 優先読み・part 書き出し成功率は U1 初回 run で確認 (4) precrop PNG 97 枚は `data/ip/quiz/.phase2/precrop/2015h27a/` (gitignored)。

## §3 Rule D (u0-reviewer = feature-dev:code-reviewer, opus) → **FAIL** → 差し戻し
- 回帰把関 4 行 (旧/新 machdiff) 同一、merge 往復 deep equal、変異 3 種 + 付加 3 種すべて exit 1、precrop 目視 5/5 (q003/q015/q045/q073/q090) 完全、page-37/39 の題は crop 無し (安全側劣化) を独立確認。
- **M-1 (MAJOR)** precrop 帯は中問共有前文を含まない: `displayed_stem_jp` = `stem_jp_clean` は前文併合済 (q097: bank 120 字 → displayed 784 字、前文は page-45 / 題は page-46)。crop 優先読みでは 3 つの回退条件がどれも発火せず、agent は 120 字しか抄写せず、新規則「源に無い挿入も計上」で前文 660 字を DISCREPANT に → fixer が正しい前文を削除する経路。`chumon_groups.json` 137 題が射程。
- **M-2 (MAJOR)** part file を相対パスで agent に渡している (`data/ip/quiz/.phase2/fidparts/...`)。Write は絶対パス必須、同類 workflow 4 本は `ROOT` 定数で絶対化。失敗時は return に audits 無しのため全文永久喪失。
- MINOR: m-1 merge が bad>0 でも out_path を先に書く / m-2 40 字要約に省略記号無し / m-3 pass 2 で `existsSync` 欠落。NIT: 未知 `--flag` 無警告 / skip 計数の粒度 / q097 帯尾に次節見出し混入。
- 処置: writer に差し戻し (M-1 = chumon member は crop 省略の安全側劣化、M-2 = ROOT 絶対化、MINOR/NIT 全件)。

## §4 差し戻し 1 回目の修正 (u0-writer) → 復験 (u0-reviewer) → **FAIL (M-1b 残留)** → 差し戻し 2 回目
- 修正済 6/7: M-2 `ROOT` 絶対化 / m-1 merge は bad>0 で out_path 非生成 / m-2 `…` / m-3 existsSync / NIT 3 件 (未知 flag exit 1、skip 計数分離、帯下端 = 次見出し∨次節見出しの早い方)。回帰 4 行同一、merge 往復 deep equal、q096/q003 目視 OK、q097〜q100 は `[chumon]` で crop 無し。
- **M-1b**: chumon 名簿は代理判据。非 chumon でも `stem_jp_clean` に帯外の前文/表が併合された題が crop を受ける (実証 `2015h27a-q085` 1032 字、crop は題幹 3 行 + 4 肢のみ)。全庫同型 27 題 (`2019r01a-q047` / `2020r02o-q055` は U4/U5 母数内)。
- 処置: 除外 = chumon ∪ 内容判据 (`displayed.length > stem_jp.length*1.6+40`、理由 `merged_preamble`) + prompt に回退第 4 条 (displayed に crop 外の前文/表があれば源ページ + 直前ページを Read、「源に無い挿入」と判定しない) + manifest に `prev_page_png`。

## §5 差し戻し 2 回目の修正 → 復験 3 回目 (u0-reviewer) → **PASS** → U0 完了
- 除外 = chumon (137) ∪ 内容判据 `displayed > stem_jp*1.6+40` (全庫 160、名簿外 27) = 2900 題中 164 題 (5.7%) が crop 対象外、94.3% は帯裁断。2015h27a all: **90/100 cropped** (page_mismatch 3 / chumon 4 / merged_preamble 3 = q008/q009/q085)。`prev_page_png` を manifest に付与 (桁数保持、存在時のみ)。prompt 回退第 4 条 (displayed に crop 外の前文/表があれば源ページ + 直前ページを Read、「crop に無い = 源に無い挿入」と判定しない) は既存規則と矛盾なしを reviewer が判定。
- 回帰把関 (reviewer 独立実行): 2015h27a_gp same=338 / AGENT_MISSED=2、2017h29h_cr same=422 / AGENT_MISSED=3 = 改修前と同一 (U0 停止条件クリア)。
- 残 NIT (非阻塞): `prev_page_png` はファイル名 −1 の機械導出 (前文有無は未検証、第 4 条発火時のみ読む) / 内容判据は q008/q009 型 (表は帯内) も保守的に除外 / 表付き 197 題は crop 対象 (表は帯内、第 4 条が第二の網)。
- ゲート: crosscheck A1–A7 B1–B7 GREEN、`git status data/` 変化 0、apps/ 変化 0 (vitest / tsc / build は対象外の変更のため省略)。
- **U1 以降の run 手順変更**: workflow 完了 → return の `merge_cmd` を実行 (`quiz-fidelity-merge-parts.mjs`、0 token) → machdiff。part 書き出し成功率と crop 優先読みの実効性は **U1 初回 run で要確認** (U0 では agent 派発禁止のため未検証)。
- Rule D 記録: writer = oh-my-claudecode:executor (opus)、reviewer = feature-dev:code-reviewer (opus)、往復 3 回 (FAIL → FAIL[M-1b 残留] → PASS)。
- 消費 (概算): writer 3 往復 + reviewer 3 往復、申告 ≤ 300 万の範囲内 (limit 未到達)。

## Pre-close self-check (D-027 第 5 条)
- 新規 D / OQ: なし (U0 は D-146 の実装 unit)。STATE.md 同期済 (最后更新 / 次セッション = U1)。PLAN_52_units.md §0 (U0 完了行) / §3 (merge_cmd 手順) 更新済。
- commit のみ、push はユーザー gate。次は新セッションで U1 (Sonnet 5 A/B) から、起動前申告必須。
