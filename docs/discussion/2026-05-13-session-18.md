# Session 18 — 2026-05-13

> Phase 1 Step 6.11.D.5 — Stage 5 trilingual translation **forward
> pass** over the 554-page Stage 4 structured/ output, glossary-locked
> against the 908-entry glossary produced in D.4.
> Continues from Session 17 close at HEAD `a44e8d2` (6.11.D.4 ✅
> Gate ③ PASS first-shot, 476/476 unit tests, 81 D locked, 3 OQ open).

---

## 0. Session Goal

Authorize and execute **Step 6.11.D.5** end-to-end:

1. Stage 5 — opus translation across 554 Stage-4 structured pages,
   sub-batched at chunk=8, glossary-locked per D-074 wrapper-clause +
   D-075 jp-preservation contract.
2. Cover the full book first (`translated/page_NNN.json` × 554) before
   chasing residue. Gate ④ evaluation deferred until residue cleanup
   complete.
3. Hand off to Session 19 user gate for residue cleanup (chunk=4 +
   chunk=1 sweep) → then Gate ④ checker.

---

## 1. Inherited State

- HEAD entering: `a44e8d2` (Session 17 close)
- Branch: `main`, clean working tree, **4 commits ahead of origin**
  (Sessions 14-17 work; not pushed)
- Unit tests: **476/476** pass entering
- Step 0~5 ✅; Step 6.0~6.10 ✅; Step 6.11.A/B/C/D.1-D.4 ✅
- Locked decisions: **81** (D-001 ~ D-081)
- 3 OQ open (Phase 1 out-of-scope: OQ-01, OQ-02, OQ-05)
- Cumulative cost going in: **Mistral $0.629 billed / Anthropic $0
  billed / shadow $238.18 / wall 3h33m**
- Run-id under work: `dry_run_2026-05-12T13-23-19`
- Disk state: `structured/` 554, `glossary/glossary.json` 908 entries,
  `checkpoints/gate_{1,2,3}_*.json` ✅; `translated/` does **not**
  exist yet — D.5 creates it.

---

## 2. Decisions Locked (this session)

_(no new ADR; the CLI gap fix is mechanical mirror of existing
classify-pages pattern, not a design change. Tracked as Phase 1 v2
follow-up: "audit all LLM-driven CLI commands have full D-071 cap-
override surface; document `fail_count` as run-cumulative semantics
in D-071 ADR addendum if not already explicit".)_

---

## 3. Open Questions

继承 OQ-01 / OQ-02 / OQ-05（Phase 1 范围外）。无新增 OQ。

---

## 4. Execution log — Step 6.11.D.5

### 4.1 Attempt 1 — CLI fail-count cap gap (instant halt)

```
uv run --project packages/extractor python -m cert_extractor.cli translate-entities \
    --structured-dir data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/structured \
    --glossary-path  data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/glossary/glossary.json \
    --tier opus --max-items-per-call 8 \
    --anthropic-soft-usd 350 --anthropic-hard-usd 500 \
    --skip-existing --confirm
```

Exit ~25s, exit code 0:

| metric | value |
|---|---|
| `pages_processed` | 7 |
| `llm_calls` | 1 |
| `fields_translated` | 2 |
| `verdict_halted` | `Verdict.FAIL` |

**Root cause**: CLI gap. `translate-entities` was the only LLM-driven
subcommand missing `--fail-count-soft / --fail-count-hard /
--wall-time-soft / --wall-time-hard`. The cumulative `fail_count = 50`
(carryover from D.2/D.3/D.4 retries + Mistral OCR transients + Stage 4
force-OCR violations) exceeded the default D-071 hard cap of 30 on the
first `_budget_check` after the first translation call.

This is a **distinct class** from the Session 17 §5 cap-misframe label
artifact: that one was operationally surfaced from a successful run;
this one was a real halt that prevented forward progress without code
change.

### 4.2 CLI patch + regression test + failure archive

Mirrored the `classify-pages` cap-override pattern (cli.py lines
226-247 + plumbing) onto `translate-entities`:

- Added 4 click options: `--fail-count-soft`, `--fail-count-hard`,
  `--wall-time-soft`, `--wall-time-hard`
- Added 6 echo lines for visibility (anthropic + fail + wall, soft +
  hard)
- Plumbed through `_build_monitor(...)`
- Added `tests/unit/test_cli_translate_entities_caps.py` smoke test
  asserting all 6 cap flags appear in `--help`
- Suite: **476 → 477 pass ✅**
- Archived attempt 1 to `failures/stage5_translate/step_06_11_D_5_attempt_1_cli_fail_count_cap_gap.md`

Committed as `ebfdbd9`:
```
feat(cli)+test+failures: Step 6.11.D.5 attempt 1 — CLI fail-count cap
  gap surfaced + patched (Session 18 mid-flight)
```

### 4.3 Attempt 2 — design-intent WARN halt at anthropic soft cap

Same command + new knobs:
- `--fail-count-soft 100 --fail-count-hard 200`
- `--wall-time-soft 50000 --wall-time-hard 86400`
- `--anthropic-soft-usd 350 --anthropic-hard-usd 500` (unchanged)

Wall ~5h09m before halt:

| metric | value |
|---|---|
| `pages_processed` | 375 (+ 7 skipped from attempt 1) |
| `llm_calls` | 529 |
| `fields_translated` | 3604 |
| `glossary_hits` | 1035 |
| `verdict_halted` | `Verdict.WARN` |
| Stage 5 cost delta | $112.49 shadow |
| Cumulative anthropic | $350.67 shadow |
| `fail_count` (runner local) | 297 (sub-batch JSON parse failures, NOT tracker `add_fail()`) |

**No archive**. Per Session 09 §3 evidence ("撞 warn 等我看") and
Session 17 §5 precedent, design-intent budget halts that the user
re-authorizes do not qualify as Rule B failures.

### 4.4 Attempt 3 — forward pass clean exit, all 554 pages on disk

Same command + bumped:
- `--anthropic-soft-usd 600 --anthropic-hard-usd 800`
- (fail / wall caps unchanged from attempt 2)

Wall ~46 min before clean exit:

| metric | value |
|---|---|
| `pages_processed` | 172 (+ 382 skipped) |
| `llm_calls` | 269 |
| `fields_translated` | 2001 |
| `glossary_hits` | 693 |
| `verdict_halted` | `None` (clean) |
| Stage 5 cost delta | $57.03 shadow |
| Cumulative anthropic | $407.70 shadow |
| `fail_count` (runner local) | 155 (more sub-batch JSON parse failures) |
| `translated/` on disk | **554 / 554** |

### 4.5 Residue analysis

Across the 554 `translated/page_NNN.json` files:

```
residue_leaves total = 452
  with real jp text  = 452  (need retry)
  with empty jp      = 0
```

Distribution skews toward "whole sub-batch failures":
- 96 pages have residue, 458 are clean
- Per-page residue counts cluster at 4 / 8 / 16 leaves (12 / 21 / 17
  pages respectively) → strongly suggests entire chunk=8 sub-batches
  returning malformed JSON, not individual leaves stuck. Distinct from
  Session 09 stuck-leaf pattern (single jp items resisting opus +
  wrapper-clause).
- Worst page: page_076 with 30 residue leaves (likely 2 full chunk=8 +
  remainder)
- Cumulative `glossary_hits` 1035 + 693 = 1728 across attempts
  confirms D-074 glossary lock is firing on real surface matches.

Cleanup strategy chosen by user: **Plan A** — chunk=4 first pass,
chunk=1 sweep on stragglers. Estimated $80-120 shadow / 40-65 min wall
combined. **Deferred to Session 19** — user wants to wrap Session 18
before the next dispatch.

---

## 5. Failure archive (per Rule B)

| File | Class | Trigger |
|---|---|---|
| `failures/stage5_translate/step_06_11_D_5_attempt_1_cli_fail_count_cap_gap.md` | `cli-gap` + `cumulative-counter-semantics` | Attempt 1 hard halt, $0.41 shadow burned |

Attempts 2 and 3 produce no archive — design-intent WARN halt and
clean exit respectively.

---

## 6. Cumulative cost ledger (post Session 18)

| Stage | tokens | usd shadow | calls |
|---|---:|---:|---:|
| 1 (Mistral) | n/a | $0.5790 billed | 579 |
| 2 (opus classify) | 32 525 | $112.16 | 669 |
| 3 (opus vision re-OCR) | 51 978 | $13.07 | 56 |
| 4 (opus structure) | 220 937 | $110.40 | 568 |
| 4.5 (opus glossary single-call) | 70 921 | $2.55 | 1 |
| 5 (opus translate, **forward only, residue pending**) | 421 538 | **$169.52** | 799 |
| **Total** | **797 899** | **$408.28** ($0.58 Mistral billed + $407.70 Anthropic shadow) | **2672** |

Wall: 21 372s = **5h56m** cumulative across all stages.

Anthropic billed: **$0** (max-plan OAuth via D-069).

---

## 7. Memory updates

None this session — no new validated heuristics. The CLI gap is a
mechanical mirror, not a design lesson worth memorizing.

---

## 8. Pre-close self-check (D-027 §5)

| Item | On disk |
|---|---|
| Session 18 log (this file) | ✅ created |
| STATE.md sync | ⏳ this turn |
| Failure archive D.5 attempt 1 | ✅ committed in `ebfdbd9` |
| CLI patch + test | ✅ committed in `ebfdbd9` |
| New D / OQ | none this session |
| `data/...translated/` artifacts | 554 files on disk (gitignored) |
| `data/.../_logs/stage5_translate_attempt{1,2,3}.log` | 3 logs on disk (gitignored) |
| `data/.../cost.json` post-attempt-3 | reflects $407.70 cumulative (gitignored) |

Pending commits to land this turn: 1 atomic (session log + STATE
sync). All other artifacts already on disk; LLM-output data
intentionally gitignored under `/data/` per D-050.

---

## 9. What's Next

**Step 6.11.D.5 forward pass ✅** — 554/554 pages on disk; glossary
lock fired 1728×; clean exit at $407.70 cumulative shadow / $0 billed.

**Step 6.11.D.5 NOT closed yet** — 452 UNTRANSLATED leaves across 96
pages remain (sub-batch parse failures, not semantic stuck-leaves).
Gate ④ (`check_gate_4_post_translation`) requires 0 UNTRANSLATED
+ 0 jp-mutation; cannot pass clean until residue cleared.

Next executable = **Session 19 attempt 4** — Plan A residue cleanup:

1. Build the 96-page residue list (already inventoried §4.5)
2. Delete those 96 page files from `translated/` so runner reprocesses
3. Re-fire `translate-entities --max-items-per-call 4` with caps
   `--anthropic-hard-usd 600 --anthropic-soft-usd 500`
   (current $407.70 + estimated $50 ≈ $458, fits; soft 500 = WARN
   above expected end so the run completes without halting)
4. Re-grep residue. If non-zero, sweep with `--max-items-per-call 1`
   on remaining residue pages.
5. After residue == 0, run Gate ④ checker for jp-mutation count
   (separate axis from untranslated).
6. If both axes clean, emit `gate_4_<ts>.json` checkpoint and hand
   to user gate for D.6 (Stage 6 audit reviewer).

User decision points entering Session 19:

- **(a) Authorize Session 19 attempt 4** — Plan A as above, ~$80-120
  shadow / ~40-65 min wall combined (chunk=4 + chunk=1 sweep)
- **(b) 5h-quota window check** — cumulative wall this run is now
  5h56m, so this dispatch will likely span a fresh quota window;
  comfortable
- **(c) Push to origin/main first** — 5 commits ahead now (Sessions
  14-17 close + ebfdbd9). Optional sanity push before more dispatch
  work.

Resume entry-point for Session 19:

1. Read `docs/STATE.md` top table + §5 6.11.D.5 row
2. Read this file §4.5 (residue analysis) + §9 (Plan A)
3. Confirm 5h max-plan window status before dispatching attempt 4
4. Build 96-page residue list (one-liner: `grep -l "<UNTRANSLATED>"
   data/.../translated/page_*.json`)
5. Delete those 96 files, re-fire chunk=4
