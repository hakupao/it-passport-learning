# Session 87 — Quiz 接過去問 Phase 1 (翻訳 backfill) 実行設計 (D-019) + pilot 実施

> 日付: 2026-06-09 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 Phase 1
> 起動: ユーザー「开 Phase 1」。前提: S86 で Phase 0 (止血+地基) commit 済 (`0167626`)、ワークツリー clean。
> Phase 0 の commit 確認ゲート (STATE 記載①) は既に解消済 (commit 済) → 次=Phase 1。

## 0. 起点確認

- STATE + session-86 + D-134/D-135 を読了。Phase 0 = v1 ship 完了・commit 済 (首页治癒、quiz 接過去問 JP-first 2 モード上線)。
- 「开 Phase 1」= D-135 の Phase 1 (翻訳 backfill、2900×{zh,en} stem+choices 預生成)。
- D-135 で**架構**は確定済。本 session は**実装レベル実行設計** (Stage 4 の D-128〜132 と同格) を D-019 で確定。

## 1. 現状診断 (実装前調査)

- `questions.json` = 純粋 JP projection、翻訳フィールド無し。`build-quiz-corpus.mjs` は**冪等で raw bank から上書き再生成** → 翻訳の保存先は再生成耐性が必須。
- garble 実態: 2900 問中 figure 問 467、うち **243 問が stem に図 OCR 混入** (サンプル q: `| @ | [ 生計画定 - ア 重要成功要因…` = 図＋選択肢が stem に混入)。非 figure 問の `|` は 19 問 (大半が正当な損益/生産性表)。
- 用語リソース: `official_glossary.json` は規約 (JIS/記号) であり訳語表ではない。実際の三語 term 源 = **教科書 unit の terms[] (1417 語、`term_zh/term_en` + `definition_*` 済、lang_status 全 generated)** + unit_index の major/medium zh/en。
- 再利用パターン: `stage4-phaseB-translate.workflow.mjs` (`pipeline(translate[general-purpose,opus] → review[code-reviewer,opus] 2ラウンド)` + サイドカー `.planning/translation_*` + 決定的 merge、本土 zh 用語方針プロンプト内蔵)。

## 2. 設計 Q&A (D-019 slow pace) → ユーザー回答 (全 4 推奨案)

| Q | 回答 |
|---|------|
| Q1 実施範囲 | **パイロット先行** (1バッチ検証→ゲート→スケール) |
| Q2 figure garble + 図 | **三語クリーン + vision** (figure 問は図画像を渡し JP も clean 化) |
| Q3 翻訳保存先 (冪等上書き問題) | **サイドカー + merge** (projection 純粋性維持) |
| Q4 用語一貫性 | **教科書 term + Stage4 zh 方針で束縛** |

## 3. ロック決策

### D-136 — Quiz Phase 1 実行設計 (ADR: `docs/decisions/D-136-quiz-phase1-translation-execution.md`)
- **D-136-A pilot-first**: パイロット=`2025r07` (令和7年度、100Q、figure 16・garble 12=現行代表+厚い garble 被覆)。Rule A+Rule D+UI 検証ゲート → GO → 残 28 回スケール。
- **D-136-B サイドカー + reader merge**: 翻訳=`data/ip/quiz/translations/<exam_id>.json` (per-question `{id, stem:{zh,en}, choices:{ア…:{zh,en}}, stem_jp_clean?}`)。build-quiz-corpus 無改修、`quizReader.ts` が read 時 join。corpus 再生成耐性 + 増量 backfill 自然。`translations/` は D-134 で un-gitignore 済 (committed)。
- **D-136-C vision 三語クリーン**: figure 問 (467) は図画像 (webp) を翻訳 agent に渡し、図 garble 除去済 clean stem_jp + zh/en 生成。非 figure 問 (2433) は text-only。
- **D-136-D 教科書 term 束縛**: プロンプトに Stage4 本土 zh 用語方針 + 各問 topic の教科書 unit 三語 term を glossary 注入。

### 小決策 (session log のみ)
- **D-小1**: translate Workflow = `scripts/quiz-phase1-translate.workflow.mjs`、Rule A = `scripts/quiz-phase1-ruleA.workflow.mjs` (Stage-4 同型、別 subagent_type で写審分離)。
- **D-小2**: パイロット exam 選定根拠=最新6回中 2025r07 が figure16/garble12 で最も vision パスを stress (2026r08=fig13/garble9 より厳)。古い 2010h22a (fig29) は OCR 品質が現行非代表のため不採用。

## 4. Phase 1 内部段階 (パイロット完了)

| Step | 内容 | 状態 |
|------|------|------|
| 1 | schema + reader merge + model 拡張 (TDD) | ✅ tsc/eslint clean、vitest **455** (+9: mergeTranslation/localizedStem/localizedChoices)。translations dir 不在=全 JP fallback (Phase 0 挙動保持) |
| 2 | translate Workflow (pilot 2025r07, 100Q) | ✅ **100/100、Rule D 99 PASS/1 CONCERNS**。216 agent/9.4M tok。read-by-id モード |
| 3 | merge + Rule A N=12 + Rule D | ✅ merge 100/100・clean stem 49。**Rule A (critic) accurate 12/12・none×10/low×2/med-high 0** |
| 4 | UI 三語検証 + 証拠 | ✅ /{ja,zh,en}/quiz 200、zh 应收账款/交付物・en accounts receivable 描画、q026 clean stem 適用。build exit0/nft IPA=0/screenshots×4 |
| 5 | ゲート → GO 後 残 28 回スケール | ⏳ **ユーザー GO 待ち** |

## 5. パイロット実施結果 (2025r07)

- **パイプライン**: `quiz-phase1-prep` (input+glossary+figure path) → `quiz-phase1-translate.workflow` (translate[general-purpose,opus]→review[code-reviewer,opus]2R、figure vision、term 束縛、read-by-id) → `quiz-phase1-merge` (tr_*.json→サイドカー、決定的検証) → `quiz-phase1-ruleA-prep`+`quiz-phase1-ruleA.workflow` (critic N=12)。
- **Rule D (in-pipeline, writer general-purpose ≠ reviewer code-reviewer)**: 99 PASS / 1 CONCERNS (q039 アジャイル穴埋め訳の自然さ、意味保持)。8 問 CONCERNS→PASS repair。
- **Rule A (独立 critic, N=12 [figure8/cleaned8])**: accurate 12/12 (100%)、severity none×10/low×2、全 check true。low=q062(穴埋め「的」)/q065(en 構文・zh 併記)、いずれも意味・正誤無影響。証拠 `evidence/phase5/stage_06_quiz_phase1/rule_a_audit_2025r07.json`。
- **vision-clean 実証 (q026)**: raw OCR 表が完全破損 (図に無い発注/入荷) → translator が図から表を完璧再構成 (受注/売上計上/現金回収、正解 ウ=2,300 整合)。独立 critic も図一致確認。
- **UI**: 三語 200、zh/en 訳描画、ja は clean stem (発注 garble 消失)、Stage4 term 方針 (交付物) 反映。build exit0・nft IPA leak 0 (quiz trace=quiz_index/questions/translations のみ)・vitest 455。
- 証拠: `evidence/phase5/stage_06_quiz_phase1/` (README + rule_a_audit + screenshots×4)。

## 6. 小決策 (session log のみ)

- **D-小3 (D-136-C 拡張)**: clean-JP を**非 figure 問にも拡張**。実装で非 figure 問の stem にも OCR 誤字 (「a こ c」「挙げけた」) を発見 → 翻訳が源を解釈する以上、誤字のみ修正の `stem_jp_clean` を非 figure 問にも生成 (意味厳守、推測追加なし)。session-86「stem garble は翻訳で自然解消」の原意に合致。Rule A で clean_stem_faithful 12/12 true。スキーマ変更なし (`stem_jp_clean` は元々 optional)。
- **D-小4**: パイプラインは read-by-id モード (agent が input ファイルを id で読む)。args 軽量 (100Q=4KB) + 29 回スケール対応。
- **既知の軽微 gap (v1 許容、backlog)**: ① clean-JP は translator 裁量で軽 garble 問に未生成あり (zh/en は clean) ② `choices_jp` は JP ノイズ残存可能 (zh/en choices は clean) ③ q039 CONCERNS 1 件。
- **next.config**: QUIZ_TRACE に `translations/*.json` 明示追加 (29 回分の確実デプロイ、nft 動的 trace に依存しない)。
- **.gitignore**: `data/ip/quiz/.phase1/` (作業中間物) 再 ignore。translations/ は D-134 で committed。

## What's Next

- **パイロット完了 (100/100 三語・Rule A 100%・Rule D 99% PASS・UI 動作・全検証 GREEN)**。
- **次 = ① ユーザーゲート (パイロット品質提示、残 28 回スケール GO 可否) ② commit (派生 translations を公開 repo へ=IPA 教育利用範囲、ユーザー確認ゲート)**。
- スケール時: 各回 `prep→translate→merge→ruleA` を回す (29 回バッチ、各回 Rule A N-sample)。その後 Phase 2 (解析預生成)、Phase 3 (教科書 unit 埋込)。

## Pre-close self-check (D-027 第5条)

- **D 新規**: D-136 (Phase 1 実行設計) — ADR + 本ログ + STATE 反映。小決策 D-小1〜4 は本ログのみ。
- **OQ**: 新規なし。OQ-01/02 carryover。
- **STATE sync**: 锁定决策 135→136 / 当前阶段 / 次セッション / 最后更新 / Stage 6 行 を更新。
- **Rule A**: パイロット翻訳 (>50% 改変) → N=12 独立 critic 抽検 (accurate 12/12)。証拠 `evidence/`。
- **Rule D**: writer (general-purpose) ≠ reviewer (code-reviewer) ≠ Rule A 監査 (critic)、全別 subagent_type・別 context。
- **Rule B**: failed attempt なし (translate_failed 0、全 100 done。CONCERNS は repair で前進)。
- **invariants**: raw bank / questions.json (JP projection) 不変、translations は新規サイドカー。build-quiz-corpus 無改修。
- **未コミット**: code (quizModel/quizReader/QuizSet/next.config/.gitignore + scripts×5 + quizReader.test) + docs (session-87/D-136/STATE) + data (`data/ip/quiz/translations/2025r07.json`) + evidence。**commit はユーザー確認ゲート** (公開 repo へ IPA 派生翻訳)。
