# Session 25 — Phase 2 detail-spec 起手

| 字段 | 值 |
|---|---|
| Date | 2026-05-18 |
| Status | **OPEN** (Phase 2 detail-spec Turn 1 — D-019 Round 1 4Q 已发) |
| Entry HEAD | `75eb651` (docs: D-083 LOCKED — Session 24 close push) |
| Goal | 按 D-083 §5.3 推荐序，把 Phase 2 design 从 high-level direction 收口到细节 spec：§5.5 mapping → 形态主线 → portability → 技术栈 → 预算 + Tier。本场覆盖多少 step 由 Q1 决定。 |
| Tier | 3 候选（沿用 Phase 1 设计阶段强度），Q4 可调 |
| LLM cost | $0 (brainstorm 型 session，无 LLM 调用) |
| Phase 阶段 | **Phase 2 设计阶段 in progress** (不写可执行代码) |

---

## 1. Session goal / 本场目的

D-083 已锁 Phase 2 high-level direction（A+C hybrid / α-now / β-ready / m2 carry-forward / cert-extractor 不通用化）。

D-083 §3 Out-of-scope 列了 7 项细节，§5.3 给了 Session 25 推荐 5-step 顺序：

1. §5.5 16 条 mapping → 锁 must-do 子集
2. 锁形态主线（intent: **iii+iv** Hybrid+Chat）→ 写 D-084
3. 锁 portability 严格度（intent: **s1** web 起步）→ 写 D-085
4. 锁技术栈 + AI 模型 + 数据源 contract → 写 D-086
5. 锁 LLM 成本预算 + Tier 评估

本场覆盖到哪一步、ADR 节奏、Tier、§5.5 mapping 方法 = 本 turn Round 1 4Q 决定。

**不在本 session 范围**：写代码 / 调 LLM API / push 决定到 release 层。本场是设计 / brainstorm / ADR 锁定。

---

## 2. Decisions this session

| D | 名字 | 状态 | 文件 |
|---|---|---|---|
| **D-084** | v1.0.3 patch release: D11 kana_helper backfill (Phase 1 patch driven by Phase 2 need) | **LOCKED final** (Turn 4，user path α sign-off 2026-05-19) | `docs/decisions/D-084-v1-0-3-kana-helper-backfill.md` |

**Minor decisions (session-log only, per D-029)**:
- Turn 3 mini-decision: §5.5 #4 F-COP21 redundant_nested_parens 视形态 → **推到 Session 26 step 2 形态锁后再评**（per Q7=a；不上 ADR）

---

## 3. Open questions

| OQ | 状态 | 说明 |
|---|---|---|
| OQ-05 | partial close LOCKED（继承自 Session 24）| Phase 3/4 形态 + 顺序继续 open |
| OQ-40 | OPEN（继承自 Session 24）| β 开放时间窗 / 触发条件 |
| OQ-41+ | TBD | 本场视答案是否产生新 OQ |

---

## 4. Per-turn ledger

### Turn 1 (2026-05-18, Session 25 起手) — D-019 slow-pace Round 1 4Q

按 D-019：design 主题先发 2-4 问，user 想完再 propose；不预设答案。

D-083 已锁 high-level direction，但 5-step entry checklist 的 **scope / 节奏 / 方法 / acceptance gate** 都还没定。Round 1 用来锁这些 meta-framing 问题，再进 Round 2 钻具体 step。

---

**Q1 — Session 25 scope（本场覆盖 D-083 §5.3 checklist 的哪几步）**

- **(a) 只跑 step 1**（§5.5 16 条 mapping）→ 下场 Session 26 起 step 2 D-084 形态主线。预算：本场 ~3-5 turn。
- **(b) step 1 + step 2**（mapping + D-084 形态主线）→ 2 步打包，下场 step 3+。预算：本场 ~5-8 turn。
- **(c) step 1 + step 2 + step 3**（mapping + D-084 + D-085 portability）→ 3 步打包，下场 step 4-5。预算：本场 ~8-12 turn。
- **(d) step 1 ~ step 5 全跑**（巨型 session，把 detail-spec 一口气锁完）→ 预算：本场 ~15+ turn。
- **(e) 其他组合 / 你说怎么切**

**注**：D-019 slow-pace 要求每个 sub-topic 都先 4Q 再 propose，所以一个 ADR ≈ 4-8 turn。c/d 选项节奏较激进。

---

**Q2 — §5.5 16 条 mapping 启动方式**（step 1 必跑）

- **(a) Claude propose 完整 mapping table**：我把 16 条 × {must-do before Phase 2 demo / 可推迟 / 不做 / 视形态而定} 4 列填好，附 reasoning，user 整体审阅 + 改判。预算：单 turn 出 table，1-2 turn 来回修订。
- **(b) User 先列硬约束**："这几条绝对 must-do"（user 说），Claude 据此补全其余 13 条。预算：user 出关键约束 → Claude 补 → 1-2 turn 来回。
- **(c) 一条一条 walk-through**：16 条按序问 user "这条 must-do 还是推迟"。预算：~16 turn（最慢但最精确）。
- **(d) 视形态主线先定**：先跳到 step 2 D-084 把形态锁了，回头再做 mapping（因为形态会影响哪些 §5.5 项是 must-do）。⚠️ 这违反 D-083 §5.3 推荐顺序。

---

**Q3 — 多 ADR 节奏**（仅当 Q1 = b/c/d）

- **(α) 紧锁挨紧**：step-by-step 一气呵成，ADR 之间不留 buffer turn；user 累就喊停。
- **(β) 留 buffer**：每个 ADR 锁完留 1-2 turn 给 user 复盘 + 可选 critic agent Rule D 预审。
- **(γ) 视具体 step 决定**：D-084（形态主线）和 D-085（portability）紧锁，D-086（技术栈）单独留 session（因为技术栈选型水深）。

---

**Q4 — §5.5 mapping 的 acceptance gate**（怎样算 step 1 完）

- **(g1) 全 mapped + user signed off**：16 条每条都标注 + user 逐条 ACK → step 1 闭合。
- **(g2) Must-do 子集 LOCKED 即可**：可推迟 / 不做 list 当 sidecar 不强制 user 一一审，只 lock must-do → step 1 闭合。
- **(g3) Mapping table out → 直接进 step 2**：不强制本场 close mapping，step 2 实施前才回头确认。⚠️ 风险：mapping 漂移。
- **(g4) Hybrid**：must-do 严格 sign-off，其他 list 留 sidecar 自动通过。

---

**回完这 4 题，我会做**：

1. Round 1 答案写回 Turn 2
2. 视 Q1+Q2 进 Round 2（具体 step 的实操 4Q）/ 或直接 propose §5.5 mapping table（如果 Q2=a）
3. STATE.md sync "下一会话" → Turn 2 状态
4. 视 Q4 给 step 1 acceptance gate 落地（mapping table 落到 evidence/ 还是只在 session log）

**不会做的事**：

- 不预设 §5.5 mapping 答案
- 不跳过 D-019 slow-pace
- 不动 D-083 §2 lock（D-083 LOCKED final 不变；本场只走 sub-ADR 路径）
- 不调 LLM API / 不写代码

---

### Turn 2 (2026-05-18) — Round 1 答案登记 + §5.5 mapping table propose

**User Round 1 答**:

| # | 问题 | 答 |
|---|---|---|
| Q1 | Session 25 scope | **(a) 只跑 step 1**（§5.5 mapping）→ 下场 Session 26 起 step 2 D-084 |
| Q2 | §5.5 mapping 启动方式 | **(a) Claude propose 完整 mapping table** → user 审阅 |
| Q3 | 多 ADR 节奏 | n/a（Q1=a 不触发，本场 0 sub-ADR）|
| Q4 | acceptance gate | **(g4) Hybrid**: must-do 严格 sign-off，其他 sidecar |

**本场 final scope**：step 1 only — §5.5 mapping → 锁 must-do 子集 + 14 条非 must-do 进 sidecar。0 sub-ADR 预期（除非 must-do 实施路径选择本身值得 standalone ADR）。

---

**§5.5 16 条 mapping table propose**（Q2=a Claude 直 propose）

判定 framework：
- 评估锚点 = **Phase 2 = A+C hybrid「带 AI 答疑的备考工具」消费 v1.0.2 `output/`**
- cert-extractor 内部改进（§5.5.3 架构层 + §5.5.1 audit detector 改进）**不直接影响 v1.0.2 output 数据** → "不做" / "可推迟"
- 影响 v1.0.2 output **可见数据质量**的项 → "must-do" / "可推迟" / "视形态"
- 治理 / 工程纪律项 → "可推迟"

| # | §5.5 项 | 类别 | 对 v1.0.2 output 数据影响 | 对 Phase 2 demo 影响 | 判定 |
|---|---|---|---|---|---|
| 1 | D7 date heterogeneity edge case (page_292) | audit detector | 无（detector 误判，output 数据不变）| 无 | **不做** |
| 2 | Stage 6 reviewer prompt refinement (learning gloss whitelist) | audit reviewer | 无（reviewer 误判，output 数据不变）| 无 | **不做** |
| 3 | D9 INFO noise (×210) | audit detector | 无（INFO 级，不到 WARN）| 无 | **不做** |
| 4 | F-COP21 redundant_nested_parens 残余 | audit | 残余 nested parens 仍可能在 output JSON / Markdown 里 | **视 Chat vs Quiz** — Chat 模式 AI 改写显示则用户不可见；Quiz 模式直显则 ugly | **视形态**（推 Session 26 step 2 形态锁后再评）|
| 5 | F-CHOICE-MARKER | export normalizer | 已修（Stage 7 已 normalize 254 choices）| 无 | **不做（已修）** |
| 6 | **D11 kana_helper 不传递**（glossary[entry].kana_helper 未 copy 到 Term entity）| translation | **有** — Term entity 缺 kana_helper 字段 | **HIGH** — kana_helper = 项目核心动机（日语字形识读辅助）；备考工具 + AI 答疑没 kana_helper 等于砍掉独特卖点 | **must-do** ⚠️ |
| 7 | D13 surface ⇎ zh_concept split wrong-layer | translation | 已修（polish #2 已 ship 进 v1.0.2，只是 architectural debt）| 无（数据已 OK，只是修在错的层）| **可推迟** |
| 8 | Stage 5 prompt v2 polish (tautology / circular EN / suffix cluster) | translation | 已 ship 273 polish items 进 `polish_items.json` sidecar | 中等 — translation 质量 polish；user 可查 sidecar | **可推迟**（sidecar 已是 disclosure，Phase 2 demo notes 提一句即可）|
| 9 | Glossary prompt cache | architecture | 无（Phase 2 不调 Stage 5 prompt）| 无 | **不做** |
| 10 | Deterministic-first pass | architecture | 同上 | 无 | **不做** |
| 11 | Stuck-leaf escalator | architecture | 同上 | 无 | **不做** |
| 12 | All-sub-batches-failed verdict | architecture | 同上 | 无 | **不做** |
| 13 | Rule A.2 新增 (LLM-only PASS + deterministic spot-check) | governance | 无 | 视 Phase 2 实施是否大量 LLM-only PASS；现在未知 | **可推迟**（等 Phase 2 实施暴露需求才补）|
| 14 | ADR amendment policy 段 | governance | 无 | 低 — D-080 v1.1 §8 已有经验；low effort | **可推迟**（某场 session 末尾顺手做 mini-ADR）|
| 15 | Failure-evidence 链路 smoke (automated check) | governance | 无 | 视 Phase 2 实施失败模式；现在过早 | **可推迟** |
| 16 | 每 10 session 一次 D status pulse | governance | 无 | 治理仪式，跟具体 Phase 无关 | **可推迟**（Session 30/35 自检 checklist 加）|

---

**汇总**:

| 判定 | 数量 | 项 |
|---|---|---|
| **must-do** | **1** | #6 D11 kana_helper |
| **视形态** | 1 | #4 F-COP21 |
| **可推迟** | 6 | #7 #8 #13 #14 #15 #16 |
| **不做** | 8 | #1 #2 #3 #5 #9 #10 #11 #12 |
| 合计 | 16 ✓ | — |

**per g4 acceptance gate**:
- **must-do (#6)** → user 严格 sign-off + 选实施路径（见下）
- **视形态 (#4)** → 知会 user，**本场就锁 "推到 Session 26 step 2 形态主线锁后再评"**（可以是 mini-decision 写进 session log，不上 ADR）
- **14 条非 must-do** → 进 sidecar (`RETROSPECTIVE.md` §5.5 已是 sidecar)，自动通过，不要求逐条 ack

---

**#6 D11 kana_helper must-do 实施路径**（3 选 1）

| # | 路径 | 优点 | 缺点 | 备注 |
|---|---|---|---|---|
| **(i)** **v1.0.3 patch release via post-process script** | 写 backfill 脚本扫 v1.0.2 output，把 `glossary[surface].kana_helper` 同步进 `Term entity.kana_helper` → release `itpassport-r6-v1.0.3` | output 数据 self-contained ✓ / 外部消费者也受益 / Phase 2 app 简单 / reproducible | 多一个 release tag + 需 mini-ADR (D-084 或 minor) + 跑一遍 publish | 我倾向这条 |
| (ii) Phase 2 应用层 join | Phase 2 app 运行时 join `glossary[term].kana_helper` 进 Term display layer | output 不变 / 不需 patch release / Phase 1 边界干净 | Phase 2 app 多一层 join 逻辑 / 应用层背数据完整性责任 / 若未来有别的消费者会重复造轮子 | 也 OK |
| (iii) Patch cert-extractor + 重跑全本 | 改 Stage 5/4.5 prompt 修 kana_helper 传递 + 全本重跑 → release v1.1.0 | 根因修复 | **Rejected** — 违反 D-083 §2.2 "cert-extractor NOT generalized for Phase 2"；要 LLM cost / wall time（Phase 1 Stage 5 ~$169 shadow）；过度工程 | Rejected |

---

**Turn 2 close gate — 3 sign-off Q**（per g4 must-do 严格 sign-off）:

**Q5** — §5.5 mapping table 整体 ACK：
- (α) ACK 全 mapping（1 must-do / 1 视形态 / 6 可推迟 / 8 不做）→ 进 Q6 选 #6 实施路径
- (β) 个别条目要 redirect → user 指出哪几条要改判 + 新判定

**Q6** — #6 D11 kana_helper 实施路径：
- (i) v1.0.3 patch release via post-process script（我倾向）
- (ii) Phase 2 应用层 join
- (其他)

**Q7** — #4 F-COP21 视形态：
- (a) 本场锁 "推到 Session 26 step 2 形态主线锁后再评"（mini-decision 进 session log，不上 ADR）
- (b) 直接判 "可推迟" 进 sidecar（同 6 条可推迟一起处理，Session 26 不再评）
- (c) 其他

---

**回完 Q5 + Q6 + Q7 我会做**：

1. 答案写回 Turn 3
2. 如果 Q6 = (i) → 视必要锁 D-084（v1.0.3 patch release 实施 ADR，含 backfill script spec + release publish spec），或仅 mini-decision；如果 Q6 = (ii) → mini-decision 记录 Phase 2 app 端 join 设计，留 Session 26+ 形态锁完时实施
3. 视 Q7 处理 #4
4. step 1 acceptance: must-do (#6) sign-off + 路径 lock + 14 条 sidecar → §5.5 mapping ✅ CLOSED
5. 本场 Session 25 可以 Turn 4 close（per Q1=a 只跑 step 1），等 user "go commit"

---

### Turn 3 (2026-05-18) — Round 2 答案登记 + D-084 LOCKED draft + step 1 acceptance close

**User Round 2 答**:

| # | 问题 | 答 |
|---|---|---|
| Q5 | §5.5 mapping 整体 ACK | **(α) ACK 全 mapping**（1 must-do / 1 视形态 / 6 可推迟 / 8 不做）|
| Q6 | #6 D11 kana_helper 实施路径 | **(i) v1.0.3 patch release via post-process script** |
| Q7 | #4 F-COP21 视形态处理 | **(a)** 本场就锁 "推到 Session 26 step 2 形态锁后再评" |

---

**本 turn 产出**:

1. ✅ **D-084 ADR** = `docs/decisions/D-084-v1-0-3-kana-helper-backfill.md` 写入（LOCKED draft, pending sign-off）— release boundary + data-shape additive sub-ADR of D-083 §2.4
2. ✅ Session 25 log Turn 3（本段）+ §2 D-084 row + mini-decision row (Q7=a F-COP21 推 Session 26)
3. ⏳ STATE.md sync（本 turn 即将 edit）— 决定数 83→84 (LOCKED draft pending), 当前阶段 cell, 下一会话 cell, 最后更新 prepend

---

**§5.5 mapping step 1 acceptance per g4**:

| 类别 | 数量 | 处理 | 状态 |
|---|---|---|---|
| **must-do** | 1 (#6) | D-084 LOCKED draft → user 严格 sign-off | ⏳ Q8 待 |
| **视形态** | 1 (#4) | Turn 3 mini-decision = 推 Session 26 | ✅ LOCKED |
| **可推迟** | 6 | sidecar 自动通过 (RETROSPECTIVE §5.5 = sidecar) | ✅ LOCKED |
| **不做** | 8 | sidecar 自动通过 | ✅ LOCKED |

step 1 闭合条件：D-084 user sign-off → 14 条非 must-do 全 sidecar 自动通过 → step 1 ✅ CLOSED → Session 25 进 Turn 4 close。

---

**Q8 (close gate) — D-084 草案签字**:

- **(α)** ACK D-084 LOCKED draft → 转 LOCKED final + session 25 Turn 4 close + 等 user "go commit"
- **(β)** D-084 需要修订（user 指出具体段 / 措辞）
- **(γ)** 仅 commit D-084 ADR 不 publish v1.0.3（implementation 推 future session）
- **(δ)** ACK + immediate "go implement v1.0.3"（Session 25 不 close，进入 implementation phase）

注：D-084 §2.7 已锁 "Implementation gate" = user 显式 "go implement" 才动手。所以 (α) = 默认推荐路径 = lock ADR + commit + close session，implementation 留 future。

---

**回完 Q8 我会做**：

1. 答案写 Turn 4
2. (α) → D-084 LOCKED final + session 25 close + commit pending
3. (β) → 重写 D-084 §N → Turn 4 重提 Q8
4. (γ) → 同 (α) 但 STATE 加 "v1.0.3 publish pending future session" 标记
5. (δ) → 进 implementation phase (开 Session 25.5 sub-session 或新 session)

---

### Turn 4 (2026-05-19) — Q8=α / D-084 LOCKED final / step 1 acceptance close / pre-close self-check / commit plan

**User answer Q8 = α** → ACK D-084 LOCKED draft as-is + session 25 Turn 4 close + wait commit。

**D-084 sign-off applied**:
- `docs/decisions/D-084-v1-0-3-kana-helper-backfill.md`：Status row + §7 sign-off table **LOCKED draft → LOCKED final** + Reviewer #1 (user) **APPROVED** path α (2026-05-19)
- 本 session §2 D-084 row：**LOCKED draft → LOCKED final**

---

**§5.5 mapping step 1 acceptance per g4 — FINAL**:

| 类别 | 数量 | 状态 |
|---|---|---|
| must-do | 1 (#6 D11 kana_helper) | D-084 LOCKED final → 等 user 显式 "go implement v1.0.3" (§2.7 gate) |
| 视形态 | 1 (#4 F-COP21) | mini-decision LOCKED → Session 26 step 2 form 锁后再评 |
| 可推迟 | 6 (#7 #8 #13 #14 #15 #16) | sidecar (RETROSPECTIVE §5.5) 自动通过 |
| 不做 | 8 (#1 #2 #3 #5 #9 #10 #11 #12) | sidecar 自动通过 |
| 合计 | 16 ✓ | **step 1 §5.5 mapping ✅ CLOSED** |

---

**Pre-close self-check** (per D-027 §5)：

| 项 | 状态 | 证据 |
|---|---|---|
| D-084 ADR LOCKED final | ✅ | Status row line 5 + §7 sign-off row |
| Session 25 log = 4 turn 完整 | ✅ | Turn 1 / Turn 2 / Turn 3 / Turn 4（本段）|
| STATE.md 已锁定决定数 = 84 (final, no pending) | ⏳ | 本 turn 即将 edit |
| STATE.md 未决问题数 = 4 (unchanged this session) | ✅ | row 13 (本 session 无新 OQ) |
| STATE.md §4 OQ 表 unchanged | ✅ | 本 session 无 OQ 状态变更 |
| STATE.md 当前阶段 (Phase 2 设计阶段 in progress) | ⏳ | 本 turn 即将 edit row 10 |
| STATE.md 下一会话 (Session 26 or v1.0.3 impl 二选一) | ⏳ | 本 turn 即将 edit row 15 |
| STATE.md 最后更新 prepend Session 25 entry | ⏳ | 本 turn 即将 edit row 9 |
| Rule D 合规 | ✅ | Writer = Claude / Reviewer = user terminal sign-off path α |
| Rule C 合规 | n/a | Phase 1 retro = RETROSPECTIVE.md FINAL；Phase 2 retro 等 Phase 2 收尾 |
| 无 LLM cost / 无 code change | ✅ | 设计阶段，brainstorm session 类型 |

---

**Commit plan** (待 user "go commit" 授权)：

- Staged：`docs/decisions/D-084-v1-0-3-kana-helper-backfill.md` (new) + `docs/discussion/2026-05-18-session-25.md` (new) + `docs/STATE.md` (modified)
- Commit message: 匹配 D-083 commit style (`docs: D-084 LOCKED — ...`)
- **Push**：等 user 显式 "push it" 才执行

---

**Session 25 ✅ CLOSED** 2026-05-18 ~ 2026-05-19 — 4 turn 完整链：
1. Turn 1 Round 1 4Q (D-019 meta-framing)
2. Turn 2 Round 1 答案登记 + Claude propose 16-row §5.5 mapping table + 3 sign-off Q
3. Turn 3 Round 2 答案登记 + D-084 LOCKED draft + Q8 close gate
4. Turn 4 Q8=α sign-off + D-084 LOCKED final + commit plan

无 LLM cost。无 code change。§5.5 mapping step 1 ✅ closed per g4 acceptance。

---

## 5. References

- `docs/decisions/D-083-phase2-direction.md` — Session 24 LOCKED final ADR（A+C hybrid 等）
- `docs/decisions/D-084-v1-0-3-kana-helper-backfill.md` — **本 session Turn 4 LOCKED final**
- `RETROSPECTIVE.md` §5.5 — 16 条 carry-forward 清单（4 类）
- `docs/discussion/2026-05-18-session-24.md` — Session 24 Turn 4 §5.3 entry checklist
- `docs/STATE.md` — 当前快照（Phase 2 设计阶段 in progress）
