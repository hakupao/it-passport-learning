# Session 32 — 2026-05-19

> **Phase 2 实施 gate ✅ UNLOCKED**：Session 31 close 全闭 (10 ADR D-083~D-092 + OQ-40 CLOSED + 0 OPEN OQ for Phase 2 design) → user kickoff "开 Phase 2 实施 gate" 2026-05-19 → STATE.md §当前阶段 flip 设计阶段 → 实施阶段 in progress。本 session = Phase 2 实施阶段第一场 → Round 1 4Q meta-framing 锁 Session 32 scope。
>
> 接续 Session 31 close (D-091 + D-092 LOCKED final + OQ-40 CLOSED LOCKED + Phase 2 设计阶段全部完成)。

| 字段 | 值 |
|---|---|
| Status | **🚧 IN PROGRESS** — Turn 1 Round 1 4Q posed, awaiting user answer |
| 前置 ADR | D-083 (direction A+C hybrid) + D-084 (v1.0.3 shipped) + D-085 (form Hybrid+Chat) + D-086 (portability s1 web) + D-087 (stack Next.js 15 + Vercel AI SDK + Vercel + SSR + TS strict) + D-088 (AI Opus 4.7 + Hybrid pin + cache + 1-retry no-fallback + δ-all-tripwire) + D-089 (DataSource interface + FsDataSource default + index.json v2 backfill + 4 per-scope assembly fns) + D-090 (LLM cost cap $5/$15/$30 + per-query $5 + α-silent / β-D071-graduated + env var switch) + D-091 (budget 三档 α $0 / β $220-$1,800-$4,000 + Tier 3 全套 + 15 step / 3 weeks) + D-092 (OQ-40 close = β trigger user_explicit AND multi-trigger entry + 全套 β-open checklist) |
| 工作流 tier | **Tier 3 实施阶段**（CLAUDE.md「实施阶段：Code is allowed under TDD」+ D-091 §2.2 Tier 3 全套 evidence + Phase 1 evidence template carry-over + 5 Phase 2 specific additions: cache_audit / Lighthouse / Vercel deploy / E2E Playwright / TTFT）|
| 预期产物 | Session 32 scope ADR 候选 (D-093?) + Phase 2 实施 PLAN.md 起手 (D-091 §2.3 15-step 落地) + `evidence/phase2/` 目录 scaffold + (依 Q1 答) Step 1 起手 (Next.js 15 scaffold + TS strict + Vercel deploy hello-world) |
| LLM cost | 暂 $0（实施 Step 1 = scaffold only；不调 LLM；Step 2+ Vercel AI SDK 接入开始受 D-090 cap 约束）|

---

## 1. Entry context（gate flip）

**Session 31 close 状态** (2026-05-19 Turn 6 path α):
- 10 ADR LOCKED final (D-083 ~ D-092) 全闭合
- OQ-40 CLOSED LOCKED by D-092
- 0 OPEN OQ for Phase 2 design
- 3 PoC evidence dir 落盘 (`phase2_d088_poc` / `phase2_d089_poc` / `phase2_d091_poc`)
- 0 `packages/extractor/` 改动 (D-083 §2.2 锁)
- 0 Phase 2 app code (实施 gate 解锁等 user kickoff)

**User kickoff** (本 session 起手): "开 Phase 2 实施 gate" — 2026-05-19

**Gate flip 含义**:

| 维度 | 设计阶段 (closed) | 实施阶段 (in progress) |
|---|---|---|
| STATE.md §当前阶段 | "Phase 2 设计阶段 ✅ 全部完成" | "Phase 2 实施阶段 in progress" |
| STATE.md §下一会话 | "Session 32 = Phase 2 实施 gate 解锁议题" | "Session 32 IN PROGRESS = 实施阶段第一场" |
| CLAUDE.md phase signaling | "Do not write executable code" | "Code is allowed under TDD" |
| 主要产物 | ADR + session log + PoC evidence | code + tests + step-level evidence + PLAN.md 进度 |
| Evidence ceremony | 设计 ADR drafts + PoC measurement | Tier 3 全套 + Phase 1 carry-over + 5 Phase 2 specific additions |
| 评审节奏 | D-019 slow-pace 4Q + user terminal sign-off path α | 待 Q4 答 (D-019 实施阶段适用度) |

---

## 2. Decisions / lock log

（待 Round 1 → Round 2 → sign-off 进展后填）

---

## 3. Open questions

（暂无 new；OQ-40 已 closed Session 31 by D-092；实施阶段第一场如现场涌现新 OQ 即时登记）

---

## 4. Turns

### Turn 1 (2026-05-19) — Round 1 4Q meta-framing

**Frame**：沿用 Session 24~31 D-019 slow-pace 模式 = Round 1 meta-framing 锁 Session 32 scope / 产物 / 颗粒度 / D-019 实施阶段适用度，再进 Round 2 实质内容。Phase 2 实施阶段第一场，4Q 决定本场起手节奏 + 后续 Sessions 节奏 baseline。

---

#### Q1 — Session 32 scope（本场要落什么）

- **(a)** **PLAN.md + evidence scaffold only（最薄 kickoff）**：本场只锁 15 step 顺序 + 评审节奏 + 谁审什么 + retro 节点 + `evidence/phase2/` 目录建出；Step 1 编码推 Session 33。2-3 turn close。
- **(b)** **PLAN.md + evidence scaffold + Step 1 起手（Next.js 15 scaffold + TS strict 本地跑通，不上 Vercel）**：本场跑 Step 1 一半；Vercel deploy hello-world 推 Session 33。3-4 turn close。
- **(c)** **PLAN.md + evidence scaffold + Step 1 完整（含 Vercel deploy hello-world + preview URL ✅ 上线）**：本场跑完 Step 1 + 收 step_01 evidence + 拿到第一个 Vercel preview URL。4-6 turn close。
- **(d)** **PLAN.md + evidence + Step 1 + Step 2 起手（Vercel AI SDK + Anthropic provider 配置 + 第一次 AI hello-world stream）**：偏重；可能首次触 D-090 daily $5 soft cap (α-silent log only)；5-7 turn close。

---

#### Q2 — PLAN.md 颗粒度

- **(g1)** **mid level**（类 Phase 1 D-077/D-079/D-080 PLAN 模式）= 15 step × 每 step 1-2 行 description + 评审 owner + evidence 类型 + 预估 wall + dependencies；约 ~150 行；最常用颗粒度。
- **(g2)** **deep level** = mid + 每 step Acceptance Criteria + TDD 起手测试列表 + 失败 fallback path；约 ~300 行；前期投入大，后期节奏稳。
- **(g3)** **shallow level**（high level only）= 15 step × 一行 description；约 ~50 行；快速起手，每 step 进入时再细化。

---

#### Q3 — `evidence/phase2/` 目录结构

- **(a)** **Phase 1 evidence/ 同构 carry-over**：`evidence/phase2/step_NN/` + `failures/phase2/step_NN_attempt_X.md` + `RETROSPECTIVE.md` Phase 2 §X；与 Phase 1 完全对称，0 学习成本；D-091 §2.2 5 Phase 2 specific additions 作为 `step_NN/` 子文件 (e.g. `step_NN/cache_audit.md`)。
- **(b)** **Phase 2 specific inline mix**：(a) + D-091 §2.2 5 specific additions 各自顶层 sub-dir (`evidence/phase2/cache_audit/` / `evidence/phase2/lighthouse/` / `evidence/phase2/vercel_deploy/` / `evidence/phase2/e2e/` / `evidence/phase2/ttft/`)；step-level 仍在 `step_NN/`；cross-step audit dir 易聚合。
- **(c)** **flat `evidence/phase2_step_NN_<topic>/`**：所有 step-level evidence flat 在 `evidence/phase2_step_NN_*/`；与现有 PoC 命名一致 (`phase2_d088_poc_*` / `phase2_d089_poc_*` / `phase2_d091_poc_*`)；Phase 1 sub-dir 模式不延续。
- **(d)** **user 来定 / 别的**

---

#### Q4 — D-019 slow-pace 实施阶段适用度

D-019 slow-pace "每个新 topic 抛 2-4 Q 再 propose" 是为设计阶段量身。实施阶段编码 step 内的微决策（如 npm vs pnpm / app router vs pages router / Tailwind vs CSS Modules）是否每个都走 slow-pace？这决定后续 15 step Sessions 的 wall + ceremony 节奏。

- **(a)** **D-019 全保留**：每 step 内微决策仍走 slow-pace 4Q；ceremony 最重 + wall 最慢；与设计阶段完全一致。
- **(b)** **D-019 仅 step-boundary 适用**：step N → step N+1 转场 + cross-cutting 多 step 影响的决策走 slow-pace；step 内部微决策 Claude 直接 propose + user 终审；与 Phase 1 实施阶段一致。
- **(c)** **D-019 完全收回 for 实施阶段**：实施全程 propose-first；user 仅 review；ceremony 最轻 + wall 最快；适合熟手 + low-coupling step。
- **(d)** **D-019 自适应**：影响 D-NNN-worthy 决策（架构 / 不可逆 / cross-step coupling）→ slow-pace 4Q；纯编码 micro → propose-first；D-NNN-worthy 标准由 Claude 判 + user 任何时刻可 override "停 slow-pace 一下"。

---

**回完 Q1-Q4 → Turn 2** = 据答案展开 Round 2（PLAN.md draft + evidence dir scaffold + 依 Q1 答 Step 1 编码）。

预期 path:
- Q1=a + Q2=g1 + Q3=a + Q4=b → 最薄 2-3 turn close（仅 PLAN + scaffold）
- Q1=c + Q2=g1 + Q3=a + Q4=b → 标准 4-6 turn close（含 Step 1 上线）
- Q1=d + Q2=g2 + Q3=b + Q4=a → 最重 6-8 turn close
- Q1=b + Q2=g1 + Q3=a + Q4=d → 推荐 3-4 turn close（Step 1 起手 + 自适应 ceremony）

---

### Turn 1 close — Round 1 答案

**User answer**: **Q1=c / Q2=g1 / Q3=a / Q4=d**

| Q | 选 | 含义 |
|---|---|---|
| Q1 | c | 本 session 跑完 Step 1 完整（Next.js 15 scaffold + TS strict + Vercel deploy hello-world ✅ 上线 + preview URL） |
| Q2 | g1 | PLAN.md mid level ~150 行（类 Phase 1 D-077/D-079/D-080 模式） |
| Q3 | a | `evidence/phase2/step_NN_<topic>/` 同构 carry-over；D-091 §2.2 5 specific additions 作为 step 子文件 |
| Q4 | d | D-019 自适应：D-NNN-worthy → slow-pace 4Q；纯编码 micro → propose-first；user 可任意 override |

预期 path: **4-6 turn close**（Turn 2 PLAN + evidence scaffold + Round 2 micro Q / Turn 3 user Q5 答 / Turn 4 Step 1 execute / Turn 5 close + STATE sync + commit plan）。

---

### Turn 2 (2026-05-19) — PLAN.md + evidence/failures scaffold + Round 2 Q5 (D-NNN-worthy micro)

**Frame**: 据 Q1-Q4 落地三件 deliverable（per Q4=d 自适应：纯 carry-over / 引用 D-091 的工作 propose-first 不抛 Q；D-NNN-worthy 抛 Round 2 Q5）：

| Deliverable | 文件 | 状态 |
|---|---|---|
| Phase 2 实施 PLAN.md（15-step mid level g1） | `docs/phase2/PLAN.md` | ✅ written this turn |
| `evidence/phase2/` 目录 scaffold + README orientation | `evidence/phase2/README.md` | ✅ written this turn |
| `failures/phase2/` 目录 scaffold + README orientation | `failures/phase2/README.md` | ✅ written this turn |

**PLAN.md 简介**（详见 file）:
- §1 15-step 路线 = Module A Scaffold (3 step / 4.5 day) + Module B AI 路径 (5 step / 5.5 day) + Module C UI/UX (4 step / 4 day) + Module D Polish+Deploy (3 step / 2.5 day) = 15 step / ~14.5 day / 3 weeks
- §2 evidence 落点 = Q3=a Phase 1 同构 `evidence/phase2/step_NN_<topic>/` + 5 specific additions inline (`cache_audit_*.md` / `lighthouse_*.md` / `ttft_*.md` / `e2e_*.json` / `vercel_deploy_*.log`)
- §3 节奏 = Q4=d 自适应 slow-pace + Step boundary 评审 + Rule B/C/D 全套
- §5 amendment cascade = D-091 §2.5 5 tripwire + PLAN-specific (节奏 drift / repo layout drift)

---

#### Q5 — Phase 2 web app monorepo location (D-NNN-worthy)

**Reason slow-pace** (per Q4=d)：本议题影响 D-082 / CLAUDE.md 原措辞 `packages/extractor/ is Phase 1's only package; apps/ reserved for Phase 3+`。Phase 2 = web app 已 locked (D-083) → `apps/` reservation 措辞过时；选哪条 option 决定后续 dir 命名 + workspace 配置 + 是否需新 D-093 sub-ADR amending D-082。

- **(a)** **`apps/itp-learn/`**（cert-scoped dir 名）— `apps/` 启用 + cert prefix；未来若 Phase 5 generalize cert-extractor 为多 cert 时 `apps/<其他>/` 自然扩展；amend D-082/CLAUDE.md 措辞 "Phase 3+" → "Phase 2+"；新 D-093 sub-ADR 锁
- **(b)** **`apps/web/`**（generic dir 名）— `apps/` 启用 + 通用名；假设单 web app 服务（future multi-cert 通过 URL path 或 env config，数据层变化不立新 app）；amend 措辞同 (a)；最简 dir 名 + 不承诺单 cert
- **(c)** **`packages/web/`** — 不动 `apps/` reservation；Phase 1 sibling；但 `packages/` 历来是 library，web app semantic mismatch
- **(d)** **顶层 `web/` 新 dir** — 不动 `apps/` 也不动 `packages/`；flat 直接；但脱离 Vercel monorepo 惯例

**Recommend**: **(b) `apps/web/`** — 最简 dir 名 + 不承诺单 cert + Vercel monorepo 惯例匹配 + future multi-cert 走 data layer (D-089 DataSource interface 已支持 cert 切换)，不需多 app 实例。

---

**Propose-first micros for Step 1**（per Q4=d；user 可任 override）：

| Micro | Proposed value | 理由 |
|---|---|---|
| Package manager | **pnpm** | Vercel 默认 / monorepo workspaces 原生 / 比 npm 快 ~3x；Phase 1 Python 用 uv，TS 用 pnpm 不冲突 |
| Init command | `pnpm create next-app@latest <dir> --ts --tailwind --eslint --app --src-dir --import-alias "@/*"` | Next.js 15 官方 scaffold + TS (D-087 strict) + Tailwind for styling + ESLint + app router (D-087 SSR 配套) + `src/` dir + `@/*` alias |
| TS strict 配置 | `tsconfig.json`: `"strict": true` + `"noUncheckedIndexedAccess": true` | D-087 strict 锁 + 额外 strictness 防 array index `undefined` hole |
| Vercel deploy 方式 | `vercel CLI` 首次 project link + preview → 然后 git push 触发 auto preview | CLI 一次性 link，后续 git push 自动 deploy（per D-087 Vercel pattern） |
| pnpm workspace 文件 | root `pnpm-workspace.yaml` 加 `apps/*`（含 Q5 选定 dir） | monorepo TS apps/ + Python packages/extractor/ 共存；`uv.lock` + `pnpm-lock.yaml` 各自工具链 |
| Node version | `.nvmrc` = `20`（LTS） | Vercel 默认 Node 20；Next.js 15 要求 ≥18.18 |
| Step 1 evidence dump 内容 | `apps/<dir>/` 文件树 outline + `vercel_deploy_<sha>.log` + preview URL screenshot 占位 | D-091 §2.2 Vercel deploy log 类型 |

---

#### Q5 close gate

- **(α)** Q5 = a/b/c/d 任 1 + propose-first 全 ACK → Turn 3 Step 1 execute
- **(β)** Q5 = any + propose-first 某项 override → 列 override 哪项 + 新值 → Turn 3 修订后 execute
- **(γ)** 别的

回完 Q5 + ACK → Turn 3 Step 1 execute（预期 wall ~30-60 min for `create-next-app` + `vercel CLI` link + first preview deploy）。

---

### Turn 2 close — Round 2 答案

**User answer**: **Q5=b / propose-first 全 ACK**

| 选 | 含义 |
|---|---|
| Q5=b | Phase 2 web app dir = `apps/web/` 通用名；不承诺单 cert；最简 Vercel monorepo dir 命名 |
| propose ACK | pnpm + create-next-app flags + TS strict + Vercel CLI + workspace + Node 20 |

**Turn 3 起手 environment check**:
- Node v25.9.0 ✅ / npm 11.12.1 ✅
- pnpm ❌ NOT installed → `npm install -g pnpm`
- vercel CLI ❌ NOT installed → `npm install -g vercel`
- corepack ❌ NOT available → 走 npm global path
- `apps/` ✅ 不存在（create-next-app 会创建）
- root `package.json` / `pnpm-workspace.yaml` / `.nvmrc` ✅ 不存在

**Override propose-first 微调** (per Q4=d 自适应，user 可再 override): `.nvmrc` 从 `20` 改 **`22`** — Vercel serverless runtime 当前最高支持 22；user 机器 Node 25.9 + Vercel 22 → 用 22 居中；`.nvmrc` 22 让团队/CI 装版本明确。

---

### Turn 3 (2026-05-19) — Step 1 execute = scaffold + D-093 + amendments + Vercel deploy

**Frame**: Q5=b ACK + propose-first ACK → Step 1 完整执行链：

1. ✅ D-093 sub-ADR 写（amend D-082 / CLAUDE.md / AGENTS.md `apps/ reserved for Phase 3+` 措辞）
2. ✅ CLAUDE.md / AGENTS.md mirror amend
3. 工具链 install（pnpm + vercel CLI via `npm install -g`）
4. `pnpm create next-app@latest apps/web --ts --tailwind --eslint --app --src-dir --import-alias "@/*"`
5. `apps/web/tsconfig.json` 加 `"noUncheckedIndexedAccess": true`
6. root `pnpm-workspace.yaml`（含 `apps/*`）+ `.nvmrc` = `22`
7. Vercel CLI link + first preview deploy → preview URL（需 user `vercel login` 浏览器 OAuth；用 `! vercel login` 起 interactive）
8. Evidence dump `evidence/phase2/step_01_scaffold/`（tree_outline.md + vercel_deploy_<sha>.log + preview screenshot 占位）

### Turn 3 execute summary

**Execution actuals** (2026-05-19, wall ~25 min total):

| 阶段 | 内容 | 状态 |
|---|---|---|
| **D-093 sub-ADR write** | 158 行 ADR amend D-082 / CLAUDE.md / AGENTS.md `apps/ reserved for Phase 3+` → `apps/ for Phase 2+` + pnpm workspace 启用 + `apps/web/` location 锁 | ✅ LOCKED final Session 32 Turn 3 path α |
| **CLAUDE.md + AGENTS.md mirror amend** | line 94 同步更新 | ✅ |
| **工具链 install** | `npm install -g pnpm@latest vercel@latest` → pnpm 11.1.3 + vercel CLI 54.1.0（11s） | ✅ |
| **Root monorepo files** | `package.json` (private umbrella + packageManager pin) + `pnpm-workspace.yaml` (apps/* + allowBuilds sharp/unrs-resolver=true) + `.nvmrc` = 22 | ✅ |
| **Scaffold attempt 001** | `pnpm create next-app@latest apps/web ...` → **Next.js 16.2.6** ❌ 违 D-087 lock | ❌ FAIL → archive `failures/phase2/step_01_scaffold_attempt_001.md` (Rule B) |
| **Scaffold attempt 002** | `pnpm create next-app@15 apps/web ...` → **Next.js 15.5.18** + React 19.1.0 + TS 5.9.3 + Tailwind 4 + ESLint 9 | ✅ |
| **tsconfig strict additions** | `apps/web/tsconfig.json` 加 `noUncheckedIndexedAccess: true` | ✅ |
| **pnpm install + sharp/unrs-resolver fix** | root yaml `allowBuilds` 显式 true → install silent；darwin-arm64 prebuilt 二进制自动到位 | ✅ |
| **Local build smoke** | `pnpm --filter web build` → Compiled 1279ms / 5 static pages / 119 kB First Load JS | ✅ |
| **Vercel CLI OAuth** | `vercel whoami` 触发 device flow → user 浏览器 confirm → account `hakupao` ✅ | ✅ |
| **Vercel deploy** | `vercel deploy --yes` from `apps/web/` → build 52s on iad1 → READY | ✅ |
| **Deploy URL smoke** | `curl -sI https://web-mu-sandy-78.vercel.app/` → HTTP/2 200 / Content-Type text/html | ✅ |
| **Evidence dump** | `evidence/phase2/step_01_scaffold/{tree_outline.md, vercel_deploy_<sha>.log, step_01_audit.md}` + `failures/phase2/step_01_scaffold_attempt_001.md` (Rule B) | ✅ |
| **PLAN.md Step 1 row update** | `docs/phase2/PLAN.md` Step 1 ✅ DONE + URL + evidence refs + actual wall | ✅ |

**Vercel deployment 关键身份**:
- Deployment ID: `dpl_2JMygfKhYsAJnZZZdVskPAKSRSUy`
- Production URL: https://web-lifln1qfn-bojiangs-projects.vercel.app
- Aliased: **https://web-mu-sandy-78.vercel.app**
- Inspector: https://vercel.com/bojiangs-projects/web/2JMygfKhYsAJnZZZdVskPAKSRSUy
- Project: `bojiangs-projects/web` (orgId `team_TBX54Ug849En1mIVwZJLhK1N`, projectId `prj_MsfuRLtz6IdKTg1WvQNmv1lb33qE`)
- Target: production (Hobby tier first deploy default)
- 真 billed: **$0** (Hobby tier per D-091 §2.1 α-now path)

**5 Phase 2 specific evidence type 触发情况** (D-091 §2.2):
- ✅ **vercel_deploy_<sha>.log** — 第 1 个 specific type 落盘 (`step_01_scaffold/vercel_deploy_dpl_2JMygfKhYsAJnZZZdVskPAKSRSUy.log`)
- ⏸ cache_audit / lighthouse / e2e / ttft — 推 Step 4 / 14 / 15 / 7

---

### Turn 4 (2026-05-19) — Session 32 close + STATE sync + commit plan

**Sign-off Q11**: D-093 LOCKED final + Step 1 ✅ DONE → user terminal sign-off path α applied (本 turn 同 step user `Q5=b, propose ACK` 即 implicit sign-off for D-093 + Step 1 execute；与 Session 27~31 path α 一致)。

---

**Pre-close self-check** (per D-027 §5):

| 项 | 状态 | 证据 |
|---|---|---|
| D-093 ADR LOCKED final | ✅ | `docs/decisions/D-093-phase2-app-location.md` Status row + §7 sign-off |
| CLAUDE.md + AGENTS.md mirror amended | ✅ | L94 both files updated |
| Phase 2 PLAN.md written + Step 1 ✅ DONE row | ✅ | `docs/phase2/PLAN.md` (172 行) + Step 1 row updated post-deploy |
| `evidence/phase2/` scaffold + Step 1 evidence | ✅ | `README.md` + `step_01_scaffold/{tree_outline.md, vercel_deploy_<sha>.log, step_01_audit.md}` |
| `failures/phase2/` scaffold + Step 1 attempt 001 (Rule B) | ✅ | `README.md` + `step_01_scaffold_attempt_001.md` |
| Vercel preview URL live + smoke test | ✅ | https://web-mu-sandy-78.vercel.app/ → HTTP/2 200 |
| Local build green | ✅ | `pnpm --filter web build` ✓ Compiled |
| TS strict + noUncheckedIndexedAccess | ✅ | `apps/web/tsconfig.json` L7-8 |
| Session 32 log 4 turn 完整 | ✅ | Turn 1 (Round 1 4Q + ans Q1=c/Q2=g1/Q3=a/Q4=d) / Turn 2 (PLAN+scaffold+Q5) / Turn 3 (Q5=b ACK + execute) / Turn 4 (本段 close) |
| Rule A 抽检 | n/a | scaffold + ADR + evidence narrative；无 LLM rewrite > 50% |
| Rule B 失败归档 | ✅ | `failures/phase2/step_01_scaffold_attempt_001.md` (Next.js 16 version drift) |
| Rule C (Phase retro) | n/a | Phase 2 retro 等 Phase 2 收尾；Phase 1 retro 已 `RETROSPECTIVE.md` |
| Rule D Writer ≠ Reviewer | ✅ | Writer = main session；Reviewer = user terminal sign-off path α (per Q5=b ACK 即 Step 1 sign-off) |
| LLM cost actual | ✅ $0 真 billed | 本 step 无 LLM call；scaffold + Vercel deploy 全程 $0（Hobby tier） |
| Code change | ✅ apps/web/ + root monorepo files | `apps/web/`（Next.js 15 scaffold output）+ `package.json` + `pnpm-workspace.yaml` + `.nvmrc` + `pnpm-lock.yaml`；不动 `packages/extractor/` ✅ |

---

**STATE.md sync** (per D-027 §5):

- §当前阶段 row: "Phase 2 实施阶段 in progress / Step 1 ✅ DONE / preview URL live / 下一 = Step 2 DataSource interface"
- §最后更新 row: prepend Session 32 ✅ CLOSED entry
- §下一会话 row: "Session 33 = Step 2 DataSource interface + FsDataSource adapter + index.json v2 backfill"
- 已锁定决定数: 92 → **93** (D-093 added)
- OPEN OQ count: 3 (unchanged — Phase 2 design 0 OPEN OQ)

---

**Commit plan** (pending user "go commit" gate per CLAUDE.md hard rule):

Staged for commit (1 atomic):

| 文件 | 类型 | 行 |
|---|---|---|
| `docs/decisions/D-093-phase2-app-location.md` | new (LOCKED final) | 158 |
| `docs/phase2/PLAN.md` | new (Step 1 ✅ DONE) | ~174 |
| `docs/discussion/2026-05-19-session-32.md` | new (4 turn full log) | ~300 |
| `docs/STATE.md` | modified (4 anchors flip 设计→实施 + Session 32 close + Step 1 done) | ~10 lines change |
| `CLAUDE.md` | modified (L94 amend) | 1 line |
| `AGENTS.md` | modified (L94 amend) | 1 line |
| `evidence/phase2/README.md` | new | 75 |
| `evidence/phase2/step_01_scaffold/tree_outline.md` | new | ~65 |
| `evidence/phase2/step_01_scaffold/vercel_deploy_dpl_2JMygfKhYsAJnZZZdVskPAKSRSUy.log` | new | ~100 |
| `evidence/phase2/step_01_scaffold/step_01_audit.md` | new | ~80 |
| `failures/phase2/README.md` | new | 57 |
| `failures/phase2/step_01_scaffold_attempt_001.md` | new (Rule B) | ~55 |
| `package.json` | new (root monorepo umbrella) | 9 |
| `pnpm-workspace.yaml` | new (apps/* + allowBuilds) | 14 |
| `.nvmrc` | new | 1 |
| `pnpm-lock.yaml` | new (committed per pnpm reproducibility) | ~5000 |
| `apps/web/**` | new (Next.js 15 scaffold output) | ~200 source + ~300K node_modules lock entries（lock 已合并入 root） |

**NOT committed**:
- `apps/web/node_modules/` — gitignored
- `apps/web/.next/` — gitignored (Next.js build cache)
- `apps/web/.vercel/` — gitignored (Vercel project link, machine-local)
- `node_modules/` (root pnpm store) — gitignored
- `data/` — D-050 gitignored

Commit message style match D-083~D-092: `docs+code: D-093 LOCKED + Phase 2 实施 gate UNLOCKED + Step 1 DONE — apps/web Next.js 15.5.18 scaffold (TS strict + noUncheckedIndexedAccess + Tailwind 4 + ESLint 9 + app router + src/ + @/* alias) + monorepo (pnpm-workspace.yaml + package.json + .nvmrc=22) + Vercel deploy ✅ LIVE https://web-mu-sandy-78.vercel.app/ + D-082/CLAUDE.md/AGENTS.md apps/ reservation amend + PLAN.md (15-step Tier 3) + evidence/phase2 + failures/phase2 + Session 32 (4 turn brainstorm; γ no LLM cost; $0 真 billed Hobby; wall ~25 min)`

Push: pending user "push it" gate per Session 27~31 pattern。

---

**Session 32 ✅ CLOSED** 2026-05-19 — 4 turn 完整链:

1. Turn 1 Round 1 4Q meta-framing + ans (Q1=c full Step 1 / Q2=g1 mid PLAN / Q3=a Phase 1 同构 / Q4=d 自适应 slow-pace)
2. Turn 2 PLAN.md + evidence/failures scaffold + Round 2 Q5 (D-NNN-worthy repo location) + propose-first micros for Step 1
3. Turn 3 Q5=b ACK + propose-first ACK → D-093 LOCKED + amendments + scaffold attempt 001 FAIL (Next.js 16 drift, Rule B archive) → attempt 002 PASS (Next.js 15.5.18) → Vercel deploy ✅ LIVE @ https://web-mu-sandy-78.vercel.app/
4. Turn 4 Session close + pre-close self-check + STATE sync + commit plan

LLM cost actual: **$0 真 billed** / wall ~25 min total session (scaffold + Vercel deploy 全程 Hobby) → 下一动作 = **Session 33 = Step 2 DataSource interface + FsDataSource adapter + index.json v2 backfill** (per PLAN.md §1 Module A Step 2)。
