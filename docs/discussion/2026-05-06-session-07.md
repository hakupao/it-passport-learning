# Session 07 — Phase 1 实施起手 (Step 0~?)

| 字段 | 值 |
|---|---|
| Session 编号 | 07 |
| 日期 | 2026-05-06 |
| 上一场 | `2026-05-06-session-06.md` (closed) |
| 起手 Topic | **Phase 1 实施起手** ★ — 设计阶段 → 实施阶段切换 + 包骨架 + 核心代码 |
| 节奏 | **实施阶段（写代码允许）** — slow-pace 3a 不再适用，但 Tier 3 evidence 文化保留 |
| Tier | Tier 3 |
| 阶段 | **实施阶段 (Phase 1)** ★ |
| 起始累计 D | D-001 ~ D-073 (73 条已锁) |
| 起始 OQ open | 3（OQ-01/02/05） |
| 状态 | **In Progress** — 阶段已切，Step 0/1/2 完成，Step 3+ 进行中 |

---

## 0. Session Goal

Phase 1 实施起手。**设计阶段彻底 ship → 实施阶段开工**。

按 D-073 三阶段启动 + user "全部按你的提议" 委托:
- Step 0: baseline commit + push (design 产物 ship 到 git)
- Step 1: branch 选择（main 直走，per user）
- Step 2: STATE.md 切阶段（设计 → 实施）
- Step 3: 实化 `packages/extractor/` 骨架（pyproject.toml + 目录结构）
- Step 4: 实化核心代码（schemas / plugin loader / 4 axis interfaces / 内置 plugin stubs）
- Step 5: dry-run 单 chapter (per D-073 Stage A) — **需 user 开门 LLM API call**

User 指令: "你就先一直继续做，直到遇到必须我确认的问题，再停下"

→ 自然停点 = Step 5 真触发 LLM API call 之前（per 项目 CLAUDE.md "任何 LLM 实现 / 真实 API 调用必须 user 开门"）。

---

## 1. 起始状态 / Inherited State

- 累计 73 D 已锁
- Topic #1~#7 全闭合
- 7 独立 ADR 落盘
- baseline commit `6d4035c` (Session 03-06 全部 design 产物)
- phase switch commit `c6c3660` (设计 → 实施)
- macOS Keychain 含 Claude Code-credentials (per D-069)
- IT-Passport.epub 244MB 在 repo 根 (gitignored)
- uv 0.11.8 / Python 系统 3.9.6 (uv 会 fetch 3.11+)

---

## 2. 本场会话锁定的决定 / Decisions Locked

> Phase 1 实施阶段一般**不**新锁 D（D 是设计决定）；如实施期发现需要新决定（如 prompt template 选择 / 性能优化路径），再回头锁。

---

## 3. 待解决问题 / Open Questions

继承自前场 3 OQ（不变）:

| OQ | 议题 | 归 Topic |
|---|---|---|
| OQ-01 | Phase 1 实际要支持的源类型优先级（v1 后） | Topic #1 收尾后 |
| OQ-02 | OCR 引擎抽象的更细颗粒度 | Topic #1 后续 |
| OQ-05 | A/B/C 三个 Phase 的具体形态 + 启动顺序 | Topic #8 |

---

## 4. 实施进度 / Implementation Progress

### Step 0 ✅ — Baseline commit + push

- Commit `6d4035c`: 12 files / 4389 insertions (Session 03-06 design 产物)
- Push to origin/main (`37b19b2..6d4035c`)
- Files: 4 session logs + 7 ADR + STATE.md update

### Step 1 ✅ — Branch 选择 (a) 直接 main

Per user "全部按你的提议"，未建 phase1-implementation branch，直接 main 写。

### Step 2 ✅ — STATE.md 切阶段标记

- STATE.md 头部 `当前阶段` 改 "设计阶段" → "实施阶段 (Phase 1)"
- Phase 1 状态字段反映 Step 进度
- Commit `c6c3660`: 1 file / +12/-8
- Push to origin/main (`6d4035c..c6c3660`)

### Step 3 ✅ — 实化 `packages/extractor/` 骨架

Commit `bd6b0c7` (16 files / 1559 insertions):
- root `pyproject.toml` (uv workspace umbrella, pytest markers, ruff, mypy)
- `packages/extractor/pyproject.toml` (cert-extractor 0.1.0, hatchling, runtime deps)
- `src/cert_extractor/__init__.py` (`__version__` + `SCHEMA_VERSION` + `UNTRANSLATED`)
- 4 axis subdirs + tests/{unit,integration,e2e,_fixtures}/ + conftest.py
- `uv.lock` (58 packages resolved, committed per D-039)

Verified: `uv sync --all-packages` clean; uv-managed Python 3.14.4.

### Step 4 ✅ — 核心模块（schemas / plugins / audit / budget）

Commit `841f5b9` (25 files / 1722 insertions, 77 tests pass):
- `schema/`: `Trilingual` / `KanaHelper` / `Anchor` / 6 entity types via Discriminated Union / `Envelope` with UNTRANSLATED leak validator (D-054 / D-012 / D-022 / D-056 / D-058 / D-055)
- `plugins/base.py`: `BasePlugin` ABC + 4 axis ABCs + `@register_<axis>` + metadata/conflict validation (D-021 / D-025 / D-067 / D-068)
- `plugins/loader.py`: `PluginLoader` with decorator-scan + entry_points stub + min_version (D-064 / D-065 / D-066)
- `audit/verdict.py`: 3-tier `Verdict` + safety-field veto (D-063)
- `audit/reviewer.py`: stage→reviewer mapping + promotion/demotion (D-061)
- `budget/monitor.py`: `BudgetMonitor` soft/hard cap + anti-mistake clamp (D-071)
- `budget/cost.py`: `CostFile` / `CostTracker` per-stage rollup + run summary (D-072)
- `cli.py`: click stub (--version)

**D-071 v1.1 revision (concurrent in same commit)**: anti-mistake clamp ×5 → ×10 because default `anthropic_usd: soft=$5 / hard=$30` ratio = 6 (preserves D-069 max plan → API key upgrade headroom). ×5 clamp would have rejected shipped defaults.

### Step 4b ✅ — 内置 source/OCR plugins + dry-run CLI

Commit `5c2251c` (9 files / 792 insertions, 92 tests pass):
- ABCs: `OCRResult` / `TranslationResult` / `PageList` dataclasses; signature update for clean budget integration
- `plugins/source/epub_image.py`: zipfile-based EPUB image extraction (no extra deps)
- `plugins/ocr/mistral.py`: Mistral OCR via official SDK; lazy client init; default $1/1k pages
- `pipeline/runner.py`: `Stage0_1DryRunner` (D-073 Stage A subset = D-070 stage 0+1 only)
- `cli.py`: `cert-extractor dry-run --source <epub> --page-limit N` 安全门:
  - 无 `--confirm` → 打印 plan + cost estimate + 安全 abort
  - 有 `--confirm` 但无 `MISTRAL_API_KEY` → exit 2
  - 有 `--confirm` + key → 真实 API call

**Verified**:
- `pytest`: 92 passed in 0.12s
- `cert-extractor --version`: 正常
- `cert-extractor dry-run --source IT-Passport.epub --page-limit 5`: 打印 plan，安全 abort（无 --confirm）

---

## 5. What's Next

**当前位置**: Step 4b 完成 + 安全门验证。**Step 5 dry-run 是 user 开门点**。

待执行（user 决定）:

```bash
# Step 5: 真实 dry-run（spend ~$0.05 + max plan quota）
export MISTRAL_API_KEY=<your-key>
uv run --package cert-extractor cert-extractor dry-run \
    --source IT-Passport.epub \
    --cert-id itpassport_r6 \
    --page-limit 50 \
    --confirm
```

完成后:
- 生成 `data/itpassport_r6/runs/dry_run_<ts>/raw/pages/page_*.jpg`
- 生成 `data/itpassport_r6/runs/dry_run_<ts>/ocr/page_*.md`
- 生成 `data/itpassport_r6/runs/dry_run_<ts>/cost.json`
- User retro: 看 OCR quality (per D-073 Stage B)
- PASS → Step 6 (stage 2-7 实施 + 全本)
- WARN/FAIL → 调 prompt / 切 Hard Re-OCR / 重 dry-run

**严格遵守**:
- 仍按规则 A/B/C/D（实施期 audit 等 stage 4-6 实化时启用）
- `data/` gitignore (per D-050)
- failures/ 不删

---

### Step 5 ✅ — Dry-run 50 pages (Stage A) + User retro (Stage B) PASS

**第一次尝试 — 全 fail**:
- 50 pages unpack OK，但 10/10 OCR 调用失败 → soft cap WARN halt
- Runner 把 error 当 string 存内存里，没打印
- 手动 reproduce: `RuntimeError: mistralai package is not installed`（实际是 import 错）
- 根因: `from mistralai import Mistral` 错误 — mistralai v2.4.4 是 namespace package，`Mistral` 类在 `mistralai.client`
- 修复 commit `140ce34`: `from mistralai.client import Mistral`

**第二次尝试 — 全成功**:
| 指标 | 实际 | 评价 |
|---|---|---|
| Pages unpacked | 579 / 579 | EPUB 总页数 |
| Pages OCR'd | **50 / 50** | 0 failure |
| Cost | **$0.0500** | 完全符合 D-005 Scale plan 估算 |
| Wall-time | 97.8s | 1.96s/page，远低于 D-071 soft 7200s |

**Stage B User retro** — PASS（详见 `evidence/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/step_01_audit.md`）:

3 samples 检查（page_001 封面 / page_010 目录 / page_030 正文）:
- ✅ 日文 OCR (汉字 + 平假名 + 片假名) 准确
- ✅ 章节编号 + 页码 + 缩进结构 100% 保留 → stage 4 Structure 抽取的绝佳输入
- ✅ 标题层级、段落、内文引用 (p.15)、图片 markdown 引用 全部保留
- ⚠️ 小问题: `&amp;` 未转回 `&` (stage 5 preprocessing 修)；偶尔单日期被表格化（语义无影响）；runner 没把 failure detail 打印（Step 6 修）

→ **Verdict: PASS** → 进 Step 6 (stage 2-7 实施 + 全本 ~$0.58)

### Step 6 ⏸ — Stage 2-7 实施 + 全本（等 user 开门）

**预期工作量**:
- 实化 stage 2 page classify (Claude Sonnet via Agent SDK)
- 实化 stage 3 hard re-OCR 条件触发 (Claude Vision 1M ctx)
- 实化 stage 4 Structure 抽取 (Claude → Pydantic Discriminated Union per D-056)
- 实化 stage 4.5 Glossary 抽取 + 锁定
- 实化 stage 5 Translate (Claude, glossary-constrained)
- 实化 stage 6 Audit 抽检 (per D-060/D-061/D-063 — reviewer LLM)
- 实化 stage 7 Export (envelope per D-058 + JSON/Markdown/SQLite exporter per D-021)
- 跑全本 579 pages dry_run → 确认 → 全本正式 run → GitHub Release (per D-046)

**预算估算（全本）**:
- Mistral OCR 全本: ~$0.58 (579 × $0.001)
- Anthropic 走 max 20 plan = $0（quota 有限制 5h window）
- Wall-time 估算: stage 1 OCR 全本 ~20 min + stage 2-7 ~更多
- **D-071 软 cap 2h wall-time** 是真实限制 — 可能撞，需 user 在线等 WARN 决策

**应在 user 不需要 Claude Code 的时段启动**（max plan quota 共享）

---

## 6. 关 session 总结（暂未关 — Session 07 mid，等 Step 6 全本完成后再关）

> 本 session 仍 in-progress。Step 5 PASS 是关键里程碑（**Phase 1 OCR 路径已验证可行**）。Step 6 是大工作量 session，建议 user 准备好时段后再开。

### 当前状态 snapshot

- 8 commits (`d50432e` → `140ce34`)
- 92 unit tests pass
- 50 pages OCR 真实数据落盘
- Step 5 evidence 文件已 commit (per Tier 3 + Rule A)
- 累计成本: $0.05 (Mistral) + $0 (Anthropic, max plan)

---

## 9. 配套产物 / Artifacts

- 本文件: `docs/discussion/2026-05-06-session-07.md`
- 上一场: `docs/discussion/2026-05-06-session-06.md`
- Live state: `docs/STATE.md`
- 7 ADR + 4 session logs
- baseline `6d4035c` / phase switch `c6c3660`

---

## 10. 关键原话 / Key Verbatim Quotes

> User (Session 07 起手 ★): "先 phase1 吧，因为是完全隔离的对吗"
>
> Claude 响应: 4 维分析隔离边界（架构 / 数据 / 环境 ✅，LLM quota 共享 ⚠️，git 默认在 main ⚠️）；提议 Step 0-5 起手 plan。

> User (执行确认 ★): "全部按你的提议"
>
> Claude 响应: Step 0 baseline commit + push (`6d4035c`) → Step 1 main branch → Step 2 切阶段 + commit (`c6c3660`)。

> User (持续执行 ★): "你就先一直继续做，直到遇到必须我确认的问题，再停下"
>
> Claude 响应: 自然停点 = Step 5 真 LLM API call 之前；继续 Step 3-4 实施。

---

> 本文件 append-only。
