# Session 101 — 2026-06-27

启动词: **「OCR 清理」** (= Session-100 What's Next #1 + #3: 決定的 detector backlog の drift-proof 一括清理 + push)。
Effort: ultracode (xhigh + workflow orchestration)。

## ゴール

Session-100 が確立した決定的 OCR-garble detector (`scripts/quiz-ocr-garble-scan.mjs`) の全 2900 backlog
(74 candidate hit / 55 問) を **drift-proof で一括清理**する。high-confidence 機械的クラスは bulk、
medium (ascii_period) は per-case source-verify、再 scan で残=0 (FP のみ) を確認。zh/en 既正・correct_answer 不変。

## 方針 (D-140 scale 執行、ultracode = 写審分離を最大化)

S100 の重大所見「単一 LLM の cosmetic-garble 検出は非決定的 (Run2≠Run3)」を踏まえ、**決定的 detector が見つけた
backlog** を 主 context が裁決 → **ブラインド独立再導出 Workflow** で敵対的検証 → drift-proof 適用、の三段。
Writer (主 context 裁決) ≠ Reviewer (workflow の独立 derive/FP agents) ≠ 最終裁決 (主 context source 実読) = Rule D。

## 実行ログ

### 1. 全 74 hit の 主 context 裁決

backlog 全件の実値 (raw choices_jp/stem_jp + zh/en 訳) を抽出し裁決。**表示元の確認が鍵** (`quizModel.ts:117`):
stem は `stem_jp_clean` (翻訳サイドカー) 優先表示 → 全29回翻訳済ゆえ **raw stem_jp は非表示**。choices は
`choices_jp` (raw bank) が表示。よって実修正は全て choices フィールド。

- **本物の garble = 58 choices_jp フィールド** (zero_in_alpha 33 / ascii_period_in_jp 15 / trailing_junk 5 / page_marker 4 / period_comma 1)
- **FALSE POSITIVE 15 hit / 7 問**:
  - `2023r05-q063` (stem+4択) = **RAID0/RAID1** は実在 RAID レベル (zh/en も RAID0/RAID1 保持)
  - `2014h26a-q089` (4択) = **ESSID `A0B1C2D3E4`** は正当な英数字識別子
  - stem 6件 (`q044`/`q074`/`q009`/`q087`/`q010`) = 図ボックス `[　　]`/`[Webサーバ]`/`[網掛け]`・IPA リスト全角整形 (`stem_jp_clean` で表示既正)

**source 画像で裁決した content 削除・曖昧末尾** (q052 protocol):
- `2014h26h-q095` エ: 末尾 `[マネジメント]` = 次問 `〔マネジメント〕問96` の見出しが OCR で漏れ込み (page-43) → strip
- `2022r04-q010` エ: page-05 で `…相互に許諾すること` で終わる (末尾 `に` も junk) → `にーー 5 =三` strip
- `2022r04-q069` エ: page-31 で `…次々と転送されること` で終わる、`対象に，`(全角comma) → `対象に.`→`対象に,`+`こ 8` strip

裁決結果は単一真相源 `evidence/.../ocr_cleanup_fixes_S101.json` に生成 (`to` のみ手起こし、from/zh/en/page は
データから自動取得 = whitespace 転記ミス 0)。scripts: `quiz-ocr-cleanup-S101.fixes.mjs`。

### 2. ブラインド独立再導出 Workflow (`wf_d188cc77-95b`、65 agent / 2.0M tok / ~5分)

`scripts/_gen-ocr-verify-workflow-S101.mjs` が blind データを埋め込んだ `quiz-ocr-verify-S101.workflow.mjs` を生成。
- **Derive (58)**: 各 agent は **私の `to` を一切見ず**、garbled JP + zh/en + source page path のみから clean_jp を独立導出 (source 画像 Read 推奨)。workflow が句読点/空白正規化して私の `to` と照合、不一致のみ返す。
- **FP-confirm (7)**: 各 agent が FP 群を独立判定 (garble か legitimate か)。

**結果: derive 57 一致 / 1 disagree、FP 7/7 = false_positive 確認 / 0 disputed、low-confidence 0**。

### 3. 唯一の disagreement が 主 context の誤りを捕捉 — `2020r02o-q096` ウ

- 私の `to`: `ISO0`→**`OSI`** (zh/en が「OSI (Open Source Initiative)」と言うのに引きずられた)
- 独立 derive agent: page-44 高倍率 crop を実読し「source は文字通り **`ISO`**、spurious 0 のみ」→ `ISO`。「訳に合わせて source を改変するな」と明記。
- **主 context が page-44 を再度高倍率 crop 実読** (`q96_acr2.png`): acronym は明確に **I-S-O** (セリフ I / S / 丸い O)。stem 自体が `Open`→`0pen` と O→0 誤読しており、字順 `ISO0` = I,S,O+phantom 0 (OSI なら `0SI` になるはず)。
- **訂正: `ISO0`→`ISO`** (spurious 0 のみ除去、ISO 保持)。当初の `OSI` は翻訳側の意味修正を source 転写に混入させる fidelity 違反だった。
- **翻訳 fidelity backlog (別軸)**: zh/en が source の `ISO`→`OSI` と黙って意味修正。distractor (正解 ア) ゆえ正解無影響だが、訳が source acronym と乖離 → 翻訳トラックへ申し送り。JP は source 通り `ISO`。

→ q096 訂正後、**58/58 が独立再導出と一致**。

### 4. drift-proof 適用 + 再生成 + 再 scan

`scripts/quiz-ocr-cleanup-S101.mjs` (full-field assert-replace、atomic = 全 from 検証通過後のみ書込) → raw bank
58 フィールド適用 → `build-quiz-corpus.mjs` 再生成。

- **questions.json diff = 58+/58−、全て choice 値 (ア/イ/ウ/エ)**、`correct_answer` 0変更 (全 2900 git 多重集合確証)、`stem_jp`/`id`/`has_figure` 等 diff 0、**quiz_index.json + translations/ 不変**。
- **再 scan = 15 hit / 7 問 = 文書化 FP 集合と完全一致** (real garble 残 0)。

### 5. 検証 (全 GREEN)

- tsc `--noEmit` exit 0
- eslint 0 error (既存 warning 1 = RetroGlossary tTerm)
- vitest **463 passed / 2 skipped** (30 files)
- next build exit 0・Compiled successfully・27/27 static pages・quiz routes (/ja|zh|en/quiz) build
- **nft IPA-exams leak 0** (quiz route + api/quiz/explain trace に data/ip/{exams,sources,syllabus} 参照 0)

## 成果物

- scripts: `quiz-ocr-cleanup-S101.fixes.mjs` (fix 真相源生成) / `quiz-ocr-cleanup-S101.mjs` (drift-proof applier) / `_gen-ocr-verify-workflow-S101.mjs` + `quiz-ocr-verify-S101.workflow.mjs` (検証 workflow)
- evidence `evidence/phase5/stage_06_quiz_ocr_cleanup/`: `adjudication_S101.md` / `ocr_cleanup_fixes_S101.json` / `verify_workflow_result_S101.json` / `README.md`
- data: `data/ip/quiz/questions.json` (58 choice 是正) + raw bank (gitignored、同期編集)

## What's Next

1. **push** (ユーザー確認ゲート) — S101 commit を origin/main へ。`origin/main..main` は現在 0 (前 session 分 push 済) ゆえ S101 commit のみ。公開 repo (IPA 派生) ゆえ CLAUDE.md hard rule で要承認。
2. **Phase 2 scale** (D-137-D「续批」) — 28 回の解説生成。各回 prep→generate.workflow→generate_result 永続化→merge→Rule A。cosmetic OCR は detector で網羅清理済ゆえ解説は腐敗非依存で進行可。
3. **翻訳 fidelity backlog** (低優先、別軸) — `2020r02o-q096` ウ zh/en=OSI vs source=ISO (distractor、正解無影響)。

## Pre-close self-check (D-027 §5)

- **D 新規**: なし (D-140 の scale 執行。phase 境界でも不可逆決定でもない)。锁定决策 = 140 据え置き。
- **OQ**: 新規なし。S100 の「cosmetic OCR scale 検出方法」gate は本 session で決定的 detector + 主 context source-verify として執行済 (クローズ相当、新 OQ-ID 不要)。
- **invariants 確認**: questions.json diff = choice 値 58+/58− のみ、**correct_answer 0変更** (git 多重集合確証)、quiz_index/translations/stem 不変、再 scan = FP のみ。raw bank (gitignored) 同期。
- **Rule A/D**: 裁決 (主 context) ≠ 独立 derive/FP agents (workflow、blind) ≠ 最終裁決 (主 context source 実読) = 三段独立。独立再導出 58/58 一致 + FP 7/7 confirmed。q096 で独立検証が主 context の誤りを実捕捉。
- **Rule B**: 失敗 attempt = q096 の主 context 初回誤判 (`ISO0→OSI`、zh/en anchor) を独立 derive + source 実読で捕捉・訂正。adjudication_S101.md §Verification workflow result に記録 (是正は drift-proof で 1 発、未コミット誤値は questions.json に未到達)。
- **STATE sync**: 最后更新 / 当前阶段 / 次セッション 更新済。本ログ記録済。MEMORY fix-checklist 更新予定。
- **commit/push**: commit はユーザーの routing 内 (clean-up)。**push はユーザー確認ゲート** — 本 session では commit まで、push は確認後。
