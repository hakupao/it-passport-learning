# Session 16 — 2026-05-12

> Phase 1 Step 6.11.D.3 — Stage 2 page-classify + Stage 3 hard-page
> re-OCR + Stage 4 structure extraction over the 579-page run produced
> in Session 15, then evaluate Gate ② per D-079 §2.1 row 2.
> Continues from Session 15 close at HEAD `a10cd53` (6.11.D.2 ✅
> Gate ① PASS, 474/474 unit tests, 81 D locked, 3 OQ open).

---

## 0. Session Goal

Authorize and execute **Step 6.11.D.3** end-to-end:

1. Stage 2 — page classification on all 579 OCR markdown files
2. Stage 3 — hard-page re-OCR on heuristic-flagged pages
3. Stage 4 — structure extraction → entities
4. Evaluate Gate ② via `check_gate_2_post_structure()`
5. Emit `gate_2_<ts>.json` checkpoint on PASS

---

## 1. Inherited State

- HEAD: `a10cd53` (Session 15 close)
- Branch: `main`, clean entering, **0 commits ahead of origin/main**
- Unit tests: **474/474** pass entering
- Step 0~5 ✅; Step 6.0~6.10 ✅; Step 6.11.A/B/C/D.1/D.2 ✅
- Locked decisions: **81** (D-001 ~ D-081)
- 3 OQ open (Phase 1 out-of-scope)
- Cumulative cost going in: **Mistral $0.629 / Anthropic $0** billed

---

## 2. Decisions Locked (this session)

_(no new ADR; one minor decision recorded)_

### Minor decisions (in-session, not D-numbered)

- **Tier escalation Sonnet → Opus 4.7 for full D.3 stack.** Triggered by
  Stage 2 sonnet soft-cap halt at $5.03 / 90 pages → user opted Option B
  ("$100 cap, opus 4.7"); after I surfaced opus pricing reality
  (~5× sonnet → ~$160 stage 2 alone), user re-opted to **soft $300 /
  hard $600**, full D.3 stack opus. This is a budget-frame escalation,
  not an architectural decision — caps are knobs (D-071), tier is per
  D-061 / D-069. Recorded for Session 17 cost reasoning.

---

## 3. Open Questions

继承 OQ-01 / OQ-02 / OQ-05（Phase 1 范围外）。无新增 OQ。

---

## 4. Execution log — Step 6.11.D.3

### 4.1 Stage 2 page-classify (multi-attempt narrative)

| Attempt | tier | caps | result | notes |
|---|---|---|---|---|
| #1 | sonnet | default ($5 soft / $30 hard) | halt @ 90/579, $5.03 | hit anthropic_usd soft cap exactly; classified the front 90 pages with sonnet |
| #2 | opus | $300 soft / $600 hard | halt @ 472/579, fail_count=10 | hit fail_count soft cap from 10 SDK reader errors caused by **5h max-plan quota throttle** (user later confirmed) |
| #3 | opus, **+skip-existing**, **+fail-count-soft 50 / hard 100** | as above | **PASS** 107/107 of remaining, 0 fail | user 5h window had reset; clean exit; 579/579 pages classified |

Final by-label distribution (all 579 opus-classified):

```
content       430
exam          107
chapter_title  17
index          11
cover           4
other           4
toc             4
glossary        2
TOTAL         579
```

Cost rolled up at end of Stage 2: **anthropic shadow $112.16** / $0
billed; mistral unchanged at $0.579.

**Code changes to enable resume** (CLI gap discovered mid-run):

- `cli.py` — extended `_build_monitor` to accept `fail_count_soft/hard`;
  added `--anthropic-soft-usd / --anthropic-hard-usd / --fail-count-soft
  / --fail-count-hard / --skip-existing` flags to `classify-pages`
  (mirrors hard-reocr / extract-structure pattern with one new pair).
- `pipeline/stage2_classify.py` — added `skip_existing: bool = False`
  param to `Stage2PageClassifier.run`; short-circuits pages with
  existing `classified/page_NNN.json`.
- `tests/unit/test_pipeline_stage2_classify.py` — new
  `test_stage2_runner_skip_existing_does_not_call_classifier_for_done_pages`
  (suite 474 → 475).

### 4.2 Stage 3 hard-reocr (single attempt)

- Heuristic: 42 pages flagged via `repetition_max >= 8` (window=8)
- Tier: opus Vision
- Caps: $300 soft / $600 hard / fail-count 50/100
- Result: **PASS** 42/42 re-OCR'd, 0 fail, verdict=None
- Wall: ~15 min
- Cost this stage: **$9.77 shadow** ($0.23/page — 5× cheaper than my
  estimate, opus Vision pricing closer to opus text than expected)
- Cumulative anthropic: $121.93 shadow / $0 billed

### 4.3 Stage 4 structure (multi-attempt narrative)

| Attempt | result | added | notes |
|---|---|---|---|
| #1 | halt @ 72 processed / 11 skipped, fail=5, **wall_time soft cap** | 72 | Cumulative wall hit 7203s ≥ 7200s default soft (2h); cost $13.90 |
| #2 (after adding `--wall-time-soft/hard` flags) | halt @ 400 / 83 / fail=80 cumulative-50 | +400 | Hit fail_count_soft=50 again from another **5h max-plan quota throttle** |
| #3 (bumped fail-count caps to 200/500) | **PASS** 82 / 497 / fail=4, verdict=None | +82 | Clean exit; remaining 13 plus auto-skipped-by-label pages |

Final structured/ count: **554/579** (the missing 25 = `cover (4) +
toc (4) + glossary (2) + index (11) + other (4) = 25` pages
auto-skipped-by-label per Stage 4 routing, by design).

Cumulative entities: **2226** across the 554 files
(`chapter / section / term / question / table / figure` all 6 types
present).

Stage 4 totals: **$107.54 shadow** ($0.19/page average), 554 calls.

**Code changes to enable resume** (CLI gap discovered mid-run):

- `cli.py` — extended `_build_monitor` to accept `wall_time_soft/hard`;
  added `--wall-time-soft / --wall-time-hard` flags to all 3 stage
  commands (classify-pages / hard-reocr / extract-structure) for
  consistency. Suite confirmed unchanged (475 passed).

### 4.4 Gate ② evaluation

#### 4.4.1 First evaluation — checker schema bug (FAIL, code defect)

```python
check_gate_2_post_structure(
    structured_dir=run_dir / "structured",
    expected_entity_count=2500,
    count_tolerance=0.20,
)
# AttributeError: 'list' object has no attribute 'get'
#   at halt_criteria.py:108  → entities = payload.get("entities", [])
```

Root cause: B.3 checker reads `payload.get("entities", [])` assuming a
dict; real Stage 4 emitter (`stage4_structure.py`) writes a top-level
JSON array. Same class as the Stage 1 / Gate ① checker bug fixed in
Session 15. Patched checker to handle both shapes (list-first, dict
fallback) plus added a realistic top-level-list fixture test:

- `pipeline/halt_criteria.py:100-117` — list / dict / unexpected-type triage
- `tests/unit/test_pipeline_halt_criteria.py` — new
  `test_gate_2_reads_top_level_list_schema_from_real_structured_files`
  (suite 475 → 476)

Failure archive: `failures/stage4_structure/stage4-2026-05-12-gate2-checker-schema.md`.

#### 4.4.2 Second evaluation — substantive D-076 envelope FAIL

```
Gate 2 passed = False
reasons = ("answer_index == -1 on 14 page(s): ['page_043.json',
  'page_064.json', 'page_065.json', 'page_103.json',
  'page_134.json']...",)
```

14 pages emit `question.answer_index = -1` (D-076 sentinel). All 14
are pages where Mistral OCR (Stage 1) did not preserve the
answer-key line, and **none of the 14 were flagged by Stage 3
hard-reocr heuristic** (intersection with the 42 heuristic-flagged set
= ∅). This reproduces F-MISTRAL-ANSWER-LINE-LOSS, the deferred
Phase-2 polish item from Session 09b — including `page_043` (the same
page that originally triggered the D-076 design in Session 09b).

Sentinel emission is **per D-076 design** (Stage 4 should emit -1
explicitly rather than silently default to 0); Gate ② is the
architectural safety net catching it before glossary / translation /
export. Cumulative entity count 2226 is well within the
`expected_entity_count=2500 ± 20%` band — only the answer_index check
fires.

Failure archive: `failures/stage4_structure/stage4-2026-05-12-d076-envelope-14-pages.md`.

The 14 pages: `043 / 064 / 065 / 103 / 134 / 154 / 180 / 181 / 182 /
260 / 300 / 416 / 503 / 525`.

---

## 5. Failure archive (per Rule B)

New files:
- `failures/stage4_structure/stage4-2026-05-12-gate2-checker-schema.md`
  (gate-checker bug — closed by patch in §4.4.1)
- `failures/stage4_structure/stage4-2026-05-12-d076-envelope-14-pages.md`
  (substantive Gate ② FAIL — open, awaiting user gate)

The intermediate operational halts (Stage 2 attempt #1 cap, Stage 2
attempt #2 fail-cap, Stage 4 attempt #1 wall-cap, Stage 4 attempt #2
fail-cap) are NOT independently archived — they were correct
halt-by-design behaviors that surfaced CLI flag gaps which are now
filled. Their narrative lives in §4 above.

---

## 6. Cumulative cost ledger (post Step 6.11.D.3 attempt 3, pre-Gate-② resolution)

| Metric | Value |
|---|---|
| Mistral billed (real) | $0.629 (unchanged) |
| Anthropic billed (real) | **$0** (max-plan OAuth — D-069 holds) |
| Anthropic shadow (informational) | **$229.47** (Stage 1 ~$0; Stage 2 $112.16; Stage 3 $9.77; Stage 4 $107.54) |
| Cumulative wall_time | 11748 s ≈ 3h 16m |
| Cumulative fail_count | 50 (across all stages — primarily 5h-quota throttle errors, not data defects) |
| Mistral budget headroom (soft $5 / hard $20) | $4.37 / $19.37 |
| Anthropic D-track remaining (est, opus) | D.4 glossary ~$15, D.5 translation ~$50-150, D.6 audit ~$30 — total D-track est $325-435 shadow / $0 billed |

---

## 7. Memory updates

No new memory files. The 5h max-plan quota throttle behavior surfacing
as `fail_count` increments rather than a distinct halt code is an
operational observation (not a user-preference signal); it could
become a future code/UX improvement (distinguish quota-throttle from
data-failure) but does not warrant memory.

---

## 8. Pre-close self-check (D-027 §5)

| Check | Status |
|---|---|
| D-NNN: any new? | None |
| OQ: any new? | None (3 inherited stay open) |
| STATE.md sync | ✅ this turn (top table + §5 6.11.D.3 → ✅ stages PASS, Gate ② FAIL pending user) |
| Session log on disk | ✅ this file |
| ADR files on disk | n/a (no new ADR) |
| Memory updates | none required |
| Code | ✅ multi-file: `pipeline/halt_criteria.py` (Gate ② schema fix), `pipeline/stage2_classify.py` (skip_existing), `cli.py` (cap-override flags + skip-existing wiring across 3 stages), 2 new tests |
| Tests | 474 → 476 (+2: skip_existing + Gate ② list-schema fixture) |
| Evidence | ⏸ `evidence/.../step_06_11_D3.md` not yet written — recommend next session before Gate ② resolution to lock the run state on paper |
| Failures | ✅ 2 archives (Gate ② checker bug + D-076 envelope 14-page violations) |
| Commits | ⏸ user gate (recommended: one atomic commit covering all CLI/runner/checker patches + 2 failure archives + this session log + STATE) |

---

## 9. What's Next

**Step 6.11.D.3 status: 4 of 4 sub-stages dispatched and completed
cleanly; Gate ② FAIL on substantive D-076 envelope violation across
14 pages.** This is a hard halt per D-073 + D-079 (Gate ② FAIL =
escalate to user, no autonomous proceed).

User decision points:

- **(α) Commit Session 16 changes** — recommended: one atomic commit
  covering halt_criteria patch, stage2_classify skip_existing, cli
  cap-override flags (3 stages × 4 new flag pairs), 2 new tests, 2
  failure archives, this session log, STATE sync.
- **(β) Pick D-076 remediation path** for the 14 violation pages — see
  failure file `stage4-2026-05-12-d076-envelope-14-pages.md`
  §"Remediation options":
  - **(β.1)** Force Stage 3 Vision opus on the 14 pages, then re-run
    Stage 4 → re-fire Gate ②. Est ~$6 shadow.
  - **(β.2)** Hand-edit answer_index per page (Plan-B-style, mirrors
    Session 09b page_022 flow). $0.
  - **(β.3)** Add a `missing_answer_line` detector to
    `pipeline/quality.py`, then run β.1 with the improved heuristic.
    Largest scope; best long-term.
  - **(β.4)** Hybrid β.1 + β.3 (tactical fix now, hygiene fix queued
    for Phase 1.bis).
- **(γ) Close Session 16 here**, open Session 17 for the chosen β.
- **(δ) Pause to inspect the 14 pages' raw images / OCR text** before
  picking β (recommended if you want to verify the F-MISTRAL-ANSWER-
  LINE-LOSS hypothesis directly).

Cannot proceed to **6.11.D.4 (Stage 4.5 glossary → Gate ③)** until
Gate ② resolves PASS.

Resume entry-point for next session:

1. Read `docs/STATE.md` top table + §5 6.11.D rows
2. Read this file §4.4 + §9 + the two failure archives in
   `failures/stage4_structure/`
3. Confirm 5h max-plan window status if planning a fresh LLM dispatch
4. Resume at the chosen β option
