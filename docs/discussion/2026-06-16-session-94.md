# Session 94 — Quiz Phase 1 (翻訳 backfill) スケール バッチ S94 (3 回)

> 日付: 2026-06-16 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 Phase 1 スケール
> 起動: ユーザー路由「Quiz Phase1 续批」(回数未指定)。前提: S93 完了・commit 済 (`fca54f4`)、翻訳済 19/29 回。

## 0. 起点

- STATE + session-93 読了。回数未指定 → 路由②「最新優先・既訳 19 回除外で 3〜5 回」→ **最新 3 回 = `2013h25h` / `2013h25a` / `2012h24h`** (STATE「次候補」と一致)。各 100 問 = 300、figure 16/9/16=41。

## 1. 実行設計

S88〜S93 の統合 1 ワークフロー (D-小5) + フルページ併読 (D-小6) + repair 語義ガード (D-小7) + 統合バッチ combiner `scripts/quiz-phase1-batch.mjs` (D-小8) を踏襲。**本バッチはコード無変更**。新規小決策なし。

## 2. 実施

| Step | 内容 | 結果 |
|------|------|------|
| prep ×3 | crop+page fail-fast + glossary 束縛 | 全存在 (figure 16/9/16=41)、統合 input 300 問・id 全一意 |
| batch translate | `quiz-phase1-batch.mjs translate S94` | `input_batch_S94.json`/`items_batch_S94.json` (figure 41 全フルページ付) |
| translate (統合) | `wf_7fdb5ae3-2ed`、writer(general-purpose)→reviewer(code-reviewer)2R | **300/300、raw 294 PASS / 3 CONCERNS / 2 FAIL / 1 null**。648 agent / 23.7M tok / ~69 分。pause 指示なし→全量完走 |
| merge ×3 | per-exam committed sidecar | 各 100/100、missing 0。clean stem 37/47/39=123。**`2013h25a-q091` で直列化修復** (下記) |
| ruleA-prep ×3 + 強制7 | 3 CONCERNS + null q028 + recovered 2 + q091 | sidecar_batch_S94 (300) + ruleA_items_S94 (43、figure27) |
| ruleA (統合) | `wf_78495593-73a`、N=43 層化独立 critic | **accurate 39/43**。not-accurate 4 = q018/q052/q096/q092 |
| q028 Rule D 補缺 | 独立 code-reviewer 再レビュー | PASS (null=reviewer パース失敗の補缺) |
| triage + 是正 | q018/q096 是正・q052 REVERT・独立検証 | 後述 |

## 3. 結果

### Rule D in-pipeline: raw 294 PASS / 3 CONCERNS / 2 FAIL / 1 null → 実効 翻訳欠陥 0

- **null 1 = `2013h25h-q028`** (figure 損益問): in-pipeline reviewer のツール呼び出しが**パース失敗** (infra)、translator 出力は well-formed → **独立 `code-reviewer` 再レビュー=PASS** (Rule D 補缺、figure page-11 実読・5行2列損益表 全数値 JP=図=zh=en 一致・正解エ整合 [営業利益 200→200 不変/経常利益 190→210 増]・0 high/medium)。S92 q017 と同型。
- **直列化修復 `2013h25a-q091`**: translator が zh stem の引用符を**未エスケープ ASCII `"`** で落盤 → JSON 不正で merge 失敗 (en は `\"` で正)。in-pipeline reviewer は schema 検証済み StructuredOutput **返却値**を審査したため PASS (ファイルではない)。→ **最小エスケープ修復** (`\"`、訳文内容 0 改変)、Rule A 強制サンプルで語義再核験。

### Rule A (独立 critic, N=43 [各回12 + 強制7、層化 figure27]) — **accurate 39/43**

- **severity none27 / low12 / medium1 / high3**。not-accurate 4 = q018 (high)・q052 (high)・q096 (high)・q092 (medium)。
- 詳細 `evidence/phase5/stage_06_quiz_phase1/rule_a_audit_S94.json` (全 43 audit + applied_fix + upstream_ocr_backlog + lesson)。

### applied_fix (genuine 翻訳欠陥 2 件)

- **`2012h24h-q018`** (high、用語誤訳、正解イ不変): 正解選択肢「職能別組織」(=functional organization、機能別組織と同義) の zh が `职务制组织` (职务=post≠职能=function)、en `Job-function organization`。→ zh `职能制组织` / en `Functional organization` (S92 q025 確証訳一致)。根因=supply glossary の同概念分裂 (機能別→职能制 / 職能別→职务制)→glossary backlog。独立 critic 再検証 PASS。
- **`2013h25h-q096`** (high、clean stem 忠実度、正解イ不変): translator の clean stem E2 セル設定で**絶対行参照 $14 脱落** (`D2/D14`、複写時 相対化で誤計算) + 複写範囲 `E3〜E13` (figure は E14)。→ `D2/D$14` / `E3〜E14` を stem_jp_clean/zh/en 3 箇所是正。**主 context が page-44 を高解像度 crop 実読**し figure (D2／D$14・E3〜E14・全角演算子) を直接確認。**glyph 決定**: figure は全角だが q096 の D2/F2/全選択肢が半角・選択肢は上流由来で非変更 → 問内一貫性優先で E2 除算も半角 `/` 統一 ($14・E14 の意味修正は保持。corpus に全角/半角の統一規約なし=2026r08 全角・2023r05 半角で混在実測)。独立 critic 再検証 PASS。

### REGRESSION 1 件 = `2013h25a-q052` (誤 fix を REVERT、本バッチ最大の学び)

- **経緯**: Rule A 監査 critic が「choices.ウ の sidecar `0.10` は figure(crop+page-18)=`0.18` に対し捏造」と判定 → 主 context が 0.10→0.18 に「是正」。
- **反証**: 独立検証 critic が page-18 を 8x 実読し figure=`0.10` と反証 → **主 context が crop (=選択肢を含まない) と page-18 を高解像度直接実読**し figure ウ=`0.10` を確認。監査 critic の「crop が 0.18 を示す」は**ハルシネーション** (crop には選択肢が無い)。
- **解決**: 0.18 → 0.10 に **REVERT** (元訳が正、figure 整合)。`input.choices_jp.ウ`=`0.18` が上流 OCR garble (0→8) → backlog。
- **Rule B**: `failures/quiz_phase1_S94_2013h25a-q052_regression{.md,_defective.json}`。
- **正解 ア=0.07 は全 attempt で不変** (defect は distractor ウ のみ)。

### 上流 (Stage 2) OCR 欠陥の申し送り (主 context が figure 直接実読で確証、翻訳成果物に影響なし、要ユーザー判断)

- **`2013h25a-q052`** `choices_jp.ウ`: raw=`0.18`、figure(page-18)=`0.10`。JP locale が誤値表示 (zh/en は 0.10 で正)。corpus fix で choices_jp.ウ→0.10 すれば三語整合。
- **`2012h24h-q092`** `choices_jp.イ`: raw=`大阪幸子 20,800円 / 東京三郎 10,800円`、figure(page-40)=`20,000円 / 10,000円` (0→8)。全 locale が誤値 (翻訳は input に忠実)、正解ア不変。corpus fix で choices_jp.イ + zh/en 同期。
- **`2013h25h-q096`** `choices_jp.ア`: raw=`C14*(D14*0.1)`、figure(page-44)=`C14＋(D14＊0.1)` (＋→* で加算→乗算の意味変化)。distractor のみ、正解イ不変。corpus fix で choices_jp.ア + zh/en 同期。
- **glossary backlog**: `2012h24h-q018` supply glossary の機能別/職能別 分裂 (同概念) → textbook glossary 統一 (S92 q025 同型)。

### 検証 (全 GREEN・回帰なし)

- tsc 0 / eslint 0err (既存 warning 1) / **vitest 455 passed** (+2 skipped) / build exit 0。
- **nft IPA leak = 0**。quiz route trace = quiz_index + questions + **translations/22 回.json** (新 3 sidecar resolve 確認、19+3)、IPA-source (exams/sources/syllabus) 0 hits across all routes。

## 4. 学び (fix-checklist 追記)

- **figure-value を変える fix は、Rule A 監査 critic の figure 主張を鵜呑みにせず、fixer (主 context) が figure フルページを高解像度 crop で直接実読して確認せよ** (D-小6 を fixer にも適用)。S94 q052 で監査 critic が「crop が選択肢 0.18 を示す」とハルシネーション → 鵜呑みにして正値 0.10 を誤値 0.18 に改悪 (REGRESSION)。独立検証 critic + 主 context の図直接実読が捕捉・REVERT。
- **特に fix が translator 産出値から AWAY に動く時は要警戒**: translator が input と違う値を出している場合、translator が figure 是正済みの可能性がある (q052: translator は figure 正値 0.10 を産出済だったが、in-pipeline reviewer は「input 0.18 と乖離」で FAIL、Rule A 監査 critic も input 0.18 を正と誤認。input ではなく figure と突き合わせよ)。
- **crop と full-page を区別せよ**: 選択肢は crop に無く full-page にのみ在ることが多い。「crop が選択肢を示す」という監査主張は即疑え。
- **多段独立検証の複利**: in-pipeline reviewer・Rule A 監査 critic の双方が q052 を誤判定したが、第3独立パス (検証 critic) + fixer 自身の figure 実読で是正 = 写審分離 (Rule D) + 独立抽検 (Rule A) + fixer 自検証の三重冗長が機能。
- **直列化と語義の分離**: q091 で「schema 検証済 StructuredOutput はOK だが落盤ファイルは JSON 不正」という乖離を merge の構造検査が捕捉 (構造検査の価値)。

## What's Next

- **バッチ S94 完了**: 300/300 三語、Rule A **39/43 accurate**、Rule D 実効 翻訳欠陥 0 (q018/q096 是正・q052 元訳復帰・q028 補缺 PASS)、全検証 GREEN、nft IPA 0。Phase 1 翻訳済 **22/29 回**、残 7 回。
- 次バッチ = ユーザー「Quiz Phase 1 续批」で起動。次候補 = `2012h24a` / `2011h23a` / `2011h23tokubetsu` (最新優先、残=2009h21h/2009h21a/2010h22h/2010h22a/2011h23a/2011h23tokubetsu/2012h24a)。D-小6/7/8 とも scripts 組込済=追加作業不要。
- **要ユーザー判断 backlog (上流データ品質、翻訳成果物に影響なし、累積)**:
  - **S94 新規**: `2013h25a-q052` choices_jp ウ (0.18→figure 0.10) / `2012h24h-q092` choices_jp イ (20,800/10,800→20,000/10,000) / `2013h25h-q096` choices_jp ア (`*`→figure `＋`) 再OCR + `2012h24h-q018` glossary 職能別組織。
  - 累積 (S92 以前): `2016h28h-q001/q012/q096`・`2015h27a-q088` 再OCR / `2016h28a-q025` glossary / `2017h29h-q069` 再OCR / `2019h31h-q061` figure crop。(S93 q093/q088 は corpus 是正済。)

## Pre-close self-check (D-027 第5条)

- **D 新規**: なし (D-136 枠内)。小決策も新規なし (D-小5〜8 は既存・scripts 組込済)。
- **OQ**: 新規の正式 OQ なし。OQ-01/02 carryover。上流データ品質項は backlog として記録。
- **STATE sync**: 最后更新 / 当前阶段 / Stage 6 行 / 次セッション (22/29) を更新。锁定决策 136 のまま。
- **Rule A**: N=43 独立 critic (accurate 39/43)。証拠 `rule_a_audit_S94.json` + README S94 節。
- **Rule B**: q052 REGRESSION + q018/q096 genuine defects をアーカイブ (`failures/quiz_phase1_S94_2013h25a-q052_regression{.md,_defective.json}` + `failures/quiz_phase1_S94_genuine_defects.md`)。既存 failures/ 不変。
- **Rule D**: writer(general-purpose) ≠ in-pipeline reviewer(code-reviewer) ≠ Rule A 監査(critic) ≠ q028 補缺(code-reviewer) ≠ q096 fixer(general-purpose) ≠ 再検証(critic、fixer=主 context とは別 agent)。q052 は監査 critic の誤りを別 critic + 主 context 実読が是正。
- **invariants**: raw bank / questions.json 不変。translations 新規 3 sidecar (内 q018/q096 是正・q052 元訳保持・q091 エスケープ)。answer_keys/correct_answer 不変。コード変更なし。
- **コミット**: 確立済 cadence で commit (`83ffbf3`)。push はしない (要明示承認)。

---

## 追記 (同 session フォローアップ): 上流 choices_jp 欠陥 3 件を派生 corpus で是正 (ユーザー指示)

> ユーザー「对，下批开始前，帮我做了」→ S94 で backlog 化した上流 choices_jp OCR 欠陥 3 件を、次バッチ前に S93 と同じ drift-proof 方式で是正。主 context は triage 段で 3 figure を高解像度実読で確証済み。

### 方式 (drift-proof)
- `questions.json` は `build-quiz-corpus.mjs` が raw bank `question_bank.json` (gitignored) から決定的 projection (choices_jp 逐語) → **raw bank を正源として choices_jp を編集 (id-scoped + before-value assert で fail-loud) → ビルド再実行** (idempotent)。zh/en は `tr_<id>.json` を編集し merge で sidecar 同期。

### 是正内容 (figure 已確証)
- **`2013h25a-q052`** (正解ア): `choices_jp.ウ` `0.18`→`0.10` (figure page-18)。sidecar zh/en は既に 0.10 (元訳が figure 正値) → 三語 0.10 整合。
- **`2012h24h-q092`** (正解ア): `choices_jp.イ` 大阪幸子 `20,800`→`20,000` / 東京三郎 `10,800`→`10,000` (figure page-40) + sidecar zh/en 同期。
- **`2013h25h-q096`** (正解イ): `choices_jp.ア` `C14*(D14*0.1)`→`C14+(D14*0.1)` (figure page-44 の ＋=加算、半角正規化) + sidecar zh/en 同期。

### 検証
- build 再生成で questions.json diff = **当該3問のみ (3+/3-)**、quiz_index/answer_keys/correct_answer (ア/ア/イ) 不変。tsc/eslint 0err / vitest 455 / build exit0 / **nft IPA 0**。
- **独立 Rule D 再検証 (critic ≠ fixer) = 3件とも PASS**: critic が page-18/40/44 を高解像度 per-cell crop 実読し `0.10` vs `0.18`・`20,000` vs `20,800`・`＋` vs `＊` の sub-glyph 区別まで figure 一致・三語整合・正解 key 不変を独立確認。

### 影響
- 3 問とも JP/zh/en 三語が figure 一致。**q052/q092/q096 は backlog から除去 (RESOLVED_IN_CORPUS)**。残 S94 backlog = `2012h24h-q018` glossary のみ (textbook scope)。
- raw bank (gitignored) 編集のため、将来 Stage 2 再 OCR 時は本是正の再適用要 (記録: 本節 + `rule_a_audit_S94.json` の `corpus_fix`)。
- **Rule 適用**: Rule A=独立 critic figure 照合 PASS (写審分離) / Rule D=fixer (主 context) ≠ verifier (独立 critic) / invariants=answer_keys/correct_answer/quiz_index 不変、questions.json diff は当該3問のみ、コード変更なし。
- **コミット**: フォローアップとして別 commit。push はしない。
