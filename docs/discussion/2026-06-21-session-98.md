# Session 98 — Quiz Phase 1.5 全量 (stem 源再構成) スケール: B1–B3 完了 / B4 中断

> 日付: 2026-06-20〜21 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 / D-138。
> 起動: ユーザー路由词「Phase 1.5 全量」(S97 合意) = 全 29 回の stem 源再構成を実行する合図。
> **結論: 母集団 538 (図題467+非fig71) のうち pilot 済 16 を除く 522 を 4 バッチ化。B1–B3 (381問) = 完了・commit 済。B4 (141問) = session limit (daily cap、resets 03:00 JST) で中断 (11/141 recon)。resume 待ち。**

## 手順 (D-138 scale procedure 踏襲)
各バッチ: `quiz-phase1.5-prep <exam>` (全 at-risk) → 新 combiner `quiz-phase1.5-batch.mjs <label> <exam...>` (skip-existing で pilot/done を自動除外) → Workflow `quiz-phase1.5-reconstruct.workflow.mjs` (writer general-purpose → 独立 critic figure↔stem 照合、repair≤2) → 各 exam `quiz-phase1.5-merge` → 回帰チェック (`\| [アイウエ] \|` leak) → 独立 Rule A (新 `quiz-phase1.5-ruleA-{prep,workflow}.mjs`、auditor=`oh-my-claudecode:code-reviewer` ≠ critic ≠ writer = Rule D) + 主 context 最高危 figure 高倍率実読 → 検証 → commit。

## 新規 scripts (S98)
- `quiz-phase1.5-batch.mjs` (バッチ combiner、skip-existing で done 自動 dedup)。
- `quiz-phase1.5-ruleA-prep.mjs` + `quiz-phase1.5-ruleA.workflow.mjs` (独立 Rule A、図↔再構成照合 + backup照合 + no_choice_table_leak 回帰検出)。
- **hardening** `quiz-phase1.5-reconstruct.workflow.mjs`: B1 で nonfig 組合せ問の選択肢表 leak (q041) を捕捉 → `nonfigBlock` に選択肢表除去指示 + CHECK_SCHEMA に `no_choice_table_leak` (required) + checkPrompt #5 を追加。B2–B4 で leak 0 を確認。

## バッチ結果
| Batch | commit | 範囲 | reconstruct | Rule A (独立) | 主な是正 |
|---|---|---|---|---|---|
| B1 | `b4a1159` | 9回 127問 | 127/127 PASS126/CONC1 | 19/19 faithful, 0 med/high | q041 選択肢表 leak 修復 + workflow hardening |
| B2 | `71707af` | 8回 130問 | 130/130 PASS126/FAIL1/CONC3 | 24/26→修正後 0 欠陥 | q086 前置文ドリフト是正 |
| B3 | `13edeaf` | 7回 124問 | 124/124 PASS119/CONC4/FAIL1 | 22/24→修正後≈0 | q096 n/m 定義是正・q093 JSON 修復 |
| B4 | (未) | 5回 141問 | **11/141 (中断)** | — | resume 待ち |

- 全バッチ共通: **merge missing 0**、回帰 leak 0、derived_answer==correct_answer (B1 127/127・B3 0 mismatch)、translations の stem フィールドのみ更新 (choices/answer keys/questions.json/quiz_index/figure 不変、git diff で確証)。
- 検証: tsc/eslint(0err)/vitest463/build0/nft IPA leak0 を B1 で full 実行。**コード+トレース file-set 不変につき B2–B4 は B1 の GREEN が継続** (apps/web 無変更、scripts は build 対象外、sidecar は既存トレース集合の内容更新のみ)。各バッチで sidecar JSON valid + 回帰を再確認。

## session limit recovery (2 回)
- **B2**: 途中 22:00 JST reset の limit 直撃 (writer 130/130 ディスク確定・critic 112 null) → **`resumeFromRunId` で cached writer 即時 + null check のみ再走** → 完走 (S96 weekly-limit recovery と同型)。
- **B4**: **daily cap (resets 03:00 JST) を recon 段階で直撃** → 11/141 のみ recon 完了。resume は 03:00 以降。

## 重大な所見 — 答案KEY / choices / source の別軸欠陥 (D-138 stem scope 外、要ユーザー判断)
Phase 1.5 は stem 忠実度を担保する作業だが、再構成の過程で**上流の別軸欠陥**を多数捕捉した。**いずれも stem は忠実に再構成済 (= Phase 1.5 の成果物は健全)**。keys/choices は不変 (scope外)。

### 確定 bad key (HIGH)
- **`2015h27h-q100`** (B2): 非figure。再構成 stem は page-46 の手続に逐字忠実。包除原理 `w−x−y+z = |価格=1 ∩ 性能=0 ∩ デザイン=0|` = **選択肢ア**。corpus choices も page-46 一致。独立 auditor も独立に ア 導出。**stored key=エ は誤** (questions.json + raw bank 双方 エ)。

### bad key 疑い (要検証)
- **`2014h26h-q090`** (B3): 「どのグラフか」問。stem 本文忠実だが choices_jp グラフ説明文が図と系統的取り違い。auditor が図グラフ読みで ア 導出 (key=ウ)。グラフ位相照合は誤読リスク高 (下記教訓) のため断定せず要検証。

### choices_jp 腐敗 (figure と不一致、key は正、配信文面が誤)
- **`2017h29a-q092`** (B2): R/W/X セル腐敗 (図エ=グループR○ だが corpus エ=R−)。key エ は正 (高解像度 crop で確認)。
- **`2015h27h-q086`** (B2): 下線注記 4 中 3 誤 (真は ア社員番号/イ社員番号+区画番号/ウ社員番号+許可区分/エ全列)。複合主キー{社員番号,区画番号}=真イ、key イ 正。
- **`2011h23tokubetsu-q099`** (B3): choices_jp が別問の座標範囲式 (0≦J+K≦20 等) で汚染。
- **`2011h23tokubetsu-q070`** (B3): choices_jp が図に無い並列位相 (×2並列) を捏造 (上流是正記録あり)。
- **`2013h25a-q052`** (B3): writer が input scratch の choices_jp.ウ を 0.10→0.18 と誤編集 (recurring hallucination)。**高解像度 crop で ウ=0.10 確定** (S94/S97 結論再々確認)。**merge は stem のみ書込のため committed sidecar の ウ=0.10 は無傷**。re-prep で scratch 清浄化。

### incomplete-source (源データ欠落、stem 単独で解けない)
- **`2015h27a-q100`** (B2): figure に処理速度表のみ、HDD 容量・台数表が欠落 (正答140 算出不能)。
- **`2014h26a-q090`** (B3): ファイルサイズ 2,400Mバイト が backup/raw に無く writer が canonical 復元 (key ウ=80 と整合だが corpus 非検証)。

### 推奨 (ユーザー判断)
1. **key 再監査の拡張**: S97 figkey 抽様 40/247 は `2015h27h-q100` 系 (nonfig key) を構造的に捕捉できず、`2014h26h-q090` も要検証。**Phase 2 (D-137) の key-guard を全問に効かせる + nonfig key も対象化**を推奨。writer が key に寄せて derive する事例 (q092) もあり、独立 critic + 主 context 高倍率実読の多段が adjudication に必須。
2. **choices_jp 再 OCR / figure 照合トラック**: 上記 choices 腐敗群 + S97/S96 既存 backlog をまとめて choices-fidelity パスで是正 (Phase 2 解説生成は distractor に言及するため Phase 2 前 or 内蔵が望ましい)。
3. **incomplete-source**: 2 件は IPA 公式源照合でデータ補完 (Stage 2 scope)。

## 教訓 (fix-checklist 追記)
- ⑩ **低解像度 figure 読みは誤判する (本 session 3 度)**: q092 (グループ cell R−/R○)・q052 (「ここで…」文の見落とし)・q052 (ウ 0.10/0.18)。**fine-grain な ○/−・数字・短文は必ず `sips` で crop+2〜4x 拡大して読む**。多段独立 (主 context 初読 → 独立 auditor → 主 context 高倍率再読) が誤判を捕捉=q066/写審分離の複利。
- ⑪ **writer (general-purpose) は scope を越えることがある**: q052/q096 で writer が choices や定義を「是正」しようとした。**merge は stem フィールドのみ書込む設計が防壁** (choices/key 汚染が committed に伝播しない)。input scratch の汚染は re-prep で清浄化可。
- ⑫ **意味を担う定義文の「修復」は例文との内部整合を必ず確認** (q096: n の定義を「桁数」に変え例文「値 n=10100」と矛盾)。in-pipeline critic が見逃し独立 Rule A が網兜。
- ⑬ **session/daily limit は `resumeFromRunId` で回復** (writer cached + 失敗分のみ再走)。daily cap (03:00 JST reset) は跨セッション待ちが必要。

## What's Next (B4 resume — 次セッション最優先)
**quota reset (03:00 JST) 後**:
1. `node scripts/quiz-phase1.5-batch.mjs S98-B4 2011h23a 2010h22a 2010h22h 2009h21a 2009h21h` (skip-existing で recon 済 11 + 他バッチ done を自動除外 → 残 ~130 が items)。
2. Workflow `quiz-phase1.5-reconstruct.workflow.mjs` (args={exam_id:"S98-B4", input_path:".phase1.5/input_batch_S98-B4.json", items:残130}) — または `resumeFromRunId: wf_d43e9332-ef2` (cached 11 + 残再走)。
3. 各 exam `quiz-phase1.5-merge` (2011h23a/2010h22a/2010h22h/2009h21a/2009h21h) → 回帰チェック → `quiz-phase1.5-ruleA-prep S98-B4 18 [forced ids]` → Workflow ruleA → triage (主 context 最高危 figure 高倍率実読) → 検証 → commit (B4)。
4. **全 29 完了 → STATE 更新 → Phase 2 (D-137) を figure-fidelity hardening + key-guard 全問化 込みで再 pilot → scale**。

## Pre-close self-check (D-027 §5) — ※ daily-limit による強制中断
- **D 新規**: なし (D-138 枠内の scale 作業)。
- **OQ**: 新規なし。
- **STATE sync**: 最后更新 / 当前阶段 / 次セッション を S98 (B1-B3 done / B4 resume) に更新。**锁定决策 138 のまま**。
- **Rule A/D**: 各バッチ独立 Rule A (auditor=code-reviewer ≠ in-pipeline critic ≠ writer)。主 context 高倍率実読で最高危 figure を裁決。
- **Rule B**: `failures/quiz_phase1.5_S98_{2022r04-q043,2024r06-q067}_attempt_1` (B1 writer 自動) + `2015h27a-q086_prologue_drift` (B2) + `2014h26a-q093_malformed_json`・`2012h24a-q096_nm_definition` (B3)。
- **invariants**: questions.json/answer keys/quiz_index/figure/choices/reader コード 不変。translations の stem フィールドのみ更新 (B1-B3 commit 済、B4 未 merge)。
- **コミット**: B1 `b4a1159` / B2 `71707af` / B3 `13edeaf` (push せず)。B4 は resume 後。
