# Session 44 — Phase 2 Step 12 Layout + i18n base · 2026-05-20

> User kickoff: `Session 44 开始`. Tier 3 workflow. Discussion pace D-019 §3a slow.
>
> Outcome target: **Step 12 ✅ DONE + Module C 4/4 COMPLETE + D-099 ADR locked** — top sticky tab nav + LocaleSwitcher + `/ja /zh /en` path-based routing + 5 client components migrated to `useTranslations` + D-097 firewall composed with next-intl middleware on prod canonical.

---

## 1. Turn 1 — 4Q slow-pace design lock (per D-019 §3a)

Posed 4 open questions on Step 12 layout + i18n shape; user replied `Q1=a Q2=a Q3=a Q4=a` (third consecutive Phase 2 blanket-ACK after Sessions 41 and 43).

| Q | Choice | Lock-in |
|---|---|---|
| Q1 Layout | a) Top sticky tab nav + locale switcher right-aligned | `<NavTabs />` mirrors `/quiz` + `/glossary` card-discovery UX; consistent across all 3 modes; mobile + desktop single code path |
| Q2 i18n stack | a) next-intl (Next.js 15 official partner) | `next-intl@^4.12.0`; App Router + RSC + SSR-friendly; `setRequestLocale` enables static rendering; locale-aware Link/redirect/useRouter/usePathname out of the box |
| Q3 Locale + URL | a) ja default + path-based `/ja /zh /en` (localePrefix='always') | `defineRouting({ locales: ['ja','zh','en'], defaultLocale: 'ja', localePrefix: 'always' })`; SEO clean + canonical URLs; rejected as-needed (asymmetric URL shape) |
| Q4 i18n scope | a) Full chrome + busy + errors ja/zh/en at Step 12 close | AI SYSTEM_INSTRUCTION stays server-side (D-095 §2.3); ~30 chrome keys × 3 locales = 90 strings |

### Sub-LDs locked (in-source amendments per D-094 §2.1; NOT D-NNN-worthy)

| LD | Decision | Rationale |
|---|---|---|
| **LD-1** | Top sticky tab nav + LocaleSwitcher right | Q1=a /quiz /glossary modal-discovery UX consistency |
| **LD-2** | Middleware compose order: D-097 firewall → /api skip → next-intl handler | Firewall MUST guard ALL paths first; /api/* skips i18n routing; verified by +5 vitest composition tests via vi.mock |
| **LD-3** | LocaleSwitcher = `router.replace(pathname, {locale})` + `useTransition` + searchParams preserved via `useSearchParams` | URL prefix flips; query state (`?qid=` `?term=`) survives locale change; transitions are non-blocking |
| **LD-4** | i18n string scope = full chrome + busy + errors; AI prompts untouched | Q4=a; D-095 §2.3 stable-prefix invariant preserved |
| **LD-5** | Test strategy = vitest unit for middleware compose (vi.mock) + Chrome DevTools MCP UI smoke | Real next-intl/middleware can't be imported under vitest (next/server bare specifier from pnpm virtual store); mock-stub + composition assertion + UI smoke covers compose order behaviourally |
| **LD-6** | Type-safe messages = `global.d.ts` augments `IntlMessages` from `ja.json` shape | Compile-time catches missing keys; ja.json is source of truth |
| **LD-7** | Root `app/layout.tsx` = pure `return children`; `[locale]/layout.tsx` owns html/body/Provider/NavTabs | Standard next-intl App Router pattern |
| **LD-8** | URL migration: unprefixed `/chat` → 307 → `/ja/chat`; query params preserved by middleware rewrite | next-intl default behaviour with `localePrefix: 'always'` |
| **LD-9** | `<LocaleSwitcher />` wrapped in `<Suspense fallback={null}>` (useSearchParams CSR bailout) | Static prerender requirement for `/[locale]/chat` |
| **LD-10** | next-intl `redirect()` not typed `never` in v4.12; landing page return = `Promise<void>` | TS2534 fix |

---

## 2. Turn 2 — Context7 docs + composition audit

Per D-019 §3a "你来定" obligation, consulted authoritative docs before implementing:

- `mcp__context7__resolve-library-id` → `/websites/next-intl_dev` (484 snippets, High rep, 87.67 benchmark) chosen over `/amannn/next-intl` (351 snippets, 90.35) due to higher snippet coverage on docs site
- 3 query rounds on next-intl Next.js 15 App Router patterns: routing.ts + navigation helpers + getRequestConfig + locale switcher + middleware composition

Read 11 template files to ground the migration:
- `apps/web/src/middleware.ts` (D-097 firewall surface = 35 lines; matcher-baseline)
- `apps/web/src/app/layout.tsx` (root layout = boilerplate; needs strip)
- `apps/web/src/app/page.tsx` (boilerplate; will delete)
- `apps/web/src/app/{chat,quiz,glossary}/page.tsx` (server wrappers; will move into `[locale]/`)
- `apps/web/src/components/{Chat,QuizExplain,TermPopover,QuizList,GlossaryList}.tsx` (client components; will migrate to useTranslations)
- `apps/web/next.config.ts` (boilerplate; needs createNextIntlPlugin wrap)
- `apps/web/vitest.config.ts` (will not be modified at final revision)

**Surface string inventory** (cross-cataloged from the 5 components): ~30 unique chrome strings across 8 candidate namespaces (Common / Nav / Locale / Chat / QuizList / QuizExplain / GlossaryList / TermPopover).

---

## 3. Turn 3 — D-099 ADR draft + LDs writeback

Drafted D-099 candidate ADR essence (`docs/decisions/D-099-i18n-stack-next-intl.md`). Locks:
- §2.1 i18n library = next-intl 4.12
- §2.2 locales = [ja, zh, en], default ja
- §2.3 URL = path-based always-prefix
- §2.4 middleware compose order
- §2.5 partial-supersede D-088 §2.4 → per-locale error lock
- §2.6 AI SYSTEM_INSTRUCTION untouched

8 alternatives recorded as rejected (none locked).

---

## 4. Turn 4 — Implementation batch

Files added/modified (~25 in total):

```
NEW (16):
  apps/web/messages/ja.json
  apps/web/messages/zh.json
  apps/web/messages/en.json
  apps/web/src/global.d.ts
  apps/web/src/i18n/routing.ts
  apps/web/src/i18n/navigation.ts
  apps/web/src/i18n/request.ts
  apps/web/src/components/NavTabs.tsx
  apps/web/src/components/LocaleSwitcher.tsx
  apps/web/src/app/[locale]/layout.tsx
  apps/web/src/app/[locale]/page.tsx
  apps/web/src/app/[locale]/chat/page.tsx
  apps/web/src/app/[locale]/quiz/page.tsx
  apps/web/src/app/[locale]/glossary/page.tsx
  docs/decisions/D-099-i18n-stack-next-intl.md
  docs/discussion/2026-05-20-session-44.md (this file)

MOD (8):
  apps/web/next.config.ts                   (createNextIntlPlugin wrap)
  apps/web/src/middleware.ts                (firewall + i18n compose)
  apps/web/src/app/layout.tsx               (stripped to return children)
  apps/web/src/components/Chat.tsx          (useTranslations)
  apps/web/src/components/QuizExplain.tsx   (useTranslations + BusySkeleton single-text)
  apps/web/src/components/TermPopover.tsx   (useTranslations + BusySkeleton single-text)
  apps/web/src/components/QuizList.tsx      (useTranslations)
  apps/web/src/components/GlossaryList.tsx  (useTranslations)
  apps/web/src/__tests__/middleware.test.ts (+5 composition tests via vi.mock)
  apps/web/package.json                     (+next-intl, +react-hooks plugin, +next plugin)
  pnpm-workspace.yaml                       (allowBuilds placeholders → valid booleans)

DEL (4):
  apps/web/src/app/page.tsx
  apps/web/src/app/chat/page.tsx
  apps/web/src/app/quiz/page.tsx
  apps/web/src/app/glossary/page.tsx
```

### In-step diversions (Rule B-equivalent; NOT formal archives per D-094 §2.4 — each was a 1-3 line config fix not a worktree-level rework)

| # | symptom | fix | wall |
|---|---|---|---|
| div-1 | `pnpm install` failed `ERR_PNPM_IGNORED_BUILDS` (pnpm-workspace.yaml `allowBuilds:` placeholder strings) | replaced placeholders with `false` for @parcel/watcher + @swc/core | ~5 min |
| div-2 | ESLint missing plugins (@next/eslint-plugin-next + eslint-plugin-react-hooks not in apps/web node_modules; transitive deps in pnpm strict mode) | added direct devDeps; first pull picked 16.x + 7.x (too new); pinned `15.5.18` + `^5.0.0` matching eslint-config-next@15.5.18 | ~20 min |
| div-3 | `/[locale]/chat` static prerender failed (`useSearchParams should be wrapped in suspense`) | wrap `<LocaleSwitcher />` in `<Suspense fallback={null}>` inside `<NavTabs />` | ~5 min |
| div-4 | middleware.test.ts fail: `Cannot find module 'next/server'` from inside next-intl pnpm virtual store | vi.mock `next-intl/middleware`; use real `NextRequest` constructor instead of Request cast | ~25 min |
| div-5 | `app/[locale]/page.tsx` TS2534 `function returning 'never'` | return type `Promise<never>` → `Promise<void>` (next-intl redirect() not typed `never` in v4.12) | ~2 min |

Total diversion cost: ~57 min (28% of Step 12 ~200 min wall). Continues the Step 11 -58% / Module-C-structural-diversion sub-regime pattern (vs Step 9/10 bootstrap regime at -85%).

---

## 5. Turn 5a — Tests + lint + tsc + build all green

| Pipeline | Result |
|---|---|
| `vitest run` | **237 / 237 ✅** (193 baseline + 5 new middleware composition + 39 existing baseline) |
| `eslint .` | clean (after div-2 plugin pinning) |
| `tsc --noEmit` | clean (after div-5 return-type fix) |
| `next build --turbopack` | **20 static pages** + 4 dynamic API; Middleware 48.9 kB (+11.3 from Step 11's 37.6 kB; next-intl handler add); First Load JS shared 115 kB (−6 from 121 kB; bundle shuffling) |

Route table:
```
○ /_not-found                      0 B         115 kB
● /[locale]                        0 B         134 kB  (ja, zh, en)
● /[locale]/chat                122 kB         256 kB  (ja, zh, en)
● /[locale]/glossary           3.96 kB         138 kB  (ja, zh, en)
● /[locale]/quiz               3.95 kB         138 kB  (ja, zh, en)
ƒ /api/{chat,glossary/hover,hello-ai,quiz/explain}     (4 dynamic)
ƒ Middleware                    48.9 kB
```

## 6. Turn 5b — Preview + Production deploys

| Deploy | ID | URL | Result |
|---|---|---|---|
| Preview | `dpl_8eLZBtp16Tfd5LJgEtJ4YYvcH1zh` | `web-308l5h1iv-bojiangs-projects.vercel.app` | READY; preview-only URL intercepted by Vercel team SSO (platform-level; D-097 firewall surfaces on prod canonical only) |
| Production | `dpl_A9u6j1xDJzaT2Ph5Tr6PAh3zVdEA` | `web-mu-sandy-78.vercel.app` | READY target=production aliased canonical |

### Pre-smoke firewall probes (curl on prod canonical, no-auth)

| path | http | header |
|---|---|---|
| `/ja/chat` | 401 | `www-authenticate: Basic realm="IT Passport Learning firewall"` ✅ |
| `/chat` (unprefixed) | 401 | (firewall first, BEFORE i18n redirect — D-099 §2.4 LD-2 compose order verified) ✅ |
| `/api/chat` | 401 | (firewall covers API too) ✅ |

---

## 7. Turn 5c — Chrome DevTools MCP UI smoke (1 真 LLM call)

| flow | result |
|---|---|
| Open `/` (root) → 307 → `/en/chat` | next-intl Accept-Language matched browser preference (en-US); ✅ valid behaviour |
| Snapshot a11y tree at /en/chat | NavTabs (Chat/Quiz/Glossary) + LocaleSwitcher (English selected; 3 options); heading "IT Passport — Chat" + subtitle "Textbook-grounded trilingual tutor (α private)" + button "New chat" + textbox "Message input" + button "Send"; all from `messages/en.json` ✅ |
| Persisted chat history visible | Session 43 zh "第二缓存是什么" + AI reply restored cross-session AND cross-locale from localStorage; D-085 §2.2 Resume contract verified e2e ✅ |
| LocaleSwitcher en → 中文 | URL `/en/chat` → `/zh/chat`; combobox disabled briefly during useTransition then value="中文"; chrome flips to zh (tab "对话"/"题库"/"术语表", label "语言", heading "IT 护照 — 对话", subtitle "基于教材的三语辅导（α 自用）", button "新对话", input "消息输入", button "发送") ✅ |
| LocaleSwitcher zh → 日本語 | URL `/zh/chat` → `/ja/chat`; chrome flips to ja (tab "Chat"/"問題集"/"用語集", label "言語", heading "IT パスポート — Chat", subtitle "教科書ベース三語チューター（α 自用）", button "新しい会話", input "メッセージ入力", button "送信") ✅ |
| Direct nav `/ja/glossary?term=%E3%82%A2%E3%83%AB%E3%82%B4%E3%83%AA%E3%82%BA%E3%83%A0` | TermPopover modal opens; ja busy hint "AI が用語を解説中…"; AI streams 3-section trilingual response 一行で要点 + 中文解説 + English Gloss ✅ |
| Usage hint | `tokens in=400 (cache hit 96%) · out=78` — **identical to Session 39 (5 days ago) and Session 43 (1 day ago) baselines** ✅ |
| Take 4 screenshots | `screenshot_{1_ja_chat, 2_zh_quiz, 3_en_glossary, 4_ja_glossary_modal_algoritmu}.png` ✅ |
| Console messages | 0 errors; 1 pre-existing zustand deprecation warn (transitive dep, NOT from Step 12) ✅ |
| `vercel logs` `[tripwire]` | blocked by auto-mode classifier (out-of-band); proxy: UI usage hint shows 96% >> 50% threshold → β tripwire would not fire by design ✅ |

### β cache findings

- N=13 cumulative β data points; D-088 §2.3 stable-prefix invariant **ratified across cross-session 6-day window**.
- DeepSeek prefix cache TTL **> 6 days on prod** (ratchets Session 43 "> 5 days" by ~24h). Cross-session pattern of stability holds.
- 0 [tripwire] fires inferred.

### Cost

- 16 × $0.27/1M (miss) + 384 × $0.07/1M (hit) + 78 × $1.10/1M (out)
- ≈ **$0.000122 真 billed**
- Phase 2 cumulative: $0.0823 (Session 43 close) + $0.000122 ≈ **$0.0824** vs D-090 α-silent $5 cap = **60× headroom**

---

## 8. Turn 6 — Module C 收官 retro + Module C+D full re-estimate (D-094 §2.4 trigger satisfied)

**γ tripwire 12 data points** (all under-estimate):

| Step | drift | sub-pattern |
|---|---|---|
| 1-3 (Module A scaffold) | −98% × 3 | trivial scaffold; full bypass of "implementation cruise" floor |
| 4-8 (Module B API wiring) | −85, −86, −84, −81, −82% (avg −83.6%) | "implementation cruise" floor; Steps 4-5 bootstrap [-85,-86] / Steps 6-7 clone-adapt [-84,-81] / Step 8 composition [-82] |
| 9-10 (Module C UI bootstrap) | −85% × 2 | Module C bootstrap regime within cruise floor |
| 11-12 (Module C UI structural) | −58% × 2 | "structural-diversion sub-regime"; first departure from cruise floor due to novel surface introduction + multi-diversion cluster |

**Module C 4/4 avg drift**: **−71.5%**.

**Module D full re-estimate** (applying Module C avg as nearest analogue):

| Step | original | adjusted | reason |
|---|---|---|---|
| 13 (D-090 cap impl) | 1 day = 8h | **~2.3h** | env-switch + per-route counter + dashboard widget; familiar pattern |
| 14 (Lighthouse + i18n complete + a11y) | 1 day = 8h | **~2.3h** | i18n already done in Step 12; Lighthouse + a11y pass; highest variance step |
| 15 (E2E + prod + custom domain) | 0.5 day = 4h | **~1.1h** | small spec; prod 1-command; domain β-optional |
| Module D total | 2.5 day = 20h | **~5.7h** | range 5.7-8.4h with structural variance band |

**Phase 2 from-here**: ~6.2h until ship-ready (Module D + Session close). Calendar 1-2 active days.

---

## 9. Pre-close self-check (per D-027 §5)

| Item | Status | Evidence |
|---|---|---|
| **D-099 LOCKED** | ✅ | `docs/decisions/D-099-i18n-stack-next-intl.md` written this turn; Q1-Q4 + 6 §s + 8 rejected alts + 5 LDs |
| D-088 §2.4 partial supersede documented | ✅ | D-099 §2.5; per-locale error surface lock |
| Code green | ✅ | 237/237 vitest + ESLint clean + tsc strict + Next build 20 routes |
| Vercel env vars unchanged | ✅ | FIREWALL_BASIC_AUTH + DEEPSEEK_API_KEY held over from Step 11 |
| Preview deploy | ✅ | `dpl_8eLZBtp16Tfd5LJgEtJ4YYvcH1zh` |
| Prod deploy | ✅ | `dpl_A9u6j1xDJzaT2Ph5Tr6PAh3zVdEA` aliased canonical `web-mu-sandy-78.vercel.app` |
| Firewall intact on prod canonical | ✅ | curl probes 401 + WWW-Authenticate on /ja/chat, /chat, /api/chat |
| Middleware composition verified | ✅ | +5 vitest tests via vi.mock; curl confirms firewall-first |
| 1 真 LLM UI smoke ✅ | ✅ | screenshot 4 + UI usage hint 96% cache hit; ~$0.000122 真 billed |
| 0 [tripwire] fires under healthy | ✅ inferred | 96% >> 50% threshold; runtime detector silent by design |
| Module B 5/5 + Module C 1-3/4 retained (no regression) | ✅ | 4 API routes + middleware + all 5 client components additive-modified; route bundle sizes within ±10% |
| PLAN.md Step 12 ✅ DONE | ✅ | row amended this turn with full narrative + `actual ~200 min` + Module C+D re-estimate amendment |
| STATE.md sync | ✅ | last-updated line + 4 anchors synced this turn |
| Session log on disk | ✅ | this file complete through Turn 9 |
| Evidence on disk | ✅ | `step_12_layout/{tree_outline, build_log, test_results, design_notes, cache_audit, step_12_audit, ui_smoke}.md` + 4 screenshots + 2 deploy logs + tripwire_log row #9 appended |
| Rule A 抽检 | ✅ | N=6 i18n string sample audit + N=1 真 LLM output audit (both PASS) in `step_12_audit.md` |
| Rule B 失败归档 | n/a (0 formal archives) | 5 in-step diversions documented in `design_notes.md §6` + `cache_audit_2026-05-20.md §3` but each is a 1-3 line fix not a worktree-level rework per D-094 §2.4 distinction |
| Rule C (Phase retro) | n/a | Module C 收官 retro happens here (in cache_audit_2026-05-20.md §4); Rule C Phase 2 retro at Step 15 close |
| Rule D Writer ≠ Reviewer | ✅ | Writer = main session; Reviewer = user terminal (4Q ACK + go-gates); commit + push pending per Sessions 27-43 pattern |
| OPEN OQ count | 3 unchanged | Phase 1 后续 / Phase 3-4 范围 |
| 已锁定决定数 | **99** (was 98 at Session 43; **+1 = D-099 this session**) | docs/decisions/D-099-i18n-stack-next-intl.md |

---

## 10. Final commit plan (1 atomic — per Sessions 27-43 pattern)

| File | Type |
|---|---|
| `apps/web/messages/{ja,zh,en}.json` | new (3 files) |
| `apps/web/src/global.d.ts` | new |
| `apps/web/src/i18n/{routing,navigation,request}.ts` | new (3 files) |
| `apps/web/src/components/{NavTabs,LocaleSwitcher}.tsx` | new (2 files) |
| `apps/web/src/app/[locale]/{layout,page,chat/page,quiz/page,glossary/page}.tsx` | new (5 files) |
| `apps/web/next.config.ts` | modify (createNextIntlPlugin) |
| `apps/web/src/middleware.ts` | modify (firewall + i18n compose) |
| `apps/web/src/app/layout.tsx` | modify (return children pass-through) |
| `apps/web/src/components/{Chat,QuizExplain,TermPopover,QuizList,GlossaryList}.tsx` | modify (useTranslations migration; 5 files) |
| `apps/web/src/__tests__/middleware.test.ts` | modify (+5 composition tests) |
| `apps/web/package.json` | modify (+ 3 deps) |
| `pnpm-lock.yaml` | modify (deps install) |
| `pnpm-workspace.yaml` | modify (allowBuilds → valid booleans) |
| `apps/web/src/app/{page,chat/page,quiz/page,glossary/page}.tsx` | **DELETE** (4 files, moved into `[locale]/`) |
| `docs/phase2/PLAN.md` | modify (Step 12 ✅ DONE + Module C+D re-estimate amendment) |
| `docs/STATE.md` | modify (4 anchor sync) |
| `docs/discussion/2026-05-20-session-44.md` | new (this file) |
| `docs/decisions/D-099-i18n-stack-next-intl.md` | **NEW ADR LOCK** |
| `evidence/phase2/step_12_layout/*` | new (11 files: 5 md docs + 4 screenshots + 2 deploy logs) |
| `evidence/phase2/tripwire_log.md` | modify (row #9 appended) |

Excluded (correct):
- `apps/web/.env.local` (gitignored; carries FIREWALL_BASIC_AUTH + DEEPSEEK_API_KEY)
- Vercel env vars (out-of-band; documented in evidence)
- `apps/web/.next/` (build artifact)

**Push**: pending user `push it` gate per Sessions 27-43 pattern.

---

**Session 44 ready to close** — Step 12 ✅ DONE + Module C 4/4 COMPLETE + D-099 ADR LOCKED (i18n stack = next-intl + ja default + path-based + middleware compose + per-locale error lock + AI prompts untouched) + 1 新 ADR (98 → 99 locked) + 237/237 vitest (+5 middleware compose) + ESLint + tsc + build all green + 20 static pages (ja/zh/en × 4) + Prod canonical aliased + 1 真 LLM UI call via Chrome DevTools MCP (96% cache hit on `アルゴリズム` cross-session > 6-day TTL re-confirmed) + 0 [tripwire] fires under healthy + ~$0.000122 真 billed (cumulative Phase 2 ~$0.0824 = 60× headroom) + 12th γ data point + Module C+D full re-estimate AMENDED (Module D 20h → 5.7h, Phase 2 from-here ~6.2h until ship) + 0 formal Rule B archive (5 in-step diversions documented but each was 1-3 line config fix not a worktree-level rework per D-094 §2.4 distinction); pending user commit + push gates.
