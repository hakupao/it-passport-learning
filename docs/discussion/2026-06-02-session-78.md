# Session 78 — G4 起動 + Stage 4 生成設計の確定 (D-128〜131)

> 日付: 2026-06-02 (kickoff 2026-06-01) / フェーズ: Phase 5 Stage 4 (AI 教科書生成、実施阶段)
> ユーザー指示: 「開始 G4」→ (勘查+建議の後)「建議認可。Q2 訳も最高 = 全工程 Opus 4.8 max effort。図解は重做 + 原裁剪図も索引付きで附し対照追溯。其余按建議」
> 前提: Stage 3.5 完了 (Session 77)。enriched question_bank.json + knowledge_tree (1,417語)。

## 本セッションのタスク

1. G4 (Stage 4) 起動の勘查 (read-only) + D-019 慢速協議で開放質問 → ユーザー回答。
2. 公式文档 consult (claude-api skill) → Stage 4 実行設計を **D-128〜131** に確定。
3. 同 turn で session log + STATE + PLAN 同期。

## 勘查 (read-only、未改データ)

- knowledge_tree: 3類 / 9大 / 23中 / **63小分類 (末端)** / **1,417 term 出現** (unique 1,395)。term/topic = 3〜91 (avg 22.4)、空節点 0。
- question_bank: 2,900題全 enriched。confidence high2292/medium591/low17。**63/63 topic 全覆盖**、7〜217題/topic (avg 46)、<10題は4 topic のみ。**難易度フィールド無し** (年度は id 内)。
- **規模更正**: D-115 の「330〜530 unit」は旧推定語数 2,651 ベースで過大。curated tree 1,417 → 実 **~180〜240 unit**。
- textbook/ 空 = Stage 4 新規。

## 開放質問 → ユーザー回答 (D-019)

- Q1 範囲: pilot-first 3 跨類節点 → **認可**。
- Q2 モデル/三語: **全工程 Opus 4.8 最高 effort** (Sonnet 降格せず) / 三語=日語権威源→翻訳 → **認可 (訳も最高に修正)**。
- Q3 切分: per-topic LLM 規劃 pass + 廉価 ToC ゲート → **認可**。
- Q4 選題/頻度: データ駆動 + 難度捏造せず → **認可**。図解: 重做 + **原裁剪図も索引附加** (追加要求)。

## 公式 consult (記憶のみに依拠せず, D-019)

claude-api skill (Anthropic 公式) で grounding:

- **Batches API**: 標準価格の **50%**、非同期 (大半1h/最大24h)、**100,000 req / 256MB per batch**、全 Messages 機能 (Opus 4.8 + adaptive thinking + effort + caching) 対応 → 品質同一・半額。
- **Opus 4.8 効力 (重要更正)**: `budget_tokens` **非対応** (400)。最高 effort = `thinking:{type:"adaptive"}` + `output_config:{effort:"max"}` (階梯 low/medium/high/xhigh/max、max は Opus 専属)。`temperature/top_p/top_k` も除去 (400)。
- **Prompt caching**: 共有 prefix (system+schema+style+syllabus) キャッシュ可、Opus 4.8 最小 prefix 4,096 tokens。sync で確実、batch でベストエフォート。

## 決定 (本セッション locked)

- **D-128 生成アーキテクチャ/実行戦略**: 二段式 (規劃→ToCゲート→内容) + pilot-first (3節点) + pilot sync/全量 Batches(50%) + prompt caching。ADR: `D-128-stage-4-generation-architecture.md`。
- **D-129 モデル/三語**: 全工程 Opus 4.8 + 最高 effort (adaptive thinking + effort:max、budget_tokens 廃止を公式確認、pilot で xhigh/max 比較) + 三語=日語権威源→二次翻訳。ADR: `D-129-stage-4-model-and-trilingual.md`。
- **D-130 単元分割/排列**: per-topic LLM 規劃 pass → 全書 ToC → 廉価ゲート。ADR: `D-130-stage-4-unit-segmentation.md`。
- **D-131 選題/頻度/図解二軌**: 即時=term題池/チャレンジ=節点抽样・頻度=題数分位・難度は捏造せず(年度+term跨度)・**図解二軌** (Mermaid 新図 + 原裁剪図を `figure_index.json` で索引附加)。ADR: `D-131-stage-4-quiz-frequency-figures.md`。

（いずれも採用理由・却下案は各 ADR に記載。Ultracode 下で成本非約束、ただし pilot 実測でユーザーに透明化。）

## What's Next

**G4 Phase A 起動**: pilot 3節点 (technology-16-43 / management-11-29 / strategy-02-04) に per-topic 規劃 pass (Opus 4.8) → 統合 ToC → **ユーザー審査ゲート**。承認後 Phase B 内容生成 (sync) + Rule A 抽检 + 単 unit 実測コスト外挿 → 全量 (Batches) gate。

実装前提の確認事項 (Phase A 着手時):
- ANTHROPIC_API_KEY / Anthropic TS SDK の有無 (既存 Stage1-3 スクリプトで設定済の見込み、要確認)。
- mmdc/mermaid-cli 工具链 (図レンダは Phase B、実装時に公式確認)。

## 追記: 実行チャネル更正 — D-132 (同 turn、ユーザー指示)

ユーザー: 「Claude Code Max plan (20×) 利用、外部 `ANTHROPIC_API_KEY`/Anthropic TS SDK は不使用」。これに伴い:

- **D-132 lock**: 全 LLM 工作 (Stage 4 Phase A/B 含む) は Claude Code (subagents/Workflow/ultracode, `model=opus`) で実行。外部 Anthropic API / Message Batches API 不使用。機械装配・図レンダ (mmdc) は TS/JS スクリプト。ADR: `D-132-llm-via-claude-code-no-external-api.md`。
- **更正**: D-128-C (Batches→Claude Code Workflow)、D-128-D (caching は harness 管理)、D-129-B (effort は Claude Code 構成) を ADR 上で更正。二段式/pilot/三語/切分/選題/図解のロジックは不変。
- Stage 1-3 の LLM 工作も実際は Claude Code 経路だったため、本決定は実績と一致。
- ユーザー記憶に恒久記録 (`~/.claude/.../memory/no_external_anthropic_api.md`)。
- コスト観の変化: 按 token 課金なし (定額)。pilot は吞吐/耗時を測り全量を見積もる (ドル成本ではない)。

## 追記2: API 使用境界の共識確定 + Mistral 清理 (Session 78 後段、ユーザー問答)

ユーザーが API key 使用の境界を明確化。コード核査 (`apps/web/src/lib/ai/provider.ts` / `.env.example` / scripts) で対齐:
- **① Mistral OCR**: Phase 1 pipeline は Session 63 削除済 (D-110/D-113)。`.env.example`/README に過時残留 → `.env.example` の `MISTRAL_API_KEY` を清理。
- **② web AI provider 実態**: chat/quiz/hover/glossary = **DeepSeek 既定 + Anthropic 切換**; tutor = +**OpenAI 予約スタブ (未実装、`getTutorModel` で throw)**。依存は `@ai-sdk/anthropic`+`@ai-sdk/deepseek` のみ。→ ユーザーの「3社」理解を更正 (OpenAI は Phase 5 予約)。
- **③ 共識 (D-132 範囲収窄)**: 🏗️ 構建期/内容管線 (OCR/vision/翻訳/映射/**Stage4 生成**) = Claude Code、外部 API 不使用。🌐 web 運行時 (chat/tutor/glossary/quiz/未来 AI 学習) = 外部 API (部署服务端は CC 訂閱を使えない)。
- D-132 + memory を「構建期のみ」に収窄 (web 運行時を明示除外)。
- 注: scripts/extract-questions.mjs 等 Stage2 期スクリプトは `ANTHROPIC_API_KEY` 参照 (歴史、go-forward では非採用)。

## G4 作業キックオフ整理 (新 session 起点 — Q1〜Q4 + 設計 + 共識 + 次の一手)

> 新しい session はこの節 + STATE「次セッション」を読めば即着手可能。

### 設計問答 (Q1〜Q4) → 確定 (D-128〜131)
- **Q1 範囲** → pilot-first、3 跨類節点: `technology-16-43` システムの構成 / `management-11-29` サービスマネジメントシステム / `strategy-02-04` 知的財産権。[D-128-B]
- **Q2 モデル/三語** → 全工程 **Opus** (Claude Code `model=opus`+ultracode、D-132 で CC 経路に確定) / 三語 = **日語権威源 → 二次翻訳**。[D-129]
- **Q3 単元切分** → **per-topic LLM 規劃 pass** (概念依存+頻度) → 全書 ToC → 廉価ゲート。[D-130]
- **Q4 選題/頻度/図解** → 即時=term題池 / チャレンジ=節点抽样 / 頻度=題数分位 / 難度は捏造せず(年度+term跨度) / **図解二軌** (Mermaid新図 + 原裁剪図を `figure_index.json` 索引)。[D-131]

### 実行共識 (API 境界、D-132 scoped)
- 🏗️ 構建期 (本 Stage 含む) = Claude Code、外部 API 不使用。🌐 web 運行時 = 外部 API、`provider.ts` 不変。Mistral 死。

### 次の一手 (Phase A)
1. (任意) mmdc/mermaid-cli の有無確認 (Phase B 図用、本地)。
2. **Phase A**: 3 pilot 節点に per-topic 規劃 pass を **Workflow + Opus subagents** で実行 → 統合 ToC。入力 = {topic, terms[], term 題頻, 前置概念}。
3. **ToC ゲート**: ユーザー審査 → 承認後 **Phase B** 内容生成 (Claude Code) + Rule A 抽检 + 単 unit 吞吐/耗時外挿。
4. 全量 (63 topic) workflow gate へ。
- 産出先: `data/ip/textbook/{unit_index.json, units/*.json, figures/*}` + `figure_index.json`。invariants 不変、Rule A/B/D 適用。

## Pre-close self-check (D-027 第5条)

- D-128〜132 locked → ADR 5件 + session log + STATE 決策歴史 ✅ (D-132 で D-128/D-129 を更正)
- STATE: 最后更新 / 当前阶段 / 锁定决策(127→132) / Stage表(Stage4) / 次セッション 全 sync ✅
- PLAN §1 (Stage4 行) + §6 (実行設計 D-128〜132) sync ✅
- API 境界共識 (構建期=Claude Code / web運行時=外部API) → D-132 範囲収窄 + memory 更新 + `.env.example` Mistral 清理 ✅
- OQ 変更なし。データ未改 (設計のみ、executable code 未作成)。commit は別途 (push は要ユーザー承認)。
