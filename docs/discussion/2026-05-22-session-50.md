# Session 50 — Phase 3 实施阶段 OPEN · Step 1 Reader 壳 ✅ DONE · 2026-05-22

> Session 49 closed Phase 3 设计阶段 ✅ COMPLETE (D-101 LOCKED + PLAN.md scaffold + LD-1~LD-4) with the 实施阶段 gate explicitly pending a user signal per CLAUDE.md "Phase/stage signaling". Session 50 opens on the user's explicit `开始 Phase 3 Step 1` signal and closes with Step 1 (Reader 壳) DONE.
>
> **Phase**: 实施阶段 (per CLAUDE.md "Phase/stage signaling": Step 1 implementation executed under TDD with Tier 3 evidence; Phase/stage gate satisfied by user signal).

---

## 1. Turn 1 — User signal "开始 Phase 3 Step 1"

After Session 49 close (设计阶段 complete; PLAN.md + D-101 ADR on disk), user gave the explicit gate signal `开始 Phase 3 Step 1`. Per CLAUDE.md "Phase/stage signaling" + Session 49 §7 entry options (a), this opens 实施阶段 for Step 1 only — Steps 2 + 3 remain gated.

Step 1 scope per PLAN.md §1 row 1:
- NavTabs refactor per LD-1 (Book 主体 + chat/quiz/glossary 视觉降级)
- Chapter routes `/[locale]/book` + `/[locale]/book/chapter/[nn]`
- `BookIndex` component (16-chapter TOC)
- `ChapterReader` component (continuous chapter-page flow)
- `/[locale]` redirect → `/[locale]/book` per D-101 §2.1
- Messages keys for new surface
- vitest cases for new lib code

User also gave a no-clarifying-questions directive (work without stopping for checks; make reasonable calls and continue), so no AskUserQuestion gates during Step 1 implementation.

---

## 2. Turn 2 — Survey + plan

Read STATE.md (anchors only — file too large to fully load), session-49 log full, Phase 3 PLAN.md full, D-101 ADR full. Surveyed apps/web codebase:

- `apps/web/src/components/NavTabs.tsx` — Phase 2 Step 12 + Session 46 a11y polish; 3-tab structure; client component using `usePathname` + `<Link>` + `LocaleSwitcher`.
- `apps/web/src/app/[locale]/{layout.tsx, page.tsx, chat/page.tsx, quiz/page.tsx, glossary/page.tsx}` — locale-segment layout chain; existing `/[locale]/page.tsx` redirects to `/chat`.
- `apps/web/src/lib/data/{FsDataSource.ts, types.ts, index.ts}` — `DataSource.loadIndex()` returns `IndexV2` containing `chapters: ChapterRef[]` (16 chapters with `chapter_id ch00..ch15`, `first_page`, `last_page`, trilingual titles), and `DataSource.loadChapter(chapter_id)` returns `Page[]` for that chapter.
- `apps/web/_fixtures/v1.0.3/index.v2.json` — confirmed 16 chapter refs with proper page ranges (7-24, 28-57, 70-99, ..., 508-551).
- Page JSON shape — `Page.entities[]` is the truth source for content body (titles for sections, captions for figures, section numbers). Page MD files referenced by index but absent on disk; this drove **LD-Step1-A** (render from `entities[]` not MD).
- `apps/web/messages/{ja,zh,en}.json` — i18n structure with `Nav.{chat,quiz,glossary}` keys to extend.
- `apps/web/src/i18n/{routing.ts, navigation.ts}` — routing config has locales only (no `pathnames` map) → `<Link>` accepts plain string hrefs (LD-Step1-E).

Plan derived: 10 TaskCreate tasks (chapterScope lib + tests → messages → NavTabs → BookIndex → ChapterReader → /book route → /book/chapter/[nn] route → /[locale] redirect → gates → evidence + docs).

---

## 3. Turn 3 — Implementation

Single sitting, all 10 tasks executed sequentially (TaskUpdate marks each completed as it landed). Notable shape decisions:

| LD | Decision | Why |
|---|---|---|
| **LD-Step1-A** | Render chapter content via `projectRenderEntities()` over `Page.entities[]` not MD | Fixture `pages/*.md` referenced in index but absent on disk. Entities carry trilingual titles + captions + section numbers, sufficient for the "shell". MD ingestion deferred. |
| **LD-Step1-B** | `parseChapterNn()` enforces zero-padded 2-digit input only ("07" yes; "7" / "007" / "16" no) | Canonical URL shape per D-101 §2.2; ambiguous variants → 404; out-of-range hard-fail avoids silent empty chapter render. |
| **LD-Step1-C** | `<BookIndex />` server-rendered (no `"use client"`) | TOC has no client state; static rendering across ja/zh/en — visible as `●` in next build output. |
| **LD-Step1-D** | `<ChapterReader />` server-rendered (no `"use client"`) | Same reasoning; Step 2 will add client wrappers for selection toolbar + chapter-end panel. Chapter route shows `ƒ` only due to `force-dynamic` (FsDataSource needs server-only fs I/O). |
| **LD-Step1-E** | `<Link>` uses plain string href (`/book/chapter/${nn}`), not `{pathname, params}` object | `routing.ts` does NOT define `pathnames` map → typed-pathname Link object form is unconfigured. Plain strings flow through next-intl's locale-prefixing wrapper transparently. |

Files landed:

```
NEW (6):
  apps/web/src/lib/book/chapterScope.ts                                  (~140 lines)
  apps/web/src/lib/book/__tests__/chapterScope.test.ts                   (~250 lines, 20 cases)
  apps/web/src/components/BookIndex.tsx                                  (~80 lines)
  apps/web/src/components/ChapterReader.tsx                              (~180 lines)
  apps/web/src/app/[locale]/book/page.tsx                                (~50 lines)
  apps/web/src/app/[locale]/book/chapter/[nn]/page.tsx                   (~75 lines)

MOD (5):
  apps/web/src/components/NavTabs.tsx                                    (LD-1 4-tab refactor)
  apps/web/src/app/[locale]/page.tsx                                     (/chat → /book redirect)
  apps/web/messages/ja.json                                              (+Nav.book + Book.* 12 keys)
  apps/web/messages/zh.json                                              (+Nav.book + Book.* 12 keys)
  apps/web/messages/en.json                                              (+Nav.book + Book.* 12 keys)
```

---

## 4. Turn 4 — Gates green

| Gate | Result |
|---|---|
| `pnpm exec vitest run` | **299 / 299 PASS** (was 279; +20 new chapterScope cases) Duration 571 ms |
| `pnpm exec tsc --noEmit` | clean — fixed 5 `noUncheckedIndexedAccess` strict-mode errors in same cycle (chapterScope.ts `m[1]` guard + `ordered[idx±1]` guard + test `out[0]` guard) |
| `pnpm exec eslint src` | clean — 0 errors / 0 warnings |
| `pnpm exec next build` | green — 23 static pages, /[locale]/book SSG ●, /[locale]/book/chapter/[nn] dynamic ƒ |

Bundle delta vs Phase 2 Session 47 baseline:
- Middleware 48.9 kB → **44.2 kB** (−4.7 kB)
- Shared First Load JS 115 kB → **102 kB** (−13 kB; Next 15.5 internal updates)
- /[locale]/chat First Load 169 kB **unchanged** (Phase 2 invariant preserved)
- /[locale]/quiz + /[locale]/glossary Size 4.53 → **4.71 kB** (+0.18 kB each; Book.* keys add ~180 B to next-intl bundle per surface)
- /[locale]/book + /[locale]/book/chapter/[nn] each **175 B / 119 kB** (NEW)

---

## 5. Turn 5 — Tier 3 evidence + docs (THIS TURN)

| Artefact | Action |
|---|---|
| `evidence/phase3/step_01_reader/tree_outline.md` | NEW — file tree of Step 1 |
| `evidence/phase3/step_01_reader/build_log.txt` | NEW — `next build` output |
| `evidence/phase3/step_01_reader/test_results.txt` | NEW — vitest summary |
| `evidence/phase3/step_01_reader/design_notes.md` | NEW — LD trace + D-101 §-mapping + bundle Δ + tripwire snapshot + Rule B disposition |
| `docs/phase3/PLAN.md` | MOD — Step 1 row ✅ DONE + actual wall amend + §7 STATUS upgrade (Step 1 done; Steps 2/3 pending) |
| `docs/STATE.md` | MOD — 5 anchors prepended with Session 50 close (最后更新 / 当前阶段 / 已锁定决定数 unchanged-101 / 未决问题数 unchanged-2 / 下一会话); legacy Session 48-49 entries preserved per D-028 trace convention |
| `docs/discussion/2026-05-22-session-50.md` | NEW (this file) |

---

## 6. Pre-close self-check (per D-027 §5)

| Item | Status | Evidence |
|---|---|---|
| Code written | ✅ | 6 new files + 5 modified — 实施阶段 per CLAUDE.md "Phase/stage signaling"; user gate honoured |
| Tests | ✅ | 299 / 299 vitest PASS (+20 new chapterScope cases); tsc + eslint + next build all green |
| Phase 3 PLAN.md Step 1 row | ✅ | upgraded to ✅ DONE with actual-wall amend per D-094 §2.1 + D-080 v1.1 §8 in-source pattern |
| Phase 3 PLAN.md §7 STATUS | ✅ | implementation gate marked OPEN; Step 1 DONE; Steps 2/3 pending |
| STATE.md anchor sync | ✅ | 最后更新 + 当前阶段 + 已锁定决定数 + 未决问题数 + 下一会话 all updated; legacy preserved per D-028 |
| Session log on disk | ✅ | this file |
| OQ changes | ✅ | 0 OQ opened, 0 OQ closed Session 50 (Step 1 is execution of locked D-101 + PLAN.md, no new design surface) — count remains 2 (OQ-01 + OQ-02 Phase 1 carryover) |
| Rule A 抽检 | n/a | no >50% compression / rewrite Session 50 (Step 1 is greenfield composition, not transformation) |
| Rule B 失败归档 | n/a | no failed attempt grade events; 5 tsc strict errors caught + fixed in same cycle ≠ Rule B archive per D-094 §2.4 distinction (documented inline at design_notes.md §8) |
| Rule C (Phase retro) | n/a | Phase 3 retro at Phase 3 close (post-Step 3) per PLAN.md §3 + Rule C |
| Rule D Writer ≠ Reviewer | ⏸ | Step 1 stays at build-time gate level (vitest + tsc + eslint + next build = automated reviewer chain). Browser-based reviewer (Playwright + axe-core + Lighthouse) deferred to Step 3 close per Phase 2 Step 14+15 pattern reuse |
| D-019 §3a slow-pace | n/a | no design surface opened Session 50 — Step 1 executed locked D-101 + LD-1~LD-4 |
| D-101 + D-094 §2.1 honoured | ✅ | 5 in-source LDs LD-Step1-A~E captured in evidence/design_notes.md §3 — NOT D-NNN-worthy per D-094 §2.1 |
| Phase 1 + Phase 2 ✅ FROZEN preservation | ✅ | tags immutable; Step 1 is additive composition on top of frozen components; zero rewrite of frozen surfaces |
| 101 locked decisions | ✅ | D-001 ~ D-101 unchanged; 0 new ADR Session 50 |
| 实施阶段 gate satisfied | ✅ | user explicit signal `开始 Phase 3 Step 1` opened the gate; Step 1 execution complete |

---

## 7. Commit plan + Session 51 entry

### Commit (1 atomic — per Sessions 27-49 pattern)

| File | Type |
|---|---|
| `apps/web/src/lib/book/chapterScope.ts` | new |
| `apps/web/src/lib/book/__tests__/chapterScope.test.ts` | new |
| `apps/web/src/components/BookIndex.tsx` | new |
| `apps/web/src/components/ChapterReader.tsx` | new |
| `apps/web/src/app/[locale]/book/page.tsx` | new |
| `apps/web/src/app/[locale]/book/chapter/[nn]/page.tsx` | new |
| `apps/web/src/components/NavTabs.tsx` | mod |
| `apps/web/src/app/[locale]/page.tsx` | mod |
| `apps/web/messages/{ja,zh,en}.json` | mod (×3) |
| `evidence/phase3/step_01_reader/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}` | new (×4) |
| `docs/phase3/PLAN.md` | mod (Step 1 row + §7 STATUS) |
| `docs/STATE.md` | mod (5 anchors) |
| `docs/discussion/2026-05-22-session-50.md` | new (this file) |

Excluded: `.claude/` (gitignored locally).

**Push**: pending user `go commit` + `push it` gate per Sessions 27-49 pattern. No `git commit` or `git push` executed proactively per CLAUDE.md + project convention.

### Session 51 entry options

(a) **Open Phase 3 Step 2** — user explicitly says `开始 Phase 3 Step 2` → I begin LD-2 inline triggers (ChapterEndPanel scoped to current chapter + SelectionToolbar listening on `selectionchange` + ParagraphTranslate modal calling /api/chat with translate prompt + translatePrompt.ts compose helper) per PLAN.md Step 2 row; est ~2-4h wall; β tripwire data re-opens at first /api/chat paragraph-translate call.

(b) **Hold + self-use feedback** — Phase 3 Step 1 stays ✅ DONE; collect own usage feedback by browsing /book + /book/chapter/NN locally or after Vercel preview deploy; defer Step 2 indefinitely.

(c) **Tweak Step 1 before Step 2** — if Step 1 reading flow needs richer content rendering (e.g. ingest MD when fixture provides it, or richer entity-type switch in `<EntityBlock />`), re-propose Step 1 amendments before Step 2.

(d) **Skip to Step 3 directly** — if Step 2 inline triggers feel unnecessary, jump to progressStore + scroll-to-end gate + Vercel deploy + RETROSPECTIVE; will need to re-justify why Step 2 was skipped (not currently expected).

γ tripwire: row #13 = first Phase 3 datapoint = first under-estimate −71% under midpoint (driver: D-101 "zero-refactor composition only" architectural prediction held with no scope drift). Module Phase-3 N=1 too early to re-estimate Steps 2/3.

β tripwire: N=14 cumulative final from Phase 2 unchanged (Step 1 added zero /api/* calls). β re-opens at Step 2.

δ runtime detector: LIVE silent (Phase 2 invariant preserved).

ε / α: no fires.

**Phase 3 ✅ on track** — Reader 壳 lands cleanly, foundation ready for Steps 2 + 3 layering per D-101 + PLAN.md design.
