# Session 72 — Stage 2.6 データ実測審核（実行）

> 日付: 2026-05-29 / フェーズ: Phase 5 Stage 2.6（実施阶段）
> 計画: `docs/phase5/STAGE_2.6_AUDIT_PLAN.md` / ゲート: D-119

## 目的

question_bank.json（2,900 題）の正確度を未使用視点 + 外部源で実測し系統欠陥を除去。Stage 3 ゲート（D-119）。

---

## Phase A — 全量センサス L3/L4/L6（決定的・全 2,900 題）

`scripts/stage026-census.mjs` → 3 census JSON + トリアージ `evidence/phase5/stage_026_census_triage.md`。

- **L6 構造**: 完全クリーン（100題/套×29、空・キー異常ゼロ、答案分布 χ²=9.4 許容）。
- **L3**: choice衝突70件・年度跨ぎ類似 = 全件良性（誤検出/正当再出題）。有効フラグは**表紙ボイラープレート汚染4件** + 同回重複2ペア。
- **L4**: orphan16 / ref_but_no_figure32 / figure_but_no_ref87。

→ 画像照合要フラグを統合: `data/ip/exams/.tmp/s026/deep_worklist.json`（53 qid）。

## Phase B — 深核 53 qid（15 並列 read-only `explore`、Rule D）

欠陥 35（critical 27 / minor 8）、no-defect 18（連問共有図の誤検出）。証拠 `stage_026_sample_audit.md`、台帳 `stage_026_defect_matrix.csv`。

- **内容欠陥**: stem_contamination 4（q099/q045×2/q001）+ duplicate_extraction 2（**新欠陥類** 2018h30h-q008/q100）+ stem_choice_mismatch 1（q094）。
- **図表欠陥**: 真の図欠落 add_figure 多数（Session70-71 の裁剪漏れ）+ 連問共有図（要 D-120）+ 誤 has_figure 降格。

## 全量 duplicate_extraction 再スキャン（ユーザー決定: 全量near-dup+PDF照合）

`scripts/stage026-dup-scan.mjs` → 近重複関与 86 qid を 10 並列で PDF 照合（`dupcheck_findings.json`）。

- **新 duplicate_extraction 2**: `2009h21a-q088`（真=DBロック, q068複製）/ `2010h22a-q008`（真=初期投資回収, q006複製）→ **計4件**、全て同回内隣接コピー。near-dup スキャンが全候補捕捉 = **収束**。
- **新 choice_swap 1**: `2015h27a-q059`（ア↔イ逆転、正答誤りの疑い）。
- **choice_ocr 3（minor）**: 2022r04-q017 / 2014h26a-q009 / 2019r01a-q049。
- 2017h29a-q071/q095 等 80 件は正当再出題（PASS）。
- 副次: 2010h22h の page_image が印刷ページ番号と系統的1ずれ（内容一致、低優先）。

## 確実欠陥の即時修復（ユーザー承認、`scripts/stage026-apply-clear-fixes.mjs`）

backup 付きで 8 件適用・検証済（question_bank + by_year 7 ファイル同期）:
- stem 復元 4（q099-2009h21a / q045-2010h22h / q045-2011h23tokubetsu / q001-2018h30h）。
- has_figure 降格 5（上記 q045×2 + 2019r01a-q096 / 2021r03-q001 / 2024r06-q053）。
- figure_path 孤児切離し 1（2010h22h-q099 → `_rejected`）。
- backup: `question_bank.json.pre-s026-clearfix`、原値 `stem_jp_corrupted_backup` / フラグ `stem_corrected_s72` 等。

---

## 決定（本セッション）

- **D-120 (Locked)**: 連問共有図「グループ共有図モデル」新設（ユーザー選択）。group_id + groups メタ、sibling は複製せず参照。
- **D-121 (Locked)**: duplicate_extraction 系統バグ確認（4件収束）+ 修復方針（PDF 再抽出 + answer_keys から正答復元）+ choice_swap/choice_ocr 新類。

## ユーザー決定（AskUserQuestion）

1. 共有図 → **グループ共有図モデル新設**（D-120）。
2. 複製再スキャン → **全量 stem 近重複 + PDF 照合**（実施済、4件収束）。
3. 確実欠陥 → **今すぐ backup 付き適用**（実施済 8 件）。

---

## 全欠陥目録（現時点）

| defect_class | 件数 | severity |
|---|---|---|
| figure_missing | 13 | critical |
| has_figure_orphan | 13 | critical/minor |
| stem_contamination | 4 | critical×3/minor×1（**修復済**） |
| duplicate_extraction | 4 | critical（要修復） |
| figure_ref_mismatch | 2 | critical |
| stem_choice_mismatch | 1 | critical（要照合） |
| choice_swap | 1 | critical（要照合） |
| choice_ocr | 3 | minor |

---

## 内容欠陥修復 — 完了（セッション後半）

- **確実欠陥8件**（`stage026-apply-clear-fixes.mjs`）+ **内容欠陥9件**（`stage026-apply-content-fixes.mjs`）を backup付きで適用。
- **Rule D 独立検証（code-reviewer ×2, opus）: 17件全 PASS**。証拠 `evidence/phase5/stage_026_fixes_applied.md`。
- 回帰チェック: 2900題/空ゼロ/不正answerゼロ/表紙汚染ゼロ/by_year全29×100。
- 内容正確度フェーズ（学習内容が誤る系）は**完了・検証済**。

## What's Next（残作業 → 新セッション）

**実行仕様書: `docs/phase5/STAGE_2.6_REMAINING.md`（自己完結・ここを読んで実行）。**

1. **図表フェーズ**（D-120、Stage2.6内完結）: 単問固有図16裁剪 + グループ共有図モデル(groups.json)実装。入力 `data/ip/exams/.tmp/s026/figure_worklist.json`。
2. **Phase C**（CI算出）: 層化ランダムN≈100 L1/L2 + L5数値~30 + L-ext外部~30 → critical欠陥率95%CI。
3. **収束判定 + ゲート4条件 + RETROSPECTIVE** → Stage 3。

ultracode 想定 → Workflow 並列推奨。落とし穴は REMAINING.md 末尾参照。
