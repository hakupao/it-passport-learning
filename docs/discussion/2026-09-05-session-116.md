# Session 116 — 2026-09-05 — Phase 2 续批 batch 14 (2009h21a) = **Phase 2 最終回**

启动词「phase2续批」。

## ユーザー gate (AskUserQuestion)

- **Rule C = 同 session で retro まで書く** (2009h21a 完走 → そのまま `RETROSPECTIVE_phase2.md`)
- commit gate = **自動 commit + push** (S105 起 11 session 連続)

## 常設順序 (S115 で 8 点に更新済)

1. s7x 保真核験 (双 pass・別 subagent_type) → 是正 → generate
2. **D-141 中問前文は generate の前に埋める**
3. generate 後 trsweep 全 100 問
4. caveat スキャンは user-facing + `key_guard.note_jp` の両方
5. caveat が指した問は保真核験 workflow に掛け直す
6. 体系 caveat スキャン → explfix → merge → verify-result
7. 全是正後に caveat 除去を 1 回。**除去先は merge の入力** (`expl_jp_/expl_tr_`)
8. `resumeFromRunId` で再開したら再開後の note も再走査

## §0 prep 実測

| exam | s7x-resourced | stem | choices |
|---|---|---|---|
| 2009h21a | **30/100** | 16 | 19 |

Phase 2 全 29 回の s7x 母数: 最大 34 (2010h22a) / 最小は近年回。本回は 30 で古い回らしい水準。

## §0b 既存是正の流入 assert (What's Next の申し送り)

`2009h21a` は **key を 2 度いじった回**なので、収束しているか確認した。

| 問 | 経緯 |
|---|---|
| `q012` | S99 (D-139-B) で ア → ウ に是正 → **S110 が「questions.json の側が誤り」と判断し ウ → ア に差し戻し** |
| `q091` | S110 で「questions=イ / answer_keys=ウ、answer_keys 側が怪しい」として **未是正のまま保留** → S111 でユーザー確認により `answer_keys` を イ に是正 |

**流入 assert PASS**: 現在 `questions.json` は q012=**ア** / q091=**イ**、
`answer_keys.json` も同値で、**2009h21a 全 100 問で key 不一致 0 件**。両者の是正は収束している。

## §1 決定的 junk / 構造スキャン

| 走査 | hit |
|---|---|
| 構造 (分野見出し・設問番号・中問見出し・選択肢の stem 漏出) | 2 (q093 中問見出し / q095 分野見出し) |
| 表示層 junk (末尾ノイズ・語を割る空白) | 2 (q065 ア 末尾ノイズ / q080 エ 区切り脱落)、他 4 は表キャプションの偽陽性 |

## §2 中問グループの構造 — **中問A の前文が corpus に丸ごと欠落**

源 page-32 を実読して確定:

| グループ | 設問 | 前文ページ | 埋める前の状態 |
|---|---|---|---|
| 2009h21a-mqA | q089〜092 | page-32 | **4 問とも前文なし**。〔販売管理業務の概要〕(1)〜(3) と〔販売管理システムの概要〕が corpus のどこにも無い |
| 2009h21a-mqB | q093〜096 | page-36 | q093 のみ前文あり (〔中問B〕見出し + 「問に答えよ」= 源の「問93 〜 96 に答えよ」を潰した形) |
| 2009h21a-mqC | q097〜100 | page-40 | **4 問とも前文なし** (groups.json に 図1 の共有図だけ登録されている) |

**とくに重い**: `q091`「11:00 の注文に対して引当可能な数量は幾つか」は、
前文 (2) の **「通常注文は在庫数の 70% まで引当可能であり、優先注文は在庫数まで引当可能」**
という引当ルールが無いと**解答不能**。`q090` (引当数の求め方の流れ図) も同様。
S115 の 2009h21h-q094 と同じ「設問が参照する当のデータが corpus に無い」型。

## §3 s7x 保真核験 (双 pass) — **Phase 2 で初めて差分ゼロ**

| pass | agentType | 結果 |
|---|---|---|
| pass1 | `general-purpose` | **CLEAN 30 / DISCREPANT 0**・差分 0 |
| pass2 | `pr-review-toolkit:code-reviewer` | **CLEAN 30 / DISCREPANT 0**・差分 0 |

UNREADABLE 0、正解肢上 0、answer_affecting 0。
`wf_db8858b5-fbb` / `wf_8400c064-e80`。

**S109 で保真核験を新設して以来、双 pass とも差分ゼロは本回が初**。
Phase 2 の 29 回で s7x 由来の腐敗が最も少なかった回ということになる
(参考: S115 の 2010h22a は母数 34 で差分 17、2010h22h は母数 32 で差分 43)。
古い回ほど s7x resource 由来が多く腐敗も多い、という S114 以来の傾向に対する**反例**で、
「母数の大きさ」と「腐敗の多さ」は必ずしも比例しないことを示す。

## §4 決定的 junk スキャン — s7x の射程外で 3 件

s7x は `*_resourced_s7x` の問だけを対象にする。下記 2 問はどちらも **s7x 対象外**で、
主 context の決定的 junk スキャン + 源実読だけが捕らえた
(S114 §1b「保真監査と junk スキャンは別ゲート」の再現)。

| 問 | 欠陥 | 源 |
|---|---|---|
| q065 ア | 「4 **パ**イト表記」+ 末尾の空白列とピリオド | 「4 **バ**イト表記」(page-25) |
| q065 ウ | 「国**ど**とに」= 非語 | 「国**ご**とに」 |
| q080 エ | 「文字や図形 静止画像動画像, 音声など」= 読点 2 個脱落 (1 個は空白化) | 「文字や図形，静止画像，動画像，音声など」(page-29) |

**同一設問 (q065) の 2 肢で別々の濁点崩れ**が起きており、この設問だけ OCR の質が落ちている。
**バ/パ は決定的 decoder にとって新しい字形ペア** — S115 の ポ/ボ (2010h22a-q076・2009h21h-q054)
に続き、**濁点/半濁点の取り違えが 3 exam 連続で出た**ことになる。
q080 エ は同設問の ア/イ/ウ が読点を保持しており、**設問内で表記が割れていた**
(S114 §8b と同じ裁決根拠)。是正 `quiz-fidfix-S116-junk.mjs` (choice 3、zh/en は既に正しく波及なし)。

## §5 中問共有前文 (D-141) — 3 グループ **3/3 PASS**

`wf_40a69954-586` (Extract=general-purpose → Verify=code-reviewer)。

### §5a 「図を持たない member」問題 — 前文だけでは自完結しない

D-141 の前文抽出は規約上**図を再現しない** (「図は設問ごとに添付済み」という前提)。
ところが本回はその前提が崩れる member がいた:

| グループ | 図を持たない member | 参照している図 |
|---|---|---|
| 2009h21a-mqB | **q095 / q096** | 図 商品別販売分析ワークシート |
| 2009h21a-mqC | **q098 / q099** | 図1 データベースの構造 / 図2 請求書の様式 |

- `q096`「売上構成比率累計が 70% 以内で商品回転率が 10 回以下の商品は何品目あるか」は
  **ワークシートの数値が無いと数えられない**。q096 は図の添付も無く、本文にも表が無かった。
- `q098`「取引先担当者表のレコードを一意に特定する主キー」は **図1 の列構成が無いと解答不能**。
- `q099`「請求書 1 枚の発行で 4 表を何回参照するか」は **図1・図2 の両方**が要る。

(q095 は自前で表を本文に持っており、q093 も同様。つまり**同じ表が corpus に 2 重にあり、
かつそれを本当に必要としている q096 だけが持っていない**という配置だった。)

→ 共有図をテキスト化して前文に追記する工程を新設
(`quiz-chumon-sharedfig-S116.workflow.mjs`、Extract≠Verify で Rule D 分離)。

### §5b 共有図のテキスト化 (`quiz-chumon-sharedfig-S116.workflow.mjs`)

Extract (`general-purpose`) → Verify (`pr-review-toolkit:code-reviewer`) の 2 段。
検証項目は values_verbatim / structure_preserved / no_fabrication / tr_faithful の 4 つ。

| グループ | 対象 | verdict |
|---|---|---|
| 2009h21a-mqB | 図 商品別販売分析ワークシート (A〜J 列 × 行 1〜12) + 注記 | **CONCERNS** |
| 2009h21a-mqC | 図1 データベースの構造 (4 表) + 図2 請求書の様式 | **PASS** |

mqB の CONCERNS は **figure 本体ではなく notes_jp の記述誤り**だった。起草者は
「I 列・J 列が空なのは網掛け (非表示) だから」と書いたが、検証者が page-36 を 3 倍拡大して
**網掛けは列見出し帯・行番号帯・C/D 列だけで、I/J 列は白地の空セル**であることを確認し、
「I・J が空なのは**前文の『列I，列Jの計算式は…整列した後に入力する』により未入力だから**」
と訂正した。**4 チェックはすべて true** で、検証者自身が「figure の内容は原典どおりで訂正不要」
と明記している。よって図本体を採用 (`quiz-chumon-augment-S116.mjs` が
「4 チェック全 true」を採用条件として assert する)。

検証者は数値の裏取りも行っており、起草者側も B 列合計 11,680 の検算と
G 列 = B ÷ F の 10 行分の検算で読み取りを自己検証していた。

### §5c 正規化 → 前置

`quiz-chumon-normalize-S116.mjs` (3 操作 × 3 言語):

- `q093` 中問見出し (源「問93 〜 96 に答えよ」→「**問に答えよ**」と対象範囲を潰した形。
  S115 の 2009h21h-q097 と同型) + 共有前文 + **重複したワークシート表**を除去 → 設問文のみに
- `q095` 分野見出し〔ストラテジ〕を除去 + **重複したワークシート表**を除去
  (設問固有の〔条件〕とランク表は残す)

**元の配置は歪んでいた**: 表を持っていたのは q093 と q095 で、
**本当に表を必要としている q096 だけが持っていなかった** (q096 は図の添付も無い)。
共有図を前文に 1 回だけ置き、全 member が同じものを見る形に正した。

`quiz-chumon-preamble-apply.mjs`: 3 グループ / **12 問**に前置 / skip 0。
3 言語整合チェック **12/12** (前文がちょうど 1 回・先頭・設問本文が非空)。

## §6 流入 assert と generate

`build-quiz-corpus` (2900 問不変) → prep (100 問 / figure 19 / tr 100/100) →
**流入 assert 19/19 全通過** (肯定 14 + 否定 5)。

とくに次の 4 件は「前は解答不能だった」ことの直接確認:

| 問 | 流入したもの | 無いとどうなったか |
|---|---|---|
| q091 | 「優先注文は在庫数まで引当可能」 | 引当可能数量を計算できない |
| q096 | ワークシート表 (合計 11,680 行まで) | 累計 70% 以内の品目を数えられない |
| q098 | 図1 取引先担当者表の列構成 | 主キーを答えられない |
| q099 | 図2 明細表の列構成 | 参照回数を数えられない |

generate `wf_ba10d75c-18f` (task wx4rv9kto) 起動。**本 session は 1 exam のみ**なので
S115 のような並行過多による limit 被弾リスクは低い。

## §7 generate 結果

| exam | 結果 | agent / tok / 時間 |
|---|---|---|
| 2009h21a | 100/100・**jp PASS 100**・tr PASS 96 / CONCERNS 4・error 0 | 436 / 14.6M / 42 分 |

session limit 被弾 **0** (1 exam のみで並行度を上げなかったため。S115 の教訓の裏返し)。

## §8 **key_guard が S111 のユーザー確認済み裁決の誤りを炙り出した** — 2009h21a-q091

`suspect` 1 件。`matches_key=false`、derived=**ウ**、stored=**イ**。

### §8a 何が起きていたか

問91「11:00 の注文に対して引当可能な数量は幾つか」は**中問A (問89〜92) の一部**で、
引当規則は**前文にしか書かれていない**:

> 〔販売管理業務の概要〕(2) …**通常注文は在庫数の 70％まで引当可能**であり，
> **優先注文は在庫数まで引当可能**である。… (源 page-32)

**この前文は corpus のどこにも存在しなかった。本 session の D-141 適用で初めて入った。**

S111 §5 はその状態でユーザーに判断を仰いだ。当時提示した計算は S111 の log に残っている:

> 引当計算: 100 − 80 − 10 = 10 ⇒ **イ**。優先注文を先に引当てる順序でも同じ 10。
> **ウ = 14 は表のどの数からも導けない**。

70% 規則を知らなければ完全に妥当な推論で、ユーザーは「10 で正しい」と確認し、
**元々 ウ だった `answer_keys` が イ に変更された**。

### §8b 前文を入れた後の導出 (主 context が page-32 + page-35 を実読)

| 時刻 | 種別 | 注文数 | 引当可能 | 引当 | 残在庫 |
|---|---|---|---|---|---|
| — | — | — | — | — | 100 |
| 10:00 | 通常 | 80 | 100 × 70% = **70** | 70 | 30 |
| 10:30 | 優先 | 10 | 在庫 30 まで | 10 | 20 |
| 11:00 | 通常 | 40 | **20 × 70% = 14** | — | — |

⇒ **ウ (14)**。**選択肢の構成が決定的な裏づけ**になる:

| 肢 | 値 | 由来 |
|---|---|---|
| ア | 7 | 10 × 70% (在庫を 10 と誤る) |
| **イ** | **10** | **100 − 80 − 10 = まさに「70% 規則を無視した」誤答肢** |
| ウ | 14 | **正解** |
| エ | 20 | 在庫 20 のまま (最後の 70% を忘れる) |

**S111 が選んだ イ は、この設問が用意した最も典型的な罠そのもの**だった。
補足: `by_year/2009h21a.json` は**一貫して ウ のまま**で、S111 の変更が上流に伝播していなかった。
元の抽出値が正しかったことの追加証拠になる。

### §8c 決着 (ユーザー再確認済み)

AskUserQuestion で経緯・計算・選択肢構成を提示し、**「ウ(14) に是正する」を選択**。
`quiz-keyfix-S116-2009h21a-q091.mjs` で `question_bank` / `answer_keys` / `by_year` の
**3 層を是正**し、**交叉核対 mismatch 0 / 2900 問**を自走確認。
Rule B の `failures/quiz_phase1.5_S98_2009h21a-q091_answerkey_mismatch.md` に経緯を追記 (削除なし)。

### §8d 教訓 — 「源の一部が corpus に無い状態で key を裁決してはいけない」

S111 の**手続きは正しかった**。推測で公式記録を書き換えず、材料を揃え、
否定的証拠 (「ウ は表のどの数からも導けない」) まで示してユーザーに渡した。
それでも誤ったのは、**提示した材料そのものが不完全だった**から。

> **「表のどの数からも導けない」という否定的証拠は、表しか見ていないなら何も否定していない。**

→ key 裁決の前に「この設問が参照している源の要素が全部 corpus にあるか」を確認する。
   中問なら前文・共有図が揃っているか。揃っていないならまず揃えてから裁決する。

そして **D-141 の前文投入は、解答可能性を回復するだけでなく、
過去の key 裁決を自動的に再検証させる**効果を持つことが実証された。

## §9 note 起点の掃引 (常設順序 ④⑤)

merge 後の `key_guard.note_jp` を全 100 問走査し、**未是正の表示層欠陥を 10 問**抽出
(q006 / q032 / q053 / q058 / q063 / q069 / q070 / q078 / q085 / q096)。
**s7x 保真核験は 0 件だったのに 10 問出た** — s7x は `*_resourced_s7x` の問だけを見るため、
**それ以外の問の選択肢腐敗は素通りする**という射程の限界がここでも出た。

caveat 自体を信じず、s7x 保真核験 workflow に掛け直す (双 pass, Rule D):
`wf_baff2a88-b58` (gp) / `wf_884f5a9f-32a` (cr)。

## §10 掛け直しの結果 — **s7x が 0 でも note 掃引は 9 問を捕らえた**

| pass | 結果 |
|---|---|
| `wf_baff2a88-b58` (general-purpose) | CLEAN 1 / DISCREPANT 9・差分 **17** |
| `wf_884f5a9f-32a` (code-reviewer) | CLEAN 1 / DISCREPANT 9・差分 **17** |

**双 pass 完全一致**。**正解肢上 4 件**、answer_affecting 0、UNREADABLE 0。

**s7x 保真核験は 30/30 CLEAN・差分 0 だったのに、note 掃引 → 掛け直しでは 17 件出た。**
s7x は `*_resourced_s7x` の 30 問しか見ないので、**それ以外 70 問の選択肢腐敗は構造的に素通りする**。
「s7x が CLEAN だから安全」とは言えないことが、最もはっきり出た回。

### §10a 欠陥の 2 系統

**(A) 字形の置換 (semantic 5 件)** — いずれも日本語として成立しない非語:

| 問 | 欠陥 | 源 |
|---|---|---|
| q006 ウ | 設**罰**した | 設**置**した |
| **q063 イ (正解肢)** | **デークタ**更新 | **データ**更新 |
| **q078 ア (正解肢)** | 高速性や**硬**故障性 | 高速性や**耐**故障性 |
| q078 イ | 小容量の**科**体 | 小容量の**筐**体 (ルビ「きょう」付き) |
| q085 エ | **披**認証者 | **被**認証者 (肢ウと同語) |

**q078 ア は正解肢で、RAID の主目的である「耐故障性」という語そのものが壊れていた。**

**(B) 右余白のスキャン汚れを OCR が文字と誤認した幻字 (cosmetic 12 件)** — 7 問に及ぶ。
「「」「」」「-」「ーー」が末尾に付着。**q085 エ はページ下部のノンブル「− 30 −」まで
選択肢に流れ込んで**いた (S114 §8b / S115 の 2009h21h-q084 と同型)。
**幻字が 7 問に及ぶのは Phase 2 で最も高い密度**で、2009 年度秋のスキャン品質に起因する
系統的なもの。個別の OCR ミスではない。

是正 `quiz-fidfix-S116b-caveat.mjs` (choice 17、zh/en は全件既に正しく波及なし)。

### §10b 保真核験が CLEAN と言った問を、別の理由で直した — q069

双 pass は q069 を **CLEAN** と判定した (源との語句一致は保たれているため。この判定は正しい)。
しかし generate の note は別の観点で指摘していた:

源は 4 行 × 3 列の 1 つの表で ①②③ の見出しを 4 肢が共有している。線形化の結果、
**ア だけが表ヘッダ行ごと抱え** (`[表] | | ① | ② | ③ | / |---| / | ア | POP3 | POP3 | POP3 |`)、
**イ/ウ/エ は列見出しの無い断片** (`[表] | イ | POP3 | SMTP | POP3 |`) になっていた。
アプリは選択肢を単独表示するので、どの列が何かが読者に分からない。

zh / en は既に「① POP3　② POP3　③ POP3」と自完結形式に整えられており、**JP だけが表断片**だった。
JP を訳文に合わせて正規化 (`quiz-fidfix-S116c-q069.mjs`、SMTP/POP3 の並びは assert して不変)。

**保真監査 (源との一致) と表示体裁 (単独表示で読めるか) は別ゲート**という S114 §1b の構図が、
今度は「保真核験が CLEAN と言った問」の側で出た。

## §11 explfix → merge → verify

`quiz-phase2-explfix-S116.mjs`: **23 問**に裁決注記を追記 (q091 の key 是正を含む)。
merge: explained **100/100**・missing 0。verify-result **GREEN**。
SUSPECT / STEM-CORRUPTION の表示数は union(round1, final) のため是正後も残る = 設計どおり。

## §12 trsweep 全 100 問 — 不成立 1 件、しかし**再訳は採らなかった**

`wf_c345531c-d7c` (102 agent / 4.8M tok / ~9 分)。**audited 100・unfaithful 1** (`q071`)。

監査指摘は 4 点で、うち 1 件が意味の反転だった:

| field | 指摘 |
|---|---|
| `distractors.ウ` (zh) | JP「チェーンメール (**不幸**の手紙型メール)」を zh が「**幸运**信」= 幸運の手紙と反転 |
| `distractors.ウ` (en) | 同じ括弧書きが en から完全に脱落 |
| `points[1]` (en) | JP「書き込み型」に対し `post-and-read` と、JP に無い read の側面を追加 |
| `distractors.イ` (en) | JP「同じメールを配信する**仕組み**」の『仕組み』(機構) の明示が落ちていた |

### §12a 核験が 2 段とも止めた — そして 2 段目は「disk 版より悪化した箇所」を数えた

trsweep は JP 源から全面再訳を生成した。**独立核験 (round 1) は `terminology_correct=false` で CONCERNS**。
理由は `垃圾邮件（迷惑邮件）` — 日本語漢字「迷惑」の直輸入で、大陸中国語では『当惑させる』の意になる。
**コーパス実測 0/2900**。併せて日本語読点「、」の残存。

主 context がこの 2 点を機械置換で当てた版を、**さらに別 agentType (round 2)** に掛けた
(`wf_a8ebd98d-446`、`quiz-trsweep-reverify-S115.workflow.mjs` を再利用)。判定は再び **CONCERNS**。
ただし内容が S115 より一段進んでいた:

> **是正版が disk 版より悪化している項目 (計 4 件、明示的に指摘):**
> 1. `向登记者同报投递` ← disk `向登记者群发同一内容` (『同报』はコーパス実測 1 件、『群发』14 件) — MEDIUM
> 2. `…等，写入消息以…的系统的说明` — 挿入した読点が連体修飾語と被修飾名詞を切断し『的』が二重積層 — MEDIUM
> 3. 語釈なしの裸 `opt-in` ← disk `选择性加入（opt-in）` — LOW
> 4. `「ア」正确` (字母をカッコ囲み、コーパス実測 0 件) ← disk `ア 正确` — LOW

**この 4 件を挙げられたのは、核験プロンプトに「是正版が現行の disk 版より悪くなっている項目があれば
必ず指摘すること」と明示的に命じてあるから**である (S115 で追加した一文)。
この一文が無ければ round 2 は「監査指摘 3 点は解消された」で PASS を出しえた。
**改善だけを見て退行を見ない検証は、検証ではない。**

### §12b 方針転換 — 再訳は既定手段ではなく最終手段

そこで**全面差し替えを捨て、disk 版に監査指摘 3 点だけを当てる**方式に切り替えた
(`explfix2-S116` の E 群、4 箇所)。

通算を数えると、trsweep の再訳は **S115 の 3 件 + S116 の 1 件 = 4/4 すべて核験に止められている**。
止められた理由も一貫していて、いずれも**コーパス実測 0〜1 件の非標準語**を持ち込んでいた
(`体力` 0/2786・`运转时间` 0/2800・`专业特化` 0/2800・`迷惑邮件` 0/2900・`同报` 1/2900)。

> **再訳は「直す」と同時に「コーパス既定から外れる」劣化を持ち込む。全面再訳が無条件に良くなったことは一度も無い。**

### §12c round 2 が言語化した、機械置換の構造的欠陥

> round-1 が捕捉した `迷惑邮件` は「**日本語漢字の直輸入**」という欠陥クラスの**一事例**に過ぎず、
> 同じクラスの別事例である `同报投递` が手つかずで残った。

S115 でも同型を踏んでいる (括弧内と裸語を同時置換して `集成测试（集成测试）` の同語反復を作った)。
**指摘は「その箇所」ではなく「そのクラス」として受け取り、全文を再走査すること。**

再訳案と 2 段の核験結果は Rule B に従い `failures/quiz_phase2_S116_2009h21a_trsweep/` に保管
(README.md + attempt1_retranslation_{raw,fixed}.json + verify_round{1,2}.json)。

## §13 Rule A 独立抽検 (N=40) — accurate 35/40・medium 5

`wf_cb5bc196-237` (40 agent / 2.0M tok / ~4 分)、critic = `pr-review-toolkit:code-reviewer`。

| 指標 | 値 |
|---|---|
| accurate | **35/40** |
| severity | none 11 / low 24 / **medium 5** |
| keyGuardMismatch | **0** |
| bad key (independent_answer ≠ stored key) | **0/40** |

### §13a 実在の欠陥 4 件

- **q064 ウ — 誤答の「由来」説明が捏造だった。** 旧文は「8進数 55 の上位桁 5 を 16 の重みで数えた
  場合に生じる誤り」と述べるが、5×16+5 = 85 = 0x55 であって **4D にはならない**。
  **存在しない機構を説明していた。** 数値事実だけを述べる形に是正 (4D = 77(10) = 115(8) ≠ 45(10))。
- **q096 イ — 説明が自己矛盾していた。** 「商品こを含めた場合に 2 品目」と述べながら、同じ文で
  「こは 70% 超・回転率 10 回超なので対象外」と自ら否定していた。しかも こ を足しても該当は
  え 1 品目のままで 2 にはならない。正しい誤り筋は「10 回**以下**」を「10 回**以上**」と読み違える型
  (累計 70% 以内の え 9.2 / う 12.0 / あ 14.4 のうち 10 回以上は う・あ の 2 品目)。差し替え。
- **q078 — 陳腐化 caveat。** 「文中の『硬故障性』は『耐故障性』の誤植」が残存。fidfix-S116b で
  既に是正済で、学習者の画面に「硬故障性」は無い。**RAID の核心語**に関する注記だけに混乱コストが高い。
- **q085 エ — 陳腐化 caveat。** 「文末には原典のページノイズが混入している」が残存。既に是正済。

いずれも jp/zh/en の 3 言語に同じ記述が伝播していたため 3 言語まとめて是正 (`explfix2-S116` の A〜D 群)。

### §13b 偽陽性 2 件 — **標本が「表示 (clean) stem」を見せる仕様に由来する**

critic は q064 と q017 について「note_jp が stem の腐敗を『除去済』と書くが、**標本の stem に
そんな記号は無い = 捏造的説明**」と medium / low で指摘した。これは**偽陽性**である。

実測すると `question_bank.json` の raw stem には確かに `ーー` があり (q064)、
`stem_jp_clean` で除去されている。`ruleA-prep` は critic に**表示 (clean) stem** を渡す仕様
(`displayStem()`、S100 以来「critic が監査するのは学習者が見るもの」という設計) なので、
**腐敗前の字が標本に現れないだけ**だった。note は正しい。

> **標本の作り方が、監査結果の解釈を変える。** critic に「学習者が見るもの」を渡す設計は正しいが、
> その結果 critic は「是正履歴の妥当性」を検証できない。両者は別のゲートが要る。

裁決注記にこの偽陽性の判定理由を明記した (`explfix3-S116`)。

### §13c 未是正で backlog に送ったもの — q017 の hosting/housing zh 訳語

critic は q017 の zh 訳が「本土 IDC 業界の慣行と**逆転**しており、本問の核心 (ハウジング vs
ホスティングの切り分け) で読者を誤導しうる」と medium で指摘した。指摘は妥当である。

ただしこれは **S112 以来 5 exam 連続で挙がっている横断 D 案件**で、corpus 全体の訳語統一を
伴うため本 session で単独に決めない。**ユーザー判断待ちの backlog として維持**する。

## §14 確認 Rule A — 5/5 accurate・medium 0

`wf_498422bd-568` (5 agent、是正した q064/q071/q078/q085/q096)。

| 指標 | 値 |
|---|---|
| accurate | **5/5** |
| severity | none 1 / low 4 / **medium 0** |
| keyGuardMismatch | **0** |

新たに 1 件、技術的不正確が出た (low): q078 の「誤り訂正用のパリティを**別ディスク**に置く方式
(RAID 5 など)」は、パリティ専用ディスクは RAID 3/4 で **RAID 5 は全ディスクに分散配置**する。
合否には影響しないが「RAID 5 = パリティ専用ディスク」という誤概念を植え付けるため
3 言語とも是正 (`explfix4-S116`)。**確認 Rule A が「是正の副産物」ではなく「元からあった誤り」を
拾ったのは Phase 2 で初めて**で、確認 Rule A の射程が是正箇所に限られないことを示している。

## §15 Phase 2 完了 — 29/29

merge: explained **100/100**・missing 0。verify-result **GREEN** (results 100/100)。

| 検証 | 結果 |
|---|---|
| `questions.json` | **2900 問** 不変 |
| `2009h21a` | 100 問・explanations 100 問 |
| `correct_answer` 変更 | **1 件のみ** = `2009h21a-q091` イ→ウ (§6 でユーザー承認済み) |
| `tsc --noEmit` | 0 error |
| `eslint` | 0 error (既存 warning 1 = tTerm) |
| `vitest` | **463 passed / 2 skipped** (30 files passed / 1 skipped) |
| `next build` | exit 0 |

**Phase 2 (D-137 解説預生成) = 29/29 完了。2900 問すべてに三語解説が載った。**

## §16 Rule C — `RETROSPECTIVE_phase2.md` 執筆

ユーザー gate「同 session で retro まで書く」に従い、Phase 2 全 29 回 (S97〜S116) の retro を執筆。
構成は規則どおり 3 部 + サマリ + 付録。素材は session log 20 本を独立 agent に実測集計させた。

要点だけ引くと:

- **保留する做法**: key-guard 内蔵 (round1 永続化つき) / 保真核験を generate の**前**に置く /
  双 pass 別 agentType / 検証者に「比較対象」を持たせる / 決定的工程を LLM から取り上げる /
  中断前提の `resumeFromRunId` / 是正は最上流の層に入れる。
- **必ず補う缺口**: **保真核験の射程が母数の一部 (521/2900) しか覆っていない** /
  ゲートごとの射程を宣言していなかった / 材料が不完全なままの key 裁決 (q091 を 3 度誤った) /
  機械置換がクラスを取りこぼす / **主 context 自身の誤りが通算二桁** / 横断 backlog 9 件。
- **決策複盤**: D-137 は 4 点とも変えずに済んだ。**D-140 の ADR を書かなかったのは失敗**
  (14 session 参照され続けた)。**D-141 は lock が遅すぎた** (同型が 8 クラスタ溜まってから)。
  「全面再訳 → 指摘点だけを当てる」への方針転換 (通算 4/4 で核験が止めたため)。

一行でまとめた結論:

> **Phase 2 が本当に作ったのは 2,900 問の解説ではなく、「解説を書く前に問題文が源と一致しているかを
> 疑う」という工程だった。そして最後まで残った穴も同じ形をしている — 射程が母数を覆っていない
> ゲートは、CLEAN と言っても何も保証していない。**

## §17 Pre-close self-check (D-027 §5)

| 項目 | 状態 |
|---|---|
| 新 D | なし (D-141 まで、141 件で不変) |
| OQ | 新規・クローズともなし |
| `docs/STATE.md` | 更新済 (Phase 2 = 29/29 完了、次段階 = 横断 backlog / Stage 6 の残り) |
| session log | 本ファイル (§1〜§17) |
| `RETROSPECTIVE_phase2.md` | 新規作成 (Rule C) |
| evidence | `s7x_fidelity_S116_*` / `caveat_fidelity_S116_*` / `chumon_{preamble,sharedfig}_S116` / `trsweep_S116_2009h21a` / `trsweep_reverify_S116_r2` / `ruleA_S116_2009h21a` / `ruleA_confirm_S116_2009h21a` |
| failures (Rule B) | `failures/quiz_phase2_S116_2009h21a_trsweep/` (README + 再訳案 2 版 + 核験 2 ラウンド + BEFORE 10 本) |
| scripts | `explfix2-S116` / `explfix3-S116` / `explfix4-S116` を新規追加 |
| 検証 | 全 GREEN (§15) |
| commit gate | ユーザー gate = 自動 commit + push |

## §18 What's Next

**Phase 2 は完了した。次は横断 backlog の処理で、いずれもユーザー判断が要る。**

優先度順 (retro §2.6 の表と同じ):

1. **s7x 未核験 462 問 / 27 exam** — 乖離見積り 200〜280 問、うち answer-affecting 数問〜十数問。
   Phase 2 最大の残穴。**射程を母数まで広げるかどうかが最初の判断。**
2. **図/シナリオ再抽出 track** — 選択肢が図そのもので、テキスト化した時点で出題構造が壊れる型。
   S108 以降 5 exam 分が蓄積。
3. **hosting/housing の zh 訳語横断 D** — 6 exam 連続で medium。**軸が反転したまま出荷中の問がある。**
4. **既存 18 exam の sidecar 再 merge** (S110 の key_guard final publish の波及) — 6 session 連続で未実施。
5. **semantic choice-OCR cleanup track** — committed 16 exam 対象、S108 以降 実施記録なし。
6. `answer_keys` ↔ `questions` 交叉核対の **CI 化** (現在 mismatch 0/2900)。
7. `source.page_image` のスキーマ問題 / 中問先頭問のページずれ候補 19 件。
8. explfix が round1 の note 本文を書き換えない件 — **運用が session 間で割れているので D で固定する。**
9. cosmetic 横断: stem 前置の分野見出し 42 問 / ASCII 引用符 139 問 / zh 本土 polish / 図クロップの端切れ。
