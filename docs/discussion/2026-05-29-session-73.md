# Session 73 — Stage 2.6 残作業（図表フェーズ + Phase C CI + ゲート）

> 日付: 2026-05-29 / フェーズ: Phase 5 Stage 2.6（実施阶段・収尾）
> 実行仕様: `docs/phase5/STAGE_2.6_REMAINING.md` / ゲート: D-119 / 図モデル: D-120
> 前提: Session 72 で Phase A/B + 全量複製スキャン + 内容欠陥17件修復（Rule D PASS, commit 5709128）完了。

## 開始時の状態（検証済）

- question_bank: 2,900 題 / 空答案0 / 内容欠陥17件 修復・独立検証済。
- 残欠陥はすべて**図表系**（学習内容ではなくレンダリング層）: 単問固有図16 + 連問共有図10（+ no-defect shared_preamble 18 を恒久解消）。
- env: PIL 11.3.0 / node v25.9.0 / git clean / figures/ 498 PNG。

## 本セッションのタスク

1. **図表フェーズ（D-120）**
   - (a) 単問固有図16件: estimate→crop→verify→finalize ループ（Session71 機構を流用、`.tmp/s026/fig/` に隔離）。
   - (b) 連問共有図: `groups.json` 新設（D-120）、共有図1枚を前文ページから裁剪しグループ紐付け、member は group_id 参照（複製せず）。
2. **Phase C — CI 算出**: 層化ランダム N≈100（L1 再解答 + L2 跨字段）+ L5 数値~30 + L-ext 外部~30 → critical 欠陥率 95%CI（Wilson）。Rule D: read-only explore/code-reviewer。
3. **収束判定 + ゲート4条件 + RETROSPECTIVE** → Stage 3。

## 決定（本セッション）

（随時追記）

## 進捗ログ

### 図表フェーズ (a) 単問固有図16 — ✅ 完了

- 機構: `stage026-fig-{prep,crop,collect,finalize}.mjs`（Session71機構を `.tmp/s026/fig/` に隔離・流用）。
- ESTIMATE(general-purpose×16) → crop → VERIFY(explore×16, Rule D)。R1: 12 PASS / 4 FAIL（表ヘッダ/行/キャプション欠け）→ verifier hint で bbox 調整 → R2: 4/4 PASS。
- 跨ページ補正3件: q093→p36 / 2014h26a-q099→p50 / 2015h27h-q089→p38（page_image 不在ページ → 隣接ページに図実在）。
- finalize: 16 figures 添付、by_year 7ファイル同期、旧crop 10件を `_rejected/` 退避（Rule B）。`figure_repaired_s73=true`。
- 検証: 16/16 figure_path+bbox+file 整合、2900題/空答案0。

### 図表フェーズ (b) 連問共有図グループ（D-120）— ✅ 完了

- 候補16グループ列挙（中問ブロック）→ vision discovery（general-purpose×16）で中問ヘッダ「問XX〜YYに答えよ」から member 範囲確定 + 共有図 bbox。全16グループで明示ヘッダ確認。
- crop（14新規 + owner再利用2: 2015h27a-q085/q089）→ VERIFY(explore×16, Rule D)。15 PASS / 1 FAIL（2009h21h-mqB 図2薄切れ）→ 4ラウンド bbox 拡張で PASS。
- `groups.json` 新設（16グループ、schema: group_id/exam/label/header_quote/shared_figure{path,page_image,bbox_pct,caption}/member_qids/owner_qid）。member 64題に `group_id` stamp、by_year 8ファイル同期。
- L4 figref-no-figure: group解決28件。

### ⚠️ 新発見（センサス recall gap）— 要トリアージ

Phase A センサスの strong_regex は **(1) `図の○○`（番号なし図参照）** と **(2) `has_figure` フラグ vs `figure_path` 整合性** を測っていなかった。本セッションの全量再スキャンで:
- **`has_figure=false` かつ `figure_path` 設定済（ファイル実在）= 110件**。stem に図参照キーワード無しでも、損益計算書/JANコード構成/ワークシート等の **インライン表**が実在するケース多数 → regex 分類不可、vision 要。
- `図の` blindspot 候補13件中、真の図参照 ~7件（アローダイアグラム3 / CAPTCHA / ランサム画面 / 作業分割図 / 3ワークシート）。多くは figure_path 既設・has_figure=false。
- 真の orphan 0 / broken path 0（その2軸はクリーン）。group member の旧 has_figure=true 残留 23件（要 false 化、図はgroup提供）。

→ 代表15サンプル: genuine 14/15。**ユーザー決定: 全110件 vision 精査・修正**（AskUserQuestion）。

### has_figure 整合性修復（110 + 7 orphan）— ✅ 完了

- vision 全量トリアージ N=117（main 101 + supp 10 + sample 15、Rule D explore）。sample×main 重複9件で一致8/9（89%）。
- 結果: genuine 75 / spurious 31 / crop_quality 4（flag）+ own_subfigure 2 / group_only 5（orphan）。
- 適用（`stage026-apply-figflags.mjs --commit`、backup `pre-s026-figflags`）: **has_figure=true 設定73** / spurious 除去+`_rejected`退避31 / group_only降格5。
- 再裁剪8件（crop_quality 6 + orphan own-subfigure 2）: round3 estimate→crop→verify(Rule D)→finalize。7 PASS 添付 + q085(2012h24a)は固有図なし→group_only降格。
  - `2018h30h-q019`（損益計算書）は verifier 2体が不整合（一方は架空行、一方は過少）→ **主ループ直接視認**で bbox 確定（教訓: flaky verifier は主ループ視認で裁定）。
- **最終整合性: stale 0 / orphan 0 / broken 0**。has_figure=true 358→467。group_id 64。証拠 `evidence/phase5/stage_026_figflag_triage.md`。

### Phase C — CI 算出（✅ 完了）

- 層化サンプル N=100（seed 20260529、figure 33/calc 32 オーバーサンプル、29套）+ L5×30 + L-ext×30。Rule D（explore + code-reviewer adjudication）。
- **結果**: answer_keys **100%信頼**（誤キー0、q073 は公式PDF実読で ア 確認＝L-ext の ウ は PDF行ずれ誤読）。**確定 critical stem/choices 欠陥 17/100、Wilson95 10.9–25.5%、層化後母集団≈12%（≈360題）**。全て OCR garble or 内容不一致、キーは正しい。
- **q085 型**（自洽だが別問題）= L1L2 偽陰性、Phase B+L-ext+手動源照合で捕捉 → **plausible-but-wrong は stored-vs-source 照合でのみ確実検出**。
- L5 新 critical 0（収束）。証拠 `evidence/phase5/stage_026_phaseC_ci.md`。

## 決定（本セッション locked）

- **D-122 (Locked)**: Stage 2.7「全量 stem/choices 源照合・修復」新設（ユーザー選択: 全量 stored-vs-source 照合→欠陥のみ修復）。Phase C で stem garble≈12%・q085型発見、answer_keys は100%健全。Stage 3 ゲートに追加。ADR: `docs/decisions/D-122-stage-2.7-stem-source-audit.md`。
- 副次記録（minor 決定）: 試験ID接尾辞 **h=春(haru) / a=秋(aki)**（IPA PDF タイトル確認）。era 表示ラベル付与時に注意（question_bank 非保存ゆえ現データ欠陥ではない）。

## ゲート状態（D-119 + D-122）

1. センサス全件 triage ✅（L3/L4/L6 + has_figure 全直積）。
2. 抽样収束+CI+受容: CI 記録済 ✅、ユーザーは Stage 2.7 修復を選択（受容は修復後の再CIで）。
3. 既知シード解消 ✅（孤児/不整合/0↔9/q026型/dup4/choice — 全て S72-73 で処理、Phase C で seed14 全PASS確認）。
4. Tier3 証拠 ✅（evidence/phase5/stage_026_*）。
5. **Stage 2.7 完了** ⏳（D-122 新条件）。

## What's Next（→ Stage 2.7、自己完結仕様 `docs/phase5/STAGE_2.7_PLAN.md`）

1. ヒューリスティック garble スコアラ（全量・速報）。
2. 全2900題 vision 源照合（Rule D explore、ページ単位バッチでトークン最適化）→ garble+内容不一致 分類。
3. critical のみ源から再転写・修復（backup付、answer_keys 不変）。
4. 修復後 再サンプル CI → ゲート → Stage 3 + RETROSPECTIVE。

**注意**: Session 73 で図表・has_figure・answer_keys は確定済。Stage 2.7 は **stem/choices テキストのみ**対象。流用: `stage026-phaseC-{sample,ci}.mjs`。落とし穴は STAGE_2.7_PLAN.md 末尾参照。
