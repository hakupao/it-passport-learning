# Session 91 — Quiz Phase 1 (翻訳 backfill) スケール バッチ S91 (3 回)

> 日付: 2026-06-15 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 Phase 1 スケール
> 起動: ユーザー路由「Quiz Phase 1 续批」(回数未指定)。前提: S90 完了・commit 済 (`f6eb8a8`)、翻訳済 10/29 回。

## 0. 起点

- STATE + session-90 読了。回数未指定 → 路由②「最新優先・既訳 10 回除外で 3〜5 回」→ **最新 3 回 = `2018h30h` / `2017h29a` / `2017h29h`** (STATE「次候補」と一致)。各 100 問 = 300、figure 13/12/10=35。

## 1. 実行設計

S88〜S90 の統合 1 ワークフロー (D-小5) + フルページ併読 (D-小6) + repair 語義ガード (D-小7、scripts 組込済) を踏襲。加えて:

**小決策 (session log のみ)**:
- **D-小8 (統合バッチ combiner のスクリプト化)**: S88〜S90 でインライン node で構築していた統合バッチファイル (input_batch / items_batch / sidecar_batch / ruleA_items) を決定的ヘルパ `scripts/quiz-phase1-batch.mjs` に切り出し。`translate`/`ruleA` 2 モード、count・id 一意・forced id を assert、per-exam 生成物の純 concat (RNG なし、既存生成物を不変更)。残 16 バッチの再現性・監査性向上。merge の coverage 検査が網兜として残るため combiner バグは loud に落ちる。

## 2. 実施

| Step | 内容 | 結果 |
|------|------|------|
| prep ×3 | crop+page fail-fast + glossary 束縛 | 全存在 (figure 13/12/10=35)、統合 input 300 問・id 全一意 |
| batch translate | `quiz-phase1-batch.mjs translate S91` | `input_batch_S91.json`/`items_batch_S91.json` (S90 と形態一致、figure 35 全フルページ付) |
| translate (統合) | `wf_0cd70973-55c`、writer(general-purpose)→reviewer(code-reviewer)2R | **300/300、raw PASS 296 / CONCERNS 4 / FAIL 0**。666 agent / 24.3M tok。pause 指示なし→全量完走 |
| merge ×3 | per-exam committed sidecar | 各 100/100、missing 0。clean stem 58/46/50 |
| CONCERNS triage | 4 件を repair 後の実落盘訳文で精査 | q090/q019 = repair 済で核心是正・q019 残 low を 1 フィールド修正・q003/q069 = low-only 受容 (後述) |
| q019 修復 | stem.zh 1 フィールド (`营业税`→`事业税`) + assert 唯一一致 | en/choices 不変 |
| batch ruleA | `quiz-phase1-batch.mjs ruleA S91` + forced 4 CONCERNS | sidecar_batch_S91 (300) + ruleA_items_S91 (37) |
| ruleA (統合) | `wf_e5514cf3-523`、N=37 層化独立 critic | **37/37 accurate**。session limit で 7 失敗→ `resumeFromRunId` で補完 (resume 4 回目実証) |

## 3. 結果

### Rule D in-pipeline: raw 296 PASS / 4 CONCERNS / **FAIL 0** → 実効 297 PASS 相当 / 3 CONCERNS

**triage は verdict ラベルでなく repair 後の実落盘訳文で判定** (構造検査≠意味検査、S88 q072 の教訓):

- **`2017h29h-q090` (figure, SQL LIKE)**: R1 medium = zh の `うどんすき`→`什锦乌冬面` 訳が操作c の前方一致 (`うどん%`=`乌冬面%`) を破壊し zh で c=1 (JP は c=2)。**repair が `乌冬面火锅` (前方一致保持) へ是正済** → zh 再計算 a=4>b=3>c=2→ア = JP/en 一致。受容。Rule A: accurate/none。
- **`2018h30h-q019` (figure, 損益計算書)**: R1 high = zh `特別利益/特別損失`→`营业外利得/损失` が `営業外収益/費用` 行と会計区分混同。**repair が `特别利益/特别损失` へ是正済** → 残 low (`事業税`→`营业税`=廃止済の別税目) を **stem.zh 1 フィールドのみ `事业税` へ定点修正** (en/choices 不変、唯一一致 assert)。Rule A 独立再監査: accurate/none (修正確認)。
- **`2017h29a-q003`** (貸借対照表) / **`2017h29h-q069`** (DB正規化): in-pipeline は low-only (`〜の部`/資格名字義訳)。受容。

### Rule A (独立 critic, N=37 [各回12 + 強制4、層化 figure24]) — **accurate 37/37 (100%)**

- **severity none22 / low14 / medium1 / high0、not-accurate 0**。
- **medium 1 = `2017h29h-q069` = 上流 (Stage 2) OCR 欠陥の申し送り (翻訳忠実度は PASS、accurate=true・4 check 全 true)**:
  - zh/en の選択肢は input `choices_jp` を 1:1 完全忠実訳出 (機械照合済)。問題は **input.choices_jp 自体**が権威フルページ `pages/2017h29h/page-29.png` とズレ (選択肢イ=表1第3列/表2第1列の誤OCR、選択肢ウ=表2から `社員名` 脱落)。翻訳は誤 input を忠実に引き継ぐ。
  - **正解ア は権威・input 一致、訳も正確で正解根拠不変**。誤選択肢イ/ウ が配信されると公式過去問と異なる → **上流 choices_jp 再OCR・是正が必要 = backlog (Stage 2 scope、ユーザー判断待ち、翻訳 scope 外)**。S89 q011 / S90 q061 と同型 (独立 critic のフルページ照合が翻訳層を超え上流欠陥を捕捉=網兜の再実証)。
- **D-小6 効果実測**: `2017h29a-q001` (源 Z行「6,7,8」→図 page-02「8,7,8」、stem_jp_clean が是正、最大22=ウ 整合、未修正なら21=イ で正解崩壊)・`2017h29a-q020` (利益行をフルページ復元)。
- low14 = 本土 zh 自然さ・説明的グロス (q049 `経営者`→`管理层` 補足等)・glossary 冗長エントリ・指標名補足注記。正誤・脱落・捏造ゼロ。
- 詳細: `evidence/phase5/stage_06_quiz_phase1/rule_a_audit_S91.json` + README S91 節。

### 検証 (全 GREEN・回帰なし)

- tsc 0 / eslint 0err (既存 warning 1) / **vitest 455 passed** (+2 skipped) / build exit 0。
- **nft IPA leak = 0** (`.next` 全 .nft.json で `data/ip/{exams,sources,syllabus}` 0 hits)。quiz route trace = quiz_index + questions + **translations/13 回.json** (新 3 sidecar が glob resolve 確認)。

## 4. 学び (fix-checklist 追記)

- **triage は repair 後の実訳文を読む**: 翻訳 WF の verdict ラベル (CONCERNS) は「最終 reviewer の所見」であり、repair が既に core を是正していても low 残で CONCERNS になりうる。verdict だけ見て修復要否を決めず、必ず落盘訳文を突合する (q090/q019 とも core は repair 済だった)。
- **独立 critic のフルページ照合は翻訳検証を超えて上流 OCR 欠陥を捕捉する** (q069 choices_jp、S89/S90 に続き 3 回連続)。翻訳 backfill の Rule A が事実上 Stage 2 データ品質の追加監査として機能している = 写審分離+独立抽検の複利効果。
- **統合バッチ構築のインライン化は誤りの温床**: combiner をスクリプト化 (D-小8) し assert を入れた。残バッチで踏襲。

## What's Next

- **バッチ S91 完了**: 300/300 三語、Rule A **37/37 accurate** (medium1=上流申し送り・翻訳は PASS)、Rule D FAIL 0、全検証 GREEN、nft IPA 0。Phase 1 翻訳済 **13/29 回**、残 16 回。
- 次バッチ = ユーザー「Quiz Phase 1 续批」で起動。次候補 = `2016h28a` / `2016h28h` / `2015h27a` (最新優先)。D-小6/7/8 とも scripts 組込済=追加作業不要。
- **要ユーザー判断 backlog (上流データ品質、翻訳成果物に影響なし)**:
  - `2017h29h-q069` の choices_jp イ/ウ を権威フルページ基準で再OCR・是正 (Stage 2 scope)。
  - `2019h31h-q061` figure crop 再裁剪 (S90 carryover、アプリ上誤図表示)。
  - q069 の id採番 (q069) vs 印刷問番号 (問59) の対応確認 (上流メタデータ)。

## Pre-close self-check (D-027 第5条)

- **D 新規**: なし (D-136 枠内)。小決策 D-小8 (combiner スクリプト化) は本ログのみ。
- **OQ**: 新規の正式 OQ なし。OQ-01/02 carryover。q069 choices_jp / q061 crop / q069 番号は backlog 項として記録 (OQ 化はユーザー判断後、S90 q061 と同方針)。
- **STATE sync**: 最后更新 / 当前阶段 / Stage 6 行 / 次セッション (13/29) を更新。锁定决策 136 のまま。
- **Rule A**: N=37 独立 critic (accurate 37/37)。証拠 `rule_a_audit_S91.json` + README S91 節。
- **Rule B**: 本バッチ FAIL attempt なし。q019 は CONCERNS 内 low の 1 フィールド修正 (修正前値 `企业所得税、居民税及营业税` を本ログ+README に記録)。アーカイブ対象なし、既存 failures/ 不変。
- **Rule D**: writer(general-purpose) ≠ in-pipeline reviewer(code-reviewer) ≠ Rule A auditor(critic)。q019 修復は主 context (writer 役) ≠ 独立 Rule A critic (検証役)。
- **invariants**: raw bank / questions.json 不変。translations 新規 3 sidecar。コード = `scripts/quiz-phase1-batch.mjs` 新規のみ (app 非依存)。
- **コミット**: 確立済 cadence で commit。push はしない (要明示承認)。
