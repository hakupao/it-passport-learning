# Session 89 — Quiz Phase 1 (翻訳 backfill) スケール バッチ S89 (3 回)

> 日付: 2026-06-10 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 Phase 1 スケール
> 起動: ユーザー路由「Quiz Phase 1 续批」(回数未指定)。前提: S88 でバッチ S88 完了・commit 済 (`b09b477`)、翻訳済 4/29 回。

## 0. 起点

- STATE + session-88 を読了。回数未指定 → 路由②「最新優先・既訳 (2025r07/2026r08/2024r06/2023r05) 除外で 3〜5 回」→ **最新 3 回 = `2022r04` / `2021r03` / `2020r02o`** を選定 (S88 と同じ 3 回 cadence 維持)。

## 1. 実行設計

S88 の統合 1 ワークフロー方式 (D-小5) を踏襲。加えて S88 fix-checklist の高優先項を実装:

**小決策 (session log のみ)**:
- **D-小6 (figure 問フルページ併読)**: S88 唯一の high 欠陥 (`2026r08-q072` 表列脱落) の根因 = translator が裁剪図のみ参照 (端クロップで列欠落)。本バッチから ① `quiz-phase1-prep.mjs` が raw bank `source.page_image` から figure 問に `figure_page_png` (原典フルページ ABS パス) を注入 (存在 fail-fast) ② translate workflow の translator/reviewer prompt + ruleA workflow の critic prompt を「crop+フルページ両方 Read、**フルページが権威**、列脱落・列順改変禁止」に増強。コード変更は scripts 3 ファイルのみ (reader/UI/next.config 不変)。

## 2. 実施

| Step | 内容 | 結果 |
|------|------|------|
| prep ×3 | figure crop+page 存在 fail-fast + glossary 束縛 | 全存在 (figure 16/8/13)、統合 input 300 問・id 全一意・整合 0 bad |
| translate (統合) | `wf_f492c4f7-038`、300 問、writer(general-purpose)→reviewer(code-reviewer)2R | **300/300、raw PASS 298 / CONCERNS 1 / FAIL 1**。658 agent / 18.9M tok |
| ├ 中断 | **ユーザー事前指示「総量の半分で一時停止」**→ 153 done で TaskStop | 153 tr 全 well-formed (破損 0、2022r04=100/2021r03=53)、merge 未実行で git clean 維持 |
| └ 再開 | ユーザー「継続」→ `resumeFromRunId` でキャッシュ 153 再利用 + 残 147 完走 | resume 2 回目の実証、300 完走 |
| q067 修復 | in-pipeline FAIL → Rule B archive → writer 是正 → 独立 re-review | PASS 6/6 check・回帰 0 (後述) |
| merge ×3 | per-exam committed sidecar 組立 | 各 100/100、missing 0。clean stem 58/62/62 |
| ruleA ×3prep+1wf | N=12×3 層化独立 critic (`wf_2d362fb8-b85`) | **36/36 accurate** (後述) |

### Rule D in-pipeline (writer ≠ reviewer): 実効 299 PASS / 1 CONCERNS

- **FAIL 1 = `2021r03-q067`** (figure 問、ISMS セキュリティ特性): **repair 誘発回帰**。R1 reviewer が distractor「保全性」の訳 (zh 保全性/en Preservability) を low 指摘し「保护性」を誤示唆 → repair が機械採用 → R2 reviewer (別インスタンス) が「保护性/Protectiveness は捏造語 (保全≠保護)、姉妹問 2020r02o-q087 は漢字保持で内部不整合」と正しく FAIL (MAX_ROUNDS 到達)。
  - **修復**: Rule B archive (`failures/quiz_phase1_S89_2021r03-q067_attempt_1{.md,_defective.json}`) → writer(general-purpose) ピンポイント是正 zh「保全性」/ en「Preservation」(同問 b 項 保守性=Maintainability との衝突回避で Maintainability 禁止) → 独立 code-reviewer 再 review **PASS 6/6・回帰 0** → Rule A critic 抽検 accurate。
  - **教訓 (fix-checklist へ)**: reviewer の low 任意示唆 (改善案) を repair で機械採用すると distractor 用語の語義区別を壊しうる。**repair は指摘欠陥の是正に限定**、low 示唆の採用は語義検証が前提。
- **CONCERNS 1 = `2020r02o-q011`** (DFD figure 問): 2 reviewer 一致で翻訳自体は忠実。実体は上流 `choices_jp` の図→テキスト転写が図の矢印方向と不一致 (source 側品質、S87 q039 型 known gap)。正解エの訳は図 (権威) と整合し正誤判定保持 → 受容 + backlog 記録。

### Rule A (独立 critic, N=36 [各回 12、層化 figure23])

- **accurate 36/36 (100%)、severity none25 / low11 / medium0 / high0** — スケール開始以来初の「修復不要」バッチ (S87 pilot は 12/12 だが N 小、S88 は high1)。
- **D-小6 の効果が実測で確認**: 生 stem の OCR 誤数値をフルページ併読で正しく clean 化した事例 3 件 — `2021r03-q050` (移動50秒/C18秒→図の60秒/10秒、critic 検算で正解ア=223秒成立、誤値だと無解)、`2021r03-q074` (「5を格納」→「1を格納」)、`2020r02o-q071` (D3=8,000→5,000)。**critic もフルページで検算して忠実確認** (D-小6 は監査側にも効く)。
- low11 = 本土 zh 自然さ微調・全角コロン表記・解釈的グロス (independently/CountIf)・glossary スタレ残骸 (q070「表」、q043 非関連項目=prep 関連度マッチの粗さ、無害)・上流データ品質メモ。正誤・脱落・捏造ゼロ。
- **scope 外メモ (backlog)**: `2021r03-q095` で critic が公式 correct_answer=ウ(B,C) と図からの再計算 (エ=C のみ、A=18,000<20,000 で除外に見える) の食い違いを記録。翻訳は全選択肢忠実 (訳ミスでの正誤崩れではない)。answer_keys は IPA 公式キー (Stage 2 で 100% 検証済) であり**改変しない**。Phase 2 (解析預生成) が全問解説を再計算する際に自然に再検証される → 記録のみ。

### 検証 (全 GREEN・回帰なし)

- tsc `--noEmit` 0 / eslint 0err (既存 warning 1=quiz 無関係) / **vitest 455 passed** (S88 維持) / build exit 0。
- **nft IPA leak = 0**: 全 .next trace で `data/ip/{exams,sources,syllabus}`・`question_bank` 0 hits (粗パターン 4 hits は Next.js 内部 `pages/` と自作 textbook SVG の誤検知と個別確認)。quiz trace = quiz_index + questions + translations/{7 回}.json。新 3 sidecar QUIZ_TRACE 済。

## 3. 学び (fix チェックリストへ)

- **repair は指摘欠陥の是正に限定** (q067 教訓): reviewer の low 任意示唆を repair プロンプトに含めて機械採用させると、欠陥でなかった箇所が改悪されうる。今後 repair feedback には「low 示唆は語義検証なしに採用しない」を明示するか、CONCERNS (low のみ) は repair 不要とする検討 (D-小7 候補、backlog)。
- **D-小6 (フルページ併読) は定着**: 翻訳側 3 件の OCR 誤数値是正 + 監査側の検算精度向上を実測。以後の全バッチで標準。
- glossary 関連度マッチ (prep) に非関連項目が混入しうる (q043 シミュレーション) — 無害 (low) だが prep の関連度ロジック微調整は backlog。

## What's Next

- **バッチ S89 完了**: 300/300 三語、Rule A 36/36 (修復不要)、Rule D 実効 299/1 CONCERNS (q067 修復済)、全検証 GREEN、nft IPA 0。Phase 1 翻訳済 **7/29 回**、残 22 回。
- 次バッチ = ユーザー「Quiz Phase 1 续批」(任意で回数/回指定) で起動。手順は STATE 路由のまま (D-小6 は scripts に組込済、追加作業不要)。
- backlog: repair の low 示唆採用ガード (D-小7 候補) / q095 answer key 食い違い (Phase 2 で再検証) / q011 型 choices_jp 上流転写品質 / prep glossary 関連度微調整 / glossary/tutor/chat 死語料移行 / Phase 2 (解析預生成) / Phase 3 (教科書 unit 埋込)。

## Pre-close self-check (D-027 第5条)

- **D 新規**: なし (D-136 の枠内スケール実行)。小決策 D-小6 (figure フルページ併読、scripts 実装済) は本ログのみ。
- **OQ**: 新規なし。OQ-01/02 carryover。
- **STATE sync**: 最后更新 / 次セッション (Phase 1 進捗 7/29) / Stage 6 行 を更新。锁定决策は 136 のまま (新 D なし)。
- **Rule A**: バッチ翻訳 (>50% 改変) → N=36 独立 critic (accurate 36/36)。証拠 `evidence/phase5/stage_06_quiz_phase1/rule_a_audit_S89.json` + README S89 節。
- **Rule B**: q067 欠陥 attempt を `failures/quiz_phase1_S89_2021r03-q067_attempt_1{.md,_defective.json}` に archive、削除せず。
- **Rule D**: writer(general-purpose) ≠ in-pipeline reviewer(code-reviewer) ≠ Rule A auditor(critic) ≠ q067 修復 writer(general-purpose 別インスタンス) ≠ 修復 re-reviewer(code-reviewer 別インスタンス)、全別 subagent_type・別 context。
- **invariants**: raw bank / questions.json (JP projection) 不変、build-quiz-corpus 無改修。translations は新規 3 sidecar。コード変更は scripts 3 ファイル (D-小6) のみ。
- **コミット**: 路由④に従い本バッチを commit (公開 repo へ IPA 派生翻訳、確立済 cadence)。push はしない (要明示承認)。
