# Session 12 — 2026-05-11

> Phase 1 Step 6.10 — Stage 7 export design + scaffold.
> Continues from Session 11 close at HEAD `c806479` (Step 6.9 Stage 6 ✅ closed,
> 324/324 unit tests pass, 77 D locked, 3 OQ open).
>
> Tier 3 evidence discipline: lock D-078 ADR + writeback session log + sync
> STATE.md same turn (per D-019 §3 + D-027 §1/§3).

---

## 0. Session Goal

Step 6.10 = design + scaffold Stage 7 export module. Stage 7's job: convert
`data/.../translated/page_NNN.json` × 40 + `glossary.json` + `audit/stage6_review.json`
into releasable artifacts under `data/.../output/`. Eventually published as
GitHub Release tag asset per D-046 (Step 6.11 territory; Step 6.10 produces and
validates locally only).

Per D-019 slow-pace + user instruction "进 Session 12 开 Stage 7":

1. Pose 2-4 open design questions (done; 4 questions at Session 12 open)
2. User answers (Q1=JSON+MD / Q2=per-page / Q3=sidecar polish_items / Q4=dual-gate)
3. Claude proposes design (done)
4. User refines via 2 specific overrides (`[JP]/[ZH]/[EN]` not emoji; Gate A full D1-D13)
5. Lock D-078 + writeback (this turn)

---

## 1. Inherited State

- HEAD: `c806479` (Session 11 close: Step 6.9 Stage 6 CLOSED at 0-FAIL clean baseline)
- Branch: `main`, working tree clean entering session
- 324/324 unit tests pass
- Step 0~5 ✅; Step 6.0~6.9 ✅
- Locked decisions: 77 (D-001 ~ D-077)
- Cumulative dry-run cost: $85.11 shadow / $0.05 Mistral + $0 Anthropic (max-plan OAuth)
- 3 OQ open (all Phase 1 out-of-scope)
- 18 WARN + 14 INFO + 2 run-level INFO polish items carried forward from Stage 6 closure
  (per `evidence/.../step_06_audit.md` § "Known polish items carried forward")

---

## 2. Decisions Locked

### D-078 — Stage 7 export v1 design

> Locked Session 12, 2026-05-11. ADR: `docs/decisions/D-078-stage7-export-v1.md`.

**One-line spec**:

Per-page JSON + Markdown export with sidecar `polish_items.json`, ASCII
`[JP]/[ZH]/[EN]` language markers, dual release gate (full D1-D13 Phase-1
re-run + Stage 7-specific contract self-check). v1 scope = JSON + Markdown
only (JSONL + SQLite deferred to v2). D-008 amended.

**8 key design choices**:

1. v1 formats: **JSON + Markdown** (Q1 user answer; defer JSONL/SQLite)
2. File granularity: **per-page** (`pages/page_NNN.json` + `pages/page_NNN.md`) plus `index.json` + `glossary.json` (copy) + `polish_items.json` + `README.md` (Q2 user answer)
3. Per-page JSON envelope: `schema_version` + `cert_id` + `run_id` + `stage` + `page` + `exported_at` + `stage6_verdict` + `leaf_count` + `entities` + optional `polish_items_ref`
4. polish_items.json: **sidecar** with WARN + INFO only (FAIL impossible post-Gate-A); Stage6Issue subset (drops verbose evidence blob) (Q3 user answer)
5. Markdown layout: **language-stacked sections** with `[JP]/[ZH]/[EN]` ASCII markers (user 2nd-round override; emoji flags rejected)
6. Gate A: **full D1-D13** Phase-1 deterministic re-run (user 2nd-round override; "only safety subset" rejected)
7. Gate B: Stage 7-specific contract self-check (5 invariants per ADR §2.6)
8. CLI: `export-trilingual --formats json,md --confirm` mirrors `audit-trilingual` shape

**Rejected alternatives** (full list in ADR §3):

- 4-format v1 — deferred non-JSON/MD to v2 (Q1)
- Single mega-bundle JSON — Q2 per-page
- Bare `list[entity]` no envelope — release artifact must self-describe
- Embedded `quality_flags` per entity — Q3 sidecar
- 3-column parallel Markdown — narrow screen overflow risk
- Emoji flag markers — user 2nd-round override
- Skip Gate A — Q4 dual-gate
- Gate A "only safety subset" — user 2nd-round override (full D1-D13)
- Trust Stage 6 closure without re-check — same reason
- Commit output to git — D-046/D-050 already lock to Release tag

### Minor decisions (in-session, not D-numbered)

- Stage 7 module path: `pipeline/stage7_export/` mirroring Stage 6 layout.
- Test target ≥ 40 new tests; suite-wide ≥ 364 pass.
- Stage 7 evidence file: `evidence/.../step_07_export.md` (scaffold pre-run).
- Step 6.10 closure criteria explicit in ADR §5.

---

## 3. Open Questions

继承 OQ-01/02/05（全 Phase 1 范围外，无新增）。

---

## 4. Implementation Plan (next user-gated)

| Step | Action | Gate | LLM ($)? |
|---|---|---|---|
| 6.10.1 | `schema.py` (Pydantic envelopes + IndexEntry + PolishItem + ReleaseGateResult) + tests | TDD | no |
| 6.10.2 | `normalizers.py` (choice_marker_normalize + untranslated_scan) + tests | TDD | no |
| 6.10.3 | `gates.py` (Gate A wrapper + Gate B contract checks) + tests | TDD | no |
| 6.10.4 | `emitters.py` (emit_page_json + emit_page_md + emit_index + emit_polish + emit_readme) + tests | TDD | no |
| 6.10.5 | `runner.py` (Stage7Export orchestrator) + CLI subcommand `export-trilingual` + tests | TDD | no |
| 6.10.6 | Scaffold `evidence/.../step_07_export.md` with pre-run input snapshot | no LLM | no |
| **GATE** | **User authorizes dry-run on existing 40-page output** | user OK | no |
| 6.10.7 | Run `export-trilingual --confirm` on `dry_run_2026-05-06T16-58-10`; verify Gate A + Gate B both PASS; write 86+ files under `data/.../output/` | dry-run | no |
| 6.10.8 | User sample-reviews `pages/page_006.json` + `pages/page_006.md` + `polish_items.json` | user | no |
| 6.10.9 | Step 6.10 closure — `step_07_export.md` write-up + STATE.md sync | — | no |

After Step 6.10.9 ✅ → Step 6.11 (全本 579-page Plan-C + GitHub Release per D-046).

Stage 7 is **all deterministic** — no LLM dispatch needed for Step 6.10. Cost = $0
regardless of how many iterations. This is intentional per D-008 + D-078: export
is a data-shape transformation + invariant check; semantic content was finalized
by Stage 6 closure.

---

## 5. Memory updates (this session)

No new memory files. Existing memories sufficient:

- `feedback_quality_over_cost.md` — Q4 dual-gate full D1-D13 reflects this (no shortcut)
- `feedback_long_context_batch_size.md` — not applicable (no LLM in Stage 7)

---

## 6. What's Next

Per ADR §5 acceptance criteria, next work units (all deterministic, no LLM):

1. TDD scaffolding 6.10.1 → 6.10.5
2. Pre-run input snapshot scaffold (6.10.6)
3. User authorizes dry-run (6.10.7 gate)
4. Dry-run + user sample review (6.10.7 + 6.10.8)
5. Step 6.10 closure (6.10.9)

Awaiting user instruction to proceed with TDD implementation.

---

## 7. Implementation log (Step 6.10.1 → 6.10.7)

After user instructed "C 结束后你继续做 A" — TDD scaffolding executed end-to-end:

| Sub-step | Output | Tests added | Status |
|---|---|---|---|
| 6.10.1 schema | `pipeline/stage7_export/schema.py` (6 Pydantic models) | 25 | ✅ |
| 6.10.2 normalizers | `pipeline/stage7_export/normalizers.py` (choice_marker_normalize + scan_untranslated + iter_trilingual_dicts) | 37 | ✅ |
| 6.10.3 gates | `pipeline/stage7_export/gates.py` (run_gate_a + run_gate_b + run_both_gates) | 14 | ✅ |
| 6.10.4 emitters | `pipeline/stage7_export/emitters.py` (emit_page_json + emit_page_md + emit_index_json + emit_polish_items + emit_readme_md) | 18 | ✅ |
| 6.10.5 runner + CLI | `pipeline/stage7_export/runner.py` (Stage7Export + Stage7Result) + `cli.py:export-trilingual` | 9 | ✅ |
| 6.10.6 evidence | `evidence/.../step_07_export.md` | — | ✅ |
| 6.10.7 real-data dry-run | 40-page output bundle at `data/.../output/` | — | ✅ (gate_a + gate_b both PASS; 84 files) |

**Total Stage 7 tests: 103. Full suite: 427 / 427 pass (324 base + 103 Stage 7). 0.39 s runtime.**

### Bug surfaced + fixed during real-data dry-run

Stage 7 runner originally ordered: `Load → Normalize → Gate A → Gate B → Emit`. The real 40-page dispatch caught **D1 jp_mutation FAIL on pages 042 + 044** because `normalize_question_choices` rewrote jp markers from half-width `ア.` to full-width `ア．`, diverging from `structured.jp` — Gate A then flagged the divergence.

**Fix**: re-ordered to `Load → Gate A (pre-normalize) → Normalize → Gate B (post-normalize) → Emit`. Gate A now sees the same jp Stage 6 closure validated; Gate B sees canonical jp markers. Runner module + class docstrings updated; `run_both_gates` no longer used by the runner (kept for direct caller convenience). Regression test `test_half_width_jp_marker_does_not_trigger_d1_jp_mutation` added to runner test module.

This is exactly the integration-shape miss that real-data dry-runs are supposed to surface; unit tests alone (synthetic minimal fixtures) had clean jp markers and didn't trip the issue.

### Real-data dry-run outcome (Step 6.10.7)

```
[export-trilingual] DONE  gate_a_passed      = True
[export-trilingual]       gate_b_passed      = True
[export-trilingual]       passed             = True
[export-trilingual]       pages_written      = 40
[export-trilingual]       files_written      = 84
[export-trilingual]       choices_normalized = 10
[export-trilingual]       output_dir         = data/.../output
```

- 40 pages × {JSON, Markdown} = 80 page files
- 4 top-level files: index.json (8.5 KB) + glossary.json (18.7 KB; verbatim copy of Stage 4.5) + polish_items.json (44.6 KB; 102 carry-forward items) + README.md
- `index.json.totals` = 40 pages / 161 entities / 382 trilingual leaves
- `index.json.stage6_summary` = WARN / PASS=22 / WARN=18 / FAIL=0 / polish_items=102
- Cost: $0 shadow / $0 billed (deterministic; no LLM)
- Wall-clock: < 1 s

## 8. What's Next — user sample-review (Step 6.10.8)

Per D-078 §5.3 + step_07_export.md § "Sample IDs for user review", suggested sample subset (5 pages, ~13% coverage):

1. **page_006** — clean PASS control (empty entity page; lightweight envelope sanity check)
2. **page_014** — clean WARN with table + figure entities (rich content; D-077 §F-CHOICE-MARKER closed; D7 numeric WARN)
3. **page_022** — WARN with `choice_marker normalize` applied + post-hand-edit "Activities" removal (Plan-B + closure correction in one page)
4. **page_038** — WARN with LLM L3 unfaithful (circular EN definition; polish item carried forward to Stage 5 v2)
5. **page_043** — WARN with multi-question D6 choice-marker normalization applied (5 questions; verify each choice has canonical A/B/C/D + ア/イ/ウ/エ)

Sample paths:

- `data/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/output/pages/page_NNN.json`
- `data/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/output/pages/page_NNN.md`
- `data/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/output/polish_items.json` (sidecar with 102 items)
- `data/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/output/index.json` (top-level index)

User retro checklist (matches step_07_export.md "Pre-sample audit checklist"):

| # | Check | Where to look |
|---|---|---|
| 1 | Envelope `schema_version="v1"`, cert_id, run_id, exported_at, stage6_verdict | any `page_NNN.json` top |
| 2 | `polish_items_ref` set for pages with issues, null otherwise | page_006 (null) vs page_014 (set) |
| 3 | Markdown uses `[JP]/[ZH]/[EN]` ASCII (not emoji) | any `page_NNN.md` |
| 4 | Normalized choice markers on pages 042/043/044 | jp = `ア．`, zh+en = `A. ` |
| 5 | polish_items.json excludes FAIL (only WARN+INFO) | `polish_items.json` top |
| 6 | glossary.json byte-identical to Stage 4.5 source | `diff` check |
| 7 | index.json totals match Stage 6 closure | `index.json.stage6_summary` |

User can sample any subset; the 5 suggested cover all corner cases.

## 9. User sample-review PASS — Step 6.10 ✅ CLOSED (2026-05-11)

User signed off the sample review with **"ok通过"** on the suggested 5-page subset (page_006 envelope sanity + page_014 rich content + page_022 post-normalize + post-hand-edit + page_038 polish carry + page_043 multi-question normalize). Step 6.10 is now closed.

`step_07_export.md` Decision section + Sign-off table updated with the closure timestamp. STATE.md top table + §5 sub-steps marked Step 6.10 ✅ done and 6.11 ⏸ as the next entry point.

## 10. Pre-close self-check (D-027 §5) — Session 12

| Check | Status |
|---|---|
| D-NNN: any new? | ✅ D-078 locked (Stage 7 export v1) |
| OQ: any new? | None |
| STATE.md sync | ✅ this turn (top table + §5 sub-steps + 下一会话 → Session 13) |
| Session log on disk | ✅ this file |
| Evidence: step_07_export.md | ✅ Decision + sign-off populated through user closure row |
| Code: Stage 7 module | ✅ 5 files + CLI subcommand |
| Tests | ✅ 103 new (suite 427/427 pass; 0.39 s) |
| Real-data dry-run | ✅ 84 files emitted; both gates PASS |
| User sample-review | ✅ PASS ("ok通过") |
| Step 6.10 closure | ✅ done — Step 6.11 unblocked |
| Memory | no changes |
| Commits | ⏸ user gate — 2 atomic commits proposed for closure |

## 11. What's Next (Session 13 entry point)

**Step 6.11** — re-run Stages 1-7 on full 579-page ITパスポート book + GitHub Release tag per D-046.

Major sub-steps (will be designed in Session 13):

1. Stage 1 — Mistral OCR all 579 pages (~$0.58 real billed; D-005)
2. Stage 2 — Page classify (Claude Sonnet)
3. Stage 3 — Hard-page re-OCR (Claude Vision, conditional)
4. Stage 4 — Structure extraction
5. Stage 4.5 — Glossary extraction
6. Stage 5 — Trilingual translation
7. Stage 6 — Audit (deterministic + LLM)
8. Stage 7 — Export
9. GitHub Release tag — output bundle as assets per D-046

Estimated dry-run shadow: ~$200-300 (extrapolated from Plan-C 40-page = $85.11 shadow × ~7-8x; max-plan OAuth keeps real billed near $0 Anthropic / ~$0.58 Mistral).

Resume entry-point for Session 13:

1. Read `docs/STATE.md` (top table + §5 sub-steps row 6.11).
2. Read `docs/decisions/D-073-phase1-launch-strategy.md` (Stage C launch contract).
3. Read `docs/decisions/D-046-output-release-strategy.md` (or session log entries — GitHub Release tag protocol).
4. Pose 2-4 open Q at Session 13 open per D-019 slow-pace.
