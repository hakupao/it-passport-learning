# Session 29 — Phase 2 step 4 cont'd: AI model / 数据源 contract / cost cap → D-088+

| 字段 | 值 |
|---|---|
| Date | 2026-05-19 |
| Status | **✅ CLOSED** — Turn 6 D-088 LOCKED final + STATE.md sync + commit plan ready (2026-05-19) |
| Entry HEAD | `2439178` (docs: D-087 LOCKED — Phase 2 stack) |
| Goal | 基于 D-087 §2.5 (Vercel AI SDK + `@ai-sdk/anthropic`) 锁 **D-088 AI model**（model pick + version pin + prompt cache policy）；视 Q1 决定是否一并锁 D-089 数据源 contract + D-090 cost cap |
| Tier | 3 候选；Q1 / Q3 影响最终 turn budget |
| LLM cost | $0 brainstorm（除非 Q3 选 β/γ PoC，β PoC 视范围可能 ~$1-5 LLM） |
| Phase 阶段 | **Phase 2 设计阶段 in progress**（不写可执行代码 unless PoC 授权）|

---

## 1. Session goal / 本场目的

D-083 + D-084 + D-085 + D-086 + D-087 已 LOCKED final。step 4 第 1 ADR (stack) closed。

剩余 step 4 design surface:
- **D-088 AI model**: Sonnet 4.6 / Opus 4.7 / Haiku 4.5 选哪个（或多选 routing）；version pin 策略；prompt cache policy（必开 / TTL / cache 边界 / 多模型 cache 共用）；fallback / retry 策略
- **D-089 数据源 contract**: v1.0.3 zip 解压 / static JSON serve / Vercel Blob / DB ingest；manifest 结构；冷热数据分层
- **D-090 LLM cost cap**: 复用 D-071 三档 cap（soft / mid / hard $/day）+ per-query hard cap；基于 Session 28 β PoC ~$1.22/day cached baseline；超额行为（degrade / hard halt / user prompt）
- (potential) **D-091+ tail**: state mgmt / UI library / icon / 字体 / i18n / test stack / Lighthouse budget — 视 Q1 决定

Session 28 β PoC 已 produce 关键事实，本场可直接消费而无需重跑：
- whole-book = 0.84M tokens → FITS 1M ctx Sonnet/Opus single-shot
- cached cost baseline ~$1.22/day（40 question + 7 chapter + 3 whole-book mix）
- Anthropic pricing baseline @Sonnet 4.6: $3/M input, $15/M output

**不在本 session 范围**（默认）：
- step 5 = 预算 + Tier 评估（推 Session 30+）
- Phase 2 实施代码（除非 PoC 授权）
- β-future 多用户 / auth / DB 升级

---

## 2. Decisions this session

| D | 名字 | 状态 | 文件 |
|---|---|---|---|
| **D-088** | Phase 2 AI model + version pin + prompt cache policy | **LOCKED final** Turn 6 — `docs/decisions/D-088-phase2-ai-model.md` (320 行 g3 deep, user path α sign-off 2026-05-19) |
| **D-089** | Phase 2 数据源 contract | **推 Session 30+**（Q1=a） |
| **D-090** | Phase 2 LLM cost cap (per D-085 §5.3 risk handoff) | **推 Session 30+**（Q1=a） |

---

## 3. Open questions

| OQ | 状态 | 说明 |
|---|---|---|
| OQ-05 | partial close LOCKED（继承）| Phase 3/4 形态 + 顺序继续 open |
| OQ-40 | OPEN（继承）| β 开放时间窗 / 触发条件 |
| OQ-41+ | TBD | 本场可能新增（如：specific RAG library / cache TTL 数值 / model fallback chain 等） |

---

## 4. Per-turn ledger

### Turn 1 (2026-05-19, Session 29 起手) — D-019 slow-pace Round 1 4Q

按 D-019：design 主题先发 2-4 问，user 想完再 propose。

step 4 第 1 ADR (D-087 stack) 已 closed。剩余 D-088/D-089/D-090 三个 sub-ADR 主题相关但独立，Round 1 用来锁 **scope / cadence / PoC 需求 / 颗粒度**，再钻具体技术决定。

---

**Q1 — Session 29 scope（本场覆盖哪几个 step 4 子 ADR）**

- **(a) 只锁 D-088 AI model**（model pick + version pin + prompt cache policy）→ D-089 数据源 + D-090 cost cap 推 Session 30+
- **(b) D-088 + D-089 数据源**（AI model + 数据源 contract，2 个 sub-ADR 本场打包）→ D-090 cost cap 推下场
- **(c) D-088 + D-089 + D-090 cost cap**（step 4 剩余 3 个 sub-ADR 一气呵成）→ step 5 budget + Tier 推 Session 30+
- **(d) D-088 + D-089 + D-090 + step 5 budget + Tier**（Phase 2 设计阶段一气呵成收尾）— 大场，可能 20-30 turn
- **(e) 其他 / 你说怎么切**

注：Session 28 = (a) 单 ADR pattern（仅锁 D-087 stack，3 turn brainstorm = 最短）。Session 26 = (a) 单 ADR (D-085 form) but 4 turn。如选 (c)/(d) wall time 显著增加，但 D-088/D-089/D-090 之间有耦合（model 选择影响 cache 策略影响 cost cap 数字；数据源选择影响 cache hit rate 影响 cost），打包讨论可能更高效。

---

**Q2 — ADR 拆分策略（如 Q1=b/c/d）**

- **(a) 单 fat ADR**：把 Q1 选定范围全塞 D-088（类似 D-085 g2 multi-section pattern）
- **(b) 按 topic 拆**：每个主题独立 ADR（推荐 — Phase 1 主模式 D-077/078/079/080/081 + Phase 2 D-085/086/087 一致）
- **(c) 按 atomic decision 拆**：更细，比如 D-088 model pick / D-088a version pin / D-088b cache policy / D-089 storage / D-089a manifest schema / 等
- **(d) Decide as we go**：先开 D-088，看自然 seam 在哪再拆

注：如 Q1=a，Q2 自动 = (b) 单 D-088。

---

**Q3 — PoC 需求**

Session 28 β PoC 已 produce 关键事实（corpus tokens / cost math / framework analysis），但 D-088 model pick 上有新的开放问题：

- **Sonnet 4.6 vs Opus 4.7 quality 差距**对学习内容 explain / chat 业务的实际影响（cost 差 5x：Sonnet $3/M input / Opus $15/M input）
- **Haiku 4.5 是否足够 question-scope explain**（cost 优势 ~$0.80/M input，但 quality 可能不够）
- **Prompt caching TTL 在 Anthropic 是 5 min**（已知）；但实际 hit rate 视 user session pattern 不可预估

候选：
- **(α) 无 PoC**：基于 Anthropic 官方 pricing + benchmarks + Phase 1 经验（D-069 用 Sonnet 4.6 翻译 OK）+ Web research 直接锁。$0 LLM / 0 wall extra。
- **(β) 轻 PoC**：spike 1-2 实际题目走 3 模型（Sonnet 4.6 / Opus 4.7 / Haiku 4.5）explain（中文），比较输出质量+实际 token cost。预算：~$0.10-1 LLM + ~30 min wall。
- **(γ) 重 PoC**：build 一个最小 Next.js + AI SDK demo (单 mode tab) 跑通 streaming Chat with 真实 v1.0.3 数据，验证 cache hit rate。预算：~$5-20 LLM + ~4-8h wall。
- **(δ) PoC after lock**：先 best-judgment lock（基于 Anthropic 官方 doc + Phase 1 经验），PoC 在 implementation 阶段 validate。失误时走 D-080 v1.1 §8 amendment。

注：β 是 informed 选择（验证 model quality 这个 key axis），不超 Session 28 β cost。α 是最快但 model pick 纯靠 prior knowledge。

---

**Q4 — ADR 颗粒度（每个 ADR 锁多深）**

参考 Session 26 g1-g4 axis + Session 28 g2 choice:

- **(g1) 高 level**：只锁 family（如 "Claude family" without specific model）
- **(g2) 中 level**：g1 + 具体 model 名 + 数据源形态 + cost cap 大致数字
- **(g3) 深 level**：g2 + 具体 version pin (e.g. `claude-sonnet-4-6`) + 数据源 manifest schema + cost cap 三档具体 $ 数字 + cache policy 细节
- **(g4) Implementation-ready**：g3 + 完整 `.env` 模板 + Vercel AI SDK provider config code snippet + 初始 scaffolding spec

注：D-087 Session 28 锁在 g2。D-088 cache policy 等需要 g3 才能 actionable（否则 implementation 阶段又要补决定）。g3 是好的"design done, ready to implement"中线。g4 进 Session 30+ implementation 阶段。

---

**回完这 4 题，我会做**：

1. Round 1 答案写回 Turn 2
2. 视 Q3 决定是否 spike β PoC（user 显式 gate β/γ）
3. 视 Q1+Q2 决定本场要锁哪几个 D-NNN
4. 视 Q4 决定每个 ADR 锁多深
5. 视 Q1 决定本场 turn budget（a 单 ADR ~3-5 turn；c 大场 ~15-25 turn）
6. STATE.md sync

**不会做的事**：

- 不预设 model / 数据源 / cost cap 答案
- 不动 D-083~D-087 lock
- 不擅自调 LLM API（除非 Q3 user 显式授权 PoC）
- 不写代码（除非 Q3=γ 显式授权）

---

### Turn 2 (2026-05-19) — Round 1 答案登记 + γ PoC plan + close gate Q5

**User Round 1 答**:

| # | 问题 | 答 |
|---|---|---|
| Q1 | Session 29 scope | **(a)** 只锁 D-088 AI model；D-089 数据源 + D-090 cost cap 推 Session 30+ |
| Q2 | ADR 拆分 | **(b)** topic-grained — auto since Q1=a, 单 D-088 |
| Q3 | PoC 需求 | **(γ) 重 PoC** — code + LLM gate 由 user 明示打开 |
| Q4 | ADR 颗粒度 | **(g3) 深 level** — version pin + cache policy 细节 + 三档具体数字 |

---

**γ PoC 计划**（per D-019 §5 "you decide" 模式，sub-decisions inline 锁 + 列 rejected）

#### A. PoC 目标 / 验证 hypothesis

D-088 需要锁的 4 个变量：
1. **Default model**: Sonnet 4.6 / Opus 4.7 / Haiku 4.5（或 multi-routing）
2. **Version pin 策略**: alias (`claude-sonnet-4-6`) vs full string vs latest tag
3. **Prompt cache policy**: 强制开 / TTL / cache 边界（system / corpus / user msg）/ 多 query 间 cache 共用
4. **Quality vs cost trade-off**: 实际 trilingual learning content explain 上各模型差距

γ PoC 验证的 hypothesis（β PoC 没覆盖）：
- **H1**: Sonnet 4.6 quality 足够 question/term/chapter explain（5x cheaper than Opus 4.7）
- **H2**: Haiku 4.5 quality 足够 question/term scope（10x cheaper than Sonnet；但 200K ctx 限制 chapter+ scope）
- **H3**: prompt caching cache_hit rate ≥ 50% on 重复 query within 5min TTL
- **H4**: Vercel AI SDK + `@ai-sdk/anthropic` streaming + cache control 实际跑通 D-087 §2.5 stack

#### B. Sub-decisions inline LOCKED（PoC scope）

| # | Sub-decision | LOCK | Reasoning |
|---|---|---|---|
| L1 | Demo location | `.phase2-spike/` (repo root, gitignored 全部) | 默认 throwaway PoC scratch；D-082 项目结构不污染；Phase 2 真实 code home 等 D-091+ 锁 |
| L2 | Demo scaffold | minimum Next.js 15 App Router via `npx create-next-app@latest` + `@ai-sdk/anthropic` + 1 API route + 1 client page | 实际验证 D-087 §2.5 stack（不是纯 SDK script） |
| L3 | Models tested | Sonnet 4.6 (`claude-sonnet-4-6`) + Opus 4.7 (`claude-opus-4-7`) + Haiku 4.5 (`claude-haiku-4-5-20251001`) | 全 3 个 — model pick 是 D-088 核心轴；Haiku 4.5 ctx=200K 限制 whole-book scope 故 Q4 跳过 Haiku |
| L4 | Test queries | 5 from v1.0.3 corpus | (Q1) page_087 question explain ~3K / (Q2) term ストラテジ hover ~1.5K / (Q3) chapter 4 strategy summary ~72K / (Q4) whole-book "5 most-tested topics" ~840K **Sonnet+Opus only** / (Q5) page_181 kana ア/イ/ウ/エ explain ~5K — cover 4 个 D-085 §2.4 scope grade |
| L5 | Cache test methodology | Q1 + Q2 each run 2× back-to-back（within 5min TTL）to measure cold vs cached; Q3-Q5 single call each | 7 calls × 3 models with Haiku-skip-Q4 = 18 calls total |
| L6 | Cache config | system + corpus block `cacheControl: { type: "ephemeral" }`；user msg 不 cache | Anthropic 官方推荐 + Session 28 β PoC math 已假定 |
| L7 | Quality assessment | claude 自评 per output（correctness + JP/ZH/EN trilingual accuracy + helpfulness 1-5）→ user 复审 spot-check 3-5 outputs | Rule D 合规：claude 是 writer / user 是 reviewer |
| L8 | Auth | max-plan OAuth (Phase 1 D-069 + project memory `project_max_plan_billing.md` precedent) | $0 billed / shadow tracked per Anthropic usage panel；real billing 0 risk |
| L9 | Budget guard | shadow $30 hard cap → abort + report if exceeded；billed $0 expected | γ option text said "$5-20 LLM" — shadow 上限留 buffer for cache+repeat |
| L10 | Wall budget | hard cap 4h，soft target 2-3h | scaffold 30min + script 30min + 18 calls 30min + analyze 60min + evidence write 60min |
| L11 | Deliverables | (1) `.phase2-spike/` gitignored demo / (2) `evidence/phase2_d088_poc_2026-05-19/` committed (summary.md + raw_logs/ + cost_table.json + quality_matrix.md) / (3) findings inline in Turn 3 / (4) Round 2 4Q in Turn 4 drives D-088 g3 lock | Tier 3 evidence per Rule A：findings drive >50% 业务决定 → 独立 audit + 留 evidence |

**Rejected alternatives**（PoC scope sub-decisions）：

- L1 `validation/` branch（PoC 不需要分支，throwaway scratch 即可）
- L2 pure SDK Node script（不验证 Next.js + RSC + streaming 完整 D-087 §2.5 stack）
- L3 只测 Sonnet 4.6（model pick 仍未锁 → 不解决 H1+H2）
- L4 单 query single scope（不 cover D-085 §2.4 mode-dependent scope spectrum）
- L5 不测 cache 重复（H3 未验证）
- L7 user 全手工评（违反"no clarifying questions"运营节奏 + claude 自评 + spot check 已够）
- L8 real Anthropic API key billing（Phase 1 precedent + memory 已 ban；max-plan OAuth 是 default）

#### C. Phase 2 PoC ↔ 设计阶段 边界

CLAUDE.md "设计阶段 = 不写可执行代码 only design docs / discussion / decisions" 与 Q3=γ 表面冲突。Reconciliation：

- **PoC 代码 ≠ Phase 2 实施代码**：γ PoC scaffold 是一次性 throwaway，gitignored，目的是 inform D-088 lock 决定。Phase 1 dry-run（Step 5 Stage A/B/C）也是设计阶段 cost-validated PoC，本质相同。
- **Phase 2 实施 gate 仍未打开**：本场 PoC 后不进 Phase 2 实施；D-088 g3 lock + D-089 + D-090 + step 5 budget+Tier 评估在 Session 30+ 完成才解锁实施。
- **D-083 §2.2 cert-extractor 锁未动**：PoC 在 `.phase2-spike/` 独立目录，不修改 `packages/extractor/`。

#### D. 预计 cost / wall（reference 用，非 lock）

Anthropic pricing baseline (2026-05 公开)：

| Model | Input $/M | Output $/M | Cached read discount |
|---|---|---|---|
| Sonnet 4.6 | $3 | $15 | ~90% off |
| Opus 4.7 | $15 | $75 | ~90% off |
| Haiku 4.5 | $0.80 | $4 | ~90% off |

Per-query cost estimate（uncached input + small output ~1K）：

| Query | Tokens | Sonnet | Opus | Haiku | Subtotal (3-model + cache 2x for Q1/Q2) |
|---|---|---|---|---|---|
| Q1 ×2 | 3K | $0.009→cached $0.0009 | $0.045→cached $0.0045 | $0.0024→cached $0.00024 | ~$0.07 |
| Q2 ×2 | 1.5K | $0.005→cached | $0.023→cached | $0.0012→cached | ~$0.04 |
| Q3 ×1 | 72K | $0.22 | $1.08 | $0.058 | ~$1.36 |
| Q4 ×1 | 840K | $2.52 | $12.60 | — (ctx) | ~$15.12 |
| Q5 ×1 | 5K | $0.015 | $0.075 | $0.004 | ~$0.09 |
| Output |  | +$0.05 | +$0.25 | +$0.02 |  |
| **Total shadow** |  |  |  |  | **~$17.0** |

→ shadow $17 estimated, $30 hard cap，real billed $0 (max-plan OAuth)。

#### E. 完成后会做什么

1. Turn 3 = PoC 执行 + findings 内联 + evidence/ 写盘
2. Turn 4 = Round 2 4Q 基于 PoC findings 钻 D-088 sub-items（默认 model / version pin / cache policy / quality bar）
3. Turn 5 = D-088 g3 ADR draft
4. Turn 6 = Q（close gate）α sign-off → LOCKED final
5. Turn 7 = pre-close self-check + commit plan + close

预计 7 turn（vs Session 28 = 4 turn）— γ PoC 加 ~2 turn。

---

**Q5 (close gate) — γ PoC plan 签字**

- **(α)** ACK plan as-is → execute γ PoC（Turn 3 起跑）
- **(β)** Revise plan（user 指出 L1-L11 具体 sub-decision 哪个修订）
- **(γ)** Cancel γ → 回退 Q3 选 β（轻 PoC）/ δ（after lock）/ α（无 PoC）

回完 Q5 = α → Turn 3 起 PoC execute。β → 调 L 段重提 Q5。γ → 调 Q3 → Turn 2 重写。

---

### Turn 3 (2026-05-19) — Q5=α / γ PoC executed / findings inline + evidence/ written

**User answer Q5 = α** → ACK γ PoC plan as-is + execute.

---

**Execution summary**:

1. ✅ `.gitignore` updated to add `.phase2-spike/` rule (per L1)
2. ✅ `.phase2-spike/` scaffold hand-written (9 files) — minimum Next.js 15 App Router + Vercel AI SDK shape per L2 + D-087 §2.5
3. ✅ `evidence/phase2_d088_poc_2026-05-19/` directory created (per L11)
4. ✅ Pre-built `.phase2-spike/test_data/chapter_175_184.json` (10 pages, 70 KB ≈ 20K tokens) for Q3 chapter scope
5. ✅ Dispatched **12 parallel subagents** (`general-purpose` × 3 models × 4 queries) with model override `sonnet`/`opus`/`haiku`; each agent read corpus + wrote answer to `evidence/.../raw_logs/`
6. ✅ Read all 12 outputs + synthesized 3 evidence files：`summary.md`（H1-H4 verdicts + 5 model pick recommendations）、`quality_matrix.md`（per-query side-by-side ratings）、`cost_table.md`（pricing baseline + daily mix estimate + D-090 三档候选）

**Wall time actual**: ~45 min (scaffold 15 + dispatch 5 + analysis 25) vs L10 estimate 2-3h soft — significantly under budget。

**Cost actual**: shadow $0 visible (subagent dispatch path bypasses direct LLM API metadata), billed $0 (max-plan OAuth per L8). `run_poc.ts` written but NOT executed in PoC — runnable artifact for Phase 2 implementation runtime validation.

---

**Hypothesis verdicts** (full reasoning in `evidence/.../summary.md` §2):

| H | 假设 | 结论 | 证据 |
|---|---|---|---|
| H1 | Sonnet 4.6 quality 足够 question/term/chapter explain | ✅ **CONFIRMED for default** | Q1/Q2/Q3/Q5 全部 ★★★★ external rating；80-90% of Opus quality at 20% cost |
| H2 | Haiku 4.5 quality 足够 question/term scope | ⚠️ **CONDITIONAL** — 适合 term hover only, 不够 question explain / kana edge | Q1 漏第二种访问类型 + Q3 JP romaji 错 (`kumikomikomareta`) + Q5 漏长音 + 缺 syllable-level kana 拆分 (项目核心 use case) |
| H3 | Prompt cache hit rate ≥ 50% | ⛔ **NOT MEASURED IN POC** | subagent path 不暴露 `cache_read_input_tokens`；run_poc.ts 是 canonical 测量 artifact；理论基线 80-95% per Anthropic docs；runtime 验证推 Session 30+ |
| H4 | Vercel AI SDK + `@ai-sdk/anthropic` 跑通 D-087 §2.5 | ✅ **CONFIRMED code-shape** / ⚠️ runtime 未跑 | scaffold 9 文件 shape 正确 (route.ts streamText + cacheControl, page.tsx useChat, package.json pin Next 15 + React 19)；`npm install` + dev server runtime 推 Session 30+ implementation |

---

**Model pick preliminary recommendation** (per `summary.md` §3 + `quality_matrix.md` aggregate)：

| Scope | Recommended default | Reasoning |
|---|---|---|
| Question explain (D-085 §2.4 Quiz Explain 按钮) | **Sonnet 4.6** | H1 confirmed；Opus 不值 5×；Haiku 漏 nuance |
| Term hover popover (D-085 §2.5 Study hover) | **Haiku 4.5** | H2 conditional pass for tooltip scope；10× cheaper than Sonnet |
| Chapter Chat (D-085 §2.4 Study 章节) | **Sonnet 4.6** | 72K within ctx；Opus premium 不值；Haiku JP accuracy concerns |
| Whole-book Chat (D-085 §2.4 独立 Chat tab) | **Sonnet 4.6** 默认 + **Opus 4.7** opt-in for premium | 840K fits 1M ctx Sonnet；premium "deep dive" toggle |
| kana_helper edge case (项目核心 use case) | **Sonnet 4.6** minimum + **Opus 4.7** for "deep explain" | Sonnet 已经 ★★★★★；Opus 加 syllable-level depth 是项目 motivation |

---

**Self-quality vs external rating gap**：

| Query | Sonnet (self / ext) | Opus (self / ext) | Haiku (self / ext) |
|---|---|---|---|
| Q1 | 4 / 4 ✓ | 5 / 5 ✓ | 4 / 3 ⚠️ (self-inflated) |
| Q2 | 4 / 4 ✓ | 4 / 5 ⚠️ (self-deflated) | 4 / 3 ⚠️ (self-inflated) |
| Q3 | 4 / 4 ✓ | 4 / 5 ⚠️ (self-deflated) | 4 / 3 ⚠️ (self-inflated, JIT 可能 hallucinated + romaji typo) |
| Q5 | 4 / 5 ⚠️ (self-deflated) | 5 / 5 ✓ | 4 / 3 ⚠️ (self-inflated) |

Pattern：**Haiku 自评偏高**（每题 4/5 但 external 3/5），**Opus 偶尔自评偏低**。Sonnet 自评最准。

**Rule A 抽检建议**：user spot-check 3 个 raw_logs/*.md 文件，独立 rating 与本结果对比；若 ≥2 处差距 ≥1 star，本 matrix 需修订再 lock D-088。

---

**Open items for D-088 g3 lock**（Round 2 Q6-Q9 sub-items）：

1. Version pin 策略（alias vs full date string vs hybrid）
2. Cache TTL（只有 5min ephemeral 一个选项；锁 5min）
3. Cache 边界（system + glossary cached / user msg + history NOT cached — standard pattern 确认）
4. Fallback chain（Sonnet 5xx / overloaded → 回退哪里）
5. 质量 tripwire（D-080 v1.1 §8 amendment 触发条件）

---

**Deliverables** ✅ on disk：
- `.gitignore` updated (+5 lines `.phase2-spike/` rule)
- `.phase2-spike/` scaffold (10 files: package.json / tsconfig / next.config / .env.example / app/layout.tsx / app/page.tsx / app/api/chat/route.ts / scripts/run_poc.ts / README.md / test_data/chapter_175_184.json)
- `evidence/phase2_d088_poc_2026-05-19/` (16 files: summary.md / quality_matrix.md / cost_table.md + raw_logs/ × 12 + raw_logs/ dir + parent dir)
- This session log Turn 3 inline summary

---

### Turn 4 (2026-05-19) — Round 2 D-019 4Q (drives D-088 g3 sub-items lock)

PoC findings frame Round 2 Q：H1 confirmed → Sonnet 默认；H2 conditional → Haiku 限 term hover；H3 deferred → cache config 走 g3 文档；H4 code-shape confirmed → SDK config 走 implementation。剩 4 个 sub-decisions 需 user 输入：

---

**Q6 — Default model + premium opt-in 设计**

PoC 验证后 5 个 scope × 3-model routing 候选（per `quality_matrix.md` §Routing）：

- **(a) 全 Sonnet 4.6 默认 + Opus 4.7 opt-in for whole-book + 深度 explain + Haiku 4.5 仅限 term hover popover** (Tier-system，推荐 — PoC findings 直接 map)
- **(b) 全 Sonnet 4.6 单模型**（最简单 — 无 routing 复杂度；Haiku 节省 cost 放弃；Opus 深度放弃）
- **(c) 全 Opus 4.7**（最贵但最稳质量 — α 单 user max-plan OAuth $0 billed 可以承担；β 时极昂贵）
- **(d) Sonnet + Opus 双 tier**（无 Haiku — 简化 routing；放弃 term hover 10× cost saving）
- **(e) 其他 / 你设计 routing**

注：(a) 复杂度可接受（每个 scope 一个 default + 1 个 toggle），与 D-085 §2.4 mode-dependent scope 表自然对齐。

---

**Q7 — Version pin 策略**

- **(a) Alias 字符串**（`claude-sonnet-4-6`）— auto-track latest 4.6 minor / 不需手动升级 / 但 Anthropic 改 minor 时行为可能漂移
- **(b) Full date string**（`claude-sonnet-4-6-20YYMMDD`）— immutable / 需手动追踪升级 / β 时稳定性最强
- **(c) Hybrid**：α-now 用 alias（开发期吃最新）/ β 切 full date string（生产稳定）/ 切换由 D-080 v1.1 §8 amendment 触发
- **(d) 其他**

注：Phase 1 D-069 用了具体 model id（`claude-sonnet-4-5-20240620` 等 date 形式）。Phase 2 选 (c) Hybrid 兼顾开发速度 + 生产稳定。

---

**Q8 — Prompt cache policy 细节**

- **(a) System + glossary cached / user msg + history NOT cached** — Anthropic 官方推荐，覆盖 90% input；TTL = 5min ephemeral；首次 cache write 1.25× input cost, 后续 read 0.1× （推荐 — Session 28 β PoC math baseline）
- **(b) System ONLY cached**（不 cache glossary）— 牺牲 cache 收益；glossary 是 80%+ 的 input；不推荐
- **(c) System + glossary + corpus excerpt cached (per page/chapter)** — 在 (a) 基础上加 corpus chunk cache；复杂度↑但 cache hit rate 进一步↑（受 5min TTL 限制）
- **(d) 强制 cache 全部 prompt 包括 user msg** — Anthropic API 不支持，invalid
- **(e) 其他**

注：(a) 是 industry 标准。(c) 是 Phase 2 多 user 时优化候选，但 α-now 单 user 不需要。

---

**Q9 — Fallback chain on 5xx / overloaded + quality tripwire**

(2 件事打包，避免再加 1 个 Q)

**Fallback chain**:
- **(a)** Sonnet 4.6 主 → 1 retry with exponential backoff → 回退到 Opus 4.7 (更高 avail) → 再 fail 提示 user
- **(b)** Sonnet 主 → 回退到 Haiku 4.5 (更便宜 + 更高 avail) → 再 fail 提示 user
- **(c)** 不 fallback，直接报错给 user
- **(d)** Sonnet 主 → 单 retry only → 再 fail 报错（无 cross-model fallback）— 最简单
- **(e)** 其他

**Quality tripwire**（何时 re-evaluate model pick → 触发 D-080 v1.1 §8 amendment）:
- **(α)** Anthropic 发布新 model 主 version（Sonnet 5.0 / Opus 5.0 / Haiku 5.0）— 自动 candidate 重审
- **(β)** User report ≥3 次 quality 问题（在 1 周内）— 重新跑 γ PoC
- **(γ)** β 切换时（自然 milestone）— 全量重审
- **(δ)** 以上全部

注：(α) + (β) + (γ) 不互斥，δ 是 super-set。

---

**回完 Q6-Q9 我会做**：

1. Round 2 答案登记 + Turn 5 propose D-088 g3 ADR draft
2. ADR 内含 §2.1-§2.5 (model pick + version pin + cache policy + fallback + tripwire)
3. Turn 6 Q10 close gate (single sign-off Q)
4. Q10 = α → LOCKED final + Turn 7 close + commit plan

**不会做的事**：

- 不擅自重新 dispatch subagent 重测（PoC 已 closed；除非 user 显式要求第二轮）
- 不动 D-083~D-087 lock
- 不写 Phase 2 实施代码（PoC scaffold 已结束 lifecycle）

---

### Turn 5 (2026-05-19) — Round 2 答案登记 + D-088 g3 LOCKED draft + Q10 close gate

**User Round 2 答**:

| # | 问题 | 答 | D-088 §2 lock |
|---|---|---|---|
| Q6 | Routing | **(c) 全 Opus 4.7** — α max-plan $0 可承担；放弃 PoC 推荐的 tier 方案 with 明示理由 | §2.1 |
| Q7 | Version pin | **(c) Hybrid** — α alias / β date string | §2.2 |
| Q8 | Cache policy | **(a) System + glossary cached / user msg + history NOT** | §2.3 |
| Q9 | Fallback + tripwire | **(d) 1 retry only no cross-model fallback** + **(δ) 全部 tripwire conditions** | §2.4 + §2.5 |

**D-088 ADR 已写入** `docs/decisions/D-088-phase2-ai-model.md`（LOCKED draft, 320 行 g3）:

- **§2.1** Default model = Claude Opus 4.7 (单模型，无 per-scope routing) — PoC ★★★★★ avg 5.0；signature kana_helper depth (Q5 ★★★★★+) 与 D-012 项目核心动机直接对齐；α max-plan OAuth $0 effective billed (D-069 precedent)
- **§2.2** Version pin = Hybrid (α `claude-opus-4-7` alias / β `claude-opus-4-7-YYYYMMDD` date string at OQ-40 close) — α dev velocity + β production stability
- **§2.3** Cache = System + glossary block (~90.5K tokens) ephemeral cache_control / user msg + history NOT cached / TTL 5min / expected hit rate 80-95% within active session
- **§2.4** Fallback = exponential-backoff 1 retry on 5xx/overloaded/timeout → user-facing error "AI 暂时不可用，请稍后重试。" — NO cross-model fallback (避免 behavioral inconsistency)
- **§2.5** Tripwire (δ 全部 conditions): (α) Anthropic 发新 major model / (β) user ≥3 quality issues/week / (γ) OQ-40 β open close / (annual floor) 12mo silent drift check — 任一 → file OQ + γ PoC re-run + D-080 v1.1 §8 amendment

**§3 Out-of-scope 推 Session 30+**:
- D-089 数据源 contract (FS read / Vercel Blob / DB / manifest schema)
- D-090 LLM cost cap (复用 D-071 三档；β 前 MUST land per §5.2 high risk)
- D-091 state mgmt / D-092 UI library / icon / 字体 / i18n / test stack / Lighthouse budget
- β-future auth / multi-user / DB / SSO / 版本 date-string switch
- step 5 Tier + Phase 2 预算（D-088+D-089+D-090 全锁后评估）

**§4 Rejected alternatives** (Q6 a/b/d/e + Q7 a/b + Q8 b/c/d + Q9 a/b/c + tripwire α/β/γ only) 都有具体 rejection 理由。

**§5.1 Positive**: signature kana_helper 体验最强 / 单模型简化 implementation / α $0 cost / cache 80-95% hit / 版本 hybrid α dev velocity + β stability / tripwire δ keeps D-088 honest。

**§5.2 Risks + mitigations** (6 项)：
- β cost spike (High) → D-090 cost cap MUST land before OQ-40 close + tripwire §2.5(γ)
- Opus 4.7 deprecation (Medium) → tripwire §2.5(α) + hybrid alias absorbs minor
- 5min cache TTL idle gap (Low) → 80-95% expected hit + idle cold re-write 1.25× input
- No fallback → 5xx (Low) → 1-retry covers 98%+ blips
- User override PoC routing → β cost over-engineering (Medium) → tripwire §2.5(γ) re-eval at β open
- Subagent path 没测真 cache hit rate H3 (Low) → run_poc.ts artifact runnable for β-pre validation

---

**Q10 (close gate) — D-088 草案签字**（single sign-off Q）

- **(α)** ACK D-088 LOCKED draft → 转 LOCKED final + Session 29 Turn 6 close + 等 user "go commit"
- **(β)** D-088 需要修订（user 指出 §2.1-§2.5 具体哪段）
- **(γ)** 别的处理

回完 Q10 = α → Turn 6 close + commit；β → 重写 + 重提 Q10。

---

### Turn 6 (2026-05-19) — Q10=α / D-088 LOCKED final / pre-close self-check / STATE sync / commit plan

**User answer Q10 = α** → ACK D-088 LOCKED draft as-is + Session 29 Turn 6 close + go commit + push（pending explicit "go" gate per project CLAUDE.md hard rule）。

**D-088 sign-off applied**:
- `docs/decisions/D-088-phase2-ai-model.md`: Status row "LOCKED draft" → **LOCKED final**; §7 Reviewer #1 (user) PENDING → **APPROVED (path α)** + bottom line "LOCKED draft pending Q10" → "LOCKED final — Session 29 Turn 6 user terminal sign-off path α 2026-05-19"
- 本 session §2 D-088 row: **LOCKED draft Turn 5 → LOCKED final Turn 6** (with file ref + 320 行 g3 deep + path α 2026-05-19)
- 本 session 顶部 Status row: "Turn 5 LOCKED draft 等 Q10" → **✅ CLOSED — Turn 6 D-088 LOCKED final + STATE.md sync + commit plan ready**

---

**Pre-close self-check** (per D-027 §5):

| 项 | 状态 | 证据 |
|---|---|---|
| D-088 ADR LOCKED final | ✅ | Status row + §7 sign-off + bottom line |
| Session 29 log = 6 turn 完整 | ✅ | Turn 1 (Round 1 4Q meta) / Turn 2 (Round 1 ans + γ PoC plan + Q5) / Turn 3 (PoC executed + findings) / Turn 4 (Round 2 4Q) / Turn 5 (Round 2 ans + D-088 g3 draft + Q10) / Turn 6 (本段 sign-off + close) |
| γ PoC evidence on disk | ✅ | `evidence/phase2_d088_poc_2026-05-19/` = summary.md + quality_matrix.md + cost_table.md + raw_logs/ × 12 = 16 files |
| γ PoC scaffold artifact | ✅ | `.phase2-spike/` 10 files (gitignored per L1 + .gitignore rule added) |
| Rule A 抽检 checklist | ✅ | `evidence/.../quality_matrix.md` 底部 "Rule A audit checklist" 4 项 user spot-check 待执行 (advisory; doesn't block lock per user explicit sign-off) |
| Rule D 合规 | ✅ | Writer = 12 subagent (general-purpose × sonnet/opus/haiku) ≠ Reviewer = main Opus 4.7 session + final user sign-off |
| Rule B (no failure to archive) | n/a | γ PoC 12 dispatch 全 success；无 failure attempt |
| Rule C (Phase retro) | n/a | Phase 1 retro = RETROSPECTIVE.md FINAL；Phase 2 retro 等 Phase 2 收尾 |
| STATE.md 已锁定决定数 87 → 88 | ⏳ | 本 turn 即将 edit |
| STATE.md 未决问题数 unchanged (4) | ✅ | 本 session 无新 OQ；OQ-40 含义 D-088 §2.2 +§2.5(γ) 强化但未关 |
| STATE.md 当前阶段 (Phase 2 设计阶段 step 4 第 2 ADR closed) | ⏳ | 本 turn 即将 edit |
| STATE.md 下一会话 (Session 30 = D-089 + D-090) | ⏳ | 本 turn 即将 edit |
| STATE.md 最后更新 prepend Session 29 entry | ⏳ | 本 turn 即将 edit |
| LLM cost actual | ✅ $0 visible / $0 billed | subagent dispatch via max-plan OAuth (D-069 + memory `project_max_plan_billing.md` precedent) |
| Code change | ✅ PoC throwaway scaffold only | `.phase2-spike/` gitignored；不动 `packages/extractor/` (D-083 §2.2 锁)；不进入 Phase 2 实施 |

---

**Commit plan** (pending user "go commit" gate per CLAUDE.md hard rule):

Staged for commit (1 atomic):
1. `docs/decisions/D-088-phase2-ai-model.md` (new, 320 行, LOCKED final)
2. `docs/discussion/2026-05-19-session-29.md` (new, 6 turn full log)
3. `docs/STATE.md` (modified — 已锁定决定数 87→88 + 最后更新 prepend Session 29 + 当前阶段 + 下一会话)
4. `.gitignore` (modified — `.phase2-spike/` rule added)
5. `evidence/phase2_d088_poc_2026-05-19/` (new dir, 16 files: summary.md + quality_matrix.md + cost_table.md + raw_logs/ × 12 + raw_logs/ subdir)

**NOT committed**:
- `.phase2-spike/` — gitignored per L1, throwaway PoC scaffold lifecycle
- `data/` — gitignored per D-050

Commit message style match D-083~D-087: `docs: D-088 LOCKED — Phase 2 AI model = Opus 4.7 default + Hybrid pin + system+glossary cache + 1-retry-no-fallback + δ-all-tripwire (Session 29)`

Push: pending user "push it" gate per Session 27 pattern (commit + push 分开跑)。

---

**Session 29 ✅ CLOSED** 2026-05-19 — 6 turn 完整链：
1. Turn 1 Round 1 4Q meta-framing (scope / ADR 拆分 / PoC / 颗粒度)
2. Turn 2 Round 1 ans (Q1=a Q2=b Q3=γ Q4=g3) + γ PoC plan L1-L11 + Q5 close gate
3. Turn 3 Q5=α + γ PoC executed (scaffold 10 files + 12 subagent dispatch + 3 evidence files + 12 raw_logs) + H1-H4 verdicts inline
4. Turn 4 Round 2 4Q (Q6-Q9 routing / version pin / cache / fallback+tripwire)
5. Turn 5 Round 2 ans (Q6=c Q7=c Q8=a Q9=d+δ) + D-088 g3 LOCKED draft 320 行 + Q10 close gate
6. Turn 6 Q10=α sign-off + LOCKED final + pre-close self-check + STATE sync + commit plan

PoC cost actual: $0 billed (max-plan OAuth) / wall ~45 min vs L10 estimate 2-3h soft (significantly under). step 4 第 2 ADR (D-088) closed; D-089 数据源 + D-090 cost cap 推 Session 30+。

---

## 5. References

- `docs/decisions/D-083-phase2-direction.md` — Session 24 LOCKED（Phase 2 大方向 + Q7=s1 intent）
- `docs/decisions/D-084-v1-0-3-kana-helper-backfill.md` — Session 25 LOCKED + v1.0.3 shipped
- `docs/decisions/D-085-phase2-form-mainline.md` — Session 26 LOCKED（form mainline + §5.3 LLM cost cap risk handoff → D-090）
- `docs/decisions/D-086-phase2-portability-strictness.md` — Session 27 LOCKED（s1 strict web stack）
- `docs/decisions/D-087-phase2-stack.md` — Session 28 LOCKED（Next.js 15 + Vercel AI SDK + Vercel + TS strict + SSR）
- `docs/decisions/D-071-budget-cap-and-emergency-halt.md` — Phase 1 三档 cap 经验（D-090 直接复用）
- `docs/decisions/D-069-anthropic-via-agent-sdk.md` — Phase 1 Anthropic SDK 经验（D-088 model 选型基线）
- `docs/discussion/2026-05-19-session-28.md` — Session 28 β PoC 完整数据（corpus + cost math + framework analysis）
- `docs/STATE.md` — 当前快照（Phase 2 设计阶段 step 4 第 1 ADR closed）
