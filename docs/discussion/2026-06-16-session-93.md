# Session 93 — Quiz Phase 1 (翻訳 backfill) スケール バッチ S93 (3 回)

> 日付: 2026-06-16 / フェーズ: Phase 5 Stage 6 (Web App 統合) — Quiz サブ段階 Phase 1 スケール
> 起動: ユーザー路由「Quiz Phase1 续批」(回数未指定)。前提: S92 完了・commit 済 (`7a8075f`)、翻訳済 16/29 回。

## 0. 起点

- STATE + session-92 読了。回数未指定 → 路由②「最新優先・既訳 16 回除外で 3〜5 回」→ **最新 3 回 = `2015h27h` / `2014h26a` / `2014h26h`** (STATE「次候補」と一致)。各 100 問 = 300、figure 6/22/14=42。

## 1. 実行設計

S88〜S92 の統合 1 ワークフロー (D-小5) + フルページ併読 (D-小6) + repair 語義ガード (D-小7) + 統合バッチ combiner `scripts/quiz-phase1-batch.mjs` (D-小8) を踏襲。**本バッチはコード無変更** (全て scripts 組込済)。新規小決策なし。

## 2. 実施

| Step | 内容 | 結果 |
|------|------|------|
| prep ×3 | crop+page fail-fast + glossary 束縛 | 全存在 (figure 6/22/14=42)、統合 input 300 問・id 全一意 |
| batch translate | `quiz-phase1-batch.mjs translate S93` | `input_batch_S93.json`/`items_batch_S93.json` (figure 42 全フルページ付) |
| translate (統合) | `wf_e84fb128-f4b`、writer(general-purpose)→reviewer(code-reviewer)2R | **300/300、raw PASS 295 / CONCERNS 4 / FAIL 1 / null 0**。660 agent / 24.2M tok。pause 指示なし→全量完走 |
| merge ×3 | per-exam committed sidecar | 各 100/100、missing 0。clean stem 46/55/49=150 |
| figure 実読 (triage前) | 非PASS の figure 問 q086/q093/q088 を crop+page で ground-truth | q088 ウ=B&C 確認、q093 choices=COUNTIF 確認、q086 表は figure 一致 (※後に critic が行12転置を捕捉) |
| batch ruleA | `quiz-phase1-batch.mjs ruleA S93` + 非PASS 5 強制 | sidecar_batch_S93 (300) + ruleA_items_S93 (39、figure23) |
| ruleA (統合) | `wf_4eaecb3f-f63`、N=39 層化独立 critic | **accurate 38/39**。not-accurate 1 = q086 |
| q086 是正 | Rule A critic が footing 分析で行12転置を pinpoint → surgical 是正 + 独立再検証 | PASS (figure 実読・全セル・footing・正解 0 mismatch) |

## 3. 結果

### Rule D in-pipeline: raw 295 PASS / 4 CONCERNS / 1 FAIL → 実効 翻訳欠陥 0 (q086 是正後)

- **FAIL 1 = `2014h26h-q086`** (表計算、図1): in-pipeline reviewer 2R とも FAIL (再構成表が raw OCR と大差ある点を疑問視、欠陥セル未特定)。**独立 Rule A critic が pinpoint** → 是正 + 独立再検証 PASS (後述)。
- **CONCERNS 4**: q029(accurate/low)・q093(accurate/medium=input汚染)・q039(accurate/low)・q088(accurate/medium=input汚染)。翻訳自体は figure/源に忠実。
- **triage は verdict ラベルでなく repair 後実落盘訳文 + 独立 Rule A + figure 実読で判定** (S88 q072/S91/S92 教訓再適用)。

### Rule A (独立 critic, N=39 [各回12 + 強制3、層化 figure23]) — **accurate 38/39**

- **severity none22 / low14 / medium3 / high0**。not-accurate 1 = q086 (是正後解消)。
- **medium3**: q086 (NOT-accurate、是正済) / q093 (accurate、input汚染 backlog) / q088 (accurate、input汚染 backlog)。
- **low14** は全て本土 zh 自然さ・文体 (意味/正誤/脱落/捏造ゼロ、受容)。
- 詳細 `evidence/phase5/stage_06_quiz_phase1/rule_a_audit_S93.json` (全 39 audit + applied_fix + upstream_ocr_backlog)。

### applied_fix 1 = `2014h26h-q086` (footing で捕捉、surgical)

- **欠陥**: translator が figure から表を大半正しく再構成したが、**行12 (17:00〜) の現状 東/西を 1 か所転置** (clean=`88|86`、figure=`86|88`)。zh/en 伝播。merge 構造検査通過 (構造≠意味)。
- **footing 証明**: 行16 表示合計 東804/西808。defective `88|86` では列実セル和=806/806 で破綻。是正後 `86|88` で 804/808 一致。
- **正解不変**: a=I15 (行12非依存) = ceil(158×8/60)=22=ウ。三語保持。
- **是正**: 行12 B|C を `88|86`→`86|88` (他セル/行/選択肢/正解 不変、三語同時)。再 merge。
- **独立再検証 (Rule D、verifier=独立 critic ≠ fixer)**: figure page-35 実読で 全13行×9列 0 mismatch・footing 三語整合・正解ウ三語保持・他転置なし → **PASS**。
- **Rule B**: `failures/quiz_phase1_S93_2014h26h-q086_attempt_1{.md,_defective.json}`。

### 上流 (Stage 2) OCR 欠陥の申し送り (翻訳は figure を正として忠実・成果物に影響なし)

- **`2014h26a-q093`** (影響大): input.choices_jp が別問汚染 (UIウィジェット)。figure の真の選択肢は COUNTIF 式4択 (正解イ)。翻訳は figure を正として正訳。**JP locale 表示が誤選択肢 → input.choices_jp 全面再OCR 推奨**。
- **`2014h26h-q088`**: input.choices_jp[ウ]=『商品Bと商品D』だが figure ウ=『商品Bと商品C』(最適化検算で B+C=220 最大=正解ウ で figure が正)。翻訳は figure に忠実。input[ウ] D→C 是正推奨。
- S89/S90/S91/S92 に続き 独立 critic のフルページ照合が上流欠陥を継続捕捉。

### 検証 (全 GREEN・回帰なし)

- tsc 0 / eslint 0err (既存 warning 1) / **vitest 455 passed** (+2 skipped) / build exit 0 (Compiled successfully)。
- **nft IPA leak = 0**。quiz route trace = quiz_index + questions + **translations/19 回.json** (新 3 sidecar resolve 確認、16+3)。

## 4. 学び (fix-checklist 追記)

- **figure 表の大規模再構成は footing/合計セルで内部整合を機械検算せよ**: 1 行 2 セルの東西転置は見た目では気付きにくいが、列実セル和 vs 表示合計行の突き合わせで機械的に検出できる。in-pipeline reviewer は「raw と違いすぎる」と FAIL したが欠陥セルを pinpoint できず、**独立 Rule A critic の footing 分析が特定** (写審分離+独立抽検の複利、S88 q072 同系)。
- **figure 問の choices_jp 汚染は『翻訳を figure に忠実』としつつ上流 backlog 化** (q093/q088、S91 q069 と同方針)。translator が figure を正として正しい選択肢を訳出するため zh/en は正だが、JP locale が raw choices_jp 表示のため input 是正までは JP/zh-en に乖離が残る。choices_jp 修正は Stage 2 corpus scope (本 session では翻訳忠実度を優先し input 不改変、backlog 記録)。
- **低 dpi crop の目視は誤読しうる**: 私 (主 context) は crop で q086 行12 を `88|86` と読んだが、footing と figure 5倍ズーム実読では `86|88` が正。独立 critic の機械検算が主 context の目視誤りを是正 = Rule A/D の網兜価値。

## What's Next

- **バッチ S93 完了**: 300/300 三語、Rule A **38/39 accurate** (medium3=q086是正+q093/q088上流backlog、high 0)、Rule D 実効 翻訳欠陥 0 (FAIL 1=q086 は是正+独立再検証 PASS)、全検証 GREEN、nft IPA 0。Phase 1 翻訳済 **19/29 回**、残 10 回。
- 次バッチ = ユーザー「Quiz Phase 1 续批」で起動。次候補 = `2013h25h` / `2013h25a` / `2012h24h` (最新優先)。D-小6/7/8 とも scripts 組込済=追加作業不要。
- **要ユーザー判断 backlog (上流データ品質、翻訳成果物に影響なし、累積)**:
  - **S93 新規**: `2014h26a-q093` choices_jp 全面汚染 (UIウィジェット→COUNTIF式 再OCR、影響大) / `2014h26h-q088` choices_jp[ウ] (商品D→商品C 再OCR)。
  - 累積 (S92 以前): `2016h28h-q001/q012/q096`・`2015h27a-q088` 再OCR / `2016h28a-q025` glossary / `2017h29h-q069` 再OCR / `2019h31h-q061` figure crop。
  - 観察 (q086): figure 原典注記の「行16合計には行14の値を使用」の「行14」表記は IPA 原典由来 (翻訳は忠実転記)。Phase 2 解析生成で自然再検証。

## Pre-close self-check (D-027 第5条)

- **D 新規**: なし (D-136 枠内)。小決策も新規なし (D-小5〜8 は既存・scripts 組込済)。
- **OQ**: 新規の正式 OQ なし。OQ-01/02 carryover。上流データ品質項は backlog として記録 (OQ 化はユーザー判断後、従前方針)。
- **STATE sync**: 最后更新 / 当前阶段 / Stage 6 行 / 次セッション (19/29) を更新。锁定决策 136 のまま。
- **Rule A**: N=39 独立 critic (accurate 38/39)。証拠 `rule_a_audit_S93.json` + README S93 節。
- **Rule B**: q086 の FAIL attempt をアーカイブ (`failures/quiz_phase1_S93_2014h26h-q086_attempt_1{.md,_defective.json}`)。既存 failures/ 不変。
- **Rule D**: writer(general-purpose) ≠ in-pipeline reviewer(code-reviewer) ≠ Rule A auditor(critic) ≠ q086 再検証(独立 critic、fixer=主 context とは別 agent)。
- **invariants**: raw bank / questions.json 不変。translations 新規 3 sidecar (内 1=q086 是正)。コード変更なし。
- **コミット**: 確立済 cadence で commit。push はしない (要明示承認)。

---

## 追記 (同 session フォローアップ): 上流 choices_jp 欠陥 2 件を派生 corpus で是正 (ユーザー指示)

> ユーザー「把 q093/q088 这两个上流 choices_jp 缺陷顺手在派生 corpus 里修掉 (图已确证、确定性单点改)」。S93 で backlog 化した 2 件を本 session 内で是正。

### 方式 (drift-proof)

- `questions.json` は `build-quiz-corpus.mjs` が raw bank `data/ip/exams/question_bank.json` (gitignored) から**決定的に projection** する (choices_jp 逐語)。よって corpus を直接編集すると次回ビルドで上書きされる → **raw bank を正源として編集し、ビルドを再実行** (idempotent、再 garble なし)。Stage 2 の従来修正と同方式。

### 是正内容 (figure 已確証)

- **`2014h26a-q093`** (表計算 COUNTIF、正解イ): raw bank の choices_jp が別問汚染 (スクロールバー/チェックボックス/テキストボックス/ラジオボタン=UIウィジェット) → figure (page-43 下部の印刷選択肢) を正として COUNTIF 式 4 択へ是正:
  - ア `条件付個数(B$2〜B$36，＝A$40)` / イ `条件付個数(B$2〜B$36，＝$A40)` [正解] / ウ `条件付個数($B2〜$B36，＝A$40)` / エ `条件付個数($B2〜$B36，＝$A40)`。
  - 加えて zh/en サイドカーの ウ/エ のセル範囲が `$B$2~$B$36` (余分な `$`) だったので figure 通り `$B2~$B36` へ整合 (ア/イ は既に正)。セル参照は言語非依存のため三語同一に統一、関数名のみ `条件付個数`/`条件计数`/`COUNTIF`。
- **`2014h26h-q088`** (利益最大化、正解ウ): raw bank choices_jp[ウ] `商品Bと商品D`→`商品Bと商品C` (figure-exact)。zh/en は既に figure 整合 (`商品B与商品C`/`Product B and Product C`) で不変。

### 検証

- `build-quiz-corpus.mjs` 再生成 → **questions.json diff = 当該 2 問のみ (5 insertions / 5 deletions)**、quiz_index.json 不変 (counts 不変)。**answer_keys/correct_answer 不変** (イ/ウ)。
- tsc 0 / eslint 0err / **vitest 455 passed** / build exit0 / **nft IPA 0** (questions.json は quiz route trace 内)。
- **独立 Rule D 再検証 (critic ≠ fixer)**: figure 5倍ズーム実読 + corpus コードポイント照合 + 算術再計算 → **両問 PASS** (q093 全4式の `$` 位置一致・三語同一・正解イ妥当 / q088 ウ=商品Bと商品C・正解ウ=B+C=220万 最大)。

### 影響

- **q093/q088 は backlog から除去 (RESOLVED_IN_CORPUS)**。JP locale 表示が figure と一致する正しい選択肢になり、JP/zh/en の三語整合も確立。
- raw bank (gitignored) を編集したため、Stage 2 再 OCR を将来走らせる場合は本是正の再適用が必要 (本ログ + `rule_a_audit_S93.json` の `corpus_fix` に before/after 記録)。
- 残 backlog (累積、未是正): `2016h28h-q001`/`q012`/`q096`・`2015h27a-q088` 再OCR / `2016h28a-q025` glossary / `2017h29h-q069` 再OCR / `2019h31h-q061` figure crop。

### Rule 適用 (追記)

- **Rule A**: 独立 critic 再検証で figure 照合 PASS (写審分離)。
- **Rule D**: fixer (主 context) ≠ verifier (独立 critic)。
- **invariants**: answer_keys/correct_answer/question counts 不変。questions.json diff は当該2問のみ。コード変更なし。
