# Session 100 — 2026-06-22

> Phase 5 / Stage 6 (Web App データ統合) / サブ段階「Quiz 接過去問」/ **Phase 2 (解析預生成, D-137) re-pilot**。
> 前段: S99 で Phase 1.6 全量完了 (key 2件是正 + choices 28件三語クリーン + figure-display 修正 + incomplete-source 2件文脈注入)。
> 本 session ルート = ユーザー路由词「**phase2 开启**」。

---

## ゴール
D-137 (解析預生成) を **figure-fidelity hardening + 全問 key-guard** 込みで 2025r07 (100問) に re-pilot → ユーザー gate → 29回 scale。S97 pilot 草稿 (`explanations/2025r07.json` Jun 20) は key/choices/figure 修正前の陳腐ドラフト → 再生成で覆盖。

## 実行ログ

### Run 1 — 初回 re-pilot (`wf_99fc9868-4b1`, 412 agent / 16.4M tok / ~56分)
- prep 100問 (16図/84非図、全100にPhase1訳+glossary) → generate workflow (JP生成[key-guard内蔵]→code-reviewer→翻訳→code-reviewer、写審分離 Rule D)。
- **結果 100/100**: jp **PASS 100** / tr **PASS 98 / CONCERNS 2** (q006/q036=訳質の軽微、merge済)。
- merge が **key_guard suspect = q030/q034/q071** を報告 (workflow 最終 summary は q030/q071/q074 を報告=不一致)。

### 重大所見1 — q034 = key-guard が捕捉した **stem 腐敗** (坏键ではない)
- merge の suspect が round-1 `generate_result` を使ったため q034 (round-1: 導出ウ≠key イ) を捕捉。workflow 最終 summary は repair 後 (イ/一致) を見て見逃し。
- **q052 協議で主 context が源実読裁決** (`page-15` を sharp crop+3x): 官方 stem = 「…少なくとも**あと何個**販売すればよいか」(=追加分)。
  - **stored key イ=1,200 は正しい** (=6,200 総量 − 5,000 現量)。**坏键ではない**。
  - 現 corpus `stem_jp` = 「…少なくとも**いくつ**販売すればよいか」(=総量) → **s7x 数字清洗が「あと」を脱落させ題意を反転** (追加→総量)。表示 stem が自分の正解と矛盾。
  - zh/en 訳 (Phase1) は「至少还需要再销售/additional units」で**既に正** (s7x腐敗前の源を訳した) → 訳修正不要、ja のみ是正で三語整合。

### 重大所見2 — パイプライン欠陥 (scale ブロッカー)
1. **generate_result 未永続化**: merge は `generate_result_<exam>.json` を key_guard 権威源とするが、本フローは未書込 → merge が **S97 陳腐ファイル (Jun 20)** を黙って消費。**「workflow 結果を永続化してから merge」手順が抜けていた**。
2. **repair が key-guard 不一致を黙殺**: generate が stem 腐敗を papered over (存在しない `stem_jp_clean` を捏造して matches_key=true 化)。q034 (Run1=単発幻覚) / q054 (「何%」→「何分」を「OCR誤り」と注記しつつ matches_key=true)。最終 key_guard のみ永続化のため suspect 信号消失。Run1 で捕捉できたのは**陳腐 S97 ファイルが偶然 round-1 を保持**していたから (脆い)。
3. **cosmetic stem 腐敗は key-guard が flag しない**: 答えが robust だと matches_key=true のまま (q054)。別軸の検出が必要。

### D-140 (lock) — Phase 2 stem-corruption handling [ユーザー gate ×2]
**Q1 (q034 の処理 + scale 範式)** → ユーザー「**内联修 + scale 同策略**」: 答案相关 stem 腐敗は key-guard suspect → 主 context 源実読裁決 → stem 腐敗なら drift-proof 内联修 (key 不変) / 真坏键なら升级ユーザー裁決。
**Q2 (cosmetic stem 腐敗の処理)** → ユーザー「**全部内联修 (最彻底)**」: `stem_corruption_suspected` 旗標で硬化 re-run が全 stem 腐敗 (答案相关+cosmetic) を枚举 → 逐個源実読裁決 → drift-proof 内联修 → 解析生成。quiz+解析を完全整合。
- 却下: cosmetic を別 track へ defer (S99 choices 型) / Phase 2 前に stem-fidelity sweep 先行。理由=解析品質と表示題面の整合を最優先 (ultracode)。

### 硬化実装 (D-140 執行、D-137 key-guard の精緻化)
- **generate.workflow**: ① STEM-CORRUPTION GUARD (stem を literal 解釈、`stem_jp_clean` 捏造禁止、辻褄合わせ禁止) ② `key_guard.stem_corruption_suspected` 追加 (cosmetic も true) ③ **round-1 key_guard を永続化** (`key_guard_round1`、repair が masking 不可、suspect=union(round1,final)) ④ JP reviewer の key_guard_honest を「無根拠な読み替え/stem_jp_clean 仮定を捕捉」へ強化。
- **merge**: suspect=union(round1,final)、sidecar は round-1 (盲推) を保持、`stem_corruptions` リストを別途枚举・報告 (`* = answer-affecting`)、`stem_corruption_count` を sidecar に追加。
- **新 script `quiz-phase2-stemfix.mjs`**: drift-proof stem 是正 (現値 assert→raw bank `stem_jp` 内一回置換→build-quiz-corpus 再生成)。FIXES 配列で複数件対応。

### q034 stem 是正 (drift-proof, 適用済)
- `quiz-phase2-stemfix.mjs`: 「少なくともいくつ販売すればよいか」→「少なくともあと何個販売すればよいか」(1回出現 assert)。
- `build-quiz-corpus` 再生成 → **questions.json diff = q034 stem_jp 1行のみ、correct_answer イ 不変、quiz_index 不変**。

### Run 2 — 硬化 re-pilot (`wf_8aff2676-a32`, 300 agent / 5.5M tok, **session limit 直撃で部分完了**)
- **session limit (resets 22:00 JST) 直撃**: jp PASS 33 / null 67、tr translate_failed 100。stage-2 (review/translate) は大半失敗。
- **但し stage-1 (盲推 key-guard, vision 源読) は全100完了** → `key_guard` + `key_guard_round1` を全100で harvest (`keyguard_harvest_2025r07.json`)。これが本 run の核心価値。
- **harvest 結果**: **matches_key=false = 0** (= **bad key 0**、q034 stem 是正後 derivable イ で一致確認) / **stem_corruption_suspected = 5** (q012/q019/q040/q051/q054、全 matches_key=true = cosmetic)。

### stem_corruption 5 件の裁決 (主 context 源実読, q052 協議)
全 5 件を原典頁 (sharp crop+3x) で確認。**2 件 stem 腐敗 + 3 件 choices 腐敗。全 zh/en は Phase1 訳が既にクリーン (OCR garble を訳者が見抜いた) → JP のみ是正**:
- **q012 stem** (page-06): 「商標**活**」→「商標**法**」(OCR 法→活)。key エ 不変。
- **q054 stem** (page-25): 「何**分**短縮」→「何**%**短縮」+ 表セル「全体の60%**5分**」→「全体の60%」。key ア=30 不変。
- **q019 choices** (page-09): ア「。,」→「,」/ イ「試し首」(truncation)→「試し置きできる。」。key ア 不変。
- **q040 choices** (page-19): ア「和要員」→「委員」/ イ「却下してよ**UN**」→「却下してよい」/ ウ「は。」→「は,」/ エ「は.」→「は,」。key イ 不変。
- **q051 choice エ** (page-23): 末尾 OCR 頁番号ゴミ「ーー 23 os」除去。key エ 不変。

### 是正適用 (drift-proof, `quiz-phase2-stemfix.mjs` 拡張: STEM_FIXES + CHOICE_FIXES)
- 6 問 / **11 フィールド** (q034 stem + q012 stem + q054 stem×2 + q019 choices×2 + q040 choices×4 + q051 choice×1) を raw bank で assert-replace → build-quiz-corpus 再生成。
- **questions.json diff = 11 行 (11+/11−)、correct_answer 全 6 問不変 (エ/ア/イ/エ/ア + q034 イ)、quiz_index 不変、translations 不変 (zh/en 既正)**。

### 新所見 — Phase 2 は **非図題の choices 腐敗**も付随的に捕捉 (scale 含意)
S99 choices-fidelity track は**図題 467 のみ**対象 (figkey 盲推駆動) → 非図題の choices 腐敗 (q019/q040/q051 = OCR garble) は無監査で残存。Phase 2 の key-guard は全問の源頁を読むため、これらを `stem_corruption_suspected` で付随捕捉。2025r07 非図 84 問中 3 問 (~3.6%) → 29回 scale で非図 choices 腐敗が相当数 surface する見込み (要ユーザー scale 方針)。

### Run 3 — 最終 re-pilot (`wf_bdbd1f50-75f`, 414 agent / 16.6M tok, **完全完了**)
- **全11フィールド是正後のクリーン corpus** + 硬化 workflow で全100フレッシュ再生成 (session limit 回復後、22:00 JST reset 経過)。
- **done 100/100 / jp PASS 100 / tr PASS 97・CONCERNS 3 (q034/q045/q076=訳質微) / bad key 0 / suspect 6 (全 benign 概念問 figure_derivable=false)**。
- 結果永続化 (`generate_result_2025r07.json` = 本 run、**従来 skip していた永続化手順を実施**) → merge。

### 重大所見3 — cosmetic 腐敗検出は **非決定的** (scale 方法の含意)
- **Run 3 が新たに 8 件 stem_corruption_suspected を flag (Run 2 の 5 件とは完全に別集合)**: q020/q026/q047/q064/q066/q084/q085/q094。
- 裁決 (主 context、source 実読 + sidecar stem_jp_clean 確認):
  - **4 件 real choices 腐敗** (q020 末尾「]」/ q047「0S」→「OS」×2 / q064「定義**む**する」/ q084 末尾「・」) → **drift-proof 是正** (zh/en 既正)。
  - **4 件 false positive** (q026/q066/q085/q094): raw stem_jp は腐敗だが **stem_jp_clean (= app 表示) は正** → 是正不要。critic は非表示の raw を見て flag (ノイズ)。
- **結論: 単一 LLM key-guard pass は cosmetic 腐敗を網羅しない (Run2=5, Run3=8 別集合, 重複0)**。29回 scale で「全部内联修」を真に達成するには **決定的 OCR-garble 検出器 (正規/ヒューリスティック: 末尾ゴミ・0S・孤立全角空白・UN 等) + 主 context source-verify** が LLM 非決定 flag より網羅的・安価。→ **要ユーザー scale 方針** (OQ 候補)。

### 追加是正 (Run 3、`quiz-phase2-stemfix.mjs` 拡張)
- CHOICE_FIXES + TRAILING_STRIPS で 4 問 / 5 フィールド是正 (q047×2/q064/q020/q084)。
- **累計 questions.json diff = 10 問 / 15 フィールド (15+/15−)、correct_answer 全不変、quiz_index/translations 不変** (git diff 確証: changed key = stem_jp×3 + choices×12)。
- sidecar: 8 flag を裁決後クリア (4 fixed + 4 FP) → re-merge → **count 100 / suspect 6 (benign) / stem_corruption 0**。

### 検証 (全 GREEN)
- tsc 0err / eslint 0err (既存 warning 1=tTerm) / **vitest 463 passed (2 skipped)** / **build exit 0** / **nft IPA-exams leak 0** (quiz route trace = data/ip/quiz/* [questions/quiz_index/translations/**explanations**] のみ、raw exams 0)。

### Run A — Rule A 独立抽検 (`wf_1d17c065-501`, N=24, **完了**)
- critic (≠ writer ≠ in-pipeline reviewer ≠ 主 context) が figure 16 + suspect 6 + plain 2 を独立核験 (図 self-read、**表示 clean stem 監査**)。
- **結果: accurate 24/24・severity none 13 / low 11・medium 0 / high 0**。independent_answer 全一致 (検算 OK)。
- **low 11 は全て無害**: ① key_guard suspect 過検出の注記 (q006/q016/q040/q041/q058/q071 = figure_derivable=false の概念問を suspect 化、`key_guard_valid=false` 2件[q016/q040]も「不要な suspect」指摘で内容誤りでない) ② 微 zh 借词 (q039「课题」→问题 / q062「物理的」=図ラベル字面一致の意図訳) ③ q045 ウ 誤答説明の例示がやや非断定 ④ q076 B$1「絶対(複合)参照」用語 nuance。**解説内容の誤り 0**。
- 証拠 `evidence/phase5/stage_06_quiz_phase2/ruleA_result_S100_2025r07.json`。
- scale 改善メモ: suspect 判定 = `matches_key=false || figure_derivable=false` は概念問を過検出 (benign noise) → scale で `figure_derivable=false && has_figure=true` に絞れる (任意)。

## Pilot 総括 (gate-ready)
- **解説 100/100** (jp PASS100 / tr PASS97・CONCERNS3) + **Rule A 独立 24/24 0med/high** + **bad key 0** + **cosmetic 10問15フィールド是正 (correct_answer 不変)** + **検証 GREEN**。Phase 2 schema/品質/key-guard/硬化 全て pilot 検証済。

## What's Next
1. **ユーザー gate**: (a) pilot 承認 → 29回 scale (D-137-D「续批」) / (b) **cosmetic OCR 腐敗 scale 検出方法** (推奨=決定的 OCR-garble detector を別 pass or Phase2 併走、LLM flag は非決定的)。
2. commit (questions.json 15フィールド + explanations sidecar + scripts + evidence + docs)。push はユーザー確認ゲート。

## Pre-close self-check (D-027 §5)
- **D 新規**: **D-140 (lock)** = Phase 2 stem-corruption handling。STATE 锁定决策 139→140 + 本ログ記録済。
- **OQ**: cosmetic OCR 腐敗 (非図 stem/choices) の scale 検出方法 = ユーザー gate 項 (STATE 次セッション に記載)。新規 OQ-ID は割当せず gate 項として扱う。
- **invariants 確認**: questions.json diff = stem_jp×3 + choices×12 = 15フィールド (15+/15−)、**correct_answer 0変更** (git grep 確証)、quiz_index 不変、translations 不変、reader コード 不変。raw bank (gitignored) 同期編集。
- **Rule A/D**: 生成 general-purpose ≠ in-pipeline code-reviewer ≠ Rule A critic ≠ 主 context 裁決 (4役)。Rule A N=24 独立。
- **Rule B**: 失敗 attempt = generation 段の papering (q034 単発幻覚 / q054 黙殺) を硬化で是正、`stemfix_adjudication_S100.md` §Rule B に記録 (是正は assert 付き drift-proof で 1 発)。
- **STATE sync**: 最后更新 / 当前阶段 / 锁定决策(140) / 次セッション 全更新済。memory (fix-checklist 19項) 更新済。

## Pre-close self-check (D-027 §5) — 暫定 (session 継続中)
- **D 新規**: **D-140 (lock)** = Phase 2 stem-corruption handling (内联修 + cosmetic 全部内联修)。本ログに記録済。
- **OQ**: 新規候補 = 非図題 choices-fidelity の scale 方針 (Run 3/Rule A 後にユーザー gate で確定)。
- **invariants**: questions.json は stem/choices 11 フィールドのみ (correct_answer 0 変更)。raw bank (gitignored) 同期。translations 不変。quiz_index/reader コード 不変。
- **STATE sync**: pilot 完了時に更新予定。

## Pre-close self-check (D-027 §5) — 暫定 (session 継続中)
- **D 新規**: **D-140 (lock)** = Phase 2 stem-corruption handling (内联修 + cosmetic 全部内联修)。本ログに記録済。standalone ADR は不要 (D-137 の執行精緻化、phase 境界でない)。
- **OQ**: 新規なし。
- **STATE sync**: pilot 完了時に更新予定 (Run 2 結果込み)。
