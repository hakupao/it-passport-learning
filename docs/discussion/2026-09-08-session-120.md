# Session 120 — 2026-09-08 — ⑤-2 U1: Sonnet 5 A/B (2015h27a 68 問、D-146 第 2 unit)

> 接続元: STATE.md「次セッション」+ `docs/phase5/PLAN_52_units.md` §0 (U0 完了 → U1)。S119 は commit 3088ec2 のみ、push 未 (ユーザー: 後で push)。

## §1 起動前申告 (D-146 第 2 条) → ユーザー承認「放行 (S119 は後で push)」
- 事前調査 (主 context) で判明した 2 点:
  1. **基準値訂正**: PLAN §2 U1 の「確認済欠陥 13 題」は波 1 全体 (2015h27a 8 + 2016h28a 5)。2015h27a 単体の基準 = `evidence/quiz_full52_wave1_S118.md` 表の **8 問 / 12 差分** (q066 既知仕様・q009 偽陽性は除外)。Opus 双 pass の偽陽性 = 2 (q009 / q066)。判定 = 問レベル召回 ≥ 7/8 かつ差分 ≥ 11/12、FP ≤ 2。
  2. **manifest**: 波 1 の是正は commit 済 (現データ q044 = 「規模÷工数」) → `--precrop` 単純再生成では召回不能。既存の是正前スナップショット `full52_fidelity_input_2015h27a.json` に、`--precrop` 再生成 (label `u1ab`) から `question_crop_png` / `prev_page_png` だけを id で移植 → `u1ab_fidelity_input_2015h27a.json` (displayed = 是正前)。
- agent: 煙試験 3 問 (q001 / q044 / q097) Sonnet → part 書き出し・crop 読み確認 → 残 65 問 Sonnet 単 pass (`model:"sonnet"`)。DISCREPANT の severity 裁決 Opus ≤ 15。evidence 検算 reviewer 1 (opus、`feature-dev:code-reviewer`)。
- token ≈ 350〜500 万 / 20〜30 分 / データ変更なし (計測のみ)。
- 停止条件: 煙試験で part 未書き出し or crop 未読 → 停止修正 (Rule B) / UNREADABLE > 5% (≥ 4 問) → 中止報告 / limit → Rule B 記録後停止。

## §2 manifest 作成 + 煙試験 (Sonnet 3 問) → PASS
- `quiz-fidelity-prep-any.mjs 2015h27a u1ab all --precrop` → 90/100 cropped (page_mismatch 3 / chumon 4 / merged_preamble 3、U0 と同一)。是正前スナップショット (68 問) に `question_crop_png` (63) / `prev_page_png` (68) を id で移植 → `u1ab_fidelity_input_2015h27a.json`。是正前 vs 現データの displayed 差 = ちょうど波 1 の 8 問 (q027/q042/q044/q059/q062/q081/q093/q097) → 基準の整合を確認。
- 煙試験 run `wf_412e4fd1-ac4` (q001 / q044 / q097、label `u1ab_smoke`): 3/3 完走、194k token / 224 秒。**part 書き出し 3/3 (絶対パス) 成功**。**crop 優先読み実効**: q001 画像 Read 1 回、q044 2 回 (crop + zoom)、crop 無し q097 は 9 回 (page-45/46 + zoom)。
- 基準命中: q044 「規模÷工数」の ÷ 脱落を捕捉 (severity semantic = Opus gp と同判定)、q097 ウ/エ の空白脱落 2 件を捕捉 (S118 検索式例外を自発適用)。**q097 stem 【】→〔〕は未計上だが transcript に〔あり → machdiff AGENT_MISSED 型で拾える見込み**。q001 CLEAN。
- merge-parts は母数 68 に対する 3 問 run のため 65 MISSING で exit 1 (想定内、本番は全量 run)。
- 判定: 停止条件 (part 未書き出し / crop 未読) に非該当 → 本番 68 問 (煙試験 3 問を含む) を label `u1ab` で起動: run `wf_ce7c01b8-868`。

## §3 本番 run (Sonnet 68 問) → merge → machdiff → 基準突合 → **PASS**
- run `wf_ce7c01b8-868`: 68/68 完走、agents_error 0、UNREADABLE 0、**4,027,589 token / 15.2 分 / tool 568**。DISCREPANT 8 問 = 基準 8 問と完全一致。
- **part 書き出し 66/68**: q034 (途中切断) / q066 (末尾余分文字) が壊れた JSON → merge-parts exit 1。journal.jsonl の schema 検証済 `result` から復元、原本は `.json.broken` で保存 (Rule B)。復元後 merge 68/68。→ ⑨: merge-parts に `--journal` 復元オプション (U3 前に実装推奨)。
- machdiff: same 339 / AGENT_MISSED 1 (q066 既知仕様) / VERDICT_CONFLICT 0 (Opus は各 pass MISSED 2)。
- 基準突合 (field 単位、欠陥語句の包含で判定): **差分召回 12/12、問召回 8/8、基準外差分 0、q009 / q066 CLEAN** (Opus は q009 を両 pass で偽陽性)。severity 差 2 (q027 cosmetic→semantic 過大 / q044 answer_affecting→semantic 過小 = Opus gp と同判定)。正解肢上 2 = 基準どおり。
- 画像 Read 合計 159 = 2.3 回/問 (1 回 = 43 問、crop 題 2.2 / crop 無し 3.8)。
- contamination: q066 agent が S118 Opus 先行監査 JSON を読んで参照 (基準外のため判定無影響) → ⑨: prompt に evidence 参照禁止。
- Opus 裁決 agent (申告 ≤ 15) は基準 severity が既に主 context 裁定済みのため**不要と判断し未起動** (申告より ≈ 75 万 token 節約)。
- **判定 PASS → 以後の抄写 agent は Sonnet 5 単 pass を既定** (machdiff は必須のまま)。evidence `evidence/quiz_sonnet_ab_S120.md`。PLAN §0 / §2 / §3 更新 (基準「13」→「8 問 / 12 差分」訂正)。
- Rule D: reviewer `u1-reviewer` (feature-dev:code-reviewer, opus) に一次データからの独立再計算 + 原寸抽検 ≥ 3 を委託 → §4。

## §4 Rule D (u1-reviewer = feature-dev:code-reviewer, opus) → **PASS-with-notes** → 全指摘反映
- 核心 5 指標 (基準 / 召回 / 偽陽性 / machdiff / manifest) と part 復元・画像統計・原寸抽検 4 箇所すべて独立再現。
- **MAJOR-1/2**: Opus **gp 単 pass が既に 12/12・8/8・FP 0** (evidence 旧記載「gp 7」は誤り)。「Sonnet 単 pass = Opus 双 pass 和集合」は誤導 → 「Opus 最良単 pass と同等、本 exam で cr の寄与 0」に訂正。Sonnet 切替の結論は不変 (むしろ安定)。
- **MINOR-3/4 (隔離)**: q066 は結果を書く**前に** S118 Opus 監査を Bash で読んだ (事前参照)。別題 part を Read した agent 7 件 (q044/q060/q066/q068/q073/q081/q093)。→ ⑨: prompt に `evidence/`・`fidparts/` 他ファイル参照禁止。
- **MINOR-5**: part と StructuredOutput の不一致 31/68 (notes_jp 30 + q081 detail_jp 1、鍵 field は全一致) → journal.jsonl を正とする運用 (⑨ `--journal`)。MINOR-6/7、NIT-8/9/10 も本文反映。
- evidence §2 / §2a / §3 / §4 訂正 + §6 追記。

## Pre-close self-check (D-027 第 5 条)
- 新規 D: なし (U1 は D-146 の実装 unit、「抄写 = Sonnet 5 単 pass 既定」は PLAN §2/§3 に運用として記載)。新規 OQ: なし。⑨ 登録 2 件 (merge-parts `--journal` / prompt に evidence・fidparts 参照禁止) は STATE「次セッション」に明記。
- STATE.md 同期済 (最后更新 = S120 / 次セッション = U2 + ⑨ 脚本改修 2 件推奨)。PLAN_52_units.md §0 (U1 完了行) / §2 (基準訂正 + 結果) / §3 (Sonnet 既定、Opus 途中分再利用不可) 更新済。
- 消費: Sonnet 68 + 3 = 4.22M token、reviewer opus 1 体。申告 350〜500 万の範囲内、Opus 裁決 ≤ 15 は未起動 (不要と判断)。データ変更 0、ゲート GREEN。
- commit のみ、push はユーザー gate (S119 3088ec2 + S120 の 2 コミット未 push)。

## §5 push (ユーザー gate「可以push」)
- `git push origin main`: 5715ef1..0419647 (S119 3088ec2 + S120 0419647)。main = origin/main。
