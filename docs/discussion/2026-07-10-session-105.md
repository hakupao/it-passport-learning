# 2026-07-10 Session 105 — Quiz Phase 2 续批 batch 4 (2019h31h + 2018h30a) + Persist 恒久硬化

启动词: **「Phase 2 续批」** (= S104 What's Next #2: D-137 解説生成 scale、残 21 回)。
Effort: ultracode (xhigh + workflow orchestration)。

## ユーザー gate (AskUserQuestion, session 冒頭)
- **Batch 範囲** = 「2 回: 2019h31h + 2018h30a」(最新優先、シリアル、2 exam/session 上限)。
- **Commit gate** = 「**commit + push 自動**」(S102-S104 は「push は確認」だったが今 session は自動 push 承認)。
- **Persist 恒久対策** = 「**今 session で実施**」(S104 の Persist 有損事故の恒久対策 = LLM Persist → 主 context の決定的書込)。

## ゴール
Phase 2 (解析預生成, D-137) scale batch 第 4 弾 + パイプライン恒久硬化。
per-exam: prep → generate.workflow → **persist (決定的、新方式)** → verify-result → merge → 裁決 (suspect/腐敗 源実読) → stemfix/trfix (必要時) → re-merge → ruleA-prep → ruleA.workflow → (Rule A 指摘是正 → 独立再検証) → 検証 GREEN → commit + push。

## 事前確認 (pre-flight)
- **exam 命名の解決**: S104 note の「次候補 2019h31h / 2019h31a」の **2019h31a は綴り誤り** (存在しない)。命名規則 `h=春期(spring/April)`, `a=秋期(autumn/October)` — 令和遷移で確証 (`2019r01a` = 令和元年**秋**期 = Heisei 後最初の試験)。∴ 残 21 回の最新順 = **2019h31h (2019春) → 2018h30a (2018秋) → 2018h30h (2018春)**。
- **盤面**: 全 29 回中 8 回済 (2019r01a/2020r02o/2021r03/2022r04/2023r05/2024r06/2025r07/2026r08) → 残 21 回 = 全 `h` 系 (2009h21a〜2019h31h)。本 session で 2 回 → 残 19 回。
- **agent 注册表**: 三互異重映射 (writer=`general-purpose` / in-pipeline reviewer=`feature-dev:code-reviewer` / Rule A critic=`pr-review-toolkit:code-reviewer`) を本 session 注册表で存在確認 → 続用 (Rule D/A 保持)。
- **2019h31h prep**: 100 問 / 18 図 [5,12,16,26,34,52,53,58,59,68,71,72,77,78,82,86,96,98] / tr 100/100 / id 連続 q001..q100 ✓ / 図 crop+page 欠落 0。
- **2018h30a prep**: 100 問 / 12 図 [1,5,6,7,15,19,24,27,77,80,85,93] / tr 100/100 / id 連続 q001..q100 ✓ / 図欠落 0。

## §0. Persist 恒久硬化 (S104 What's Next #3、本 session で実施) — ✅ 完了

**背景 (S104 Rule B)**: generate.workflow の Persist フェーズは LLM agent に `generate_result_<exam>.json` を「verbatim」書込させていたが、resume 後の agent が契約違反 — 全 note_jp 空・verdict 脱落の有損コピーを書込 (構造的には妥当、意味的に空洞化)。S104 は task output `result` から手動再構築で回復 + verify-result に語義 sanity check を追加した。

**恒久対策 (S105)**: **LLM を直列化パスから完全排除**。
- **transcript 機構の実証 (probe `wf_a4fcf490-9b6`)**: background workflow の script return 値は、harness が task の output ファイル (`…/tasks/<taskId>.output`) の **`.result` フィールドに verbatim 書込**することを確認 (= S104 が 151KB を回復したのと同じ源)。
- **generate.workflow.mjs 改修**: `phase('Persist')` + LLM agent ブロックを削除。workflow の return 値 (`results: clean` = 全問の untrimmed key_guard + round-1 + notes + verdicts) が権威 generate_result となる。meta.phases から Persist 除去、未使用 `resultPath` 除去。
- **新 `scripts/quiz-phase2-persist.mjs`**: `<task_output_file> <exam_id>` を引数に、output ファイルの `.result` を読み `generate_result_<exam>.json = {exam_id,total,done,results}` を**決定的に書込** (LLM 0)。guard: `.result` が object でない (truncate/wrong file) / exam_id 不一致 / results 空 → 即 fail。
- **note_jp 安全性の確認**: `localizeExplanation` (quizModel.ts:297-302) は `correct/distractors/points/suspect` のみ UI 露出、**note_jp は非表示**。∴ 全問 full note を sidecar に載せても安全 (2019r01a=S104 が既に full note で committed=既定慣例)。
- **単体テスト**: persist.mjs 負例 (exam_id 不一致 guard 発火) + 正例 (合成 output → 正しい generate_result 形式) 両 PASS。verify-result が下流の決定的 gate。

**恒久効果**: 残 19 回 + 本 session 2 回、Persist 有損事故モードを構造的に排除。

## 進行記録

### 1. 2019h31h — generate → 決定的 persist (新方式初実戦) → merge → q100 裁決
- **generate** `wf_53ae6509-f39` (task `wcnv9hj5u`): **100/100・jp PASS 100・tr PASS 94 / CONCERNS 6・suspect 0**。416 agent / 12.5M tok / ~53 分、**error 0・一発完走** (session limit 直撃なし = S103/S104 の pacing 再現、resume 不要)。
- **決定的 persist (S105 新方式、初の実戦投入) = ✅**: task output ファイル (981KB) の `.result` を `quiz-phase2-persist.mjs` が読み `generate_result_2019h31h.json` に書込。**empty-note 0 / missing jp_verdict 0** = S104 有損モードの症状ゼロ。`verify-result` 100/100 PASS (well-formed 全問)。
- **merge**: explained 100/100・missing 0・**SUSPECT 0**・**STEM-CORRUPTION 1 → q100**。
- **q100 裁決 (主 context 源実読 page-41、q052 protocol)**: generate key-guard が choices_jp の cosmetic OCR 腐敗 2 件を捕捉 (答え ウ=ディジタル署名 不変):
  - **ア**: 「05 のログインパスワード」→ 源=「**OS** のログインパスワード」(OS→05 OCR 誤読)。zh/en は既に OS = clean。
  - **エ**: 末尾 junk「> 渦一」= page-41 頁脚/列こぼれ → 源=「…送ってもらう。」で終端。zh/en 既 clean。
  - 是正層: **raw bank choices_jp のみ** (新 `quiz-phase2-stemfix-S105.mjs`、CHOICE_FIXES 2 件、assert-once) → `build-quiz-corpus` → re-merge。**trfix/explfix 不要** (zh/en + 解説 distractor は既に OS/clean、腐敗は内部 note_jp の audit trail にのみ残存 = 非 UI)。
- **invariants (git 確証)**: questions.json diff = **2 行 (q100 ア/エ choice 値)**・correct_answer **0 変更**・quiz_index **不変**・translations **不変**。
- **tr CONCERNS 6** (q024/q026/q038/q063/q090/q094) = repair 後の残 CONCERNS。Rule A で全 5 (plain) + q026 (figure) を独立再核験対象に強制包含。

### 2. Rule A 起動 (ruleA-prep 硬化 + tr-CONCERNS 強制)
- **ruleA-prep 硬化 (S105)**: `take(figures, ~N/2)` は figures>N/2 で図を黙って落とす欠陥 (18 図 @ N=24 で 12 図しか採らない) → **全図 + 全 stem-corruption を強制** (N は plain の floor に降格) + 新 `forceNums` CLI arg (tr-CONCERNS 等を強制包含)。
- **サンプル N=28**: 全 18 図 + q100 (stem-corrupt) + tr-CONCERNS 5 (q024/q038/q063/q090/q094) + plain 4 (q001/q046/q073/q099)。
- **Rule A** `wf_09ab0606-e6d` (task `whwf1oa9n`): N=28 = **accurate 28/28・none 21/low 7・0 med/high・keyGuardMismatch 0・independent_answer == stored key 28/28 (bad key 0)**。
- **tr-CONCERNS 5 (q024/q038/q063/q090/q094) 再核験 = 全 accurate** → CONCERNS は benign 確定 (強制包含の判断が的中)。
- **low 7 = 全て非 answer-affecting → backlog/不動**: zh 本土 polish 4 (q026 增长率/q072 妥当性/q090 耐故障/q094 rootkit gloss) + en 「」括弧 1 (q071、JSON_SAFE 由来 systematic) + q052 swap-vs-cyclic 表現 (判定正) + q078 note_jp meta artifact (非UI)。
- **是正**: q100 以外なし (medium/high 0)。

### 3. 2019h31h — 検証 GREEN → commit
- tsc 0 / eslint 0err (既存 warning 1=tTerm) / **vitest 463** (2 skip) / build exit 0 / **nft IPA-source leak 0** (exams/sources/question_bank/syllabus=0、explanations sidecar traced=9=8既存+2019h31h)。
- 証拠: `evidence/phase5/stage_06_quiz_phase2/{adjudication_S105.md, ruleA_result_S105_2019h31h.json}`。
- **checkpoint commit + push** (auto gate)。

## What's Next
- (session 末に記入)
