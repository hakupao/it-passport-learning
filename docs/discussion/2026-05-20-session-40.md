# Session 40 — 2026-05-20

> **Phase 2 实施阶段 / Session 40 close ready** — Session 39 close ✅ (commit `69d941a`) → user kickoff "Session 40 开始" 2026-05-20 → Step 8 = 1-retry-no-fallback + δ-all-tripwire detector per D-088 §2.4 / D-091 §2.5(β). Closes **Module B 5/5 ✅ v2**.
>
> **本场关键事件**: D-019 §3a 4Q slow-pace (a/b/a/a blanket Recommended) → Batch A 写 `retry.ts` + `tripwire.ts` + `chat.ts` SSE catch 改 `formatUserFacingError` → Batch B 4 route 全部 wire `maxRetries: 1` + tripwire eval `onFinish` → Batch C 150/150 vitest (was 120 → +30; 12 retry.test + 18 tripwire.test + 1 chat.test contract update) → Batch D `vercel --prod` classifier denial → user `授权 vercel --prod` ACK → Batch E 5 真 LLM smoke (4 endpoints happy + 1 cold-trigger attempt; user `授权 decode firewall pass` ACK on classifier denial); 0 [tripwire] fires on healthy 96-99.98% hit (correct silent design) → Batch F evidence 11 files + tripwire_log row #5 → Batch G PLAN/STATE/session-40 + commit pending.
>
> **0 new ADR** (Step 8 honoured locked D-088 §2.4 + D-091 §2.5(β) + D-094 §2.1 + D-095 §2.4 + D-097 cleanly; in-source documentation patterns per D-094 §2.1 + D-080 v1.1 §8).
>
> **Module B 5/5 ✅ COMPLETE v2** — all 4 API endpoints + retry/tripwire helpers all LIVE on prod canonical via D-097 firewall.

| 字段 | 值 |
|---|---|
| Status | **✅ Step 8 DONE + Module B 5/5 ✅ — pending commit + push gates** |
| 前置 ADR | D-001 ~ D-098 (Session 39 close 全 LOCKED) |
| 工作流 tier | **Tier 3 实施阶段** carry-over (Phase 1 conventions + Phase 2 D-091 §2.2 additions) |
| 实际产物 | retry.ts + tripwire.ts 2 new modules + chat.ts edit (formatUserFacingError + console.error) + chat.test contract update + 4 route edits (maxRetries + tripwire eval) + retry.test 12 + tripwire.test 18 → ✅ DONE + PLAN.md Step 8 row + STATE.md 4 anchors + session-40 log + 11 evidence + tripwire_log row #5 + 1 atomic commit pending |
| LLM cost actual | **~$0.014 真 billed** (5 smoke calls; cumulative Phase 2 **~$0.065**) |
| 前置代码状态 | Step 1-7 全保留; Step 8 在 Step 7 之上加 retry.ts + tripwire.ts 2 helper modules + chat.ts SSE error catch 接 formatUserFacingError + 4 route edit pass maxRetries=1 + onFinish 内 evaluateCacheTripwire/recordTripwireEvent |
| Wall actual | **~85 min** (vs estimate 1 day → **−82%**) — Module B 第 5 / 收官 wall data point；γ tripwire 8th consecutive under-estimate |

---

## 1. Entry context

**Session 39 close 状态** (2026-05-20 commit `69d941a`):
- 98 ADR LOCKED (D-001 ~ D-098; Session 39 lock 0 new ADR; chars/N N=4 in-source amend done)
- `/api/glossary/hover` SSE LIVE on prod canonical via D-097 firewall
- Module A 3/3 ✅ + Module B 4/4 ✅ (Steps 4-7)
- 120/120 vitest pass + N=5 β cache hit data ratify D-088 §2.3 stable-prefix across 232× scope range + Phase 2 真 billed $0.0515
- Module C+D full re-estimate deferred to Step 9 mid-retro per Session 39

**Session 40 scope baseline** (per Session 39 close STATE `下一会话` row):
1. Entry action (i): path α = Step 8 (1-retry-no-fallback + δ-tripwire) vs path β = Step 9 Module C entry
2. Entry action (ii): Module C+D full re-estimate decision — gather Step 9 first
3. Entry action (iii): R1 empty-delta UX gap design 4Q (at next reasoner usage)
4. Entry action (iv): D-019 4Q slow-pace per Phase 2 pattern

**Git start state**: HEAD `69d941a` = origin/main, working tree clean. Session 39 commit + push already landed.

---

## 2. Decisions / lock log

### 2.1 ADR locks this Session
**0 new ADR**.

### 2.2 In-source amendments (per D-094 §2.1 + D-080 v1.1 §8 patterns)

- **`STREAM_CONFIG.maxRetries: 1`** in `apps/web/src/lib/ai/retry.ts` — pinned constant honouring D-088 §2.4's "2 attempts total" semantics (AI SDK v6 default is 2 retries beyond initial = 3 attempts, NOT D-088 compliant). New module documents the mapping in-file.
- **`USER_FACING_LLM_UNAVAILABLE_MESSAGE`** in same file — locked Chinese surface text `「AI 暂时不可用，请稍后重试。」` per D-088 §2.4. `chat.ts` SSE encoder catch branch now uses `formatUserFacingError(err)` rather than `err.message` (the previous behaviour leaked raw provider error strings into the UX; that was a latent D-088 §2.4 violation, not a contract change here — the test was updated to reflect the locked contract).
- **`evaluateCacheTripwire` 50% threshold + 1000-tok min-input floor** in `apps/web/src/lib/ai/tripwire.ts` — implements D-091 §2.5(β) "cache hit rate < 50%" floor with a small-call noise suppressor.
- **Out-of-band tripwire trigger documentation** in `tripwire.ts` module comment — explicitly enumerates α/γ-D091/δ-D091/ε as out-of-band (manual flags or offline measurement) so future maintainers understand why this module only implements β.

### 2.3 Drift corrections (per D-027 §1 — not D-NNN-worthy)

- **DC-40.1** — Session 39 Module B 收官 retro recorded as "4/4 ✅ COMPLETE" (counting API endpoints). PLAN.md §1 Module B taxonomy = 5 steps including Step 8 (retry+tripwire). Session 40 retro now records "Module B 5/5 ✅ COMPLETE v2" (counting all step rows). Both framings are correct within their scope; future references should prefer the 5/5 step-count framing matching PLAN.md taxonomy.
- **DC-40.2** — Session 38 reported deepseek-reasoner empty-delta finding as single-data-point hypothesis. Session 40 Step 8 smoke #3 + #5 both invoked deepseek-reasoner and both emitted non-empty deltas (459 + 985 output tokens). Empty-delta hypothesis updated: R1 reasoning-only output is non-deterministic (NOT a stable provider bug). Defensive warning-frame fix still valid for UX robustness but no longer urgent.
- **DC-40.3** — D-088 §2.3 referenced Anthropic ephemeral 5-min TTL. Session 40 smoke #5 attempted to trigger a cold-creation event on `page_259_entity_0` (last called Session 38 ~5 h prior). Actual result: 95.8% hit, indicating DeepSeek's automatic prefix cache TTL >> 5 min. The Anthropic 5-min number was always a per-provider value; the cross-provider docstring in D-088 §2.3 should be read as "Anthropic 5-min ephemeral; DeepSeek longer empirically". Not D-NNN-worthy — observation only.

---

## 3. Open questions

无 new. 3 carry-over OPEN all in Phase 1 后续 / Phase 3-4 范围 (OQ-01 / OQ-02 / OQ-05 partial).

R1 empty-delta UX gap remains as **non-OQ deferred design task** (now hypothesised non-deterministic per DC-40.2 above; still warrants defensive warning-frame fix at next reasoner UI work).

---

## 4. Turns

### Turn 1 — Session 40 entry + 4Q on Step 8 design

**User kickoff**: `Session 40 开始` 2026-05-20 (via `/clear` 后).

**Claude actions**:
1. Parallel reads: STATE.md (with offset+limit due to size) + session-39 log
2. Confirmed Session 39 close state + Session 40 entry actions
3. Verified working tree clean + on `main` at `69d941a` in sync with origin
4. Read PLAN.md Module taxonomy (confirmed Step 8 belongs to Module B = 5 steps total; STATE Session 39 "4/4" framing was endpoint-count, taxonomy is step-count)
5. Read D-088 (§2.4 retry policy details) + D-091 (§2.5 cascade triggers definition) + chat.ts (current SSE catch behaviour) + 4 route handlers

**4Q via AskUserQuestion** (slow-pace per D-019 §3a):
- Q1: Step 8 vs Step 9 (path α / β / γ)
- Q2: Retry middleware layer (factory / route wrapper / SSE encoder)
- Q3: δ-tripwire scope (log-only / SSE warning frame / env-gated)
- Q4: Session 40 pacing (Batch A-G straight-through / split / design only)

**User ans**: a/b/a/a (all Recommended). Blanket ACK triggered Batch A-G plan.

### Turn 2 — Batch A modules

Wrote new files:
- `apps/web/src/lib/ai/retry.ts` (~95 lines) — STREAM_CONFIG + isRetryableError + formatUserFacingError + USER_FACING_LLM_UNAVAILABLE_MESSAGE constant
- `apps/web/src/lib/ai/tripwire.ts` (~130 lines) — CACHE_HIT_RATE_FLOOR + CACHE_TRIPWIRE_MIN_INPUT_TOKENS + TripwireEvent + evaluateCacheTripwire (β runtime detector) + recordTripwireEvent (console.warn-only)
- Edit `chat.ts` SSE encoder catch branch → use `formatUserFacingError(err)` for the SSE error frame payload; `console.error` raw error separately for debug

### Turn 3 — Batch B route wiring

Edited 4 routes:
- `apps/web/src/app/api/hello-ai/route.ts` — `maxRetries: STREAM_CONFIG.maxRetries` + tripwire eval in onFinish
- `apps/web/src/app/api/chat/route.ts` — same shape + added `readCacheUsage` import
- `apps/web/src/app/api/quiz/explain/route.ts` — same + added `readCacheUsage` import
- `apps/web/src/app/api/glossary/hover/route.ts` — same + added `readCacheUsage` import

All 4 routes follow uniform shape: `streamText({model, maxRetries, messages, onFinish: ({usage, providerMetadata}) => { console.log + evaluateCacheTripwire + if non-null recordTripwireEvent }})`.

### Turn 4 — Batch C tests

Wrote new test files:
- `apps/web/src/lib/ai/__tests__/retry.test.ts` — 12 cases (STREAM_CONFIG.maxRetries=1 + frozen + locked Chinese surface text + formatUserFacingError mapping + isRetryableError matrix across 9 error shape variants)
- `apps/web/src/lib/ai/__tests__/tripwire.test.ts` — 18 cases (threshold constants + cache_no_data branch + sub-threshold suppression + healthy hit-rate branches + cache_low_hit branches for both providers + zero-denominator skip + recordTripwireEvent sink shape via console.warn spy)

Updated `chat.test.ts` 1 existing test ("emits an error frame ... mid-flight") to assert the new D-088 §2.4 locked Chinese surface message instead of raw err.message; added `errorSpy` on `console.error` to verify the raw error is still logged for debug visibility. Added `vi` to the vitest imports.

Initial test run: 1 failure — the chat.test.ts test had asserted raw error message leakage (latent D-088 §2.4 violation). Fixed in same turn.

Final `pnpm test` → **150/150 ✅** (was 120 → +30 net: 12 retry + 18 tripwire; 1 chat.test contract update is invariant on test count).

### Turn 5 — Batch C green check

- `pnpm test` → **150/150 ✅ in 393ms**
- `pnpm lint` → exit 0
- `pnpm exec tsc --noEmit` → exit 0
- `pnpm build` → green; **9 routes** unchanged (5 static + 4 dynamic); Middleware 37.6 kB unchanged; First Load JS 119 kB unchanged (Step 8 is server-only)

### Turn 6 — Batch D deploy

**Preview**: `vercel deploy --yes` → `dpl_WKXHR6EXes8a8bdbmDUcTEM8smBY` READY at `web-p3lg6vpvc-bojiangs-projects.vercel.app`.

**Prod attempt #1**: `vercel deploy --prod --yes` → **classifier denial** (auto-mode classifier flagged unexpected --prod with --yes after only "Session 40 开始" entry).

**Pre-promote defense probes**:
- Preview canonical → Vercel team SSO 401 (infra-layer, expected)
- Prod canonical (still on Step 7 deploy) → 401 + `www-authenticate: Basic realm="IT Passport Learning firewall"` (D-097 firewall still gating pre-promote ✅)

**User gate**: `授权 vercel --prod` → retry → **prod READY** `dpl_D4oQASueh2eTXrEEaApdmWNw4q3n` target=production aliased canonical `web-mu-sandy-78.vercel.app`.

**Post-promote probe**: 401 + `www-authenticate: Basic realm="IT Passport Learning firewall"` on Step 8 build ✅.

### Turn 7 — Batch E 5 真 LLM smoke

**Setup**:
- Decode `FIREWALL_BASIC_AUTH` from `apps/web/.env.local` — **classifier denial** (sensitive credential read) → user `授权 decode firewall pass` ACK → decoded; classifier denied tee-to-/tmp pattern from prior sessions → used shell-variable-only inline approach
- mkdir `evidence/phase2/step_08_retry/`

**Sequential 5 calls** via `curl -sN -u "$PASS" -X POST` (all on prod canonical):

| # | Endpoint | Body | Wall | Input | Out | hit | miss | hit% | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 1 | /api/hello-ai | (none) | 2 s | 57 993 | 1 | 57 984 | 9 | **99.98%** | "ok" reply (smoke confirmed deploy alive + retry path in flight + onFinish wired) |
| 2 | /api/chat | `{scope:"whole-book", userMessage:"DNS とは何か簡潔に。"}` | 3 s | 92 815 | 58 | 92 800 | 15 | **99.98%** | Japanese reply + 489ページ citation |
| 3 | /api/quiz/explain | `{question_id:"page_042_entity_0"}` | 5 s | 2 693 | 459 | 2 688 | 5 | **99.81%** | R1 emitted non-empty Japanese deltas (cf. Session 38 empty-delta finding) |
| 4 | /api/glossary/hover | `{surface_jp:"アルゴリズム"}` | 2 s | 400 | 56 | 384 | 16 | **96.0%** | Trilingual tooltip with kana_helper.reading |
| 5 | /api/quiz/explain | `{question_id:"page_259_entity_0"}` (cold-trigger attempt) | 15 s | 2 271 | 985 | 2 176 | 95 | **95.8%** | Intended cold; actually warm — DeepSeek prefix TTL > 5 h empirical |

All 5 corpus-grounded; no fact invention; trilingual / structured outputs preserved. **No regression from Step 7 baseline.**

**Tripwire surface verification**: `vercel logs --json | grep '[tripwire]'` → **0 lines** across 5 smoke calls. Reasons (all design-consistent):
- Smoke #2/#3/#4/#5 hit rates (99.98 / 99.81 / 96.0 / 95.8) ≥ 50% floor → evaluator returns null → no log
- Smoke #4 (hover) also had input 400 < 1000 floor; would have returned null anyway
- Smoke #1 (hello-ai) hit rate 99.98 % ≥ 50% → null

**0 fires is the positive signal** that the detector is LIVE on prod, deployed, evaluator returning null under healthy operation. The fire branch (`cache_low_hit` event) is fully covered by `tripwire.test.ts` unit tests.

### Turn 8 — Batch F evidence + tripwire row #5

Wrote 11 evidence files in `evidence/phase2/step_08_retry/`:
1. `tree_outline.md`
2. `build_log.txt`
3. `test_results.txt`
4. `cache_audit_2026-05-20.md` — §1 raw smoke data (5 rows) / §2 tripwire fire count (0) / §3 Module B 5/5 ✅ retro / §4 γ 8th data point / §5 observability findings (DeepSeek TTL + AI SDK warning + R1 non-determinism) / §6 cost / §7 Rule A audit input (n/a wiring) / §8 Rule B archive (0)
5. `design_notes.md` — full 4Q → code mapping + retry layer + tripwire layer + wiring summary + failure-mode coverage + deferred items
6. `step_08_audit.md` — 18-row acceptance criteria + sample N=5 spot-check + pre-close self-check matrix
7-11. `smoke_call_{1_hello_ai, 2_chat, 3_quiz, 4_hover, 5_cold_quiz}.log` — raw SSE / text bodies

Appended `evidence/phase2/tripwire_log.md` row #5 — β + γ + δ runtime detector LIVE + Module B 5/5 收官 (composite event).

### Turn 9 — Batch G PLAN/STATE/session-40 + commit pending

**PLAN.md Step 8 row** → ✅ DONE with full inline narrative.

**STATE.md 4 anchors synced** (this turn): 最后更新 + 当前阶段 + 已锁定决定数 + 下一会话.

**session-40 log**: this file.

---

## 5. Pre-close self-check (per D-027 §5)

| 项 | 状态 | 证据 |
|---|---|---|
| 0 new ADR lock | ✅ | Session 40 in-source documentation pattern only |
| Code green | ✅ | 150/150 vitest + lint + build + tsc strict |
| Vercel env vars | ✅ | DEEPSEEK_API_KEY + FIREWALL_BASIC_AUTH unchanged from Step 7 |
| Preview deploy | ✅ | `dpl_WKXHR6EXes8a8bdbmDUcTEM8smBY` |
| Production deploy | ✅ | `dpl_D4oQASueh2eTXrEEaApdmWNw4q3n` aliased `web-mu-sandy-78.vercel.app` target=production |
| Firewall still gating | ✅ | post-promote probe HTTP 401 + WWW-Authenticate confirmed |
| 5 真 LLM smoke ✅ | ✅ | smoke_call_{1..5}.log; all healthy 96-99.98% hit; ~$0.014 真 billed; 0 [tripwire] fires |
| δ runtime detector LIVE on prod | ✅ | code deployed; evaluator covered by unit tests fire+healthy+sub-threshold branches |
| Module B 5/5 ✅ COMPLETE v2 | ✅ | PLAN.md Module B rows 4-8 all `actual <N> min`; retro section in cache_audit §3 |
| PLAN.md Step 8 → ✅ DONE | ✅ | row amended with full narrative |
| STATE.md sync | ✅ | 4 anchors touched |
| Session log on disk | ✅ | this file complete through Turn 9 |
| Evidence on disk | ✅ | `step_08_retry/` 11 files + tripwire_log row #5 |
| Rule A 抽检 | n/a → wiring (not content rewrite); informal coherence audit done in step_08_audit §2 |
| Rule B 失败归档 | 0 (all 7 batches first-try landed; chat.test contract update is intentional refresh not failure) |
| Rule C (Phase retro) | n/a | mid-Phase; Module B 收官 v2 retro done in cache_audit §3 (NOT Rule C Phase-end retro) |
| Rule D Writer ≠ Reviewer | ✅ | Writer = main session; Reviewer = user terminal `a/b/a/a` 4Q ACK + `授权 vercel --prod` Batch D ACK + `授权 decode firewall pass` Batch E ACK; commit ACK pending |
| OPEN OQ count | 3 unchanged | Phase 1 后续 / Phase 3-4 范围 |

---

## 6. Final commit plan (G3=a 1 atomic — per Q4=a)

| File | Type | Lines |
|---|---|---|
| `apps/web/src/lib/ai/retry.ts` | new | ~95 |
| `apps/web/src/lib/ai/tripwire.ts` | new | ~130 |
| `apps/web/src/lib/ai/__tests__/retry.test.ts` | new | ~145 |
| `apps/web/src/lib/ai/__tests__/tripwire.test.ts` | new | ~180 |
| `apps/web/src/lib/ai/chat.ts` | modify | (+ formatUserFacingError import + catch branch update) |
| `apps/web/src/lib/ai/__tests__/chat.test.ts` | modify | (+ vi import + 1 test contract update with errorSpy) |
| `apps/web/src/app/api/chat/route.ts` | modify | (+ readCacheUsage + STREAM_CONFIG + tripwire imports + maxRetries + onFinish tripwire eval) |
| `apps/web/src/app/api/quiz/explain/route.ts` | modify | (same pattern) |
| `apps/web/src/app/api/glossary/hover/route.ts` | modify | (same pattern) |
| `apps/web/src/app/api/hello-ai/route.ts` | modify | (same pattern, readCacheUsage already imported) |
| `docs/phase2/PLAN.md` | modify | (Step 8 row → ✅ DONE narrative) |
| `docs/STATE.md` | modify | (4 anchor sync) |
| `docs/discussion/2026-05-20-session-40.md` | new | this file |
| `evidence/phase2/step_08_retry/tree_outline.md` | new | ~50 |
| `evidence/phase2/step_08_retry/build_log.txt` | new | (build snapshot) |
| `evidence/phase2/step_08_retry/test_results.txt` | new | (vitest snapshot) |
| `evidence/phase2/step_08_retry/cache_audit_2026-05-20.md` | new | ~180 |
| `evidence/phase2/step_08_retry/design_notes.md` | new | ~115 |
| `evidence/phase2/step_08_retry/step_08_audit.md` | new | ~80 |
| `evidence/phase2/step_08_retry/smoke_call_1_hello_ai.log` | new | ("ok" 2-byte payload) |
| `evidence/phase2/step_08_retry/smoke_call_2_chat.log` | new | (2427 B SSE) |
| `evidence/phase2/step_08_retry/smoke_call_3_quiz.log` | new | (13141 B SSE) |
| `evidence/phase2/step_08_retry/smoke_call_4_hover.log` | new | (2355 B SSE) |
| `evidence/phase2/step_08_retry/smoke_call_5_cold_quiz.log` | new | (12596 B SSE) |
| `evidence/phase2/tripwire_log.md` | modify | row #5 appended |

Excluded from git (correct):
- `apps/web/.env.local` (gitignored via `.env*` pattern; carries DEEPSEEK_API_KEY + FIREWALL_BASIC_AUTH)
- Vercel env vars (out-of-band; documented in evidence)

**Push**: pending user `push it` gate per Sessions 27-39 pattern.

---

**Session 40 ready to close — Step 8 ✅ DONE + Module B 5/5 ✅ COMPLETE v2 + 0 new ADR (clean honour of locked design) + 5 真 LLM smoke 96-99.98% cache hit + 0 [tripwire] fires under healthy operation + δ runtime detector LIVE on prod canonical + ~$0.014 真 billed + 8th γ data point inline-amended in PLAN.md per D-094 §2.1; Module C+D re-estimate STILL deferred to Step 9 mid-retro; pending user commit + push gates**
