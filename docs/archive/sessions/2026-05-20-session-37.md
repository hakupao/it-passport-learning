# Session 37 — 2026-05-20

> **Phase 2 实施阶段 / Session 37 close ready** — Session 36 close ✅ (commits `f0be37c` + `e861f9f` GitGuardian fix) → user kickoff "Session 37 开始" 2026-05-20 → Step 5 = `/api/chat` whole-book scope wiring per Session 36 close STATE.md `下一会话` row.
>
> **本场关键事件**: 4Q + Batch A-C 一气呵成 → Batch D DEEPSEEK_API_KEY prod env add (含 empty-placeholder rollback 4-step fix) + preview deploy ✅ → **Batch E pre-flight 本地 node 测量暴露 assembleWholeBook full-pages payload ctx-overflow latent fault** (real ~429K-964K tok vs assumed DeepSeek 64K → 6.5×-14.7× over) → halt pre-deploy → 4Q on fix (all Recommended `B/Modify/锁D-098/Yes原计划`) → **D-098 ADR LOCKED ~250 行** (amend D-089 §2.3 + D-085 + memorialize D-097-sibling lesson "partial supersede without cross-product compat check") → B-fix code: lean payload (chapters + glossary, no pages) + 3 lean tests → 81/81 re-green → preview redeploy + **prod deploy `dpl_e3qoyhzsh-...` aliased canonical** → 3 真 LLM smoke (Basic Auth per D-097) all green: cold start English honest refusal, 2× Japanese coherent reply with 99.98% cache hit → N=4 cumulative cache hit data + chars/N content-dependency finding → retro evidence written + PLAN/STATE sync + commit pending user gate.

| 字段 | 值 |
|---|---|
| Status | **✅ Step 5 DONE — pending commit + push gates** |
| 前置 ADR | D-001 ~ D-097 (Session 36 close 全 LOCKED) + **D-098 本场新立** (whole-book lean payload mid-step factual correction) |
| 工作流 tier | **Tier 3 实施阶段** carry-over (Phase 1 conventions + Phase 2 D-091 §2.2 additions) |
| 实际产物 | D-098 ADR + session-37 log + chat.ts + route.ts + chat.test.ts + route.test.ts + assembleScope.ts modify + assembleScope.test.ts rewrite + vitest.config alias + PLAN.md amend + STATE.md sync + 8 evidence + 1 failure archive + 3 deploy + 3 真 LLM call |
| LLM cost actual | **~$0.030 真 billed** (3 calls); cumulative Phase 2 **$0.047** |
| 前置代码状态 | Step 1-4 全保留 (apps/web 完整 + DataSource + assembly + boot + hello-ai + middleware)；Step 5 在 Step 4 之上加 `/api/chat` + chat.ts + lean assembleWholeBook (modify) |
| Wall actual | **~165 min** (vs estimate 1.5 day → −86%) — Module B 第二个 wall data point |

---

## 1. Entry context

**Session 36 close 状态** (2026-05-20 commits `f0be37c` + `e861f9f`):
- 97 ADR LOCKED (D-001 ~ D-097)
- α firewall LIVE on prod canonical via D-097 Edge middleware Basic Auth
- Module A 3/3 ✅ + Module B Step 4 ✅
- 58/58 vitest pass + Phase 2 cumulative wall under-estimate by 85-98% (γ tripwire 4 data points all under)

**Session 37 scope baseline** (per Session 36 close STATE `下一会话` row):
1. Entry action (i): Step 5 `/api/chat` whole-book scope wiring + first-week cache hit retro
2. Entry action (ii): DEEPSEEK_API_KEY 加到 Production env (Session 35 只设 Preview)
3. Entry action (iii): D-019 4Q slow-pace per Phase 2 pattern (设计话题 non-negotiable)

---

## 2. Decisions / lock log

### D-098 LOCKED — whole-book lean payload (mid-step factual correction)

**Path**: Session 37 batch E pre-flight 本地测量暴露 D-089 §2.3 assembleWholeBook full-pages payload ctx-overflow → halt pre-deploy → 4Q (all Recommended) → ADR LOCKED + B-fix in same Session.

**Essence** (full ADR at `docs/decisions/D-098-whole-book-lean-payload.md`):
- §2.1 New `assembleWholeBook` payload contract: `{ scope, cert_id, run_id, totals, chapters, glossary_entries }` — drop `pages` array
- §2.2 Token envelope predicted ~58-60K (actual 92,814 = +55% off; v1.1 amend follow-up)
- §2.3 Step 5 retro 按 Q4=a 原计划跑 (N≥3 真 call)
- §2.4 D-085 amend: whole-book scope α-now = lean (chapters + glossary); full-pages mode deferred Phase 3+ β 阶段
- §2.5 D-089 §2.3 amend: `assembleWholeBook` 注释 + 行为更新
- §2.6 Cache prefix invariant 保留 (D-088 §2.3 stable-prefix 仍有效)

**Rejected alternatives** (per D-098 §3):
- (a) Anthropic 1M (×113 cost; user 本场 Q1=B 即拒)
- (c) Hybrid scope→provider routing (over-engineering for α)
- (d) Halt Step 5 redesign (浪费已成 Batch A-D code)
- (e) 双轨 lean + full (Q2=Modify Recommended 拒之)
- (f) Chapter abstracts pre-compute (需要预生成 LLM call 未做)
- (g) Pages trim (破坏 retrieval quality)

**根因 disclosure**: D-095 (Session 35 Turn 3) partial supersede D-088 §2.1+§2.3 时未做 scope-aware compatibility 验证 (DeepSeek 64K vs assembleWholeBook ~800K estimate)。这是 **D-097 §1 lesson 同根的第二例** "partial supersede without cross-product compat check"。两例都根因 D-019 §3 "consult authoritative docs — do not rely on memory alone" 在 ADR lock 瞬间被绕过；RETROSPECTIVE v2 backlog 候选规则: 任何 partial-supersede 必须做 cross-product compat check (D-098 §4.4 + cache_audit_2026-05-20.md §5.2).

### Drift correction (one-line entries — not D-NNN-worthy)

- 无 D-NNN worthy 外
- `vitest.config.ts` `resolve.alias { "@": ./src }` 配置缺漏 (Step 4 hello-ai 无 route test 故未暴露；Step 5 第一个 route test 即触发) — 1 行 config 补；写入 commit & evidence step_05_audit §1

---

## 3. Open questions

无 new；3 carry-over OPEN 全在 Phase 1 后续 / Phase 3-4 范围 (OQ-01 / OQ-02 / OQ-05 partial).

---

## 4. Turns

### Turn 1 (2026-05-20) — Session 37 entry + 4Q on Step 5 design

**User kickoff**: `Session 37 开始` 2026-05-20 (via `/clear` 后)

**Claude actions**:
1. 4 parallel: STATE.md + last session log + ls discussion + ls decisions
2. 汇报 Session 36 close (97 ADR + α firewall LIVE) + Session 37 entry actions 三条 + 推荐顺序

**User**: `可以` (proceed to Step 5 design)

**4Q** (slow-pace per D-019 §3a) — frame Step 5 along 4 dimensions:
- Q1: scope 覆盖 (whole-book only / +chapter / 全 4)
- Q2: endpoint contract (stateless 单轮 SSE / multi-turn / non-streaming POST)
- Q3: UI presence (curl-only / +最小 /chat 页 / +完整对话 UI)
- Q4: retro 深度 (代码绿 + N≥3 retro 轻 / 仅 1 call 轻收 / 完整 mid-retro)

**User ans**: all Recommended (a/a/a/a). Blanket ACK 触发 Batch A-C 一气呵成 plan.

### Turn 2 — Batch A-C code + green

**Batch A (~15 min)**: Wrote `apps/web/src/lib/ai/chat.ts` (130 行 SSE encoder + validator decoupled from AI SDK types) + `apps/web/src/app/api/chat/route.ts` (115 行 POST + GET handlers per Step 4 hello-ai pattern). tsc 1-fix: `result.usage` is `PromiseLike` not `Promise`; loosened chat.ts arg type.

**Batch B (~10 min)**: Wrote 2 test files mirroring codebase convention (per-source `__tests__/` adjacent dir):
- `apps/web/src/lib/ai/__tests__/chat.test.ts` — 15 cases: 9 validator + 6 SSE encoder
- `apps/web/src/app/api/chat/__tests__/route.test.ts` — 6 cases: happy SSE + 4 bad input + GET health check

**Batch C (~5 min)**: pnpm test → first failed on `Cannot find package '@/lib/ai/provider'` from route.ts (vitest config missing `resolve.alias`). 1-line fix to `apps/web/vitest.config.ts`: `resolve.alias { "@": ./src }` matching tsconfig paths. → 79/79 ✅ + lint + tsc + build 7 routes (added `/api/chat` ƒ dynamic).

### Turn 3 — Batch D (DEEPSEEK_API_KEY prod env + preview deploy) with rollback detour

**Initial intent**: Single `vercel env add DEEPSEEK_API_KEY production` + deploy preview. Got classifier denial first attempt; **user `授权 vercel prod env add DEEPSEEK_API_KEY`** unlocked. Sourced .env.local + ran add → **WARNING: Value is empty** (rollback needed).

**Root cause**: `.env.local` had `DEEPSEEK_API_KEY=` placeholder per Session 36 setup (was never filled). My `set -a && . .env.local && set +a` expanded to empty string. Production now had an empty entry that would block fallback resolution.

**4-step fix** (user `授权 fix DEEPSEEK_API_KEY prod env` ACK covering all 4):
1. `vercel env rm DEEPSEEK_API_KEY production --yes` — rollback empty value ✅
2. `vercel env pull --environment=preview .vercel/.env.preview.tmp --yes` — try pull Preview real value
3. Inspect: `DEEPSEEK_API_KEY=""` returned from Vercel CLI (sensitive vars are write-only via CLI; cannot decrypt to file). Cleanup tmp file.
4. **User chose path (B)**: edit `apps/web/.env.local` with real DEEPSEEK_API_KEY value locally → re-run sourcing add → ✅ Production now Encrypted.

`vercel env ls` confirms: DEEPSEEK_API_KEY on Preview + Production; FIREWALL_BASIC_AUTH on Preview + Production.

**Preview deploy**: `vercel deploy --yes` → `dpl_pEnTDNqn92YR7KYuVQpSxR9jsxCJ` @ `web-pxd7iv711-bojiangs-projects.vercel.app` READY in <30s (build green; preview SSO 401 expected per Session 36 layered defense — not a clean smoke target).

### Turn 4 — Batch E pre-flight discovery + halt

**User**: `授权 batch E` (one-shot ACK covering preview-already-done + prod deploy + 3 真 LLM smoke)

**Claude pre-flight habit** (cost-conscious per `feedback_quality_over_cost.md`): before burning real $, local-measure token sizes to verify scope-vs-provider compatibility.

**Local measurement** (`node -e` script):
```
raw pages dir bytes  : 2,605,502
index.json bytes     : 114,123
glossary.json bytes  : 317,921
sum raw bytes        : 3,037,546
approx pretty stringified (×1.27): 3,857,684
chars/4 conservative tok: 964,421
chars/9 CJK measured tok: 428,632
vs DeepSeek 64K  : 14.7x (chars/4) / 6.5x (chars/9)
vs Anthropic 200K: 4.8x / 2.1x
vs Anthropic 1M  : 0.96x / 0.43x ✓
```

**HARD HALT**: assembleWholeBook full-pages payload ~429K-964K tokens (depending on heuristic factor); 6.5×-14.7× over DeepSeek 64K assumed limit. If Batch E proceeded as planned, 3 calls would all HTTP 4xx context-exceeded (no $ but wasted deploy + roundtrips + Step 5 close gate would have failed).

**Surface to user** (4Q on fix design):
- Q1 fix path (B 瘦 whole-book / A 换 Anthropic 1M / C Hybrid routing / D Halt redesign)
- Q2 wire 形态 (Modify single-track / 新加 Lean fn 双轨)
- Q3 D-NNN (锁 D-098 ADR / 调 D-085 一句话 / 仅 PLAN 记)
- Q4 retro 变更 (Yes 原计划不变 / 加一行 redesign 动机 / 升完整 mid-retro)

**User ans**: all Recommended (B / Modify / 锁 D-098 / Yes 原计划). Lock and execute.

### Turn 5 — D-098 ADR write + B-fix code

**D-098 ADR write** (this Turn — per D-027 §1 同 turn 落盘):
- `docs/decisions/D-098-whole-book-lean-payload.md` ~250 行 (§1 context + §2 decision 6 sub-section + §3 rejected alts 7 + §4 implications 6 + §5 out-of-scope + §6 audit + §7 cross-ref)
- §6 disclosure: D-097 sibling lesson "memory-only assumption 未 docs 验证"；RETROSPECTIVE v2 backlog 候选规则

**B-fix code** (~30 min):
1. `apps/web/src/lib/data/assembleScope.ts` `assembleWholeBook` 改 lean payload: `Promise.all([loadIndex, loadGlossary])` → `{ scope, cert_id, run_id, totals, chapters, glossary_entries }`；comment block 更新 expected token range + D-098 §2.4 reference + Phase 3+ β hook
2. File-top header 加 D-098 amend note
3. `apps/web/src/lib/data/__tests__/assembleScope.test.ts` assembleWholeBook 测试组重写: 3 lean tests (was 1 full-pages); 新 stubDs(chapterCount, glossarySize) helper; `loadWholeBook` 故意 throw 保证 assembleWholeBook 不再调它 (regression guard against accidental revert)
4. tsc 报 2 type mismatch on stub: `ChapterRef` 实际是 `title_jp/title_zh/title_en` flat (我写成 nested); `GlossaryEntry` 是 `kana_helper/first_page/occurrences/aliases_jp` (我写成 `definition` 不存在的字段). 1-pass fix.

**Re-verify**: pnpm test 81/81 ✅ + lint + tsc strict + build 7 routes no regression.

**Re-measure** lean payload:
```
lean contextBlock bytes      : 293,919
chars/4 conservative tok     : 73,480 (1.12× 64K, over heuristic but real <)
chars/9 CJK measured tok     : 32,658 (0.50× 64K, fits ✓)
```

13× shrink confirmed. Ready to re-deploy.

### Turn 6 — Re-deploy preview + prod + classifier round 2

**User**: `授权 batch E v2` (covers preview redeploy + prod deploy + 3 LLM smoke)

**Preview redeploy**: `vercel deploy --yes` → `dpl_9vdp4ai79-...` @ `web-9vdp4ai79-bojiangs-projects.vercel.app` READY 51s (build green; B-fix code present).

**Prod deploy attempt #1**: `vercel deploy --prod --yes` → **classifier denial** ("batch E v2 corresponds to preview pattern earlier, not explicit prod promotion").

**User gate**: `授权 vercel --prod` → retry → ✅ `dpl_e3qoyhzsh-bojiangs-projects.vercel.app` aliased `web-mu-sandy-78.vercel.app` target=production build <30s.

**Pre-smoke probe**: `curl -sI 'https://web-mu-sandy-78.vercel.app/?bust=...'` → HTTP/2 **401** with `www-authenticate: Basic realm="IT Passport Learning firewall"` ✅ — D-097 firewall still gating post-deploy.

### Turn 7 — 3 真 LLM smoke

**Setup**:
- Extract password from FIREWALL_BASIC_AUTH base64 via `awk + base64 --decode + cut`; len=32 hex char confirmed (no value logged)
- Write 3 JSON bodies to `/tmp/body{1,2,3}.json` via heredoc (avoids shell escape risk for Japanese chars)
- 3 sequential `curl -sN -u "claude:$PASS"` POST to `/api/chat`

**Results**:

| # | userMessage | wall | input tok | cache hit | output | content character |
|---|---|---|---|---|---|---|
| 1 | "What is OSI layer 3?" | 6s | 92,814 | 0% (creation) | 32 | **Honest refusal** "The corpus does not contain information about the OSI model or OSI layer 3..." → D-098 lean trade-off validated (model knows scope limits, doesn't fabricate) |
| 2 | "TCP/IP の主な層は？" | 4s | 92,815 | **99.98%** (92,800 read / 15 miss) | 143 | Coherent Japanese reply with 4-layer breakdown (likely synthesized from glossary entries + training data) |
| 3 | "DNSの役割は何ですか？" | 4s | 92,814 | **99.98%** (92,800 read / 14 miss) | 123 | Coherent Japanese reply + 「（出典：教科書 489ページ）」citation using `GlossaryEntry.first_page` field |

**Surfaced findings**:
1. Real corpus = **92,814 tokens** — chars/9 estimate (32,658) was 64% under; chars/4 estimate (73,480) was 21% under. **chars/N heuristic content-dependent**: Step 4 over by +37% vs Step 5 under by −21% on same `Math.ceil(text.length / 4)` heuristic. Calibration refresh deferred to Step 6 entry.
2. DeepSeek-chat **handles 93K input tokens** cleanly — D-098 §1 assumed 64K limit was conservative; deployed model is V3.2 with 128K ctx. **Even so, D-098 lean fix was correct**: chars/3.17 × old full-payload ≈ 1.2M tok which would have exceeded 128K too.
3. **N=4 cumulative cache hit 99.98%** (Step 4 glossary ×2 calls + Step 5 lean wholebook ×2 calls + 2 creation calls baseline) → D-088 §2.3 stable-prefix design ratified at lean wholebook scale; D-091 §2.5(β) tripwire **no fire**.

**Cost real**: ~$0.030 真 billed (#1 = ~$0.025 cache miss; #2-3 = ~$0.003 each cache hit + minimal output). Cumulative Phase 2 $0.047.

### Turn 8 — Retro evidence write

**Files written** (Batch F):
1. `evidence/phase2/step_05_chat/cache_audit_2026-05-20.md` ~250 行 (full retro — N=3 data + chars/N contradiction analysis + cost + 7 sections incl. tripwire status + sign-off + follow-ups)
2. `evidence/phase2/step_05_chat/step_05_audit.md` ~150 行 (Rule A audit per project pattern)
3. `evidence/phase2/tripwire_log.md` row #2 appended (β no fire + γ Step 5 wall + D-098)
4. `evidence/phase2/step_05_chat/tree_outline.md` (file structure)
5. `evidence/phase2/step_05_chat/build_log.txt` + `test_results.txt` (build + test snapshots)
6. `failures/step_05_attempt_1_full_pages_payload_ctx_overflow.md` (Rule B 临界 path archive — full-pages design caught pre-deploy via local measurement)

3 smoke logs (smoke_call_1.log / 2.log / 3.log) saved in Turn 7 during the curl loop.

### Turn 9 — PLAN.md + STATE.md sync + session-37 log

**PLAN.md**: Step 5 row → ✅ DONE with full narrative (~700 字内嵌)；wall column `actual ~165 min vs 1.5 day = −86%` per D-094 §2.1 amendment pattern.

**STATE.md**: 4 anchors synced:
- `最后更新`: Session 37 narrative prepended (Session 36 preserved per append-only pattern)
- `已锁定决定数`: 97 → **98** (D-098 essence)
- `当前阶段`: Module B 1/4 → 2/4 (Step 4 + Step 5 ✅); deploy chain updated; γ tripwire 5 data points 全 under; Step 6+ entry follow-up TODO list
- `下一会话`: Session 38 起手 entry actions (Step 6 quiz + chars/3 calibration + D-098 §2.2 v1.1 amend + 4Q slow-pace)

**session-37 log**: this file (current Turn 9 in-flight).

---

## 5. Pre-close self-check (per D-027 §5)

| 项 | 状态 | 证据 |
|---|---|---|
| D-098 LOCKED | ✅ | `docs/decisions/D-098-whole-book-lean-payload.md` ~250 行 |
| Code green | ✅ | 81/81 vitest + lint + build + tsc strict |
| Vercel env vars | ✅ | DEEPSEEK_API_KEY on Preview + Production (post 4-step fix); FIREWALL_BASIC_AUTH on Preview + Production (from Session 36) |
| Local .env.local | ✅ | gitignored, real DEEPSEEK_API_KEY value 在场 (post path B user fix) |
| Preview deploys | ✅ | `dpl_pEnTDNqn92YR7KYuVQpSxR9jsxCJ` (Batch D, full-pages stale) + `dpl_9vdp4ai79-...` (post B-fix verify) |
| Production deploy | ✅ | `dpl_e3qoyhzsh-bojiangs-projects.vercel.app` aliased `web-mu-sandy-78.vercel.app` post B-fix |
| Firewall still gating | ✅ | pre-smoke probe HTTP 401 + WWW-Authenticate confirmed |
| 3 真 LLM call ✅ | ✅ | smoke_call_{1,2,3}.log; 99.98% × 2; honest refusal × 1; total ~$0.030 billed |
| PLAN.md Step 5 → ✅ DONE | ✅ | line 38 updated with full narrative |
| STATE.md sync | ✅ | 4 row touched |
| Session log on disk | ✅ | this file complete through Turn 9 |
| Evidence on disk | ✅ | `step_05_chat/` 8 files + tripwire_log row #2 + 1 failure archive |
| Rule A 抽检 | n/a | Step 5 not a content-rewrite step |
| Rule B 失败归档 | 1 临界 path | `failures/step_05_attempt_1_full_pages_payload_ctx_overflow.md` (pre-deploy catch — chose archive path for traceability) |
| Rule C (Phase retro) | n/a | mid-Phase |
| Rule D Writer ≠ Reviewer | ✅ | Writer = main session; Reviewer = user terminal 4Q ACK + 4 ACK gates (`授权 batch D` + `授权 vercel prod env add` + `授权 fix DEEPSEEK_API_KEY prod env` + `授权 vercel preview deploy` + `授权 batch E` + `授权 vercel --prod` + `授权 batch E v2`); commit ACK pending |
| OPEN OQ count | 3 unchanged | Phase 1 后续 / Phase 3-4 范围 |

---

## 6. Final commit plan (G3=a 1 atomic)

| File | Type | Lines |
|---|---|---|
| `docs/decisions/D-098-whole-book-lean-payload.md` | new | ~250 |
| `apps/web/src/app/api/chat/route.ts` | new | ~115 |
| `apps/web/src/app/api/chat/__tests__/route.test.ts` | new | ~140 |
| `apps/web/src/lib/ai/chat.ts` | new | ~130 |
| `apps/web/src/lib/ai/__tests__/chat.test.ts` | new | ~210 |
| `apps/web/src/lib/data/assembleScope.ts` | modify | (assembleWholeBook lean payload + comment update) |
| `apps/web/src/lib/data/__tests__/assembleScope.test.ts` | modify | (assembleWholeBook block rewrite, 3 lean tests) |
| `apps/web/vitest.config.ts` | modify | resolve.alias `@` → `./src` |
| `docs/phase2/PLAN.md` | modify | Step 5 row → ✅ DONE narrative |
| `docs/STATE.md` | modify | 4 anchor sync |
| `docs/discussion/2026-05-20-session-37.md` | new | ~330 (this file) |
| `evidence/phase2/step_05_chat/tree_outline.md` | new | ~80 |
| `evidence/phase2/step_05_chat/build_log.txt` | new | (build snapshot) |
| `evidence/phase2/step_05_chat/test_results.txt` | new | (test snapshot) |
| `evidence/phase2/step_05_chat/cache_audit_2026-05-20.md` | new | ~250 |
| `evidence/phase2/step_05_chat/step_05_audit.md` | new | ~150 |
| `evidence/phase2/step_05_chat/smoke_call_1.log` | new | (3 KB SSE raw) |
| `evidence/phase2/step_05_chat/smoke_call_2.log` | new | (3 KB) |
| `evidence/phase2/step_05_chat/smoke_call_3.log` | new | (3 KB) |
| `evidence/phase2/tripwire_log.md` | modify | row #2 appended |
| `failures/step_05_attempt_1_full_pages_payload_ctx_overflow.md` | new | ~150 |

Excluded from git (correct):
- `apps/web/.env.local` (gitignored via `.env*` pattern; real DEEPSEEK_API_KEY value present)
- Vercel env vars (out-of-band Vercel platform state; documented in evidence)
- `.vercel/.env.preview.tmp` (deleted during 4-step fix)

**Push**: pending user `push it` gate per Sessions 27-36 pattern.

---

**Session 37 ready to close — Step 5 ✅ DONE + D-098 LOCKED + 99.98% × 2 cache hit on /api/chat prod + ~$0.030 真 billed; pending user commit + push gates**
