# Session 17 — 2026-05-13

> Phase 1 Step 6.11.D.4 — Stage 4.5 glossary extraction over the
> 554-page structured/ output produced in Session 16, then evaluate
> Gate ③ per D-079 §2.1 row 3 + D-080 v1.1 amendment.
> Continues from Session 16 close at HEAD `7d90739` (6.11.D.3 ✅
> Gate ② PASS post β.1, 476/476 unit tests, 81 D locked, 3 OQ open).

---

## 0. Session Goal

Authorize and execute **Step 6.11.D.4** end-to-end:

1. Stage 4.5 — single-call glossary extraction on the 554 Stage-4 structured pages (opus tier; quality-critical because glossary locks all downstream Stage 5 translation)
2. Evaluate Gate ③ via `check_gate_3_post_glossary` with the orchestrator-computed inputs (D11 / D13 / untranslated / entry count)
3. Emit `gate_3_<ts>.json` checkpoint on PASS
4. Hand off to next user gate (D.5 Stage 5 translation)

---

## 1. Inherited State

- HEAD entering: `7d90739` (Session 16 close)
- Branch: `main`, clean working tree, 0 ahead of origin
- Unit tests: **476/476** pass entering
- Step 0~5 ✅; Step 6.0~6.10 ✅; Step 6.11.A/B/C/D.1/D.2/D.3 ✅
- Locked decisions: **81** (D-001 ~ D-081)
- 3 OQ open (Phase 1 out-of-scope: OQ-01, OQ-02, OQ-05)
- Cumulative cost going in: **Mistral $0.629 billed / Anthropic $0 billed / shadow $235.63 / wall 3h24m**
- Run-id under work: `dry_run_2026-05-12T13-23-19`
- Disk state: `structured/` = 554 files (the 25 by-label-skipped pages are
  by design); `cleaned/` = 56 files; `classified/` = 579; `ocr/` = 579;
  `glossary/` does **not** exist yet — D.4 creates it.

---

## 2. Decisions Locked (this session)

_(no new ADR planned; D-080 v1.1 §8 amendment governs Gate ③ semantics)_

### Operational interpretation of Gate ③ (orchestrator wiring)

Per **D-080 v1.1 §8.5**, the v1.0 acceptance "D11 INFO == 0" is **withdrawn**
because D11 (`kana_helper_missing`) operates on Stage 5 `translated_entities`
which do not exist at Gate ③. The Stage 5 kana_helper-propagation gap is
tracked as Phase 1 v2 follow-up (D-080 v1.1 §8.6).

Concrete wiring choices for `check_gate_3_post_glossary`:

| Input | Source at Gate ③ | Rationale |
|---|---|---|
| `d11_info_count` | Pass `0` (no `translated/` to scan) | D-080 v1.1 §8.5 — D11 is not measurable here; deferred to Gate ④/⑤ on Stage 5 output |
| `d13_run_level_info_count` | `len(detect_glossary_consistency(glossary))` | Run-level detector reads glossary directly — fully measurable at Gate ③ |
| `untranslated_surface_count` | scan `glossary.entries[*].surface.{jp,zh,en}` for empty / `UNTRANSLATED` | Direct glossary self-inspection |
| `glossary_entry_count` | `len(glossary.entries)` | Direct |
| `expected_entry_count` | `1000` with `count_tolerance=0.50` | Spans both 40-page baselines: original 91→554 ≈ 1260; polish_a 63→554 ≈ 873; midpoint ~1000 with ±50% accepts 500-1500 |

Recorded in this session log only — not a new D (no architectural change;
mechanical interpretation of D-080 v1.1 amendment).

---

## 3. Open Questions

继承 OQ-01 / OQ-02 / OQ-05（Phase 1 范围外）。无新增 OQ。

---

## 4. Execution log — Step 6.11.D.4

### 4.1 Stage 4.5 extract-glossary (single attempt, opus, single-call design)

```
uv run --project packages/extractor cert-extractor extract-glossary \
  --structured-dir data/itpassport_r6/runs/dry_run_2026-05-12T13-23-19/structured \
  --cert-id itpassport_r6 --tier opus \
  --anthropic-soft-usd 50 --anthropic-hard-usd 100 --confirm
```

Result:

```
[extract-glossary] DONE   pages_scanned   = 554
[extract-glossary]        terms_harvested = 1350
[extract-glossary]        unique_surfaces = 978
[extract-glossary]        entries_locked  = 908
[extract-glossary]        fail_count      = 0
[extract-glossary]        verdict_halted  = Verdict.FAIL    ← (cap-misframe artifact, NOT a data failure; see §4.2)
[extract-glossary]        glossary.json   = data/.../glossary/glossary.json (317 KB)
```

Cost (delta this stage): **$2.549 shadow / $0 billed**, 1 LLM call,
70 921 tokens (input+output combined). Wall +539 s ≈ 9 minutes.
Single-call design held — 978 unique surfaces fit comfortably in one
opus prompt; canonicalization deduped 70 surfaces into 908 entries
(70/978 = 7% alias-merge rate).

### 4.2 `verdict_halted = FAIL` is a known cap-cumulative artifact

`Stage4_5Glossary._budget_check` calls `BudgetMonitor.check` against
`tracker.current.anthropic_usd` — which is **cumulative across all
stages** (cost.json accumulates per D-072). My `--anthropic-soft-usd
50 --anthropic-hard-usd 100` were sized assuming "per stage", but the
checker compares to the rolling total (D.3 left $235.63 → $238.18 after
this stage). Hence the "FAIL" verdict, despite `fail_count = 0` and
`entries_locked = 908`.

This is the same class of UX wart noted in Session 16 §7 ("future
code/UX improvement: distinguish quota-throttle from data-failure"
applies analogously to "distinguish per-stage cap from cumulative
spend"). Tracked as Phase 1 v2 follow-up, not blocking. The actual
work completed cleanly (file on disk, all schema validation passed,
0 fails, gate ③ inputs all clean).

### 4.3 Gate ③ orchestrator pass

Per §2 wiring, computed inputs against the on-disk glossary using
the existing `pipeline/halt_criteria.py` checker + `stage6_audit/
detectors.py::detect_glossary_consistency`:

| Input | Value | Source |
|---|---|---|
| `d11_info_count` | 0 | per D-080 v1.1 §8.5 — no `translated/` at Gate ③ |
| `d13_run_level_info_count` | **0** | `len(detect_glossary_consistency(g))` |
| `untranslated_surface_count` | **0** | scan all 908 entries, no empty/UNTRANSLATED in surface.{jp,zh,en} |
| `glossary_entry_count` | **908** | `len(g.entries)` |
| `expected_entry_count` | 1000 | midpoint of 91→554 (1260) and 63→554 (873) baseline extrapolations |
| `count_tolerance` | 0.50 | wide band spans both 40-page baselines |

```python
check_gate_3_post_glossary(...) → HaltResult(passed=True, reasons=())
```

### 4.4 δ-equivalent quality spot-check

Sampled 9 entries (first 3, middle 3, last 3) + the 1 auto-backfill:

```
g_001  '1-Click で今すぐ買う' / '一键立即购买' / '1-Click Buy Now'        kana_helper=None
g_002  '10の知識エリア'   / '十大知识领域'   / 'Ten Knowledge Areas'        kana_helper=None
g_003  '10進数'        / '十进制'      / 'Decimal number'             kana_helper=None
g_454  'ハッカソン'      / '黑客马拉松'    / 'Hackathon'                kana_helper={'ハッカソン','hakkason','黑客马拉松' auto_backfill=False}
g_455  'ハッシュ関数'     / '哈希函数'     / 'Hash Function'              kana_helper=None
g_456  'ハブ'         / '集线器'      / 'Hub'                       kana_helper={'ハブ','habu','集线器' auto_backfill=False}
g_906  '魔の川'        / '魔鬼之河'     / "Devil's River"               kana_helper=None
g_907  '黒字倒産'       / '黑字破产（盈利破产）' / 'Profitable Bankruptcy'  kana_helper=None
g_908  'ITパスポート'    / 'IT护照'      / 'IT Passport'                kana_helper=None

auto_backfill (D-080 polish #1 safety-net):
g_451  'ネットワークデータモデル' / '网状数据模型' / 'Network Data Model'
       kh={surface=ネットワークデータモデル, reading=ネットワークデータモデル, zh_concept=网状数据模型}
       (opus omitted kana_helper; polish #1 injected placeholder reading == surface)
```

Translations are professional-grade, mix-script handling correct
(g_001 mixes ASCII + kana + kanji; g_002 starts with a digit; g_007
uses paragraph-style zh translation gloss `黑字破产（盈利破产）`).

D-080 v1.1 §8.5 prediction empirically held: opus self-attached
kana_helper to most katakana surfaces (e.g. ハッカソン, ハブ); polish #1
fired exactly once (`g_451 ネットワークデータモデル`, a long compound
where opus likely judged kana_helper redundant). Distribution:
307 LLM-authored kana_helper / 1 auto-backfill / 600 absent
(non-katakana entries).

Distribution stats: 32% multi-page entries (291/908, real cross-chapter
consolidation working), 9% with aliases (85/908).

### 4.5 Gate ③ checkpoint emitted

`emit_checkpoint(plan_resume('4.5'), ...)` → `data/itpassport_r6/runs/
dry_run_2026-05-12T13-23-19/checkpoints/gate_3_2026-05-13T09-50-35.json`:

```json
{
  "gate": 3,
  "stage_completed": "4.5",
  "next_stage": "5",
  "halt_time": "2026-05-13T09:50:35+09:00",
  "summary": { "pages_scanned": 554, "glossary_entries_locked": 908,
               "d11_info_count": 0, "d13_run_level_info_count": 0,
               "untranslated_surface_count": 0,
               "kana_helper_auto_backfill": 1,
               "kana_helper_human_or_llm_authored": 307,
               "kana_helper_absent": 600,
               "cost": { "anthropic_usd_billed": 0.0,
                         "anthropic_usd_shadow_stage45": 2.549,
                         "anthropic_usd_shadow_cumulative": 238.18,
                         "wall_time_seconds_cumulative": 12787,
                         "fail_count_cumulative_sunk": 50 } },
  "halt_criteria_passed": true,
  "samples_for_review": ["g_001", "g_455", "g_908"],
  "resume_command": "uv run cert-extractor stage --from 5
                     --run-id dry_run_2026-05-12T13-23-19
                     --cert-id itpassport_r6"
}
```

---

## 5. Failure archive (per Rule B)

None this session. The `verdict_halted=FAIL` cumulative-cap artifact
(§4.2) is a known UX wart, not a data/operational failure — `fail_count=0`,
all schema validation passed, Gate ③ all-green. Not archived per the
same rationale Session 16 §5 used for cap-WARN halts (operational signal,
not a failure attempt).

---

## 6. Cumulative cost ledger (post Step 6.11.D.4)

| Metric | Value | Δ this session |
|---|---|---|
| Mistral billed (real) | **$0.629** | +$0 |
| Anthropic billed (real) | **$0** (max-plan OAuth — D-069) | +$0 |
| Anthropic shadow (informational) | **$238.18** | +$2.55 (Stage 4.5 single call) |
| Cumulative wall_time | **12 787 s ≈ 3 h 33 m** | +539 s (~9 m) |
| Cumulative fail_count | **50** (sunk; mostly 5h-quota throttle from D.3) | +0 |
| Total pages classified / hard-reocr'd / structured | 579 / 56 / 554 | unchanged |
| Total entities (Stage 4) | **2 224** | unchanged |
| **Glossary entries locked (Stage 4.5)** | **908** | **NEW** |
| D-track remaining estimate (opus) | D.5 ~$50-150 + D.6 ~$30 = ~$80-180 shadow | revised down (D.4 came in at $2.55, well below the $15-25 STATE estimate) |

Stage 4.5's single-call design hugely outperformed expectations on
cost — opus authored 908 trilingual entries with kana_helper coverage
in one prompt for $2.55 shadow. The STATE.md "$15-25 shadow" guess
extrapolated from per-page Stage 2/3/4 costs and missed that 4.5 is
batch-amortized differently.

---

## 7. Memory updates

No new memory files. The `verdict_halted=FAIL` cumulative-cap framing
issue is operational (UX/wart) not a user-preference signal — already
catalogued for Phase 1 v2 follow-up.

---

## 8. Pre-close self-check (D-027 §5)

| Check | Status |
|---|---|
| D-NNN: any new? | None |
| OQ: any new? | None (3 inherited stay open: OQ-01, OQ-02, OQ-05) |
| STATE.md sync | ✅ this turn (top table + §5 row 6.11.D.4 → ✅ Gate ③ PASS, D.5 next) |
| Session log on disk | ✅ this file |
| ADR files on disk | n/a (no new ADR; D-080 v1.1 §8 amendment governs Gate ③ wiring) |
| Memory updates | none required |
| Code | ✅ no source changes — D.4 used the CLI + Python orchestrator that 6.11.B.2/B.3 already shipped (Session 14) |
| Tests | 476/476 (unchanged from `7d90739`) |
| Evidence | ⏸ `evidence/.../step_06_11_D4.md` semantic audit deferred — same rationale as Session 16 §11 (Rule A applies once compression/rewrite ratios become measurable post-Stage-5; this session's checkpoint + log + spot-check serve as evidence of D.4 completion) |
| Failures | none |
| Commits | ⏸ user gate (recommend single atomic commit covering this session log + STATE sync; cleaned/structured/glossary/checkpoint files are gitignored per D-050) |

---

## 9. What's Next

**Step 6.11.D.4 ✅ COMPLETE.** Gate ③ PASS, checkpoint emitted, 908
trilingual glossary entries locked, D11/D13/untranslated all 0,
$2.55 shadow / $0 billed.

Next executable = **Step 6.11.D.5 — Stage 5 trilingual translation
→ Gate ④** (per D-079 §2.1 row 4). This is the **largest LLM dispatch
in Phase 1** — opus translation across ~554 pages × ~4-8 sub-batches
each (per `--max-items-per-call=8` default), constrained to glossary
locks per D-075 jp-preservation + D-074 wrapper-clause prompt.
Estimated $50-150 shadow / $0 billed via D-069 OAuth. Gate ④ checker
= `check_gate_4_post_translation` (jp-mutation + UNTRANSLATED guards).

User decision points:

- **(a) Commit Session 17 close** — recommended: single atomic commit
  for this session log + STATE sync (gate_3 checkpoint JSON + glossary.json
  are gitignored under `data/`).
- **(b) Authorize 6.11.D.5** (next user gate per D-073 + D-079) — be
  aware this is the longest-running stage; expect 1-3 hours wall depending
  on chunk_size + 5h-quota throttle behavior. May want to bump
  `--anthropic-hard-usd` above the $238 cumulative + $200 headroom = $440.
  Mid-stage cap halt requires `--skip-existing` + cap-bump rerun, same
  pattern as D.3 stages 2/4.
- **(c) Close Session 17 here**, open Session 18 for D.5.
- **(d) 5h-quota window check** — if recently throttled, give 5h before
  D.5; cumulative wall is 3h33m so a fresh window is comfortable.

Resume entry-point for next session:

1. Read `docs/STATE.md` top table + §5 6.11.D rows
2. Read this file §4 (D.4 narrative) + §9 + the gate_3 checkpoint
3. Confirm 5h max-plan window status before dispatching D.5
4. Resume at 6.11.D.5 — `translate-entities` CLI per D-074/D-075 contract
   → Gate ④
