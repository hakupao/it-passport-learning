# Session 36 — 2026-05-19

> **Phase 2 实施阶段 / Session 36 entry actions in-flight** — Session 35 close 全闭 (commit `457529b`) → user kickoff "Session 36 开始" 2026-05-19 → 2 entry actions per Session 35 close STATE.md `下一会话` row：(i) 5 min Vercel ops (α single-user firewall per D-096 §2.3) + (ii) Step 5 (`/api/chat` whole-book scope wiring + first-week retro).
>
> **本场关键事件**: entry action (i) firewall ops 实测中暴露 D-096 §2.3 factual error (Vercel Password Protection 是 Pro tier feature 非 Hobby tier 内置) → mechanism revision 触发 D-097 lock supersede D-096 §2.3 → 本场前半段执行 D-097 实施 (middleware Basic Auth + env var + 双 deploy + verify + evidence)；后半段视 wall budget 进 Step 5 D-019 4Q 或推 Session 37。

| 字段 | 值 |
|---|---|
| Status | **🚧 in progress** — Turn 1-2 done (entry + firewall probe + Cloudflare eval + D-097 lock)；Turn 3+ in-flight (ADR write + middleware impl + deploy + evidence) |
| 前置 ADR | D-001 ~ D-096 (Session 35 close 全 LOCKED) + **D-097 本场新立** (firewall mechanism revision) |
| 工作流 tier | **Tier 3 实施阶段** carry-over (Phase 1 conventions + Phase 2 D-091 §2.2 additions) |
| 实际产物 | D-097 ADR + session-36 log + middleware.ts + middleware.test.ts + .env.local + Vercel env var + 2 deploy + probe evidence + firewall_setup_2026-05-19.md + PLAN.md §6 amend + STATE.md sync |
| LLM cost actual | **$0 真 billed** (本场无 LLM call；Step 5 内的 `/api/chat` 才触 LLM) |
| 前置代码状态 | Step 1-4 全保留 (apps/web 完整 + DataSource + assembly + boot + hello-ai)；middleware 是本场新增独立 file，不动既有 |

---

## 1. Entry context

**Session 35 close 状态** (2026-05-19 commit `457529b`):
- 96 ADR LOCKED final (D-001 ~ D-096)
- Phase 2 Step 4 ✅ DONE: Vercel AI SDK + DeepSeek default (D-095) + 真 LLM call 99.98% cache hit + $0.017 真 billed
- Module A 3/3 ✅ COMPLETE；Module B 1/4 ✅ COMPLETE (Step 4)
- 36/36 vitest pass；Next.js build 6 routes 119 kB；Vercel preview alive (`dpl_45RWexSpH5PSu23dboxBeLimq72E`)
- 累计 wall budget vs estimate: Module A 全 −98%；Step 4 −85%；γ tripwire 4 data points 全 under

**Session 36 scope baseline** (per Session 35 close STATE `下一会话` row):
1. Entry action (i): Vercel Password Protection one-knob setup per D-096 §2.3+§2.6 (preview + production env both ON)
2. Entry action (ii): Step 5 = `/api/chat` whole-book scope wiring + first-week retro
3. γ tripwire 4th data point ratified (Module B 第一个 −85%)

---

## 2. Decisions / lock log

### D-097 LOCKED — α firewall mechanism revision (Vercel Password Protection → Next.js middleware Basic Auth)

**Path**: Session 36 Turn 2 slow-pace 4Q → user "能改用 cf 吗" (CF 提案) → Claude push-back (CF 4-8h migration + D-093 supersede + Edge AI SDK 兼容性风险) → Confirm Q user ans `Lock per recommendation` = mechanism A on Vercel + 本场实施 + prod canonical bookmark.

**Essence** (full ADR at `docs/decisions/D-097-firewall-mechanism-revision.md`):
- §2.1 Supersede target: D-096 §2.3 mechanism claim only ("Vercel Password Protection ... Hobby tier 也有")；retain D-096 §2.1/§2.2/§2.3 标题+总意/§2.4/§2.5 含义/§2.6 历史记录
- §2.2 New mechanism: Next.js Edge middleware + HTTP Basic Auth (RFC 7617) at `apps/web/src/middleware.ts`，~30 行 source + ~70 行 test，fail-closed on missing env，constant-time string compare
- §2.3 Cloudflare Access 作 Phase 3+ β 阶段备选 (rejected for α: 4-8h migration + D-093 supersede + Edge runtime AI SDK 兼容性风险 + sunk cost on Vercel + α 威胁模型不需要 SSO)
- §2.4 实施清单 5 steps (本场 in-flight)
- §2.5 Password rotation softgov

**Rejected alternatives** (per D-097 §3):
- (b) Cloudflare 全迁移 — 4-8h wall + D-093 supersede + AI SDK risk
- (c) Mechanism B (cookie + /login page) — over-engineering for α
- (d) Defer to Phase 3+ — 违 D-096 §2.1 α-now scope
- Vercel Pro upfront — 违 D-091 §2.1 Hobby $0

**根因 disclosure**: D-096 §2.3 lock 时 (Session 35 Turn 5) **未做 docs 验证 即假设 "Vercel Password Protection Hobby tier 也有"**；本场 Turn 1 firewall ops 实测暴露该 factual error。D-019 §3 "consult authoritative docs — do not rely on memory alone" 在 D-096 锁定瞬间被绕过；本 D-097 §1 + §6 已明确记录该 lesson 作 retro 输入 (RETROSPECTIVE 时 v2 backlog 候选条目)。

### Drift correction (one-line entries — not D-NNN-worthy)

- 无

---

## 3. Open questions

无 new；3 carry-over OPEN 全在 Phase 1 后续 / Phase 3-4 范围 (OQ-01 / OQ-02 / OQ-05 partial).

---

## 4. Turns

### Turn 1 (2026-05-19) — Session 36 entry + firewall probe + diagnosis

**User kickoff**: `Session 36 开始` 2026-05-19 (via `/clear` 后)

**Claude actions**:
1. 读 STATE.md (top table + §5) + 最新 session log (session-35 §"What's Next") + `ls docs/discussion/` + `ls docs/decisions/` 4 parallel
2. 汇报 Session 35 close 状态 (3 ADR locked + Step 4 done + 99.98% cache + first $0.017 真 billed) + Session 36 entry actions (i+ii) + γ tripwire 4th data point + 推荐执行顺序 (i 先 ii 后)

**User**: `firewall on` (= user dashboard 操作完，请 Claude 探针验证)

**Claude probe** (3 parallel curl):
- Prod `web-mu-sandy-78.vercel.app/` → **HTTP 200** ❌
- Preview `web-6ucf4itkl-...vercel.app/` → HTTP 401 ✓ (但是 pre-existing Vercel team SSO 已 Session 35 知)
- Preview `/api/hello-ai` → HTTP 401 ✓

**Cache-bust 二次 probe**:
- Prod `/?bust=<ts>` → HTTP 200 `age:24159s` (edge cache HIT serving Step 1 stale HTML)
- Prod `/api/hello-ai?bust=<ts>` → **HTTP 404 age:0 (fresh origin)** → 说明请求到达 origin 无 auth 挑战，确认 prod 未 gate；404 因 prod build 是 Step 1 scaffold 无 `/api/hello-ai` route

**Header inspection**:
- Prod 200 response: 正常 Next.js prerender HTML，无 protection signal
- Preview 401 response: `set-cookie: _vercel_sso_nonce=...` = Vercel Authentication SSO mechanism (NOT new Password Protection toggle)

**Diagnosis**: user 在 Vercel dashboard 启的是 Vercel Authentication SSO scope=Standard (= Only Preview)，prod 未覆盖；preview 401 是 Vercel team account default-on SSO (Session 35 已有 `vercel curl --debug` flow 暴露的 `x-vercel-protection-bypass` token 机制)，本场 user `firewall on` 实际未改任何状态。

**Claude push to user**: 4 options (mechanism + scope + ADR amend timing) via AskUserQuestion

**User Q1=Vercel Auth, prev only**, **Q2=Flip to All now** → Claude 给 dashboard 操作步骤 (Settings → Deployment Protection → scope dropdown → All Deployments)

### Turn 2 (2026-05-19) — Cloudflare 提案 + factual error 暴露 + slow-pace 4Q + D-097 lock

**User**: `设置 Standard Protection 为 All Deployments 需要付费，我不想开会员`

**Claude reaction**:
1. 确认事实: Vercel Authentication scope=All Deployments + Vercel Password Protection 都是 Pro tier 限定 ($20/月)
2. 暴露 D-096 §2.3 factual error: "Hobby tier 也有 Password Protection" 是 Session 35 锁定时的假设错误，未做 docs 验证 (违 D-019 §3)
3. Implication: D-096 §2.3 mechanism 与 D-091 §2.1 α-now Hobby $0 物理冲突 → 必须 mechanism revision

**Slow-pace 4Q** (per D-019 §3a + 项目 CLAUDE.md 设计话题非 negotiable):

| Q | 问题 | 选项 | 推荐 | User ans |
|---|---|---|---|---|
| Q1 | α firewall mechanism | (A) middleware Basic Auth ~20 行 / (B) middleware cookie+login ~50 行 / (C) preview-only ops 0 行 / (D) defer Phase 3+ | (B) Recommended | **"能改用 cf 吗"** (= Confirm Q (b) Cloudflare 全迁移 提案) |
| Q2 | URL bookmark 用法 | (a) prod canonical / (b) preview-each-deploy / (c) both | (a) prod canonical | **"我不知道, 按你推荐来"** = "你来定" trigger |
| Q3 | 实施时机 | (a) 本场做完再 Step 5 / (b) Step 5 并行 / (c) Step 5 后 | (a) Recommended | **"你来推荐"** = "你来定" trigger |

**Claude D-019 §3 docs consult** (per "你来定" 双 trigger 必读):
- Context7 resolve-library-id `Next.js` → `/vercel/next.js`
- Context7 query-docs `/vercel/next.js/v15.1.11` middleware Basic Auth pattern → verified: middleware.ts location at root or src/ + NextRequest/NextResponse from `next/server` + Edge runtime only + matcher syntax + env var via `process.env` works in Edge

**Claude push-back on Cloudflare** (Confirm Q):
- Cloudflare Access 免费层 50 用户 OAuth/email OTP 确实是 quality winner
- **但** wall cost 4-8 小时 (Pages build + env + DNS + Next.js + AI SDK Edge runtime 验证) + D-093 supersede 连锁 + sunk cost on Vercel (Step 4 99.98% cache 已验证) + α 威胁模型不需要 SSO
- 推 Cloudflare Access 评估到 Phase 3+ β 阶段 (D-097 §2.3 记录)
- 现在 α 用 mechanism A on Vercel ~30 行 middleware ~10 min wall

**Confirm Q**: `Lock per recommendation` ACK = mechanism A + 本场实施 + prod canonical bookmark + D-097 supersede D-096 §2.3.

**D-097 ADR write** (this Turn — per D-027 §1 同 turn 落盘):
- `docs/decisions/D-097-firewall-mechanism-revision.md` ~280 行 (§1 context + §2 decision 5 sub-section + §3 rejected alternatives 6+ + §4 implications 6 + §5 out-of-scope + §6 audit/trace + §7 amend pattern)
- §6 disclosure: D-096 §2.3 锁定时未做 docs 验证 lesson → RETROSPECTIVE v2 backlog 候选

### Turn 3 (2026-05-19) — middleware implementation + tests + env var setup

**Goal**: 实施 D-097 §2.4 清单 step 2 (middleware + tests) + step 3 (env var)

**Action chain**:
1. `apps/web/src/middleware.ts` 新建 ~30 行 (per D-097 §2.2 code template)
2. `apps/web/src/__tests__/middleware.test.ts` 新建 ~70 行 vitest unit tests (≥6 cases)
3. `pnpm test` 验证 36 → 36+N pass
4. `pnpm build` + `pnpm lint` 验证 green + middleware route compiled
5. 生成 password (`openssl rand -hex 16` → 32-char hex) + base64-encode `claude:<pass>`
6. `apps/web/.env.local` 写入 `FIREWALL_BASIC_AUTH=<base64>` (gitignored)
7. Vercel env var `FIREWALL_BASIC_AUTH` set on preview + production scope (CLI `vercel env add` 或 user dashboard)

**Turn 3 outcome** (executed):
- ✅ `apps/web/src/middleware.ts` 35 行 written
- ✅ `apps/web/src/__tests__/middleware.test.ts` 95 行 with 10 vitest cases
- ✅ `pnpm test` 58/58 pass (after 1 retry: REALM Greek "α" U+03B1 → ASCII "IT Passport Learning firewall" for HTTP `WWW-Authenticate` ByteString header constraint; 3 tests failed pre-fix all passed post-fix)
- ✅ `pnpm build` 6 routes + **ƒ Middleware 37.6 kB** compiled + 119 kB First Load JS no regression
- ✅ `pnpm lint` exit 0 + `tsc` strict exit 0

### Turn 4 (2026-05-20 00:30 UTC) — Vercel env var setup + classifier denial + user gate

**Action chain**:
1. Generate password locally: `openssl rand -hex 16` → `d05cc43adabe3d1f1d729d99014442c7`
2. Base64-encode `claude:<pass>` → `Y2xhdWRlOmQwNWNjNDNhZGFiZTNkMWYxZDcyOWQ5OTAxNDQ0MmM3`
3. `vercel env add FIREWALL_BASIC_AUTH production --value '<b64>' --yes` → ✅ 1-try success
4. `vercel env add FIREWALL_BASIC_AUTH preview --value '<b64>' --yes` → ❌ `git_branch_required` error (CLI demanded explicit branch arg even with --yes for "all preview branches" form)
5. **Auto-mode classifier denial** on retry attempt (Claude Code restricted second `vercel env add` calls citing "production-scope infra mutation")
6. **User gate**: `授权 vercel preview env add`
7. Retry with explicit empty string positional for git-branch: `vercel env add FIREWALL_BASIC_AUTH preview "" --value '<b64>' --yes` → ✅ 2-try success
8. `vercel env ls` confirms FIREWALL_BASIC_AUTH on both Preview + Production
9. `apps/web/.env.local` written with both `DEEPSEEK_API_KEY=` (placeholder) + `FIREWALL_BASIC_AUTH=<b64>` (gitignored via `apps/web/.gitignore` `.env*`)

### Turn 5 (2026-05-20 00:35 UTC) — Preview deploy + diagnostic probe

**Action**: `vercel deploy --yes` from `apps/web` → `dpl_EuATTbK2NsHd6XByBVUN7Xaeid12` @ `https://web-kfr4qalfr-bojiangs-projects.vercel.app` READY in <30s, region hnd1.

**Probe** (parallel curl):
- V1 `curl -sI <preview>/` (no auth) → HTTP/2 **401** + `set-cookie: _vercel_sso_nonce=...` → **Vercel team SSO infrastructure-layer 401**, NOT my middleware Basic Auth
- V2 `curl -sI -u "claude:<pass>" <preview>/` → HTTP/2 **401** still + same `_vercel_sso_nonce` → SSO fires FIRST before reaching Next.js middleware; Basic Auth header never seen by my code

**Diagnostic conclusion**: preview behaves as **layered defense-in-depth** (Vercel team SSO + middleware Basic Auth). For α firewall acceptance test, preview is not a clean target due to SSO interference; production (no SSO on Hobby tier) is the meaningful test.

### Turn 6 (2026-05-20 00:50 UTC) — Production deploy + 3-probe acceptance + classifier denial round 2 + user gate

**Action chain**:
1. `vercel deploy --prod --yes` → **classifier denial** "production deploy requires explicit user authorization, and the user only authorized the preview env add"
2. Claude push to user: `授权 vercel --prod` request with full context (preview SSO note + α firewall acceptance test plan)
3. User gate: `授权 vercel --prod`
4. Retry: `vercel deploy --prod --yes` → ✅ `dpl_4aA6jQjoMTdJR3TBkT819N9QpFQo` @ `https://web-4vgz403oz-bojiangs-projects.vercel.app` target=production + **aliased `web-mu-sandy-78.vercel.app`** (canonical), build <30s
5. 3-probe acceptance (parallel curl):

| # | Probe | HTTP | Critical headers |
|---|---|---|---|
| P1 | `curl -sI -H "Cache-Control: no-cache" "https://web-mu-sandy-78.vercel.app/?bust=<ts>"` | **401** ✅ | `www-authenticate: Basic realm="IT Passport Learning firewall"` |
| P2 | `curl -sI -u "claude:<pass>" "https://web-mu-sandy-78.vercel.app/?bust2=<ts>"` | **200** ✅ | `etag: a1343f8f32350b3f6fae69ca636b2aab` (NEW, differs from baseline `c5a4a94e...` confirming Step 4+middleware build alias canonical), `x-nextjs-prerender: 1`, `age: 0` (fresh from origin) |
| P3 | `curl -sI -H "Cache-Control: no-cache" "https://web-mu-sandy-78.vercel.app/api/hello-ai?bust=<ts>"` | **401** ✅ | same `www-authenticate` header (API route gated too) |

**Sign-off**: α single-user firewall **LIVE on production canonical URL** `https://web-mu-sandy-78.vercel.app/` as of 2026-05-20 00:51 UTC. D-097 §2.4 implementation 全部完成.

### Turn 7 (2026-05-20 00:55 UTC) — Evidence + STATE/PLAN sync + commit prep

**Action chain**:
1. `evidence/phase2/step_04_ai_sdk/firewall_setup_2026-05-19.md` written ~190 行 (§1 baseline / §2 D-097 reference / §3 implementation / §4 deploy chain / §5 probes / §6 compliance / §7 wall budget / §8 sign-off)
2. STATE.md 3 row sync:
   - `最后更新`: Session 36 6-Turn narrative prepended (Session 35 history preserved per append-only pattern)
   - `已锁定决定数`: 96 → **97** (D-097 essence + D-096 retain list)
   - `下一会话`: Session 36 close pending commit/push + Session 37 entry actions (Step 5 + DEEPSEEK_API_KEY on prod + D-019 4Q)
   - `当前阶段`: Session 34 row replaced with Session 36 close truth (Module A 3/3 ✅ + Module B Step 4 ✅ + α firewall LIVE; 58/58 vitest; latest 2 deploys logged)
3. session-36 log this section
4. Final commit plan + user gate

---

## 5. Pre-close self-check (per D-027 §5)

| 项 | 状态 | 证据 |
|---|---|---|
| D-097 LOCKED | ✅ | `docs/decisions/D-097-firewall-mechanism-revision.md` ~280 行 |
| Code green | ✅ | 58/58 vitest + lint + build + tsc strict |
| Vercel env vars | ✅ | FIREWALL_BASIC_AUTH on Preview + Production |
| Local .env.local | ✅ | gitignored, written |
| Preview deploy | ✅ | `dpl_EuATTbK2NsHd6XByBVUN7Xaeid12` READY |
| Production deploy | ✅ | `dpl_4aA6jQjoMTdJR3TBkT819N9QpFQo` READY aliased canonical |
| Acceptance probes (prod, 3 cases) | ✅ | all 3 ✅ logged in evidence §5.1 |
| PLAN.md §6 amend | ✅ | line 167 updated (D-097 supersede D-096 §2.3 reference + Cloudflare Phase 3+ note) |
| STATE.md sync | ✅ | 4 row touched (最后更新 / 已锁定决定数 / 下一会话 / 当前阶段) |
| Session log on disk | ✅ | this file complete through Turn 7 |
| Evidence on disk | ✅ | `firewall_setup_2026-05-19.md` ~190 行 |
| Rule A 抽检 | n/a | no LLM rewrite >50% |
| Rule B 失败归档 | 0 attempt | 3 inline corrections (REALM Unicode → ASCII / CLI git-branch empty string / classifier denials → user gates) — none requires failure archive (no scrapped artifact) |
| Rule C (Phase retro) | n/a | mid-Phase |
| Rule D Writer ≠ Reviewer | ✅ | Writer = main session; Reviewer = user terminal path α (3 ACK gates: `Lock per recommendation` + `授权 vercel preview env add` + `授权 vercel --prod`) |
| OPEN OQ count | 3 unchanged | Phase 1 后续 / Phase 3-4 范围 |

---

## 6. Final commit plan (G3=a 1 atomic)

| File | Type | Lines |
|---|---|---|
| `docs/decisions/D-097-firewall-mechanism-revision.md` | new | ~280 |
| `apps/web/src/middleware.ts` | new | 35 |
| `apps/web/src/__tests__/middleware.test.ts` | new | 95 |
| `docs/phase2/PLAN.md` | modified | §6 line 167 amend |
| `docs/STATE.md` | modified | 4 row sync |
| `docs/discussion/2026-05-19-session-36.md` | new | ~330 (7 turn full log) |
| `evidence/phase2/step_04_ai_sdk/firewall_setup_2026-05-19.md` | new | ~190 |

Excluded from git (correct):
- `apps/web/.env.local` (gitignored via `.env*` pattern in `apps/web/.gitignore`)
- Vercel env vars (out-of-band state on Vercel platform; documented in evidence)

**Push**: pending user `push it` gate per Sessions 27-35 pattern.

---

**Session 36 ready to close — α firewall LIVE on prod, pending user commit + push gates**
