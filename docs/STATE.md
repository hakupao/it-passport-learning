# 项目当前状态 / Project Live State

> **本文件 = "当前累计状态"的真相源**。Session 日志是历史档案（append-only，记录每一步如何达成）；本文件是当下事实快照。两者关系由 **D-028** 锁定。
>
> **更新规则**: 每场 session 结束前 Claude 必须 sync 到本文件（per **D-027** 第 5 条「关 session 前自检」）。读者优先读本文件。

| 字段 | 值 |
|---|---|
| 最后更新 | **2026-05-22 Session 56 (CLOSE) — Phase 4 Module B 实施阶段 ✅ COMPLETE (B.1 + B.2 + B.3 all DONE); mid-session pivot to D-104 (3-way env-routable tutor brain matrix) + D-105 (Phase 2 deepseek-chat/-reasoner V4 migrate) supersedes D-102 §7.2 — locked-decisions count 103 → 105**. User gate G2 second-half `开始 Phase 4 Module B Step B.3` opened cost dry-run gate; survey found no ANTHROPIC_API_KEY in `.env.local`; user pivoted: "deepseek + anthropic + chatgpt plan 代理，可以切换" + DeepSeek V4 pro default + thinking:enabled high effort + cache markers preserved + D-103 \$15 cap unchanged. D-019 §3a slow-pace 3Q round-2 + Context7 verification (V4 pro callable + legacy chat/reasoner deprecation 2026-07-24 = D-095 §2.5(ε) tripwire FIRE → D-105 ADR + @ai-sdk/deepseek 2.0.35 supports thinking + reasoningEffort) preceded ADR locks. **D-104 LOCKED Turn 6** = tutor brain matrix: deepseek (V4 pro default + thinking:enabled high → max on escalate) + anthropic (Sonnet 4.6 default / Opus 4.7 escalation toggle; key not required in dev) + openai (reserved-stub Phase 5; LLM_PROVIDER_TUTOR=openai throws at runtime); env var `LLM_PROVIDER_TUTOR` separate from `LLM_PROVIDER` per LD-Module-B-10; cache_control markers preserved as DUAL-PURPOSE per LD-Module-B-5 (no-op on DeepSeek + active on Anthropic toggle); D-103 \$15 cap unchanged per Q3 round-1 affirmation. **D-105 LOCKED Turn 7** = Phase 2 four routes (`/api/{chat, quiz/explain, glossary/hover, hello-ai}`) migrate from legacy `deepseek-chat`/`deepseek-reasoner` → `deepseek-v4-flash` with route-handler-level `providerOptions.deepseek.thinking.type` injection per §2.1 table; execution = B.4 single-commit atomic pivot per §2.4 ordering; D-085 §2.4 frozen surface preservation explicitly documented (model param change ≠ behavior change; same disposition pattern as D-095 supersede of D-088 §2.1). **B.1 revision Session 56** = provider.ts MOD getTutorModel now 3-way env-routable (DeepSeek V4 pro default + Anthropic toggle + OpenAI throw-stub) + getActiveTutorProvider env reader + getTutorProviderOptions thinking + reasoningEffort builder + DeepseekRole = Exclude<ModelRole,"tutor"> preserved; provider.test.ts +22 cases (D-104 routing + getTutorProviderOptions + LD-Module-B-14 nested-usage fallback). **B.2 revision v2 Session 56 per LD-Module-B-13** = TUTOR_SYSTEM_INSTRUCTION bulked from ~150 → ~1278 tokens (4472 chars, supersedes LD-Module-B-6 minimal rationale empirically falsified by 0% Anthropic cache hit on v2 dry-run; new content = ITパスポート 3-area curriculum framework + 3-step pedagogical pattern + citation conventions + anti-hallucination guards + reply style); tutorPrompt.test.ts +7 LD-Module-B-13 cases (curriculum areas + IPA mention + pedagogical pattern + citation conventions + chapter nn format) + REPLACED LD-Module-B-9 inline-snapshot with length-threshold + key-phrase assertions; tutorPrompt.ts docstring updated. **B.3 cost dry-run Session 56 = 4 attempts** (v1 timeout 30→15 calls reduced + incremental persist added / v2 0% Anthropic cache surfaced threshold issue → LD-Module-B-13 / v3 post-bulk DeepSeek 70% agg + Anthropic still 0% → SDK reader bug discovered via anthropic-debug.test.ts diagnostic → LD-Module-B-14 reader fallback / v4 post-fix Anthropic 72% cache_read of input) — **D-103 §2.4 ≥80% target MET on per-turn steady-state both providers**: DeepSeek turn 2: 80.9% + turn 3: 80.2%; Anthropic warm-cache hits 100% across all turns (~72% honest cache_read of total input). Latency profile: DeepSeek V4 pro thinking:high 31s/call avg / Anthropic Sonnet 4.6 7.7s/call avg. Cost actuals: DeepSeek $0.014 + Anthropic $0.10 + earlier attempts ~$0.24 = **~$0.35 真 B.3 total** vs $15 cap = 42× headroom; Phase 4 projection ~$0.13 DeepSeek / ~$0.96 Anthropic = 15-114× headroom. **LD-Module-B-14 readCacheUsage nested-usage fallback** also benefits Phase 2 routes (β-tripwire telemetry — chat / quiz/explain / glossary/hover / hello-ai all use this helper). Phase 2 frozen surface untouched per D-085 §2.4. **Gates all green**: vitest **416/416 PASS + 2 skipped** (tutor-cost-dryrun opt-in via RUN_TUTOR_DRYRUN=1; +26 net from Session 55 close 390); tsc clean first try (Exclude<ModelRole,"tutor"> + new helper types + nested-usage typing all sound); eslint 0 errors; next build green 23 static pages 1.2s. **Bundle invariants vs Module A close**: Middleware 44.2 kB / Shared First Load JS 102 kB / /[locale]/chat 169 kB (Phase 2 D-085 §2.4 invariant preserved through D-104 + D-105 + B.2 v2 + B.3 + reader fix) / /[locale]/quiz 120 kB / /[locale]/book 121 kB — all UNCHANGED. **14 in-source LDs LD-Module-B-1~14** all captured in evidence/phase4/module_b_step_03_cost_dryrun/{design_notes.md + tree_outline.md + summary.md} + provider.ts/tutorPrompt.ts docstrings. **0 new ADR equivalents below D-104/D-105**: LD-Module-B-13/14 in-source per D-094 §2.1 (Anthropic 1024-token threshold + AI SDK shape asymmetry are implementation patterns of locked D-103 §2.4/D-104 §2.3, not D-NNN-worthy individually). **γ tripwire Phase 4 N=6 mean -48% under midpoint** (Module A N=3 -70% + Module B N=3 -26%); B.3 first OVER-midpoint datapoint at +67%; PLAN.md §5 hypothesis "new-infra reverts to midpoint × 1.0" validated empirically. **β tripwire Phase 4 N=1 OPEN** with both provider buckets seeded; LD-Module-B-14 retroactively improves Phase 2 N=14 cumulative going forward. **δ runtime detector LIVE silent** via tripwire.ts. **ε model release tripwire FIRE** absorbed by D-105 (legacy deepseek deprecation 2026-07-24). **α model deprecation tripwire FIRE** absorbed by D-105 (same event). **Cost tripwire** silent ($0.35 spent of $15 cap = 42× headroom; $10 tripwire silent). **Phase 1+2+3 ✅ FROZEN preservation intact**: tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` + `phase3-α-ship-2026-05-22` immutable; D-085 §2.4 chat 169 kB invariant verified at build level; D-097 firewall 44.2 kB unchanged; D-099 next-intl additive only; D-095 §2.3 stable-prefix invariant + buildMessagesWithStablePrefix untouched; D-105 Phase 2 migrate is scheduled for B.4 atomic commit, not landed yet. **0 Rule A 抽检** (B.2 bulk is greenfield content addition, not >50% compression/rewrite). **0 Rule B 失败归档 filed** despite 4 dry-run attempts — per D-094 §2.4 the v1-v3 outcomes were design DISCOVERIES that immediately produced corrective LDs (B-13 + B-14), not failed attempts per Rule B's "LLM/user grading as wrong" criterion. **Rule C** retro at Phase 4 close (D.3). **Rule D Writer ≠ Reviewer** build-time chain fired. Excluded `.claude/` (gitignored locally). **Push pending user `push it` gate** per Sessions 27-55 convention. [LEGACY Session 55 (CLOSE)]: **2026-05-22 Session 55 (CLOSE) — Phase 4 Module B 实施阶段 B.1 + B.2 ✅ DONE; B.3 cost dry-run user-pending per CLAUDE.md "first LLM API call gate" + PLAN.md §4 G2 — user explicit `开始 Phase 4 Module B` gate G2 signal opened module 2026-05-22**. Files: `apps/web/src/lib/ai/provider.ts` MOD (+ ModelRole extended with "tutor"; + ANTHROPIC_TUTOR_DEFAULT_MODEL_ID="claude-sonnet-4-6" + ANTHROPIC_TUTOR_ESCALATION_MODEL_ID="claude-opus-4-7" per D-102 §7.2; + GetTutorModelOptions + getTutorModel(opts) anthropic-pinned ignoring LLM_PROVIDER per LD-Module-B-2; + DeepseekRole=Exclude<ModelRole,"tutor"> per LD-Module-B-4; + getModel routes "tutor"→getTutorModel before provider switch per LD-Module-B-3) + `apps/web/src/lib/ai/__tests__/provider.test.ts` MOD (+ getTutorModel import + 2 getModel tutor cases + 3 getTutorModel cases; 19→25 cases) + `apps/web/src/lib/ai/tutorPrompt.ts` NEW (~150 lines: TUTOR_SYSTEM_INSTRUCTION 5-paragraph ~150-token domain-anchored persona per LD-Module-B-6; formatTutorPreamble(ctx) deterministic markdown-heading projection with `(count)` summaries + `(none)` empty markers per LD-Module-B-7; buildTutorMessages(ctx, conversation) composes [system SYSTEM, system preamble, ...conversation] with cache_control:ephemeral on BOTH system messages per LD-Module-B-5 nested-breakpoint layout — outer = SYSTEM invariant across users / inner = preamble per-session) + `apps/web/src/lib/ai/__tests__/tutorPrompt.test.ts` NEW (~190 lines; 19 vitest cases: 5 SYSTEM stability incl. inline-snapshot lock per LD-Module-B-9 + 6 formatTutorPreamble projection + 8 buildTutorMessages cache + composition). **Gates all green**: pnpm exec vitest run → 24 test files / **390/390 PASS** in 692ms (was 366 at Module A close → +24 = +5 provider tutor + +19 tutorPrompt); pnpm exec tsc --noEmit clean first try (noUncheckedIndexedAccess strict mode survives cache_control providerOptions typing); pnpm exec eslint src 0 errors / 0 warnings; pnpm exec next build 23 static pages all green in 1205ms. **Bundle invariants vs Module A close baseline**: Middleware 44.2 kB UNCHANGED; Shared First Load JS 102 kB UNCHANGED; /[locale]/chat First Load 169 kB UNCHANGED (Phase 2 D-085 §2.4 invariant preserved through B.1+B.2); /[locale]/quiz 1.57 kB / 120 kB UNCHANGED; /[locale]/book 1.38 kB / 121 kB UNCHANGED; /[locale]/book/chapter/[nn] 5.13 kB / 181 kB UNCHANGED (tutorPrompt.ts is server-only — referenced only by future /api/tutor route in B.4 — so zero client-bundle impact). **9 in-source LDs LD-Module-B-1~9** captured in `evidence/phase4/module_b_step_01_model_lock/design_notes.md` + `evidence/phase4/module_b_step_02_system_preamble/design_notes.md`. **γ tripwire rows #19 + #20**: B.1 estimate 60 min midpoint / actual ~20 min = **−67% under**; B.2 estimate 135 min midpoint / actual ~30 min = **−78% under**; Module-B partial N=2 mean **−72.5% under midpoint**; Phase 4 N=5 cumulative (Module A N=3 + Module B N=2) mean **−71% under**. **PLAN.md §5 hypothesis ("new-infra reverts to midpoint × 1.0") remains unfalsified** — B.1+B.2 are typing + constant + pure-builder work, same composition profile as Module A; the meaningful new-infra wall-cost datapoints are B.3 (stream-fire + cache-telemetry instrumentation) + B.4 (/api/tutor endpoint with real stream handling). **β tripwire**: NEW Anthropic-cache bucket OPENS at B.3 dry-run (~10 mock conversations target ≥80% input-token cache hit ratio per D-103 §2.4); first end-to-end empirical row at B.4 ship; full prod table at D.2. **Cost ledger Module B B.1+B.2**: \$0.00 LLM cost (typing + constant + pure helpers — zero API call); cumulative all phases unchanged ~\$0.66 vs \$15 D-103 cap = **22.7× headroom**; B.3 user gate G2 is the first Anthropic call gate per CLAUDE.md. **Phase 1+2+3 ✅ FROZEN preservation**: tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` + `phase3-α-ship-2026-05-22` immutable; D-085 §2.4 chat 169 kB invariant verified at build level through Module B B.1+B.2; D-097 firewall 44.2 kB unchanged; D-099 next-intl additive only; tutorPrompt SYSTEM is NEW byte-stable prefix (different cache namespace from Phase 2 chat/quiz/hover SYSTEMs per Anthropic ephemeral cache contract). **0 new ADR Session 55** (LD-Module-B-1~9 are in-source per D-094 §2.1; 103 locked decisions D-001~D-103 unchanged). **0 OQ changes Session 55** (count remains 2: OQ-01 + OQ-02 Phase 1 carryover; Module B B.1+B.2 execute locked D-102 + D-103 + PLAN.md). **0 Rule A 抽检** (no >50% compression/rewrite — typing + constant authoring + pure helpers; greenfield in NEW tutorPrompt.ts file). **0 Rule B 失败归档** (gates clean on first try across both steps — provider.test.ts 25/25 PASS first run; tutorPrompt.test.ts 19/19 PASS first run; full suite 390/390 PASS). **Rule C** RETROSPECTIVE_phase4.md ⏸ at Module D Step D.3 close (per PLAN.md). **Rule D Writer ≠ Reviewer**: build-time reviewer chain fired (vitest + tsc + eslint + next build); LD-Module-B-9 inline-snapshot lock acts as drift-detector reviewer for the SYSTEM cache key; browser-based reviewer chain (Playwright + axe + Lighthouse) engages at Module D ship. Excluded: `.claude/` (gitignored locally). **Push pending user `push it` gate** per Sessions 27-54 convention. [LEGACY Session 54 (CLOSE)]: **2026-05-22 Session 54 (CLOSE) — Phase 4 Module A 实施阶段 ✅ DONE — A.1 (TutorContext types + projection helpers) + A.2 (persistQuizOutcome + `<QuizExplain />` self-report wire + i18n) + A.3 (loadTutorContext composer) all landed in one sitting per user explicit `开始 Phase 4 Module A` gate G1 signal 2026-05-22 (Session 53 entry option (b) whole-module-1-sitting path)**. Files: `apps/web/src/lib/tutor/tutorContext.ts` NEW (~150 lines: A.1 types `TutorContext` + `QuizAttempt` + `DEFAULT_RECENT_QUIZ_LIMIT` + `projectChapterStatuses` (3-bucket completed/inProgress/pending preserving source order per LD-Module-A-2) + `projectRecentQuiz` (desc by lastAnswered + qid tiebreaker per LD-Module-A-2c); A.3 `LoadTutorContextOptions` + `loadTutorContext` composer SSR-safe via StorageLike per LD-Module-A-3 Phase 3 LD-Step3-A pattern) + `apps/web/src/lib/tutor/__tests__/tutorContext.test.ts` NEW (~290 lines; 18 vitest cases: 6 projectChapterStatuses + 7 projectRecentQuiz + 5 loadTutorContext) + `apps/web/src/lib/book/progressStore.ts` MOD (+`persistQuizOutcome` helper composite load → recordQuizAnswer → save; +29 lines) + `apps/web/src/lib/book/__tests__/progressStore.test.ts` MOD (+4 cases; +43 lines) + `apps/web/src/components/QuizExplain.tsx` MOD (+loadProgress/persistQuizOutcome imports; +`selfReport` state slot; +rehydrate useEffect on summary change; +handleSelfReport useCallback; +self-report UI block inside scrollable body at `phase === 'done'` per LD-Module-A-1 — honest signal absent picker UX since Phase 2 QuizList/QuizExplain has no user-pick interaction; D-085 §2.4 frozen contract honored — modal lifecycle unchanged + frame footer untouched; emerald-600 correct / amber-600 wrong per LD-Module-A-1f; +90 lines) + `apps/web/messages/{ja,zh,en}.json` MOD (+3 keys × 3 locales = 9 keys: selfReportPrompt + selfReportCorrect + selfReportWrong). **Gates all green**: pnpm exec vitest run → 23 test files / **366/366 PASS** in 692ms (was 344 at Phase 3 close → +22 = +18 tutorContext + +4 persistQuizOutcome) / pnpm exec tsc --noEmit clean first-try / pnpm exec eslint src 0 errors 0 warnings / pnpm exec next build 23 static pages green compile 1433ms. **Bundle invariants vs Phase 3 close**: Middleware 44.2 kB UNCHANGED / Shared First Load 102 kB UNCHANGED / **`/[locale]/chat` First Load 169 kB UNCHANGED** (Phase 2 D-085 §2.4 frozen contract preserved through Module A) / `/[locale]/quiz` Size 1.57 kB UNCHANGED (self-report UI absorbed in tolerance — loadProgress/persistQuizOutcome already in progressStore chunk graph from Phase 3 ChapterCompletionGate/BookProgressSummary islands) / `/[locale]/quiz` First Load 120 kB UNCHANGED / `/[locale]/book` 1.38 kB / 121 kB sub-noise. **13 in-source LDs Session 54** (LD-Module-A-1[a-f] self-report wire / LD-Module-A-2[a-e] TutorContext shape + projection invariants / LD-Module-A-3[a-d] loadTutorContext composer) per D-094 §2.1 documented in 3 `evidence/phase4/module_a_step_NN_*/design_notes.md` files. **8 Tier 3 evidence files** at `evidence/phase4/module_a_step_01_tutor_context/` (tree_outline + design_notes) + `module_a_step_02_record_quiz_wire/` (tree_outline + design_notes) + `module_a_step_03_load_tutor_context/` (tree_outline + build_log + test_results + design_notes — close-gate evidence shared across A.1+A.2+A.3 per whole-module-1-sitting convention). **γ tripwire Module-A N=3**: A.1 -72% / A.2 -56% / A.3 -83% / mean **-70% under midpoint** (below Phase 3 mean -59%; PLAN.md §7 STATUS amended with interpretation: Module A behaves like composition since data layer projects existing Phase 3 progressStore — Module B new-infra surface may revert to midpoint × 1.0 as predicted by RETROSPECTIVE_phase3 §3.6). **β tripwire** N=14 cumulative unchanged (no LLM calls Module A — first Anthropic call at Module B Step B.3 gate G2). **δ runtime detector** LIVE silent preserved. **ε/α/cost** tripwires no fire. **Cost tracking**: \$0.00 LLM cost incurred Module A (types + wire-up + projection helper — no API call); cumulative all-phases unchanged \~\$0.66 真 vs \$15 D-103 cap = **22.7× headroom**. **0 new ADR Session 54** (LD-Module-A-1/2/3 are in-source per D-094 §2.1; 103 locked decisions D-001 → D-103 unchanged from Session 53). **0 OQ changes** (global count remains 2: OQ-01 + OQ-02 Phase 1 carryover both still open). **0 Rule A 抽检** (no >50% compression / rewrite). **0 Rule B 失败归档** (gates clean on first try across all 3 steps). **Rule C** Phase 4 retro at Phase 4 close per PLAN.md §1 row D.3 — future commitment. **Rule D Writer ≠ Reviewer** build-time reviewer chain (vitest + tsc + eslint + next build) fired at module close; browser-based reviewer chain (Playwright + axe-core + Lighthouse) engages at Module D ship. **Phase 1 + Phase 2 + Phase 3 ✅ FROZEN posture preserved** — 3 tags immutable; D-085 §2.4 QuizExplain contract honored (additive leaf UI in scrollable body; modal frame + lifecycle untouched + chat 169 kB invariant preserved); D-097 firewall 44.2 kB unchanged; D-099 next-intl additive only (9 new keys across 3 locales). Session 54 commit plan: 1 atomic covering 12 files (2 new code + 6 mod code + 8 evidence + 3 docs + this STATE entry) — push pending user `push it` gate per Sessions 27-53 convention. **Session 55 entry options** (per session-54 log §9 + PLAN.md §4 gates): (a) `开始 Phase 4 Module B` (gate G2 opens; B.1 model lock + B.2 SYSTEM + B.3 cost dry-run **explicit user approval gate** + B.4 `/api/tutor` ship) / (b) `开始 Phase 4 Module B Step B.1` (just B.1, defer rest) / (c) `push it` (origin sync only, no deploy yet) / (d) `hold` / (e) `adjust PLAN.md`. → legacy: **2026-05-22 Session 53 Turn 6 (CLOSE) — Phase 4 设计阶段 ✅ COMPLETE — D-102 (Phase 4 形态 = AI 学习助手) + D-103 (Phase 4 α-silent cap \$5 → \$15) both LOCKED; `docs/phase4/PLAN.md` on disk (4 modules × ~3-4 steps; ~14 steps total)**. Round-2 4Q OQ-A/B/C/D answered Session 53 Turn 5 (OQ-A signals = chapters+quiz only progressStore baseline / OQ-B infra = Anthropic Claude Sonnet 4.6 default + Opus 4.7 fallback + ephemeral prompt caching via `@anthropic-ai/sdk` reuse / OQ-C 4-modules = Data → Brain → Surface → Ship Phase 2 A/B/C/D mirror / OQ-D cost envelope = lift cap to \$15 via D-103 ADR midpoint of \$10-25 range). D-102 §7 'Round-2 close' appended Session 53 Turn 6 (§7.1-§7.6 OQ-A/B/C/D resolutions + PLAN.md reference + status update); D-102 Status: LOCKED partial → LOCKED full. D-103 cost ADR NEW Session 53 Turn 6 (Phase 4-scoped supersede of D-090 §2.x; Anthropic Sonnet 4.6 unit cost ~40× DeepSeek input / ~14× output → Phase 4 projected ~\$2-5 真 → cumulative all-phases ~\$3-6 vs new \$15 cap = 2.5-5× headroom; ≥80% Anthropic ephemeral cache hit ratio MANDATORY per D-103 §2.4; cost tripwire at \$10 = 66% of cap per D-103 §2.5; per-call user gate per CLAUDE.md unchanged). PLAN.md §4 gate sequence: G1 Module A 实施 / G2 Module B cost dry-run + user approval / G3 Module B Step B.4 real LLM ship / G4 Module C 实施 / G5 Module D `push it` / G6 `freeze phase 4 and tag` / G7 cost tripwire ≥\$10. PLAN.md §5 tripwire baseline: γ = midpoint × 1.0 (Phase 1/2 unit cost model returns per RETROSPECTIVE_phase3 §3.6; NOT Phase 3 × 0.4); β re-opens at Module D prod deploy NEW bucket (Anthropic-cache vs DeepSeek-prefix mechanism); δ LIVE silent; ε/α no fires expected. **Phase 1 + Phase 2 + Phase 3 ✅ FROZEN posture preserved** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` + `phase3-α-ship-2026-05-22` immutable; D-102 + D-103 strictly additive; AI 学习助手 surface additive on top of frozen 4 surfaces (chat / quiz / glossary / book); Module A Step A.2 `recordQuizAnswer` wire (RETROSPECTIVE_phase3 §2.3 holdover #1 promoted) is **additive** on `<QuizExplain />` (D-085 §2.4 frozen contract honored — wire calls a helper, doesn't refactor the modal). Decision chain: D-001 → D-103 = 103 ADRs locked (+2 ADR Session 53). 0 new in-source LDs Session 53 (设计阶段 — LD-N appear at Module A 实施 G1). Session 53 commit plan: 1 atomic covering D-102 + D-103 + PLAN.md + STATE.md + this file; push pending user `push it` per Sessions 27-52 convention. Session 54 entry options per session-53 log §11: (a) `开始 Phase 4 Module A Step A.1` types-only no cost / (b) `开始 Phase 4 Module A` whole module 1 sitting / (c) `cost dry-run` frontload cost validation / (d) `adjust PLAN.md` / (e) `hold` review D-102 + D-103 + PLAN.md before 实施. → legacy: **2026-05-22 Session 53 Turn 1-4 — Phase 4 设计阶段 OPEN per user explicit `open phase 4` gate signal (via /clear + `open phase 4 — 由于Claude故障而中止，请你继续`, recovery from interrupted earlier Session 53 attempt) per CLAUDE.md 'Phase/stage signaling' + Session 52 §7 entry option (c). D-019 §3a round-1 4Q answered Session 53 Turn 2 (Q1 Form = **AI 学习助手** (D-083 §2.5 deferred form lifted) / Q2 Infrastructure envelope = **Major new infrastructure** (new model endpoint / DB / SDK) / Q3 Phase 3 holdovers = **Separate polish list** (form work first; RETROSPECTIVE §2.3 items tracked but not blocking) / Q4 Step plan granularity = **4 modules × N steps** (Phase 2-scale ceremony)). User picked the **heaviest** available option in 3 of 4 dimensions = deliberate big-investment Phase 4. **D-102 (Phase 4 形态锁) LOCKED partial Session 53 Turn 3** at `docs/decisions/D-102-phase4-form-ai-tutor.md` per D-027 §1 + D-029 (form + infra envelope + holdover treatment + step granularity locked; 9 rejected alternatives explicitly listed covering each round-1 Q option the user did not pick; OQ-A/B/C/D round-2 reserved for specifics — personalization signals / model+SDK pick / 4-module decomposition / cost+β envelope). γ tripwire prediction per RETROSPECTIVE_phase3 §3.6: Phase 4 new-infra path reverts to Phase 1/2 unit cost model (midpoint × 1.0 NOT Phase 3's × 0.4); PLAN.md estimates must NOT use Phase 3 multiplier. Cost gate: Phase 4 first LLM API call (likely Module B development) needs explicit user approval per CLAUDE.md. Cumulative cost across all phases ~\$0.66 真 vs \$5 D-090 α-silent cap = 7.6× headroom (unchanged from Phase 3 close; Phase 4 may require D-103 cost projection ADR pending OQ-B + OQ-D round-2). β re-opens on Phase 4 Module D prod deploy with new model paths (existing N=14 cumulative from Phase 2 carries forward; Phase 4 likely opens a new β bucket since SYSTEM differs). Phase 1 + Phase 2 + Phase 3 ✅ FROZEN posture **preserved** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` + `phase3-α-ship-2026-05-22` immutable; D-102 strictly additive; AI 学习助手 surface additive on top of frozen 4 surfaces (chat / quiz / glossary / book). Tier 3 ceremony confirmed (Phase 2-scale) — full Phase 4 RETROSPECTIVE per Rule C at close; per-module evidence under `evidence/phase4/module_X_step_NN_*/`. D-019 §3a round-2 4Q (OQ-A/B/C/D) posted Session 53 Turn 4 (THIS TURN) — awaiting user answer. **On round-2 lock**: D-102 §2 amended in-place with round-2 answers + ADR finalizes + `docs/phase4/PLAN.md` written + Module A 实施 gate pending user signal. → legacy: **2026-05-22 Session 52 — Phase 3 Step 3 ✅ DONE + Phase 3 ✅ COMPLETE (Progress + RETROSPECTIVE / D-101 §2.4 + LD-3 + LD-Step3-A~H)** — `apps/web/src/lib/book/progressStore.ts` NEW (pure helpers: StorageLike abstraction + emptyProgress + schemaVersion=1 envelope `{schemaVersion, chapters:{nn:{scrollY?, completedAt?}}, quiz:{qid:{lastAnswered, correct}}, updatedAt}` + storage key `itp:book:progress:v1` + loadProgress with corrupt-tolerant fallback + saveProgress with schemaVersion enforcement + clearProgress + markChapterCompleted IDEMPOTENT (returns same reference when completedAt already set per LD-Step3-B) + recordChapterScroll overwrite semantics + recordQuizAnswer overwrite semantics SHIPPED-AS-HELPER-ONLY per LD-Step3-H to preserve D-085 §2.4 frozen QuizExplain surface + isChapterCompleted + isChapterInProgress + countCompletedChapters iterating supplied nn list per LD-Step3-F future-proofing against corpus updates) + `apps/web/src/lib/book/__tests__/progressStore.test.ts` NEW (28 vitest cases covering emptyProgress + load cold/round-trip/parse-error/schemaVersion-mismatch/chapters-non-object/chapters-as-array/getItem-throws/custom-key + save envelope shape + schemaVersion tamper-resistance + setItem-throws swallow + clear default-key + clear idempotent + clear removeItem-throws + mark new stamp + mark idempotent ref-equality + mark preserves scrollY + mark cross-chapter non-collision + recordChapterScroll new/overwrite/preserves-completedAt + recordQuizAnswer new/overwrite + isChapterCompleted + isChapterInProgress + countCompletedChapters ignores stale ids). `apps/web/src/components/ChapterCompletionGate.tsx` NEW (client island per LD-Step3-D mount-gate; IntersectionObserver 1×1 sentinel at panel top, sticky-once via obs.disconnect on first hit per LD-Step3-C; button states: pre-mount disabled-loading → ready → completed-badge with localized timestamp via useFormatter; aria-live=polite mirror on button for SR state transition; emerald-600 completed swatch with checkmark glyph aria-hidden). `apps/web/src/components/BookProgressSummary.tsx` NEW (2 exports per LD-Step3-E: BookProgressSummary header X/N overlay role=status aria-live=polite + ChapterProgressPill per-card 3-state pill completed/in-progress/not-started — both mount-gated to avoid SSR mismatch; separate islands rather than React context provider keeps server shell deterministic at α-private scale). `apps/web/src/components/ChapterReader.tsx` MOD (+ChapterCompletionGate mounted between ChapterEndPanel and SelectionToolbar — sentinel naturally sits past chapter body content; lang=ja body preserved). `apps/web/src/components/BookIndex.tsx` MOD (header +BookProgressSummary nns={chapters.map(c=>c.nn)}; each card +ChapterProgressPill nn={c.nn}; server shell preserved). `apps/web/messages/{ja,zh,en}.json` MOD (+Book.* 13 keys each: progressSummary {done}{total} / chapterCompleted / chapterInProgress / chapterNotStarted / completionPanelLabel / completionPanelTitle / markCompleted / markCompletedReady / markCompletedGateHint / completedBadge / completedAt {date} — fully trilingual matching Phase 2 + Step 1/2 i18n discipline). **Gates all green**: `pnpm exec vitest run` → 22 test files / 344 tests passed (was 316 → +28 progressStore) in 911ms; `pnpm exec tsc --noEmit` clean on first try (noUncheckedIndexedAccess strict mode); `pnpm exec eslint src` 0 errors / 0 warnings; `pnpm exec next build` 23 static pages all green. **Bundle invariants vs Step 2 Session 51 baseline**: Middleware 44.2 kB UNCHANGED; Shared First Load JS 102 kB UNCHANGED; `/[locale]/book` Size 172 B → 1.3 kB / First Load 119 → 121 kB (+1.13 kB / +2 kB driven by BookProgressSummary + ChapterProgressPill islands); `/[locale]/book/chapter/[nn]` Size 4.57 → 5.57 kB / First Load 180 → 181 kB (+1.00 kB / +1 kB driven by ChapterCompletionGate island); `/[locale]/chat` 1.93 kB / 169 kB UNCHANGED (Phase 2 D-085 §2.4 invariant preserved zero regression); `/[locale]/quiz` 1.57 kB / 120 kB UNCHANGED; `/[locale]/glossary` 4.72 kB / 119 kB UNCHANGED (+0.01 kB sub-noise i18n key addition). **8 in-source LDs LD-Step3-A~H** per D-094 §2.1 + D-080 v1.1 §8 documented in `evidence/phase3/step_03_progress/design_notes.md §3` — implementation patterns of locked D-101 NOT D-NNN-worthy individually. **4 evidence files** at `evidence/phase3/step_03_progress/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}` per Tier 3 Phase 1+2+Step 1+2 同构 + **RETROSPECTIVE_phase3.md** at repo root per Rule C (5 sections: §1 保留下来的做法 / §2 必须补上的缺口 + β graduation queue carried + Phase 4 hand-off list / §3 关键决策复盘 / §4 tripwire final tables / §5 final posture + 4 user-action options). **γ tripwire row #15 = third Phase 3 datapoint = third under-estimate −53%** (PLAN midpoint 150 min vs actual ~70 min); Module Phase-3 N=3 final mean −59% under midpoint = composition-on-frozen-surfaces work plans at midpoint × 0.4 for Phase 4. **β tripwire** N=14 cumulative unchanged (Step 3 zero new /api/* call paths; re-opens at user-gated Vercel deploy). **δ runtime detector** LIVE silent preserved. **ε / α** no fires. **Phase 1 + Phase 2 ✅ FROZEN posture preserved** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable; D-101 + Steps 1 + 2 + 3 additive composition on top of frozen Phase 2 components (chat / quiz / glossary surfaces + /api/* endpoints + middleware D-097 firewall + D-099 next-intl chrome + D-085 historyStore + D-095 §2.3 stable-prefix invariant all untouched; progressStore is parallel store NOT extending historyStore). **0 new ADR Session 52** / **0 OQ changes** / **0 Rule A 抽检** / **0 Rule B 失败归档**. **Rule C** Phase 3 retro at /RETROSPECTIVE_phase3.md ✅ DONE. **Rule D** Step 3 stays at build-time gate level until user-gated Vercel deploy enables browser-based reviewer chain. New session-52 log `docs/discussion/2026-05-22-session-52.md` + Phase 3 PLAN.md Step 3 row → ✅ DONE + §7 STATUS final + STATE.md 5-anchor sync with Session 51 preserved per D-028. Excluded: .claude/ gitignored locally. **Phase 3 ✅ COMPLETE** — Steps 1 + 2 + 3 DONE. Vercel deploy + Playwright + tag `phase3-α-ship-2026-05-22` pending user `push it`/`freeze` gate per Session 48 precedent. **Push pending user gate** per Sessions 27-51 pattern. *(legacy Session 51)* **2026-05-22 Session 51 — Phase 3 Step 2 ✅ DONE (Inline triggers / D-101 §2.3 + §2.5 + LD-2 + LD-Step2-A~H)** — `apps/web/src/lib/book/translatePrompt.ts` NEW (pure helpers: `composeTranslatePrompt({source,target:"zh"|"en"})` returns translate-only user-message body with explicit "ONLY the translation — no preamble / no romaji" instructions + ja-source between `---` fences; `clampTranslateSource()` hard caps source at 1000 chars with `truncated` flag; `composeChapterScopePreface({nn,titleJp,firstPage,lastPage})` emits `[Scope: 第NN章「title」 p.A-B] ` marker; `applyChapterScope()` idempotent prepend) + `apps/web/src/lib/book/__tests__/translatePrompt.test.ts` NEW (14 vitest cases: zh + en target labels / fences / no-preamble + no-romaji instructions / trim / empty throws / clamp boundary at exact cap + over cap / preface canonical shape + zero-padded nn / applyChapterScope idempotency + cross-chapter non-collision). `apps/web/src/components/ParagraphTranslate.tsx` NEW (modal: useChat() against /api/chat seeded with composeTranslatePrompt once per request; original ja text on top + streamed assistant translation below; lang="zh-Hans"/lang="en" on output for screen-reader voice switching; aria-busy + focus trap + ESC + scroll lock + retry on error; truncation amber banner when source > 1000 chars). `apps/web/src/components/SelectionToolbar.tsx` NEW (document-level `selectionchange` listener with rAF debounce; scope guard via `data-chapter-content="true"` data-attribute walk on anchor + focus nodes; floating toolbar position computed from `range.getBoundingClientRect()` with above/below heuristic + 8px viewport clamp; 译中/译英 buttons mousedown-prevent-default to not collapse selection; ESC clears selection; hides on scroll if selection collapses). `apps/web/src/components/ChapterChatModal.tsx` NEW (small modal `<Chat />` analog using useChat() against /api/chat; first user message text passed through `applyChapterScope()` so scope marker rides INSIDE the user message body — SYSTEM stays byte-identical preserving D-088 §2.3 + D-095 §2.3 stable-prefix cache invariant; separate from Phase 2 `<Chat />` to avoid corrupting D-085 §2.2 global Resume history; ephemeral lifecycle — close = forget). `apps/web/src/components/ChapterQuizPicker.tsx` NEW (compact picker listing QuizSummary[] within chapter page range; clicking a card opens existing Phase 2 `<QuizExplain />` modal verbatim — z-index stacking handled by separate ESC handler; focus trap engaged on picker only when QuizExplain not stacked). `apps/web/src/components/ChapterEndPanel.tsx` NEW (always-visible 章末固定区 mounted at end of `<ChapterReader />`; 问本章 button opens ChapterChatModal seeded with chapter scope; 测本章 button opens ChapterQuizPicker; quiz button disabled when chapter has 0 questions; chapter count rendered in button label). `apps/web/src/lib/book/chapterScope.ts` MOD (+ `buildChapterQuestionSummaries(ref, index, pages): QuizSummary[]` projecting `index.entity_by_id` filtered by `r.type === "question" && r.page ∈ [first_page, last_page]`, ordered by (page, entityIndex), reusing already-loaded Page[] via Map lookup — silently skips ids whose page isn't in the provided array for graceful partial-load handling) + `apps/web/src/lib/book/__tests__/chapterScope.test.ts` MOD (+3 new cases for buildChapterQuestionSummaries: chapter-range filter + ordering / partial pages → silent skip / non-empty other chapter → []). `apps/web/src/components/ChapterReader.tsx` MOD (+chapterQuestions + titleJp props; mounts `<ChapterEndPanel scope=... questions=... />` at the end of the page list + `<SelectionToolbar />` as a sibling client island; page list container gets `data-chapter-content="true"` marker so SelectionToolbar scopes itself to body text only — NavTabs / modal text / panel buttons are excluded; lang="ja" body markup preserved per D-101 §2.3). `apps/web/src/app/[locale]/book/chapter/[nn]/page.tsx` MOD (+ `buildChapterQuestionSummaries(ref, index, pages)` call after `loadChapter()` returns; passes `chapterQuestions={chapterQuestions}` + `titleJp={ref.title_jp}` to ChapterReader; titleJp stays raw ja regardless of chrome locale per D-101 §2.3 LocaleSwitcher independence). `apps/web/messages/{ja,zh,en}.json` MOD (+Book.* 17 new keys: chapterEndPanelLabel/Title/Hint + askChapter + askChapterModalTitle + askChapterEmptyHint + quizChapter {count} + quizChapterModalTitle + quizChapterCount + quizChapterEmptyHint + translateZh/En + translateToolbarLabel + translateModalTitle {target} + translateOriginal + translateResult {target} + translateBusy + translateTruncatedHint — fully trilingual matching Phase 2 + Step 1 i18n discipline). **Gates all green**: `pnpm exec vitest run` → 21 test files / 316 tests passed (was 299 → +17 cases) in 576ms; `pnpm exec tsc --noEmit` clean (noUncheckedIndexedAccess strict mode on first try); `pnpm exec eslint src` 0 errors / 0 warnings; `pnpm exec next build` 23 static pages all green; chapter-route bundle Size 175 B → **4.57 kB** (+4.4 kB) / First Load 119 kB → **180 kB** (+61 kB driven by useChat + AI SDK + QuizExplain bundle now shared with /quiz); chat surface 169 kB UNCHANGED preserving Phase 2 invariant; Middleware 44.2 kB UNCHANGED; Shared 102 kB UNCHANGED. **8 in-source LDs LD-Step2-A~H** per D-094 §2.1 documented in `evidence/phase3/step_02_triggers/design_notes.md §3` — implementation patterns of locked D-101 NOT D-NNN-worthy individually. **4 evidence files** at `evidence/phase3/step_02_triggers/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}` per Tier 3 Phase 1+2+Step 1 同构 (deploy_log + smoke_ui + screenshots + Playwright + axe-core + Lighthouse all deferred to Step 3 prod deploy gate per Phase 2 Step 14+15 + Step 1 pattern reuse). **γ tripwire row #14 = second Phase 3 datapoint = second under-estimate** (Step 2 actual ~85 min vs PLAN midpoint 180 min = **−53% under**; tightening direction Step 1 −71% → Step 2 −53% = converging on PLAN; Module Phase-3 N=2 still insufficient for re-estimate). **β tripwire** N=14 cumulative final from Phase 2 UNCHANGED Session 51 (zero live LLM calls fired this session — gates only; β re-opens at Step 3 prod deploy). **δ runtime detector** LIVE silent. **ε / α** no fires. **Phase 1 + Phase 2 ✅ FROZEN posture preserved** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable. **0 new ADR Session 51** (8 in-source LDs LD-Step2-A~H captured in design_notes.md §3). **0 OQ changes Session 51** (count remains 2). **0 Rule A 抽检** (Step 2 is greenfield composition). **0 Rule B 失败归档** (gates clean on first try). **Rule C** Phase 3 retro deferred to Phase 3 close. **Rule D Writer ≠ Reviewer** Step 2 stays at build-time gate level. New session-51 log + Phase 3 PLAN.md Step 2 row → ✅ DONE upgrade with `actual ~85 min` amend + §7 STATUS upgrade. Push pending user `push it` gate per Sessions 27-50 pattern. *(legacy Session 50)* **2026-05-22 Session 50 — Phase 3 Step 1 ✅ DONE (Reader 壳 / D-101 §2.1-§2.5 + LD-1 + LD-Step1-A~E)** — `apps/web/src/lib/book/chapterScope.ts` NEW (pure projection / parse fns: `buildChapterSummary` / `parseChapterNn` / `chapterIdToNn` / `getChapterSiblings` / `findChapterByPage` / `projectRenderEntities`) + 20 new vitest cases in `apps/web/src/lib/book/__tests__/chapterScope.test.ts` (was 279 baseline → 299 cumulative); `apps/web/src/components/BookIndex.tsx` NEW (16-chapter TOC server component, locale-aware title via `pickTitle()`, links to `/[locale]/book/chapter/NN` with zero-padded NN); `apps/web/src/components/ChapterReader.tsx` NEW (continuous chapter-page flow rendering `Page.entities[]` via `<EntityBlock />` switch on `type` = section / figure / default; `lang="ja"` on every content body node per D-101 §2.3; prev/next sibling nav footer); `apps/web/src/app/[locale]/book/page.tsx` NEW (server component loading 16 ChapterRefs via `DataSource.loadIndex()` + projecting through `buildAllChapterSummaries()` + hydrating `<BookIndex />`, `dynamic=force-dynamic` + `setRequestLocale()` + metadata via `getTranslations`); `apps/web/src/app/[locale]/book/chapter/[nn]/page.tsx` NEW (parses NN segment via `parseChapterNn()` → 404 on malformed/out-of-range; loads chapter pages via `DataSource.loadChapter()`; projects ChapterSummary + sibling NNs; hydrates `<ChapterReader />`); NavTabs refactored (LD-1: Book chip larger `text-sm sm:text-base font-semibold` + full-fill active state; chat/quiz/glossary 视觉降级 to smaller `text-[11px] sm:text-xs` after a `·` divider with lighter `text-black/55` color + `h-7` height; aria-current preserved on Book for any path under `/book/*`; LocaleSwitcher untouched on right); `apps/web/src/app/[locale]/page.tsx` redirect target `/chat` → `/book` per D-101 §2.1 (next-intl `redirect({href:"/book",locale})` preserves locale prefix); `apps/web/messages/{ja,zh,en}.json` +Nav.book (教科書/教科书/Textbook) +Book.* keys (12 strings: title/subtitle/chapterBadge/pageRange/pageCount/open/chapterTitle/prevChapter/nextChapter/backToIndex/pageHeading/emptyHint/noEntities). Gates all green: vitest 299/299 PASS (Duration 571ms) + tsc --noEmit clean (after fixing 5 `noUncheckedIndexedAccess` strict-mode errors caught in same cycle — chapterScope.ts m[1] guard + ordered[idx±1] guard + test `out[0]` guard; documented inline at `evidence/phase3/step_01_reader/design_notes.md §8`, NOT Rule B archive grade per D-094 §2.4 distinction) + eslint src clean (0 errors / 0 warnings) + next build green 23 static pages (●●● /[locale] + ●●● /[locale]/book + ƒ /[locale]/book/chapter/[nn] for ja/zh/en + ●●● /[locale]/chat + ●●● /[locale]/quiz + ●●● /[locale]/glossary + ƒ /api/{chat,quiz/explain,glossary/hover,hello-ai} + ○ /_not-found = 1 + 3 + 3 + 1 + 3 + 3 + 3 + 4 + 1 ≈ 23); bundle invariants: Middleware 48.9 kB → 44.2 kB (−4.7 kB; tree-shake delta after Book becomes primary tab + i18n route bundle rebalance — verified via Next 15.5 build summary); /[locale]/chat First Load 169 kB unchanged from Phase 2 invariant; /[locale]/quiz + /[locale]/glossary Size 4.53 → 4.71 kB (+0.18 kB each — +Book.* message keys in next-intl bundle adds ~180 bytes per surface; well within noise band); /[locale]/book + /[locale]/book/chapter/[nn] each 175 B Size + 119 kB First Load (NEW). 5 evidence files at `evidence/phase3/step_01_reader/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}` (4 files actual — tree outline + build log + test results + design notes). γ tripwire row #13 = first Phase 3 datapoint = **first under-estimate** (actual ~70 min wall vs PLAN.md Step 1 estimate 180-300 min midpoint 240 min = **−71% under**; driver: D-101 "zero-refactor composition only" architectural prediction held with no scope drift; Module Phase-3 N=1 too early to re-estimate Steps 2/3). β tripwire N=14 cumulative final from Phase 2 unchanged (Step 1 added zero /api/* calls; β reopens at Step 2 paragraph-translate). δ runtime detector LIVE silent. ε / α no fires. Phase 1 + Phase 2 ✅ FROZEN posture preserved (tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable; D-101 + Step 1 additive composition on top of frozen Phase 2 components; zero rewrite of frozen surfaces). 0 new ADR Session 50 (5 in-source LDs LD-Step1-A~E per D-094 §2.1 documented in `evidence/phase3/step_01_reader/design_notes.md §3`; collectively implementation patterns of locked D-101 NOT D-NNN-worthy individually; 101 locked decisions D-001 → D-101 unchanged from Session 49). 0 OQ changes Session 50 (OQ count remains 2: OQ-01 + OQ-02 both Phase 1 carryover). New session-50 log + Step 1 PLAN.md row → ✅ DONE + STATE.md 5-anchor sync + 11 file changes (6 new + 5 modified) staged; commit pending user `go commit` + `push it` gate per Sessions 27-49 pattern. 〔legacy Session 48-49 → **2026-05-21 Session 48 ✅ Step 15 ✅ DONE (Playwright E2E close) + Module D 3/3 ✅ COMPLETE + Phase 2 implementation phase ✅ COMPLETE + `RETROSPECTIVE_phase2.md` per Rule C (3 sections 保留下来的做法 / 必须补上的缺口 / 关键决策复盘 + Appendix A γ tripwire 16-data-point summary + Appendix B β cache TTL trajectory across 7-day prod ratchet) + 11/11 Playwright tests PASS in 22.8s wall on prod canonical against `dpl_CCjwr37vkFKJoDBwV1q4T9PgQrjT` Session 47 deploy: 3 chat ja/zh/en (h1 / input aria-label / send btn / user-bubble appears / `aria-busy=true` stream indicator) + 3 quiz ja/zh/en (Explain → `?qid=` → dialog → busy text → close clears) + 3 glossary ja/zh/en (same shape for `?term=`) + 2 D-097 firewall (401 without Auth + WWW-Authenticate Basic / 200 with valid Auth) — 0 unexpected, 0 flaky, 0 retries actually triggered + ~$0.002 真 LLM (9 calls at ≥95% DeepSeek prefix cache hit; modal close aborts stream mid-flight on quiz+glossary tests) + Playwright @1.60.0 + Chrome Headless Shell 148.0.7778.96 v1223 + `apps/web/playwright.config.ts` ~70 行 (baseURL = prod canonical per LD-2 since preview URLs SSO-blocked per Session 44 §ii; workers=1 serial; retries=1; reporter json+html+list; extraHTTPHeaders Authorization Basic injected via process.env / .env.local fallback parser) + 4 spec files at `apps/web/e2e/{chat,quiz,glossary,firewall}.spec.ts` + 5 evidence files at `evidence/phase2/step_15_deploy/{tree_outline.md, test_results.txt, e2e_smoke.md, design_notes.md, e2e_results.json}` + new session-48 log + PLAN.md Step 15 ✅ DONE row + Module D ✅ COMPLETE banner + Phase 2 implementation phase ✅ COMPLETE banner + STATE.md 4-anchor sync + `tripwire_log.md` row #12 (Session 48 γ +14% small over) + apps/web/.gitignore +3 patterns (`/playwright-report` + `/test-results` + `/playwright/.cache`) + apps/web/package.json +1 devDep `@playwright/test@^1.60.0` + 1 script `test:e2e`. **γ tripwire row #12 = 16th data point + 3rd Module D over-estimate** (Step 15 actual ~75 min vs Session 44 lock 66 min → **+14%** small over; Module D N=3 mean = (−31 + 52 + 14)/3 = **+12%** within ±15% noise band → **D-094 §2.4 re-estimate mechanism well-calibrated** since Module D total actual ~380 min ≈ 6.3h vs Session 44 re-estimate 5.7h → +11%). **β tripwire N=14 unchanged** (Playwright fires 9 calls but modal-abort + chat no-wait prevents SSE usage chunk observability on 9/9; cumulative N=14 from Session 47 baseline; cross-session DeepSeek prefix cache TTL > 7 days re-confirmed indirectly via 22.8 s wall on prod canonical = ≥95% hit by economic deduction). δ runtime detector LIVE silent under healthy. 100 locked decisions unchanged (Session 48 lock 0 new ADR; 8 in-source LDs locked per D-094 §2.1 + D-080 v1.1 §8 amend patterns in design_notes §1 covering LD-1 testDir colocation / LD-2 prod-canonical baseURL / LD-3 extraHTTPHeaders injection / LD-4 .env.local manual parser / LD-5 workers=1 serial + retries=1 / LD-6 happy-path = UI plumbing not AI content / LD-7 first-locale-label close button / LD-8 separate firewall spec — none D-NNN-worthy individually, implementation patterns of locked Q1=a/Q4=a). 0 formal Rule B archive Session 48 (0 in-step diversions; Playwright bootstrap proceeded clean per design_notes §1). 279/279 vitest cumulative preserved + ESLint clean + tsc --noEmit clean + Next build untouched (test infra outside Next bundle). **Phase 2 ✅ COMPLETE** posture: α-private prod canonical ratified by 11/11 Playwright + Lighthouse 100/100/100/100 + axe-core 0 violations + cumulative 真 LLM **~$0.085** vs $5 cap = **59× headroom**; β graduation criteria queued in RETROSPECTIVE §2.1-§2.6 (R1 empty-delta hard fix / `[cap-wall]` real-prod evidence / `meta-description` SEO 1-line / Vercel logs CLI suppression workaround / PHASE2_CAP_MODE β choice / custom domain D-092 §3). (legacy Session 47 entry preserved below for trace) — **2026-05-21 Session 47 ✅ Step 14 ✅ DONE (Session 46 code-complete + Session 47 deploy/Lighthouse/axe close) + LD-11 cap.ts KV_* fallback amend (D-100 §2.3 in-place amend per D-080 v1.1 §8; +2 vitest 279/279) + Vercel prod deploy `dpl_CCjwr37vkFKJoDBwV1q4T9PgQrjT` aliased canonical `web-mu-sandy-78.vercel.app` + 9× Lighthouse audit on prod URLs (ja/zh/en × chat/quiz/glossary) = 100/100/100/100 on 6/9 + 100/100/90/100 on 3/9 (single `meta-description` SEO fail on glossary pages; ≥90 floor met) → **Q1=a Perf≥90 + A11y≥95 + BP≥95 + SEO≥90 ratified** + 3× perf trace (LCP 270/578/577ms /ja/chat/quiz/glossary, CLS 0.00 all, TTFB 101-105ms) + 9× axe-core 4.10.2 via MCP `evaluate_script` = **0 violations across all 9** → **Q2=a Full WCAG 2.1 AA ratified** + ~$0.000122 真 LLM (one DeepSeek hover 96% cache hit; cumulative Phase 2 真 ~$0.0825 vs $5 cap = 60× headroom) + `[cap-wall]`/`[cap-breach]` real-prod evidence deferred (DeepSeek pricing physically can't cross $1 per-query wall; coverage via 40 cap.test.ts unit cases Session 45 + new 2 LD-11 cases Session 47). **γ tripwire row #11 = first over-estimate datapoint** (Step 14 actual ~210 min end-to-end Session 46 ~125 min code-complete + Session 47 ~85 min deploy+amend+audits vs adjusted 138 min ~2.3h → **+52%** total; LD-11 amend + KV_*/UPSTASH_* mismatch diagnosis ~30 min unplanned). Module D N=2: Step 13 −31% + Step 14 +52% avg +10% drift. Step 15 ~1.1h Session 44 lock STAYS (structurally distinct = Playwright E2E + custom domain + RETROSPECTIVE not deploy verification). β tripwire N=14 (+1 new prod DeepSeek hover smoke; 96% cache hit matches Sessions 43/44 baseline = **cross-session DeepSeek prefix TTL > 7 days re-confirmed**). 100 locked decisions unchanged (D-100 §2.3 in-place amend per D-080 v1.1 §8 pattern; LD-11 is in-source amend per D-094 §2.1, NOT new ADR). 0 formal Rule B archive (1 in-step Vercel KV_*/UPSTASH_* env-var-name mismatch diagnosed via empirical `vercel env ls` after provisioning; resolved by LD-11 fallback code amend ~30 min; NOT Rule B archive grade per D-094 §2.4 distinction — env-var-naming reality discovery not architectural surprise). 3 new evidence files (`step_14_polish/{lighthouse_results.md, axe_core_results.md, deploy_log.txt}`) + 18 Lighthouse reports (9 report.json + 9 report.html) + 3 perf traces. PLAN.md Step 14 row → ✅ DONE 2026-05-21 + tail wall amend +85 min Session 47 cycle. STATE.md 4-anchor sync (this row). Next: Step 15 design 4Q per D-019 §3a slow-pace = E2E Playwright + custom domain + Phase 2 RETROSPECTIVE.md scoping. (legacy Session 46 entry preserved below for trace) — **2026-05-20 Session 46 ✅ Step 14 CODE-COMPLETE + Full WCAG 2.1 AA a11y polish + zh `QuizExplain.busyText` Sample 5 polish + 0 new ADR (honoured locked D-085/D-088/D-089/D-091/D-094/D-095/D-097/D-099/D-100; 10 in-source LDs per D-094 §2.1 + D-080 v1.1 §8 amend patterns) + 1 new util `apps/web/src/lib/a11y/useFocusTrap.ts` (~115 行 2-arg `useFocusTrap(active, rootRef)` hook: rising-edge captures document.activeElement for restore + focuses first focusable inside rootRef + Tab/Shift+Tab cycles via document.keydown listener excluding [disabled]+[aria-hidden] + cleanup restores prior focus; server-side guard `typeof document === "undefined"`) + 1 new component `apps/web/src/components/SkipLink.tsx` (~20 行 WCAG 2.4.1 Bypass Blocks: sr-only focus:not-sr-only reveal-on-focus + href="#main-content" targets `<main tabIndex={-1}>` + reads `useTranslations("Common").skipToMain` trilingual label) + layout.tsx +2 行 (`<SkipLink />` injected as first child inside `<NextIntlClientProvider>` before `<NavTabs />` so it's the first focusable in tab order) + 3 messages files +1 key each (`Common.skipToMain` ja/zh/en) + **zh `QuizExplain.busyText` 改「正在分析」→「正在生成回答」** per Q3=a closing Step 12 audit §3 Sample 5 (严格对齐 ja「回答を生成」/ en「composing the answer」mental model) + 7 components polished (NavTabs `/65→/70` inactive + ring on Link / LocaleSwitcher `/70→/75` label + border `/12→/18` + ring on select / Chat `/50→/60` emptyHint + ring on newChat/input/submit + `aria-busy={isStreaming}` + `<main id="main-content" tabIndex={-1}>` / QuizList `/40→/55` badge + `/50→/60` choices + ring + `<main id>` / GlossaryList `/40→/55` badge + `/50→/60` body + ring + `<main id>` / QuizExplain `/40→/55` usage + `/50→/60` streaming + `useFocusTrap(summary!==null, dialogRef)` + `aria-busy={isBusy}` + ring on 3 buttons + `motion-safe:animate-pulse` + `@media (prefers-reduced-motion: reduce)` static-bar fallback in BusySkeleton `<style>` / TermPopover symmetric modal a11y) + **WCAG 2.1 AA SC coverage per `step_14_polish/a11y_audit.md`**: 1.4.3 Contrast (`text-black/40` 2.85:1 → `/55` 4.83:1 + `text-black/50` 3.95:1 → `/60` 5.74:1 both ≥4.5 AA Normal) + 1.4.11 Non-Text Contrast (retired `focus:border-black/40` 2.85:1; uniform `focus-visible:ring-2 ring-offset-2 ring-{black,white}` ≥3:1) + 2.3.3 Animation from Interactions (LD-6 AAA-grade above Q2=a target — `motion-safe:` + media query fallback in 2 BusySkeletons) + 2.4.1 Bypass Blocks (LD-2 SkipLink) + 2.4.3 Focus Order (LD-5 useFocusTrap engaged in 2 modals; restores to Explain trigger on close) + 2.4.7 Focus Visible (LD-4 uniform ring) + 4.1.3 Status Messages (LD-7 `aria-busy` on 3 streaming containers paired with `aria-live="polite"`) + **0 new vitest cases** (existing project test discipline = pure-logic in node env per `apps/web/vitest.config.ts` `environment: "node"` with no jsdom/happy-dom installed; UI/DOM behavior verified via Chrome DevTools MCP smoke per Sessions 41-44 pattern; useFocusTrap DOM behavior deferred to Session 47 axe-core integration smoke per `test_results.txt` "Test discipline note" + design_notes §4 — adding jsdom is reversible if Session 47 surfaces focus-trap bugs) + 277/277 vitest pass cumulative preserved + ESLint clean + **tsc --noEmit clean** after 3-line fix to `useFocusTrap.ts` array-index strict mode per design_notes §6.1 (TS `noUncheckedIndexedAccess` made `focusables[0]` typed `HTMLElement\|undefined` even after `.length > 0` check; fixed via destructured locals + truthy guard pattern identical to Step 10 `quizScope.ts`; NOT Rule B archive grade — TS strict-mode tooling correction at implementation time, not architectural surprise) + **next build green** 20 static pages unchanged from Step 13 (ja/zh/en × {/[locale] redirect, /chat, /quiz, /glossary} + /_not-found + 4 dynamic API; **Middleware 48.9 kB UNCHANGED** + **First Load JS shared 115 kB UNCHANGED** + **/chat 122 kB UNCHANGED** + /quiz + /glossary 3.95 kB → **4.53 kB** +0.58 kB each = useFocusTrap import ~0.3 kB + focus-visible CSS class strings ~0.3 kB reasonable per-page cost of modal a11y polish + /[locale] redirect 135 kB First Load due to SkipLink+NavTabs in layout — page immediately redirects to /chat so user-visible First Load is /chat 256 kB unchanged) + **γ tripwire 14th consecutive Module A+B+C+D under-estimate** (Step 14 actual ~125 min vs Session 44 adjusted 138 min ~2.3h → **−9%**; **first sub-30% drift datapoint** across all 14 measurements; "wide-but-shallow polish" sub-regime — 12 files but mostly mechanical contrast bumps + CSS string adds + 2 modal hook integrations + 1 tsc strict refactor §6.1; D-094 §2.4 mid-retro pattern continues PLAN.md inline `actual ~125 min` amend NOT full re-estimate per D-094 §2.1 + D-080 v1.1 §8 patterns; `tripwire_log.md` row #10) + **Module D N=2 still too early to re-estimate Step 15** — Session 44 lock ~1.1h stays in effect (Step 15 is structurally distinct: Playwright + deployment NOT UI polish so −9% polish drift doesn't extrapolate) + **β tripwire data N=13 unchanged** (no 真 LLM call this session; Step 14 was pure source-level a11y polish + 1 zh i18n string change; cross-session DeepSeek prefix cache TTL >6 days carry-over from Session 44 still in effect) + **0 真 LLM billed this session** (cumulative Phase 2 真 $0.0824 unchanged vs D-090 α-silent $5 cap = **60× headroom**; Session 47 deploy cycle budget envelope $0.01-0.05 真 will consolidate cap-trigger smoke + Lighthouse + axe-core into one $$ spend) + new session-46 log + STATE.md 4-anchor sync + PLAN.md Step 14 ✅ CODE-COMPLETE row + `tripwire_log.md` row #10 append + 5 evidence files (`step_14_polish/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md, a11y_audit.md}` — `lighthouse_results + axe_core_results + deploy_log + screenshots` held for Session 47) + 0 formal Rule B archive (zero diversions; "wide-but-shallow polish" proceeded straight with 1 in-step tsc strict fix). **PROVISIONING + DEPLOY + SMOKE GATE HELD** per CLAUDE.md "What you should NOT do without explicit user approval" — DEFERRED to Session 47 entry actions consolidating Step 13 (cap.ts) + Step 14 (a11y) into single deploy cycle: (i) Vercel Marketplace Upstash Redis provisioning Session 45 carry-over + (ii) env var pull to local + Vercel preview+production + (iii) Vercel prod deploy with Step 13 + Step 14 bundled + (iv) deliberate cap-trigger smoke ($0.01-0.05 真 envelope) + (v) MCP `lighthouse_audit` on 9 prod URLs (ja/zh/en × {chat,quiz,glossary}) targeting Perf≥90 + A11y≥95 + BP≥95 + SEO≥90 + (vi) axe-core via `evaluate_script` expecting 0 serious/critical violations per Q2=a Full WCAG 2.1 AA + (vii) Step 14 close (PLAN.md ✅ DONE upgrade + evidence completion lighthouse_results + axe_core_results + deploy_log + screenshots) + (viii) Step 15 design 4Q per D-019 §3a slow-pace = E2E Playwright + custom domain + Phase 2 RETROSPECTIVE.md scoping. (legacy Session 45 entry preserved below for trace) — **2026-05-20 Session 45 ✅ Step 13 CODE-COMPLETE + D-100 LOCKED (cap counter persistence = Upstash Redis via `@upstash/redis@^1.38.0`; pivoted from deprecated `@vercel/kv` per Turn 2 Context7 audit on `/vercel/storage` README; same Upstash Redis backend, only consuming package name changes; 6 §s + 7 rejected alternatives) + cap.ts core module live (`apps/web/src/lib/ai/cap.ts` ~340 行: cost estimator per provider × role pricing matrix + per-query wall $1.00 + per-day rolling counter $5 JST + PHASE2_CAP_MODE env switch silent-log default + β stubs warn/confirm/halt + graceful degradation contract per LD-8 + RedisLike interface deps injection per LD-10) + 4 /api routes wired (`/api/{chat,quiz/explain,glossary/hover,hello-ai}` each +3 lines additive `void recordCapEvent(...)` in onFinish post recordTripwireEvent — zero existing-path refactor) + 40 net new vitest cases (277/277 cumulative; was 237 Session 44 baseline) + ESLint clean + tsc --noEmit clean + next build --turbopack green (unchanged from Step 12: 20 static pages ja/zh/en × 4 + 4 dynamic API + Middleware 48.9 kB + First Load JS shared 115 kB — cap.ts is server-only post-stream telemetry; no client bundle bloat; @upstash/redis lives only in /api/* lambda bundles via lazy dynamic import) + γ tripwire 13th consecutive Module A+B+C+D under-estimate (Step 13 actual ~95 min vs adjusted 138 min → **−31%**; "pure-backend wire-in" sub-regime; first departure from Module C structural-diversion −58% towards Module B clone-adapt -81 to -84 band; D-090 was fully pre-locked Session 30 4 months prior so zero design overhead) + β tripwire data N=13 unchanged (no new 真 LLM call this session) + 0 真 LLM billed this session (cumulative Phase 2 unchanged $0.0824 vs D-090 α-silent $5 cap = 60× headroom) + new D-100 ADR (`docs/decisions/D-100-cap-counter-persistence-upstash.md` 7 §s + 7 rejected alternatives + Vercel Storage README quote citing @vercel/kv deprecation) + new session-45 log + STATE.md 4-anchor sync + PLAN.md Step 13 CODE-COMPLETE row + 4 evidence files (`step_13_cap/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}` — no smoke logs held for Session 46) + 0 formal Rule B archive (zero diversions; pure-backend wire-in proceeded straight). **PROVISIONING GATE HELD** per CLAUDE.md "What you should NOT do without explicit user approval" — DEFERRED to Session 46 entry: (i) Vercel Marketplace Upstash Redis provisioning + (ii) `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` env var pull to local `.env.local` + Vercel preview+production + (iii) deliberate ~$0.005 真 LLM cap-trigger smoke to log `[cap-wall]` + `[cap-breach]` evidence + (iv) Vercel prod deploy with cap.ts. (legacy Session 44 entry preserved below for trace) — **2026-05-20 Session 44 ✅ Step 12 DONE + Module C 4/4 ✅ COMPLETE + D-099 LOCKED (i18n stack = next-intl + ja default + path-based + middleware compose + per-locale error lock partial-supersede D-088 §2.4 + AI SYSTEM_INSTRUCTION untouched per D-095 §2.3) + Module C+D full re-estimate per D-094 §2.4 trigger SATISFIED N=4 UI data points in hand → Module D 20h → ~5.7h adjusted (Step 13 ~2.3h + Step 14 ~2.3h + Step 15 ~1.1h); 237/237 vitest cumulative (+5 new middleware composition tests via vi.mock); ESLint + tsc + Next build all green (20 static pages ja/zh/en × 4 + 4 dynamic API + Middleware 48.9 kB + First Load JS shared 115 kB); Vercel prod deploy `dpl_A9u6j1xDJzaT2Ph5Tr6PAh3zVdEA` aliased canonical `web-mu-sandy-78.vercel.app`; D-097 firewall intact via curl probes (/ja/chat 401 + WWW-Authenticate + /chat unprefixed 401 BEFORE i18n redirect + /api/chat 401) confirming D-099 §2.4 LD-2 compose order; **1 真 LLM UI smoke** via Chrome DevTools MCP on `/ja/glossary?term=アルゴリズム` → 400 in / 78 out / **96% cache hit identical to Session 39 (5 days ago) AND Session 43 (1 day ago) baselines** ⇒ **cross-session DeepSeek prefix cache TTL > 6 days on prod re-confirmed** ratchets Session 43 ">5 days" by ~24h; 3-locale en→zh→ja round-trip UI smoke verified (URL flip + chrome strings + localStorage persisted cross-locale via D-085 §2.2 Resume); 0 [tripwire] fires inferred (96% ≫ 50% threshold); ~$0.000122 真 billed (cumulative Phase 2 ~$0.0824 vs $5 cap = **60× headroom**); γ tripwire 12th consecutive Module A+B+C under-estimate (Step 12: −58% = Step 11 −58% same; "structural-diversion sub-regime" within Module C confirmed: Step 9/10 bootstrap [-85,-85] vs Step 11/12 structural [-58,-58]; Module C 4/4 avg drift −71.5%); 0 formal Rule B archive (5 in-step diversions documented in design_notes §6: pnpm allowBuilds placeholder / eslint plugin version pinning / Suspense wrap on LocaleSwitcher / vi.mock next-intl/middleware for vitest / Promise<never>→Promise<void> on LocaleRootPage; each was 1-3 line config fix not worktree-level rework per D-094 §2.4 distinction); 11 evidence files (5 md + 4 screenshots + 2 deploy logs) + D-099 ADR (`docs/decisions/D-099-i18n-stack-next-intl.md` 6 §s + 8 rejected alternatives) + tripwire_log row #9 appended + PLAN.md Step 12 ✅ DONE + Module D Step 13/14/15 re-estimate amendments inline; β tripwire N=13 cumulative ratify D-088 §2.3 stable-prefix invariant **survives the i18n migration intact** (no SYSTEM_INSTRUCTION change → no bucket fragmentation; cache hit % unchanged on identical surface across i18n + middleware-compose + URL-prefix migration). (legacy Session 43 entry preserved below for trace) — **2026-05-20 Session 43 ✅ Step 11 DONE + Module C 3/4 third-data-point captured — `<TermPopover />` modal + `<GlossaryList />` page LIVE on prod canonical at `/glossary` via hand-rolled SSE consumer (cloned from Step 10 `quizSseTransport`; single-shot non-conversational per LD-5) + 0 new ADR (locked design honoured D-085 §2.4 / D-088 §2.3 / D-088 §2.4 / D-089 §2.3 / D-091 §2.5(β) / D-094 §2.1 / D-095 §2.3 / D-097 cleanly) + 39 new vitest cases (232/232 cumulative; 21 glossaryScope.test + 18 glossarySseTransport.test) + 2 真 LLM UI calls via Chrome DevTools MCP on prod canonical: Call A `アルゴリズム` (same surface as Step 7 Session 39 1st smoke, 5 days ago) 400 in / 62 out / **96% cache hit** identical to baseline + Call B `マルチコアプロセッサ` (fresh, never previously hit in Phase 2) 421 in / 98 out / **0% cache hit** cold creation + both 3-section JP/中文/English structure coherent corpus-grounded under 120-tok soft cap + UI usage hints `TOKENS IN=400 (CACHE HIT 96%) · OUT=62` (Call A) / `TOKENS IN=421 (CACHE HIT 0%) · OUT=98` (Call B) displayed on completion + 0 [tripwire] fires (correct healthy-silent design) + ~$0.0003 真 billed (cumulative Phase 2 $0.0823 vs $5 cap = **60× headroom**) + URL `?term=encodeURIComponent(surface_jp)` ⇆ modal open contract verified e2e (router.push on click + router.replace on close) + 1 Rule B archive `failures/step_11_attempt_1_kana_helper_object_shape_mismatch.md` (root cause: TS `GlossaryEntry.kana_helper: string|null` did not match actual runtime shape `{surface, reading, zh_concept, auto_backfill} | null` — 308/908 entries are objects, 600/908 null; Step 11 was first TS consumer reading the field [Steps 4-10 only stringified it via assembleScope] so drift went undetected until <TermPopover /> attempted to render the object as a React child → React error #31; fix = introduce `KanaHelper` interface in `types.ts` + `buildGlossarySummary` reads `entry.kana_helper?.reading ?? null` + test fixture refresh) + 5 observability findings (i: **cross-session DeepSeek prefix cache TTL > 5 days on prod re-confirmed** — Session 39 last call on `アルゴリズム` 5 days ago still hot at 96% identical to Session 43; ratchets Session 42 "> 4 days" finding by ~24h; very favourable for D-091 §2.1 cost projections / ii: AI SDK system-msg-in-prompts warning per call intentional D-095 §2.3 layout / iii: HOVER_USER_PROMPT 120-tok soft cap respected both calls (62+98 tok) / iv: Call B mild SYSTEM_INSTRUCTION deviation — model echoed katakana surface as kana_helper reading rather than romaji `maruchi koa purosessa`; cosmetic UX miss for screen-reader users deferred to Step 14 polish / v: vercel logs CLI MESSAGE column truncation prevents full `[glossary/hover]` JSON capture — UI usage hint surfaces exact figures so not blocker) + γ tripwire 11th consecutive Module A+B+C under-estimate (Step 11 actual ~100 min vs 0.5 day → −58%; **first sub-pattern departure from −80%+ "implementation cruise" band** — drift widened by Rule B in-step diversion ~10 min; D-094 §2.4 mid-retro pattern continues PLAN.md inline `actual ~100 min` amend NOT full re-estimate; Module C+D full re-estimate STILL deferred to Step 12 close per N=4 UI data points plan) + β tripwire N=12 cumulative ratifies D-088 §2.3 stable-prefix invariant across **5-day cross-session window** + per-surface creation event scoping + 232× scope spread (400 → 92,914 tok) + Session 42 carry-over `resolveEndpoint(window.location.origin)` absolute-URL fetch pattern landed in `glossarySseTransport.ts` (defence-in-depth pair with `<GlossaryList />` `history.replaceState` strip mount effect)** | 全局 D-094 ratchet ε threshold unchanged (1万 fold) |〕 |
| 当前阶段 | **Phase 4 Module B 实施阶段 ✅ COMPLETE Session 56 — B.1 + B.2 + B.3 all DONE; D-104 + D-105 ADRs LOCKED mid-session.** Module B Step B.4 (`/api/tutor` + D-105 Phase 2 migrate atomic commit) is the next gate (G3 user-pending Session 57). Code state: getTutorModel 3-way env-routable (LLM_PROVIDER_TUTOR=deepseek default / =anthropic toggle / =openai throws Phase-5-reserved); TUTOR_SYSTEM_INSTRUCTION ~1278 tokens (LD-Module-B-13); cache_control markers DUAL-PURPOSE (no-op on DeepSeek active on Anthropic); readCacheUsage nested-usage fallback (LD-Module-B-14) benefits Phase 2 routes too. D-103 §2.4 ≥80% cache hit target MET on per-turn steady-state both providers (DeepSeek turn 2+ 80%+ / Anthropic 72% cache_read of input). Phase 4 cost ledger: ~$0.35 spent (B.3 dry-runs across 4 attempts) of $15 cap = 42× headroom. Gates: vitest 416/416 + 2 skipped opt-in / tsc + eslint + next build all green / Phase 1+2+3 frozen invariants preserved. [LEGACY Session 55 (CLOSE)]: **Phase 4 Module B 实施阶段 — B.1 + B.2 ✅ DONE Session 55; B.3 cost dry-run gate G2 user-pending per CLAUDE.md "first LLM API call gate" + PLAN.md §4 G2. B.1 = tutor model lock (Sonnet 4.6 default / Opus 4.7 escalation per D-102 §7.2) + getTutorModel selector anthropic-pinned + ModelRole `tutor` routing. B.2 = TUTOR_SYSTEM_INSTRUCTION constant (inline-snapshot locked) + formatTutorPreamble deterministic markdown projection + buildTutorMessages with nested-breakpoint cache_control:ephemeral on both system messages per D-103 §2.4. Files: `apps/web/src/lib/ai/provider.ts` MOD + `apps/web/src/lib/ai/__tests__/provider.test.ts` MOD + `apps/web/src/lib/ai/tutorPrompt.ts` NEW + `apps/web/src/lib/ai/__tests__/tutorPrompt.test.ts` NEW. Gates: vitest 390/390 PASS + tsc + eslint + next build all green; chat 169 kB / quiz 120 kB / middleware 44.2 kB Phase 2 invariants preserved. γ Module-B partial N=2 mean −72.5% under midpoint; Phase 4 N=5 cumulative mean −71% under (composition-leverage continues; midpoint × 1.0 reversion hypothesis remains unfalsified until B.3 + B.4 new-infra steps). \$0.00 LLM cost B.1+B.2; cumulative all-phases ~\$0.66 vs D-103 \$15 cap = 22.7× headroom. Next user signal needed = explicit B.3 cost-dry-run approval (e.g. `开始 Phase 4 Module B Step B.3` or `开始 dry run`) to fire first Anthropic API call; or `hold` to defer; or `push it` to push origin sync of Session 55 commit. Phase 1+2+3 ✅ FROZEN preservation intact (3 tags immutable; D-085 §2.4 chat 169 kB / D-097 firewall 44.2 kB / D-099 next-intl chrome all unchanged). [LEGACY Session 54 (CLOSE)]: **Phase 4 Module A ✅ DONE Session 54** — A.1 + A.2 + A.3 all landed; gate G1 ✅ FIRED + CLOSED. **Next gate G2** (`开始 Phase 4 Module B`) user-pending — Module B opens Tutor brain layer (B.1 lock Anthropic Sonnet 4.6 default + Opus 4.7 fallback + confirm `@anthropic-ai/sdk` ephemeral cache support / B.2 author tutor SYSTEM_INSTRUCTION with progress-aware preamble + cache blocks ≥80% hit ratio target per D-103 §2.4 / **B.3 cost dry-run requires explicit user approval per CLAUDE.md — entire Phase 4 LLM burn starts here** / B.4 ship `/api/tutor` endpoint). Phase 4 form (D-102 §2.1) AI 学习助手 chat-driven personalized tutor reads progressStore (chapters + quiz via TutorContext shape from Module A) and engages in conversation tailored to where user is in the 16-chapter textbook trunk. Module A Data layer ✅ contractually ready for Module B consumption: `loadTutorContext(window.localStorage, chapters)` emits `{completedChapters: ChapterSummary[], inProgressChapters: ChapterSummary[], pendingChapters: ChapterSummary[], recentQuiz: QuizAttempt[]}` ready for tutor SYSTEM preamble assembly. **Phase 1 + Phase 2 + Phase 3 ✅ FROZEN preserved** through Module A — D-085 §2.4 chat 169 kB / quiz 120 kB / glossary 119 kB / book 121 kB invariants all UNCHANGED; D-097 firewall + D-099 next-intl additive only. **Cost cap D-103 \$15 untouched** — Module A had \$0 LLM cost (types + wire-up + projection); cumulative ~\$0.66 / 22.7× headroom. → legacy: **Phase 4 Module A 实施阶段 OPEN Session 54 — gate G1 fires per user explicit `开始 Phase 4 Module A` signal 2026-05-22** (Session 53 entry option (b) whole-module-1-sitting path). Module A scope = Data / Profile layer (3 steps per PLAN.md §1 rows A.1+A.2+A.3): **A.1** TutorContext types + projection helpers + vitest (new `apps/web/src/lib/tutor/tutorContext.ts` projecting `progressStore` → `{completedChapters[], inProgressChapters[], pendingChapters[], recentQuiz[]}` reusing Phase 3 LD-Step1 ChapterSummary shape; pure types + no API call; LD-Module-A-2 design rationale 3 mutually-exclusive status buckets + recentQuiz sorted-by-lastAnswered-desc slice) / **A.2** wire `recordQuizAnswer` into `<QuizExplain />` via new helper `persistQuizOutcome(storage, qid, correct)` wrapping `recordQuizAnswer + saveProgress`; self-report binary UI 我答对了/我答错了 buttons in QuizExplain footer at `phase === 'done'`; additive on D-085 §2.4 frozen contract — modal lifecycle unchanged (open / explain / close); LD-Module-A-1 design rationale = self-report is honest answer wire absent true picker UX since correct answer letter is visible from QuizList card + QuizExplain header / **A.3** `loadTutorContext(storage, chapters, opts?)` helper composing `loadProgress` + A.1 projection helpers; SSR-safe via StorageLike (Phase 3 LD-Step3-D mount-gate pattern); LD-Module-A-3 design rationale = client `useEffect`-callable post-mount-gate. **Estimated wall** ~2-4h at γ midpoint × 1.0 baseline (Phase 1/2 unit cost model returns per RETROSPECTIVE_phase3 §3.6; Phase 4 N=1 first datapoint forthcoming). **0 LLM cost Module A** — pure types + wire-up + projection helper; first LLM call deferred to Module B Step B.3 dry-run gate G2 + explicit user approval per CLAUDE.md. Tier 3 evidence per-step (3 step dirs × 4 files = 12 evidence files); build-time reviewer chain vitest + tsc + eslint + next build engages at Module A close. → legacy: **Phase 4 设计阶段 ✅ COMPLETE Session 53 Turn 6** — D-102 (Phase 4 形态 = AI 学习助手) + D-103 (cost cap \$5 → \$15) both LOCKED; `docs/phase4/PLAN.md` on disk with 4 modules × ~3-4 steps each (~14 steps total). Per CLAUDE.md 'Phase/stage signaling' 实施 boundary remains closed — Module A 实施 gate G1 user-pending per Session 54 entry options. Phase 4 form: AI 学习助手 = chat-driven personalized tutor reading `progressStore.chapters[nn].{completedAt, scrollY}` + `quiz[qid].{lastAnswered, correct}` (chapters+quiz signals only per OQ-A), powered by Anthropic Claude Sonnet 4.6 default + Opus 4.7 fallback + ephemeral prompt caching (≥80% input cache hit ratio target per D-103 §2.4); new `/api/tutor` endpoint (or `/api/chat` extension with role=tutor); new surface either `/[locale]/tutor` standalone route or modal inside book trunk (Module C Step C.1 mini-Q to decide). 4 modules Phase 2-scale ceremony: **A Data/Profile** (TutorContext types + recordQuizAnswer wire-up + loadTutorContext helper; 3 steps; no LLM cost) → **B Tutor brain** (model lock + SYSTEM_INSTRUCTION + cost dry-run G2 user approval + `/api/tutor` ship; 4 steps; first real Anthropic burn at B.3 dry-run) → **C UI surface** (route decision + UI + i18n + wire + smoke + NavTabs; 4 steps) → **D Ship** (Vercel preview + Playwright + prod deploy + axe-core + Lighthouse + RETROSPECTIVE_phase4.md per Rule C + tag candidate `phase4-α-ship-YYYY-MM-DD`; 3 steps). γ tripwire baseline midpoint × 1.0 (NOT Phase 3 × 0.4 multiplier per RETROSPECTIVE_phase3 §3.6 module-level observation); β re-opens at Module D prod deploy as NEW Anthropic-cache bucket; cost tripwire at \$10 cumulative real Phase 4 spend per D-103 §2.5. Phase 3 holdovers from RETROSPECTIVE_phase3 §2.3 disposition: holdover #1 (wire `recordQuizAnswer` into `<QuizExplain />`) **promoted to Module A Step A.2 scope** per D-102 §7.1 (quiz signal meaningless without it); holdovers #2-5 stay on separate polish list — NOT blocking Phase 4 form work. → legacy: **Phase 4 设计阶段 OPEN — D-102 (Phase 4 形态 = AI 学习助手) LOCKED partial Session 53 Turn 3** — round-2 4Q (OQ-A/B/C/D for personalization signals / model+SDK pick / 4-module decomposition / cost+β envelope) **pending user answer**. Per CLAUDE.md 'Phase/stage signaling' 设计阶段 = NO executable code (only design docs / discussion / decisions). Phase 4 form headline known: AI 学习助手 chat-driven personalized tutor that knows progressStore + quiz state + engages in conversation tailored to where the user is in the 16-chapter textbook trunk. Infrastructure envelope = major new (new model endpoint / DB / SDK — candidates Anthropic Claude Sonnet 4.6/Opus 4.7 + prompt caching, Vercel AI Gateway, Anthropic Agent SDK with tools, Upstash + server-side session memory; specific pick TBD per OQ-B). Step plan granularity = Phase 2-scale 4 modules × N steps (typical decomposition Data / Brain / Surface / Ship; specific TBD per OQ-C). Phase 3 holdovers from RETROSPECTIVE_phase3 §2.3 (wire recordQuizAnswer + wire scrollY restore + re-open β tripwire + β graduation queue + adaptive recommendation) tracked as **separate polish list** — NOT blocking Phase 4 form work. → legacy: **Phase 3 ✅ FROZEN + TAGGED — `phase3-α-ship-2026-05-22`** (Session 52 Turn 9 — user-gated `freeze phase 3 and tag` 2026-05-22; annotated git tag `phase3-α-ship-2026-05-22` created pointing at deploy-verified commit `73018ef` per Session 48 phase-1/2 freeze precedent + pushed to origin + GitHub Release published at <https://github.com/hakupao/it-passport-learning/releases/tag/phase3-%CE%B1-ship-2026-05-22> with RETROSPECTIVE_phase3.md body excerpt; this freeze-marker commit updates STATE.md on main for live-status surface — release tag itself points at the actual ship commit `73018ef` NOT at this marker per Session 48 precedent. Phase 3 totals at freeze: 设计 ✅ D-101 + 4 LD round-2 / 实施 ✅ Steps 1+2+3 all DONE / 16-chapter × 554-page textbook navigable with in-chapter chat + chapter quiz + selection translate + completion gate / 101 locked decisions D-001~D-101 unchanged from Phase 2 close (0 new ADR Phase 3) / 25 in-source LDs documented per D-094 §2.1 / 2 OQ open both Phase 1 carryover (Phase 3 opened 0) / Tests vitest 344/344 + Playwright 14/14 PASS / Real LLM ~\$0 Phase 3 prod + cumulative all phases ~\$0.66 vs \$5 cap = 7.6× headroom / γ tripwire N=3 mean −54% under midpoint / β tripwire N=14 cumulative final unchanged from Phase 2 / Rule A 0 / Rule B 0 / Rule C ✅ /RETROSPECTIVE_phase3.md / Rule D Playwright + build chain as reviewer separate from writer. Tag name rationale matches Session 48 precedent: date-stamped on actual ship commit (73018ef pushed 2026-05-22); α matches conversation vocabulary through Sessions 27-52; distinct prefix from cert-data tags `itpassport-r6-vN.N.N`. Phase 1 + Phase 2 + Phase 3 ✅ ALL FROZEN — tags phase1-ship-2026-05-19 + phase2-α-ship-2026-05-21 + phase3-α-ship-2026-05-22 immutable. **Phase 4 form 设计阶段** = D-102 candidate via D-019 §3a slow-pace 4Q on form lock when user opens that session (D-101 §2.6 deferred form lifted now that Phase 3 实施反馈 is collected)). *(legacy Session 52 Turn 8 deploy)* **Phase 3 ✅ COMPLETE + prod deploy ✅ READY + Playwright 14/14 PASS** (Session 52 Turn 8 — user-gated `push it` 2026-05-22; commit `2cf48a3` pushed to origin/main; Vercel prod deploy `web-n14h489yu` built in 60s; canonical alias `web-mu-sandy-78.vercel.app` re-pointed; Playwright e2e 14/14 PASS in 29.3s including 3 new book.spec.ts smoke specs ja/zh/en covering `/[locale]` redirect → `/[locale]/book` + chapter 00 reader + completion gate disabled-pre-scroll LD-3 enforcement; 11 Phase 2 specs preserved verbatim from Session 48 baseline; D-097 firewall re-confirmed 401/200; 0 live LLM calls fired (β empirical re-open deferred to next user-fired translate/chapter-chat); RETROSPECTIVE_phase3.md §4 Empirical prod-deploy verification + β tripwire row appended; `evidence/phase3/step_03_progress/{smoke_ui_2026-05-22.md, playwright_results.json}` ✅ on disk; cumulative real LLM ~$0.66 vs $5 cap = 7.6× headroom; tag candidate `phase3-α-ship-2026-05-22` still pending `freeze phase 3 and tag` gate per Session 48 precedent). *(legacy Session 52 close)* **Phase 3 ✅ COMPLETE — Steps 1 + 2 + 3 all DONE** (Session 52 close 2026-05-22; PLAN.md §1 Step 3 Progress + RETROSPECTIVE complete; RETROSPECTIVE_phase3.md per Rule C ✅ at repo root; Vercel prod deploy + Playwright e2e + tag candidate `phase3-α-ship-2026-05-22` pending user `push it` / `freeze phase 3 and tag` gate per Session 48 phase-1/2 freeze precedent; γ tripwire N=3 final mean −59% under PLAN midpoint = composition-on-frozen-surfaces work plans at midpoint × 0.4 for Phase 4; β tripwire N=14 cumulative unchanged re-opens at user-gated deploy; Phase 1 + Phase 2 ✅ FROZEN posture preserved across all 3 steps). *(legacy Session 51)* **Phase 3 实施阶段 — Step 1 + Step 2 ✅ DONE / Step 3 pending** (Session 51 close 2026-05-22; PLAN.md §1 Step 2 Inline triggers complete; Step 3 = progressStore + scroll-to-end gate + RETROSPECTIVE_phase3 per LD-3 + LD-4 — gated by user explicit "开始 Phase 3 Step 3" signal). *(legacy Session 50)* **Phase 3 实施阶段 — Step 1 ✅ DONE / Step 2 pending / Step 3 pending** (Session 50 close 2026-05-22; PLAN.md §1 Step 1 Reader 壳 complete; Step 2 = inline triggers per LD-2 [ChapterEndPanel 问本章+测本章 reusing /api/chat + /api/quiz/explain at chapter scope + SelectionToolbar listening on `selectionchange` + ParagraphTranslate modal calling /api/chat with translate prompt] est ~2-4h; Step 3 = progressStore + scroll-to-end gate + 「我看完了」 button + completion visualization on BookIndex + Vercel prod deploy + Playwright e2e smoke + RETROSPECTIVE_phase3.md per Rule C + optional `phase3-α-ship-YYYY-MM-DD` tag est ~2-3h; total remaining wall ~4-7h vs Phase 2 16 steps × ~15h pattern = much lighter ceremony per LD-4 OQ-D=(a) lock) 〔legacy Session 48-49 → **Phase 3 设计阶段 ✅ COMPLETE — 实施阶段 gate pending user explicit "开始 Phase 3 Step 1" signal** (2026-05-22 Session 49 Turn 5 close — round-2 4Q OQ-A/B/C/D + 1 sub-clarification LOCKED): **LD-1** NavTabs 保留 4-tab + chat/quiz/glossary 视觉降级 / **LD-2** 章末固定区 (chat scoped + quiz scoped) + selection toolbar (translate-only ja → zh/en) — hybrid per sub-clarification / **LD-3** 章节完成 = scroll-to-end gate + 「我看完了」 button manual commit / **LD-4** 3 大 step (Reader / Inline triggers / Progress + RETROSPECTIVE_phase3.md) ~7-12h wall. **`docs/phase3/PLAN.md` SCAFFOLD WRITTEN this turn** with §0 LDs + §1 step routing + §2 evidence + §3 cadence + §4 ADR linkage + §5 tripwire + §6 out-of-scope + §7 STATUS. **No code written Session 49 Turn 5** (设计阶段 per CLAUDE.md "Phase/stage signaling" — design docs only; 实施阶段 gate stays closed until user explicit signal). **D-101 LOCKED Turn 4** unchanged; round-2 4Q is implementation pattern not architectural — NOT D-NNN-worthy individually per D-094 §2.1; in-source LD-1~LD-4 captured in PLAN §0. **Phase 1 + Phase 2 ✅ FROZEN posture unchanged** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable. (legacy Session 49 Turn 4 entry preserved below for trace) — **Phase 3 设计阶段 in progress** (2026-05-22 Session 49 Turn 4 close — **D-101 LOCKED**: Phase 3 形态 = 教科书阅读体 (B 学习站) as canonical trunk; opened by user feedback Session 49 Turn 1 "现在没有主体" closing the gap that Phase 2's 3 lookup surfaces (chat/quiz/glossary) didn't form a continuous reading experience; D-019 §3a slow-pace round-1 4Q (导航 / 粒度 / 语言 / 进度) answered → D-101 §2.1-§2.7 locked: `/[locale]/book` as canonical trunk + Chapter (16) reading unit + ja-only content body with zh/en on-demand inline triggers via `/api/chat` reuse + localStorage progressStore extending D-085 §2.2 pattern + zero API/middleware/AI-prompt changes (Phase 2 infrastructure 100% reused); **closes OQ-05 fully** (partial → fully closed; Phase 4 形态 仍 open until Phase 3 实施反馈; Phase 3↔4 顺序 = 先 3 再 4); 11 rejected alternatives recorded. **No code written this Session 49 turn** (设计阶段 per CLAUDE.md "Phase/stage signaling"). **Round-2 4Q OQ-A/B/C/D queued** (NavTabs disposition / inline trigger UI / 章节完成 criterion / step plan granularity). **Phase 1 + Phase 2 ✅ FROZEN posture unchanged** — both tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable; D-101 is additive (new Phase 3 trunk on top of Phase 2 components). (legacy Session 48 close entry preserved below for trace) — **Phase 2 ✅ FROZEN + TAGGED `phase2-α-ship-2026-05-21`** (Session 48 close — user gate `freeze and tag the release` 2026-05-21; α-private shippable state preserved as the canonical Phase 2 deliverable; annotated git tag `phase2-α-ship-2026-05-21` points at the freeze marker commit; GitHub Release created via `gh release create` with RETROSPECTIVE_phase2.md + STATE.md current-phase value + ship URL `web-mu-sandy-78.vercel.app` linked; β graduation queue from RETROSPECTIVE §2.1-§2.6 deferred indefinitely until user opens Phase 3; no implicit Session 49 work — Phase 2 is the steady-state). (legacy Session 48 close entry preserved below for trace) — **Phase 2 实施阶段 ✅ COMPLETE** (2026-05-21 Session 48 close — **Step 15 ✅ DONE** = 11/11 Playwright E2E PASS in 22.8s wall on prod canonical via `apps/web/e2e/{chat,quiz,glossary,firewall}.spec.ts` against `dpl_CCjwr37vkFKJoDBwV1q4T9PgQrjT` Session 47 deploy; 3 chat × 3 locales + 3 quiz × 3 locales + 3 glossary × 3 locales + 2 D-097 firewall tests; Q1=a Medium 9 happy-path ratified empirically. **Module D 3/3 ✅ COMPLETE** = Step 13 ~95 min cap.ts wire-in + Step 14 ~210 min a11y polish + deploy + Lighthouse + axe-core + Step 15 ~75 min Playwright = **~380 min ≈ 6.3h actual** vs Session 44 re-estimate 5.7h → **+11%** within noise band → D-094 §2.4 re-estimate mechanism well-calibrated. **Phase 2 implementation phase ✅ COMPLETE** — 15/15 steps DONE; α-private prod canonical `https://web-mu-sandy-78.vercel.app` ratified by 11/11 Playwright E2E + Lighthouse 100/100/100/100 (6/9 URLs) + 100/100/90/100 (3/9 SEO floor) + axe-core 4.10.2 0 violations × 9 surfaces + cumulative 真 LLM **~$0.085** vs $5 cap = **59× headroom**. **`RETROSPECTIVE_phase2.md` per Rule C complete** (3 sections 保留下来的做法 / 必须补上的缺口 / 关键决策复盘 + Appendix A γ tripwire 16-data-point summary + Appendix B β cache TTL trajectory). **β graduation criteria queued** in RETROSPECTIVE §2.1-§2.6: (a) R1 empty-delta hard fix via reasoningStream consumption integration; (b) `[cap-wall]` + `[cap-breach]` real-prod evidence via Anthropic Opus ~$1.37 真; (c) `meta-description` SEO 1-line patch on `/[locale]/glossary` per Lighthouse 90 finding; (d) Vercel logs CLI verbose-JSON suppression workaround (structured logger or response header); (e) `PHASE2_CAP_MODE` env var explicit choice (warn/confirm/halt) for β; (f) custom domain registration D-092 §3 backlog. Module D N=3 data points captured (Step 13 −31% pure-backend wire-in + Step 14 +52% wide-but-shallow polish + Step 15 +14% Playwright bootstrap; mean +12% drift). **Module A 3/3 ✅ COMPLETE + Module B 5/5 ✅ COMPLETE v2 + Module C 4/4 ✅ COMPLETE + Module D 3/3 ✅ COMPLETE = Phase 2 15/15 ✅ COMPLETE**. (legacy Session 47 entry preserved below for trace) — **Phase 2 实施阶段 in progress** (2026-05-21 Session 47 close — **Step 14 ✅ DONE** (Session 46 code-complete + Session 47 deploy + Lighthouse 9× + axe-core 9× + 3 perf traces + LD-11 cap.ts amend); **Module D 1/3 pending = Step 15** per PLAN.md Module D row 15 with Session 44 lock ~1.1h remaining wall — Phase 2 from-here ~1.1h until ship-ready under one active session). Module D N=2 data points captured (Step 13 −31% "pure-backend wire-in" + Step 14 **+52%** "wide-but-shallow polish + deploy + audit verification" — first over-estimate datapoint across 15 measurements; avg +10% drift across N=2 with widened variance). **Module A 3/3 ✅ COMPLETE + Module B 5/5 ✅ COMPLETE v2 + Module C 4/4 ✅ COMPLETE + Module D 2/3 DONE (Steps 13 + 14) + Step 15 pending**. (legacy Session 45 close entry preserved below for trace) — **Phase 2 实施阶段 in progress** (2026-05-20 Session 45 close — **Step 13 ✅ CODE-COMPLETE** under user gate for Vercel Marketplace Upstash provisioning + deliberate cap-trigger smoke at Session 46 entry; **Module D 1/3 pending = Steps 14 + 15** per PLAN.md Module D rows with Session 44 lock ~2.3h + ~1.1h ≈ 3.4h total wall remaining). Module D 1/3 N=1 data point captured (Step 13: −31% drift, "pure-backend wire-in" sub-regime). (legacy Session 44 close summary preserved below for trace) — **Phase 2 实施阶段 in progress** (2026-05-20 Session 44 close — **Module C 4/4 ✅ COMPLETE**; Module D 3/3 pending = Steps 13/14/15 per PLAN.md Module D rows with adjusted ~2.3+2.3+1.1 ≈ 5.7h total wall after Session 44 D-094 §2.4 re-estimate). **Module A 3/3 ✅ COMPLETE** + **Module B 5/5 ✅ COMPLETE v2** + **Module C 4/4 ✅ COMPLETE** (Step 9 Chat UI / Step 10 Quiz Explain UI / Step 11 Term Popover UI / Step 12 Layout + i18n). **D-099 LOCKED Session 44**: i18n stack = next-intl + ja default + path-based `/ja /zh /en` (localePrefix='always') + middleware compose D-097 firewall first → /api skip → next-intl handler + per-locale error lock (partial-supersede D-088 §2.4) + AI SYSTEM_INSTRUCTION untouched (D-095 §2.3 stable-prefix invariant intact). Phase 2 from-here projection ~6.2h until ship-ready (1-2 active calendar days). **(legacy Session 42 close summary preserved below for trace)** — **Module C 2/4 second-data-point captured**; Module B 5/5 ✅ COMPLETE retained) — **Module A 3/3 ✅ COMPLETE** (Step 1 scaffold / Step 2 DataSource + index.v2 / Step 3 assembly + boot) + **Module B 5/5 ✅ COMPLETE v2** (Step 4 hello-ai + Step 5 chat + Step 6 quiz/explain + Step 7 glossary/hover + Step 8 retry/tripwire) + **Module C 2/4 in progress** (**Step 9 Chat UI ✅ DONE Session 41** + **Step 10 Quiz Explain UI ✅ DONE Session 42**; Step 11/12 pending). **α single-user firewall LIVE** on production canonical `https://web-mu-sandy-78.vercel.app/` via Next.js Edge middleware Basic Auth per D-097 supersede D-096 §2.3; `/api/chat` + `/api/quiz/explain` + `/api/glossary/hover` + `/api/hello-ai` + `/chat` + `/quiz` all gated. **δ runtime detector LIVE on prod** via `apps/web/src/lib/ai/tripwire.ts` + `recordTripwireEvent` writing to `console.warn '[tripwire]'`; 4 routes wire `maxRetries: STREAM_CONFIG.maxRetries=1` per D-088 §2.4. **`<Chat />` UI surface LIVE** at `/chat` on prod canonical via Step 9 v2 deploy `dpl_ola34hvzr` aliased (Session 41). **`<QuizExplain />` modal + `<QuizList />` page LIVE** at `/quiz` on prod canonical via Step 10 v3 deploy `dpl_BqdybbaGeBEqmvX9vv5zUVwKmHEL` aliased (Session 42): hand-rolled SSE consumer via `quizSseTransport.streamQuizExplain` (NOT useChat — single-shot quiz explain non-conversational per Q4=a); URL `?qid=page_NNN_entity_M` reactive backing; 254 question cards rendered server-side from corpus index (68 unique pages). **193/193 vitest pass ✅** cumulative (Step 2 13 + Step 3 12+3 lean / Step 4 8+20 / Step 5 chat 6 net / Step 6 quiz 11+7 / Step 7 hover 11+8 / Step 8 retry 12 + tripwire 18 / Step 9 historyStore 14 + /api/chat route 8 net + middleware 10; chat.test 9 validator cases retired Step 9 / **Step 10 quizScope 18 + quizSseTransport 18**) + Next.js build **11 routes** (5 static + 4 dynamic + `/chat` ○ 122kB + new `/quiz` ƒ 4.16kB First Load JS 118kB) + ƒ Middleware 37.6 kB unchanged + 121 kB First Load JS shared no regression. **Latest deploys**: Prod-v3 `dpl_BqdybbaGeBEqmvX9vv5zUVwKmHEL` aliased canonical `web-mu-sandy-78.vercel.app` (Step 10 final, target=production 2026-05-20 Session 42 with `quizSseTransport.resolveEndpoint` absolute-URL fix); Prod-v2 `dpl_FcrwVPJuLNUEKexBaN7AfST3rXCm` superseded same Session (failed UI smoke on `document.baseURI` credentials pollution); Prod-v1 `dpl_32KMSnDfUY6fNUBUMhreetCSGLJm` superseded same Session (pre-empted before smoke by adding Session 41 defensive strip carry-over). Module C 2/4 second-data-point captured; **next = Step 11 Study Term Hover Popover (inline trigger on words inside chat/quiz content)** per Session 43+ design 4Q. **γ tripwire 10 data points all under by 80%+** (Module A 全 −98%, Module B 5/5 avg drift −83.6%, Step 9 −85%, **Step 10 −85%**; "implementation cruise" sub-pattern now confirmed WITHIN Module C: Step 9 entry / Step 10 clone-adapt both −85% — same shape as Module B's Steps 4-5 bootstrap pair); D-094 §2.4 mid-retro pattern continues PLAN.md Module B-D wall column inline `actual <N> min` amend (Step 4: 140 min / Step 5: 165 min / Step 6: 135 min / Step 7: 90 min / Step 8: ~85 min / Step 9: ~110 min / **Step 10: ~145 min**) NOT full re-estimate (per D-094 §2.1 amendment pattern; `tripwire_log.md` row #7). **Module C+D full re-estimate decision**: STILL NOT done at Module C 2/4 (deferred to Step 12 close per `evidence/phase2/step_10_quiz_ui/cache_audit_2026-05-20.md` §5; N=4 UI data points needed; Step 11 popover and Step 12 layout+i18n are structurally distinct sub-shapes within Module C, may diverge in velocity). **β tripwire data N=10 collected** (Step 4 hello-ai ×1 / Step 5 chat ×2 / Step 6 quiz ×1 / Step 7 hover ×1 / Step 8 5-smoke verification batch / Step 9 UI smoke ×2 turnCount={1,3} / **Step 10 UI smoke ×2 on `page_042_entity_0` 99.81% × 2**); all design-consistent ≥95.8% intra/cross-prefix hit; **cross-scope D-088 §2.3 stable-prefix invariant ratified across scope range 400 → 93K tok over 10 N data points** AND **under multi-turn conversation** (turnCount=3 added only ~114 miss tokens vs ~92800 hit Session 41) AND **across cross-session time windows** (Session 38 → Session 42 on same `page_042_entity_0` 99.81% identical = DeepSeek prefix TTL > 4 days). **R1 empty-delta**: still data-point-3 non-deterministic (Session 38 1 fire, Session 40 + 41 + 42 0 fires across 6 reasoner calls); defensive warning-frame safety net implemented in `<QuizExplain />` `onComplete` (loading→error transition); detailed reasoning-stream consumption fix Module D backlog. **DeepSeek prefix cache TTL > 4 days on prod re-confirmed** Session 42 (Session 38 → Session 42 cross-session continuity on `page_042_entity_0`; ratchets Session 41 "> 5h" finding by ~20×; materially favourable for D-091 §2.1 cost projections). **AI SDK v6 system-message-in-prompts security warning** surfaced per call (intentional D-095 §2.3 layout; mitigation `allowSystemInMessages: true` deferred as cosmetic). Evidence cumulative `evidence/phase2/step_0{3,4,5,6,7,8,9,10}_*/` complete + `failures/step_05_*` (Session 37) + `failures/step_09_attempt_{1,2}_*.md` (Session 41 Rule B archives) + `failures/step_10_attempt_1_*.md` (Session 42 Rule B archive). |〕 |
| Phase 1 状态 | **Phase 1 ✅ FROZEN + TAGGED `phase1-ship-2026-05-19`** (2026-05-21 Session 48 retro-tag — user gate `freeze phase 1 and tag it too` after Phase 2 tag; Phase 1 actually closed Session 23 2026-05-17 per `/RETROSPECTIVE.md` FINAL with user terminal sign-off PASS per D-033 Rule C; subsequent Session 25 Turn 4 D-084 LOCKED for kana_helper backfill → Session 25-26 v1.0.3 implementation cycle → Session 26 turn 1 `v1.0.3 PUBLISHED — close D-084 implementation cycle (STATE sync + evidence post-publish)` commit `b136424` 2026-05-19 = canonical Phase 1 end-commit). Annotated tag `phase1-ship-2026-05-19` points at `b136424` (Phase 1 end, NOT current HEAD). GitHub Release published via `gh release create` linking to `/RETROSPECTIVE.md` body + 3 cert-data ship tags (`itpassport-r6-v1.0.0/1.0.2/1.0.3`) + final Phase 1 commit. **Phase 1 totals**: 设计 ✅ (Topic #1~#7 D-001~D-073, 23 sessions Sessions 01~23 = 11 自然日 11 sessions design + 12 sessions impl) + Step 0~6.9 ✅ + Step 6.10 ✅ + Step 6.11 ✅ (5 tracks A-E all closed by Session 22 retro + Session 23 sign-off FINAL + D-084 §9 addendum kana_helper backfill v1.0.3 Session 25-26) + cert-extractor pipeline shipped `itpassport-r6` data 3× (v1.0.0 2026-05-16 / v1.0.2 2026-05-18 / v1.0.3 2026-05-19 D-084 kana_helper backfill) + Mistral ~$0.579 billed + Anthropic $0 billed (max-plan OAuth via D-069 = $657.36 shadow visibility) + D-001~D-081 ADR set (81 locked decisions covering Topics 1-7 + 25 ADR concept files) + 40 closed OQs + 12 failure archives in `failures/` + 13 evidence audits in `evidence/`. **D-084 §9 addendum** documents post-publication full-corpus deep validation (iter-7 100% atomic-leaf coverage + iter-8 release-level) per user request after iter-5+6 sampling; v1.0.0 GitHub Release immutable; §1-§8 of RETROSPECTIVE unchanged. (legacy Session-22-era status entry preserved below for trace per D-028) — 设计 ✅ + Step 0~6.9 ✅ + Step 6.10 ✅ CLOSED + **Step 6.11 设计 ✅ (D-079/080/081)**: D-079 cadence + D-080 partial polish + D-081 Release asset shape. 实施分 5 轨: A=D-080 polish (~$10 shadow validation), B=D-079 runner+checkpoint infra, C=D-081 release-publish module, D=579-page Stage C 5-gate execution (Mistral ~$0.58 billed, Anthropic $0 billed via max-plan OAuth), E=Release publish + sign-off. |
| 已锁定决定数 | **105** — +2 ADR Session 56 = D-104 (Phase 4 tutor brain matrix 3-way env-routable per user pivot Turn 3; supersedes D-102 §7.2) + D-105 (Phase 2 deepseek-chat/-reasoner → V4-flash migrate per D-095 §2.5(ε) tripwire FIRE 2026-07-24 deprecation deadline; execution scheduled into B.4 atomic commit). **103** — +2 ADR Session 53 = D-102 (Phase 4 形态 = AI 学习助手, fully LOCKED Turn 6 via §7 round-2 close amendment) + D-103 (Phase 4 LLM α-silent cap raise \$5 → \$15 for Phase 4 only, supersede of D-090 §2.x scope-limited to Phase 4). D-001 → D-103 chain intact. **0 new in-source LDs Session 53** (设计阶段 — implementation patterns LD-N appear when Phase 4 Module A 实施 opens at gate G1). → legacy: **102** — +1 ADR Session 53 = D-102 (Phase 4 形态 = AI 学习助手, partial lock pending OQ-A/B/C/D round-2) at `docs/decisions/D-102-phase4-form-ai-tutor.md`. D-001 ~ D-102 chain intact. **0 new in-source LDs Session 53** (设计阶段 — implementation patterns LD-N appear when Phase 4 Module A 实施 opens). → legacy: **101** unchanged from Session 49 + Session 50 + Session 51 + Session 52 (D-001 ~ D-101; 0 new ADR Session 50 + Session 51 + Session 52; 5 in-source LDs LD-Step1-A~E + 8 in-source LDs LD-Step2-A~H + 8 in-source LDs LD-Step3-A~H per D-094 §2.1 documented in `evidence/phase3/step_01_reader/design_notes.md §3` + `evidence/phase3/step_02_triggers/design_notes.md §3` + `evidence/phase3/step_03_progress/design_notes.md §3` — implementation patterns of locked D-101 NOT D-NNN-worthy individually). *(legacy Session 51)* **101** unchanged from Session 49 + Session 50 + Session 51 (D-001 ~ D-101; 0 new ADR Session 50 + Session 51; 5 in-source LDs LD-Step1-A~E + 8 in-source LDs LD-Step2-A~H per D-094 §2.1 documented in `evidence/phase3/step_01_reader/design_notes.md §3` + `evidence/phase3/step_02_triggers/design_notes.md §3` — implementation patterns of locked D-101 NOT D-NNN-worthy individually). *(legacy Session 50)* **101** unchanged from Session 49 (D-001 ~ D-101; 0 new ADR Session 50; 5 in-source LDs LD-Step1-A~E per D-094 §2.1 documented in `evidence/phase3/step_01_reader/design_notes.md §3` — implementation patterns of locked D-101 NOT D-NNN-worthy individually) 〔legacy Session 48-49 → **101** (D-001 ~ D-101; **Session 49 Turn 4 lock 1 NEW ADR = D-101 Phase 3 形态 = 教科书阅读体 (B 学习站) as canonical trunk**) — 7 §s: §2.1 `/[locale]/book` 升为首页 + 唯一 trunk / §2.2 阅读单元 = Chapter (16) per index.v2.json chapters[] / §2.3 正文严格 ja + 术语 inline via `<TermPopover />` reuse + 段落级 zh/en on-demand inline triggers via `/api/chat` reuse (NOT permanent split-pane/sidebar/pre-rendered translation) / §2.4 localStorage progress (chapters + quiz; D-085 §2.2 模式扩展 via 新 `progressStore.ts`) / §2.5 复用 Phase 2 设施 — zero API/middleware/AI-prompt changes (`/api/{chat,quiz/explain,glossary/hover}` + `<TermPopover />` + `<QuizExplain />` + D-097 firewall + D-099 i18n + D-085 historyStore + D-095 §2.3 stable-prefix invariant all preserved) / §2.6 closes OQ-05 fully (Phase 3 形态 锁；Phase 4 形态 仍 open until Phase 3 实施反馈；Phase 3↔4 顺序 = 先 3 再 4；Phase 5/6+ unchanged per D-083 §3.1+§3.5) / §2.7 reversibility via git revert + escape-hatch URLs + localStorage reset. **11 rejected alternatives** recorded (stay-frozen / PDF-viewer / page-unit / 3-level-nav / split-pane / locale-bound-body / sidebar / pre-rendered-translation / 4-tab-parallel / no-progress / adaptive-recommendation). See `docs/decisions/D-101-phase3-form-book-trunk.md`. (legacy Session 48 entry preserved below for trace) — **100** unchanged (D-001 ~ D-100; **Session 48 lock 0 new ADR** — Step 15 Playwright E2E close honoured locked D-085 §2.1/§2.4 + D-088 §2.3/§2.4 + D-091 §2.5(β) + D-094 §2.1 + D-095 §2.3 + D-097 + D-099 §2.3 + D-100 §2.3 cleanly; 8 in-source LDs documented per D-094 §2.1 + D-080 v1.1 §8 amend patterns in `evidence/phase2/step_15_deploy/design_notes.md §1` covering: LD-1 `playwright.config.ts` testDir colocation under apps/web/ + LD-2 baseURL = prod canonical not preview (Session 44 §ii SSO-block constraint) + LD-3 `extraHTTPHeaders: Authorization: Basic <FIREWALL_BASIC_AUTH>` injected via config not per-request + LD-4 FIREWALL_BASIC_AUTH read from process.env fallback to manual `.env.local` parser (no dotenv dep) + LD-5 workers=1 serial + retries=1 for LLM cost envelope + Vercel function concurrency respect + LD-6 happy-path = UI plumbing not AI content (Sessions 41-44+47 Chrome DevTools MCP smokes cover AI correctness) + LD-7 modal close via first-locale-label-button match (works for both ✕ and footer Close since both share locale string in given modal) + LD-8 optional D-097 firewall tests as separate spec file isolating surface area. NOT D-NNN-worthy individually — collectively implementation patterns of locked Q1=a + Q4=a, not architectural choices changing decision space. **`RETROSPECTIVE_phase2.md` per Rule C** ratifies 8 key decisions reviewed (§3.1 D-085 Resume model / §3.2 D-088 §2.3 + D-095 §2.3 stable-prefix invariant / §3.3 D-093 Next.js 15 pivot / §3.4 D-095 DeepSeek default / §3.5 D-097 Edge Basic Auth pivot / §3.6 D-099 next-intl path-based / §3.7 D-100 Upstash Redis pivot from @vercel/kv / §3.8 decisions explicitly rejected without ADR ceremony = correct restraint). (legacy Session 47 entry preserved below for trace) — **100** unchanged (D-001 ~ D-100; **Session 47 lock 0 new ADR** — Session 47 added 1 in-source LD (LD-11 cap.ts KV_REST_API_* env-var fallback alongside D-100 §2.3 UPSTASH_REDIS_REST_* contract per D-094 §2.1 amend pattern; UPSTASH_* preferred on tie) + D-100 §2.3 in-place amendment per D-080 v1.1 §8 pattern documenting Vercel Marketplace 'Upstash for Redis' integration empirical naming + 2 new vitest cases covering fallback (279/279 cumulative, was 277). Neither D-NNN-worthy individually — implementation patterns of locked D-100 §2.3 env-var contract, not architectural choices changing decision space. (legacy Session 46 entry preserved below for trace) — **100** unchanged (D-001 ~ D-100; **Session 46 lock 0 new ADR** — Step 14 a11y polish honoured locked D-085/D-088/D-089/D-091/D-094/D-095/D-097/D-099/D-100 cleanly; 10 in-source LDs documented per D-094 §2.1 + D-080 v1.1 §8 amend patterns in `evidence/phase2/step_14_polish/design_notes.md §3` covering: LD-1 useFocusTrap landing site + LD-2 SkipLink + LD-3 contrast bump policy + LD-4 focus-visible ring policy + LD-5 focus trap mechanism + LD-6 reduced motion gate + LD-7 aria-busy on streaming + LD-8 main landmark id + LD-9 Common.skipToMain i18n key + LD-10 Lighthouse + axe-core evidence deferred to Session 47. NOT D-NNN-worthy individually — collectively are implementation patterns of locked WCAG-coverage decisions, not architectural choices changing existing decision space. (legacy Session 45 lock entry preserved below for trace) — **100** (D-001 ~ D-100; **Session 45 lock 1 NEW ADR = D-100 cap counter persistence via Upstash Redis** — sub-ADR of D-090 §2.1 closing the deferred accounting-source gap; 7 §s = §2.1 counter store Upstash Redis HTTP REST + INCRBY/EXPIRE atomic + 172800s TTL + §2.2 package `@upstash/redis` NOT deprecated `@vercel/kv` + §2.3 env contract exactly 2 vars `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` no fallback naming + §2.4 graceful-degradation contract missing env → log + serve never crash + §2.5 $0 cost envelope under Upstash free tier 10k/day × 167× α-headroom + §2.6 reversibility via RedisLike interface + §7 history; 7 rejected alternatives in-memory ephemeral / deprecated @vercel/kv / fs /tmp / raw REST / per-request header / Cloudflare KV / Vercel Edge Config; see `docs/decisions/D-100-cap-counter-persistence-upstash.md`). (legacy Session 44 entry preserved below for trace) — **99** (D-001 ~ D-099; **Session 44 lock 1 NEW ADR = D-099 i18n stack = next-intl + ja default + path-based + middleware compose + per-locale error lock + AI SYSTEM_INSTRUCTION untouched**; 6 §s + 8 rejected alternatives + partial-supersede D-088 §2.4 (locked Chinese error → per-locale lock ja/zh/en); see `docs/decisions/D-099-i18n-stack-next-intl.md`). (legacy Session 43 entry preserved below for trace) — **98** unchanged (D-001 ~ D-098; **Session 42 lock 0 new ADR** — Step 10 honoured locked D-085 §2.4 (quiz scope = page + entity pin via assembleQuestion) / D-088 §2.3 (stable-prefix invariant — ratified cross-session via 99.81% × 2 on `page_042_entity_0`) / D-088 §2.4 (1-retry maxRetries=1 + locked Chinese fallback retained at `/api/quiz/explain`) / D-089 §2.3 (assembleQuestion reused as-is) / D-091 §2.5(β) (cache-hit tripwire eval — 0 fires under healthy operation) / D-094 §2.1 (PLAN.md inline `actual <N> min` amend pattern) / D-095 §2.3 (stable-prefix layout server-side untouched) / D-097 (Basic Auth firewall gating /quiz + /api/quiz/explain) cleanly; in-source amendments documented per D-094 §2.1 + D-080 v1.1 §8 patterns (`quizSseTransport.resolveEndpoint` — absolute URL via `window.location.origin` to avoid Chrome's `Request` ctor resolving relatives against `document.baseURI` polluted by credentials-in-URL load; defensive `history.replaceState` carry-over to `<QuizList />` from Session 41), NOT D-NNN-worthy. **Session 41 lock 0 new ADR** (history) — Step 9 honoured locked D-085 §2.2 (Resume pin-last + state file α-private) / D-088 §2.4 (1-retry maxRetries=1 retained through AI SDK migration) / D-091 §2.5(β) (cache-hit tripwire eval retained in onFinish) / D-094 §2.1 (PLAN.md inline `actual <N> min` amend pattern) / D-095 §2.3 (stable-prefix corpus→SYSTEM→conversation layout) / D-097 (Basic Auth firewall gating /chat + /api/chat) cleanly; in-source amendments documented per D-094 §2.1 + D-080 v1.1 §8 patterns (React 19.1.0 → 19.2.6 minor bump driven by `@ai-sdk/react@3.0.187` peer tilde gap; defensive `history.replaceState` URL-cred strip in `<Chat />` mount effect resolving Chrome fetch credentials-in-URL restriction), NOT D-NNN-worthy. **Session 40 lock 0 new ADR** (history) — Step 8 honoured locked D-088 §2.4 + D-091 §2.5(β) + D-094 §2.1 + D-095 §2.4 + D-097 cleanly; `STREAM_CONFIG.maxRetries=1` + δ-tripwire detector + `formatUserFacingError` constants are implementation of locked ADRs not new decisions; in-source comment-only documentation of out-of-band tripwire triggers in `tripwire.ts` module per D-094 §2.1 + D-080 v1.1 §8 patterns. **Session 39 lock 0 new ADR** (history) — Step 7 honoured locked D-085/D-088/D-089/D-095/D-097 cleanly; chars/N universal-chars/3 decision + N=4 data table extension applied as comment-only refresh per D-094 §2.1 amendment pattern + D-080 v1.1 §8 in-place amend pattern, NOT D-NNN-worthy (estimator constant unchanged; header N=3 table extended to N=4 with Step 7 hover row + decision rationale). **Session 38 lock 0 new ADR** (history) — Step 6 honoured locked D-085/D-088/D-089/D-095/D-097/D-098 cleanly; chars/3 calibration applied as comment-only refresh per same patterns; D-098 §2.2 v1.1 in-place amend done same Session. Session 37 lock 1 ADR (history): **D-098 mid-step factual correction — whole-book lean payload**, amend D-085 + D-089 §2.3; Session 36 lock 1: D-097). **D-097 essence**: α single-user firewall mechanism revised from "Vercel Password Protection (assumed Hobby-free, factually Pro-only $20/mo)" to "Next.js Edge middleware + HTTP Basic Auth (RFC 7617) on Vercel Hobby"; ~30 行 source + 95 行 vitest test + `FIREWALL_BASIC_AUTH` env var on Vercel preview+production + `apps/web/.env.local` gitignored mirror; fail-closed on env missing (503) + constant-time string compare + matcher excludes `_next/static`+`_next/image`+`favicon.ico`; Cloudflare Access kept as Phase 3+ β option (rejected for α: 4-8h migration + D-093 supersede + Edge runtime AI SDK 兼容性风险 + sunk cost on Vercel + α 威胁模型不需要 SSO). **Retain D-096**: §2.1 (ChatGPT Plus vs OpenAI API 关系) + §2.2 (α vs β scope split) + §2.3 标题+总意 (α firewall in-scope) + §2.4 (β out-of-scope) + §2.5 含义 + §2.6 历史. Session 35 lock 3 ADR (history): D-094 (γ tripwire wall amendment) / D-095 (DeepSeek default + Anthropic switchable) / D-096 (auth scope split + ChatGPT Plus 澄清). **D-094 essence**: γ tripwire 3rd data point cascade resolved by Module A wall actuals inline; Module B-D estimate held; threshold unchanged. **D-095 essence** (Turn 3, partial supersede D-088 §2.1+§2.3+§2.4; retain §2.2+§2.5+§2.6): DeepSeek default (chat=`deepseek-chat` / quiz=`deepseek-reasoner` / hover/smoke=`deepseek-chat`) + Anthropic switchable (`claude-opus-4-7` per D-088 §2.1 Anthropic-side pin retained) via `LLM_PROVIDER` env switch; stable-prefix message layout (corpus→instruction→user) serves both providers via `providerOptions.anthropic` namespace. `deepseek-v4-pro` 非 API model string (Context7 verified 2026-05-19); future V4 via §2.5(ε) DeepSeek mirror tripwire. **D-096 essence** (Turn 5, amend D-083 §2.5 + PLAN.md §6 + ChatGPT Plus / OpenAI API 关系澄清入 ADR): user `我有 chatgpt plus 的会员，我想加一个 auth 的功能` 2026-05-19 + 4Q ans `a/a/a/a` + `你的计划可以，执行` ACK → §2.1 锁 ChatGPT Plus ($20/月 chat.openai.com 消费级) ≠ OpenAI Platform API access (platform.openai.com pay-as-you-go) 是两个独立产品+独立计费；§2.2 拆 auth 成 α single-user firewall (平台层访问 token) vs β multi-user account system (应用层 user identity + state) 性质不同；§2.3 α firewall via Vercel Password Protection (零代码 / 5 min dashboard toggle) **in-scope** Phase 2 α；§2.4 β account system 仍 out-of-scope per D-083 §2.5；§2.5 PLAN.md §6 amend in-place 同 turn 落盘；§2.6 实施 deferred Session 36 per Q3=a (先关 Step 4 LLM gate)。D-090/D-091 cost envelope re-baseline 仍 triggered → defer to Step 5 mid-retro. |〕 |
| 未决问题数 | **2** unchanged from Session 52 (global OQ count). OQ-A/B/C/D round-2 4Q posted Session 53 Turn 4 + RESOLVED Turn 5 are **ADR-local** per D-101 §5 precedent (alphabetic OQ-A/B/C/D scoped to host ADR's round-2 design interview, NOT global numeric OQ-NN registry); resolved in-session and do NOT bump the global count. OQ-01 Phase 1 源类型优先级 + OQ-02 OCR 引擎抽象细颗粒度 both Phase 1 carryover — still open, not Phase 4 scope. → legacy: **2** unchanged from Session 52 (global OQ count). OQ-A/B/C/D round-2 4Q posted Session 53 Turn 4 are **ADR-local** per D-101 §5 precedent (alphabetic OQ-A/B/C/D scoped to the host ADR's round-2 design interview, NOT global numeric OQ-NN registry); they resolve in-session and do not bump the global count. OQ-01 Phase 1 源类型优先级 + OQ-02 OCR 引擎抽象细颗粒度 both Phase 1 carryover — still open, not Phase 4 scope. → legacy: **2** unchanged from Session 49 close + Session 50 + Session 51 + Session 52 (OQ-01 Phase 1 源类型优先级 + OQ-02 OCR 引擎抽象细颗粒度 — both Phase 1 carryover; no Phase 3 OQs opened across the entire phase — Steps 1/2/3 all execution of locked D-101 + PLAN.md, no new design surface). *(legacy Session 51)* **2** unchanged from Session 49 close + Session 50 + Session 51 (OQ-01 Phase 1 源类型优先级 + OQ-02 OCR 引擎抽象细颗粒度 — both Phase 1 carryover; no Phase 3 OQs opened Session 51 since Step 2 is execution of locked D-101 + PLAN.md, no new design surface). *(legacy Session 50)* **2** unchanged from Session 49 close (OQ-01 Phase 1 源类型优先级 + OQ-02 OCR 引擎抽象细颗粒度 — both Phase 1 carryover; no Phase 3 OQs opened Session 50 since Step 1 is execution of locked D-101 + PLAN.md, no new design surface) 〔legacy Session 48-49 → **2** open（OQ-01 / OQ-02；**OQ-05 FULLY CLOSED LOCKED Session 49 Turn 4 2026-05-22 by D-101 §2.6** — Phase 3 形态 + Phase 3↔4 顺序均锁；Phase 4 形态仍 deferred until Phase 3 实施反馈 但 not OQ-05 范围），42 closed（OQ-05 = Phase A/B/C 形态 + 顺序 = **FULLY CLOSED LOCKED Session 49 Turn 4 by D-101 §2.6** path 先 3 再 4 + Phase 3 形态 = B 学习站 textbook reading trunk; OQ-40 = β 开放时间窗 / 触发条件 = **CLOSED LOCKED Session 31 Turn 6 path α 2026-05-19 by D-092**；剩余 2 OQ 全在 Phase 1 后续范围） |〕 |
| GitHub repo | **https://github.com/hakupao/it-passport-learning** (Public, main, **origin synced at `773ecfd` Session 18 close**. Session 19 pushed 6 commits at session start (`6f5310e..773ecfd`). Session 19 close commit pending will move both HEAD and origin/main forward by 1.) |
| 下一会话 | **Session 57 = open** — Phase 4 Module B ✅ COMPLETE Session 56 (B.1+B.2+B.3); **next gate G3 = Module B Step B.4** (`/api/tutor` route ship + D-105 Phase 2 V4 migrate atomic commit). **Session 57 entry options** (per session-56 log §10): (a) **`开始 Phase 4 Module B Step B.4`** → fires G3; I ship `/api/tutor` Server Action / Route Handler using getTutorModel + getTutorProviderOptions + buildTutorMessages (D-104 default DeepSeek V4 pro) + AI SDK v6 toUIMessageStreamResponse; SAME commit migrates Phase 2 four routes per D-105 §2.1 table; vitest + Playwright + manual smoke regression gate per D-105 §2.3. (b) **`push it`** → push origin sync of Session 56 commit; no Vercel deploy yet (Module D ship is the deploy step). (c) **`hold`** → Session 56 closes; Module B ✅ COMPLETE stands; B.4 user-pending remains. (d) **`开始 Phase 4 Module C`** (skipping B.4) — NOT recommended per PLAN.md §4 (B.4 ships the actual tutor endpoint Module C consumes). (e) **`adjust PLAN.md`** → revise B.4 + Module C/D rows before opening B.4 (e.g., split B.4 into two commits — tutor route first, D-105 migrate second). [LEGACY Session 56 (OPEN)]: **Session 56 = open** — Phase 4 Module B B.1 + B.2 ✅ DONE Session 55; **B.3 cost dry-run gate G2 user-pending** per CLAUDE.md + PLAN.md §4. **Session 56 entry options** (per session-55 log §8): (a) **`开始 Phase 4 Module B Step B.3`** (or `开始 dry run`) → fires cost gate G2 second-half; ~10 mock conversations against locked SYSTEM + preamble + Sonnet 4.6; measure input tokens / output tokens / cache hit ratio (target ≥80% per D-103 §2.4) / cost per conversation; if projection exceeds \$15 cap, revise Module B before B.4 ship. (b) **`开始 Phase 4 Module B Step B.4`** (skipping B.3) — NOT recommended per PLAN.md §4 (B.3 is the cost-projection guard). (c) **`push it`** → push origin sync of Session 55 commit; no Vercel deploy yet (Module D ship is the deploy step). (d) **`hold`** → Session 55 closes; B.1+B.2 ✅ DONE stands; B.3 user-pending remains. (e) **`adjust PLAN.md`** → revise B.3/B.4 or Module C/D rows. (f) **`escalate to Opus`** → optional pre-B.3 adjustment to also test getTutorModel({escalate:true}) Opus 4.7 path alongside Sonnet baseline (discuss cap impact before firing). [LEGACY Session 55 (OPEN)]: **Session 55 = open** — Phase 4 Module A ✅ DONE; **next gate G2 Module B 实施** user-pending per PLAN.md §4. **Session 55 entry options** (per session-54 log §9): (a) `开始 Phase 4 Module B` → Module B opens (B.1 model lock → B.2 SYSTEM authoring → **B.3 cost dry-run + explicit user approval per CLAUDE.md** — Phase 4 LLM burn starts here → B.4 ship `/api/tutor`); (b) `开始 Phase 4 Module B Step B.1` → just B.1 (model lock + SDK version confirm), defer B.2-B.4 to next session; (c) `push it` → push Session 54 commit to origin (no Vercel deploy yet — Module D ship is the deploy step); (d) `hold` → Session 54 closes; Module B gate G2 stays user-pending; (e) `adjust PLAN.md` → revise Module B/C/D PLAN rows before opening B (given Module A's -70% γ datapoint, estimates may be tightened). Push pending user `push it` per Sessions 27-53 convention — no proactive git push. → legacy: **Session 53 ✅ CLOSED Turn 6** — Phase 4 设计阶段 ✅ COMPLETE. **Session 54 entry options** (per session-53 log §11): (a) `开始 Phase 4 Module A Step A.1` → Module A Step A.1 实施 OPEN (gate G1 fires); tutor read-surface TutorContext types ship; no LLM cost (pure types). (b) `开始 Phase 4 Module A` whole module 1 sitting (A.1 + A.2 + A.3 = ~2-4h wall projected at γ midpoint × 1.0). (c) `cost dry-run` before Module B → frontload cost question via partial Module B work (B.1 model lock + B.3 dry-run) before B.2 prompt authoring. (d) `adjust PLAN.md` if user wants structural revisions before 实施 (supersede ADR if structural; inline edit if minor). (e) `hold` → Phase 4 设计阶段 ✅ COMPLETE stands; Module A 实施 gate G1 stays user-pending; user reviews D-102 + D-103 + PLAN.md before opening implementation. Push pending user `push it` per Sessions 27-52 convention — no proactive git push. → legacy: **Session 53 mid-session — Turn 4 round-2 4Q posted, awaiting user answer**. On answer: (a) D-102 fully locks (round-2 OQ-A/B/C/D resolved + §2 amended in-place) → `docs/phase4/PLAN.md` written (4 modules × N steps per Q4 + module decomposition per OQ-C) → Session 53 closes with Module A 实施 gate user-pending. (b) If OQ-D answer requires lifting D-090 \$5 cap, **D-103 cost projection ADR** opens (major decision per D-029). (c) If OQ-D answer triggers immediate β-open on prod, D-102 §2.5 amends or sub-ADR. (d) User can 'hold' — Session 53 closes with D-102 partial + PLAN.md deferred to Session 54. Phase 1 + Phase 2 + Phase 3 freeze posture preserved across all branches. → legacy: **Session 53 = open** per user explicit signal. 4 entry options per session-52 log §7 + RETROSPECTIVE_phase3.md §5: (a) `push it` → Vercel prod deploy + Playwright e2e + axe-core + Lighthouse on chapter routes + RETROSPECTIVE empirical β/axe/Lighthouse rows appended / (b) `freeze phase 3 and tag` → annotated git tag `phase3-α-ship-2026-05-22` pointing at this Session 52 close commit + GitHub Release with RETROSPECTIVE body per Session 48 phase-1/2 freeze precedent / (c) `open phase 4` → Phase 4 设计阶段 with D-019 §3a slow-pace 4Q on form lock (likely D-102 candidate; D-101 §2.6 deferred lifted now that Phase 3 实施 is complete) / (d) hold + self-use feedback on Phase 3 surfaces. Phase 3 ✅ COMPLETE stands regardless of which option fires. *(legacy Session 51)* **Session 52 = Phase 3 Step 3 implementation** per PLAN.md §1 Step 3 (LD-3 progressStore localStorage schema {chapters[NN].{scrollY, completedAt}, quiz[qid].{lastAnswered, correct}} + scroll-to-end gate observer + 「我看完了」 button manual commit + BookIndex progress visualization X/16 + per-chapter % bar + Vercel prod deploy + Playwright e2e smoke + RETROSPECTIVE_phase3.md per Rule C + Phase 3 tag candidate `phase3-α-ship-YYYY-MM-DD` if user gates "freeze and tag"). Entry gated by user explicit "开始 Phase 3 Step 3" signal per CLAUDE.md "Phase/stage signaling". 4 entry options per session-51 log §7: (a) Step 3 begin / (b) hold + self-use feedback on Step 2 / (c) tweak Step 2 polish before Step 3 / (d) skip directly to RETROSPECTIVE if Step 3 progress tracker feels unnecessary. *(legacy Session 50)* **Session 51 = Phase 3 Step 2 implementation** per PLAN.md §1 Step 2 (LD-2 inline triggers: 章末固定区 [chat + quiz scoped to current chapter — reuse existing `<QuizExplain />` modal + wrap `<Chat />` UI in smaller modal; `apps/web/src/components/ChapterEndPanel.tsx` NEW] + selection toolbar [new `apps/web/src/components/SelectionToolbar.tsx` listening on `selectionchange` → buttons 「译中」/「译英」 → opens new `apps/web/src/components/ParagraphTranslate.tsx` modal calling existing `/api/chat` with paragraph + translate prompt; new `apps/web/src/lib/book/translatePrompt.ts` composing the translate prompt; **zero new API endpoint**] + vitest cases for new components + evidence at `evidence/phase3/step_02_triggers/{tree_outline.md, build_log.txt, test_results.txt, design_notes.md}`; est wall ~2-4h; entry gated by user explicit "开始 Phase 3 Step 2" signal per CLAUDE.md "Phase/stage signaling"). Alternative entry options: (a) hold and gather Step 1 self-use feedback before Step 2 / (b) tweak Step 1 (e.g. content rendering richness) before Step 2 / (c) skip to Step 3 directly if Step 2 inline triggers feel unnecessary. **Session 50 commit pending user `go commit` + `push it`** (1 atomic commit per Sessions 27-49 pattern: 6 new files + 5 modified files + 4 evidence files + new session-50 log + STATE.md 5-anchor sync + Phase 3 PLAN.md Step 1 row ✅ DONE upgrade; excluded: `.claude/` gitignored). 〔legacy Session 48-49 → **Session 49 close pending user `go commit` + `push it` gates** for Phase 3 设计阶段 deliverables (G4=a 1 atomic commit pattern carryover from Sessions 27-48: D-101 ADR new + `docs/phase3/PLAN.md` new + `docs/discussion/2026-05-22-session-49.md` new + STATE.md 4-anchor sync + OQ-05 row flipped fully-closed). **After Session 49 close: 实施阶段 gate pending user explicit `开始 Phase 3 Step 1` signal**. Step 1 scope per `docs/phase3/PLAN.md §1`: NavTabs 重构 (LD-1) + chapter 路由 `/[locale]/book/chapter/NN` + `ChapterReader` 组件 + `BookIndex` (16-chapter TOC) + `/[locale]` redirect → `/[locale]/book` + i18n messages +Book.* keys + vitest cases. Est wall ~3-5h. **Phase 3 实施阶段 gate ≠ Session 50 implicit start**; user explicit "开始 Phase 3 Step 1" required per CLAUDE.md "Phase/stage signaling". (legacy Session 48 entry preserved below for trace) — **Phase 3 设计阶段 round-2 4Q pending** (Session 49 Turn 5+ — round-1 4Q done Turn 3 + D-101 LOCKED Turn 4; round-2 OQ-A/B/C/D = NavTabs disposition (delete/simplify/footer-dock/strip+repurpose) / Inline chat/quiz trigger UI (persistent buttons/FAB/章末-fixed/context-menu-on-selection) / 章节完成 criterion (scroll-to-end/chapter-quiz-completed/manual-mark/no-strict) / Phase 3 step plan granularity (1 large/3-5 steps/4 modules × N steps); after round-2 lock → Phase 3 PLAN.md scaffold + 设计阶段 → 实施阶段 transition gate). **D-101 fully closes OQ-05** — Phase 3 形态 锁 + Phase 3↔4 顺序锁 (先 3 再 4); Phase 4 形态 still deferred until Phase 3 实施反馈. **Phase 1 + Phase 2 ✅ FROZEN unchanged** — tags `phase1-ship-2026-05-19` + `phase2-α-ship-2026-05-21` immutable; D-101 is additive (new Phase 3 trunk on top of Phase 2 components). **No code written Session 49 Turn 4** (设计阶段 per CLAUDE.md "Phase/stage signaling" — 设计阶段 only design docs / discussion / decisions; 实施阶段 gate pending Phase 3 PLAN.md scaffold post round-2 4Q lock). **Holdovers from RETROSPECTIVE_phase2.md §2.1-§2.6** still queued for β graduation (NOT Phase 3 blockers): R1 empty-delta hard fix via reasoningStream / `[cap-wall]` real-prod evidence via Anthropic Opus ~$1.37 真 / `meta-description` SEO 1-line patch on /[locale]/glossary / Vercel logs CLI verbose-JSON suppression workaround / `PHASE2_CAP_MODE` env var explicit β choice / custom domain D-092 §3 backlog. **真 LLM cost projection Phase 3**: ≤ $0.05 cumulative 真 (paragraph-translate triggers at 95%+ DeepSeek prefix cache hit per D-088 §2.3 invariant; chapter-quiz reuse). **γ tripwire 16 data points final from Phase 2 unchanged** + **β tripwire N=14 cumulative final unchanged** (Phase 3 will start collecting fresh tripwire data on first real LLM call). δ runtime detector LIVE silent under healthy. (legacy Session 48 close entry preserved below for trace) — **Phase 2 ✅ FROZEN + TAGGED** (Session 48 close 2026-05-21 — user picked option (c) `freeze and tag the release` from session-48 log §7 next-session menu; annotated tag `phase2-α-ship-2026-05-21` points at the freeze marker commit; GitHub Release created via `gh` CLI with RETROSPECTIVE_phase2.md body + ship URL link). **No implicit Session 49 work** — Phase 2 is the steady-state α-private deliverable. Future sessions are explicit user-opened by picking from the menu: (a) **Open Phase 3 design** (β multi-user + custom domain + RETROSPECTIVE §2.1-§2.6 graduation queue) — D-019 §3a slow-pace 4Q gate required before implementation; (b) **Resume Phase 1** (cert-extractor pipeline; historical STATE.md sections 5+5b preserved that flow at Step 6.11 entry point); (c) Phase 2 stays frozen as-is (β items deferred indefinitely). **Holdovers from RETROSPECTIVE §2.1-§2.6** queued for β graduation (NOT Session 49 blockers): R1 empty-delta hard fix via reasoningStream consumption / `[cap-wall]` + `[cap-breach]` real-prod evidence via Anthropic Opus ~$1.37 真 / `meta-description` SEO 1-line patch on /[locale]/glossary / Vercel logs CLI verbose-JSON suppression workaround / `PHASE2_CAP_MODE` env var explicit β choice / custom domain D-092 §3 backlog. **γ tripwire 16 data points final** (Module A 3× avg −98% / Module B 5× avg −83.6% / Module C 4× avg −71.5% / Module D 3× avg +12%; D-094 §2.4 re-estimate mechanism vindicated by Module D actual ~6.3h vs re-estimate 5.7h = +11%). **β tripwire N=14 cumulative final** (cross-session DeepSeek prefix cache TTL > 7 days on prod re-confirmed Session 47; D-088 §2.3 stable-prefix invariant ratified across the full phase; D-091 §2.5(β) tripwire **0 fires across the full phase**). δ runtime detector LIVE silent under healthy. **真 LLM cost track**: cumulative Phase 2 真 ~$0.085 across 48 sessions ≈ $0.0018/session; α-private steady-state projection ~$0.01-0.05/day ≈ $3-15/year if Phase 3 stays single-user. **R1 empty-delta safety net** still in `<QuizExplain />` onComplete (loading → error transition); proper fix queued in RETROSPECTIVE §2.1. **Phase 2 RETROSPECTIVE.md per Rule C ✅ COMPLETE at repo root** — `/RETROSPECTIVE_phase2.md`. **Ship artefacts**: git tag `phase2-α-ship-2026-05-21` (annotated) + GitHub Release at `https://github.com/hakupao/it-passport-learning/releases/tag/phase2-α-ship-2026-05-21` (linked to ship URL `https://web-mu-sandy-78.vercel.app` behind D-097 Basic Auth) + commit `d19a8df` (Session 48 Phase 2 ✅ COMPLETE close) + freeze marker commit (this commit). (legacy Session 48 commit-plan entry preserved below for trace) — **Session 48 close pending user `go commit` + `push it` gates per Sessions 27-47 pattern** (G4=a 1 atomic commit with `apps/web/playwright.config.ts` new + 4 spec files `apps/web/e2e/{chat,quiz,glossary,firewall}.spec.ts` new + `apps/web/package.json` modify (+1 devDep + 1 script) + `apps/web/pnpm-lock.yaml` modify + `apps/web/.gitignore` modify (+3 patterns) + 5 evidence files `evidence/phase2/step_15_deploy/{tree_outline.md, test_results.txt, e2e_smoke.md, design_notes.md, e2e_results.json}` new + `RETROSPECTIVE_phase2.md` new (Rule C close, root-level per Phase 1 precedent if exists, or otherwise at repo root since Phase 2 implementation phase complete) + new session-48 log `docs/discussion/2026-05-21-session-48.md` + STATE.md 4-anchor sync + PLAN.md Step 15 ✅ DONE row + Module D ✅ COMPLETE banner + Phase 2 implementation phase ✅ COMPLETE banner + tripwire_log row #12). **Phase 2 implementation phase ✅ COMPLETE Session 48** — all 15 steps DONE; α-private prod canonical `https://web-mu-sandy-78.vercel.app` ratified by 11/11 Playwright E2E (22.8s wall) + Lighthouse 100/100/100/100 (6/9 URLs) + 100/100/90/100 (3/9 SEO floor) + axe-core 4.10.2 0 violations × 9 surfaces + cumulative 真 LLM ~$0.085 vs $5 cap = **59× headroom**. **Next phase entry actions (Phase 3 design / pre-design)**: (i) User decision on whether/when to open Phase 3 (β multi-user) — currently 3 OQ open (Phase 1 后续 / Phase 3-4 范围); RETROSPECTIVE §2.1-§2.6 enumerates the β graduation queue: (a) R1 empty-delta hard fix via reasoningStream consumption / (b) `[cap-wall]` + `[cap-breach]` real-prod evidence via Anthropic Opus ~$1.37 真 / (c) `meta-description` SEO 1-line patch on /[locale]/glossary / (d) Vercel logs CLI verbose-JSON suppression workaround (structured logger or X-Phase2-Cap-Microusd response header) / (e) `PHASE2_CAP_MODE` env var explicit β choice warn/confirm/halt / (f) custom domain registration D-092 §3 backlog; (ii) User may also choose to **freeze Phase 2 here and resume Phase 1** (cert-extractor pipeline; STATE.md historical sections 5+5b preserved that flow); (iii) D-019 §3a slow-pace continues for any new design topic. **Holdovers indefinitely deferred to β** (not Session 49 blockers): all 6 RETROSPECTIVE §2.x items above. **γ tripwire 16 data points** (Module A 3× avg −98% / Module B 5× avg −83.6% / Module C 4× avg −71.5% / Module D 3× avg +12%; D-094 §2.4 re-estimate mechanism vindicated by Module D actual ~6.3h vs re-estimate 5.7h = +11%). **β tripwire N=14 cumulative final** (cross-session DeepSeek prefix cache TTL > 7 days on prod re-confirmed Session 47; D-088 §2.3 stable-prefix invariant ratified across the full phase; D-091 §2.5(β) tripwire **0 fires across the full phase**). δ runtime detector LIVE silent under healthy. **真 LLM cost track**: cumulative Phase 2 真 ~$0.085 = ~$0.0009 per session over 48 sessions; if Phase 3 stays single-user α-private with similar usage pattern (~20 questions/day mixed across surfaces), projected spend = ~$0.01-0.05/day = ~$3-15/year. **R1 empty-delta safety net** still in `<QuizExplain />` onComplete (loading → error transition); proper fix queued in RETROSPECTIVE §2.1. **Phase 2 RETROSPECTIVE.md per Rule C ✅ COMPLETE at repo root** — `/RETROSPECTIVE_phase2.md`. (legacy Session 47 close entry preserved below for trace) — **Session 47 close pending user `go commit` + `push it` gates per Sessions 27-46 pattern** (G3=a 1 atomic commit with cap.ts +LD-11 KV_* fallback amend + cap.test.ts +2 LD-11 cases + D-100 §2.3 in-place amend + 3 new evidence files step_14_polish/{lighthouse_results.md, axe_core_results.md, deploy_log.txt} + 18 Lighthouse reports + 3 perf traces under step_14_polish/lighthouse_reports/ + new session-47 log + STATE.md 4-anchor sync + PLAN.md Step 14 ✅ DONE row amendment + tripwire_log row #11). **Step 15 design 4Q ANSWERED LOCKED Session 47 close** (6th consecutive blanket-ACK across Sessions 41/43/44/45/46/47): **Q1=a** Playwright Medium 9 happy-path tests (3 surfaces × 3 locales = covers D-099 i18n + D-097 firewall + D-085 三 scope; `@playwright/test` + 1 Chromium + Vercel preview URL / ~3h wall) / **Q2=a** Custom domain defer to β (α firewall D-097 sufficient; vercel.app canonical zero ops; in D-092 §3 backlog) / **Q3=a** Standard Rule C 3-section RETROSPECTIVE (保留/缺口/复盘 + γ tripwire 15-data-point summary appendix + β cache TTL data appendix / ~45-60 min) / **Q4=a** 1 atomic commit per Sessions 27-46 pattern (user `go commit` + `push it` gates). **Session 48 起手 entry actions** (in order): (i) **Step 15 implementation** Module D 3/3 final step under above locked 4Q: install `@playwright/test` + 9 tests (ja/zh/en × {chat-send, quiz-explain-open, glossary-hover-open}) + Vercel preview URL with Basic Auth header injection + optional D-097 firewall test (401 without auth, 200 with); (ii) **RETROSPECTIVE_phase2.md** per Rule C 3 sections + γ 15-data-point appendix + β TTL appendix; (iii) **PLAN.md Step 15 ✅ DONE row** + STATE.md sync + Phase 2 implementation phase → completed status; (iii) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall + **all UI paths must include /[locale] prefix** per D-099 §2.3 — same as Steps 9-14. **γ tripwire 15 data points** (Module A 3× −98% / Module B 5× avg −83.6% / Module C 4× avg −71.5% / Module D 2× = Step 13 −31% + Step 14 **+52%** = avg +10% variance widened; Step 15 ~1.1h Session 44 lock STAYS, structurally distinct from Step 14). **β tripwire data N=14** (one new Session 47 DeepSeek hover smoke; cross-session DeepSeek prefix TTL > 7 days re-confirmed on `アルゴリズム`). δ runtime detector LIVE silent under healthy. **真 LLM cost track**: cumulative Phase 2 真 ~$0.0825 vs D-090 α-silent $5 cap = **60× headroom**; Session 48 budget envelope ~$0.001 真 for Playwright e2e smoke (DeepSeek calls at 95%+ cache hit). **R1 empty-delta safety net unchanged**. **Phase 2 RETROSPECTIVE.md** per Rule C at Step 15 close (Q3=c sub-question of Step 15 4Q). **Holdovers from Session 47 deferred to future**: (a) `[cap-wall]` + `[cap-breach]` real-prod evidence via Anthropic Opus call (cost ~$1.37 真 if user opens that gate; not Session 48 priority since unit tests cover behaviour); (b) missing `meta-description` on /[locale]/glossary pages (1-line fix per Lighthouse SEO 90 finding; α firewall makes external SEO irrelevant so cosmetic only); (c) verify Vercel dashboard log viewer shows `[cap]` line directly (CLI suppresses verbose JSON per Session 44 §iv observability; dashboard URL https://vercel.com/bojiangs-projects/web/CCjwr37vkFKJoDBwV1q4T9PgQrjT for visual confirmation if desired); (d) PHASE2_CAP_MODE env var setting decision (currently unset → defaults to silent-log per LD-7; β graduation needs explicit `warn`/`confirm`/`halt` choice). **Session 47 起手 entry actions** (in order — all UNDER USER GATE per CLAUDE.md "What you should NOT do without explicit user approval"; consolidates Session 45 deferred Upstash provisioning + Step 13 cap smoke + Step 14 Lighthouse/axe-core into single deploy cycle per Step 14 LD-10 design): (i) **Vercel Marketplace Upstash Redis provisioning** Session 45 carry-over — manual dashboard click to provision instance + verify env vars `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` land in Vercel project preview+production scopes via `vercel env ls`; (ii) **Env var pull** via `vercel env pull apps/web/.env.local` so dev mode hits same Upstash instance; (iii) **Vercel prod deploy** with Step 13 cap.ts + Step 14 a11y bundled in one deploy; smoke `/api/glossary/hover` with `アルゴリズム` to confirm `[cap]` line appears in `vercel logs --grep '\[cap'`; (iv) **Deliberate cap-trigger smoke** — single whole-book Opus uncached `/api/chat` (~$1.37 est) to cross $1.00 per-query wall + log `[cap-wall]` evidence; iterative 50× ~$0.10 to trigger `[cap-breach]` when crossing $5 JST day; budget envelope $0.01-0.05 真 for Session 47 incl retries; (v) **MCP `lighthouse_audit` on 9 prod URLs** (ja/zh/en × /chat /quiz /glossary) targeting **Perf≥90 + A11y≥95 + BP≥95 + SEO≥90** per Q1=a Session 46 lock; (vi) **axe-core via MCP `evaluate_script`** injecting axe-core CDN running on each surface expecting **0 serious/critical violations** per Q2=a Full WCAG 2.1 AA Session 46 lock; (vii) **Step 14 close**: PLAN.md ✅ DONE upgrade + STATE.md sync + evidence dir completion (lighthouse_results + axe_core_results + deploy_log + screenshots); (viii) **Step 15 design 4Q** per D-019 §3a slow-pace = E2E Playwright + custom domain + Phase 2 RETROSPECTIVE.md scoping; (ix) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall + **all UI paths must include /[locale] prefix** per D-099 §2.3 — same as Steps 9-14. **γ tripwire 14 data points all under-estimate** (Module A 全 −98%, Module B 5/5 avg −83.6%, Module C 4/4 avg −71.5%, Module D 2/3 avg −20% [Step 13 −31% "pure-backend wire-in" + Step 14 −9% "wide-but-shallow polish"]); Module D N=2 still too early to re-estimate Step 15 — Session 44 lock ~1.1h stays in effect. **β tripwire data N=13 unchanged** (no 真 LLM call Session 46; Step 14 was pure source-level a11y polish + 1 zh i18n string change). δ runtime detector LIVE silent under healthy. **真 LLM cost track**: cumulative Phase 2 真 $0.0824 unchanged vs D-090 α-silent $5 cap = **60× headroom**; Session 47 budget envelope $0.01-0.05 真 for consolidated cap-trigger + Lighthouse + axe-core deploy cycle. **R1 empty-delta safety net unchanged**. **Phase 2 RETROSPECTIVE.md** per Rule C at Step 15 close (NOT Step 14 close). (legacy Session 45 close entry preserved below for trace) — **Session 45 close pending user `go commit` + `push it` gates per Sessions 27-44 pattern** (G3=a 1 atomic commit with cap.ts + cap.test.ts + 4 /api route wiring + @upstash/redis dep + D-100 ADR + session-45 log + STATE.md 4-anchor sync + PLAN.md Step 13 CODE-COMPLETE row + 4 evidence files). **Session 46 起手 entry actions** (in order — all UNDER USER GATE per CLAUDE.md "What you should NOT do without explicit user approval"): (i) **Vercel Marketplace Upstash Redis provisioning** — manual dashboard click to provision instance + verify env vars `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` land in Vercel project preview+production scopes via `vercel env ls`; (ii) **Env var pull to local** via `vercel env pull apps/web/.env.local` so dev mode hits the same Upstash instance; (iii) **Vercel prod deploy** with cap.ts wired + Upstash env vars; smoke `/api/glossary/hover` with `アルゴリズム` to confirm `[cap]` line appears in `vercel logs --grep '\[cap'`; (iv) **Deliberate cap-trigger smoke** — issue a whole-book Opus uncached `/api/chat` call (~$1.37 estimated) to cross $1.00 per-query wall + log `[cap-wall]` evidence; iteratively trigger Day-cap by 50 calls × ~$0.10 to surface `[cap-breach]` when crossing $5 JST day; budget envelope $0.01-0.05 真 spend for Session 46 incl retries; (v) **Step 14 design 4Q** per D-019 §3a slow-pace = Lighthouse audit + a11y polish + 1 minor zh `QuizExplain.busyText` verb-choice polish from `step_12_audit.md §3 Sample 5`; (vi) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall + **all UI paths must include /[locale] prefix** per D-099 §2.3 — same as Steps 9-12. **γ tripwire 13 data points all under-estimate** (Module A 全 −98%, Module B 5/5 avg −83.6%, Module C 4/4 avg −71.5%, Module D 1/3 -31% Step 13 "pure-backend wire-in" sub-regime); Module D N=1 too early to re-estimate Steps 14/15 — Session 44 lock ~2.3h + ~1.1h ≈ 3.4h remaining stays in effect. **β tripwire data N=13 unchanged** (no new 真 LLM call Session 45; cap.ts adds telemetry layer but no API exercise). δ runtime detector LIVE silent under healthy. **真 LLM cost track**: cumulative Phase 2 真 $0.0824 unchanged vs D-090 α-silent $5 cap = **60× headroom**; Session 46 budget envelope $0.01-0.05 真 for deliberate cap-trigger smoke. **R1 empty-delta safety net unchanged**. **Phase 2 RETROSPECTIVE.md** per Rule C at Step 15 close (NOT Step 13 close). (legacy Session 44 close entry preserved below for trace) — **Session 44 close pending user `go commit` + `push it` gates per Sessions 27-43 pattern** (G3=a 1 atomic commit with i18n scaffold + middleware compose + [locale] segment + 5 component i18n migration + NavTabs + LocaleSwitcher + D-099 ADR + session-44 log + STATE.md sync + PLAN.md Step 12 ✅ DONE + Module D re-estimate amendment + 11 evidence files + tripwire_log row #9 + 0 Rule B archive + 4 deleted old route files + pnpm-workspace.yaml fix + 3 dev-deps added). **Session 45 起手 entry actions** (in order): (i) **Module D Step 13 design 4Q** per D-019 §3a slow-pace — D-090 cap implementation shape: (a) cap granularity (per-query / per-day / per-route / combined) / (b) env switch (`PHASE2_CAP_MODE`) values matrix (silent-log / warn / confirm / halt) / (c) cost accounting source (Vercel env counter / external Redis / in-memory ephemeral) / (d) UI surface (toast on warn / modal on confirm / 503 on halt / dashboard widget for visibility); (ii) **Module D re-estimate already locked** in PLAN.md Session 44 close: Step 13 ~2.3h / Step 14 ~2.3h / Step 15 ~1.1h ≈ Module D ~5.7h; (iii) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall + **all UI paths must include /[locale] prefix** per D-099 §2.3 — same as Steps 9-12; UI surface auto-handles via browser HTTP auth cache + next-intl Link wrappers. **γ tripwire 12 data points all under-estimate** (Module A 全 −98%, Module B 5/5 avg −83.6%, Module C 4/4 avg −71.5%); Module C+D full re-estimate **NOW DONE** (Module D 20h → ~5.7h applied to all 3 sub-step PLAN.md rows). **β tripwire data N=13 collected**; cross-session DeepSeek prefix TTL > 6 days re-confirmed Session 44. δ runtime detector LIVE silent under healthy (correct Q3=a design). **真 LLM cost track**: cumulative Phase 2 $0.0824 vs D-090 α-silent $5 cap = **60× headroom**; budget envelope unchanged. **R1 empty-delta safety net**: implemented in `<QuizExplain />` `onComplete` (loading→error transition); detailed reasoning-stream consumption fix Module D backlog. **Module D risk flags** per `evidence/phase2/step_12_layout/cache_audit_2026-05-20.md §6`: Step 13 cap-trigger deliberate spend ~$0.005 / Step 14 Lighthouse ≥90 + zh `QuizExplain.busyText` verb-choice polish from `step_12_audit.md §3 Sample 5` / Step 15 Playwright fragility + custom domain β-optional. **Phase 2 RETROSPECTIVE.md** per Rule C at Step 15 close (NOT Step 12 close per `evidence/phase2/step_12_layout/cache_audit_2026-05-20.md §7.3`). (legacy Session 42 close entry preserved below for trace) — **Session 42 close pending user `go commit` + `push it` gates per Sessions 27-41 pattern** (G3=a 1 atomic commit with quizScope.ts + quizSseTransport.ts + quizScope.test.ts + quizSseTransport.test.ts + QuizExplain.tsx + QuizList.tsx + quiz/page.tsx + PLAN.md Step 10 amend + STATE.md 4-anchor sync + session-42 log + 7 evidence docs + 4 screenshots + tripwire_log row #7 append + 1 Rule B archive). **Session 43 起手 entry actions** (in order): (i) **Module C Step 11 design 4Q** per D-019 §3a slow-pace — Study Term Hover Popover shape: (a) Trigger mechanism (hover-only / click-then-hover / hover-with-delay) / (b) Surface attachment (`<Chat />` only, `<QuizExplain />` only, both, or extended to `<QuizList />` cards) / (c) Word-detection strategy (regex over glossary surfaces / IME-tokenizer / per-render scan) / (d) Popover positioning (floating-ui / CSS sticky / Radix UI / hand-rolled) ; (ii) **Third UI data point capture** continues D-094 §2.4 Module C+D re-estimate input — Step 11 will be N=3 of 4; Module C+D full re-estimate STILL deferred to Step 12 close (when N=4 UI data points in hand); (iii) **DeepSeek-chat (not reasoner) provider role** for hover per Step 7 Session 39 precedent — sidesteps R1 empty-delta entirely; (iv) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall — same as Steps 5-10; UI surface auto-handles via browser HTTP auth cache after first dialog fill OR via the absolute-URL pattern from Step 10 if the popover triggers relative fetches. **γ tripwire 10 data points all under 80%+** (Module A 全 −98%, Module B 5/5 avg −83.6%, Step 9 −85%, Step 10 −85%); D-094 §2.4 mid-retro pattern continues; Module C Step 11 will give 11th data point; "implementation cruise" sub-pattern now confirmed within Module C (Step 9 entry / Step 10 clone-adapt both −85% matching Module B's Steps 4-5 bootstrap pair). **β tripwire data N=10 collected**; Step 11 will add N=2 (cold + warm popover); cross-session DeepSeek prefix TTL > 4 days re-confirmed Session 42. δ runtime detector LIVE silent under healthy (correct Q3=a design); β fire branch covered by unit test (not exercised at runtime due to consistently high hit rate). **真 LLM cost track**: cumulative Phase 2 $0.082 vs D-090 α-silent $5 cap = **60× headroom**; budget envelope healthy. **R1 empty-delta safety net**: implemented in `<QuizExplain />` `onComplete` (loading→error transition); detailed reasoning-stream consumption fix Module D backlog. (legacy Session 42 close entry preserved below for trace) — **Session 41 close pending user `go commit` + `push it` gates per Sessions 27-40 pattern** (G3=a 1 atomic commit with Chat.tsx + historyStore.ts + chat/page.tsx + layout.tsx metadata + chat route.ts rewrite + chat.ts trim + chat.test trim + route.test refresh + historyStore.test new + package.json `@ai-sdk/react` add + react 19.2.6 bump + pnpm-lock.yaml + PLAN.md Step 9 amend + STATE.md 4 row sync + session-41 log + 7 evidence docs + 4 screenshots + tripwire_log row #6 append + 2 Rule B failures archives). **Session 42 起手 entry actions** (in order): (i) **Module C Step 10 design 4Q** per D-019 §3a slow-pace — Quiz Explain UI shape: (a) Modal vs side-drawer vs inline expansion / (b) busy-state UX for ⚠️ 22-42s TTFT (Step 6 measured wall on quiz/explain was 5-15s but R1 reasoner role on Step 10 may diverge) / (c) source-of-truth for "which question to explain" (route param `?qid=` vs path `/chat/quiz/:qid` vs in-tab state) / (d) reuse `<Chat />` infrastructure (useChat + transport) vs new dedicated component; (ii) **Second UI data point capture** continues D-094 §2.4 Module C+D re-estimate input — Step 10 will be N=2 of 4; Module C+D full re-estimate STILL deferred to Step 12 close (when N=4 UI data points in hand); (iii) **R1 empty-delta carry-over** finally exercised at Step 10 (deepseek-reasoner role for quiz explain per provider.ts:42); defensive warning-frame fix from Module C/D backlog should land in Step 10; (iv) D-019 4Q slow-pace per Phase 2 pattern. **All API calls must carry Basic Auth header** per D-097 α firewall — same as Step 5-9; UI surface auto-handles via browser HTTP auth cache after first dialog fill. **γ tripwire 9 data points all under 80%+** (Module A 全 −98%, Module B 5/5 avg −83.6%, Step 9 −85%); D-094 §2.4 mid-retro pattern continues; Module C Step 10 will give 10th data point; "implementation cruise" sub-pattern now confirmed extending to UI work via Step 9 −85% matching Module B Steps 4-5 bootstrap [-85,-86]. **β tripwire data N=8 collected** (Step 4-7 + Step 8 smoke batch + Step 9 UI ×2 turnCount={1,3}; all design-consistent ≥95.8%; D-088 §2.3 invariant ratified under multi-turn). δ runtime detector LIVE silent under healthy (correct Q3=a design); β fire branch covered by unit test (not exercised at runtime due to consistently high hit rate). **真 LLM cost track**: cumulative Phase 2 $0.079 vs D-090 α-silent $5 cap = **63× headroom**; budget envelope healthy. **DeepSeek prefix cache TTL > 5h** re-confirmed at 4-hour-gap warm hit Session 41. |〕 |

---

## 1. 项目概览

构建一个**资格考试三语学习内容工厂**。第一目标书：

> 【令和６年度】 ITパスポート 〈書名 + 著者已脱敏 per 2026-05-17 用户规则〉

**核心动机**: 非母语技术学习者卡在日语片假名/平假名的字形识读，而**不是**概念理解。三语对照（日/中/英）+ `kana_helper` 字段 = 直击痛点。

**愿景路线图**:

```
Phase 1: 三语化内容工厂 (本次锁定)
   ↓
Phase 2 (A): 个人备考工具
Phase 3 (B): Web App 题库/学习站
Phase 4 (C): AI 学习助手
   ↓
Phase 5: cert-extractor 通用框架（任意资格教材）
```

---

## 2. Phase 1 当前架构（已锁）

### 2.1 技术栈

- 主语言: **Python 3.11+** (D-024)
- Phase 3 前端: TypeScript / Next.js 或 Astro（Phase 3 才决定，通过文件契约解耦）
- 包管理 / 测试 / 日志: 留 Topic #7 决定

### 2.2 形态：三层 Hybrid (D-023)

```
Layer 3: YAML pipeline 配置 (pipelines/itpassport-r6.yaml)
   ↓ 解析
Layer 2: CLI (cert-extractor run / ocr / inspect ...)
   ↓ 调用
Layer 1: Python 库 (cert_extractor)
   ↓ 实例化
Layer 0: 插件实现 (plugins/source/, plugins/ocr/, ...)
```

GUI **不在 v1**，Phase 3 再考虑。

### 2.3 4 轴可插拔 (D-021)

| 轴 | v1 内置 | 占位接口（v2+ 实现） |
|---|---|---|
| Source Reader | `epub_image` | `pdf` / `txt` / `html` / `docx` / `markdown` |
| OCR Engine | `mistral` | `claude_vision` / `paddle` / `olmocr` / `tesseract` |
| Translator | `claude_sonnet_46` | `gpt` / `gemini` / `deepl` |
| Exporter | `json` / `markdown` / `sqlite` | `anki` / `notion` / `csv` |

### 2.4 数据模型

- **三语** (D-009): 每个文本字段 = `{jp, zh, en}`
- **Hybrid 锚点** (D-022): 每实体保留 `page` + `block_id` + `section_path`
- **Cert-agnostic** (D-010): `cert_id` 顶层区分；`itpassport_r6` 是首个
- **kana_helper** 字段 (D-012): 难读片假名词附 `{surface, reading, zh_concept}`

### 2.5 Pipeline (D-008 + D-011)

```
0. Unpack EPUB → raw/pages/page_NNN.jpg
1. OCR (Mistral) → ocr/page_NNN.md
2. Page Classify (Claude Sonnet) → 标签
3. Hard-page Re-OCR (Claude Vision, 条件触发) → cleaned MD
4. Structure (Claude) → entities (chapters/sections/terms/questions/tables/figures)
4.5 Glossary 抽取 + 锁定 (在翻译前!)
5. Trilingual Translation (Claude, glossary-constrained)
6. Audit (per 规则 A)
7. Export → JSON + JSONL + Markdown + SQLite
```

### 2.6 插件机制 (D-025 + D-026)

- 内部: `@register_<axis>("<name>")` 装饰器 + 自动扫描 `plugins/`
- 第三方: Python `entry_points`（Phase 5 生态用）
- 版本化: 库 semver + 插件 `__cert_extractor_min_version__` 单行声明

### 2.7 OCR 选型 (D-005 + D-006 + D-007)

- 主: **Mistral OCR** (Scale plan, $1/1k pages)
- 难页复核: **Claude Sonnet 4.6 Vision (1M ctx)**

---

## 3. 工程纪律

### 3.1 用户硬规则 (per User CLAUDE.md `<personal_operating_principles>`)

- **A** — 语义抽检 (>50% 压缩或改写必须 N 样本独立抽检)
- **B** — 失败归档不删
- **C** — Retro 强制
- **D** — Writer/Reviewer 分离

### 3.2 讨论操作守则 (D-027)

1. 决定即写入
2. 待定即列入
3. 状态变更即同步
4. Live state vs Historical journal 分离 (本文件 vs session 日志)
5. 关 session 前自检

### 3.3 追溯结构 (Topic #2 ✅ 闭合，全部组件就位)

| 文件 / 目录 | 角色 | 状态 |
|---|---|---|
| `docs/STATE.md` (本文件) | Live state 真相源 | ✅ D-028 已建 |
| `docs/discussion/YYYY-MM-DD-session-NN.md` | Session 编年体日志 | ✅ session-01 (closed) + session-02 (in progress) |
| `docs/discussion/README.md` | 讨论规约 + 操作守则 | ✅ 已建 |
| `docs/decisions/D-NNN-slug.md` | 重大决定 ADR | ✅ 8 条 ADR 已写完 (D-005/008/013/016/021/022/023/024) |
| `docs/decisions/README.md` | ADR 规约 | ✅ 已建 |
| `docs/templates/evidence-template.md` | 抽检证据模板 | ✅ D-030 已建 |
| `docs/templates/failure-template.md` | 失败 attempt 模板 | ✅ D-032 已建 |
| `docs/templates/retrospective-template.md` | Phase 收尾复盘模板 | ✅ D-033 已建 |
| `evidence/` | 抽检证据落点 (per 规则 A) | ⏳ 进入 Phase 1 实施时建 |
| `failures/` | 失败 attempt 归档落点 (per 规则 B) | ⏳ 进入 Phase 1 实施时建 |
| `RETROSPECTIVE.md` | Phase 1 收尾复盘 | ⏳ Phase 1 收尾时由模板拷贝 |

---

## 4. 当前未决问题（open）

| OQ | 问题 | 归 Topic | 状态 |
|---|---|---|---|
| OQ-01 | Phase 1 实际要支持的源类型优先级（v1 后） | Topic #1 收尾后开放 | open |
| OQ-02 | OCR 引擎抽象的更细颗粒度（部分覆盖） | Topic #1 后续 | open |
| OQ-05 | A/B/C 三个 Phase 的具体形态 + 启动顺序 | Topic #8 | **FULLY CLOSED LOCKED** (Session 49 Turn 4 2026-05-22 by D-101 §2.6) — Phase 2 = A+C hybrid (D-083 §2.5) + Phase 3 = B 学习站 textbook reading trunk (D-101 §2.1-§2.5) + Phase 3↔4 顺序 = 先 3 再 4 (D-101 §2.6); Phase 4 形态 deferred until Phase 3 实施反馈 但 not OQ-05 范围. (legacy partial-close note preserved for trace: Session 24 Turn 4 originally locked Phase 2 part; Phase 3/4 形态 + 顺序 was the remaining open portion) |
| ~~OQ-40~~ | ~~β 开放时间窗 / 触发条件（"什么时候 / 什么条件下 α → β 切换"）~~ | Topic #8 | **CLOSED LOCKED** Session 31 Turn 6 path α 2026-05-19 by D-092 |

（已闭合的 OQ-04/06/07/08~39 详见 session-01~06 日志 §3-4）

> **里程碑**:
> - **Phase 1 设计阶段彻底收尾**: Topic #1~#7 ✅ 全闭合（D-001~D-073, 7 独立 ADR）
> - **Phase 1 实施 gate ✅ 解锁**: 等 user 显式 "开始实施" 切阶段
> - 剩余 3 OQ 全在 Phase 1 范围外（Topic #1 后续 / Topic #8 Phase 2-4）

---

## 5. 下一步 / Resume Instructions (current = **Session 13 open — Step 6.11 设计层 3D 锁完，进入实施 TDD 起手 (6.11.A.1)**)

### Session 11 progress (2026-05-11) — **Step 6.9 Stage 6 ✅ CLOSED**

- ✅ Stage A user retro worksheet → 3-Q sign-off (Q1=D / Q2=✓ / Q3=✓)
- ✅ Stage A JSON snapshot: `evidence/.../stage6_review_stageA_rerun2.json` (23KB)
- ✅ Stage B dispatched → halted at page_042 safety FAIL (32/40 audited)
- ✅ Stage B JSON snapshot: `evidence/.../stage6_review_stageB.json` (99KB)
- ✅ Stage B root-cause: both FAILs = detector FPs (page_19 D7 + page_42 D5)
- ✅ Stage B retro worksheet → 5-Q sign-off (Q1=A / Q2=A / Q3=A / Q4=B / Q5=✓)
- ✅ Detector fixes (D5 regex + D7 subset-difference + D9 → INFO) + 4 regression tests
- ✅ Full unit test suite: 324/324 pass (320 base + 4 new)
- ✅ Stage B rerun #2 → 40/40 audited, 21 PASS / 18 WARN / 1 real FAIL (page_22 LLM-caught hallucination); evidence snapshot 133KB
- ✅ Closure worksheet Q1 = B (user hand-edited page_022 entity[2].rows[1][1].en, removed "Activities")
- ✅ Hand-edit documented: `evidence/.../page_022_hand_edit_2026-05-11.md`
- ✅ Stage B rerun #3 → 40/40 audited, **22 PASS / 18 WARN / 0 FAIL**, safety=False, overall=WARN; evidence snapshot 135KB
- ✅ `evidence/.../step_06_audit.md` updated with full Stage B narrative + closure summary + known polish items + sign-off table populated through "Final" row
- ✅ STATE.md synced this turn (per D-027 §3)
- ✅ Session 11 log §7 covers all phases of Stage 6 closure

**Cumulative dry-run shadow cost (post Step 6.9 closure)**: $47.44 + $37.67 = **$85.11 shadow / $0.05 Mistral real / $0 Anthropic real (max-plan OAuth)**

### Step 6.10 Stage 7 export — next entry point (Session 12)

Stage 6 carried forward as "known polish items" (all WARN-level, non-blocking):

- D6 choice_marker_inconsistent ×3 pages (rs=7) → Stage 7 export normalizes zh+en → A/B/C/D, jp keeps ア/イ/ウ/エ
- LLM L3 translation_unfaithful WARN clusters (page_022 ストラテジ→Strategy tautology, page_038 circular EN, suffix inconsistency) → Stage 5 prompt v2 polish item
- D7 numeric_inconsistent WARN ×22 leaves (style/subset only, no conflict) → Stage 5 / Stage 7 normalization candidate
- D11 kana_helper_missing INFO ×11 leaves → Stage 4.5 backfill
- D13 run-level INFO ×2 (g_022 / g_028 glossary surface-concept split) → Stage 4.5 glossary self-consistency

Stage 7 export contract reminders:

- Refuse UNTRANSLATED leaves (D-076 envelope)
- Refuse `answer_index == -1` (D-076 envelope)
- Normalize choice markers (D6 rs=7 carry-forward from Stage 6)
- Document known polish items in release notes for first GitHub Release tag

### Session 10 close summary — Stage 6 scaffold + Stage A 0-FAIL baseline ✅

### Session 10 close summary — Stage 6 scaffold + Stage A 0-FAIL baseline ✅

**D locked**: D-077 — Stage 6 audit reviewer LLM (two-pass deterministic + LLM, two-tier verdict, repair_stage tagging, Stage A 5-page → Stage B 40-page; amends D-063 retry semantics). ADR: `docs/decisions/D-077-stage6-audit-reviewer.md`. Session log: `docs/discussion/2026-05-07-session-10.md`.

**Memory**: new `feedback_quality_over_cost.md` (default to highest-quality / safest design option; do not pre-frame around shadow cost — user feedback Session 10 起手).

**3 commits this session**:
- `39b8710` — Stage 6 audit reviewer scaffold (D-077 + 5 modules + 4 test files + 103 unit tests)
- `a624f28` — D5/D7 false-positive fixes from Stage A retro (D5 short-circuit on 0-question pages; D7 circled-numeral normalization)
- `162aebb` — D7 numeric_inconsistent severity heuristic (FAIL only on real conflict; WARN on style/paraphrase)

**3 LLM dispatches** (Stage A 5-page audit, total 36 calls, $8.42 shadow / $0 billed):
- Stage A #0: surfaced D5+D7 FPs; halted on page_045 safety FAIL
- Stage A re-run #1: D5 fixed, 2 D7 strict FAILs persist
- Stage A re-run #2: 0 FAILs / 5 WARN, clean baseline ready for Stage B

**Stage A re-run #2 result** (current `audit/stage6_review.json`): overall=WARN, safety_failed=False, P/W/F=0/5/0, repair_stage="5". Real catches verified: F-CHOICE-MARKER (page_043 D6) + LLM Phase 2 circular-definition (page_038) + LLM idiomatic 自我完结型 (page_043).

### Next sub-steps for Session 11

| # | Action | LLM ($)? | Status |
|---|---|---|---|
| 6.9.1~6.9.5 | Stage 6 scaffold + 320 unit tests | no | ✅ done (Session 10) |
| Stage A #0/#1/#2 | 5-page audit verification (3 dispatches) | yes ($8.42 shadow) | ✅ done (Session 10) |
| 6.9.7 | Stage A user retro worksheet + 3-Q sign-off | no | ✅ done |
| 6.9.8 | Stage B 40-page audit dispatch (halt) | yes ($7.56 / $0 billed) | ✅ partial (halt @ page_042) |
| 6.9.8a | Stage B user retro worksheet + 5-Q sign-off | no | ✅ done (A/A/A/B/✓) |
| 6.9.8b | Detector fixes (D5 regex + D7 subset + D9 INFO) + 4 regression tests | no | ✅ done (324/324 pass) |
| 6.9.8c | Stage B rerun #2 (full 40-page after fix) | yes ($10.95 / $0 billed) | ✅ done (21/18/1, 1 real LLM-caught FAIL) |
| 6.9.8d | Closure worksheet Q1 = B → user hand-edit page_022 + documented | no | ✅ done |
| 6.9.8e | Stage B rerun #3 (after hand-edit) | yes ($10.75 / $0 billed) | ✅ done (22/18/0 clean) |
| **6.9.9** | **Stage 6 closure — step_06_audit.md narrative + sign-off + STATE sync** | no | ✅ **done — Stage 6 CLOSED** |
| 6.10 design | Stage 7 export v1 — D-078 ADR | no | ✅ done (Session 12) |
| 6.10.1 | `schema.py` + 25 tests | no | ✅ done |
| 6.10.2 | `normalizers.py` + 37 tests | no | ✅ done |
| 6.10.3 | `gates.py` + 14 tests | no | ✅ done |
| 6.10.4 | `emitters.py` + 18 tests | no | ✅ done |
| 6.10.5 | `runner.py` + CLI `export-trilingual` + 9 tests (incl. half-width-jp regression) | no | ✅ done |
| 6.10.6 | `evidence/.../step_07_export.md` post-dispatch | no | ✅ done |
| 6.10.7 | Real 40-page dry-run (84 files emitted; Gate A pre-normalize fix applied) | no | ✅ done (0 LLM cost) |
| 6.10.8 | User sample-review (page_006/014/022/038/043) | — | ✅ PASS ("ok通过", 2026-05-11) |
| 6.10.9 | Step 6.10 closure (step_07_export.md + STATE sync) | — | ✅ done (this turn) |
| **6.11 设计层** | D-079 + D-080 + D-081 — Stage C cadence + 部分 polish + Release asset shape | no | ✅ done (Session 13) |
| **6.11.A** | D-080 Stage 4.5 polish 实施 + 40-page 重 baseline 验证 | A.3 = yes (~$10 shadow / $0 billed) | ⏸ next entry point |
| 6.11.A.1 | Stage 4.5 `scan_katakana_terms_for_backfill` + `kana_stop_list.txt` (20 词种子) + KanaHelper.auto_backfill 字段 + 9 unit tests | no | ✅ done (suite 436/436) |
| 6.11.A.2 | Stage 4.5 `split_multi_concept_items` (6 separators, balanced/unbalanced 双路径, warn 透传) + 5 unit tests | no | ✅ done (suite 441/441) |
| 6.11.A.3 | 40-page re-baseline (Stage 4.5 → 5 → 6 phase-1); D11/D13 INFO = 0 验证 | yes (~$22-27 shadow / $0 billed via max-plan OAuth, 5 attempts incl 2 bug fixes) | ⚠️ done with NO-ACCEPTANCE — D-080 v1.1 §8 amended (acceptance withdrawn) |
| 6.11.A.4 | `evidence/.../step_45_polish.md` + STATE.md sync + D-080 v1.1 amend | no | ✅ done (this turn) |
| **6.11.B** | D-079 runner + checkpoint infra | no | ✅ done (Session 14, all 3 sub-steps) |
| 6.11.B.1 | CLI `stage --from N [--redo]` + 6 tests (`pipeline/stage_dispatch.py` planner + cleanup; suite 442→448) | no | ✅ done (Session 14) |
| 6.11.B.2 | Checkpoint emitter `gate_N_<ts>.json` + 5 tests (`pipeline/checkpoint.py` Pydantic Checkpoint + emit_checkpoint + tokyo_timestamp_slug + load_checkpoint; suite 448→453) | no | ✅ done (Session 14) |
| 6.11.B.3 | 5 个 gate 的 halt criteria checker + 8 tests (`pipeline/halt_criteria.py` HaltResult + `_within_tolerance` + 5 checkers + `_walk_translation` Plan-B regression guards; suite 453→461) | no | ✅ done (Session 14) |
| **6.11.C** | D-081 release-publish 模块 | no | ✅ done (Session 14, all 4 sub-steps) |
| 6.11.C.1 | `cert_extractor.release.tag_name()` + 4 tests (`release/__init__.py` + `release/tag_name.py` underscore↔dash mapping + ParsedTag NamedTuple + version normalization + round-trip; suite 461→465) | no | ✅ done (Session 14) |
| 6.11.C.2 | `cert_extractor.release.compose_notes()` + 4 tests (`release/notes.py` D-081 §2.3 5-section Markdown composer + polish aggregation + GitContext; suite 465→469) | no | ✅ done (Session 14) |
| 6.11.C.3 | `cert_extractor.release.publish()` orchestrator + integration test (mock gh) (`release/publish.py` D-081 §2.4 8-step: validate→zip→SHA256SUMS→compose-notes→gh-create→gh-view + injectable `gh_runner`; suite 469→473) | no | ✅ done (Session 14) |
| 6.11.C.4 | 手写 `docs/release-notes/itpassport-r6-v1.0.0-intro.md` (~250 字; project background + kana_helper rationale + what-you-get + audience; smoke-rendered through compose_notes → clean 2.9KB body) | no | ✅ done (Session 14) |
| **6.11.D** | Stage C 579-page 5-gate 执行 | yes (Mistral ~$0.58 billed; Anthropic $0 billed via max-plan OAuth) | ⏸ |
| 6.11.D.1 | Pre-flight: `gh auth status` ✅ (hakupao, repo+workflow scopes), 579-page EPUB ✅ (`./IT-Passport.epub` 256 MB, 579 page-image entries), Anthropic OAuth ✓ (Session 13 path), **MISTRAL_API_KEY ❌ 未在 shell** → Session 15 user sourced `.env.local` (inline loaded into Bash subshell) | no | ✅ done (Session 14 + 15) |
| 6.11.D.2 | Stage 0 unpack + Stage 1 OCR → Gate ①. **run_id=`dry_run_2026-05-12T13-23-19`**, 579/579 pages OCR'd in 1026 s, 0 fail, `mistral_usd=$0.5790`. Gate ① 第一次 FAIL (B.3 checker schema bug — read flat `cost.mistral_usd` but emitter writes `cost.current.mistral_usd`); patched `halt_criteria.py` nested-first + flat fallback + 1 new test (suite 473→474). Gate ① 重跑 PASS, checkpoint `gate_1_2026-05-12T13-42-47.json` emitted. Rule B archive `failures/stage1_ocr/stage1-2026-05-12-gate1-checker-schema.md`. | Mistral $0.579 | ✅ done (Session 15) |
| 6.11.D.3 | Stage 2 classify (579 opus) + Stage 3 re-OCR (42 opus Vision; +14 force on D-076 violators = 56 cleaned) + Stage 4 structure (554 of 579, 25 auto-skipped-by-label, **2224 entities** chapter/section/term/question/table/figure 6 类齐). β.1 remediation: δ visual confirm F-MISTRAL-ANSWER-LINE-LOSS → α commit `0fa65e9` (CLI flag matrix + checker patches + 2 tests + 2 failure archives) → β.1 14-page force-OCR + re-Stage-4 → **Gate ② PASS** clean. Checkpoint `gate_2_2026-05-12T23-15-29.json` emitted. Per-page sanity vs visual: page_043 `[2,2,2,3,2]` ✓✓✓ / page_525 `[2,1,2,1,1]` ✓✓✓. | $0 billed / **$235.63 shadow** opus | ✅ done (Session 16) |
| 6.11.D.4 | Stage 4.5 glossary single-call opus → Gate ③ first-shot PASS。 1 LLM call, 70 921 tokens, **908 trilingual entries** locked from 978 unique surfaces (7% alias-merge), kana_helper coverage = 308 (307 LLM-authored + 1 D-080 polish-#1 auto-backfill safety-net on g_451)。 Gate ③ inputs (orchestrator-computed per D-080 v1.1 §8.5 wiring): D11=0 (deferred — no `translated/` at this gate), **D13=0** (`detect_glossary_consistency` clean), **untranslated=0**, entry count 908 in [500, 1500] band。 Checkpoint `gate_3_2026-05-13T09-50-35.json` emitted。 δ-equivalent quality spot-check (9 entries) clean。 `verdict_halted=FAIL` operational artifact (cumulative-cap framing, NOT a data fail) noted as Phase 1 v2 UX wart, no archive。 | $0 billed / **$2.549 shadow** opus | ✅ done (Session 17) |
| 6.11.D.5 forward | Stage 5 translation forward pass — 554/554 pages on disk via 3 attempts (1 instant halt CLI gap → patch+test+commit `ebfdbd9` 477/477; 2 WARN halt $350 anthropic soft; 3 clean exit `verdict_halted=None`). 5607 fields translated, 1728 glossary hits, **452 UNTRANSLATED leaves residue across 96/554 pages** (sub-batch parse failures, not stuck-leaves). | $0 billed / **$169.52 shadow** Stage 5 alone; cumulative $407.70 shadow | ✅ done forward (Session 18) |
| 6.11.D.5 residue | Plan A executed Session 19: chunk=4 attempt-4 ($46.35, 96→67 residue) → chunk=1 attempts 5/6/7/8 ($172.40 combined, API-quota churn + iterative convergence) → **71 stuck leaves** (term.definition class, Session 09 pattern at scale) → Claude-Opus hand-edit drafts → user-approved `apply_hand_edits.py` 71/71 transactional apply → Gate ④ checker **passed:True / reasons:[]** (jp_mutation=0 + untranslated=0) → checkpoint `gate_4_2026-05-16T08-08-58.json` emitted. Evidence: `step_06_11_D_5_stuck_leaves.json` + `step_06_11_D_5_hand_edit_checklist.md` + `apply_hand_edits.py`. | **$218.74 Stage 5 shadow** Session 19 / $0 billed via OAuth; cumulative $626.44 shadow | ✅ **CLOSED (Session 19)** |
| 6.11.D.6 | Stage 6 audit reviewer LLM per D-077 (Stage A 5-page → Stage B 40-page → closure) → Gate ⑤ (`safety_failed=False` + manageable FAIL count) → `gate_5_<ts>.json` checkpoint. Also serves as system-level Writer ≠ Reviewer audit (Rule D) for the 71 hand-edits + 5076 LLM translations. | $30.92 shadow / Anthropic $0 billed | ✅ **CLOSED (Session 20)** — Gate ⑤ PASS w/ user auth on 2 documented edge-case FAILs (page_292 D7 date-format heterogeneity + page_479 L learning-gloss); 3 detector patches (`c627e13`/`f7eecc7`/`114a1af`) + 6 hand-edits applied; checkpoint `gate_5_2026-05-16T19-27-52.json` emitted |
| 6.11.D.7 | Stage 7 export (D-078 dual gate) → 579-page `output/` | no LLM ($0) | ✅ **CLOSED (Session 21)** — Stage 7 PASS after 5 detector patches (`2c3c66f`/`8c68c2e`) + 2 hand-edits (page_292 en numeric date + page_445 answer_index real defect); 554 pages emitted to `output/` (1112 files); Gate A + Gate B both PASS; 254 choices normalized per F-CHOICE-MARKER policy |
| **6.11.E** | Release publication | no LLM ($0) | ✅ **CLOSED (Session 22)** — `itpassport-r6-v1.0.0` published to GitHub Releases on target `820235c`; 6 assets (2.11 MB); release_url https://github.com/hakupao/it-passport-learning/releases/tag/itpassport-r6-v1.0.0 |
| 6.11.E.1 | Push prior commits to origin/main | no | ✅ done (Session 20/21 close pushes; Session 21 final push at `820235c`) |
| 6.11.E.2 | `release.publish()` Python API invocation → `itpassport-r6-v1.0.0` | no | ✅ done (Session 22, `run_release_publish.py --confirm`, 6 assets uploaded) |
| 6.11.E.3 | Verify Release page: 6 assets, sizes match | no | ✅ done (Session 22, `gh release view` byte-for-byte match) |
| 6.11.E.4 | `evidence/.../step_06_11_release.md` + STATE.md sync Step 6.11 ✅ | no | ✅ done (this turn) |
| **6.12** | Phase 1 RETROSPECTIVE.md (per 规则 C) | no LLM ($0) | ✅ **DONE (Session 23 CLOSED 2026-05-17)** — `RETROSPECTIVE.md` FINAL 351 行 per Rule C + D-033 template. 6 turns: D-019 4Q → outline → full expansion → critic Rule D NEEDS-REWORK → Turn 5 rework (9 must-fix + §2.2 ⑤ Stage 7 firefight + §5.5 v2 backlog 16 条；D-058 critic 误报已驳回保留 ✅) → Turn 6 user terminal PASS (path α skip Reviewer #2). §3 ADR review: 22 ✅ / 2 ⚠️ (D-065 / D-071) / 1 🔄 (D-080) / 0 ❌；§4.1 cost variance -99.6% billed vs 预算中位 $250 (Mistral $0.579 + Anthropic $0 billed / shadow $657.36)；§5.5 v2 backlog 16 条结转 Phase 2 |

### Session 09 + 09b summary (Step 6.8 Stage 5 ✅ post Plan-B)

**Session 09 (false-PASS, then user retro)**:
- 393 trilingual leaves, 6 retry attempts produced 0 untrans residue
- D-074 locked: Stage 5 prompt wrapper-clause
- Closed at PASS, but user retro caught architectural bugs (see Session 09b)

**Session 09b (Plan-B fix)**:
- User retro worksheet (`docs/discussion/2026-05-07-stage5-user-retro-worksheet.md`) + 3 review sub-files caught:
  - Stage 4 `answer_index` bug: page_043 had `[0,0,0,0,0]` should be `[2,2,2,3,2]`
  - Stage 5 `_glossary_lookup` jp-mutation: 10 leaves on 7 pages had `translated.jp != structured.jp`
  - Glossary content: ~10 entries needed translation polish
- D-075 locked: Stage 5 jp-preservation contract + regression test
- D-076 locked: Stage 4 answer-line parsing + envelope `-1` rejection + regression test
- Stage 4 re-run (40 pages) + page_043 re-OCR (vision_full → cleaned/) + glossary 13 patches + Stage 5 re-run (40 pages, opus, chunk=8) + 2 chunk=1 retries on stubborn pages + 2 hand-translations
- **Final: 382/382 leaves translated, 0 UNTRANSLATED, 0 jp mutations, 10/10 question.answer_index match ground truth**
- Cumulative dry-run shadow **$47.44** / $0.05 billed (max-plan OAuth = $0 Anthropic billed)
- 212 unit tests pass (197 base + 15 Plan-B regression guards)

**Open per Plan-B Decision**:
- F-MISTRAL-ANSWER-LINE-LOSS — Phase-2 Stage 3 heuristic enhancement
- F-CHOICE-MARKER — Stage 6 WARN + Stage 7 export normalize
- F-COP21 — partially mitigated by glossary patch; remainder defers to Stage 7
- 2 hand-translations awaiting user verbal sign-off (claude-drafted, doc'd in evidence)

### Next sub-steps (6.9 onwards)

| Sub-step | Content | State |
|---|---|---|
| 6.9 | Stage 6 audit reviewer LLM (per D-060/D-061/D-063) | ⏸ — next entry point |
| 6.10 | Stage 7 export (envelope + JSON/Markdown/SQLite, refuse UNTRANSLATED) | ⏸ |
| 6.11 | 全本 579 pages run (per D-073 Stage C) + GitHub Release (per D-046) | ⏸ |
| 6.12 | Phase 1 收尾 RETROSPECTIVE.md (per 规则 C) | ⏸ |

### Resume entry-point for Session 11

1. Read this file (`docs/STATE.md`) — top table + §5 "Next sub-steps".
2. Read `docs/discussion/2026-05-07-session-10.md` §2 + §6/§7/§8 (D-077 lock, 3 commits, Stage A iteration ledger, close summary).
3. Read `evidence/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/step_06_audit.md` Stage A re-run #2 section (clean baseline) — Session 11 picks up from this verdict.
4. Read `docs/decisions/D-077-stage6-audit-reviewer.md` only if needed for Stage B mechanics (§2.7 Stage B plan + §2.8 halt strategy).
5. Resume at **Step 6.9.8 — Stage B 40-page dispatch**. Command in `step_06_audit.md` "Stage B dispatch plan" section. **GATE**: user must explicitly authorize Stage B LLM dispatch ("go Stage B" / "授权 Stage B"). Stage A re-run #2 user retro is implicit per "下次再跑 Stage B" instruction; if user wants explicit retro on the 5 pages first, present `audit/stage6_review.json` summary before dispatching.

---

## 5b. Historical resume note (pre-Session-09)

**Session 01-07 全已闭合**。Phase 1 设计 + 实施起手 + Stage 1 dry-run + Stage B 用户 retro PASS 全部完成。**Phase 1 Step 6 (stage 2-7 + 全本) 是 Session 08 第一题**。

**Session 07 关键产物 (HEAD `652b09e`)**:
- 9 commits: design baseline (`6d4035c`) → phase switch (`c6c3660`) → 包骨架 (`bd6b0c7`) → 核心模块 + 77 tests (`841f5b9`) → 内置 plugins + dry-run CLI + 92 tests (`5c2251c`) → state/log sync (`98f2a63`) → mistralai import fix (`140ce34`) → Step 5 evidence (`4a3958c`) → Session 07 close (`652b09e`)
- 92 unit tests pass
- 50 pages 真实 OCR 数据 + Stage B PASS evidence 落盘 (`evidence/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/step_01_audit.md`)
- 累计成本: $0.05 (Mistral) + $0 (Anthropic max plan)

**Session 08 起手 user 三选**:
- **(a) 直接开 Step 6** — Claude 从 stage 2 page classify 一路实施到 stage 7 + 全本，撞 cap 就停等 user 决策
- **(b) 先升级 Anthropic 双轨** — 加 ANTHROPIC_API_KEY env 走 pay-as-you-go ~$30 全本，不撞 max plan 5h quota（per D-069 §2.4 零代码变更升级）
- **(c) 先复盘 dry-run OCR samples 调 prompt** — review `data/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/ocr/page_*.md`，针对 known 小问题（HTML entity / 表格化）调 stage 5 翻译 prompt 或 stage 4 structure prompt

### 已锁的仓库形态（D-034 ~ D-053，Topic #3 产物）

```
. (mono-repo, 单 git repo, 所有 Phase 1-5)
├── pyproject.toml                # 根: uv workspace 伞包 (D-038)
│                                 #     hatchling backend (D-036)
│                                 #     requires-python = ">=3.11,<4.0" (D-037)
│                                 #     [tool.uv.workspace] members=["packages/*"] (D-039)
│                                 #     [dependency-groups] dev=[...] (PEP 735)
│                                 #     [tool.pytest.ini_options] testpaths+markers (D-041,042)
├── uv.lock                       # 单根 lock (D-039)
├── README.md                     # ✅ 已建 (D-048)
├── README.zh-CN.md               # ✅ 已建 (D-048)
├── CLAUDE.md                     # ✅ 已建 (D-049, 项目级 Claude 指引)
├── .gitignore                    # ⏳ 关 session 时建 (per D-044/045/046/050)
├── packages/
│   └── extractor/
│       ├── pyproject.toml        # name="cert-extractor", hatchling, runtime deps
│       ├── src/cert_extractor/
│       │   ├── __init__.py
│       │   ├── pipeline.py
│       │   └── plugins/{source,ocr,translator,exporter}/
│       └── tests/                # 测试与 src 平级 (D-040)
│           ├── conftest.py
│           ├── _fixtures/        # 下划线防 pytest collect (D-043)
│           │   ├── MANIFEST.md
│           │   ├── mini_sample.epub      # commit
│           │   └── pages/                # gitignored
│           ├── unit/             # @pytest.mark.unit (D-042)
│           ├── integration/      # @pytest.mark.integration
│           └── e2e/              # @pytest.mark.e2e
├── apps/                         # 未来 Phase 3；暂不入 workspace glob
├── docs/                         # ✅ 已建（共享）
├── pipelines/                    # YAML 配置（共享）
├── package.json                  # 未来加 (pnpm workspace)
├── evidence/                     # 实施期建（规则 A，commit）
├── failures/                     # 实施期建（规则 B，commit）
└── data/                         # 实施期建（runtime，gitignore per D-050）
    └── <cert_id>/runs/<run_id>/
        ├── raw/ ocr/ classified/ cleaned/
        ├── structured/ glossary/ translated/
        └── output/                       # GitHub Release 发版 (D-046)
```

> Canonical pyproject.toml + tests + .gitignore + data 范例见 [`docs/discussion/2026-05-06-session-02.md`](discussion/2026-05-06-session-02.md) §4.1 + §4.2 + §4.3 + §4.5。

### Session 06 ✅ Closed — Topic #7 全闭合 ★ Phase 1 实施 gate 解锁

| Q | OQ | D 锁定 | 一行 spec |
|---|---|---|---|
| — | OQ-06 | (status) | ✅ user 答 max 20 plan ok |
| — | OQ-07 | (status) | ✅ user 答 Scale plan 升级了 |
| Q32 | OQ-35 | **D-069** | Claude Agent SDK + max plan via OAuth；零额外费用起步；**独立 ADR**: `docs/decisions/D-069-anthropic-via-agent-sdk.md` |
| Q33 | OQ-36 | **D-070** | Mistral Python SDK + 50 pages dry-run + user 审核 → 全本 |
| Q34 | OQ-37 | **D-071** | 软硬 cap 三档（wall-time/cost/fail count）+ WARN 等人决 + 重跑上限；**独立 ADR**: `docs/decisions/D-071-budget-cap-and-emergency-halt.md` |
| Q35 | OQ-38 | **D-072** | per-stage cost 进 evidence + run summary 写 cost.json |
| Q36 | OQ-39 | **D-073** | 单 chapter dry-run → user retro → 全本；与规则 C retro 对接；**独立 ADR**: `docs/decisions/D-073-phase1-launch-strategy.md` |

> 历史: Session 01-06 全闭合。Topic #1~#7 全 ✅。**Phase 1 设计阶段彻底收尾**。

### 接续动作（Session 07 起手 / 新 Claude 接手）

1. 读本文件 (`docs/STATE.md`) — 30 秒概览（注意 **Phase 1 实施 gate ✅ 解锁**）
2. 读 `docs/discussion/2026-05-06-session-06.md` §2 + §6（5 D + 关 session 总结 + §6.6 就绪状态总览）
3. 必要时读 7 份 ADR (`D-058` schema / `D-061` reviewer / `D-063` audit failure / `D-065` plugin loading / `D-069` Anthropic SDK / `D-071` budget cap / `D-073` Phase 1 启动)
4. 必要时回看 session-01~05 §2 + §6 复习 D-001~D-068
5. **当前位置**: Topic #7 ✅ 闭合 + **Phase 1 实施 gate 解锁**。Session 07 起手是 user 选择题:
   - **(1) 进入 Phase 1 实施**（design 阶段不写代码 → 解锁；起手实施 dry-run 单 chapter per D-073）
   - **(2) 保留设计阶段** 开 Topic #8 (Phase 2-4 形态) / Topic #9 (Phase 1 实施前 retro 触发条件)
6. 进 Session 07 时按 user 选择执行

---

## 6. 文件权威指南 / Where to Find What

| 想知道...... | 看这里 |
|---|---|
| 项目当前状态全貌 | **本文件 (`docs/STATE.md`)** |
| 某条决定为什么这么定 | session 日志（搜 `D-NNN`），重大决定还有 `docs/decisions/D-NNN-*.md` |
| 已经讨论过什么 | `docs/discussion/` 目录按日期排 |
| 操作守则 / 命名规范 | `docs/discussion/README.md` + `docs/decisions/README.md` |
| 抽检证据 | `evidence/` 实施期落点；模板见 `docs/templates/evidence-template.md` |
| 失败归档 | `failures/` 实施期落点；模板见 `docs/templates/failure-template.md` (D-032) |
| 用户硬规则 | User CLAUDE.md 的 `<personal_operating_principles>` |
