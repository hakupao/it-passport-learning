# ⑤-2 全量保真掃引 U3b — 2019h31h の集計と是正 (S123)

⑤-2 全量核験の **U3b** (`docs/phase5/PLAN_52_units.md` §2 の U3 後半、D-146 第 5 unit)。母数は
`evidence/phase5/stage_06_quiz_fidelity/full52_population_S118.json` の 2019h31h (総 100 問 − 既往 s7x 双 pass 済 13 問)
= **87 問**。**Sonnet 5 単 pass** (U1 で PASS、U3a で本番適用済) を継続適用した 2 回目の本番 unit。

本 unit は同時に **D-147 (丸数字列挙の区切り字種)** を dataset 全体 23 問に適用した回でもある (§9a)。

---

## 1. 入力と実行

- run: `wf_f840c3f7-511` (label `u3b`、pass `sn`、model = **Sonnet 5**、agent_type general-purpose)
- マニフェスト: `data/ip/quiz/.phase2/u3b_fidelity_input_2019h31h.json` (`--precrop` 版、crop 83/87、`prev_page_png` 87/87)
- 結果 JSON: `evidence/phase5/stage_06_quiz_fidelity/full52_fidelity_u3b_2019h31h_sn.json`
- **覆盖 87/87 / UNREADABLE 0 / agents_error 0**
- 是正器: `scripts/quiz-fidfix-S123-u3b.mjs` (assert-once / 冪等 / `--dry-run`) +
  `scripts/quiz-marunum-sep-D147.mjs` (決定的正規表現 / dry-run 既定 / `--apply`)

### ⑨ precrop 較正の修正 (S123 log §1、本 unit の前提)

初回 prep は **0/87 crop** (40 ページ全て `page_mismatch`)。原因は見出し bucket を「exam 全体の行数」で選ぶ実装で、
2019h31h は 問N 見出しの左縁が奇数/偶数ページで 140/150 に揺れ、表・図枠の左縁 170 が行数首位を取って誤選択されていた。
修正 (`quiz-fidelity-prep-any.mjs` 1 箇所、`HEAD_PAGE_ROWS_LO/HI`) 後 **83/87 cropped**、
回帰として **2018h30h 69/70 (U3a) / 2015h27a 90/100 (U1) の crop PNG が md5 で byte 同一**。

### Sonnet 単 pass の実測 (U1 / U3a との対照)

| 項目 | U1 (2015h27a 68 問) | U3a (2018h30h 70 問) | **U3b (2019h31h 87 問)** |
|---|---|---|---|
| token | 4,030,000 | 4,249,378 | **5,167,455** |
| 時間 | 15 分 | 26.2 分 | **16.2 分** |
| tool 呼び出し | — | 601 (8.6 回/問) | **698 (8.0 回/問)** |
| part 壊れ | 2/68 → journal 復元 | 2/70 → journal 復元 | **1/87** (`q089`) → `--journal` で復元 |
| UNREADABLE | 0 | 0 | **0** |

> 申告 ≈ 520 万に対し実消費 517 万 (誤差 0.6%)。`journal.jsonl` が part の正である教訓は 3 unit 連続で再現。

---

## 2. 率

| exam | n | agent DISCREPANT | 率 | +machdiff | 計上 (率) | **是正** (率) | 正解肢上 | answer_affecting |
|---|---|---|---|---|---|---|---|---|
| 2019h31h | 87 | **10** | 11.5% | **+1** (`q001`) | 9 (**10.3%**) | **9 (10.3%)** | **1** | **0** |

- agent DISCREPANT 10 のうち **2 問は非採用** (`q082` 図の言語化 / `q084` 紙面の染み — §10)、machdiff 由来 `q001` を加えて **計上 9 問**。
- **severity 内訳** (17 論理差分): cosmetic **14** / semantic **3** / answer_affecting **0**。
  - agent の `bySeverity` は {cosmetic 14, semantic 4} = 18 差分。うち非採用 2 (`q082` semantic / `q084` cosmetic) を引き、
    machdiff 由来 `q001` (cosmetic) を足して **17 = cosmetic 14 / semantic 3**。
- **正解肢上 1** = `q062` choice.ウ (丸数字区切りの脱落、cosmetic)。`correct_answer=ウ` は不変。
- `correct_answer` 変更 **0 / 2900**、`data/ip/exams/answer_keys.json` **バイト不変** (md5 `6802bb0b…` 前後同一)。
- 単 pass のため severity の pass 間不一致は構造的に発生しない。裁定は fixer の原寸実読による (§4)。

### 既往波との比較

| | 標本 | 計上 | 率 | 正解肢上 | answer_affecting | 抄写 model |
|---|---|---|---|---|---|---|
| ⑤-3 (層化抽検 4 exam × 20) | 80 | 14 | 17.5% | 5 | 1 | Opus 双 pass |
| 波 1 (2015h27a + 2016h28a) | 152 | 13 | 8.6% | 3 | 1 | Opus 双 pass |
| 波 2 (2016h28h + 2017h29a) | 158 | 15 | 9.5% | 3 | 0 | Opus 双 pass |
| 波 3 (2017h29h + 2018h30a) | 152 | 12 | 7.9% | 0 | 1 | Opus 双 pass |
| U3a (2018h30h) | 70 | 8 | 11.4% | 2 | 1 | Sonnet 単 pass |
| **U3b (2019h31h)** | **87** | **9** | **10.3%** | **1** | **0** | **Sonnet 単 pass** |
| **累計 (波 1–3 + U3a + U3b)** | **619** | **57** | **9.2%** | **9** | **3** | — |

U3b の 10.3% は U3a の 11.4% とほぼ同水準で、波 1〜3 (7.9〜9.5%) よりやや高い。内訳は U3a と同じ傾向で、
cosmetic 14 のうち **11 が引用符・括弧の字種置換** (`q007` 4 対 / `q096` 6 対 + 亀甲 1) と **区切りの脱落** (`q062` 4 / `q064` 1)。
S119 で prompt に追記した「区切り記号脱落・引用符字種置換・源に無い記号の挿入も計上」が引き続き効いている結果であって、
dataset の品質が悪化したわけではない。**answer_affecting は本 unit 0 件**。

---

## 3. 機械 diff (machdiff) — AGENT_MISSED 18 / **偽陽性 13・非採用 4・実残差 1**

```
node scripts/quiz-fidelity-machdiff.mjs data/ip/quiz/.phase2/u3b_fidelity_input_2019h31h.json \
  evidence/phase5/stage_06_quiz_fidelity/full52_fidelity_u3b_2019h31h_sn.json
machdiff: fields same=417 | AGENT_MISSED=18 | VERDICT_CONFLICT=0 | audits w/o transcript=0 | UNREADABLE skipped=0 | coverage 87/87
```

| id | field | 残差の中身 | 判定 |
|---|---|---|---|
| `q052` / `q072` / `q082` | choice.ア〜エ (各 4、計 12) | 表型選択肢の transcript `[表] a: X, b: Y` vs 表示層の一行化 | **偽陽性** (既知型) |
| `q053` | stem | 源の表を表示層は選択肢側に載せる体裁差 | **偽陽性** (既知型) |
| `q059` | choice.ア〜エ (4) | 表 cell 末尾の句点。表の一行化に伴う体裁差 | **非採用** (主 context 裁定、⑨ に登記) |
| `q001` | stem | **stem 先頭に見出し「問1 」が混入** | **実残差 1 → 是正** |

- **実残差 1** → PLAN §2 の停止条件「machdiff 残差 > 3 (既知型偽陽性除外)」に**非該当**。
- `q001` は agent が **CLEAN** と判定していた。crop の見出しと同語であるため見逃したもので、
  **machdiff だけが拾えた 1 件**。U3a で「machdiff 由来の新規是正 0 件」だったのに対し、本 unit は 1 件出た。
  → **machdiff は agent の代替ではなく補完**という位置づけが再確認された (§11)。
- **`q001` は `stem_jp_clean` を持たない題** (sidecar / `.phase1` とも `stem` / `choices` のみ) で **raw `stem_jp` が出荷層**。
  したがって「問1 」は**学習者に見えていた**。cosmetic 分類だが可視性は高い。
- **machdiff の構造的盲点 (再掲・本 unit で該当)**: `normStr` は引用符 (`「」` ↔ `“”`)・括弧・読点字種・空白・波ダッシュ族を統一するため、
  `q007` / `q096` の引用符字種、`q053` / `q096` の亀甲括弧、`q062` / `q064` の区切り脱落は **machdiff では原理的に検出できない**。
  本 unit でこれらを拾えたのは agent (Sonnet) が S119 規定に従って計上したから。

---

## 4. 裁定 (fixer が源 crop 9 枚を原解像度〜12 倍で独立実読)

実読した crop: `data/ip/quiz/.phase2/precrop/2019h31h/` の
**q001 / q007 / q015 / q034 / q037 / q053 / q062 / q064 / q084 / q096** (源ページ page-02/04/07/14/15/21/25/26/34/39 に対応)。
agent の `source_text` は候補として扱い、**すべて原寸で読み、疑義のある字形は 4〜12 倍まで拡大して確定してから** from/to を決めた。

### 4a. `q001` — 見出しの混入 (machdiff 由来の唯一の実残差)

- page-02 の crop を原寸で実読。紙面は 「問 1」 が見出しとして独立して置かれ、設問本文は 「リスクアセスメントを…」 から始まる。
- dataset の raw `stem_jp` は `"問1 リスクアセスメントを…"` と見出しを本文に取り込んでいた → 除去。
- **`stem_jp_clean` が存在しないため raw が出荷層**。3 層 (`questions` / `question_bank` / `by_year`) に同一置換。

### 4b. `q007` — 引用符の字種 (源 `“ ”` U+201C/U+201D) **+ 区切りの脱落**

- page-04 の crop を原寸 → 4 倍で実読。**4 対すべて**が `“財務”` `“顧客”` `“業務プロセス”` `“学習と成長”` の欧文二重引用符。
  dataset の `stem_jp_clean` は全て `「」` に置換されていた → 波 2 `2016h28h-q094` / 波 3 `2017h29h-q090` / U3a `2018h30h-q001` と
  同一規則で**源の字形へ戻す**。
- **あわせて 4 項目間の区切りが全脱落**していた (源 `“財務”，“顧客”，…` / dataset `「財務」「顧客」「業務プロセス」「学習と成長」`)。
  → **§6 の「指示から外した点 (a)」**。区切り字種は D-147 の jp 規則 = ASCII `", "`。
- raw `stem_jp` は別系統の重腐敗 (`"財務", “顧客", “業務プロセス", “学 習と成長”` = ASCII `"` と `“` の混在 + 語中空白) で
  `from` が 0 回 → assert-once が自動 skip (§7)。**N5 系列として §11 に登記**。
- zh (`“财务”“顾客”…`) / en (`\"financial,\" \"customer,\"…`) は **⑨-c 保留**で不変 (各言語の慣行に従う)。

### 4c. `q015` — 語義是正 (semantic、zh/en 追随)

- page-07 の crop を原寸 → 6 倍で実読: 「多品種**大量**生産を効率的に行うリーン生産方式」。
  dataset は「多品種**少量**生産」で **語の意味が反転**していた。
- 「多品種少量生産を効率的に行う」は corpus 内で本題のみ (同語 `2011h23a-q019` は「多品種少量生産**向きの**生産方式」で別文脈、
  **不変**であることを機械確認済) → assert-once 成立。
- **zh / en は追随**: zh `多品种小批量生产` → `多品种大批量生产` / en `high-variety, low-volume production` → `high-variety, high-volume production`。
- **解説の書換は 0 件**。`expl_jp_2019h31h-q015.json` の「少量」は `distractors_jp[2]` の
  「一つの製品を最初から完成まで一人が担当するのはセル生産方式 (屋台生産) の説明。**多品種少量生産には適するが**、
  JIT・かんばんを取り込むリーン生産方式とは異なる方式。」であり、**セル生産方式についての独立に正しい記述**で
  stem の「大量/少量」の写しではない。`correct_jp` / `points_jp` / `key_guard` は 少量・大量 のいずれにも言及しない。
  stem が「大量」になると当該記述はむしろエを退ける根拠として**強まる**ため、追随不要と判定 (zh 小批量 / en low-volume も同じ理由で不変)。
- `correct_answer` は **イ のまま不変** (判別根拠は JIT・かんばんの有無で、生産量には依存しない)。

### 4d. `q034` — 源に無い括弧の挿入

- page-14 の crop を原寸で実読: 表の上の注記は **「単位　円」で括弧が無い**。dataset は `（単位 円）` と全角括弧を挿入していた。
- 波 2 `2017h29a-q001`「単位：万円／日」→ 源「単位　万円／日」/ 同 `q020`「単位：百万円」→「単位　百万円」の precedent と同型。
  全角空白 (U+3000) も当該 precedent に合わせた。
- **zh / en は不変**。波 2 precedent でも zh 「单位：万日元／日」/ en "Unit: million yen" は追随させていない (⑨-c)。
- markdown 表のヘッダ行 `| 項目 | | 金額 |` は源に文字として存在しないが、表形式化に伴う体裁 → **データ不変** (§8a / §10)。

### 4e. `q037` — 源に無い文字の挿入 (semantic)

- page-15 の crop を原寸で実読: 選択肢イ は 「リスクが発生するとプロジェクトに**問題が生じる**ので，」。
  dataset は 「問題が**な**生じるので」で、「が」の直後に源に無い「な」が入り**日本語として破綻**していた
  → 波 3 §4a の基準「非語になる欠落・挿入は semantic」に合致。
- zh 「就会给项目带来问题」/ en "problems arise in the project when risks occur" は既に忠実 → **追随不要**。
- 誤答肢 (正解はウ) のため答えには影響しない。

### 4f. `q053` / `q096` — 亀甲括弧 (源 `〔 〕` U+3014/U+3015)

- `q053` page-21 の crop を **8 倍**で実読: 見出しは `〔前提〕`。角の切り欠きが明確で `［前提］` ではない。dataset は `[前提]`。
- `q096` page-39 の crop を原寸で実読: 見出しは `〔指定方法〕`。dataset は `[指定方法]`。
- 波 1 precedent (dataset 全体で `〔` 288 / `【` 11 = `〔` が規範) に従い**源の字形へ戻す**。
- zh / en の `[前提]` / `[Premises]` / `[指定方法]` / `[Specification method]` は **⑨-c 保留**で不変
  (実測: zh は `〔` 153 / `[` 92 の混在、en は `[` 131 / `〔` 3 で `[` が明確な慣行)。

### 4g. `q096` — 引用符の字種。**`.phase1` が独立の裏取りになった**

- page-39 の crop を原寸で実読: (1)〜(4) の引用符は**すべて** `“ ”`
  (`“ディレクトリ名/…/ディレクトリ名/ファイル名”` / `“/”` ×3 / `“.”` / `“..”` = **6 対**)。dataset の sidecar は全て `「」`。
- **裏取り**: `data/ip/quiz/.phase1/tr_2019h31h-q096.json` の `stem_jp_clean` は **既に `“ ”` を保持**していた
  (括弧だけ `[指定方法]`)。つまり引用符の腐敗は **phase1 より下流の工程で入った**もので、
  源が `“ ”` であることが**別系統の転写でも独立に確認**できる。
- 是正後、sidecar の `stem_jp_clean` は **`.phase1` 版と逐字一致**することを機械確認済
  (差分は sidecar のみが持つ末尾の ASCII 木構造ブロック ``` ルート / d1 / d2 … ``` だけ)。
- raw `stem_jp` は別系統の重腐敗 (`[指定方法〕` の括弧混在 / `経路ヒト` / `“\" /”\"` 等) で 7 subs すべて `from` 0 回 → 自動 skip (§7)。
- 空白の有無 (`“/” で区切って` の前後空白) は既存の表示層のままとし、**引用符の字種だけ**を是正した (最小差分)。

### 4h. `q062` — 肢が参照する範囲の脱落 (semantic) + 丸数字区切りの脱落 (D-147)

- page-25 の crop を原寸で実読:
  - stem は 「取り扱うための**方式 ① 〜 ③** のうち，」。dataset は `方式のうち` で **①〜③ が脱落**していた。
    肢が `①` `②` `③` を参照する以上、範囲の明示が落ちると設問が自己完結しない → **semantic**。
  - 4 肢は `ア ①, ②` / `イ ①, ②, ③` / `ウ ①, ③` / `エ ②, ③` で、**区切りが全脱落**して `①②` 等になっていた。
- **波ダッシュの字種決定** (U3a §4e と同一規則「同 exam の兄弟問に合わせる」):
  源の紙面からは U+301C (`〜`) と U+FF5E (`～`) を**画素で判別できない** (どちらも同一の全角波形) ため字種は house rule でしか決まらない。
  実測 3 段で **U+301C** を採用した:

  | 根拠 | U+301C | U+FF5E | ASCII `~` |
  |---|---|---|---|
  | **同 exam の同構文兄弟問** `2019h31h-q082`「①〜③で使われている」 | **該当** | — | — |
  | 同 exam の波ダッシュ全体 | **5 問** (q004/q016/q029/q043/q082) | 2 問 (q061/q068) | 0 |
  | corpus 横断の丸数字レンジ `[①-⑳]〜[①-⑳]` | **11** | 9 | 1 |
  | corpus 横断の波ダッシュ全体 (jp raw、**出現回数**; 問数では 81 / 52 / 21 — S123 Rule D NIT-2) | **157** | 108 | 21 |

  3 段すべてで U+301C が優位。**兄弟問 `q082` が同一 exam・同一構文である点が決定的**。
  (agent の `source_text` は ASCII `~` で書かれているが、machdiff の `normStr` は `[〜～~]` を同一視するので判定に影響しない — §8b)
- **区切り字種は D-147**: jp `①, ③` / zh `①、③` / **en は `(1) and (3)` 形で丸数字を持たないため対象外**
  (D-147 §3「`and` 形は自然英語として適合扱い」)。
- zh / en の stem も範囲を補った: zh `…当作一块来处理的方式①〜③中` / en `Among the methods (1) to (3) for logically treating…`。
  **en は既存の `(1)/(2)/(3)` 様式を維持**した (D-147 §3: en の数字様式 ① vs (1) の統一は本 D の対象外)。
- **`ウ` は正解肢**だが変更は区切り記号のみで、`correct_answer=ウ` は不変。
- 解説の肢引用 (`「①③」` `「①②」` 等) は **U3a `2018h30h-q085` の precedent** (区切り是正後も expl の引用は不変で Rule D PASS) に従い
  **追随させない**。⑨ に横断登記 (§11)。

### 4i. `q064` — 読点の脱落

- page-26 の crop を原寸で実読: 選択肢ウ は 「送信側でウイルス検査を行い**，**ウイルスが含まれないパケットを送る。」。
  dataset は読点が空白 1 個に置き換わり `行い ウイルスが` になっていた → 区切り記号の脱落 (S119 規定の計上対象)。
- jp 字種は house rule の ASCII `", "` (同題の選択肢ア `加え, 2 回線で` と同じ)。
- zh 「在发送端进行病毒检查**，**发送不含病毒的数据包。」/ en "…and send packets…" は既に忠実 → **追随不要**。
- 誤答肢 (正解はア) のため答えには影響しない。

### 4j. `q084` — **非採用**。agent の「；」主張を画素計測で棄却

agent は選択肢ウ 「共有して」 直後の区切りを 「**；**」 (上にドットの付く形) と主張し、
「同ページ内の他の読点と字形を高倍率で比較した」と述べていた。**原寸 → 12 倍 + 連結成分の画素計測で棄却**した。

**(1) 当該マークの計測** (crop `2019h31h-q084.png`、閾値 grey<170 の 8 近傍連結成分):

| 成分 | 画素数 | bbox x | bbox y | 水平中心 | 最小輝度 | 平均輝度 |
|---|---|---|---|---|---|---|
| 読点本体 | 26 | 918..921 | 309..316 | **919.6** | 2 | 82 |
| 上のドット | 9 | 922..924 | 299..301 | **923.0** | 23 | **118** |

**(2) 同 exam の実在する 2 点句読点との対照** — `q053` 「生産性**：**プログラム」 のコロン:

| 成分 | 画素数 | bbox x | 水平中心 | 最小輝度 | 平均輝度 |
|---|---|---|---|---|---|
| 上の点 | 14 | 437..440 | **438.5** | 2 | 78 |
| 下の点 | 14 | 437..440 | **438.5** | 1 | 78 |

実在の 2 点句読点は **水平中心ずれ 0.0px / 画素数同一 / 輝度同一**。
一方 `q084` のドットは **中心が +3.4px 右にずれ** (読点本体の幅 4px より大きいずれ)、**画素数は 35%** (9/26、65% 減; S123 Rule D MINOR-1 訂正)、**平均輝度は 44% 淡い**。
3 指標すべてで印刷グリフとして不整合。

**(3) 母集団の裏取り** — 同一ページ `page-34` の**文字が無い余白帯** (150,1450)–(1300,1800) (S123 Rule D MINOR-2 訂正: y1300〜1390 は本文行のため y≥1450 に限定) を走査すると、
4〜16px の孤立斑点が 3 件あり、うち 1 件は `n=11 / 4x4 / 平均輝度 117` で **`q084` のドット (n=9 / 3x3 / 118) と区別できない**。

**(4) 言語的整合** — 「共有して；編集できる」は日本語の組版として成立しない (テ形接続は読点を取る)。

→ **紙面の染み**と判定。源は 「，」 で、dataset の `, ` は許容表記揺れ → **データ不変**。
U3a の `q062` / `q063` (末尾の `.` が染み由来) と同じクラスだが、本件は「染みが既存グリフに重なって別字に見えた」型で**初出**。§11 に登記。

---

## 5. SOURCE_TYPOS

本 unit では**該当なし** (源が誤っていて dataset が正しい型は 0 件)。
波 3 §5 の `2018h30a-q043` が唯一の既存登記であり、横断台帳化は §11 backlog のまま。

---

## 6. 採用した差分 (9 題 / 17 論理差分)

> 「17 論理差分」の定義 = agent が計上した 18 discrepancy のうち**採用 16** (`q082` / `q084` を除く) + **machdiff 由来 1** (`q001`)。
> 置換操作としての回数は §7 の field 数で数える (例: `q096` stem は 1 field だが 7 操作)。

| id (page) | field | 差分 | severity | 由来 |
|---|---|---|---|---|
| q001 (p02) | stem | 先頭に見出し `問1 ` が混入 → 除去。**raw が出荷層** | cosmetic | **machdiff** |
| q007 (p04) | stem | `「財務」`→`“財務”` ほか 4 対の引用符字種 **+ 4 項目間の区切り脱落** | cosmetic | agent (+ fixer 実読) |
| q015 (p07) | stem | 「多品種**少量**生産」→ 源「多品種**大量**生産」 | **semantic** | agent |
| q034 (p14) | stem | `（単位 円）`→ 源`単位　円` (源に無い括弧の挿入) | cosmetic | agent |
| q037 (p15) | choice.イ | 「問題が**な**生じるので」→ 源「問題が生じるので」 | **semantic** | agent |
| q053 (p21) | stem | `[前提]`→ 源`〔前提〕` (亀甲括弧) | cosmetic | agent |
| q062 (p25) | stem | 「方式のうち」→ 源「方式**①〜③**のうち」(肢が参照する範囲の脱落) | **semantic** | agent |
| q062 (p25) | choice.ア/イ/ウ/エ | 区切りの全脱落 `①②` → 源 `①, ②` ほか。**ウ は正解肢** | cosmetic ×4 | agent |
| q064 (p26) | choice.ウ | 読点の脱落 `行い␣ウイルスが` → 源 `行い，ウイルスが` | cosmetic | agent |
| q096 (p39) | stem | `[指定方法]`→`〔指定方法〕` + `「」`×6 対 → 源 `“ ”` | cosmetic ×5 | agent |

### zh / en の追随

| id | zh | en |
|---|---|---|
| q015 stem | `多品种小批量生产` → `多品种大批量生产` | `high-variety, low-volume` → `high-variety, high-volume` |
| q062 stem | `…处理的方式中` → `…处理的方式①〜③中` | `Among the methods for…` → `Among the methods (1) to (3) for…` |
| q062 choices | `①②` → `①、②` ほか (D-147) | `(1) and (2)` 形 → **不変** (D-147 §3 適合扱い) |

**追随不要と確認したもの** (既に源に忠実 / 腐敗が伝播していない):
`q037` イ zh「就会给项目带来问题」 en "problems arise in the project" / `q064` ウ zh 読点あり en 接続詞形。
`q007` / `q096` の zh・en 引用符、`q034` の zh「（单位：日元）」en "(Unit: yen)"、`q053` / `q096` の zh・en `[ ]` は
**⑨-c 保留**および波 2 precedent で不変。

### 指示から外した点 (2 件)

- **(a) `q007` の区切り脱落を追加採用**。主 context (S123 log §4) の採用リストは `q007` を「`「」`×4 → `“ ”`」のみとしていたが、
  源 page-04 の原寸 4 倍実読で **4 項目の間に読点が実在し dataset では全脱落**していることを確認した。
  区切りの脱落は **D-147 §2 が「源に区切りがある以上 cosmetic 差分として計上・是正」と明文で規定する型**で、
  本 unit の `q062` / `q064` と同一クラス。引用符だけ直して区切りを残すと**同一 stem 内で処置が割れる**ため同時に是正した。
  字種は D-147 の jp 規則どおり ASCII `", "`。
- **(b) `q084` ウ「；」は非採用**。指示は「原寸で字形を確認し、『，』なら非採用・本当に『；』なら採用」という条件付きで、
  §4j の画素計測により **「，」+ 紙面の染み** と確定したため非採用側に倒した (指示の条件分岐に従った結果)。

---

## 7. 層と局所性

| 層 | 置換操作数 | 備考 |
|---|---|---|
| raw (`questions.json` / `question_bank.json` / `by_year/2019h31h.json` の `stem_jp` / `choices_jp`) | **30** | 3 層に同一置換 (q001 1×3 + q015 1×3 + q034 1×3 + q037 1×3 + q053 1×3 + q062 choices 4×3 + q064 1×3) |
| sidecar (`translations/2019h31h.json`: `stem_jp_clean` / `stem.{zh,en}` / `choices.<L>.{zh,en}`) | **19** | **再 merge 禁止** (S117 §10a 失敗②) — sidecar と `.phase1` の両方に当てる。distinct field 数は 13 |
| `.phase1/tr_<id>.json` | **13** | q096 は `[指定方法]` の 1 操作のみ (引用符は既に `“ ”` — §4g) |
| 解説 `.phase2/expl_{jp,tr}_*.json` | **0** | §4c / §4h のとおり書換不要 |
| key_guard final note (`.phase2/generate_result_2019h31h.json`) | **3** | D-143: **final のみ**、round1 不可触 |
| **fidfix 小計** | **65** | `quiz-fidfix-S123-u3b.mjs` の `applied` と一致 |
| D-147 (`quiz-marunum-sep-D147.mjs --apply`) | **294** | 別勘定。§9a 参照 |
| **合計** | **359** | |

- **計上 skip 33** = raw / `.phase1` の別系統腐敗による assert-once の自動 skip。内訳:
  - `q007` raw `stem_jp` ×3 層 (raw は `"財務", “顧客"…` の ASCII/カーブ混在腐敗) = **3**
  - `q062` raw `stem_jp` ×3 層 (raw は `方式Dー③` / `論理的につのもの` の重腐敗) = **3**
  - `q096` raw `stem_jp` 7 操作 ×3 層 = **21**
  - `q096` `.phase1` の引用符 6 操作 (**既に `“ ”` のため from が 0 回** — §4g の裏取り) = **6**
- **無言 guard skip 4** = `q001` / `q015` の sidecar・`.phase1` に `stem_jp_clean` キー自体が不在 (`stem` / `choices` のみ) で
  `if (t?.stem_jp_clean)` / `if (t1?.stem_jp_clean)` が偽になり試行されない。**両題は raw が出荷層なので表示は正**。
  → 潜在 sub 102 = 計上 98 (applied 65 + skipped 33) + 無言 4。
- **key_guard final note の対象 3 件** = `q015` / `q037` / `q062`。**3 件とも純粋な後置**で、
  追記前 prefix が `key_guard_round1.note_jp` と**逐字一致することを機械照合済**。`key_guard_round1` への MARK 混入 **0 件**。
  - **NOTE_SUB は本 unit では該当なし**。既存 final note のいずれも「腐敗が**ある**」と現在形で主張していない
    (`q053` の note は raw の「59本」「4万円日」に言及するが、本 unit はそれらを触らないので矛盾しない)。
  - junk・記号のみの是正 (`q001` / `q007` / `q034` / `q053` / `q064` / `q096`) は**追記しない** — 波 2 / 波 3 / U3a と同一方針。
- `--dry-run` 再実行: **applied 0 / skipped 98** (完全冪等)。

### questions.json の差分 (build-quiz-corpus 再生成後)

| lane | 変更 id | 数 |
|---|---|---|
| **本 unit (U3b)** | `2019h31h-q001, q015, q034, q037, q053, q062, q064` | **7 問** |
| **D-147 (別 lane、§9a)** | `2012h24a-q058, 2013h25a-q001, 2013h25a-q044, 2013h25h-q081, 2014h26a-q056, 2014h26h-q076, 2015h27h-q070, 2016h28a-q067, 2016h28h-q066, 2018h30a-q041, 2018h30a-q057, 2018h30a-q081, 2019r01a-q089, 2019r01a-q096, 2022r04-q052` | **15 問** |
| 合計 | | **22 問** |

- `2019h31h-q007` / `q096` は **clean 層のみ**の是正なので `questions.json` には現れない (正)。
- `correct_answer` 変更 **0 / 2900** (HEAD の `questions.json` と全量照合)。`answer_keys.json` **バイト不変**。
  `git diff -- data/` の `+/-` 行で `correct_answer` を含むのは 1 行のみで、それは
  **`q062` の key_guard note 本文中の「correct_answer=ウ は不変」という記述**であり、実際の答えの変更ではない。
- **他 lane / 他 exam の踏み潰し 0**: U3b の変更 id はすべて `2019h31h-` 前缀、D-147 の変更 id はすべて §9a の 23 問の内側。
  さらに 29 exam **2900 問**について `question_bank` vs `by_year` の `stem_jp` + `choices_jp` を全量照合 → **不一致 0**。
- 図の産物に変更なし (本 unit に D-144 段 2 化・図の切り出しは無し) → `build-quiz-figures.mjs` は実行不要。

---

## 8. 事後核験 (Rule A 相当の自証)

**(a) 是正後 manifest を再構成して machdiff を再実行**

```
node scripts/quiz-fidelity-prep-any.mjs 2019h31h u3bpost <同じ 87 qnums> --precrop   # 87/87、crop 83/87 (事前と同一)
node scripts/quiz-fidelity-machdiff.mjs data/ip/quiz/.phase2/u3bpost_fidelity_input_2019h31h.json <同じ結果 JSON>
machdiff: fields same=417 | AGENT_MISSED=18 | VERDICT_CONFLICT=0 | coverage 87/87
```

残差 18 の**内訳が事前から 1 件入れ替わっている**。事前・事後の残差一覧を機械 diff した結果:

| | 事前 | 事後 |
|---|---|---|
| `q001` stem (**実残差**) | 残差 | **解消** ✅ |
| `q034` stem (markdown ヘッダ行) | 同一扱い | **残差として浮上** (新規) |
| `q052`/`q072`/`q082` choice ×12 + `q053` stem (偽陽性 13) | 残差 | 残差 (不変) |
| `q059` choice ×4 (非採用) | 残差 | 残差 (不変) |

**`q034` の浮上は machdiff の機構由来であり、データの退行ではない。** 証明:

1. agent の `q034` discrepancy の `current_text` は **是正前 displayed の全文**だった (機械確認: `pre.displayed_stem_jp === d.current_text` → `true`)。
   machdiff は「agent が報告した `current_text`→`source_text` を displayed に当ててから transcript と比べる」実装なので、
   **事前は field 全体が `source_text` に置換され、比較が自明に一致していた** (= 事前の「same」は空虚な一致)。
2. 是正後は `current_text` が displayed に含まれなくなり (`false`) パッチが当たらないため、
   **元から存在していた markdown ヘッダ行の差**が表に出た。
3. 是正後 displayed から **ヘッダ 2 行 `| 項目 | | 金額 |` / `|---|---|---:|` を除くと agent の `source_text` と一致する** (機械確認 → `true`)。
   **是正前は同じ操作をしても一致しなかった** (`false` — `（単位 円）` が残るため)。
4. 表内の数値は事前・事後で完全同一。事前→事後の唯一のテキスト差分は `"（単位 円）"` → `"単位　円"`。

→ **是正はフィデリティを厳密に改善しており、新規残差は §10 の「表型 / markdown 体裁差」と同じ既知クラス**。
   **実残差 0 / 新規の実質残差 0**。

**(b) 採用 discrepancy の `source_text` と是正後表示テキストの直接照合**
(許容表記揺れ = NFKC / 読点字種 `、，,` / 空白 のみ潰し、**引用符・括弧の字種は潰さない**):

| 判定 | 件数 | 内訳 |
|---|---|---|
| EXACT (逐字一致) | **2** | `q053` stem / `q096` stem (5 discrepancy すべて) |
| TOLERANT (許容表記揺れのみ差) | **8** | `q007` / `q015` / `q037` / `q062` choice.ア・イ・ウ・エ / `q064` |
| RESIDUAL | **2** | **両方とも説明済・データ正** (下記) |

- `q062` stem の RESIDUAL = **波ダッシュの字種のみ**。agent の `source_text` は ASCII `~`、dataset は U+301C (§4h の 3 段根拠)。
  `[〜～~]` を同一視すると**一致** (機械確認済)。machdiff の `normStr` も同じ同一視をするため machdiff 側では残差にならない。
- `q034` stem の RESIDUAL = **markdown ヘッダ行のみ**。除外すると**一致** (§8a-3 と同じ機械確認)。

→ **実残差 0**。

---

## 9. ゲート

| ゲート | 結果 |
|---|---|
| `node scripts/quiz-keys-crosscheck.mjs` | ✅ `questions=2900 exams=29 layerB=ran` / **all invariants hold (A1–A7, B1–B7)** |
| `node scripts/quiz-pagefix-derive-groups.mjs --assert-clean` | ✅ SPLIT_FIGURE 0 件 / **(A) 共有図メンバー全員がページ整合 / (B) 判定 JSON の SPLIT_FIGURE 50 件が両層で判定どおり** |
| `node scripts/quiz-chumon-groups-build.mjs --check` | ✅ `chumon_groups.json は生成結果と一致` |
| `pnpm -C apps/web exec tsc --noEmit` | ✅ **0 error** |
| `pnpm -C apps/web exec vitest run` | ✅ **33 passed / 1 skipped (34 files)、501 passed / 2 skipped (503 tests)** |
| `node --check` 両スクリプト | ✅ |
| `--dry-run` / dry-run 2 回目 | ✅ fidfix applied 0 / skipped 98、marunum 変更 0 / 23 問 (両者とも完全冪等) |
| `correct_answer` 変更 | ✅ **0 / 2900** (HEAD 比較)。`answer_keys.json` **バイト不変** (md5 前後同一) |
| `question_bank` vs `by_year` 全量照合 | ✅ **2900 問 / 不一致 0** |
| D-143 (final のみ / round1 不可触) | ✅ MARK 付き final note **3 件**、うち **3 件とも純粋後置** (prefix === round1 を機械照合)、round1 への MARK 混入 **0** |

再生成: `build-quiz-corpus.mjs` (2900 問 / 63 topic / 29 exam / **with_fig 511**) →
`quiz-phase2-merge.mjs 2019h31h` (explained 100 / missing 0 / **SUSPECT 0** / STEM-CORRUPTION 1 = `q100`、既知で本 unit の変更対象外)。

### 9a. D-147 の適用結果 — 丸数字列挙の区切り (23 問)

`scripts/quiz-marunum-sep-D147.mjs` (新設)。決定的正規表現
`/([①-⑳])([ \t　]*[,，、]?[ \t　]*)(?=[①-⑳])/g` で **choices の丸数字間の区切りだけ**を置換する。
題幹は一切触らない (D-147 §4)。**dry-run 既定**、`--apply` で書込。

**対象 23 問の同定**: 「選択肢が丸数字の組合せ」型を機械抽出した結果が **D-147 §背景 の実測 (ASCII 10 / `，` 10 / `、` 1 / 無し 2 = 23) と完全一致**。
`①と②` (2013h25a-q087) / `①→②` (2010h22a-q098) / `①and②` (2011h23tokubetsu-q059) / `d：③ e：①` (2010h22h-q095) /
表ヘッダの `①②③` (2020r02o-q094) は「丸数字の**組合せ選択肢**」ではないので 23 問に含めない。

**正規表現の安全性検証** (区切り以外のテキストが挟まると先読みが外れて**構造的に不変**):

| 入力 | 結果 |
|---|---|
| `(②, ③) と (④, ⑤, ⑥)` (jp 接続詞) | **UNCHANGED** (`) と (` 保存、括弧内のみ正規化) |
| `（②、③）和（④、⑤、⑥）` (zh 接続詞) | `和` **保存**、括弧内のみ正規化 |
| `①→②→④→⑤→⑧` (順序矢印) | **UNCHANGED** |
| `①と②` / `①and②` / `(①and②)or③` | **UNCHANGED** |
| `d：③ e：① f：②` (対応表) | **UNCHANGED** |
| `①, ②` (既に規範形) | **UNCHANGED** (冪等) |

**適用前後 (23 問全件)**:

| # | id | jp 前 | jp 後 | zh 前 | zh 後 | en 前 | en 後 |
|---|---|---|---|---|---|---|---|
| 1 | `2010h22a-q097` | `ASCII,␣` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 2 | `2012h24a-q058` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 3 | `2013h25a-q001` | `，` | `ASCII,␣` | `，` | `、` | `-` | `-` |
| 4 | `2013h25a-q044` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 5 | `2013h25h-q081` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 6 | `2014h26a-q056` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 7 | `2014h26h-q076` | `、` | `ASCII,␣` | `、` | `、` | `-` | `-` |
| 8 | `2015h27h-q070` | **`無`** | `ASCII,␣` | **`無`** | `、` | `-` | `-` |
| 9 | `2016h28a-q067` | `ASCII,` | `ASCII,␣` | `ASCII,` | `、` | `-` | `-` |
| 10 | `2016h28h-q066` | `ASCII,` | `ASCII,␣` | `ASCII,␣` | `、` | `ASCII,␣` | `ASCII,␣` |
| 11 | `2016h28h-q099` | `ASCII,␣` | `ASCII,␣` | `ASCII,␣` | `、` | `ASCII,␣` | `ASCII,␣` |
| 12 | `2017h29a-q031` | `ASCII,␣` | `ASCII,␣` | `、` | `、` | `ASCII,␣` | `ASCII,␣` |
| 13 | `2018h30a-q041` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 14 | `2018h30a-q057` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 15 | `2018h30a-q081` | `，` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 16 | `2018h30h-q072` | `ASCII,␣` | `ASCII,␣` | `、` | `、` | `ASCII,␣` | `ASCII,␣` |
| 17 | `2018h30h-q085` | `ASCII,␣` | `ASCII,␣` | `、` | `、` | `ASCII,␣` | `ASCII,␣` |
| 18 | **`2019h31h-q062`** | **`無`** | `ASCII,␣` | **`無`** | `、` | `-` | `-` |
| 19 | `2019r01a-q089` | `，` | `ASCII,␣` | `，` | `、` | `-` | `-` |
| 20 | `2019r01a-q096` | `，` | `ASCII,␣` | `，` | `、` | `-` | `-` |
| 21 | `2020r02o-q079` | `ASCII,␣` | `ASCII,␣` | `，` | `、` | `ASCII,␣` | `ASCII,␣` |
| 22 | `2020r02o-q098` | `ASCII,␣` | `ASCII,␣` | `、` | `、` | `-` | `-` |
| 23 | `2022r04-q052` | `ASCII,` | `ASCII,␣` | `ASCII,` | `、` | `ASCII,` | `ASCII,␣` |

> `-` = その言語の肢に丸数字が無い (`(1) and (2)` 形など) → D-147 §3 で**対象外**、正規表現も一切ヒットしない。
> 表の「前」は D-147 適用**直前** (= U3b fidfix 適用後) の状態。`2019h31h-q062` は fidfix が先に規範形へ直しているため
> marunum 側の変更数は 0 だが、**両スクリプトが独立に同一の目標形に到達した**ことの相互検証になっている (下記)。

- **結果: jp 23/23 = `①, ②` / zh 23/23 = `①、②` / en は丸数字を持つ 17 問のうち区切り形 15 問すべて `①, ②`、残 2 問は D-147 §3 の「and」形で不変 (S123 Rule D NIT-1 訂正)。字種の併存は解消。**
- 変化数: **jp 16 問 / zh 18 問 / en 1 問** (`2022r04-q052` のみ ASCII `,` → `, `)。書換 field 数 **294**。
- **相互検証**: `quiz-fidfix-S123-u3b.mjs` (源実読ベース、`2019h31h-q062` を jp `①, ③` / zh `①、③` へ) を先に適用した後、
  `quiz-marunum-sep-D147.mjs` の dry-run が `q062` を **n=0 (変更不要)** と報告した。
  **源実読の判断と D-147 の決定的規則が独立に同一の目標形に一致**したことの機械証明。
- 冪等: `--apply` 後の再 dry-run で **変更のあった問 0 / 23、書換 field 0**。

---

## 10. 見送り (クラス登記のみ、本 unit では是正しない)

| クラス | 該当 | 理由 |
|---|---|---|
| 図の言語化 | `q082` stem の箇条書き `- ①：PC1 → メールサーバ` 等 | D-141 型 (`has_figure=true`)。源に地の文は無く図のみだが、表示層の言語化は**データ不変** |
| 表型選択肢の一行化 | `q052` / `q072` / `q082` の choice ア〜エ (12 件) | transcript の `[表] a: X, b: Y` vs 表示層の一行化。machdiff の**既知型偽陽性** |
| 表の体裁差 (stem) | `q053` stem / **`q034` stem の markdown ヘッダ行 `\| 項目 \| \| 金額 \|`** | 源に文字として存在しない表形式化の産物。**データ不変** (§8a で機械証明) |
| **表 cell 末尾の句点** | `q059` choice ア〜エ (4 件) | 表の一行化に伴う体裁差。主 context 裁定で**非採用**。⑨ に「表型選択肢の cell 句点方針」として登記 |
| **紙面の染み (グリフ重畳型)** | `q084` ウ の「；」主張 | §4j の画素計測で**印刷グリフではない**と確定。源は「，」→ **データ不変** |
| 読点の字種 | `q001` / `q007` / `q034` / `q062` 等の `, ` vs 源 `，` | 従来どおり許容表記揺れ |
| 語間空白・全半角 | `q034`「単位␣円」の空白幅、`q053`「A 社」の語間空白、`q062`「1 台」 | ⑤-4 R8 lane の射程 / 許容表記揺れ |
| zh/en 引用符・括弧・単位注記 | `q007` / `q096` の zh・en `「」`、`q053` / `q096` の `[ ]`、`q034` の `（单位：日元）` | ⑨-c (「zh/en は各言語の慣行に従う」保留)。波 2 `2017h29a-q001` precedent と同じ |
| N5 (clean 保有題の raw 残存腐敗) | `q007` raw `"財務", “顧客"…` / `q053` raw「59本」「4万円日」/ `q062` raw「方式Dー③」「論理的につのもの」/ `q096` raw「[指定方法〕」「経路ヒト」「“\" /”\"」 | N5 系列。clean 層が出荷源なので学習者不可視。ただし `q053` の **「59本」は源「50本」で数値が変わる**ため §11 に格上げ登記 |

---

## 11. backlog (⑨ / 次 unit へ)

- **N6-a 完了**: 丸数字区切り backlog は **D-147 の 23 問一括適用で全解消**。
  U3a §11 が残していた 2 問 (`2015h27h-q070` / `2019h31h-q062`) を含め、区切り字種の exam 横断併存も同時に解消した。
  → **U3a §11 の「⑨ 新規: 丸数字区切りの字種が dataset 全体でばらついている」はクローズ**。
- **⑨ 新規 (優先度高): 解説 (`expl_*`) の肢引用が是正後の選択肢本文と乖離する。**
  `2019h31h-q062` の解説は肢を `「①③」`「①②」等と引用するが、是正後の肢は `①, ③` `①, ②`。
  U3a `2018h30h-q085` でも同じ乖離が発生し Rule D PASS 済 (precedent として本 unit も追随させていない)。
  **D-147 で 23 問の区切りが一括変更されたので、乖離は本 session でさらに広がった可能性がある。**
  → 23 問について「expl 内の丸数字引用 vs 現行 `choices_jp`」の突合を一度回し、方針 (追随する / 引用は独立とみなす) を D-NNN で固定することを推奨。
- **⑨ 新規: 紙面の染みがグリフに重畳して別字に見える型** (`q084` ウ「，」+ 上部の斑点 → agent は「；」と判定)。
  U3a の `q062` / `q063` は「単独の染みを `.` と誤読」だったが、本件は**既存グリフに重なって字種判定を誤らせる**型で初出。
  → fidelity prompt に「句読点の字種を主張するときは、同ページの実在する同種グリフと**画素サイズ・輝度・中心座標**で対照すること」を
  追記することを推奨。fixer 側の反証手順 (§4j の 4 段) は本 evidence が雛形になる。
- **⑨ 新規: `q001` 型 = `stem_jp_clean` を持たない題で raw に見出し「問N 」が混入。**
  本 unit では `q001` 1 件だが、**clean 層を持たない題は raw が直接出荷される**ため可視性が高い。
  → dataset 全体で `^問\d+\s` に一致する `stem_jp` を持ち、かつ `stem_jp_clean` が無い題を機械走査して台帳化することを推奨。
- **⑨ 新規: `q053` raw `stem_jp` の「59本」は源「50本」で数値が変わる N5 腐敗。**
  clean 層は正しい (「50本」) ので学習者には見えないが、`.phase1` / raw からの再 merge が起きると
  **問題の条件そのものが誤る**。U3a `2018h30h-q085` の「9個以上」(源「0個以上」) と同族。
  N5 の中でも「数値・論理が変わる型」の別ランク台帳化は **2 unit 連続で提起**されている — 次 unit で着手を推奨。
- **⑨ (継続): agent の `source_text` は差分文だけの truncated 引用になることがある** (U3a `q096` が実例)。
  本 unit では該当なし (`q096` は 5 discrepancy に分割されており全文が復元できた) が、事後照合は接頭辞/部分一致で行う運用を継続。
- **machdiff の位置づけ (更新)**: 本 unit の machdiff 由来の新規是正は **1 件** (`q001`)。
  U3a は 0 件だったが、本 unit で「agent が CLEAN と誤判定した題を machdiff だけが拾った」実例が出た。
  一方で machdiff は §8a のとおり **agent の `current_text` が field 全文だと比較が自明に一致してしまう**という
  構造的な弱点も併せて露呈した (`q034`)。**agent と machdiff は互いの盲点を埋める関係で、どちらも常設を維持**。
- **SOURCE_TYPOS の常設化** (波 3 §11 から繰越、本 unit では該当 0): `evidence/` 横断台帳は未着手。

---

## 12. Rule B (失敗記録)

**本 unit の抄写 run・是正器に失敗 attempt は無し。** run は 1 回で完走 (87/87 / UNREADABLE 0 / agents_error 0)、
part 壊れ 1/87 (`q089`) は `quiz-fidelity-merge-parts.mjs --journal` で復元でき再 run 不要。
是正器も初回適用で assert-once 違反 0、`--apply` 前の dry-run で全件の from/to を確認済。

**ただし S123 log §1 の precrop 較正失敗 (初回 prep が 0/87 crop) は本 unit の前段で発生した失敗**であり、
主 context が原因特定・修正・回帰確認 (U3a / U1 の crop PNG が md5 で byte 同一) まで完了させている。
`failures/` への新規追加は主 context の判断に委ねる (本 evidence では §1 に記録)。

---

## 13. Rule D

Writer = `u3b-fixer` (opus)。**Reviewer は別 `subagent_type` (opus) で本 evidence の後に別途実施すること**
(本ファイルは writer の自己申告であり、Rule D の審査は未了)。
審査時の重点:

1. 源 crop 9 枚の原寸実読による 17 差分の独立再確認 (特に `q007` の引用符 4 対 + 区切り、`q096` の引用符 6 対、`q062` の `①〜③`)。
2. **§4j の `q084` 非採用判定** — 画素計測 3 指標 (中心ずれ / 面積 / 輝度) と余白帯の斑点母集団による棄却が妥当か。
   **agent が明示的に「高倍率で比較した」と主張した箇所を覆している**ので、独立の再計測が必須。
3. **§6 指示から外した点 (a)** — `q007` の区切り脱落を追加採用した判断の当否。
4. **§4h の波ダッシュ字種 U+301C** — 同 exam 兄弟問 `q082` 準拠という決定と、3 段の実測根拠の再確認。
5. **§8a の `q034` 新規残差の無害性証明** — machdiff の patch 機構に起因し、データは改善しているという 4 段の論証の再検証。
6. **§9a の D-147 適用 23 問全件** — 対象同定 (23 問で過不足なし)、正規表現の安全性 (接続詞・矢印・対応表の不変)、
   jp/zh/en の到達形、`q062` における 2 スクリプトの独立一致。
7. `q015` 解説の追随 0 件 — 「セル生産方式は多品種少量生産に適する」が stem とは独立の正しい記述である、という判断の当否。


---

## 13. Rule D 独立審閲 (S123、reviewer `general-purpose` opus、fixer `oh-my-claudecode:executor` opus と別 type)

**判定: PASS-with-notes** — MAJOR 0 / 誤是正 0 / 漏れ 0 / `correct_answer` 変更 0。**データ修正を要する指摘 0**。
MINOR 3 / NIT 3 はいずれも evidence・脚本ヘッダの記述精度に関するもので、データ・判断は不変。

### 13a. 独立実読 (源ページ PNG から。fixer の precrop は使わず自前で切り出し)

前提検証: manifest の `question_crop_png` 10 枚が `source_page_png` の**画素完全な部分矩形**であることを全数照合
(いずれも x=0 の全幅切り出し、リサンプリング無し)。よって源ページからの実読は crop 実読と等価で、独立性が保たれる。
実読は源ページを 2.4〜14 倍に lanczos 拡大して行った。

| id (page) | fixer の from → to | 独立実読の結果 | 判定 |
|---|---|---|---|
| q001 (p02) | `問1 ` 除去 | 紙面は 「問 1」 が見出しとして独立配置、本文は 「リスクアセスメントを…」 から開始 | **一致** |
| q007 (p04) | `「」`×4 → `“ ”`、**+ 項目間の区切り** | 9 倍実読: 4 対すべて欧文二重引用符 (開き 66 形 / 閉じ 99 形)。**項目間に読点が実在** (`”，“` ×3) | **一致** |
| q015 (p07) | 少量 → **大量** | 「多品種**大量**生産を効率的に行うリーン生産方式」。語義反転の是正で正しい | **一致** |
| q034 (p14) | `（単位 円）` → `単位　円` | 表上の注記は 「単位　円」 で**括弧は無い**。全角空白 1 個 | **一致** |
| q037 (p15) | `問題がな生じる` → `問題が生じる` | 選択肢イ は 「問題が生じるので，リスクは全て回避する…」。「な」は源に無い | **一致** |
| q053 (p21) | `[前提]` → `〔前提〕` | 見出しは角の切り欠きが明確な亀甲括弧 `〔前提〕` | **一致** |
| q062 (p25) | stem に `①〜③` 補完 + 4 肢の区切り | stem は 「方式 ① 〜 ③ のうち，」。4 肢は `ア ①, ②` / `イ ①, ②, ③` / `ウ ①, ③` / `エ ②, ③` で**区切りが実在** (12 倍) | **一致** |
| q064 (p26) | `行い␣ウイルスが` → `行い, ウイルスが` | 選択肢ウ は 「ウイルス検査を行い，ウイルスが含まれない…」。読点が実在 | **一致** |
| q096 (p39) | `[指定方法]` → `〔指定方法〕` + `「」`×6 → `“ ”` | 亀甲括弧を確認。(1)〜(4) の引用符は**すべて `“ ”` で計 6 対** (path 形 1 / `“/”` ×3 / `“.”` / `“..”`) | **一致** |

**CLEAN 標本 2 題を無作為追加実読** — `q018` (p08 貸借対照表) / `q047` (p19 アジャイル開発): 題幹・4 肢とも源と逐字一致
(読点字種の許容揺れのみ)。**見落とし 0**。

### 13b. 非採用 2 件の独立裁定

- **`q084` ウ 「；」 → 非採用に同意 (fixer 支持、agent 棄却)**。自前の連結成分計測 (8 近傍 / grey<170) で fixer の数値を**完全再現**:
  読点本体 `n=26 x[918..921] y[1212..1219] cx=919.62 mean=82` / 上のドット `n=9 x[922..924] y[1202..1204] cx=923.00 mean=118`。
  **対照を fixer より強い形で取った** — 同 exam の実在コロン `q053`「生産性：」は上下 2 点とも **x 範囲が完全同一 `[437..440]`、中心ずれ 0.21px、面積 14 対 16、輝度 73 対 82**。
  一方 `q084` のドットは **x 範囲 `[922..924]` が読点本体 `[918..921]` と完全に離れており** (中心ずれ +3.38px)、面積は 35%、輝度は 44% 淡い。
  **印刷グリフの 2 点句読点は 2 点の x 範囲が一致する**という決定的性質を満たさない。
  さらに page-34 の**真の余白帯** (y≥1450) を走査すると孤立斑点 3 件、うち `n=11 / 4×4 / mean 117` が `q084` のドット (`n=9 / 3×3 / 118`) と区別できない。
  → **紙面の染みと確定**。源は「，」、dataset の `, ` は許容揺れ → **データ不変が正**。
- **`q082` stem 箇条書き → 非採用に同意**。源 page-33 は PC1/PC2 の**図**と選択肢の**表**のみで、`- ①：PC1 → メールサーバ` 等の地の文は紙面に存在しない。
  `has_figure=true` を確認。D-141 型の図の言語化で**データ不変が正**。あわせて q082 の選択肢が源では 3 列の表であることを確認 → machdiff の表型偽陽性も独立に裏付け。

### 13c. fixer の指示逸脱 2 点の裁定

- **(1) `q007` の項目間読点脱落を追加採用 → 容認**。源に読点が実在することを 9 倍実読で独立確認済 (13a)。
  D-147 §2 が「区切りの**脱落**は源に区切りがある以上 cosmetic 差分として計上・是正」と明文規定しており、本 unit の `q062` / `q064` と同一クラス。
  同一 stem 内で引用符だけ是正して区切りを残すと処置が割れるという理由も妥当。字種 ASCII `", "` は D-147 §1 の jp 規則どおり。
- **(2) `q062` 波ダッシュ = U+301C → 容認**。3 段の根拠を**全段独立再現**:
  兄弟問 `2019h31h-q082` の 「①〜③で使われている」 が **U+301C** で同一 exam・同一構文 (決定的) /
  exam 内 U+301C 5 問 (q004,q016,q029,q043,q082) 対 U+FF5E 2 問 (q061,q068) /
  corpus 横断の丸数字レンジ U+301C 11 対 U+FF5E 9 対 ASCII 1 / corpus 横断の波ダッシュ**出現回数** 157 対 108 対 21。
  源から両字種を画素判別できないという前提も妥当 (同一字形)。U3a §4e と同一規則で一貫。

### 13d. D-147 適用 23 問の全件確認

- **冪等**: `node scripts/quiz-marunum-sep-D147.mjs` (dry-run) → **変更のあった問 0 / 23、書換 field 0** ✅。`quiz-fidfix-S123-u3b.mjs --dry-run` → **applied 0 / skipped 98** ✅。
- **波及ゼロの機械証明**: HEAD と作業樹の**追跡下データ全件** (`questions.json` 2900 問 + `translations/` 14 exam) を field 単位で照合し、
  変化した全 field を「(a) marunum 正規表現で説明できる」「(b) U3b lane (2019h31h)」に分類 → **説明できない変更 0 件** ✅。
  marunum 起因は **128 field / 19 問**、すべて `choices.*` のみ。**題幹・他 field への波及 0** (1 件も無し)。
- **到達形 (全層)**: 23 問すべてで jp = ASCII `", "` / zh = `、` (U+3001) / en (区切り保有 15 問) = ASCII `", "`。
  `questions.json` / `question_bank.json` / `by_year` / sidecar / `.phase1` の **5 層すべてで字種一致、不一致 0** ✅。
- **en の `and` 形は不変**: HEAD 比で en が変化した肢は `2022r04-q052` の 4 肢のみ (ASCII `,` → `, `)。
  `and` 形 5 問はすべて保存 — 特に `2010h22a-q097` の **`(②, ③) and (④, ⑤, ⑥)`** は 「) and (」 が保存され括弧内だけ正規化されており、正規表現の安全性主張を実証 ✅。
- `2019h31h-q062` について fidfix (源実読) 適用後に marunum が n=0 を報告する = **2 脚本が独立に同一目標形へ到達**、を再現確認 ✅。

### 13e. 層の網羅・guard

- `questions` / `question_bank` / `by_year` の `stem_jp`+`choices_jp` を **2900 問全量照合 → 不一致 0** (bank 対 by_year、questions 対 bank とも) ✅。
- **再 merge 無し** (S117 失敗②): `q096` sidecar は sidecar 固有の ASCII 木構造ブロックを保持、`q034` sidecar は markdown 表を保持。
  追跡下 sidecar 14 exam に説明できない変更 0 (上記) → **phase1 からの上書きは起きていない** ✅。
- `q096` は sidecar・`.phase1` とも `“ ”` 6 対 + 亀甲括弧で一致 (§4g の裏取りを再現)。`q001` / `q015` の sidecar・`.phase1` に `stem_jp_clean` キーが**実在しない**ことを確認 (§7 の無言 skip 4 の説明は正) ✅。
- **D-143**: MARK 付き final note は **3 件** (`q015` / `q037` / `q062`)、**3 件とも純粋後置** (`final.startsWith(round1)` を機械照合)、
  `key_guard_round1` への MARK 混入 **0**、ファイル内 MARK 出現も **3** ✅。
- 解説 (`explanations/2019h31h.json`) の変更は **3 件の `key_guard.note_jp` のみ**。`correct` / `points` / `distractors` / `derived_answer` / `matches_key` は**全問不変** → §7「解説 0 件」は正 ✅。
- `correct_answer` **0 / 2900** 変更 (HEAD 比)、三層で答え一致、`answer_keys.json` md5 `6802bb0b…` (evidence 記載と同一) ✅。
- `questions.json` の変更 id は **22 問** = U3b 7 + D-147 15 で §7 の表と一致 ✅。

### 13f. ゲート再実行 (すべて reviewer が自分で実行)

| ゲート | 結果 |
|---|---|
| `node scripts/quiz-keys-crosscheck.mjs` | ✅ `questions=2900 exams=29 layerB=ran` / **all invariants hold (A1–A7, B1–B7)** |
| `node scripts/quiz-pagefix-derive-groups.mjs --assert-clean` | ✅ SPLIT_FIGURE 0 件 / (A)(B) とも GREEN |
| `node scripts/quiz-chumon-groups-build.mjs --check` | ✅ 生成結果と一致 |
| `pnpm -C apps/web exec tsc --noEmit` | ✅ **0 error** |
| `pnpm -C apps/web exec vitest run` | ✅ **33 passed / 1 skipped (34 files)、501 passed / 2 skipped (503 tests)** |
| `correct_answer` 変更 | ✅ **0 / 2900** |
| 事後 machdiff (自前で manifest 再生成 → 実行) | ✅ `same=417 / AGENT_MISSED=18 / VERDICT_CONFLICT=0 / coverage 87/87` |
| 事後 machdiff の残差内訳 | ✅ 偽陽性 13 (`q052`×4 / `q072`×4 / `q082`×4 / `q053` stem) + `q059`×4 (非採用) + `q034` 1 (構造的) = **18**、**`q001` は解消済** = §8a の説明と完全一致 |
| §8a の 4 段証明 | ✅ 自前実装で全段再現: (1) `current_text === 事前 displayed` **true** / (2) 是正後は含まれない **false** / (3) ヘッダ 2 行除去で `source_text` と一致 **true** (事前は **false**) / (4) 表内数値は事前事後同一、唯一の差は `（単位 円）`→`単位　円` |
| 計数の独立再計算 | ✅ 結果 JSON から n=87 / CLEAN 77 / DISCREPANT 10 / 差分 18 {cosmetic 14, semantic 4} / aa 0 / 正解肢上 1。採用 16 + machdiff 1 = **17** {cos 14 / sem 3 / aa 0}。累計 152+158+152+70+87=**619**、13+15+12+8+9=**57**、57/619=**9.2%**、正解肢上 3+3+0+2+1=**9**、aa 1+0+1+1+0=**3** — すべて evidence と一致 |
| `quiz-fidelity-prep-any.mjs` 回帰 | ✅ **2018h30h 全 70 問** 再実行 → 69/70 crop、crop PNG 集合 md5 `b318ddc7…` が**再実行前後で同一** / **2015h27a (u1ab 68 問)** → 63/68 crop、集合 md5 `5b48bd8b…` が同一。生成した `regr*` / `revpost*` manifest は削除済 |

### 13g. 指摘と処置 (主 context / fixer 宛。**MAJOR 0 = データ修正は不要**)

| # | 内容 | 処置 |
|---|---|---|
| MINOR-1 | §4j(1) 本文の「**画素数は 65%**」が誤り。実測 9/26 = **35%** (= 65% *減*)。表の生値 (9 / 26) は正しく、3 指標の結論も不変 | 文言訂正 |
| MINOR-2 | §4j(3) が page-34 の `(150,1300)–(1300,1800)` を「**文字が無い**余白帯」と記述しているが、この矩形の y1300〜1390 は**本文行そのもの** (グリフ連結成分 86 件)。真の余白は y≳1450。主張の斑点 (`n=11 / 4×4 / mean 117`) は **y=1549 に実在し再現する**ので結論は不変 | 領域座標を `y≥1450` に訂正 |
| MINOR-3 | `scripts/quiz-fidfix-S123-u3b.mjs` ヘッダ 18 行目が「9 題 / **18** 論理差分」。evidence §2 / §6 は **17** で定義も明示 (採用 16 + machdiff 1)。committed 成果物の自己矛盾 | 脚本ヘッダを 17 に訂正 |
| NIT-1 | §9a の「en は丸数字を持つ **15** 問すべて」— 実測は en に丸数字を持つ問が **17**、うち区切りが comma なのが 15、残り 2 (`2014h26h-q076` / `2015h27h-q070`) は「① and ②」形。表の凡例 `-` = 「その言語の肢に丸数字が無い」もこの 2 行では不正確 (正しくは「区切りが `and` 形」)。到達形の結論は不変 | 文言訂正 |
| NIT-2 | §2 の「corpus 横断の波ダッシュ全体 (jp raw) 157 / 108 / 21」は**出現回数**。**問数**では 81 / 52 / 21。同じ表の上 3 行は「問数」なので単位が混在 | 列に単位を明記 |
| NIT-3 | `quiz-fidelity-prep-any.mjs` の `HEAD_PAGE_ROWS_LO = MIN_H` は次元の異なる 2 量 (`MIN_H` = 見出し 1 行の**画素高** 14px / 本定数 = 1 ページあたりの**行数**下限 14 行) を同一定数に相乗りさせている。現状の動作は正しいが、帯高の都合で `MIN_H` を変えると較正が黙って変わる | 独立した定数に分離 |

**脚本 `quiz-fidelity-prep-any.mjs` のコードレビュー (S123 改修)**: 変更は 1 箇所で、head bucket の選択を「総行数」から
「その bucket が 14〜150 行載るページ数の投票 (同票は総行数)」に変えるもの。**投票が 0 のときは旧挙動 (行数順) に自然縮退**するため
安全側で、既存 exam の較正を壊さないことを 2 exam の全量回帰 (crop PNG md5 同一) で確認した。
図枠は 220〜470 行で HI=150 から十分離れており閾値は際どくない。ログに上位 3 候補の票数/行数を出す改善も妥当。
なお 2018h30h では投票が **34p 対 34p の同票**で、結果は行数タイブレーク (旧規則) が決めている — 本 exam に関しては新ロジックの寄与は無く、
byte 同一なのはそのため。欠陥ではないが、投票の識別力は exam 依存であることを記録しておく。

### 13h. ⑨ backlog の事実確認

§11 の「解説の肢引用が是正後の選択肢本文と乖離する」は**実在を確認**した。
`2019h31h-q062` の解説は肢を 「①③」「①②」「①②③」「②③」 と引用する一方、現行 `choices_jp` は `①, ③` / `①, ②` / `①, ②, ③` / `②, ③`。
さらに本 unit で追記した final note 自身が `①, ③` / `①、③` を含むため、**同一解説オブジェクト内に旧形と新形が併存**している。
U3a `2018h30h-q085` の precedent どおり本 unit で追随させない判断は容認するが、D-147 で 23 問に拡大した以上、
§11 が推奨する「23 問の突合 + D-NNN での方針固定」の優先度は高い。
`q053` raw の `59本` (源は **50本** — 源ページ p21 で実読確認) が clean 層 `50本` と食い違う N5 腐敗も実在を確認、§11 の格上げ登記は妥当。
