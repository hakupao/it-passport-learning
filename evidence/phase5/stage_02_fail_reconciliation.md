# Stage 2 補完 — FAIL 図表計数の調和 (Session 71 / Item 1)

**日付**: 2026-05-29
**目的**: STATE.md / session-70 が記載する「図表 FAIL 102 件」と `_all_fails.json` の `fails` 配列 97 件の食い違いを解明し、修復作業用の canonical リストを確定する。

---

## 結論

| 数値 | 真偽 | 根拠 |
|------|------|------|
| `total_fail: 102` | ❌ **誤り** | `_all_fails.json` のメタ数値。どの実データとも一致しない。 |
| `fails` 配列 97 件 | △ 重複あり | `2013h25h-q068.png` が 2 回登場（理由違いの 2 エントリ）。 |
| **真の唯一 FAIL = 96 件** | ✅ **確定** | 重複排除後。26 套にまたがる。 |

→ STATE.md / session-70 / PLAN の「102」「97」はすべて **96** に訂正されるべき。

---

## 調和プロセス（証拠）

### 1. verify_batch 13 ファイルの再集計

```
_verify_batch_01..13.json を全パース（配列 / オブジェクト両形式対応）
総検証 verdict: 512
  PASS: 415
  FAIL: 97
```

### 2. 512 ↔ 502（ディスク PNG 数）の調和

- batch エントリを file 単位で去重 → **唯一ファイル 502**（ディスク PNG 502 と完全一致 ✅）
- 差分 10 = `2013h25h` 套の 10 ファイルが **2 バッチで重複検証**された分
  - `2013h25h-q061/q067/q068/q074/q076/q082/q083/q096/q098/q099`
- うち 1 件 `2013h25h-q074.png` のみ判定衝突（一方 PASS・一方 FAIL）→ 保守的に **FAIL 計上**（選択肢 ア/イ が上端で切断との指摘あり、要再裁剪）

### 3. 97 → 96（FAIL 配列の重複）

- `fails` 配列 97 件 → 去重後 **96 件**
- 重複: `2013h25h-q068.png`（×2、両方 FAIL・理由文言違い）

### 4. 整合性チェック

| 項目 | 結果 |
|------|------|
| 96 FAIL ファイルのディスク存在 | 96/96 ✅（欠損 0） |
| question_bank.json との突合 | 96/96 ✅（全件 id 一致、`source.page_image` 完備） |

---

## FAIL カテゴリ分布（96 件）

| カテゴリ | 件数 | 修復方針 |
|---------|------|---------|
| `bbox_truncated`（座標不正確で截断） | 84 | 源ページ PNG から bbox 再推定 → 再裁剪 |
| `mislabeled`（実際は図なし／本文のみ） | 5 | `has_figure=false` に訂正 + figure_path 等除去（裁剪しない） |
| `other`（その他不備） | 5 | 個別精査 |
| `blank`（空白・劣化画像） | 2 | 源ページ再確認 → 再裁剪 or 図なし判定 |

### 套別分布（古い年度ほど多 = スキャン品質起因）

```
2009h21a:6  2009h21h:3  2010h22a:9  2010h22h:5  2011h23a:6
2011h23tokubetsu:3  2012h24a:2  2012h24h:2  2013h25a:1  2013h25h:3
2014h26a:9  2014h26h:2  2015h27a:1  2015h27h:2  2016h28a:3
2016h28h:2  2017h29a:2  2018h30a:4  2018h30h:2  2019h31h:2
2020r02o:6  2021r03:2  2022r04:7  2023r05:3  2024r06:6  2025r07:3
```

---

## 成果物

| ファイル | 内容 |
|---------|------|
| `data/ip/exams/figures/_fails_canonical.json` | 唯一 96 件の修復作業リスト（id / file / category / fail_reason / figure_type / current_bbox_pct / page_image / page_number 完備） |

`_all_fails.json` は **Rule B により削除せず温存**（誤った `total_fail:102` も履歴として残す）。canonical リストが正本。

---

## 次アクション（Item 3）

`_fails_canonical.json` を入力に、84 件の `bbox_truncated` を源ページ PNG から bbox 再推定 → 再裁剪。`mislabeled` 5 件は図フラグ除去。`blank`/`other` は個別判定。
