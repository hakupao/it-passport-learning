# Phase 2 Step 12 — UI smoke 2026-05-20

## Harness

- Chrome DevTools MCP (existing browser session; HTTP auth cache from previous Session 43 carried Basic Auth credentials, so `mcp__chrome-devtools__new_page` against bare `https://web-mu-sandy-78.vercel.app/` succeeded without needing to embed credentials in the URL — a clean improvement over the Session 41 Rule B archive 2 pattern).
- Prod canonical: `https://web-mu-sandy-78.vercel.app/`
- Active deploy: `dpl_A9u6j1xDJzaT2Ph5Tr6PAh3zVdEA` (target=production, aliased canonical)

## Flow (chronological)

| step | action | URL | assertion | result |
|---|---|---|---|---|
| 1 | Open `/` (root) | redirected to `/en/chat` | next-intl Accept-Language negotiation matched the browser's `en-US` preference (default-locale `ja` would only kick in for unmatched preferences) | ✅ valid behaviour |
| 2 | Snapshot a11y tree | — | NavTabs (Chat / Quiz / Glossary) + LocaleSwitcher combobox (label "Language", value "English", 3 options 日本語/中文/English) + heading "IT Passport — Chat" + subtitle "Textbook-grounded trilingual tutor (α private)" + button "New chat" + textbox "Message input" + button "Send" | ✅ all English chrome strings render from `messages/en.json` |
| 2b | Persisted chat history | (carries from prior session via localStorage) | prior Session 43 zh-CN conversation "第二缓存是什么" + AI reply still in DOM | ✅ D-085 §2.2 Resume CONTRACT VERIFIED CROSS-SESSION (5+ days persistence) AND CROSS-LOCALE (zh content displayed under en chrome) |
| 3 | LocaleSwitcher en → 中文 | `/en/chat` → `/zh/chat` | URL prefix flips; all chrome strings translate; combobox disabled briefly during useTransition; selected value = "中文" | ✅ verified |
| 3a | zh chrome strings | navigation.title "IT 护照三语学习" / tab "对话" "题库" "术语表" / label "语言" / heading "IT 护照 — 对话" / subtitle "基于教材的三语辅导（α 自用）" / button "新对话" / textbox "消息输入" / button "发送" | every label from `messages/zh.json` | ✅ all surfaced correctly |
| 3b | localStorage chat history | preserved across locale change | "第二缓存是什么" still in DOM | ✅ localStorage is locale-independent (correct design — chat history is per-user-not-per-locale) |
| 4 | LocaleSwitcher zh → 日本語 | `/zh/chat` → `/ja/chat` | URL prefix flips; chrome strings translate to ja | ✅ verified |
| 4a | ja chrome strings | nav.title "IT パスポート 三語学習" / tab "Chat" "問題集" "用語集" / label "言語" / heading "IT パスポート — Chat" / subtitle "教科書ベース三語チューター（α 自用）" / button "新しい会話" / textbox "メッセージ入力" / button "送信" | every label from `messages/ja.json` | ✅ all surfaced correctly |
| 5 | NavTab ja → Glossary (Quiz tab is the middle; we go directly to Glossary via URL) | `/ja/glossary?term=...` direct nav (the 908-card a11y tree is too large for snapshot — direct URL skips the page click) | next-intl `<Link>` keeps locale prefix | ✅ verified |
| 6 | Direct nav: `/ja/glossary?term=アルゴリズム` (URL-encoded) | modal opens via `useSearchParams()` reactive backing | TermPopover busy state shows ja "AI が用語を解説中…" / streams 3-section trilingual response | ✅ verified |
| 6a | 真 LLM call → DeepSeek-chat `/api/glossary/hover` body `{surface_jp: "アルゴリズム"}` | streaming reply | 3-section structure: **一行で要点** (JP) + **中文解説** (ZH) + **English Gloss** (EN) | ✅ corpus-grounded trilingual |
| 6b | Cache-hit usage hint | UI badge `tokens in=400 (cache hit 96%) · out=78` | identical 400-tok input AND 96% hit AS BOTH Session 39 (5 days prior) AND Session 43 (1 day prior) | ✅ **cross-session DeepSeek prefix cache TTL > 6 days on prod re-confirmed** (ratchets Session 43's "> 5 days" by ~24h) |
| 7 | Take 4 screenshots (1 per locale + 1 modal) | `screenshot_1_ja_chat.png` `screenshot_2_zh_quiz.png` `screenshot_3_en_glossary.png` `screenshot_4_ja_glossary_modal_algoritmu.png` | covers all 3 locales × 3 surfaces (Chat / Quiz / Glossary) + modal trilingual output | ✅ |
| 8 | Console messages | `mcp__chrome-devtools__list_console_messages types=[error,warn]` | 0 errors; 1 warn (zustand default-export deprecation — pre-existing transitive dep, NOT from Step 12 changes; will surface in any AI SDK app on this branch) | ✅ clean |
| 9 | `vercel logs --grep '[tripwire]'` | blocked by auto-mode classifier (out-of-band log inspection on prod canonical without explicit gate) | observable proxy: β cache hit rate = 96% >> 50% threshold → β tripwire would NOT fire by design (Q3=a healthy-silent detector) | ✅ no β fire inferred |

## Pre-smoke firewall probes (curl, on prod canonical)

| path | no-auth probe | expected | actual |
|---|---|---|---|
| `/ja/chat` | `curl -sI` | 401 + `www-authenticate: Basic realm="IT Passport Learning firewall"` | ✅ matched |
| `/chat` (unprefixed) | `curl -sI` | 401 (firewall first, BEFORE i18n redirect — proves composition order) | ✅ matched |
| `/api/chat` | `curl -sI` | 401 (firewall covers API too) | ✅ matched |

**D-097 firewall fully intact on prod canonical** + **D-099 §2.5 LD-2 composition order verified end-to-end**.

## Smoke summary

- **Locales tested**: en (first), zh (second), ja (final default settled). Cross-locale round-trip: en → zh → ja, all 3 directions clean.
- **Surfaces tested**: Chat (3 locales), Quiz (1 locale = zh), Glossary (2 locales = ja + en), modal (1 = ja).
- **真 LLM calls**: 1 on `/ja/glossary?term=アルゴリズム` → 400 in / 78 out / 96% cache hit / ≤3s wall TTFT.
- **D-085 §2.2 Resume**: ✅ verified cross-session + cross-locale persistence of chat history.
- **D-097 firewall**: ✅ intact via curl + via HTTP auth cache pass-through in browser.
- **D-099 §2.5 LD-2 middleware compose**: ✅ firewall-first then api-skip then i18n verified by curl + vitest.
- **β tripwire**: 0 fires inferred (96% hit ratio); cross-session TTL > 6 days re-confirmed (Sessions 39 / 43 / 44 same surface, identical 96% hit).
- **Console**: 0 errors; 1 unrelated zustand deprecation warning.

## Cost

- Call: 16 × $0.27/1M (miss) + 384 × $0.07/1M (hit) + 78 × $1.10/1M (out)
- ≈ **$0.000122 真 billed**
- Phase 2 cumulative: $0.0823 (Session 43 close) + $0.000122 ≈ **$0.0824** vs D-090 α-silent $5 cap = **60× headroom**

## Screenshots

- `screenshot_1_ja_chat.png` — /ja/chat post-localeSwitcher zh→ja with persisted chat history visible
- `screenshot_2_zh_quiz.png` — /zh/quiz NavTabs ja→zh→quiz round trip with full zh chrome
- `screenshot_3_en_glossary.png` — /en/glossary with English chrome strings and 908-card grid
- `screenshot_4_ja_glossary_modal_algoritmu.png` — TermPopover modal open on アルゴリズム with 3-section trilingual response and UI usage hint visible
