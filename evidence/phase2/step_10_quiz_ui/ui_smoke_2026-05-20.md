# UI smoke log — Step 10 Quiz Explain modal · 2026-05-20

Session 42 · 1 real LLM UI call via Chrome DevTools MCP + 1 ancillary fetch via `evaluate_script` to confirm the absolute-URL fix.

## Environment

- Deploy: `dpl_BqdybbaGeBEqmvX9vv5zUVwKmHEL` (prod-v3), aliased canonical `https://web-mu-sandy-78.vercel.app/`.
- Build: `pnpm build` ⇒ `/quiz` 4.16kB / First Load JS 118 kB / Middleware 37.6 kB / 11 routes total.
- Auth: D-097 firewall Basic Auth ⇒ initial navigation via `https://claude:<32hex>@web-mu-sandy-78.vercel.app/quiz` (`<QuizList />` mount effect immediately strips creds via `history.replaceState`).
- Browser: Chrome (Chrome DevTools MCP managed instance).

## Smoke sequence

| # | Action | Result |
|---|---|---|
| 1 | `new_page` with credentialed URL → `/quiz` | HTTP 200; full 254-card list rendered |
| 2 | `evaluate_script` ⇒ `{ href, hasAt }` | `hasAt: false` ⇒ defensive strip worked on `window.location.href` |
| 3 | `evaluate_script` ⇒ `{ baseURI, documentURI }` | Both STILL contain `claude:...@` — root cause of v2 failure |
| 4 | `evaluate_script` ⇒ `fetch(new URL('/api/quiz/explain', window.location.origin), ...)` | HTTP 200; SSE deltas streaming `### ...` (test fetch — primes the prefix cache) |
| 5 | `click` uid `13_9` (page 42 問1 「解説を見る」) | URL → `?qid=page_042_entity_0`; modal opens (`role=dialog` uid `14_0`) |
| 6 | `take_screenshot` busy phase | `screenshot_2_modal_busy.png` saved (skeleton + indeterminate progress bar) |
| 7 | `wait_for` ["tokens in=", "選択肢の判定", "AI 暂时不可用"] | resolves on streaming completion |
| 8 | snapshot grep usage hint | `TOKENS IN=2693 (CACHE HIT 100%) · OUT=621` |
| 9 | `take_screenshot` done phase fullPage | `screenshot_3_modal_done.png` saved |
| 10 | `click` uid `14_8` ("閉じる / 关闭") | modal dismisses; `?qid=` removed |
| 11 | `evaluate_script` ⇒ `{ href, search, dialogPresent }` | `search: ""`, `dialogPresent: false` |
| 12 | `take_screenshot` post-close | `screenshot_4_post_close.png` saved |

## LLM call records (via `vercel logs --json | grep quiz/explain`)

### Call A — `evaluate_script` test fetch (in-step diagnostic for the baseURI fix)

```json
{
  "provider": "deepseek",
  "scope": "question",
  "question_id": "page_042_entity_0",
  "page": 42,
  "entity_index": 0,
  "tokenEstimate": 2610,
  "inputTokens": 2693,
  "outputTokens": 831,
  "totalTokens": 3524,
  "providerMetadata": {
    "deepseek": {
      "promptCacheHitTokens": 2688,
      "promptCacheMissTokens": 5
    }
  }
}
```

- **Cache hit ratio**: 2688 / 2693 = **99.81%** (matches Step 6 Session 38 data point identically).
- **Wall**: ~5-7s (R1 reasoner). No β tripwire fire.

### Call B — UI click smoke (the headline data point)

```json
{
  "provider": "deepseek",
  "scope": "question",
  "question_id": "page_042_entity_0",
  "page": 42,
  "entity_index": 0,
  "tokenEstimate": 2610,
  "inputTokens": 2693,
  "outputTokens": 621,
  "totalTokens": 3314,
  "providerMetadata": {
    "deepseek": {
      "promptCacheHitTokens": 2688,
      "promptCacheMissTokens": 5
    }
  }
}
```

- **Cache hit ratio**: 2688 / 2693 = **99.81%**.
- **Output**: 621 tokens (slightly over 600-tok soft cap in `QUIZ_SYSTEM_INSTRUCTION`; acceptable).
- **Visible UI usage hint**: `TOKENS IN=2693 (CACHE HIT 100%) · OUT=621` — UI rounds (2688/2693)*100 → 100% via `Math.round`. Internal precise figure remains 99.81%.
- **R1 output coherence**: 3-section structure honoured per QUIZ_SYSTEM_INSTRUCTION:
  1. 問題の要点 (one-liner: definition of 経営理念)
  2. 各選択肢の判定 (all 4 choices walked; ア correct + 経営資源/経営戦略/企業文化 distinguished for イ/ウ/エ)
  3. 正答の理由 (concise grounded justification)

## Tripwire status

`vercel logs --json | grep '\\[tripwire\\]'` ⇒ **empty** — δ runtime detector silent under healthy operation (correct per D-091 §2.5(β) + Step 8 Q3=a log-only design).

## Observability findings (non-failures)

1. **β cache hit ratified across 2 sessions**: Session 38 measured 99.81% on this exact same `page_042_entity_0` question. Session 42 (4 days later) measured 99.81% identically. D-088 §2.3 stable-prefix invariant ratified across long TTL windows. DeepSeek prefix cache TTL clearly > 48h on prod (Session 41 measured > 5h; Session 42 ratchets the floor to > 4 days).

2. **AI SDK system-message-in-prompts security warning** still emitted per call (intentional D-095 §2.3 layout; mitigation `allowSystemInMessages: true` remains deferred as cosmetic per Sessions 40-41 backlog).

3. **R1 empty-delta NOT reproduced this run** — Call A produced 831 output tokens; Call B produced 621 output tokens; both with substantial `text` deltas. Continues to support the Session 40 "non-deterministic" hypothesis. The `QuizExplain` empty-output safety net (`onComplete: loading → error`) was not exercised live; covered by vitest case "fires onComplete exactly once even on the empty-delta case".

4. **Output token soft-cap drift**: QUIZ_SYSTEM_INSTRUCTION says "≤600 tokens"; Call B emitted 621 (+3.5% over). Acceptable for a single data point. Cluster monitoring deferred to Module C/D backlog.

## Cost

- DeepSeek-reasoner pricing: input miss $0.14/1M; input hit $0.014/1M; output $2.19/1M.
- Call A: 5 × $0.14/1M + 2688 × $0.014/1M + 831 × $2.19/1M ≈ **$0.00190**.
- Call B: 5 × $0.14/1M + 2688 × $0.014/1M + 621 × $2.19/1M ≈ **$0.00143**.
- Total Step 10 smoke ≈ **$0.0033 真 billed**.
- Cumulative Phase 2 真: $0.079 (Session 41 close) + $0.0033 ≈ **$0.082** vs D-090 α-silent $5 cap = **60× headroom**.
