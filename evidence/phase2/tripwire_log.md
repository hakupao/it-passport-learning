# Phase 2 §2.5 cascade event log

> Per **D-091 §2.5** cascade pattern (5 triggers: α/β/γ/δ/ε) + **D-094** §2.5.
>
> Any trigger fire → append a row here, then either:
>   - Lock / amend a sub-ADR (D-094 first instance; future ones may amend D-094 in-place per D-080 v1.1 §8 pattern), or
>   - Note + resolve in-line if mechanical (e.g. drift correction within noise)
>
> File created **2026-05-19 Session 35 Turn 2** alongside D-094 lock.

---

## Event log

| Date | Trigger | Data points | Resolution |
|---|---|---|---|
| 2026-05-19 | **γ** (Step 数 / wall > 30% drift, 3 consecutive Module A data point) | Step 1 actual 25 min vs 1 day → −98% / Step 2 actual 30 min vs 2 day → −98% / Step 3 actual ~30 min vs 1.5 day → −98% | **D-094 LOCKED 2026-05-19** — Module A wall actuals recorded inline (`actual <N> min (vs estimate <M> day → −X%)`); Module B-D wall estimate held until Step 5 mid-implementation retro; D-091 §2.5(γ) 30% threshold unchanged (worked as designed). See `docs/decisions/D-094-tripwire-wall-amendment.md` §2 + §3 + §4. |
| 2026-05-20 | **β + γ + 设计 factual error**, Session 37 Step 5 | β: N=3 cache hit 99.98%×2 (no fire) / γ: Step 5 wall ~165 min vs 1.5 day → −86% (5th consecutive Module A+B under-estimate by 85%+) / 设计 factual error: assembleWholeBook full-pages payload would exceed DeepSeek 64K ctx (real 92,814 tok vs assumed 64K limit; empirically deployed model handles 93K so V3.2 128K) caught pre-deploy via local measurement | **β**: D-088 §2.3 stable-prefix design ratified at lean wholebook scale (N=4 cumulative across Steps 4+5). **γ**: D-094 §2.4 mid-implementation retro decision input collected — **path = PLAN.md Module B-D wall column inline `actual <N> min` amend (Step 4: 140 min / Step 5: 165 min) rather than full B-D re-estimate** (per D-094 §2.1 amendment pattern); D-091 §2.5(γ) 30% threshold unchanged, mechanism worked as designed. **设计**: **D-098 LOCKED 2026-05-20** — whole-book lean payload (chapters + glossary, no pages); amend D-085 + D-089 §2.3. See `docs/decisions/D-098-whole-book-lean-payload.md`. **chars/N heuristic content-dependency** surfaced (Step 4 chars/4 over by 37% vs Step 5 chars/4 under by 21% — same heuristic, different content shapes); refresh deferred to Step 6 entry per `evidence/phase2/step_05_chat/cache_audit_2026-05-20.md` §4.5. |
| 2026-05-20 | **β + γ + observability finding**, Session 38 Step 6 | β: N=3 quiz cache hit — call #1 cold creation (2693 miss / 0 hit) / call #2 same question_id (2688 hit / 5 miss = **99.81% hit**) / call #3 different question_id (2271 miss / 0 hit creation) — design-consistent across scope sizes (Step 5 lean ~93K vs Step 6 question ~2.7K, both ~99.8%+ within-prefix hit); γ: Step 6 wall ~135 min vs 1 day → **−84%** (6th consecutive Module A+B under-estimate by 84%+); observability: deepseek-reasoner call #3 emitted 0 delta frames (Hypothesis A: R1 produced reasoning-only output, `text` chunks empty + stripped by `chat.ts:115 if (chunk)` filter) — UX gap but data still captured via usage frame | **β**: cache discipline ratified at second scope size (D-088 §2.3 invariant cross-scope). **γ**: D-094 §2.4 6th data point → PLAN.md Step 6 wall column inline `actual ~135 min` amend pattern continues (no full re-estimate); 30% threshold mechanism still healthy. **D-098 §2.2 v1.1 in-place amend** done same turn (predicted ~58-60K, actual 92,814 = +55% off; note appended to ADR per D-080 v1.1 §8 pattern). **assembleScope.ts chars/4 → chars/3 calibration** applied (in-source comment refresh, no D-NNN). **observability**: empty-delta on reasoner deferred to Step 7+ as either (a) add reasoningStream consumption, (b) emit warning frame on empty tail, (c) provider-role retune; NOT a fire — documented in `evidence/phase2/step_06_quiz/cache_audit_2026-05-20.md` §3.2. |
| 2026-05-20 | **β + γ + Module B 收官**, Session 39 Step 7 | β: N=3 hover cache hit at smallest scope — call #1 `アルゴリズム` cold creation (400 miss / 0 hit) / call #2 same `アルゴリズム` (384 hit / 16 miss = **96.0% hit** at 400-tok scale) / call #3 different `データベース` (391 miss / 0 hit cross-surface creation) — **N=5 cumulative β data ratify D-088 §2.3 stable-prefix invariant across 232× scope range (400 → 93K tok)**; γ: Step 7 wall ~90 min vs 1 day → **−81%** (7th consecutive Module A+B under-estimate by 80%+; Module B average drift −84% across 4 data points, converging from [−85,−86] in Steps 4-5 to [−84,−81] in Steps 6-7 = "implementation cruise" pattern); R1 empty-delta carry-over: sidestepped by Q2=a `deepseek-chat` choice (deferred to design 4Q at next reasoner usage) | **β**: D-088 §2.3 stable-prefix invariant ratified at smallest scope (96.0% ≫ 50% threshold; finer-grained relative miss at small scale is intuitive); D-091 §2.5(β) tripwire **no fire**. **γ**: D-094 §2.4 7th data point + Module B 收官 path — PLAN.md Step 7 wall column inline `actual ~90 min` amend continues; **Module C+D full re-estimate decision: NO this turn**, deferred to Step 9 mid-implementation retro (rationale: Module B drift is API-wiring-specific velocity multiplier; UI work is structurally different and needs its own first data point per `evidence/phase2/step_07_glossary/cache_audit_2026-05-20.md` §6.1); 30% threshold mechanism still healthy. **chars/N decision (Q3=a bundled)**: KEEP universal chars/3 (no per-scope split); `assembleScope.ts` header comment updated to N=4 data table (in-source amend pattern, no D-NNN). **Module B 收官**: 4/4 ✅ — `/api/{hello-ai,chat,quiz/explain,glossary/hover}` all LIVE on prod canonical; Phase 2 真 billed $0.0515 cumulative vs $5 cap (97× headroom); 0 Rule B archive this Step. |
| 2026-05-20 | **β + γ + δ runtime detector LIVE + Module B 5/5 收官**, Session 40 Step 8 | β: N=5 cumulative smoke (4 endpoints × 1 + 1 cold-trigger) all healthy hit rate 96.0%-99.98% — chat 92800/15 = 99.98% / quiz #3 page_042 2688/5 = 99.81% / quiz #5 page_259 2176/95 = 95.8% (DeepSeek prefix TTL > 5h empirical; Session 38 warmed prefix still cached) / hover 384/16 = 96.0% / hello-ai 57984/9 = 99.98%; **N=6 cumulative β data points** all design-consistent (D-088 §2.3 stable-prefix invariant ratified across scope range 400 → 93K tok over 6 cumulative measurements); γ: Step 8 wall ~85 min vs 1 day → **−82%** (8th consecutive Module A+B under-estimate by 80%+; Module B 5/5 average drift −83.6% across 5 data points; Steps 4-5 bootstrap [-85,-86] / Steps 6-7 clone-adapt [-84,-81] / Step 8 composition [-82] confirms "implementation cruise" sub-pattern segmentation); δ: runtime detector LIVE on prod via `apps/web/src/lib/ai/tripwire.ts` + `recordTripwireEvent` writing to `console.warn '[tripwire]'`; D-088 §2.4 + D-091 §2.5(β) per-call evaluator at `onFinish` in 4 routes; 0 [tripwire] fires across 5 healthy smoke (correct healthy-silent design per Q3=a log-only) | **β**: D-088 §2.3 invariant ratified across 6 N data points; D-091 §2.5(β) tripwire **no fire** runtime + manual review. **γ**: D-094 §2.4 8th data point — PLAN.md Step 8 inline `actual ~85 min` amend pattern continues; Module B 5/5 COMPLETE (all 5 step rows now `actual <N> min`); Module C+D full re-estimate **STILL deferred** to Step 9 mid-implementation retro (UI velocity multiplier still TBD; Module B "implementation cruise" pattern is API-wiring-specific). **δ runtime detector LIVE**: D-088 §2.4 "δ-all-tripwire detector" semantic mapped per `evidence/phase2/step_08_retry/design_notes.md` §3 — β (cache hit rate) is the only runtime-observable trigger; α (model release) / γ (wall drift, observed offline via PLAN.md) / δ-D091 (β user >10/mo) / ε (pricing change) are out-of-band and documented in `tripwire.ts` module comment. **Module B 收官 v2**: 5/5 ✅ — `/api/{hello-ai,chat,quiz/explain,glossary/hover}` + retry/tripwire helpers all LIVE on prod canonical; Phase 2 真 billed ~$0.065 cumulative vs $5 cap (77× headroom); 0 Rule B archive this Step. **Observability findings (non-failures)**: (i) DeepSeek prefix cache TTL > 5h empirical (Session 38 last call still cached; D-088 §2.3's Anthropic 5-min TTL doesn't apply to DeepSeek path; favourable for D-091 §2.1 cost projections); (ii) AI SDK v6 system-message-in-prompts security warning surfaced on each /api call — intentional layout per D-095 §2.3; mitigation `allowSystemInMessages: true` deferred as cosmetic; (iii) R1 empty-delta NOT reproduced this run (2 reasoner calls both emitted non-empty deltas 459 + 985 tok output) — hypothesis: R1 reasoning-only output is non-deterministic; defensive warning-frame fix still valid as Module C/D backlog. |
| 2026-05-20 | **β + γ + Module C 2/4 + cross-session cache TTL > 4 days**, Session 42 Step 10 | β: N=2 quiz UI calls on `page_042_entity_0` (same question as Step 6 Session 38) — call A (evaluate_script test fetch for absolute-URL fix) 2688 hit / 5 miss = **99.81% hit** / call B (UI click smoke) 2688 hit / 5 miss = **99.81% hit**; **N=10 cumulative β data ratify D-088 §2.3 stable-prefix invariant ACROSS CROSS-SESSION TIME WINDOWS** — Session 38 measured 99.81% on the same question 4 days earlier; Session 42 measures 99.81% identically ⇒ DeepSeek prefix TTL > 4 days on prod (ratchets Session 41 "> 5h" finding by ~20×); γ: Step 10 wall ~145 min vs 1 day → **−85%** (10th consecutive Module A+B+C under-estimate by 80%+; "implementation cruise" sub-pattern within Module C extends from Step 9 −85% bootstrap to Step 10 −85% second-data-point with 1 in-step diversion [document.baseURI credentials pollution]) | **β**: D-088 §2.3 invariant ratified across cross-session time; D-091 §2.5(β) tripwire **no fire** (99.81% ≫ 50% threshold). **γ**: D-094 §2.4 10th data point + Module C 2/4 — PLAN.md Step 10 wall column inline `actual ~165 min` amend pattern continues; Module C+D full re-estimate **STILL deferred** to Step 12 close per N=4 plan (Steps 10-11 quiz + popover are visually similar; Step 12 layout+i18n is structurally distinct). **Cross-session TTL finding**: DeepSeek prefix cache TTL > 4 days on prod (Session 38 → Session 42 on `page_042_entity_0`); this is MUCH longer than Anthropic's 5-min ephemeral cache and very favourable for D-091 §2.1 cost projections. **R1 empty-delta NOT reproduced** this run (Call A 831 tok output / Call B 621 tok output) — continues Session 40 "non-deterministic" hypothesis. **0 [tripwire] fires** in vercel logs across both healthy calls. Phase 2 真 billed ~$0.082 cumulative vs $5 cap (**60× headroom**). **Rule B 归档 1** (in-step diversion, fixed same wall): `failures/step_10_attempt_1_document_baseuri_credentials_pollution.md` — `history.replaceState` updates `window.location.href` but NOT `document.baseURI`; Chrome's `Request` ctor resolves relatives against baseURI, so credentials-in-URL load → relative `fetch("/api/quiz/explain")` fails even after the Session 41 carry-over strip. Fix = `quizSseTransport.resolveEndpoint()` constructs absolute URL via `window.location.origin` (which IS strip-respecting per URL spec). **Observability findings (non-failures)**: (i) cross-session prefix cache TTL > 4 days re-confirmed (favourable for D-091 §2.1); (ii) AI SDK system-message-in-prompts warning surfaced again (intentional D-095 §2.3 layout); (iii) UI usage hint displays cache-hit ratio via `Math.round` ⇒ 99.81% → "100%" visually — acceptable cosmetic rounding, internal precise figure preserved in vercel logs. |
| 2026-05-20 | **β + γ + Module C entry: first UI data point**, Session 41 Step 9 | β: N=2 UI smoke calls — call #1 (zh-CN "DNS是什么", turnCount=1) 92800 hit / 8 miss = **99.99% hit** / call #2 (ja "それは何の階層で動いていますか？" multi-turn turnCount=3) 92800 hit / 114 miss = **99.88% hit**; **N=8 cumulative β data ratify D-088 §2.3 stable-prefix invariant under multi-turn conversation** (conversation suffix grows linearly while corpus+SYSTEM prefix stays byte-identical — exactly as designed); DeepSeek prefix TTL > 5h re-confirmed (Session 40 last call same day still cached); γ: Step 9 wall ~110 min vs 1.5 day → **−85%** (9th consecutive Module A+B+C under-estimate by 80%+; "implementation cruise" sub-pattern extends to UI work despite 2 mid-step diversions [react peer dep + Chrome fetch creds restriction]) | **β**: D-088 §2.3 invariant ratified under multi-turn; D-091 §2.5(β) tripwire **no fire** (99.88%-99.99% ≫ 50% threshold). **γ**: D-094 §2.4 9th data point + Module C entry — PLAN.md Step 9 wall column inline `actual ~110 min` amend pattern continues; Module C+D full re-estimate **STILL deferred** to Step 12 close (N=4 UI data points needed; Steps 10-11 visual QA + popover work and Step 12 layout+i18n are structurally distinct sub-shapes within Module C). **Module C entry first UI data point captured** via `<Chat />` mount on prod canonical with 2 真 LLM calls + Resume pin-last + clear button + localStorage persistence verified e2e on `web-mu-sandy-78.vercel.app/chat`. **0 [tripwire] fires** across 2 healthy UI smoke + zero error paths exercised. Phase 2 真 billed ~$0.079 cumulative vs $5 cap (**63× headroom**). **Rule B 归档 2** (in-step diversions, both fixed same wall): `failures/step_09_attempt_1_react_peer_dep_eresolve.md` (Vercel npm strict-mode rejected react@19.1.0 vs `@ai-sdk/react@3.0.187` peer tilde gap; fix = pnpm bump React 19.1.0 → 19.2.6) + `failures/step_09_attempt_2_fetch_credentials_in_url.md` (Chrome refuses fetch from URL with embedded Basic Auth creds; fix = `<Chat />` defensive `history.replaceState` mount strip — does not weaken D-097 because browser HTTP auth cache holds creds by then). **Observability findings (non-failures)**: (i) DeepSeek prefix cache TTL > 5h re-confirmed by 4-hour-gap warm hit (D-088 §2.3 Anthropic 5-min TTL is per-provider, NOT cross-provider); (ii) AI SDK system-message-in-prompts warning surfaced again (intentional D-095 §2.3 layout; cosmetic mitigation `allowSystemInMessages: true` still deferred); (iii) clear-then-empty-write churn (low severity; `handleClear` writes empty envelope back via save-effect after removeItem; flag for Step 12 cleanup). |

---

## Append rules

1. Date format: ISO `YYYY-MM-DD`.
2. Trigger column: one of `α` / `β` / `γ` / `δ` / `ε` per D-091 §2.5 plus a short hint.
3. Data points: the evidence that fired the trigger (file / commit / step number).
4. Resolution: ADR id + one-line summary; link to evidence dir.

Sub-ADR amend in-place is preferred over supersede chain for minor numerical drift — per D-080 v1.1 §8 pattern recurring in this project.
| 2026-05-20 | **β + γ + Module C 3/4 + cross-session cache TTL > 5 days**, Session 43 Step 11 | β: N=2 hover UI calls on prod — Call A `アルゴリズム` (same surface as Step 7 Session 39 1st smoke, 5 days ago) 384 hit / 16 miss = **96% hit** identical to baseline / Call B `マルチコアプロセッサ` (fresh surface, never previously hit) 0 hit / 421 miss = **0% hit cold creation**; **N=12 cumulative β data** ratify D-088 §2.3 stable-prefix invariant — cross-session TTL ratchet from Session 42's "> 4 days" to **"> 5 days" on prod** (アルゴリズム warm-hit identical to baseline 5 days later); per-surface creation event ratified again on fresh マルチコアプロセッサ at 0%; γ: Step 11 wall ~100 min vs 0.5 day (240 min) → **−58%** (11th consecutive Module A+B+C under-estimate; first sub-pattern departure from −80%+ "implementation cruise" band — drift band widened by Rule B in-step diversion [kana_helper shape mismatch, ~10 min wall consumed]) | **β**: D-088 §2.3 invariant ratified across cross-session 5-day window; D-091 §2.5(β) tripwire **no fire** (96% ≫ 50% threshold; 421 cold-creation below the 1000-tok activation threshold for cache_low_hit). **γ**: D-094 §2.4 11th data point + Module C 3/4 — PLAN.md Step 11 wall column inline `actual ~100 min` amend pattern continues; Module C+D full re-estimate **STILL deferred** to Step 12 close per the N=4 UI data points plan (Step 12 = 3-tab layout + i18n, structurally distinct). **Cross-session TTL finding**: DeepSeek prefix cache TTL **> 5 days** on prod (Session 39 → Session 43 on `アルゴリズム`, 96% hit identical); ratchets Session 42's "> 4 days" by ~24h. **0 [tripwire] fires** in vercel logs across both healthy calls. Phase 2 真 billed ~$0.0823 cumulative vs $5 cap (**~60× headroom**). **Rule B 归档 1** (in-step diversion, fixed same wall): `failures/step_11_attempt_1_kana_helper_object_shape_mismatch.md` — TS `GlossaryEntry.kana_helper: string|null` did not match actual runtime shape `{surface, reading, zh_concept, auto_backfill} | null` (308/908 entries are object, 600/908 null); Step 11 was first TS consumer reading the field (Steps 4-10 only stringified it for the LLM via assembleScope), so the drift went undetected until <TermPopover /> attempted to render the object as a React child → React error #31. Fix = (1) introduce `KanaHelper` interface, (2) `buildGlossarySummary` reads `entry.kana_helper?.reading ?? null`, (3) test fixture updated to real-shape object. **Observability findings (non-failures)**: (i) cross-session prefix cache TTL > 5 days re-confirmed (favourable for D-091 §2.1); (ii) AI SDK system-message-in-prompts warning surfaced again (intentional D-095 §2.3 layout); (iii) HOVER_USER_PROMPT 120-tok soft cap respected on both calls (62 + 98 tok output); (iv) Call B mild SYSTEM_INSTRUCTION deviation — model echoed katakana surface as kana_helper reading rather than romaji `maruchi koa purosessa`; cosmetic UX miss for screen-reader users, deferred to Step 14 polish. |
