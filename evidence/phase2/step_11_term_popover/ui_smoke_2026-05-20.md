# Phase 2 · Step 11 · UI smoke evidence (Session 43 · 2026-05-20)

Chrome DevTools MCP driven UI smoke on prod canonical
`https://web-mu-sandy-78.vercel.app/glossary` (deploy
`dpl_GyyYvBNrFpaYb4c26zVPqrtie24E`, target=production).

## Pre-smoke firewall + page-load verification

```
$ curl -s -I https://web-mu-sandy-78.vercel.app/glossary
HTTP/2 401
www-authenticate: Basic realm="IT Passport Learning firewall"

$ curl -s -I -X POST -H "Content-Type: application/json" https://web-mu-sandy-78.vercel.app/api/glossary/hover
HTTP/2 401
www-authenticate: Basic realm="IT Passport Learning firewall"
```

D-097 firewall gating verified on both routes.

Chrome DevTools MCP page load via credentialed URL
`https://claude:<pass>@web-mu-sandy-78.vercel.app/glossary`:
- Chrome's HTTP auth cache populated on the first 401-redirect cycle.
- `<GlossaryList />` mount effect calls `history.replaceState` to strip the
  `@` credential prefix from `window.location.href` (Session 41 carry-over).
- 908 glossary cards rendered in 50音 order via `Intl.Collator('ja')`.
- All cards display `surface_jp` + `読み: <kana_helper.reading>` (where
  populated; ~308/908 entries) + `surface_zh · surface_en` + first-page chip.

Screenshot 1: `screenshot_1_glossary_list.png` — top-of-list viewport showing
the first ~12 cards in 50音 order ("1-Click で今すぐ買う", "3Dプリンター", …).

## Call A — `アルゴリズム` (warm; cross-session TTL ratchet)

Click `uid=16_1493` (アルゴリズム card "解説を見る" button).

Sequence observed:
1. URL updated to `?term=%E3%82%A2%E3%83%AB%E3%82%B4%E3%83%AA%E3%82%BA%E3%83%A0`
   (encodeURIComponent of アルゴリズム) — verified in browser address bar.
2. `<TermPopover />` mounts; phase = `loading`; skeleton 3 rows + indeterminate
   progress bar visible.
3. Screenshot 2 captured during loading: `screenshot_2_modal_busy.png`.
4. Wait_for landed on `tokens in=` (UI usage hint) within ~3s.

Final modal state (snapshot uid=17_*):
```
dialog "アルゴリズム" modal
  heading "アルゴリズム" level="2"
  StaticText "読み："
  StaticText "arugorizumu"
  StaticText "算法"
  StaticText " · "
  StaticText "Algorithm"
  StaticText "第 72 ページ · 出現 3"
  button "閉じる / 关闭"
  generic live="polite" relevant="additions text"
    StaticText "**アルゴリズム (arugorizumu)**：問題を解決するための手順や計算方法のこと。
**中文**：算法是解决问题的明确步骤或计算方法。
**English**：An algorithm is a finite sequence of well-defined steps to solve a problem or perform a computation."
    StaticText "TOKENS IN=400 (CACHE HIT 96%) · OUT=62"
  button "閉じる / 关闭"
```

Screenshot 3: `screenshot_3_call_a_done_algoritmu.png` — modal with full
output + usage hint.

Output coherence check:
- ✅ Japanese line uses the surface + `(reading)` form as `HOVER_SYSTEM_INSTRUCTION`
  prescribes.
- ✅ Definition matches `_fixtures/v1.0.3/glossary.json` entry — "問題解決の
  ための手順" (problem-solving procedure) is the canonical IT-Passport
  textbook gloss.
- ✅ All 3 sections present (JP / 中文 / English).
- ✅ Output total 62 tokens — comfortably under the 120-tok soft cap.

Then click `uid=17_12` ("閉じる / 关闭" footer button).
- URL `?term=` cleared via `router.replace` (verified — back to `/glossary`
  with no query string).
- `<TermPopover />` unmounts (returns `null` when summary is null).

## Call B — `マルチコアプロセッサ` (cold; cross-surface creation event)

Click `uid=16_3875` (マルチコアプロセッサ card "解説を見る" button).

Final modal state (snapshot uid=18_*):
```
dialog "マルチコアプロセッサ" modal
  heading "マルチコアプロセッサ" level="2"
  StaticText "読み："
  StaticText "maruchi koa purosessa"
  StaticText "多核处理器"
  StaticText " · "
  StaticText "Multi-Core Processor"
  StaticText "第 398 ページ · 出現 2"
  button "閉じる / 关闭"
  generic live="polite" relevant="additions text"
    StaticText "**マルチコアプロセッサ**（読み: マルチコアプロセッサ）: 1つのCPU内に複数のコア（処理核心）を搭載したプロセッサ。
**中文**: 多核处理器指在一个CPU芯片中集成两个或更多独立处理核心，以提高并行处理能力。
**English**: A multi-core processor integrates two or more independent processing cores on a single CPU chip to enhance parallel performance."
    StaticText "TOKENS IN=421 (CACHE HIT 0%) · OUT=98"
  button "閉じる / 关闭"
```

Screenshot 4: `screenshot_4_call_b_done_multicore.png` — modal with full
output + usage hint.

Output coherence check:
- ✅ Japanese line includes the surface + reading helper form.
- ✅ Definition matches canonical multi-core processor gloss; English mention
  of "single CPU chip" + "parallel performance" aligns with the source corpus.
- ✅ 3-section structure (JP / 中文 / English).
- ✅ Output 98 tokens; well under 120-tok soft cap.

Cache behaviour:
- 421 input tok (slightly higher than Call A's 400 — マルチコアプロセッサ has a
  longer surface and the corpus entry has more aliases).
- **0% cache hit** — this surface has never been hit before in Phase 2 (Step 7
  Session 39 smokes hit only アルゴリズム × 2 + データベース × 1), so DeepSeek's
  prefix cache had no prior entry. **Per-surface creation event ratified
  per D-088 §2.3.**

## Vercel access log corroboration

```
$ vercel logs https://web-mu-sandy-78.vercel.app --since=15m
18:04:01.84  warning  λ POST /api/glossary/hover   200   AI SDK Warning: System message…
18:02:58.34  warning  ε POST /api/glossary/hover   200   AI SDK Warning: System message…
... + GET /glossary 200 entries
```

2× POST `/api/glossary/hover` both 200 ✅. The "AI SDK Warning: System
message…" prefix is the AI SDK v6 informational warning that surfaces per
call due to the D-095 §2.3 stable-prefix layout (intentional; suppression
deferred per cosmetic backlog).

**0 `[tripwire]` lines** in the same log window — the β cache-low-hit detector
correctly stays silent on both the 96% warm-hit and the 0% cold-creation
because neither exceeds the 1000-tok input threshold required to trigger
`cache_low_hit` evaluation.

## D-085 §2.4 contract verification

| Behaviour | Verified |
|---|---|
| `<GlossaryList />` 908 cards in 50音 order | ✅ via snapshot ([surface index 1486] アルゴリズム at row ~150 of 908) |
| Click → `router.push('?term=...')` | ✅ URL updated to `?term=%E3%82%A2%E3%83%AB%E3%82%B4%E3%83%AA%E3%82%BA%E3%83%A0` |
| Modal phase machine `idle→loading→streaming→done` | ✅ skeleton observed before completion |
| Close → `router.replace` clears qs | ✅ back to `/glossary` post-close |
| `?term=` reload-survives | (not exercised this smoke; Step 9 + 10 patterns ratify reload-survives) |
| Output 3-section JP/中文/English | ✅ both calls |
| Locked error surface "AI 暂时不可用，请稍后重试。" | (not exercised — both calls succeeded; unit test `streamGlossaryHover dispatches server error frames` covers this branch) |
| `role="dialog" aria-modal="true"` a11y | ✅ snapshot shows `dialog ... modal` |

## Summary

| Item | Value |
|---|---|
| 真 LLM calls | 2 |
| Cumulative wall (both calls) | ≈ 6s of streaming |
| Total cost | ≈ $0.0003 真 billed |
| Cache TTL re-confirmed | > 5 days on prod for warm `アルゴリズム` |
| Per-surface creation event | ratified for fresh `マルチコアプロセッサ` |
| 0 [tripwire] fires under healthy | ✅ |
| Module C 3/4 data point | captured |
