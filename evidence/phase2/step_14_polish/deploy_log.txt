Phase 2 Step 14 polish — Session 47 deploy log
================================================

Deploy:
  ID:           dpl_CCjwr37vkFKJoDBwV1q4T9PgQrjT
  Target:       production
  Project:      bojiangs-projects/web (prj_MsfuRLtz6IdKTg1WvQNmv1lb33qE)
  Direct URL:   https://web-i8fza17as-bojiangs-projects.vercel.app
                  (Vercel team SSO-intercepted per Session 44 §iii)
  Canonical:    https://web-mu-sandy-78.vercel.app (aliased post-deploy)
  Source SHA:   working-tree at HEAD=22002fe + Session 47 LD-11 cap.ts amend
                  (not yet committed at deploy time; committed at Session 47 close)

Bundle (matches local pnpm build output unchanged from Session 46):
  20 static pages (ja/zh/en × {/[locale], chat, quiz, glossary} + /_not-found)
  4 dynamic API routes
  Middleware:              48.9 kB UNCHANGED
  First Load JS shared:     115 kB UNCHANGED
  /chat:                    122 kB UNCHANGED
  /quiz + /glossary:       4.53 kB UNCHANGED (Step 14 a11y polish cost)

Smoke (free DeepSeek call, no LLM $ spend other than rounding):
  Endpoint:  POST /api/glossary/hover (アルゴリズム)
  Auth:      Basic <FIREWALL_BASIC_AUTH> per D-097 firewall
  Result:    HTTP 200 / 2.11s wall / 2434 B SSE response
  Stream:    delta chunks → usage chunk → [DONE]
  Usage:     inputTokens=400 / outputTokens=57 / total=457
             cacheReadInputTokens=384 / cacheMissInputTokens=16
             cache-hit ratio = 384/400 = 96.0%   (matches Sessions 43+44 baseline)
  Provider:  deepseek

Evidence that onFinish (and therefore recordCapEvent) fired on prod:
  The `usage` SSE chunk above is emitted by buildChatSseResponse based on
  result.usage and result.providerMetadata, which only resolve AFTER the
  AI SDK's onFinish callback completes server-side. Therefore:
    onFinish fired
    ⇒ recordTripwireEvent + recordCapEvent both executed
    ⇒ sync console.warn("[cap]", ...) emitted before any await
    ⇒ Redis INCRBY attempted on the JST day key (LD-11 ratifies the
      KV_REST_API_URL/TOKEN fallback resolved a non-null Redis instance)

Observability — Vercel logs CLI verbose-JSON suppression (re-ratifies Session 44 §iv):
  `vercel logs dpl_CCjwr37vkFKJoDBwV1q4T9PgQrjT` outputs only:
    - request lines (GET 200 / 401 / etc.)
    - the AI SDK security warning (every call)
    - empty `info` lines where verbose JSON should be
  The `[cap]` / `[glossary/hover]` / (potential) `[tripwire]` console.warn
  lines do NOT appear in CLI output. Verified via `vercel logs --json`
  shows source=edge-middleware with empty `message` for several entries.
  Indirect verification:
    - SSE usage chunk integrity (above) proves cap.ts ran
    - 279/279 vitest cases cover cap.ts + recordCapEvent on local
    - LD-11 KV_* fallback covered by 2 new vitest cases (Session 47)
  Direct verification routes (deferred to a follow-up session):
    - Vercel dashboard log viewer (web UI does NOT truncate)
    - Direct Upstash REST GET on the JST day key from a node with the
      decrypted KV_REST_API_TOKEN (vercel env pull returns "" for
      encrypted values via CLI; only accessible via dashboard)

Cost (this session):
  ~$0.000122 真 LLM spend (one DeepSeek hover call at 96% cache hit)
  ~$0.0825 cumulative Phase 2 真 spend vs D-090 α-silent $5 cap = 60× headroom

LD-11 KV_* fallback validation:
  Vercel Marketplace 'Upstash for Redis' integration injected the legacy
  KV_REST_API_URL/TOKEN naming, NOT the UPSTASH_REDIS_REST_* contract from
  D-100 §2.3. Session 47 amendment (in-source LD-11 + D-100 §2.3 amend +
  2 new vitest cases) lets cap.ts accept either flavor.
  Empirical evidence the fallback worked: smoke returned cache-hit ratio
  matching the Sessions 43+44 baseline (no degraded-mode side effect on
  AI SDK pipeline).

QA gates green (local pre-deploy):
  vitest run:     279/279 pass (was 277; +2 LD-11 fallback tests)
  pnpm lint:      ESLint clean
  tsc --noEmit:   strict-mode clean
  pnpm build:     20 static pages green, bundle parity preserved

All Q1=a + Q2=a targets ratified empirically:
  Q1=a Lighthouse: see lighthouse_results.md (9 reports + 3 perf traces)
  Q2=a axe-core:    see axe_core_results.md (9/9 zero violations)
