# Failure: `stage5_translate` / `stage5-2026-05-07-004`

> Per 规则 B + D-032: attempt 004 swapped opus → **sonnet** at chunk=1
> on page_038 to test whether the failure is opus-specific. Result: it
> got *worse* (sonnet failed on **2** definitions vs opus's 1). Confirms
> the failure is **not model-specific** — it is a system-prompt /
> content-interaction issue triggered by definitions that prominently
> reference glossary-locked surfaces. Attempt 005 will modify the
> system prompt to add an explicit "always translate the wrapper" clause.

## 元数据 / Metadata

| 字段 | 值 |
|---|---|
| `attempt_id` | `stage5-2026-05-07-004` |
| `stage` | `stage5_translate` |
| `timestamp` | 2026-05-07T11:58:00+09:00 |
| `triggered_by` | `failures/stage5_translate/stage5-2026-05-07-003.md` |
| `git_sha` | `03170ef` + Stage 5 sub-batch refactor |
| `model_or_tool` | `claude-sonnet-4-6` via `claude-agent-sdk` 0.1.74 |
| `cost_jpy_or_cny` | $0 billed; ~$0.10 shadow (5 calls × ~$0.02 sonnet) |
| `elapsed_minutes` | ~1 |

## 输入 / Input

- `--tier sonnet` (changed from opus)
- `--max-items-per-call 1` (unchanged)
- Only page_038 re-processed (page_031 was already clean from attempt 003)

## 产物 / Product

| Page | Untrans @ 003 (opus) | Untrans @ 004 (sonnet) | Δ |
|---|---|---|---|
| page_038 | 1 | **2** | +1 (regression) |

Sonnet stuck on two definitions, both wrappers for a glossary-locked
near-synonym pair `職能別組織` ↔ `機能別組織`:

```text
ent[0] definition: 業務を専門的な機能に分けて、各機能を単位として構成する組織…「機能別組織」と呼ぶ場合もある。
ent[1] definition: 事業部を単位として構成する組織…職能別組織の1つ上に「事業部」という階層を増やして構成する。
```

Both definitions:
- Are the wrapper definition of a Term whose `surface.jp` is in the glossary
- Mention the glossary-key surface (or its near-synonym in 「」 quotes) inside the body

Sonnet was MORE susceptible than opus to the glossary-substring
confusion. We can't simply "switch model" away from this.

## 技术判定 / Technical Verdict

**FAIL** (2 leaves). Confirms the failure is not opus-specific.

## 业务判定 / Business Verdict

**FAIL** (cannot ship).

## 失败模式分类 / Failure Mode

- [x] `prompt-issue` — confirmed. Model-agnostic at chunk=1 ⇒ system
  prompt is the controllable variable that hasn't been tested.
- [ ] `model-bug` (downgraded — sonnet behavior is consistent with
  the prompt-issue hypothesis, not orthogonal evidence)

## 下一 attempt 输入

- **改 prompt**: append an explicit clause to `TRANSLATE_SYSTEM_PROMPT_TEMPLATE`:
  > "ALWAYS return a complete trilingual rendering for every input.
  > The glossary lock tells you HOW to render locked surfaces within
  > your output, NOT whether to translate. If an input contains a
  > locked surface as a substring, render the rest of the string in
  > zh+en normally and use the locked translation for the substring
  > only. Never return an empty zh or en just because a glossary
  > surface appears in the input."
- Re-run sonnet chunk=1 on page_038 (faster + cheaper than opus).
- See attempt 005 (`stage5-2026-05-07-005.md`).

## 链接

- 上一次: `stage5-2026-05-07-003.md`
- partial: `failures/stage5_translate/attempt_004/page_038.partial.json`
- evidence: `evidence/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/step_05_audit.md` Stage B section

## 签字

| 字段 | 值 |
|---|---|
| 归档时间 | 2026-05-07T11:58+09:00 |
| 归档人 | Claude main session (Opus 4.7 1M ctx) |
| 是否触发新 D | likely yes after success — will lock a D for "wrapper-clause requirement in Stage 5 prompt" if attempt 005 confirms the fix |
