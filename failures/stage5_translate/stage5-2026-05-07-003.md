# Failure: `stage5_translate` / `stage5-2026-05-07-003`

> Per 规则 B + D-032: archive of attempt 003 (opus, chunk=1) on
> page_031 + page_038. **Page_031 fully recovered.** Page_038 ent[0]
> `definition` (the wrapper definition for the glossary-locked term
> `職能別組織`) remained untranslated even at chunk=1 (one item per
> LLM call, no truncation tail possible). This rules out the batching
> hypothesis as the *sole* cause and points at content-prompt
> interaction.

## 元数据 / Metadata

| 字段 | 值 |
|---|---|
| `attempt_id` | `stage5-2026-05-07-003` |
| `stage` | `stage5_translate` |
| `timestamp` | 2026-05-07T11:54:00+09:00 |
| `triggered_by` | `failures/stage5_translate/stage5-2026-05-07-002.md` |
| `git_sha` | `03170ef` + Stage 5 sub-batch refactor |
| `model_or_tool` | `claude-opus-4-7` via `claude-agent-sdk` 0.1.74 |
| `cost_jpy_or_cny` | $0 billed; ~$2.9 shadow (11 calls × $0.26) |
| `elapsed_minutes` | ~3 |

## 输入 / Input

- Same as attempts 001/002 except `max_items_per_call` 4 → **1**.

## 产物 / Product

| Page | Untrans @ 002 | Untrans @ 003 | Δ |
|---|---|---|---|
| page_031 | 4 | **0** | −4 (clean!) |
| page_038 | 4 | 1 | −3 |
| **Total** | **8** | **1** | **−7** |

The single remaining failure on page_038:

```json
{
  "id": "itpassport_r6::term::p038::0",
  "type": "term",
  "surface": {"jp": "職能別組織", "zh": "职能型组织", "en": "Functional Organization"},  ← glossary hit OK
  "definition": {
    "jp": "業務を専門的な機能に分けて、各機能を単位として構成する組織。社員は配属された部門の機能だけを専門的に行う。「機能別組織」と呼ぶ場合もある。",
    "zh": "<UNTRANSLATED>",
    "en": "<UNTRANSLATED>"
  }
}
```

Failed even at chunk=1. The jp definition (a) **explicitly contains the
near-synonym `機能別組織`** in 「」 quotes (different first kanji vs
the glossary key `職能別組織`, same English meaning) and (b) is the
**wrapper definition** for the glossary-locked surface above. Both
features are absent from the 391 leaves that succeeded.

## 技术判定 / Technical Verdict

**FAIL** (1 leaf). But the chunk-size hypothesis is now **falsified** —
chunk=1 leaves no batch tail to drop, so this is not a truncation
issue. The model is refusing / failing on this specific (input, system
prompt) pair.

## 业务判定 / Business Verdict

**FAIL** (still cannot ship to Stage 7 export per spec).

## 失败模式分类 / Failure Mode

- [x] `prompt-issue` — the system prompt's glossary clause says
  "render locked surfaces verbatim" but is silent on *whether*
  the wrapping string must still be translated. Hypothesis: the
  model interprets the constraint as "if a glossary surface appears,
  output the locked surface and only the locked surface".
- [x] `model-bug` (residual; chunk=1 + opus + this prompt + this
  input → repeatable empty-output).
- [ ] `glossary-mismatch` — the *resolved* surface translation is
  correct; this is about the wrapper, not the lock-table content.

## 下一 attempt 输入

- Try `--tier sonnet` chunk=1 on page_038 only — does a different
  model handle this content cleanly under the same prompt?
- See attempt 004 (`stage5-2026-05-07-004.md`).

## 链接

- 上一次: `stage5-2026-05-07-002.md`
- 下一次: `stage5-2026-05-07-004.md`
- partial: `failures/stage5_translate/attempt_003/page_038.partial.json`
- evidence: `evidence/itpassport_r6/runs/dry_run_2026-05-06T16-58-10/step_05_audit.md` Stage B section

## 签字

| 字段 | 值 |
|---|---|
| 归档时间 | 2026-05-07T11:54+09:00 |
| 归档人 | Claude main session (Opus 4.7 1M ctx) |
| 是否触发新 D | not yet — exhausting model + prompt knobs first |
